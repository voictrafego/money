---
phase: 02-motores-por-arqu-tipo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/core/motores.py
  - config.yaml
  - tests/test_motores.py
autonomous: true
requirements: [ENG-02, ENG-03, ENG-04, ENG-05]
must_haves:
  truths:
    - "rim() com inputs tipo-ITUB4 (VPA~22, ROE~19,3%, Ke~12,5%, retenção~0,53, n=10) devolve intrínseco ~R$28 (faixa R$26–34 — modelo honesto/conservador com fade a zero, D-02, SEM prêmio terminal), materialmente > DDM ao vivo (~R$16, i.e. >= 25)"
    - "O excesso de ROE faz fade até o Ke: o RI do último ano do horizonte é ~0 (valor ancorado no VPA, sem perpetuidade de excesso)"
    - "ke_rim(beta~1,0, cfg) devolve um Ke estrutural (~0,11–0,14) MENOR que o Ke do CAPM ao vivo de banco (~0,156)"
    - "lucro_normalizado valua sobre a média 7–10a (base_normalizada) — uma série com 1 ano 3× não move o intrínseco como moveria o lucro do último ano"
    - "dcf_crescimento (reuso de ddm.ddm_dois_estagios com lucro no lugar de dividendo) devolve número positivo e finito para compounder payout-baixo (não zero/lixo)"
    - "nav_contabil devolve o VPA (lentes.vpa) do ano-base; None quando PL/ações faltam"
    - "Todos os motores nunca levantam (never-raise): input None / divisor ≤ 0 → None"
  artifacts:
    - path: "src/analista/core/motores.py"
      provides: "rim() + ResultadoRIM, ke_rim(), lucro_normalizado(), dcf_crescimento(), nav_contabil() — funções puras config-driven"
      contains: "def rim"
    - path: "config.yaml"
      provides: "bloco novo motores: (rim/ciclica/crescimento) aditivo, anti-rebaseline"
      contains: "motores:"
    - path: "tests/test_motores.py"
      provides: "golden puro por motor (RIM ~R$40, fade→RI≈0, normalizado 7-10a, dcf>0 finito, nav=vpa, ke_rim<ke_live)"
      exports: []
  key_links:
    - from: "src/analista/core/motores.py::rim"
      to: "config.yaml motores.rim (erp_banco, ke_piso, ke_teto, n_fade)"
      via: "ke_rim(beta, cfg) lê os knobs; golden ITUB4 ~R$40 calibra"
      pattern: "cfg\\[.motores.\\]"
    - from: "src/analista/core/motores.py::dcf_crescimento"
      to: "src/analista/core/ddm.py::ddm_dois_estagios"
      via: "import + call (ddm.py INTOCADO)"
      pattern: "ddm\\.ddm_dois_estagios"
    - from: "src/analista/core/motores.py::nav_contabil"
      to: "src/analista/core/lentes.py::vpa"
      via: "import + call"
      pattern: "lentes\\.vpa"
    - from: "src/analista/core/motores.py::lucro_normalizado"
      to: "src/analista/core/ddm.py::valor_gordon + src/analista/core/normalizacao.py::base_normalizada"
      via: "import + call (P/L justo Gordon sobre lucro médio 7-10a)"
      pattern: "(valor_gordon|base_normalizada)"
---

<objective>
Implementar os 4 motores de valuation que faltam como **funções puras config-driven** num novo
`core/motores.py`, espelhando o esqueleto de `core/ddm.py` (dataclass de resultado, `Number =
Optional[float]`, never-raise no topo). Cada motor **compõe** primitivas já testadas do `core/`
(`lentes.vpa`, `ddm.ddm_dois_estagios`, `ddm.valor_gordon`, `normalizacao.base_normalizada`) e
consome sempre os `*_valuation()` canônicos — nunca recalcula método (FIX-04, consistência
cross-modo). Nenhum arquivo existente do `core/` é tocado.

O único elemento genuinamente novo é a **calibração numérica do Ke estrutural do RIM** (D-01): o
RIM precisa de um Ke MENOR (~12,5%) que o CAPM ao vivo de banco (~15–17%) para destravar o ~R$40
do ITUB4 — a troca spot→through-the-cycle já está feita e é insuficiente. `ke_rim()` deriva esse Ke
(rf ciclo + ERP de banco sem prêmio small-cap, com piso/teto), config-driven, calibrado contra o
golden ITUB4.

Purpose: entregar as fórmulas de livro-texto (~20% do esforço) prontas e golden-testadas, para o
Plan 02 apenas as plugar no registry e wirear no funil sem regredir o ITUB4.
Output: `core/motores.py`, bloco `motores:` em `config.yaml`, `tests/test_motores.py` (goldens puros).

Cobre: ENG-02 (RIM), ENG-03 (lucro normalizado), ENG-04 (DCF/multi-estágio), ENG-05 (NAV contábil).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motores-por-arqu-tipo/02-CONTEXT.md
@.planning/phases/02-motores-por-arqu-tipo/02-RESEARCH.md
@.planning/phases/02-motores-por-arqu-tipo/02-PATTERNS.md

<interfaces>
<!-- Contratos já existentes que os motores CONSOMEM (não recalculam). Extraídos do código. -->

De src/analista/core/ddm.py (INTOCADO — reusar por import):
- `Number = Optional[float]`
- `def valor_gordon(dpa1: float, ke: float, g: float) -> Number`  # None se ke-g<=0 ou input None
- `def ddm_dois_estagios(dpa_inicial, g_alto, n, g_estavel, ke, decrescente=False, tributacao=0.0) -> Optional[ResultadoDDM]`
- `@dataclass ResultadoDDM: valor_intrinseco, vp_dividendos, vp_residual, valor_residual_futuro, dividendos_projetados, vp_por_ano, peso_residual`

De src/analista/core/lentes.py (INTOCADO):
- `def vpa(patrimonio_liquido: float, num_acoes: float) -> Number`  # PL/ações via _safe_div (None/zero-safe)

De src/analista/core/normalizacao.py (INTOCADO):
- `def base_normalizada(valores: Sequence, anos_media: int = 3, winsor: float = 0.10) -> Number`  # janela final, mediana p/2-4, winsor p/>=5

De src/analista/core/capm.py (INTOCADO — espelhar a forma em ke_rim):
- `def ke_local(beta_acao: float, rf_local: float, erp_local: float) -> float`  # rf + beta*erp

De config.yaml capm: (NÃO editar; ke_rim LÊ rf_local/erp_local daqui):
- `capm.rf_local` = Selic through-the-cycle (média 10a) resolvida pelos entry points (~0,096 offline default 0,105)
- `capm.erp_local: 0.06` (embute ~1,5% small-cap — por isso o RIM usa um ERP menor)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RIM (ENG-02) + Ke estrutural (D-01) + bloco config motores:</name>
  <files>src/analista/core/motores.py, config.yaml</files>
  <read_first>
    - src/analista/core/ddm.py:1-46 (docstring citando capítulo, `Number`, `@dataclass ResultadoDDM` com `__post_init__`, `valor_gordon` never-raise) — esqueleto a espelhar 1:1
    - src/analista/core/ddm.py:97-115 (loop de projeção+desconto+valor residual — mecânica de PV a replicar no RIM)
    - src/analista/core/capm.py:69-71 (`ke_local` = rf + beta×erp — forma que `ke_rim` espelha)
    - src/analista/core/lentes.py:51-53 (`vpa`)
    - config.yaml:60-85 (bloco `capm:` — NÃO tocar; ke_rim lê `rf_local`/`erp_local` daqui) e :171-203 (bloco `arquetipo:` — padrão do bloco irmão comentado a espelhar)
    - .planning/phases/02-motores-por-arqu-tipo/02-RESEARCH.md:141-172 (Pattern 1 — esqueleto do RIM pronto: fade linear do ROE até Ke, B_t=B_{t-1}·(1+ROE_t·retencao)) e :229-246 (Ke do RIM — opções e recomendação (a)+clamp)
  </read_first>
  <behavior>
    - rim(vpa0=22.0, roe0=0.193, ke=0.125, retencao=0.53, n=10) → ResultadoRIM com valor_intrinseco ~R$28 (faixa R$26–34; a fórmula especificada — fade linear do ROE até Ke, book clean-surplus, SEM prêmio terminal — rende ~R$28,20 nesses inputs, verificado aritmeticamente) e materialmente > DDM ao vivo (~R$16): assert valor_intrinseco >= 25; ri_por_ano[-1] ≈ 0 (fade completo → valor ancorado no VPA, D-02)
    - rim(vpa0=None|0|negativo, ...) ou ke<=0 ou n<=0 → None (never-raise)
    - rim com roe0 == ke → excesso zero em todos os anos → valor_intrinseco ≈ vpa0
    - ke_rim(beta=1.0, cfg) → Ke em [0.11, 0.14] E estritamente < ke do CAPM ao vivo de banco (ke_local(1.0, rf_local=0.105, erp_local=0.06) = 0.165 com o default offline do config; ~0.156 quando o rf ao vivo do BCB é menor — o teste recomputa ke_live dinamicamente, não hardcoda); config-driven (muda com os knobs de motores.rim)
    - ke_rim usa rf through-the-cycle (cfg["capm"]["rf_local"]) + erp_banco (menor que erp_local, sem prêmio small-cap), com clamp piso/teto e teto ≤ ke ao vivo
  </behavior>
  <action>
    **Nota de reconciliação (desvio documentado, aprovado pelo usuário) — o número honesto vence:** o
    "~R$40" do D-01 era um alvo APROXIMADO. O RIM conservador (Ke estrutural ~12,5% + fade do excesso a
    zero em ~7–10a, per D-02, SEM prêmio terminal) rende ~R$28 para o ITUB4 (a fórmula especificada dá
    ~R$28,20). ~R$28 continua coerente com o mercado (ITUB4 ~R$32) e materialmente acima do DDM ao vivo
    (~R$16) — cumprindo a INTENÇÃO real do critério de aceite #1 (destravar o ITUB4 do "evitar", não
    bater um número exato). Não adicionar prêmio terminal para forçar ~R$40 (violaria D-02). Calibrar
    os knobs contra ~R$28, não ~R$40.

    Criar `src/analista/core/motores.py` com o cabeçalho de módulo (docstring citando RIM = VPA + VP
    do excesso de ROE sobre Ke, Cap. do livro), imports `from __future__ import annotations`,
    `from dataclasses import dataclass, field`, `from typing import List, Optional`, `Number =
    Optional[float]`, e `from . import ddm, lentes, capm` + `from . import normalizacao as norm`.

    Definir `@dataclass ResultadoRIM(valor_intrinseco: float, vpa_base: float, vp_residual_income:
    float, ri_por_ano: List[float])` espelhando `ResultadoDDM`.

    Implementar `rim(vpa0, roe0, ke, retencao, n, fade_para=None)` seguindo RESEARCH Pattern 1
    (linhas 146-172): guard never-raise no topo (`if None in (vpa0, roe0, ke, retencao) or n <= 0
    or ke <= 0 or vpa0 <= 0: return None`); `fade_para = ke if None`; loop t=1..n com fade LINEAR do
    ROE de roe0 até fade_para (excesso→0 naturalmente, D-02), book compondo `b_prev *= (1 +
    roe_t*retencao)`, `ri = (roe_t - ke)*b_prev`, `vp += ri/(1+ke)**t`; retornar `ResultadoRIM(vpa0 +
    vp, vpa0, vp, ris)`. Ancorar valor no VPA sem perpetuidade de excesso (RI terminal ≈ 0).

    Implementar `ke_rim(beta, cfg)` espelhando `capm.ke_local` mas com ERP de banco: `rf =
    cfg["capm"]["rf_local"]`; `erp_banco = cfg["motores"]["rim"]["erp_banco"]`; `ke_live = rf +
    beta*cfg["capm"]["erp_local"]`; `ke = rf + beta*erp_banco`; aplicar clamp
    `max(ke_piso, min(ke, ke_teto, ke_live))` com `ke_piso`/`ke_teto` de `cfg["motores"]["rim"]`
    (per D-01: RIM nunca herda o Ke ~17% que comprime o DDM). Never-raise se beta None.

    Adicionar em `config.yaml` um bloco NOVO `motores:` irmão de `arquetipo:` (NÃO editar
    `capm:`/`ddm:`/`arquetipo:` — anti-rebaseline, Pitfall 5), com cabeçalho comentado no estilo do
    bloco `arquetipo:` (:171-177). Chaves: `rim: { erp_banco: 0.045, ke_piso: 0.11, ke_teto: 0.14,
    n_fade: 10 }` (números iniciais ajustáveis sem deploy; o golden ITUB4 do RIM ancora ~R$28 —
    número honesto do modelo conservador, ver nota de reconciliação acima — NÃO ~R$40), comentando que o small-cap premium é impróprio para banco large-cap/líquido
    e que a calibração empírica é BACKTEST-01 (deferida). Deixar as chaves `ciclica:`/`crescimento:`
    para a Task 2 preencher (ou já criar os três sub-blocos aqui — coordenar com Task 2).
  </action>
  <acceptance_criteria>
    - `src/analista/core/motores.py` contém `def rim(` e `def ke_rim(` e `class ResultadoRIM`
    - `config.yaml` contém a chave de topo `motores:` com sub-bloco `rim:` e NENHUMA linha dos blocos `capm:`/`ddm:`/`arquetipo:` alterada (`git diff config.yaml` mostra só adição do bloco novo)
    - `python -c "import yaml,analista"` — config.yaml carrega sem erro de sintaxe
    - `core/ddm.py`, `core/lentes.py`, `core/capm.py` NÃO aparecem em `git diff --name-only` (INTOCADOS)
    - Golden da Task 3 `test_rim_*` e `test_ke_rim_*` passam (ver Task 3)
  </acceptance_criteria>
  <verify>
    <automated>cd "$PWD" && python -c "from analista.core import motores; assert hasattr(motores,'rim') and hasattr(motores,'ke_rim') and hasattr(motores,'ResultadoRIM')"</automated>
  </verify>
  <done>rim(), ke_rim() e ResultadoRIM existem; bloco motores.rim em config.yaml sem tocar blocos existentes; ddm/lentes/capm intocados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cíclica (ENG-03), Crescimento (ENG-04) e NAV holding (ENG-05)</name>
  <files>src/analista/core/motores.py, config.yaml</files>
  <read_first>
    - src/analista/core/motores.py (o que a Task 1 criou — imports, Number, padrão never-raise)
    - src/analista/core/ddm.py:37-46 (`valor_gordon` = P/L justo da cíclica) e :78-115 (`ddm_dois_estagios` — reuso PURO no crescimento, D-05; ddm.py INTOCADO)
    - src/analista/core/normalizacao.py:58-75 (`base_normalizada(valores, anos_media, winsor)` — lucro médio 7-10a)
    - src/analista/core/lentes.py:51-53 (`vpa` — NAV contábil D-03)
    - .planning/phases/02-motores-por-arqu-tipo/02-RESEARCH.md:174-194 (Pattern 2 crescimento + Pattern 3 cíclica) e :282-307 (Code Examples NAV + disparo no funil)
    - .planning/phases/02-motores-por-arqu-tipo/02-CONTEXT.md:56-76 (D-03 rótulo NAV obrigatório; D-04 P/L justo; D-05 multi-estágio)
  </read_first>
  <behavior>
    - lucro_normalizado(lpa_normalizado=5.0, ke=0.12, g_estavel=0.025) → intrínseco = lpa × fair_PE via valor_gordon (positivo, finito); None se ke-g<=0 ou input None
    - lucro_normalizado consome a média 7–10a: a mesma função alimentada por `base_normalizada(serie_oscilante, anos_media=10)` ignora pico/vale (fixture com 1 ano 3× → intrínseco ≈ sobre a mediana, NÃO sobre o último ano)
    - dcf_crescimento(lpa_valuation=2.0, g_alto=0.15, g_estavel=0.025, ke=0.14, n=10) → valor positivo e finito via ddm.ddm_dois_estagios(dpa_inicial=lpa*(1+g_alto), decrescente=True); NÃO zero/lixo mesmo com payout baixo; None se ke-g_estavel<=0
    - nav_contabil(pl=5000.0, num_acoes=1000.0) → 5.0 (VPA); None se PL ou ações None/zero
    - Todos never-raise
  </behavior>
  <action>
    Acrescentar a `core/motores.py` três funções puras, todas consumindo primitivas existentes (não
    recalcular método — Pitfall 2):

    `lucro_normalizado(lpa_normalizado, ke, g_estavel)` — P/L justo (Gordon) sobre o lucro médio:
    retornar `ddm.valor_gordon(dpa1=lpa_normalizado, ke=ke, g=g_estavel)` (= lpa × fair_PE). O lucro
    médio 7–10a é resolvido pelo chamador via `norm.base_normalizada(serie, anos_media=cfg["motores"]
    ["ciclica"]["anos_media"])` — a função aqui recebe já o LPA normalizado (fronteira FIX-04). Rótulo
    (D-04): "P/L justo sobre lucro normalizado (média 7–10a)". Fonte do P/L justo = Gordon sobre o Ke
    já calculado (custo-zero, sempre disponível no single-ticker — A3 do RESEARCH).

    `dcf_crescimento(lpa_valuation, g_alto, g_estavel, ke, n, decrescente=True)` — reuso PURO de
    `ddm.ddm_dois_estagios` (D-05, SEM tocar ddm.py): guard never-raise; `dpa_inicial = lpa_valuation
    * (1 + g_alto)` (LUCRO do ano 1, NÃO × payout — Pattern 2); chamar `ddm.ddm_dois_estagios(dpa_inicial,
    g_alto, n, g_estavel, ke, decrescente=decrescente)`; devolver o resultado (ou seu
    `.valor_intrinseco`). Modelo-H (decrescente=True) por ser conservador (Pitfall 4). Rótulo honesto:
    "DCF sobre lucro, aproximação capital-light".

    `nav_contabil(patrimonio_liquido, num_acoes)` — retornar `lentes.vpa(patrimonio_liquido, num_acoes)`.
    Rótulo OBRIGATÓRIO (D-03): "NAV contábil (piso patrimonial), não SOTP por segmento".

    Definir os rótulos humanos de cada motor de forma acessível ao Plan 02 (ex.: um dict
    `MOTOR_ROTULO = {"rim": "...", "normalizado": "...", "dcf": "...", "nav": "...", "ddm": "lente
    conservadora"}` OU docstrings) — o Plan 02 exibe no render. Completar em `config.yaml` os
    sub-blocos `motores.ciclica: { anos_media: 10, winsor: 0.10 }` e `motores.crescimento: {
    n_anos_explicito: 10 }` (se não criados na Task 1), sem tocar blocos existentes.
  </action>
  <acceptance_criteria>
    - `core/motores.py` contém `def lucro_normalizado(`, `def dcf_crescimento(`, `def nav_contabil(`
    - `dcf_crescimento` referencia `ddm.ddm_dois_estagios` (grep confirma) e `ddm.py` segue fora de `git diff --name-only`
    - `nav_contabil` referencia `lentes.vpa`
    - `config.yaml` tem `motores.ciclica` e `motores.crescimento`; blocos `ddm:`/`capm:`/`arquetipo:` intocados
    - Goldens da Task 3 para os 3 motores passam
  </acceptance_criteria>
  <verify>
    <automated>cd "$PWD" && python -c "from analista.core import motores; assert all(hasattr(motores,n) for n in ('lucro_normalizado','dcf_crescimento','nav_contabil'))"</automated>
  </verify>
  <done>Os 3 motores restantes existem, compõem primitivas testadas, nunca recalculam método, e config tem os 3 sub-blocos; ddm/lentes/normalizacao intocados.</done>
</task>

<task type="auto">
  <name>Task 3: Golden puro por motor (tests/test_motores.py)</name>
  <files>tests/test_motores.py</files>
  <read_first>
    - tests/test_ddm.py:1-60 (padrão do golden puro: inputs fixos de livro, tolerância absoluta, checagem de composição/peso_residual) — analog exato
    - src/analista/core/motores.py (as 5 funções que as Tasks 1-2 criaram — assinaturas reais)
    - .planning/phases/02-motores-por-arqu-tipo/02-RESEARCH.md:338-345 (tabela de golden por motor) e :296-307 (padrão de golden puro)
    - .planning/phases/02-motores-por-arqu-tipo/02-PATTERNS.md:291-328 (analog test_ddm + fixtures)
  </read_first>
  <action>
    Criar `tests/test_motores.py` (offline, sem rede — padrão do repo), carregando `config.yaml` via
    yaml para os testes de `ke_rim`. Um golden por motor, com inputs fixos e tolerância absoluta:

    RIM (ENG-02, o crítico): `rim(vpa0=22.0, roe0=0.193, ke=0.125, retencao=0.53, n=10)` → assert
    `valor_intrinseco` em R$26–34 (a fórmula especificada rende ~R$28,20 — verificado aritmeticamente;
    o teto R$34 = caso no-fade R$34,15), assert materialmente > DDM ao vivo com `valor_intrinseco >= 25`
    (NÃO `> 2*16` — o modelo honesto não alcança R$32 sem prêmio terminal, o que violaria D-02), assert
    `ri_por_ano[-1]` ≈ 0 (abs < 0.05 — fade completo). Caso degenerado: `rim(vpa0=None, ...)` e `ke<=0`
    → None. Caso `roe0==ke` → `valor_intrinseco` ≈ `vpa0` (=22.0). As asserções aqui DEVEM bater
    numericamente com o `<behavior>`/`<acceptance_criteria>` da Task 1.

    ke_rim (D-01, a alavanca): carregar cfg do config.yaml; assert `ke_rim(1.0, cfg)` em [0.11, 0.14]
    E estritamente `< capm.ke_local(1.0, cfg["capm"]["rf_local"], cfg["capm"]["erp_local"])` (menor
    que o Ke ao vivo — o coração do critério #1).

    Lucro normalizado (ENG-03): construir uma série oscilante (ex.: [200,1500,100,1800,150,2000,...])
    e mostrar que `norm.base_normalizada(serie, anos_media=10)` ≠ `serie[-1]`; `lucro_normalizado(
    lpa_mid, ke=0.12, g_estavel=0.025)` > 0 e finito; None se ke-g<=0.

    DCF crescimento (ENG-04): `dcf_crescimento(lpa_valuation=2.0, g_alto=0.15, g_estavel=0.025,
    ke=0.14, n=10)` → intrínseco > 0 e finito (assert NÃO zero/lixo — é o critério #3); modelo-H
    (decrescente=True) ≤ constante (decrescente=False).

    NAV (ENG-05): `nav_contabil(5000.0, 1000.0)` == 5.0; `nav_contabil(None, 1000.0)` is None.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_motores.py -x` sai 0 (todos os goldens verdes)
    - `python -m pytest tests/test_ddm.py -x` sai 0 (aceite #5 — ddm.py intocado; regressão-guarda)
    - `python -m pytest` (suite completa) sai 0 — nenhum golden existente regride
    - test_motores.py cobre os 5 pontos: rim(~R$28 faixa R$26–34 & >=25 (>DDM) & fade→0), ke_rim(<ke_live), lucro_normalizado(7-10a), dcf_crescimento(>0 finito), nav_contabil(=vpa)
  </acceptance_criteria>
  <verify>
    <automated>cd "$PWD" && python -m pytest tests/test_motores.py tests/test_ddm.py -x</automated>
  </verify>
  <done>Golden puro por motor verde; test_ddm continua verde; suite completa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados CVM/Yahoo/BCB → motores | Insumos numéricos já ingeridos a montante entram nos motores puros. Superfície de ataque baixíssima: engine single-tier offline, sem auth, sem rede, sem escrita. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0201-01 | Tampering (dado-lixo → intrínseco absurdo) | motores.rim/dcf/normalizado/nav | accept | Guard never-raise no topo de cada motor (input None / divisor ≤ 0 → None, padrão ddm.py) evita crash e faixa degenerada silenciosa. Os guarda-corpos anti-aberração COMPLETOS (SAN-01: intrínseco < 0,5× mediana dos pares) são **Fase 3 por design** — NÃO puxar para cá. |
| T-0201-02 | Denial of Service (levantar exceção no funil) | core/motores.py | mitigate | Todos os motores retornam Optional (nunca raise), espelhando `ddm.valor_gordon`; golden cobre inputs degenerados (None/zero/negativo). |
</threat_model>

<verification>
- `python -m pytest tests/test_motores.py -x` verde (goldens dos 4 motores + ke_rim)
- `python -m pytest tests/test_ddm.py -x` verde (ddm.py intocado — aceite #5)
- `git diff --name-only` NÃO lista core/ddm.py, core/lentes.py, core/capm.py, core/normalizacao.py, core/fundamentals.py, report/selo.py
- `git diff config.yaml` mostra só o bloco novo `motores:` (blocos capm/ddm/arquetipo intactos)
</verification>

<success_criteria>
- core/motores.py com rim/ke_rim/lucro_normalizado/dcf_crescimento/nav_contabil, todos funções puras never-raise
- ke_rim devolve Ke estrutural (~12,5%) menor que o CAPM ao vivo de banco (D-01) — validado por golden
- RIM tipo-ITUB4 ~R$28 (faixa R$26–34, honesto/conservador — desvio documentado do "~R$40" de D-01) e materialmente > DDM ao vivo (~R$16, >= 25); fade → RI terminal ≈ 0 (D-02)
- Bloco motores: aditivo em config.yaml sem rebaseline
- Suite completa de testes verde
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-motores-por-arqu-tipo/02-01-SUMMARY.md`.
</output>
