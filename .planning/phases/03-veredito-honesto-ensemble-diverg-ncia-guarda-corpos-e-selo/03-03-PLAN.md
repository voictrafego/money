---
phase: 03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo
plan: 03
type: execute
wave: 3
depends_on: [1]
files_modified:
  - src/analista/report/report.py
  - tests/test_arquetipo_roteamento.py
autonomous: true
requirements: [VER-02]
must_haves:
  truths:
    - "Em caso-fronteira (arquetipo_fronteirico), a ferramenta roda o motor de cada arquétipo candidato e exibe o range [menor..maior] dos intrínsecos + a bandeira 'classificação incerta entre X e Y' — assume a dúvida em voz alta"
    - "O selo NÃO estampa faixa/rótulo no fronteiriço (reusa a supressão do prefixo VERIFICAR), o range/candidatos aparecem como conteúdo exibido"
    - "Se um motor candidato degrada (None), lista só os candidatos que resolveram lado a lado, sem forçar um range de 1 ponto"
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "Helper de dispatch por motor reutilizável + ramo fronteiriço que monta range dos candidatos + campos veredito_range/candidatos_intrinsecos"
      contains: "arquetipo_fronteirico"
  key_links:
    - from: "report.analisar_acao (ramo fronteiriço)"
      to: "dispatch de motor por candidato (arquetipo_candidatos)"
      via: "helper _intrinseco_por_motor reutilizado do dispatch principal"
      pattern: "_intrinseco_por_motor"
    - from: "veredito fronteiriço (prefixo VERIFICAR)"
      to: "selo.montar_selo (faixa/rótulo suprimidos)"
      via: "overlay VERIFICAR já existente em selo.py:119"
      pattern: "VERIFICAR"
---

<objective>
VER-02: em **caso-fronteira** (`a.arquetipo_fronteirico`, conflito real de sinais da Fase 1), o veredito **assume a dúvida** em vez de cravar um selo falso. Rodar o motor de cada arquétipo candidato (`a.arquetipo_candidatos`), exibir o **range [menor..maior]** dos intrínsecos + bandeira **"classificação incerta entre X e Y"** (D-06).

Analog: o próprio ramo de suspensão `report.py:296-319` (estampa VERIFICAR + exibe número do motor como conteúdo, sem faixa cravada) — VER-02 generaliza para N candidatos. O dispatch motor→intrínseco (`report.py:200-230`) é extraído para um helper puro reutilizado por candidato.

**Selo (D-06):** reusar o overlay VERIFICAR (`selo.py:119-122`) — o prefixo já faz `montar_selo` suprimir faixa/rótulo sem tocar o firewall. O range/candidatos aparecem como conteúdo no veredito/markdown.
**Degradação:** motor candidato None -> listar só os que resolveram, sem range de 1 ponto.

Purpose: fechar SC#4 e o item 5 do brief — nos ~15% fronteiriços, span da dúvida honesto em vez de certeza fingida.
Output: helper `_intrinseco_por_motor` + ramo fronteiriço + campos em `AnaliseAcao`, em `report.py`.

**Firewall:** `selo.py` intocado (o prefixo VERIFICAR já existe). Toda mudança na borda do veredito em `report.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo/03-CONTEXT.md
@.planning/phases/03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo/03-PATTERNS.md
@.planning/phases/03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo/03-01-SUMMARY.md

<interfaces>
De src/analista/report/report.py:
- a.arquetipo_fronteirico: bool                 # populado na Fase 1 (conflito real de sinais)
- a.arquetipo_candidatos: List[str]             # 2-3 chaves de arquétipo candidatas
- a.intrinseco_motor, a.motor, a.arquetipo, a.veredito, a.alertas
- dispatch atual motor->intrínseco: report.py:200-230 (if a.motor == "rim"/"normalizado"/"dcf"/"nav") — a EXTRAIR em helper

De src/analista/core/arquetipo.py:
- ARQUETIPO_MOTOR: Dict[str,str]  (arquétipo -> motor primário)  # mapeia candidato -> motor

De src/analista/report/selo.py (NÃO tocar):
- montar_selo suprime faixa/rótulo quando veredito começa com "VERIFICAR" (selo.py:119)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extrair _intrinseco_por_motor + ramo fronteiriço (range dos candidatos + bandeira)</name>
  <read_first>
    - src/analista/report/report.py:192-242 (dispatch motor->intrínseco a EXTRAIR + guarda de não-positivo)
    - src/analista/report/report.py:296-319 (analog do ramo VERIFICAR — estrutura de "exibir número(s) sem faixa cravada")
    - src/analista/core/arquetipo.py (ARQUETIPO_MOTOR — candidato -> motor)
    - src/analista/report/selo.py:119-122 (overlay VERIFICAR — supressão de faixa reutilizada)
    - .planning/phases/.../03-CONTEXT.md (D-06: range [menor..maior] + "classificação incerta entre X e Y"; degradação)
  </read_first>
  <behavior>
    - _intrinseco_por_motor(motor, c, a, cfg) devolve Optional[float] com a MESMA lógica do dispatch atual (rim/normalizado/dcf/nav; ddm -> mid da banda DDM ou None), never-raise, consumindo sempre números-síntese (*_valuation, base_normalizada), nunca o cru.
    - O dispatch principal (:200-230) passa a chamar o helper (comportamento idêntico ao baseline — refator sem mudança observável para não-fronteiriços).
    - Quando a.arquetipo_fronteirico: para cada chave em a.arquetipo_candidatos, resolve o motor via ARQUETIPO_MOTOR e chama o helper; coleta os intrínsecos não-None em a.candidatos_intrinsecos (lista de (arquetipo, valor)).
    - >=2 candidatos resolvidos: a.veredito_range = (menor, maior); a.veredito começa com "VERIFICAR" e exibe o range + "classificação incerta entre {X} e {Y}".
    - Exatamente 1 resolvido: exibe esse único intrínseco (sem range de 1 ponto), prefixo VERIFICAR.
    - Nenhum resolvido: VERIFICAR informando que os motores candidatos não estimaram preço-alvo.
    - Não-fronteiriço: ramo não roda; VER-01 (03-01) segue mandando.
  </behavior>
  <action>
    Extrair o dispatch motor->intrínseco (`report.py:200-230`) para `_intrinseco_por_motor(motor: str, c: CompanyData, a: AnaliseAcao, cfg: dict) -> Optional[float]`, preservando exatamente a lógica atual (rim via `motores.rim`/`ke_rim`; normalizado via `norm.base_normalizada`+`mult.lpa`+`motores.lucro_normalizado`; dcf via `motores.dcf_crescimento`; nav via `motores.nav_contabil`; caso "ddm" retorna o mid da banda DDM quando disponível, senão None) e a guarda de não-positivo. O dispatch principal passa a chamar o helper (refator puro, comportamento idêntico — travar por golden existente).

    Adicionar campos em `AnaliseAcao` (bloco Fase 3): `arquetipo_incerto: bool = False`, `candidatos_intrinsecos: List[tuple] = field(default_factory=list)`, `veredito_range: Optional[tuple] = None`. Inserir o ramo fronteiriço na borda do veredito: quando `a.arquetipo_fronteirico`, iterar `a.arquetipo_candidatos`, resolver `ARQUETIPO_MOTOR.get(cand)`, chamar `_intrinseco_por_motor`, coletar os não-None; montar `a.veredito` com prefixo `VERIFICAR` + range [menor..maior] + "classificação incerta entre {primeiro} e {último}" (ou o caso 1-candidato/0-candidato conforme behavior); gravar `a.arquetipo_incerto`/`a.candidatos_intrinsecos`/`a.veredito_range`; `a.alertas.append` a nota da dúvida honesta. O ramo fronteiriço tem precedência sobre o veredito de motor único do VER-01 (roda antes, ou sobrescreve, quando fronteiriço). NÃO tocar `selo.py` — o prefixo VERIFICAR já suprime a faixa.
  </action>
  <acceptance_criteria>
    - `_intrinseco_por_motor` existe e o dispatch principal o consome; casos não-fronteiriços inalterados (`python -m pytest tests/test_arquetipo_roteamento.py tests/test_motores.py -q` sai 0).
    - Fixture fronteiriça com >=2 candidatos resolvidos: `a.veredito` começa com "VERIFICAR", contém o range e "classificação incerta entre"; `a.veredito_range == (menor, maior)`; `selo.faixa_do_veredito(a.veredito) is None`.
    - Degradação: fixture com 1 motor candidato válido -> exibe o único valor, sem range de 1 ponto; `a.veredito_range is None`.
    - `git diff --exit-code src/analista/report/selo.py` (intocado).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -m pytest tests/test_arquetipo_roteamento.py tests/test_arquetipo.py tests/test_motores.py tests/test_report.py -q; git diff --exit-code src/analista/report/selo.py</automated>
  </verify>
  <done>Helper de dispatch extraído + ramo fronteiriço com range/bandeira; selo suprime faixa via VERIFICAR; não-fronteiriços intactos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render do range/candidatos no markdown + golden fronteiriço</name>
  <read_first>
    - src/analista/report/report.py:594-653 (bloco motor + seção Veredito no relatorio_markdown)
    - tests/test_arquetipo_roteamento.py (goldens do roteamento; onde encaixar o caso fronteiriço)
    - .planning/phases/.../03-CONTEXT.md (D-06 conteúdo exibido; VER-02 range+bandeira)
  </read_first>
  <behavior>
    - relatorio_markdown, quando a.arquetipo_incerto, exibe um bloco listando cada candidato e seu intrínseco + a frase "classificação incerta entre X e Y" + o range.
    - Sem fronteiriço: nenhum bloco novo (render limpo).
    - Números formatados ptBR via _num.
  </behavior>
  <action>
    Em `relatorio_markdown` (`report.py:645-653`, seção Veredito, ou próximo ao bloco motor `:594-606`), quando `a.arquetipo_incerto`, `L.append` um bloco "Classificação incerta (caso-fronteira)" listando `a.candidatos_intrinsecos` (arquétipo -> R$ intrínseco) e o range `a.veredito_range`, com a frase da bandeira. Reusar `_num`. Adicionar em `tests/test_arquetipo_roteamento.py` um golden: fixture fronteiriça -> `relatorio_markdown` contém "classificação incerta", os candidatos e o range; fixture normal -> não contém. Declarar rebaseline no SUMMARY se strings mudarem.
  </action>
  <acceptance_criteria>
    - `relatorio_markdown` de um caso fronteiriço contém "classificação incerta", os candidatos com seus valores e o range; caso normal não contém o bloco.
    - `python -m pytest tests/test_arquetipo_roteamento.py tests/test_report.py -q` sai 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -m pytest tests/test_arquetipo_roteamento.py tests/test_report.py tests/test_selo.py tests/test_consistencia_modos.py -q</automated>
  </verify>
  <done>Range/candidatos renderizados no markdown; golden fronteiriço verde; render limpo fora do fronteiriço.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream CVM/Yahoo → motores dos candidatos | Dados atípicos podem fazer um motor candidato devolver None/valor absurdo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0303-01 | Denial (crash em loop de candidatos) | _intrinseco_por_motor por candidato | mitigate | helper never-raise (herda a guarda de não-positivo/None do dispatch); candidato None é filtrado, não quebra o range |
| T-0303-02 | Information disclosure (range de 1 ponto enganoso) | montagem do range | mitigate | range só com >=2 candidatos resolvidos; 1 candidato -> valor único; 0 -> VERIFICAR informativo (D-06 degradação) |
| T-0303-03 | Tampering (selo estampa faixa no fronteiriço) | selo consumindo veredito | mitigate | prefixo VERIFICAR já suprime faixa/rótulo (selo.py:119); selo.py intocado; teste de firewall trava |
</threat_model>

<verification>
- `python -m pytest tests/test_arquetipo_roteamento.py tests/test_arquetipo.py tests/test_motores.py tests/test_report.py tests/test_selo.py tests/test_consistencia_modos.py -q` sai 0.
- `git diff --exit-code src/analista/report/selo.py`.
- Refator do dispatch não muda o intrínseco de nenhum ticker não-fronteiriço (goldens de roteamento verdes).
</verification>

<success_criteria>
- SC#4: caso-fronteira exibe range + bandeira de divergência (dúvida honesta), não selo cravado.
- SC#5 (parcial): firewall preservado; goldens de roteamento/selo/consistência verdes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo/03-03-SUMMARY.md`
</output>
