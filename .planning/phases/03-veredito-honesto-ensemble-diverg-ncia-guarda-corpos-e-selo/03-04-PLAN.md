---
phase: 03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo
plan: 04
type: execute
wave: 4
depends_on: [1, 2, 3]
files_modified:
  - app.py
  - tests/test_vulc3_regressao.py
autonomous: true
requirements: [VER-01, ENS-01, SAN-01, VER-02]
must_haves:
  truths:
    - "A UI Streamlit (Analisar) exibe a bandeira de divergência (razão + hipótese), o range fronteiriço e a nota da reetiqueta SAN-01 — só onde o veredito/bandeira aparecem (sem redesenho)"
    - "O rótulo 'Intrínseco (DDM)' reflete o motor do arquétipo quando motor != ddm (não engana o usuário chamando o RIM de DDM)"
    - "O mesmo número aparece em Analisar/Garimpo/Ranking (Core Value) e todos os goldens da fase seguem verdes; qualquer rebaseline é declarado com intenção"
    - "Os tickers-âncora se comportam: ITUB4 sem 'Evitar', TAEE11 idêntica, VALE3/WEGE3 pelo motor certo, VULC3 ainda 'VERIFICAR' por risco real"
  artifacts:
    - path: "app.py"
      provides: "Render da bandeira/range/reetiqueta no bloco veredito+selo do Analisar + rótulo do intrínseco corrigido"
      contains: "divergencia_hipotese"
  key_links:
    - from: "app.py bloco veredito (linha ~880-942)"
      to: "campos a.divergencia_*/a.veredito_range/a.san01_reetiquetado"
      via: "render read-only dos campos já derivados na engine"
      pattern: "veredito_range"
---

<objective>
Superfície de render Streamlit + capstone e2e. Os planos 01-03 fizeram a lógica em `report.py` (e o CLI já exibe via `relatorio_markdown`); a UI do Streamlit (`app.py`) tem um bloco de veredito+selo PRÓPRIO (`app.py:880-942`) que precisa exibir os novos sinais: **bandeira de divergência** (razão + hipótese, ENS-01), **range fronteiriço** (VER-02) e **nota da reetiqueta SAN-01**. Também corrige o rótulo `"Intrínseco (DDM)"` (`app.py:915`) para refletir o motor quando `motor != ddm` (hoje chamaria o RIM de "DDM" — engana).

Escopo de UI é mínimo e read-only (Out of Scope: redesenho): só LÊ os campos já derivados na engine, zero recálculo. Fecha com a bateria e2e sobre os tickers-âncora e a suíte de goldens da fase.

Purpose: paridade CLI↔UI dos novos sinais + validação capstone dos 4 requisitos e do Core Value (mesmo número nos 3 modos).
Output: `app.py` atualizado + golden capstone.

**Firewall:** `selo.py`/`report.py` não mudam aqui; `app.py` é read-only sobre os campos de `AnaliseAcao`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo/03-CONTEXT.md
@.planning/phases/03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo/03-01-SUMMARY.md
@.planning/phases/03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo/03-02-SUMMARY.md
@.planning/phases/03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo/03-03-SUMMARY.md

<interfaces>
De src/analista/report/report.py (campos de AnaliseAcao adicionados nos planos 01-03):
- a.divergencia_ativa: bool ; a.divergencia_razao: Optional[float] ; a.divergencia_hipotese: str
- a.contraponto_valor: Optional[float] ; a.intrinseco_motor: Optional[float] ; a.motor / a.motor_rotulo: str
- a.san01_reetiquetado: bool
- a.arquetipo_incerto: bool ; a.candidatos_intrinsecos: List[tuple] ; a.veredito_range: Optional[tuple]
- a.vmin/a.vmax ; a.banda_do_motor: bool

De app.py (bloco a editar):
- ~880-942: bloco veredito colorido + selo_badge + m2.metric("Intrínseco (DDM)", intervalo) + nota ddm_inaplicavel
- helpers existentes: fmt_rs, fmt_pct, esc_md, h(...)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render da bandeira/range/reetiqueta + rótulo do intrínseco no Analisar (app.py)</name>
  <files>app.py</files>
  <read_first>
    - app.py:878-945 (bloco veredito colorido + selo + métricas + nota ddm_inaplicavel — onde os novos sinais entram)
    - src/analista/report/report.py:594-653 (como o markdown exibe os mesmos sinais — paridade de copy)
    - .planning/phases/.../03-01-SUMMARY.md, 03-02-SUMMARY.md, 03-03-SUMMARY.md (nomes finais dos campos)
  </read_first>
  <action>
    No bloco de veredito do Analisar (app.py ~882-942), read-only sobre `a`:
    (1) Bandeira de divergência (ENS-01): quando `a.divergencia_ativa`, exibir um `st.info`/`st.warning` com a razão (`a.divergencia_razao`), os DOIS números (`a.intrinseco_motor` motor primário e `a.contraponto_valor` DDM lente) e a hipótese `a.divergencia_hipotese` — divergência é informação exibida.
    (2) Range fronteiriço (VER-02): quando `a.arquetipo_incerto`, exibir a lista `a.candidatos_intrinsecos` (arquétipo -> intrínseco) e o `a.veredito_range` com a frase "classificação incerta entre X e Y".
    (3) Reetiqueta SAN-01: quando `a.san01_reetiquetado`, exibir a nota honesta ("guarda-corpo anti-aberração: ver motor primário do arquétipo").
    (4) Rótulo do intrínseco: no `m2.metric("Intrínseco (DDM)", ...)` (app.py:915), quando `a.motor != "ddm"`, trocar o label para `f"Intrínseco ({a.motor_rotulo or a.motor})"` (e o help correspondente) — não chamar o motor do arquétipo de "DDM". Manter "Intrínseco (DDM)" quando `motor == "ddm"` (TAEE11 idêntica). Reusar `fmt_rs`/`fmt_pct`/`esc_md`; nenhuma fórmula nova. Copy descritiva, nunca recomendação (gate regulatório do selo).
  </action>
  <acceptance_criteria>
    - `app.py` referencia `a.divergencia_hipotese`, `a.veredito_range`, `a.san01_reetiquetado` no bloco do Analisar.
    - O label do intrínseco usa `a.motor_rotulo` quando `motor != "ddm"` e "Intrínseco (DDM)" quando `motor == "ddm"`.
    - `python -c "import ast; ast.parse(open('app.py').read())"` sai 0 (sintaxe válida).
    - Nenhum recálculo: o diff de `app.py` só LÊ campos de `a` (sem chamadas a motores/ddm/comparables).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -c "import ast; ast.parse(open('app.py').read()); print('ok')"</automated>
  </verify>
  <done>Bandeira/range/reetiqueta exibidos no Analisar; rótulo do intrínseco reflete o motor; render read-only.</done>
</task>

<task type="auto">
  <name>Task 2: Capstone e2e — tickers-âncora + suíte de goldens da fase + Core Value</name>
  <files>tests/test_vulc3_regressao.py</files>
  <read_first>
    - tests/test_vulc3_regressao.py (capstone e2e existente — invariante VULC3 "VERIFICAR")
    - tests/test_consistencia_modos.py (mesmo número nos 3 modos — Core Value)
    - .planning/ROADMAP.md §"Phase 3" (5 success criteria + lista de goldens que devem seguir verdes)
    - .planning/phases/.../03-CONTEXT.md (tickers-âncora: ITUB4/TAEE11/VALE3/WEGE3/VULC3)
  </read_first>
  <action>
    Adicionar/estender asserções e2e (em `tests/test_vulc3_regressao.py` ou um novo caso no mesmo módulo, usando as fixtures existentes das fases 1-2) cobrindo os tickers-âncora sobre o veredito FINAL da fase: ITUB4 -> sem token "Evitar", motor RIM referenciado, DDM como lente; TAEE11 -> veredito/selo idênticos ao baseline (motor ddm); VALE3 -> motor "normalizado"; WEGE3 -> motor "dcf" sem faixa lixo; VULC3 -> `a.veredito` começa com "VERIFICAR" por risco real (payout>100%). Onde faltar fixture pronta, reutilizar as fixtures/goldens das fases 1-2 já no repo (não puxar rede). Rodar a suíte completa da fase e declarar no SUMMARY qualquer rebaseline (com `faixa_do_veredito` selo.py:88 e `_veredito_token` report.py:491 atualizados JUNTOS, se algum prefixo mudou — nenhum esperado).
  </action>
  <acceptance_criteria>
    - Asserções e2e presentes para ITUB4 (sem "Evitar"), TAEE11 (idêntica), VALE3 (normalizado), WEGE3 (dcf), VULC3 (VERIFICAR por risco).
    - Toda a suíte de goldens da fase sai 0: test_selo, test_vulc3_regressao, test_guardrails_fix06, test_guardrails_ddm, test_consistencia_modos, test_ddm, test_report, test_arquetipo, test_arquetipo_roteamento, test_ranking_freio, test_motores, test_lentes, test_comparables.
    - Firewall: `grep -n "import report" src/analista/report/selo.py` vazio.
    - Qualquer rebaseline está declarado no SUMMARY com intenção; caso contrário, zero mudança de golden.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -m pytest tests/test_selo.py tests/test_vulc3_regressao.py tests/test_guardrails_fix06.py tests/test_guardrails_ddm.py tests/test_consistencia_modos.py tests/test_ddm.py tests/test_report.py tests/test_arquetipo.py tests/test_arquetipo_roteamento.py tests/test_ranking_freio.py tests/test_motores.py tests/test_lentes.py tests/test_comparables.py -q</automated>
  </verify>
  <done>e2e dos tickers-âncora + suíte completa de goldens verde; Core Value (3 modos) e firewall confirmados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine (AnaliseAcao) → UI Streamlit | app.py só LÊ campos derivados; risco = exibir número enganoso se o render acessar o campo errado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0304-01 | Information disclosure (rótulo enganoso) | m2.metric label "Intrínseco (DDM)" | mitigate | label passa a refletir a.motor_rotulo quando motor!="ddm"; DDM só quando é o motor de fato |
| T-0304-02 | Tampering (UI recalcula e diverge dos 3 modos) | render app.py | mitigate | render é read-only sobre `a`; test_consistencia_modos trava o mesmo número em Analisar/Garimpo/Ranking |
| T-0304-03 | Denial (campo None quebra o render) | acesso a a.veredito_range/divergencia_* | mitigate | guards `if a.divergencia_ativa`/`if a.arquetipo_incerto` antes de acessar; defaults degradáveis dos campos |
</threat_model>

<verification>
- `python -m pytest tests/test_selo.py tests/test_vulc3_regressao.py tests/test_guardrails_fix06.py tests/test_guardrails_ddm.py tests/test_consistencia_modos.py tests/test_ddm.py tests/test_report.py tests/test_arquetipo.py tests/test_arquetipo_roteamento.py tests/test_ranking_freio.py tests/test_motores.py tests/test_lentes.py tests/test_comparables.py -q` sai 0.
- `python -c "import ast; ast.parse(open('app.py').read())"` sai 0.
- `git diff --exit-code src/analista/report/selo.py src/analista/report/report.py` (não mudam neste plano).
</verification>

<success_criteria>
- SC#1..SC#4 exibidos na UI (paridade CLI↔UI): ITUB4 sem Evitar + bandeira; range fronteiriço; reetiqueta SAN-01.
- SC#5 (fecha): firewall selo↛report + toda a suíte de goldens da fase verdes; Core Value (3 modos) travado.
</success_criteria>

<output>
After completion, create `.planning/phases/03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo/03-04-SUMMARY.md`
</output>
