---
phase: 12-custo-de-capital-ke-ke
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - tests/test_ke_validacao.py
  - tests/classificacao.yaml
autonomous: true
requirements: [KE-04]

must_haves:
  truths:
    - "O invariante estrutural rf_carimbado + 0,33 x erp_local > g_cap é asseverado (desigualdade), sem cravar 11,07% como golden"
    - "Sobre os 104 tickers reais, todo V computado é finito e positivo, Ke - g_T > 0 e Ke >= Ke_min — SEM clamp"
    - "Nenhuma perpetuidade diverge por aritmética; nenhum guard novo foi introduzido"
  artifacts:
    - path: "tests/test_ke_validacao.py"
      provides: "Invariante estrutural + regressão anti-explosão dos 104 tickers"
      contains: "erp_local"
  key_links:
    - from: "tests/test_ke_validacao.py"
      to: "data/beta_setorial.yaml"
      via: "carimbar_beta_setorial(cfg) — o Ke offline usa o MESMO mapa do app"
      pattern: "beta_setorial"
    - from: "tests/test_ke_validacao.py"
      to: "config (erp_local, g_cap)"
      via: "lê os valores dinamicamente — robusto ao drift do rf"
      pattern: "erp_local"
---

<objective>
Provar por EXECUÇÃO que "nada explode sem clamp" (KE-04 / D-11), das duas formas exigidas: (a) o
invariante estrutural `Ke_min > g_cap` como DESIGUALDADE (não como número cravado), e (b) a regressão
anti-explosão sobre o mapa REAL dos 104 tickers, espelhando GROW-04/05. É o gate final da fase: a
convergência da perpetuidade vem da aritmética do piso do Blume, não de uma trava.

Purpose: fechar a Doença 3 com evidência de execução (guardrail provado por rodar a regressão, não
por "suíte verde" genérica — memória `guardrails-devem-ser-provados-por-execucao`).
Output: `tests/test_ke_validacao.py` com os dois testes, verdes no estado final (ERP 0,045, sem clamp).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-custo-de-capital-ke-ke/12-CONTEXT.md
@.planning/phases/12-custo-de-capital-ke-ke/12-RESEARCH.md
@CLAUDE.md
@.planning/phases/12-custo-de-capital-ke-ke/12-03-SUMMARY.md

<aritmetica_verificada>
Do RESEARCH (VERIFIED): o Blume põe PISO em β em 0,33 (mesmo β negativo/outlier -> β_blume >= 0,33).
Logo `Ke_min = rf + 0,33 × erp_local` INDEPENDE de outlier de beta. Com rf carimbado ao vivo 9,58%:
`0,0958 + 0,33×0,045 = 0,11065` (~11,07%). Com o rf offline default 0,105: `0,105 + 0,33×0,045 =
0,11985`. Ambos > `g_cap = 7,28%` (GROW-03). O "11,07%" corresponde ao rf AO VIVO, NÃO ao default
offline — por isso o teste assevera a DESIGUALDADE (robusta ao drift do rf), nunca o número.
Margem: para `Ke_min` tocar `g_cap` seria preciso `rf < 7,28% − 0,33×0,045 = 5,79%` — implausível.
</aritmetica_verificada>

<interfaces>
Harness offline dos 104 (mirror GROW-04/05) — `tests/helpers_sanidade.py`:
```python
CAMINHO_SNAPSHOT_LIMPO = .../snapshot_sanidade_limpo_2026-07-15.yaml   # L31
def carregar_snapshot_sanidade(path=None) -> Dict[str, CompanyData]: ...  # L69
```

Carimbo do mapa (do Plano 01) — `src/analista/ingest/macro.py`:
```python
def carimbar_beta_setorial(cfg): cfg["capm"]["beta_setorial"] = carregar_beta_setorial()
```

Engine e derivação do g_cap — `src/analista/report/report.py`:
```python
a = report.analisar_acao(c, cfg)   # a.ke, a.g_estavel (g_cap), a.intrinseco_motor
```
`g_cap = (1 + pi_ciclo) × (1 + PIB_real) − 1`. Offline, `pi_ciclo` cai no default `config['macro']
['pi_ciclo']` (0,0518) e `PIB_real` = `config['ddm']['pib_real']` (0,02) — derivar como a engine faz,
ou ler `a.g_estavel` de uma corrida de `analisar_acao`.

Detector BLIND-04a-safe: o teste NÃO pode conter `ticker == valor de nível` (sem `assert V["ITUB4"]
== 3x.xx`). Assertivas são estruturais/adimensionais sobre o CONJUNTO dos 104.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invariante estrutural Ke_min > g_cap (desigualdade, não número)</name>
  <files>tests/test_ke_validacao.py, tests/classificacao.yaml</files>
  <read_first>
    - config.yaml (capm.erp_local == 0.045 após o Plano 03; macro.pi_ciclo; ddm.pib_real)
    - src/analista/report/report.py (derivação de g_cap; a.g_estavel)
    - .planning/phases/12-custo-de-capital-ke-ke/12-RESEARCH.md §"Invariante estrutural Ke_min > g_cap" e Pitfall 3 (não cravar 11,07%)
    - CLAUDE.md (não cravar golden de nível; justificativa/threshold estrutural sem ticker)
  </read_first>
  <behavior>
    - Lendo `erp_local` e o `rf_local`/`g_cap` do config/engine dinamicamente: `rf + 0.33*erp_local > g_cap` é VERDADEIRO
    - O teste NÃO hardcoda 0.11065 nem 0.1107 como valor esperado (documenta 11,07% só em comentário, como o valor no Selic-ciclo atual)
    - O piso do Blume (β->0 => β_blume == 0.33) é o mínimo estrutural — asseverado via `capm.beta_blume(algum_beta_negativo_ou_zero, "setor_inexistente", {})` >= 0.33
  </behavior>
  <action>
    Criar `tests/test_ke_validacao.py` (classe `invariante`). Escrever `test_ke_min_estrutural_acima_do_g_cap`:
    resolver `erp_local` do config, `rf` = `config['capm']['rf_local']` (default offline), e `g_cap`
    derivado do mesmo jeito que a engine (`(1+pi_ciclo)(1+pib_real)-1`) OU lido de `analisar_acao(...).
    g_estavel`. Asseverar a DESIGUALDADE `rf + 0.33*erp_local > g_cap`. Documentar em comentário que
    no Selic-ciclo ao vivo (rf 9,58%) isso dá ~11,07% > 7,28% — SEM cravar o número num assert.
    Adicionar um assert de que o piso do Blume é 0,33 (`capm.beta_blume` com base <= 0 -> >= 0,33),
    provando que `Ke_min` independe de outlier de beta. Registrar a entrada em `classificacao.yaml`
    (`invariante`) no mesmo diff. Justificativa do threshold 0,33 é ESTRUTURAL (é o intercepto do
    Blume), nunca um ticker.
  </action>
  <acceptance_criteria>
    - `pytest -k "ke_min_estrutural"` PASSA
    - `grep -Ec "0\.1107|0\.11065|11,07" tests/test_ke_validacao.py` — permitido SÓ em comentário, ZERO em linha de `assert`
    - `grep -c "assert" tests/test_ke_validacao.py` >= 1 e nenhum assert crava número de Ke de nível
    - `grep -v '^#' tests/classificacao.yaml | grep -c "ke_min_estrutural"` == 1
    - O teste lê `erp_local` do config dinamicamente (robusto: passaria com 0,06 e com 0,045)
  </acceptance_criteria>
  <verify>
    <automated>pytest -k "ke_min_estrutural"</automated>
  </verify>
  <done>Desigualdade estrutural asseverada sem cravar 11,07%; piso do Blume 0,33 provado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regressão anti-explosão sobre os 104 tickers reais (D-11a)</name>
  <files>tests/test_ke_validacao.py, tests/classificacao.yaml</files>
  <read_first>
    - tests/helpers_sanidade.py (L27-90: `CAMINHO_SNAPSHOT_LIMPO`, `carregar_snapshot_sanidade`, `falhas_do_snapshot`)
    - src/analista/ingest/macro.py (`carimbar_beta_setorial`, `carregar_beta_setorial` — do Plano 01)
    - src/analista/report/report.py (`analisar_acao` -> a.ke, a.g_estavel, a.intrinseco_motor)
    - .planning/phases/12-custo-de-capital-ke-ke/12-RESEARCH.md §"Validação sem clamp — superfície D-11" e §"Runtime State Inventory" (o snapshot precisa carregar o beta_setorial carimbado)
    - .planning/STATE.md (padrão GROW-04/05: limite de sanidade sem nomear ticker, BLIND-04a-safe)
  </read_first>
  <behavior>
    - Para cada ticker com Ke e V computados (None aceitável para falhas de mercado): V é finito e > 0
    - Para cada ticker com intrínseco de perpetuidade: Ke - g_T > 0 (spread convergente)
    - Para cada ticker com Ke: Ke >= Ke_min estrutural (rf + 0.33*erp_local)
    - Nenhum V "explode" — limite de sanidade adimensional (ex.: V < 50x preço) sem NOMEAR ticker
    - O Ke offline usa o mapa setorial (via carimbar_beta_setorial), não o fallback individual — senão diverge do app
  </behavior>
  <action>
    Escrever `test_regressao_104_sem_explosao` em `tests/test_ke_validacao.py`. Carregar as empresas
    via `helpers_sanidade.carregar_snapshot_sanidade()` (OFFLINE, 104 tickers). Montar o cfg do
    `config.yaml` e CARIMBAR o mapa via `macro.carimbar_beta_setorial(cfg)` (para o Ke offline usar o
    β SETORIAL, idêntico ao app — RESEARCH §Runtime State). Rodar `analisar_acao` em cada empresa e
    asseverar, por ticker COM Ke/V (pulando as `falhas_do_snapshot`): `a.ke >= rf + 0.33*erp_local`;
    quando houver `intrinseco_motor` finito, `intrinseco_motor > 0` e o spread `a.ke - g_T > 0` (com
    `g_T` derivado como a engine: `min(ROE_T×retenção, g_cap)`, piso 0); limite de sanidade `V < 50×
    preço` (adimensional, BLIND-04a-safe, SEM nomear ticker). `None` é aceitável (never-raise / falha
    de mercado). NENHUM guard novo é introduzido — o teste OBSERVA, não corrige. Registrar a entrada
    em `classificacao.yaml` (`invariante`) no mesmo diff. Sem `ticker == valor`.
  </action>
  <acceptance_criteria>
    - `pytest -k "regressao_104"` PASSA sobre os 104 tickers reais
    - `grep -c "beta_setorial\|carimbar_beta_setorial" tests/test_ke_validacao.py` >= 1 (o Ke offline usa o mapa)
    - O teste NÃO contém `ticker == número` (BLIND-04a): `pytest -k "blindagem_meta"` continua verde com o arquivo novo na varredura
    - `grep -v '^#' tests/classificacao.yaml | grep -c "regressao_104"` == 1
    - Limite de sanidade é adimensional (múltiplo de preço), sem literal de ticker
    - `pytest` (suíte default completa) VERDE: 0 failed, 0 xpassed, 0 xfailed, 1 skipped
  </acceptance_criteria>
  <verify>
    <automated>pytest -k "regressao_104 or ke_min_estrutural or blindagem_meta" && pytest</automated>
  </verify>
  <done>Os 104 tickers passam sem explosão nem divergência, com Ke setorial; guardrail provado por execução; BLIND-04a limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot local -> teste offline | fixture congelada dos 104 + artefato β setorial versionado; sem rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | test_ke_validacao (regressão) | accept | Validação offline sobre snapshot congelado e artefato versionado; sem input não confiável, auth, secrets ou rede. No new attack surface introduced by this change. |
</threat_model>

<verification>
- `pytest -k "ke_min_estrutural or regressao_104"` verde no estado final (ERP 0,045, sem clamp).
- `pytest` (suíte default) VERDE: 0 failed, 0 xpassed, 0 xfailed, 1 skipped (jackknife -> Fase 14).
- `pytest -m golden_nivel` sem CLASSIFICACAO ORFA.
- `pytest -k "blindagem_meta"` verde (o arquivo novo é BLIND-04a-safe na varredura AST).
- Gate de fase: suíte verde + regressão dos 104 verde + BLIND-02b como teste normal passando + `xfail_estritos()` == 0.
</verification>

<success_criteria>
Prova de execução de que a perpetuidade converge por aritmética (KE-04): desigualdade estrutural
`Ke_min > g_cap` asseverada sem cravar número, e os 104 tickers reais sem explosão/divergência, com o
Ke setorial idêntico ao do app. Nenhum guard novo. Suíte inteiramente verde.
</success_criteria>

<output>
Após completar, criar `.planning/phases/12-custo-de-capital-ke-ke/12-04-SUMMARY.md`.
</output>
