---
phase: 12-custo-de-capital-ke-ke
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - config.yaml
  - calibracao.lock.yaml
autonomous: true
requirements: [KE-02, KE-04]

must_haves:
  truths:
    - "capm.erp_local == 0.045 no config E no grau ERP do lock (mesmo commit sancionado)"
    - "motores.rim.erp_banco / ke_piso / ke_teto não existem no config nem no lock congelados — inclusive nenhuma MENÇÃO stale em comentário"
    - "O escopo do lock caiu de 29 -> 26 folhas; o orçamento continua em 3 graus (ERP, n_fade, PIB_real)"
  artifacts:
    - path: "config.yaml"
      provides: "erp_local 0.045; sem erp_banco/ke_piso/ke_teto no bloco motores.rim"
      contains: "erp_local: 0.045"
    - path: "calibracao.lock.yaml"
      provides: "grau ERP valor 0.045; sem os 3 congelados; comentários de contagem 26/23"
      contains: "0.045"
  key_links:
    - from: "config.yaml"
      to: "calibracao.lock.yaml"
      via: "diff de knob sancionado no MESMO commit com trailer Knob-Change-Justification"
      pattern: "erp_local"
    - from: "test_orcamento_de_knobs_e_exatamente_3"
      to: "partição folhas == graus | congelados"
      via: "config e lock mudam juntos"
      pattern: "erp_local"
---

<objective>
O commit de knob SANCIONADO (KE-02 + KE-04 lado-config): unificar o ERP em 4,5% e remover as folhas
do clamp do `config.yaml` E do `calibracao.lock.yaml` NO MESMO commit. É o único plano que toca
config/lock, e por causa do hook BLIND-05 ele NÃO pode conter nenhum arquivo `tests/*` — é um commit
atômico `config.yaml` + `calibracao.lock.yaml` apenas, com trailer `Knob-Change-Justification:`.

Purpose: unificar os dois prêmios de risco num só (o 2º ERP era sintoma, não design) e formalizar a
remoção do clamp no orçamento — o piso aritmético do Blume substitui a trava.
Output: `erp_local: 0.045`, `erp_banco`/`ke_piso`/`ke_teto` deletados, lock 29 -> 26 folhas,
orçamento intacto em 3 graus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-custo-de-capital-ke-ke/12-CONTEXT.md
@.planning/phases/12-custo-de-capital-ke-ke/12-RESEARCH.md
@CLAUDE.md
@.planning/phases/12-custo-de-capital-ke-ke/12-02-SUMMARY.md

<restricao_de_commit>
**HOOK BLIND-05 (VERIFICADO):** `.githooks/commit-msg` BLOQUEIA `config.yaml` + `tests/*` no mesmo
commit. `config.yaml` + `calibracao.lock.yaml` é o par SANCIONADO (o lock mora na raiz, fora de
`tests/`). Portanto este plano é UM commit com EXATAMENTE esses dois arquivos e nada mais. O trailer
`Knob-Change-Justification:` é obrigatório e **NUNCA menciona ticker** (teste `-k justificativa` +
hook validam contra `ticker_map.json`).

**Ordem (por que este plano vem DEPOIS do 02):** o Plano 02 já deletou o código do clamp, destravou
o BLIND-02b E reescreveu o único teste que fazia bracket-read de `ke_teto`
(`test_terminal_load_bearing`) — com a suíte verde. Aqui as folhas `erp_banco`/`ke_piso`/`ke_teto`
(que viraram config MORTO após o 02) saem do config/lock, e o ERP muda 0,06->0,045. Como o Plano 02
já removeu todo consumidor vivo dessas folhas, apagá-las NÃO quebra a coleta. A invariância do
BLIND-02b independe do NÍVEL do ERP (é sobre reatividade ao rf), então a suíte segue verde.
</restricao_de_commit>

<interfaces>
`config.yaml` — bloco capm (L72-96):
```yaml
capm:
  erp_local: 0.06    # L77  -> 0.045 (remover o texto do "+1,5% small-cap")
```
`config.yaml` — bloco motores.rim (L243-272): DELETAR as 3 folhas E scrubar a menção em comentário
```yaml
    erp_banco: 0.045   # L245  DELETAR (linha + comentário)
    ke_piso: 0.11      # L249  DELETAR (linha + comentário)
    ke_teto: 0.13      # L250  DELETAR (linha + comentário)
    ...
    ke_g_spread_min: 0.03   # L271-272  MANTER a folha, mas o comentário L272 cita "ke_piso=0,11"
                            # ("Com g=0,025 e ke_piso=0,11 o spread real é folgado") -> reescrever
                            # sem citar ke_piso (folha que deixa de existir).
```
(manter `n_fade`, `excesso_sustentavel`, `ke_g_spread_min` e demais do bloco — só a MENÇÃO a ke_piso sai.)

`calibracao.lock.yaml`:
```yaml
# L34: comentário "29 folhas (motores 10 + capm 12 + ddm 5 + normalizacao 2)"  -> 26 (motores 7 ...)
graus_de_liberdade:
  ERP:
    caminho: capm.erp_local
    valor: 0.06        # L62 -> 0.045
congelados:            # L127
  motores.rim.erp_banco: 0.045   # L130  DELETAR
  motores.rim.ke_piso: 0.11      # L132  DELETAR
  motores.rim.ke_teto: 0.13      # L134  DELETAR
# L122: "as outras 26 folhas do escopo (29 - 3 graus)"  -> "as outras 23 folhas (26 - 3 graus)"
# L140: "os 26 congelados ... das 29 folhas"             -> "os 23 congelados ... das 26 folhas"
```

Testes do lock (dinâmicos — ficam verdes se os dois lados mudarem juntos):
`test_orcamento_de_knobs_e_exatamente_3` (test_blindagem_orcamento.py:44) verifica a PARTIÇÃO
`folhas(escopo) == graus | congelados`. `test_knobs_batem_com_o_lock` (L119) verifica valor a valor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ERP 0,06 -> 0,045 + remover erp_banco/ke_piso/ke_teto (config + lock, mesmo commit)</name>
  <files>config.yaml, calibracao.lock.yaml</files>
  <read_first>
    - config.yaml:72-96 (bloco capm, erp_local L77) e config.yaml:243-272 (bloco motores.rim, incl. o comentário L271-272 de `ke_g_spread_min` que cita `ke_piso`)
    - calibracao.lock.yaml:1-100 (cabeçalho do orçamento, grau ERP L58-71) e L121-142 (congelados, contagens L34/L122/L140)
    - .planning/phases/12-custo-de-capital-ke-ke/12-RESEARCH.md §"Inventário do lock (D-10)" e §"Sequenciamento de commits"
    - CLAUDE.md ("qualquer knob de valuation exige mexer no lock no mesmo diff"; justificativa nunca menciona ticker)
  </read_first>
  <action>
    Em `config.yaml`: `capm.erp_local` 0.06 -> **0.045**; reescrever o comentário L77-79 removendo o
    "+1,5% de prêmio small-cap" (agora ERP = mercado maduro puro, Damodaran). DELETAR as três linhas
    `motores.rim.erp_banco`, `motores.rim.ke_piso`, `motores.rim.ke_teto` (e seus comentários), mantendo
    o resto do bloco `motores.rim`. **WARNING 4 do checker:** o comentário de `ke_g_spread_min`
    (L271-272) cita `ke_piso=0,11` na sua justificativa ("Com g=0,025 e ke_piso=0,11 o spread real é
    folgado") — reescrever esse comentário para NÃO citar `ke_piso` (a folha deixa de existir); manter
    a folha `ke_g_spread_min` e seu valor 0.03 intactos, só o texto muda. NO MESMO commit.
    Em `calibracao.lock.yaml`: no grau `ERP` (L60-71), `valor:` 0.06 -> **0.045** (remover a nota "NAO
    escrever o alvo aqui hoje" — o alvo agora é o estado de hoje). REMOVER as três entradas de
    `congelados` (`erp_banco`/`ke_piso`/`ke_teto`, L130-138). Atualizar os comentários de contagem
    STALE: L34 "29 folhas (motores 10 ...)" -> "26 folhas (motores 7 + capm 12 + ddm 5 + normalizacao
    2)"; L122 "as outras 26 folhas do escopo (29 - 3 graus)" -> "as outras 23 folhas (26 - 3 graus)";
    L140 "os 26 congelados ... das 29 folhas" -> "os 23 congelados ... das 26 folhas".
    Commitar SÓ estes dois arquivos, com o trailer (razão ECONÔMICA, sem ticker), ex.:
    `Knob-Change-Justification: ERP unificado em 4,5% (Damodaran mercado maduro); segundo premio de
    risco (erp_banco) e clamp estrutural (ke_piso/ke_teto) removidos — o piso aritmetico do Blume
    garante a convergencia da perpetuidade sem trava.`
    NÃO incluir nenhum arquivo `tests/*` neste commit (hook BLIND-05). NÃO recriar clamp com outro nome.
  </action>
  <acceptance_criteria>
    - `grep -c "erp_local: 0.045" config.yaml` == 1; `grep -c "erp_local: 0.06" config.yaml` == 0
    - `grep -Ec "erp_banco|ke_piso|ke_teto" config.yaml` == 0 (folhas E menções em comentário — inclui o scrub de `ke_piso` no comentário de `ke_g_spread_min`)
    - `grep -c "ke_g_spread_min" config.yaml` >= 1 (a folha permanece; só o texto mudou)
    - `grep -Ec "erp_banco|ke_piso|ke_teto" calibracao.lock.yaml` == 0
    - `grep -c "valor: 0.045" calibracao.lock.yaml` >= 1 (grau ERP)
    - `grep -Ec "29 folhas|29 -|das 29" calibracao.lock.yaml` == 0 (comentários stale atualizados p/ 26/23)
    - `pytest -k "orcamento or knobs_batem or justificativa"` PASSA (partição 26 == 3 graus | 23 congelados; valores casam)
    - `pytest` (suíte default) VERDE (BLIND-02b segue passando; invariância independe do nível do ERP; nenhum bracket-read vivo de ke_teto após o Plano 02)
    - O commit contém EXATAMENTE `config.yaml` + `calibracao.lock.yaml` e um trailer `Knob-Change-Justification:` sem ticker
  </acceptance_criteria>
  <verify>
    <automated>pytest -k "orcamento or knobs_batem or justificativa" && pytest</automated>
  </verify>
  <done>ERP 4,5% unificado; folhas do clamp removidas de config+lock (inclusive a menção stale a ke_piso no comentário de ke_g_spread_min); lock 29->26; orçamento em 3 graus; commit sancionado limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml -> engine | knobs de valuation lidos no boot; nenhuma borda de rede/input introduzida |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | config.yaml / lock | mitigate | O par config+lock é auditável no diff (BLIND-06) e travado por partição dinâmica; trailer obrigatório. No new attack surface: offline config change; no auth/secrets/network introduced. |
</threat_model>

<verification>
- `pytest -k "orcamento or knobs_batem or justificativa"` verde (partição e valores casam).
- `pytest` (suíte default) verde.
- Orçamento em 3 graus (ERP, n_fade, PIB_real); lock 29 -> 26 folhas; comentários de contagem coerentes.
- `grep -Ec "erp_banco|ke_piso|ke_teto" config.yaml calibracao.lock.yaml` == 0 (folhas E comentários).
- `git show --stat HEAD` do commit de knob lista SÓ `config.yaml` + `calibracao.lock.yaml`.
</verification>

<success_criteria>
ERP unificado em 4,5% (KE-02) e as folhas do clamp removidas de config+lock (KE-04 lado-config), num
único commit sancionado config+lock com justificativa econômica sem ticker, sem nenhuma menção stale
remanescente. Orçamento intacto em 3 graus; suíte verde.
</success_criteria>

<output>
Após completar, criar `.planning/phases/12-custo-de-capital-ke-ke/12-03-SUMMARY.md`.
</output>
