---
phase: 12-custo-de-capital-ke-ke
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/analista/report/report.py
  - src/analista/core/motores.py
  - tests/test_motores.py
  - tests/test_capm_local.py
  - tests/test_invariantes_v24.py
  - tests/test_blindagem_selecao.py
  - tests/test_beta_setorial.py
  - tests/classificacao.yaml
autonomous: true
requirements: [KE-01, KE-03, KE-04, KE-05]

must_haves:
  truths:
    - "Existe um único Ke: motores.ke_rim não existe mais; report.py:261 alimenta o RIM com o a.ke único"
    - "a.ke é computado com beta setorial+Blume e é O Ke exibido, com a matriz de sensibilidade em torno dele"
    - "A mesma ação tem Ke idêntico entre analyze e rank (fonte única carimbada, D-06)"
    - "BLIND-02b passa como teste normal (Ke reage ao rf sem clamp); xfail_estritos() == 0 e a guarda de seleção aceita a realidade pós-cura"
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "a.ke via beta_blume (L470); RIM lê a.ke (L261)"
      contains: "beta_blume"
    - path: "src/analista/core/motores.py"
      provides: "sem ke_rim (função deletada, clamp removido)"
    - path: "tests/test_invariantes_v24.py"
      provides: "test_invariancia_inflacao_engine_itub4 sem xfail — passa como invariante normal"
  key_links:
    - from: "src/analista/report/report.py:261"
      to: "a.ke"
      via: "RIM recebe o Ke único (não recomputa)"
      pattern: "ke=a\\.ke|ke = a\\.ke"
    - from: "src/analista/report/report.py:470"
      to: "capm.beta_blume"
      via: "a.ke usa o beta setorial+Blume carimbado"
      pattern: "beta_blume"
---

<objective>
Colapsar os DOIS Ke num só (KE-01/KE-05), aplicar o beta setorial+Blume ao `a.ke` (KE-03), remover
o clamp por CÓDIGO (deletar `motores.ke_rim`, KE-04 lado-código) e destravar o BLIND-02b (a Doença 3
morre porque o Ke volta a reagir ao rf). Reconciliar todos os testes que chamam `ke_rim` ou que
codificavam o método antigo, mais a guarda de seleção que assumia doenças pendentes.

Purpose: o Ke exibido passa a ser **o mesmo** que produz o número; a perpetuidade converge por
aritmética (piso do Blume), não por clamp.
Output: `a.ke` único (β setorial+Blume, ERP ainda 0,06 neste plano), RIM lendo `a.ke`, `ke_rim`
deletado, BLIND-02b como teste normal verde.

**Fronteira deste plano:** NÃO tocar `config.yaml` nem `calibracao.lock.yaml`. O ERP segue 0,06 e as
folhas `erp_banco`/`ke_piso`/`ke_teto` permanecem no config/lock como CONFIG MORTO (código deletado
não as lê; `test_knobs_batem_com_o_lock` e `test_orcamento` seguem verdes porque config e lock ainda
casam). A troca 0,06->0,045 + remoção dessas folhas é o Plano 03 (commit sancionado config+lock,
separado por causa do hook BLIND-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-custo-de-capital-ke-ke/12-CONTEXT.md
@.planning/phases/12-custo-de-capital-ke-ke/12-RESEARCH.md
@CLAUDE.md
@.planning/phases/12-custo-de-capital-ke-ke/12-01-SUMMARY.md

<correcoes_de_pesquisa>
**CORREÇÃO #1 (VERIFICADA):** o golden `ITUB4 = 32,88` **JÁ FOI DELETADO na Fase 10 (PRIM-05)**.
NÃO existe assert vivo de 32,88 para deletar aqui — **não planejar caça a ele**. D-12/ROADMAP-regra-B
restatam um critério HERDADO da Fase 10. Os goldens que a Fase 12 realmente mata são goldens de
**banda de Ke**, já em quarentena `golden_nivel` (morte agendada p/ Fase 12 em `classificacao.yaml`):
`test_ke_local_na_faixa_small_cap_br` (test_capm_local.py:55-66) e `test_ke_rim_na_banda_estrutural`
(test_motores.py:107-116). (A sub-banda de Ke da VULC3 já foi removida na Fase 11 — o assert vivo
`a.ke > 0.094` é RELACIONAL e SOBREVIVE.)

**CORREÇÃO #3 — BLOQUEADOR (MEDIDO):** há hoje **UM** único xfail estrito (BLIND-02b), NÃO dois
(D-13 diz "2->1" — ERRADO; BLIND-03 foi curado na Fase 10). Remover o marker leva `xfail_estritos()`
de **1 -> 0**, e **0 QUEBRA** `test_blindagem_selecao.py:100-104` (`doencas = set(h.xfail_estritos());
assert doencas, ...`). Isso NÃO é "só remover o marker": a guarda precisa ser reconciliada NO MESMO
diff — a premissa "as duas doenças estão escritas como código" deixa de valer porque AMBAS foram
curadas (BLIND-03 na Fase 10, BLIND-02b aqui). Adaptar a guarda à realidade pós-cura (0 doenças
pendentes é válido, desde que os invariantes normais sigam no run default). **É mudança de contrato
JUSTIFICADA PELO SISTEMA ESTAR CURADO — NÃO um afrouxamento.** PROIBIDO deletar o assert para ficar
verde; PROIBIDO trocar por skip; PROIBIDO afrouxar o detector.
</correcoes_de_pesquisa>

<interfaces>
Cálculo do a.ke — `src/analista/report/report.py:461-482`:
```python
cap = cfg["capm"]                                   # L462
a.beta = c.beta
if c.beta is not None:
    if cap.get("abordagem") == "local":
        a.ke = capm.ke_local(c.beta, cap["rf_local"], cap["erp_local"])   # L470  <- muda
    else:
        a.ke = capm.ke_eua_ajustada(c.beta, params)
if a.g_alto is not None and a.ke is not None:
    a.g_alto = min(a.g_alto, a.ke)                  # L481-482
```

Consumo pelo RIM — `src/analista/report/report.py:261`:
```python
ke=motores.ke_rim(c.beta, cfg),                     # <- vira ke=a.ke
```

Rota de segurança e matriz JÁ usam a.ke (nada a mudar):
```python
v_seg = ddm.valor_gordon(dpa_sust*(1+g_cap), a.ke, g_cap)   # L241
a.sensibilidade = ddm.matriz_sensibilidade(dpa_inicial, a.g_alto, n, g_cap, a.ke, ...)  # L540
L.append(f"- Beta: **{_num(a.beta)}**  |  Ke (CAPM): **{_pct(a.ke)}**")  # L982 (já exibe a.ke)
```

Função a DELETAR — `src/analista/core/motores.py:148-172` (`ke_rim`, com o clamp
`max(ke_piso, min(ke, ke_teto))`).

`capm.beta_blume(beta_cru, setor, mapa_setorial)` — criado no Plano 01 (never-raise, fallback D-04).

Testes que quebram a COLETA ao deletar ke_rim (test_motores.py) — TODOS chamam `motores.ke_rim`:
```python
def test_ke_rim_menor_que_ke_live_de_banco():  # L91-104  classificacao:350 = invariante "SOBREVIVE"
def test_ke_rim_na_banda_estrutural():         # L107-116 classificacao:351 = golden_nivel -> Fase 12
def test_ke_rim_never_raise():                 # L118-119 classificacao:352 = contrato
```

BLIND-02b — `tests/test_invariantes_v24.py:105-154`: decorator xfail (L105-112) a REMOVER; manter
`@pytest.mark.invariante`; `assert variacao < LIMIAR_INFLACAO` (0.05, L149) **NÃO MEXER**.

Guarda de seleção — `tests/test_blindagem_selecao.py:100-104`: `doencas = set(h.xfail_estritos());
assert doencas, "..."`. Guarda meta — `tests/test_blindagem_meta.py:68`: `tolerados =
h.quarentenados() | h.xfail_estritos()`.
</interfaces>

<colapso_verificado>
Prova do RESEARCH (sem resíduo): pós-Fase 12, `ke_rim(β) = rf + β×0,045` e `ke_local(β, rf, 0,045)
= rf + β×0,045` são IDÊNTICOS. O `min(ke, ke_live)` some sozinho (ke < ke_live para β>0). A única
diferença INTENCIONAL é o β de entrada: `a.ke` passa a usar β setorial+Blume (KE-03), então o Ke do
RIM MUDA DE NÍVEL — é a unificação, não resíduo. `ke_rim` é DELETADO, não sobrevive.
</colapso_verificado>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unificar o Ke — beta_blume no a.ke, deletar ke_rim, RIM lê a.ke</name>
  <files>src/analista/report/report.py, src/analista/core/motores.py</files>
  <read_first>
    - src/analista/report/report.py (L461-482 cálculo do a.ke; L230-269 rota de segurança + RIM; L539-543 matriz; L982 exibição)
    - src/analista/core/motores.py:148-172 (`ke_rim` — a deletar) e L258-268 (chamada `motores.rim(...)` com `ke=...`)
    - src/analista/core/capm.py (`beta_blume` do Plano 01, `ke_local`)
    - .planning/phases/12-custo-de-capital-ke-ke/12-RESEARCH.md §"Colapso ke_rim ≡ ke_local"
    - CLAUDE.md (never-raise nas bordas; NÃO reintroduzir clamp com outro nome)
  </read_first>
  <action>
    Em `report.py:470` (ramo `abordagem == "local"`): trocar o β cru pelo β setorial+Blume —
    `a.ke = capm.ke_local(capm.beta_blume(c.beta, c.setor, cap.get("beta_setorial")), cap["rf_local"],
    cap["erp_local"])`. Manter o ramo `eua_ajustada` intocado (legado). O guard L481-482
    (`a.g_alto = min(a.g_alto, a.ke)`) segue.
    Em `report.py:261`: trocar `ke=motores.ke_rim(c.beta, cfg)` por `ke=a.ke` (D-09: o RIM recebe o
    Ke único PRONTO, não recomputa). A rota de segurança L240-241 e a matriz L540 já leem `a.ke`.
    Deletar `motores.ke_rim` inteiro (motores.py:148-172), incluindo o clamp. **NÃO** criar guard
    substituto, **NÃO** clampar em outro nome — o piso do Blume garante `Ke_min > g_cap` por
    aritmética (provado no Plano 04). Se algum V explodir sem clamp, o bug é `ROE_T`/spread (Fase 13),
    não o Ke — sinalizar, não clampar.
    Confirmar que a exibição L982 e a matriz L540 usam `a.ke` (D-14: nenhum Ke diferente de `a.ke` é
    exibido) — já é o caso; nada a acrescentar.
  </action>
  <acceptance_criteria>
    - `grep -c "def ke_rim" src/analista/core/motores.py` == 0
    - `grep -c "ke_rim" src/analista/report/report.py` == 0
    - `grep -Ec "ke=a\.ke|ke = a\.ke" src/analista/report/report.py` >= 1 (L261 alimenta o RIM com a.ke)
    - `grep -c "beta_blume" src/analista/report/report.py` >= 1
    - Nenhum novo clamp: `grep -Ei "ke_teto|ke_piso|min\(ke|max\(.*ke" src/analista/report/report.py src/analista/core/motores.py` não introduz nova trava de Ke
    - `python -c "import ast,analista.core.motores"` importa sem erro (coleta não quebra por callers órfãos — reconciliados na Task 2)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "def ke_rim" src/analista/core/motores.py; grep -Ec "ke=a\.ke|ke = a\.ke" src/analista/report/report.py</automated>
  </verify>
  <done>Ke único: `ke_rim` deletado, RIM lê `a.ke` (β setorial+Blume), clamp removido, sem guard novo.</done>
</task>

<task type="auto">
  <name>Task 2: Reconciliar testes de ke_rim + testes de fórmula do Ke</name>
  <files>tests/test_motores.py, tests/test_capm_local.py, tests/test_beta_setorial.py, tests/classificacao.yaml</files>
  <read_first>
    - tests/test_motores.py (L91-119: os três testes que chamam `motores.ke_rim`)
    - tests/test_capm_local.py (L42-52 material_acima; L55-66 faixa_small_cap golden; L104-127 engine_offline_determinístico)
    - tests/classificacao.yaml (L66-67, L73, L350-352 — entradas a deletar/manter)
    - .planning/phases/12-custo-de-capital-ke-ke/12-RESEARCH.md §"Golden(s) a deletar / testes afetados" e Correção #1 e Open Question #2
    - CLAUDE.md (golden de nível DELETA, não atualiza; teste que muda classificação atualiza classificacao.yaml no mesmo diff; split-before-delete WR-04)
  </read_first>
  <action>
    **DELETAR (goldens de banda de Ke — Correção #1, DELETAR não atualizar) + suas entradas em
    classificacao.yaml no MESMO diff:**
    - `test_ke_local_na_faixa_small_cap_br` (test_capm_local.py:55-66) + classificacao.yaml:66. O
      invariante que morava aqui já foi extraído para `test_ke_local_materialmente_acima_do_ke_de_2019`
      (SOBREVIVE) — verificar antes de apagar (WR-04).
    - `test_ke_rim_na_banda_estrutural` (test_motores.py:107-116) + classificacao.yaml:351.
    - `test_ke_rim_never_raise` (test_motores.py:118-119) + classificacao.yaml:352 (a função deletada
      não tem mais never-raise a testar).
    **REESCREVER (Open Question #2 — resolver a contradição D-08 × classificacao:350):**
    - `test_ke_rim_menor_que_ke_live_de_banco` (test_motores.py:91-104) chama `motores.ke_rim`
      DELETADO. Reescrever como invariante da UNIFICAÇÃO: o Ke que alimenta o RIM É `a.ke` (via
      `analisar_acao`, comparando o Ke usado no RIM com `a.ke`), preservando o espírito "o Ke do RIM
      não excede o Ke ao vivo" — agora trivialmente verdadeiro porque são o MESMO. Atualizar o
      comentário de classificacao.yaml:350 (segue `invariante`, mas o texto "SOBREVIVE chamando
      ke_rim" está inconsistente — corrigir para a relação unificada).
    **ATUALIZAR (mudança de fórmula, NÃO crava nível):**
    - `test_engine_offline_ke_determinístico` (test_capm_local.py:104-127): `esperado` passa a ser
      `rf_local + capm.beta_blume(0.88, "Calçados", cfg["capm"].get("beta_setorial"))*erp_local`. Como
      "Calçados" não agrupa (n<3), cai no fallback individual: β_blume = 0,33+0,67×0,88. Continua
      `invariante` de pureza offline; atualizar a fórmula ao novo modelo é legítimo.
    - `test_ke_local_materialmente_acima_do_ke_de_2019` (test_capm_local.py:42-52): usar o β de
      entrada Blume-ajustado; o assert `ke > 0.094` deve seguir valendo (Ke novo ~12% > 9,4%).
      Verificar por execução; se falhar, é sinal do sistema, não afrouxar.
    Manter a completude de `classificacao.yaml` (teste sem entrada quebra a coleta).
  </action>
  <acceptance_criteria>
    - `pytest -k "ke_rim or capm_local or engine_offline_ke or beta_setorial"` PASSA (0 failed)
    - `grep -c "ke_rim" tests/test_motores.py` == 0 (nenhuma chamada à função deletada)
    - `grep -c "test_ke_local_na_faixa_small_cap_br\|test_ke_rim_na_banda_estrutural\|test_ke_rim_never_raise" tests/classificacao.yaml` == 0 (entradas removidas junto)
    - Nenhum número de nível novo asserido nos testes reescritos/atualizados (sem `assert ke == 0.1x` cravado)
    - `python -m pytest --collect-only -q 2>&1 | grep -c "CLASSIFICACAO ORFA"` == 0
  </acceptance_criteria>
  <verify>
    <automated>pytest -k "ke_rim or capm_local or engine_offline_ke or beta_setorial"</automated>
  </verify>
  <done>Zero caller órfão de `ke_rim`; goldens de banda deletados; testes de fórmula atualizados ao β Blume; coleta completa.</done>
</task>

<task type="auto">
  <name>Task 3: Destravar BLIND-02b + reconciliar a guarda de seleção (1 -> 0 xfail)</name>
  <files>tests/test_invariantes_v24.py, tests/test_blindagem_selecao.py, tests/classificacao.yaml</files>
  <read_first>
    - tests/test_invariantes_v24.py:105-154 (decorator xfail L105-112 a remover; assert L149 NÃO mexer)
    - tests/test_blindagem_selecao.py:96-109 (`doencas = set(h.xfail_estritos()); assert doencas`)
    - tests/test_blindagem_meta.py:50-79 (detector + `tolerados = quarentenados | xfail_estritos`)
    - tests/helpers_blindagem.py:271-325 (`detectar_ticker_com_valor_cravado`) e L444 (`xfail_estritos`)
    - .planning/phases/12-custo-de-capital-ke-ke/12-RESEARCH.md §"BLIND-02b" e Correção #3 e Pitfall 6
    - CLAUDE.md (NUNCA trocar xfail por skip, NUNCA afrouxar, NUNCA deletar assert para ficar verde)
  </read_first>
  <action>
    Remover APENAS o decorator `@pytest.mark.xfail(strict=True, reason=...)` de
    `test_invariancia_inflacao_engine_itub4` (test_invariantes_v24.py:105-112). Manter
    `@pytest.mark.invariante`. **NÃO** tocar `assert variacao < LIMIAR_INFLACAO`. O teste passa a
    asseverar a invariância de verdade e PASSA porque o clamp saiu (Task 1) e o Ke reage ao rf.
    Reconciliar a guarda de seleção (Correção #3, BLOQUEADOR): `xfail_estritos()` vai a 0 e
    `test_blindagem_selecao.py:100-104` (`assert doencas`) QUEBRA. Adaptar a guarda à realidade
    pós-cura — ambas as doenças (BLIND-03 na Fase 10, BLIND-02b aqui) estão CURADAS. Opção recomendada:
    converter a asserção em "0 doenças pendentes é válido pós-cura, DESDE QUE os ex-doenças rodem como
    invariantes normais no run default" — ou seja, verificar que `test_invariancia_inflacao_engine_itub4`
    (e `test_normalizacao_nao_pune_crescimento`) estão SELECIONADOS no run default e passando, em vez de
    exigir que sejam xfail. Atualizar o comentário L96-99 que ainda diz "as duas doenças são os
    xfail(strict=True)". É mudança de contrato JUSTIFICADA PELA CURA — documentar o racional no teste e
    no commit. PROIBIDO: deletar o assert, trocar por skip, afrouxar.
    Verificar `test_blindagem_meta`: com `xfail_estritos()` vazio, `tolerados = quarentenados()`. Se o
    detector AST passar a flagar o ex-BLIND-02b (tem literal "ITUB4" + limiar 0,05 chegando a assert),
    ele viraria "novo" ofensor e quebraria `test_blindagem_meta`. Se isso ocorrer, higienizar a
    NARRATIVA do teste (remover o literal de nível da mensagem/variáveis de forma que não seja
    `ticker == valor de nível`), **nunca** afrouxar o detector nem excluir arquivos da varredura.
    Se não flagar, nada a fazer aqui.
  </action>
  <acceptance_criteria>
    - `pytest -k "invariancia_inflacao or blindagem"` PASSA (0 failed, 0 xpassed)
    - `python -c "import tests.helpers_blindagem as h; print(len(h.xfail_estritos()))"` imprime `0`
    - `grep -c "xfail" tests/test_invariantes_v24.py` reflete a remoção (o marker de BLIND-02b não existe mais)
    - `test_invariancia_inflacao_engine_itub4` roda no run DEFAULT (não deselecionado) e passa
    - O `assert variacao < LIMIAR_INFLACAO` está INTACTO (`grep -c "variacao < LIMIAR_INFLACAO" tests/test_invariantes_v24.py` == 1)
    - `pytest` (suíte default COMPLETA) VERDE: 0 failed, 0 xpassed, 0 xfailed (BLIND-02b virou normal), 1 skipped (jackknife)
    - `pytest -m golden_nivel` sem `CLASSIFICACAO ORFA`
  </acceptance_criteria>
  <verify>
    <automated>pytest -k "invariancia_inflacao or blindagem" && pytest</automated>
  </verify>
  <done>BLIND-02b é teste normal verde; `xfail_estritos()` == 0; guarda de seleção reconciliada à cura (sem afrouxar); suíte default inteiramente verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cfg -> engine (a.ke) | o mapa β setorial e o rf são lidos de `cfg` (carimbados na borda); a engine é offline/determinística |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | report.py a.ke / RIM | accept | Cálculo offline sobre `cfg` local; sem input não confiável, auth, secrets ou rede. No new attack surface introduced by this change. |
| T-12-04 | DoS | beta_blume no a.ke | mitigate | never-raise (β None -> Ke None); a engine já degrada Ke None nos consumidores |
</threat_model>

<verification>
- `pytest` (suíte default) VERDE: 0 failed, 0 xpassed, 0 xfailed, 1 skipped.
- `pytest -m golden_nivel` sem CLASSIFICACAO ORFA.
- `grep -c "def ke_rim" src/analista/core/motores.py` == 0; `grep -c "ke_rim" tests/` sem chamadas vivas.
- `xfail_estritos()` == 0; BLIND-02b roda no default e passa.
- `git diff config.yaml calibracao.lock.yaml` VAZIO (o knob é o Plano 03).
- Se algum capstone/regressão (ex.: test_vulc3_regressao, test_capstone_*) quebrar por MUDANÇA DE
  NÍVEL do Ke: é golden de nível -> DELETAR + entrada de classificacao no mesmo diff, extraindo antes
  qualquer invariante estrutural preso (WR-04); NUNCA atualizar o número.
</verification>

<success_criteria>
Um único Ke no sistema (KE-01), computado com β setorial+Blume (KE-03), exibido == calculado com a
matriz em torno dele (KE-05), clamp removido por código (KE-04 lado-código) e BLIND-02b curado como
teste normal. Suíte default inteiramente verde. Config/lock intocados (Plano 03).
</success_criteria>

<output>
Após completar, criar `.planning/phases/12-custo-de-capital-ke-ke/12-02-SUMMARY.md`.
</output>
