---
phase: 12-custo-de-capital-ke-ke
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/ingest/macro.py
  - scripts/gerar_beta_setorial.py
  - data/beta_setorial.yaml
  - src/analista/core/capm.py
  - src/analista/cli.py
  - app.py
  - src/analista/backtest.py
  - tests/test_beta_setorial.py
  - tests/classificacao.yaml
autonomous: true
requirements: [KE-03]

must_haves:
  truths:
    - "Existe um artefato versionado data/beta_setorial.yaml com setor_normalizado -> mediana(beta cru), contendo SÓ setores com n>=3 betas"
    - "Os TRÊS entry points (cli, app, backtest) carimbam o mapa em cfg['capm']['beta_setorial'] — a engine nunca recomputa"
    - "capm.beta_blume aplica 0,33 + 0,67 x base UMA vez, com fallback ao beta individual e never-raise (None -> None)"
  artifacts:
    - path: "data/beta_setorial.yaml"
      provides: "Mapa setor_normalizado -> mediana(beta) derivado do universo real (n>=3)"
      contains: "Bancos"
    - path: "scripts/gerar_beta_setorial.py"
      provides: "Build script offline que gera o artefato a partir do snapshot limpo"
    - path: "src/analista/ingest/macro.py"
      provides: "mapa_beta_setorial, _normalizar_setor, carregar_beta_setorial, carimbar_beta_setorial"
      contains: "def mapa_beta_setorial"
    - path: "src/analista/core/capm.py"
      provides: "beta_blume(beta_cru, setor, mapa_setorial) never-raise"
      contains: "def beta_blume"
  key_links:
    - from: "src/analista/cli.py"
      to: "cfg['capm']['beta_setorial']"
      via: "carimbar_beta_setorial dentro de _carimbar_macro"
      pattern: "beta_setorial"
    - from: "src/analista/backtest.py"
      to: "_CHAVES_GLOBAIS"
      via: "adição de 'beta_setorial' às chaves globais do snapshot"
      pattern: "beta_setorial"
---

<objective>
Construir a INFRAESTRUTURA do beta setorial+Blume (KE-03), de forma **puramente aditiva**:
gerador offline, artefato versionado, carimbo nos três entry points e o helper `beta_blume`
na engine. Este plano **NÃO altera `a.ke`** — nada consome `beta_blume` ainda (a engine segue
lendo `capm.ke_local(c.beta, ...)`). A suíte fica idêntica, sem tocar config, lock ou nenhum
golden. O consumo do mapa (que muda o Ke) é o Plano 02.

Purpose: separar a montagem da fonte-única do beta setorial (rede/artefato na borda, engine pura)
da mudança de comportamento do Ke — a fronteira WR-03/D-05/D-06 que impede a mesma ação mostrar
Ke diferente entre `analyze` e `rank`.
Output: `data/beta_setorial.yaml`, o gerador em `ingest/macro.py`, `capm.beta_blume`, e o carimbo
`carimbar_beta_setorial` nos três sítios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-custo-de-capital-ke-ke/12-CONTEXT.md
@.planning/phases/12-custo-de-capital-ke-ke/12-RESEARCH.md
@CLAUDE.md

<correcao_de_pesquisa>
**CORREÇÃO #2 do RESEARCH (VERIFICADA):** o ponteiro `report/setup.py` do CONTEXT.md está ERRADO —
`report/setup.py` é o agregador de swing trade (`SetupSwing`), sem relação com macro. **NÃO carimbar
nada lá.** O carimbo de fonte única vive em TRÊS sítios e o mapa setorial precisa ser carimbado nos
três, senão `analyze`/`rank`/`backtest` DRIFTAM (D-06/WR-03):
- `cli.py` `_carimbar_macro` (L66-93) — chamado por `cmd_analyze` (L103) E `cmd_rank` (L177)
- `app.py` (L882-896) — bloco de carimbo Streamlit
- `backtest.py` (`_CHAVES_GLOBAIS` L33 + `rodar_cesta` L127-131)
</correcao_de_pesquisa>

<interfaces>
<!-- Padrão a espelhar (fonte única carimbada). Extraído do código. Use direto, sem explorar. -->

Sibling exato do gerador — `src/analista/ingest/macro.py`:
```python
def ipca_ciclo_para_g(fallback: float, anos: int = 10) -> float: ...   # L160
def selic_ciclo_para_capm(fallback: float, anos: int = 10) -> float: ... # L183
```

Carimbo CLI — `src/analista/cli.py:80-93` (`_carimbar_macro`):
```python
cfg["capm"]["rf_local"] = macro.selic_ciclo_para_capm(cfg["capm"]["selic_fallback"], anos)
cfg["macro"] = {**cfg.get("macro", {}), "ipca_deflatores": ..., "pi_ciclo": ...}
```

Carimbo Streamlit — `app.py:882-896`: escreve `CFG["capm"]["rf_local"]` e `CFG["macro"][...]`
via funções `@st.cache_data` (`rf_capm`/`ipca_deflatores_capm`/`pi_ciclo_capm`, L242-261).

Backtest — `src/analista/backtest.py`:
```python
_CHAVES_GLOBAIS = {"data_base", "rf_local", "ipca_deflatores"}   # L33
def rodar_cesta(empresas, fair_values, cfg, rf_local, ipca_deflatores=None):  # L112
    cfg = {**cfg, "capm": {**cfg.get("capm", {}), "rf_local": rf_local}, "macro": {...}}  # L127
```

Fonte offline do universo (104 tickers com `setor` + `beta`) — `tests/helpers_sanidade.py`:
```python
CAMINHO_SNAPSHOT_LIMPO = .../snapshot_sanidade_limpo_2026-07-15.yaml   # L31
def carregar_snapshot_sanidade(path=None) -> Dict[str, CompanyData]: ...  # L69
```

Normalização de string de setor (limite de palavra) — `src/analista/core/arquetipo.py:107`:
```python
def _setor_casa_token(setor: str, tokens: list) -> bool: ...
```

Fórmula única do Ke — `src/analista/core/capm.py:69`:
```python
def ke_local(beta_acao: float, rf_local: float, erp_local: float) -> float:
    return rf_local + beta_acao * erp_local
```
</interfaces>

<distribuicao_setorial>
Do RESEARCH (medido sobre `snapshot_sanidade_limpo_2026-07-15.yaml`, 104 tickers): normalizando o
prefixo "Emp. Adm. Part. - " e usando limiar **3**, agrupam 14 setores (fallback cai de 42 -> 24
tickers). Ex.: Energia Elétrica (mediana β 0,615), Bancos (1,216), Comércio (1,154). BB e Bradesco
já caem em "Bancos" mesmo na string crua.
</distribuicao_setorial>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gerador offline do mapa setorial + artefato versionado</name>
  <files>src/analista/ingest/macro.py, scripts/gerar_beta_setorial.py, data/beta_setorial.yaml, tests/test_beta_setorial.py, tests/classificacao.yaml</files>
  <read_first>
    - src/analista/ingest/macro.py (L143-195: `ipca_deflatores_anuais`, `ipca_ciclo_para_g`, `selic_ciclo_para_capm` — o padrão "derivado de fonte, degradação graciosa" a espelhar)
    - src/analista/core/arquetipo.py:107 (`_setor_casa_token`)
    - scripts/capturar_snapshot_limpo.py (L75-144: leitura do universo `data/ticker_map.json`, `_carregar_universo`, dump YAML — irmão do build script)
    - tests/helpers_sanidade.py (L27-90: `CAMINHO_SNAPSHOT_LIMPO`, `carregar_snapshot_sanidade`)
    - .planning/phases/12-custo-de-capital-ke-ke/12-RESEARCH.md §"Distribuição setorial" e §"Padrão de carimbo"
    - CLAUDE.md ("derivado, não digitado"; justificativa estrutural nunca menciona ticker)
  </read_first>
  <behavior>
    - `mapa_beta_setorial([], limiar=3)` -> `{}` (never-raise em universo vazio)
    - Setor com >=3 betas não-None -> chave presente, valor == mediana dos betas crus (não Blume)
    - Setor com <3 betas -> chave AUSENTE do mapa (cai no fallback da engine, D-04)
    - β `None` não entra na mediana (contagem é de betas disponíveis, não de tickers)
    - `_normalizar_setor("Emp. Adm. Part. - Energia Elétrica")` == `_normalizar_setor("Energia Elétrica")` (holding e operadora agrupam)
    - `carregar_beta_setorial()` lê `data/beta_setorial.yaml` e devolve dict setor->float
  </behavior>
  <action>
    Em `ingest/macro.py`, criar três funções puras (irmãs de `ipca_ciclo_para_g`):
    (1) `_normalizar_setor(setor)` — remove o prefixo "Emp. Adm. Part. - " (usar/replicar a
    disciplina de limite de palavra de `arquetipo._setor_casa_token`, dentro do D-01: só higieniza
    a representação, NÃO troca a chave `c.setor`); never-raise com setor None -> "".
    (2) `mapa_beta_setorial(empresas, limiar=3)` — agrupa por `_normalizar_setor(c.setor)`, coleta
    os `c.beta` não-None, calcula a **mediana** (D-02) e emite só setores com `n_betas >= limiar`.
    O limiar default 3 é ESTRUTURAL: é o menor n em que a mediana rejeita 1 outlier — a propriedade
    pela qual o D-02 escolheu a mediana. A justificativa é uma propriedade da mediana amostral,
    **nunca** um alvo de ticker.
    (3) `carregar_beta_setorial(path=None)` — lê `data/beta_setorial.yaml` (default) via PyYAML,
    devolve dict; arquivo ausente/vazio -> `{}` (degradação graciosa).
    Criar `scripts/gerar_beta_setorial.py` (irmão de `scripts/capturar_snapshot_limpo.py`): carrega
    as empresas via `helpers_sanidade.carregar_snapshot_sanidade` (OFFLINE, determinístico, MESMA
    fonte dos 104 do Plano 04), chama `mapa_beta_setorial`, faz dump YAML ordenado em
    `data/beta_setorial.yaml`. Rodar o script e commitar o artefato gerado (derivado, não digitado).
    Escrever `tests/test_beta_setorial.py` cobrindo o bloco <behavior>; adicionar as entradas
    correspondentes em `tests/classificacao.yaml` (classe `invariante`) NO MESMO commit (completude
    imposta na coleta).
  </action>
  <acceptance_criteria>
    - `pytest -k "beta_setorial" -x` PASSA (todos os testes do bloco behavior verdes)
    - `data/beta_setorial.yaml` existe e é um dict setor->float; `grep -c "Bancos" data/beta_setorial.yaml` >= 1
    - Nenhum setor com <3 betas aparece no artefato (verificado por um teste do parse do YAML)
    - `python -c "from analista.ingest import macro; assert macro.mapa_beta_setorial([])=={}"` não levanta
    - `grep -v '^#' tests/classificacao.yaml | grep -c "test_beta_setorial"` == número de funções de teste do arquivo (0 órfão na coleta)
    - `git diff --name-only config.yaml calibracao.lock.yaml` VAZIO (nenhum knob tocado)
  </acceptance_criteria>
  <verify>
    <automated>pytest -k "beta_setorial" -x</automated>
  </verify>
  <done>Gerador puro + artefato versionado + testes verdes; zero knob/golden tocado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: capm.beta_blume + carimbo nos três entry points</name>
  <files>src/analista/core/capm.py, src/analista/ingest/macro.py, src/analista/cli.py, app.py, src/analista/backtest.py, tests/test_beta_setorial.py, tests/classificacao.yaml</files>
  <read_first>
    - src/analista/core/capm.py (L69-71: `ke_local`; manter puro)
    - src/analista/cli.py:66-93 (`_carimbar_macro`) e L103/L177 (chamadas de analyze/rank)
    - app.py:242-261 (funções cacheadas) e L882-896 (bloco de carimbo)
    - src/analista/backtest.py:33 (`_CHAVES_GLOBAIS`) e L112-131 (`rodar_cesta`)
    - .planning/phases/12-custo-de-capital-ke-ke/12-RESEARCH.md §"Padrão de carimbo" (assinaturas recomendadas) e §Correção #2
  </read_first>
  <behavior>
    - `capm.beta_blume(0.615, "Energia Elétrica", {"Energia Elétrica": 0.615})` == `0.33 + 0.67*0.615` (usa o setorial)
    - `capm.beta_blume(0.88, "Calçados", {})` == `0.33 + 0.67*0.88` (fallback ao β individual, D-04)
    - `capm.beta_blume(None, "Bancos", mapa)` is None (never-raise)
    - `capm.beta_blume(0.88, None, mapa)` cai no fallback individual (setor None -> "")
    - `carimbar_beta_setorial(cfg)` grava `cfg["capm"]["beta_setorial"]` == `carregar_beta_setorial()`
  </behavior>
  <action>
    Em `capm.py` (mantendo `ke_local` intocado): criar `beta_blume(beta_cru, setor, mapa_setorial)`
    — resolve `base = mapa_setorial.get(macro._normalizar_setor(setor))` quando o mapa existe; se
    ausente, `base = beta_cru` (fallback D-04); aplica Blume `0.33 + 0.67*base` UMA vez (D-03); se
    `base is None`, devolve None (never-raise). NÃO importar rede; a normalização de setor vem de
    `ingest.macro` (ou duplicar a regra localmente se o import criar ciclo — decidir sem acoplar a
    engine à rede).
    Em `ingest/macro.py`: criar `carimbar_beta_setorial(cfg)` (irmão de `_carimbar_macro`) que grava
    `cfg["capm"]["beta_setorial"] = carregar_beta_setorial()`.
    Carimbar nos TRÊS sítios (Correção #2, NÃO `report/setup.py`):
    (a) `cli._carimbar_macro`: chamar `carimbar_beta_setorial(cfg)` (assim analyze L103 e rank L177
    recebem o MESMO mapa — D-06/WR-03).
    (b) `app.py` bloco L889-896: gravar `CFG["capm"]["beta_setorial"]` (via helper cacheado análogo
    a `rf_capm`, ou chamada direta a `carregar_beta_setorial()` — é leitura de arquivo local, sem rede).
    (c) `backtest.py`: adicionar `"beta_setorial"` a `_CHAVES_GLOBAIS` (L33) e injetá-lo na cópia do
    cfg em `rodar_cesta` (L127-131), degradando para `{}` quando ausente.
    Estender `tests/test_beta_setorial.py` com o bloco <behavior> de `beta_blume` e um teste de que
    os três carimbos gravam a MESMA chave; atualizar `classificacao.yaml`.
    **Aditivo:** nenhum consumidor lê `beta_blume` ainda; `a.ke` NÃO muda (é o Plano 02).
  </action>
  <acceptance_criteria>
    - `pytest -k "beta_setorial or capm" -x` PASSA
    - `grep -n "beta_setorial" src/analista/cli.py src/analista/backtest.py app.py` retorna match nos TRÊS arquivos
    - `grep -c "beta_setorial" src/analista/report/setup.py` == 0 (Correção #2: NÃO é lá)
    - `pytest` (suíte default) fica VERDE e com a MESMA contagem de antes do plano (nenhum golden/xfail mexido; `a.ke` inalterado)
    - `git diff --name-only config.yaml calibracao.lock.yaml` VAZIO
  </acceptance_criteria>
  <verify>
    <automated>pytest -k "beta_setorial or capm" -x && pytest</automated>
  </verify>
  <done>β setorial carimbado nos 3 entry points; `beta_blume` pronto e testado; suíte inalterada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script -> data/beta_setorial.yaml | artefato local versionado gerado offline do snapshot congelado |
| entry points -> engine (cfg) | mapa carimbado em `cfg`; a engine lê `cfg`, nunca a rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | data/beta_setorial.yaml | accept | Artefato derivado, versionado no git; regenerável pelo build script; sem input não confiável. No new attack surface: offline computation over versioned local data; no auth/secrets/network introduced by this change. |
| T-12-02 | DoS | carregar_beta_setorial | mitigate | Arquivo ausente/vazio -> `{}` (never-raise); a engine degrada para o β individual |
</threat_model>

<verification>
- `pytest -k "beta_setorial or capm"` verde.
- `pytest` (suíte default) verde e com contagem idêntica à de antes (mudança aditiva: `a.ke` não muda).
- Artefato `data/beta_setorial.yaml` presente, derivado do snapshot (não digitado).
- `git diff config.yaml calibracao.lock.yaml` VAZIO — orçamento de 3 knobs intacto (β setorial é DADO, fora do lock — D-07).
</verification>

<success_criteria>
Infra do KE-03 pronta e aditiva: gerador offline, artefato versionado, `beta_blume` testado, carimbo
nos três entry points. `a.ke` ainda NÃO consome o mapa (Plano 02). Suíte verde e inalterada.
</success_criteria>

<output>
Após completar, criar `.planning/phases/12-custo-de-capital-ke-ke/12-01-SUMMARY.md`.
</output>
