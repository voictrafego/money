---
phase: 10-crescimento-robusto-de-poison-do-screening
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/analista/core/screening.py
  - src/analista/core/comparables.py
autonomous: true
requirements: [GROW-02]
must_haves:
  truths:
    - "indicadores_bsd calcula crescimento de lucro/dividendos/FCO via o MESMO estimador log-linear sobre a serie NORMALIZADA (winsorizada), nao CAGR sobre serie CRUA"
    - "A serie de crescimento do BSD usa a serie completa winsorizada — consistente por construcao com o g_historico do Analisar (D-04)"
    - "A regressao de preco-alvo do Ranking clampa payout em [0,1] na ENTRADA do fit; payout_valuation() canonico permanece SEM clamp (D-03 Fase 9)"
    - "Fronteira per-ano CRU preservada: payout medio, cobertura, fc_sobre_lucro, var_tangivel, proxy cresc_lucro_lp e bandas REFERENCIA_BSD intactos (D-07)"
  artifacts:
    - path: "src/analista/core/screening.py"
      provides: "Fatores crescimento_lucro_3a/_dividendos_3a/_fc_3a via crescimento_log_linear sobre serie_winsorizada"
      contains: "crescimento_log_linear"
    - path: "src/analista/core/comparables.py"
      provides: "Clamp min(max(dp,0),1) no vetor DP que alimenta ajustar_regressao_pl (de-poison do fit)"
      contains: "ajustar_regressao_pl"
  key_links:
    - from: "src/analista/core/screening.py"
      to: "growth.crescimento_log_linear"
      via: "estimador sobre normalizacao.serie_winsorizada(c.serie(attr))"
      pattern: "crescimento_log_linear\\("
    - from: "src/analista/core/comparables.py"
      to: "fit OLS"
      via: "dp clampado em [0,1] antes de montar X em ajustar_regressao_pl"
      pattern: "min\\(max\\("
---

<objective>
Entrega GROW-02 (de-poison do Garimpo/Ranking) + resolve o efeito cruzado da Fase 9:

1. **Garimpo (BSD):** os tres fatores de crescimento de serie (`crescimento_lucro_3a`,
   `crescimento_dividendos_3a`, `crescimento_fc_3a`) passam a vir do MESMO estimador log-linear
   do Plan 01, aplicado sobre a serie NORMALIZADA (winsorizada) de cada atributo — um ano
   extraordinario de lucro/provento/caixa deixa de envenenar o BSD (D-04, D-05). Como o lucro usa
   a mesma serie winsorizada do Analisar, o `crescimento_lucro_3a` do screening = o `g_historico`
   do Analisar por construcao (fonte unica, D-04).

2. **Ranking (preco-alvo):** clampa o payout em [0,1] na ENTRADA do AJUSTE da regressao P/L. Hoje a
   PREVISAO ja clampa (comparables.py L148), mas o VETOR DP que alimenta `ajustar_regressao_pl`
   (cli.py:154 / app.py:468) entra SEM clamp — e ai que TAEE11 ~= 2.16 (payout sem clamp da Fase 9,
   D-03) envenena os coeficientes b1 (per 09-CROSS-EFFECT-FASE10.md). D-06.

Purpose: a mesma empresa nao pode ranquear barata por um g cru inflado num unico ano, nem ter o
preco-alvo distorcido por um payout legitimamente >100% poluindo o fit.

Output: screening.py (3 fatores via log-linear/winsorizado) + comparables.py (clamp no fit). NAO
toca payout_valuation()/mediana_payout (D-03 Fase 9) nem a fronteira per-ano CRU (D-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-crescimento-robusto-de-poison-do-screening/10-CONTEXT.md
@.planning/phases/10-crescimento-robusto-de-poison-do-screening/10-PATTERNS.md
@.planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-CROSS-EFFECT-FASE10.md

<interfaces>
<!-- Contratos extraidos do codebase. Use direto. -->

Estimador (criado no Plan 01) — src/analista/core/growth.py:
- def crescimento_log_linear(serie: Sequence[float]) -> Number   # None se len<2 ou qualquer ponto<=0

Primitiva de winsorizacao (read-only, reusar) — src/analista/core/normalizacao.py:
- def serie_winsorizada(valores: Sequence[Number], winsor=0.10) -> List[float]   # L94; _limpar (descarta None) interno; <5 pontos validos -> devolve os limpos sem winsorizar

Serie ordenada por ano — src/analista/core/fundamentals.py:
- def serie(self, attr: str, anos=None) -> List[float]   # L62; valores na ordem dos anos, pula faltantes

screening.indicadores_bsd (src/analista/core/screening.py) — estado atual a trocar:
- import L18: `from . import growth`  (adicionar normalizacao)
- L211: anos = c.anos_ordenados()[-anos_media:]   # JANELA 3a — usada por payout/cobertura/fc/var_tangivel/proxy: NAO mexer
- L247: var_tangivel = growth.cagr(...)            # fora de escopo, fica CAGR (D-07)
- L256-258: proxy cresc_lucro_lp via crescimento_por_fundamentos(roe_medio, payout_medio) CRU: NAO mexer
- L260-264: closure cagr_serie(d) -> growth.cagr(d.get(anos[0]), d.get(anos[-1]), len(anos)-1)   # SUBSTITUIR
- L274-276: crescimento_fc_3a / _dividendos_3a / _lucro_3a = cagr_serie(c.fco/c.dividendos/c.lucro_liquido)   # SUBSTITUIR
- L191-202: REFERENCIA_BSD (bandas, inclui chaves crescimento_*_3a e payout (0.0,0.80)): NAO renomear/mexer

comparables.ajustar_regressao_pl (src/analista/core/comparables.py L96-118):
- def ajustar_regressao_pl(pl, dp, roe) -> Optional[RegressaoPL]
- L104-108: linhas = [(p,d,r) for p,d,r in zip(pl,dp,roe) if None not in (p,d,r) and p>0]
- L112: X = np.array([[1.0, l[1], l[2]] ...])   # l[1] = dp -> ponto a clampar
comparables.preco_alvo_por_regressao (L133): JA clampa dp na previsao (L148 dp_clamp) — NAO mexer.
Call sites espelhados (FIX-04): cli.py:154/158-159 e app.py:468/472 (ambos chamam as duas funcoes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Screening — 3 fatores de crescimento via log-linear sobre serie winsorizada</name>
  <files>src/analista/core/screening.py</files>
  <read_first>
    - src/analista/core/screening.py L180-277 (REFERENCIA_BSD + indicadores_bsd inteiro)
    - src/analista/core/normalizacao.py L94-105 (serie_winsorizada — _limpar interno, comportamento <5 pontos)
    - src/analista/core/fundamentals.py L62-66 (serie(attr) — serie ordenada por ano)
    - src/analista/core/growth.py (crescimento_log_linear do Plan 01)
    - .planning/phases/10-crescimento-robusto-de-poison-do-screening/10-CONTEXT.md (D-04, D-05, D-07)
  </read_first>
  <action>
    Adicionar `from . import normalizacao` aos imports de screening.py (junto do `from . import
    growth`, L18). Em `indicadores_bsd`, substituir o closure `cagr_serie` (L260-264) por um helper
    que, para um dado atributo, monta a serie COMPLETA ordenada por ano via `c.serie(attr)`,
    winsoriza via `normalizacao.serie_winsorizada(...)` (D-05) e estima via
    `growth.crescimento_log_linear(...)` (D-04). Usar a serie COMPLETA (todos os anos), NAO a janela
    `anos` de 3 anos (L211): com <5 pontos a winsorizacao nao morde, entao a janela 3a tornaria D-05
    inocuo; a serie completa winsorizada e o que faz o lucro do screening coincidir com o
    g_historico do Analisar por construcao (D-04). Aplicar aos TRES atributos
    (`lucro_liquido`, `dividendos`, `fco`) nas chaves `crescimento_lucro_3a` / `crescimento_dividendos_3a`
    / `crescimento_fc_3a` (L274-276). Manter as chaves com o sufixo `_3a` (NAO renomear — sao chaves
    de REFERENCIA_BSD, L191-202). PRESERVAR intactos (D-07): `payout` medio (L218), `cobertura`
    (L221-226), `fc_sobre_lucro` (L229-234), `var_tangivel` via growth.cagr (L246-247 — fica CAGR),
    o proxy `cresc_lucro_lp` via crescimento_por_fundamentos com roe/payout CRU medios (L254-258), a
    janela `anos` (L211) e TODAS as bandas REFERENCIA_BSD. Atualizar o comentario L260 ("CAGR") para
    refletir log-linear sobre serie normalizada.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -m pytest tests/test_screening.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from . import normalizacao" src/analista/core/screening.py` retorna 1 match.
    - `grep -n "crescimento_log_linear" src/analista/core/screening.py` retorna >=1 match (usado nos 3 fatores).
    - `grep -n "serie_winsorizada" src/analista/core/screening.py` retorna >=1 match.
    - indicadores_bsd NAO usa mais growth.cagr para os fatores de serie: `grep -n "cagr_serie" src/analista/core/screening.py` vazio.
    - var_tangivel ainda usa CAGR: `grep -n "growth.cagr(tangivel" src/analista/core/screening.py` retorna 1 match (D-07).
    - REFERENCIA_BSD ainda contem as chaves `crescimento_lucro_3a`/`crescimento_dividendos_3a`/`crescimento_fc_3a` e `payout` (sem rename): `grep -c "crescimento_.*_3a\|\"payout\"" src/analista/core/screening.py` >= 4.
    - Consistencia por construcao: para uma CompanyData de teste, `indicadores_bsd(c)["crescimento_lucro_3a"]` == `report.analisar_acao(c, cfg).g_historico` (ambos = crescimento_log_linear sobre serie_winsorizada do lucro) — checar manualmente ou via assert no Plan 03.
    - pytest tests/test_screening.py sai 0 (divergencias de valor -> rebaseline deliberado no Plan 03, nao aqui).
  </acceptance_criteria>
  <done>Os 3 fatores de crescimento de serie do BSD vem do estimador log-linear sobre serie completa winsorizada; per-ano CRU, var_tangivel CAGR, proxy e bandas REFERENCIA_BSD preservados; suite de screening verde ou divergencias anotadas.</done>
</task>

<task type="auto">
  <name>Task 2: De-poison do fit da regressao P/L (clamp do payout na entrada, D-06)</name>
  <files>src/analista/core/comparables.py</files>
  <read_first>
    - src/analista/core/comparables.py L96-163 (ajustar_regressao_pl + preco_alvo_por_regressao — ver o clamp L148 ja existente na PREVISAO)
    - .planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-CROSS-EFFECT-FASE10.md (origem do poison: fit calibrado para payout em [0,1])
    - .planning/phases/10-crescimento-robusto-de-poison-do-screening/10-CONTEXT.md (D-06)
    - src/analista/cli.py L149-159 e app.py L466-472 (call sites espelhados — confirmar que clampar no fit cobre ambos sem editar os dois)
  </read_first>
  <action>
    Em `ajustar_regressao_pl` (comparables.py L96-118), clampar o componente de payout `dp` ao
    intervalo [0,1] ANTES de montar a matriz de design — i.e., no list-comprehension que monta
    `linhas` (L104-108) ou imediatamente antes do `np.array([[1.0, l[1], l[2]] ...])` (L112),
    substituir o `d`/`l[1]` por `min(max(d, 0.0), 1.0)` (mesmo padrao do clamp da previsao em L148,
    `min(max(x,0.0),1.0)`). Manter o filtro `None not in (p,d,r) and p>0`. PONTO DE DECISAO RESOLVIDO
    (per guidance + 10-PATTERNS.md L141-154): clampar AQUI, no fit, e nao apenas na previsao —
    `preco_alvo_por_regressao` (L148) JA clampa a previsao por-empresa, mas o VETOR DP que ALIMENTA o
    fit (cli.py:154 / app.py:468) entrava SEM clamp e e a origem REAL do envenenamento de b1
    (TAEE11 ~= 2.16). Clampar dentro de `ajustar_regressao_pl` e FONTE UNICA: cobre cli e app de uma
    vez (FIX-04), sem duplicar clamp nos dois call sites. NAO reintroduzir clamp em `payout_valuation()`
    (fundamentals.py L77) nem `mediana_payout` (normalizacao.py L78) — D-03 da Fase 9 preservado, a
    mediana pode ser legitimamente >100%. Adicionar comentario curto citando 09-CROSS-EFFECT-FASE10.md
    e D-06 explicando por que o clamp vive no fit (a regressao foi calibrada com payout em [0,1]).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -m pytest tests/test_comparables.py tests/test_consistencia_modos.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "min(max(" src/analista/core/comparables.py` retorna >=2 matches (o clamp ja existente da previsao L148 + o novo no fit).
    - payout_valuation NAO ganhou clamp: `grep -n "min(.*1.0)" src/analista/core/fundamentals.py` continua sem clamp em payout_valuation (so a mediana, D-03).
    - Propriedade do de-poison (checar via python -c ou assert no Plan 03): ajustar_regressao_pl com um payout=2.16 numa das linhas produz coeficientes IGUAIS a se essa linha tivesse payout=1.0 (o fit nao "ve" >1.0). Ex.: dois fits identicos exceto um dp=2.16 vs dp=1.0 -> mesmos coeficientes.
    - pytest tests/test_comparables.py tests/test_consistencia_modos.py sai 0 (divergencias -> rebaseline no Plan 03).
  </acceptance_criteria>
  <done>O vetor de payout que alimenta o fit da regressao P/L e clampado em [0,1] em fonte unica (ajustar_regressao_pl), cobrindo cli e app; payout_valuation()/mediana_payout canonicos seguem sem clamp; TAEE11 ~=2.16 deixa de envenenar os coeficientes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| series CVM/Yahoo -> screening/regressao | Entradas numericas de dados publicos. Sem rede/auth/input de usuario novo (refactor de calculo puro). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Denial of Service (numerico) | indicadores_bsd / serie_winsorizada -> crescimento_log_linear | mitigate | A fronteira de None do estimador (qualquer ponto<=0/len<2 -> None) e o `_limpar` de serie_winsorizada (descarta None) impedem log de nao-positivo no fator de crescimento. |
| T-10-05 | Tampering (envenenamento de modelo) | ajustar_regressao_pl | mitigate | Clamp de payout em [0,1] no fit impede que um payout legitimamente >100% (entrada valida mas fora do dominio de calibracao) distorca os coeficientes da regressao P/L. |
| T-10-06 | Information Disclosure | — | accept | N/A: dados publicos, sem PII/segredos, app local. |
</threat_model>

<verification>
- pytest tests/test_screening.py, tests/test_comparables.py, tests/test_consistencia_modos.py verdes OU divergencias anotadas para rebaseline deliberado (Plan 03).
- screening.crescimento_lucro_3a coincide com report.g_historico para a mesma CompanyData (fonte unica D-04).
- Fit da regressao insensivel a payout >1.0 (clamp); payout_valuation()/mediana_payout sem clamp.
</verification>

<success_criteria>
- BSD calcula crescimento de lucro/dividendos/FCO via log-linear sobre serie completa winsorizada (D-04, D-05); per-ano CRU + bandas + proxy preservados (D-07).
- Regressao de preco-alvo de-poisoned no fit (D-06) sem reintroduzir clamp no canonico (D-03 Fase 9).
</success_criteria>

<output>
After completion, create `.planning/phases/10-crescimento-robusto-de-poison-do-screening/10-02-SUMMARY.md`
</output>
