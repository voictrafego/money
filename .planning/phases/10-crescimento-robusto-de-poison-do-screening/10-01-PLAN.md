---
phase: 10-crescimento-robusto-de-poison-do-screening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/core/growth.py
  - src/analista/report/report.py
  - tests/test_growth.py
autonomous: true
requirements: [GROW-01]
must_haves:
  truths:
    - "O g_historico exibido vem de regressao log-linear sobre a serie de lucro NORMALIZADA, nao mais CAGR endpoint-a-endpoint"
    - "Um unico ano de fundo/topo deixa de mandar no g (usa todos os pontos da serie)"
    - "Serie com qualquer ano <= 0 (prejuizo) retorna None — fronteira de None do CAGR preservada (D-03)"
  artifacts:
    - path: "src/analista/core/growth.py"
      provides: "Estimador puro crescimento_log_linear(serie) -> Number (irmao de cagr/crescimento_aritmetico)"
      contains: "def crescimento_log_linear"
    - path: "src/analista/report/report.py"
      provides: "g_historico calculado pelo estimador log-linear sobre serie_lucro_normalizada()"
      contains: "growth.crescimento_log_linear"
    - path: "tests/test_growth.py"
      provides: "Golden unitario do estimador (slope conhecido, fronteira de None)"
      contains: "crescimento_log_linear"
  key_links:
    - from: "src/analista/report/report.py"
      to: "growth.crescimento_log_linear"
      via: "chamada sobre lucros = serie_lucro_normalizada() (report.py ~L79)"
      pattern: "growth\\.crescimento_log_linear\\(lucros\\)"
    - from: "src/analista/core/growth.py"
      to: "numpy.polyfit"
      via: "OLS de ln(serie) vs tempo"
      pattern: "polyfit"
---

<objective>
Entrega GROW-01: substitui o CAGR endpoint-a-endpoint do `g_historico` por uma estimativa
robusta (regressao log-linear) sobre a serie de lucro JA normalizada. Um unico exercicio de
fundo/topo deixa de mandar no g exibido.

Purpose: o `g_historico` exibido na aba Analisar deve refletir a TRAJETORIA do lucro
normalizado, nao o salto entre o primeiro e o ultimo ponto (sensivel a um ano atipico).
Este plano cria o estimador puro que tambem sera reusado pelo screening (Plan 02) — a fonte
unica Analisar<->Screening nasce aqui (D-04).

Output: nova funcao pura `crescimento_log_linear` em `growth.py` (com golden unitario) +
swap in-place em `report.py` (g_historico). `g_alto`/`g_fundamentos`/per-ano CRU intactos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-crescimento-robusto-de-poison-do-screening/10-CONTEXT.md
@.planning/phases/10-crescimento-robusto-de-poison-do-screening/10-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa. Extraidos do codebase. Use direto — sem exploracao. -->

Estimadores existentes (padrao a espelhar) — src/analista/core/growth.py:
- Number = Optional[float]  (alias de retorno, L13)
- def cagr(valor_inicial, valor_final, n_periodos) -> Number  (L16; None se base/ponta <= 0)
- def crescimento_aritmetico(serie: Sequence[float]) -> Number  (L32; recebe a serie inteira)
- growth.py hoje NAO importa numpy — adicionar `import numpy as np` no topo.
- DISCIPLINA DE IMPORT (ciclo): screening.py importa growth (L18), report.py importa growth (L16).
  O estimador novo NAO pode importar de fundamentals/report/screening. So numpy/statistics.

Serie normalizada ja em escopo — src/analista/core/fundamentals.py:
- def serie_lucro_normalizada(self, winsor=0.10) -> List[float]  (L126; = serie_winsorizada(serie("lucro_liquido")))

Site de chamada a trocar — src/analista/report/report.py (analisar_acao, ~L76-79):
  lucros_raw = c.serie("lucro_liquido")           # CRU — D-07, NAO tocar
  lucros = c.serie_lucro_normalizada()            # serie winsorizada (ja existe)
  if len(lucros) >= 2:
      a.g_historico = growth.cagr(lucros[0], lucros[-1], len(lucros) - 1)   # <- L79, trocar
Downstream a PRESERVAR: g_alto (L93-98, piso max(0, min(g,0.25)) em L97), g_fundamentos (L82),
lucros_raw / lucro_positivo / lucro_decrescente (L103-104, leem a serie CRUA).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estimador puro crescimento_log_linear em growth.py</name>
  <files>src/analista/core/growth.py, tests/test_growth.py</files>
  <read_first>
    - src/analista/core/growth.py (todo — espelhar cagr/crescimento_aritmetico: assinatura, alias Number, guard-clauses, docstring citando o Cap. 14)
    - src/analista/core/normalizacao.py L24-31 (padrao `import numpy as np` + alias Number para modulo numerico puro)
    - .planning/phases/10-crescimento-robusto-de-poison-do-screening/10-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <behavior>
    - Serie estritamente crescente em PG (ex.: [100,110,121,133.1] = +10%/ano): g ~= 0.10 (exp(slope)-1).
    - Serie constante [500,500,500,500]: slope 0 -> g == 0.0 (nao None).
    - Serie com 1 ponto ou None ou len<2: retorna None.
    - Serie com QUALQUER ano <= 0 (ex.: [100, -5, 120]): retorna None (ln indefinido em prejuizo, D-03).
    - Serie decrescente [1000,900,810,729] (-10%/ano): g ~= -0.10 (negativo permitido, pontos > 0).
  </behavior>
  <action>
    Adicionar `import numpy as np` no topo de growth.py (hoje ausente). Criar a funcao pura
    `crescimento_log_linear(serie: Sequence[float]) -> Number`, irma de `crescimento_aritmetico`
    (recebe a serie inteira, devolve Number). Fronteira de None IDENTICA ao CAGR (D-03): retornar
    None se `serie is None`, `len(serie) < 2`, OU qualquer ponto for None ou <= 0. Nucleo (D-01):
    OLS de ln(serie) contra o tempo — x = 0..n-1, slope = primeiro coeficiente de
    `numpy.polyfit(x, numpy.log(serie), 1)`; g anualizado = `exp(slope) - 1` (anualizado por
    construcao, passo de x = 1 ano). NAO introduzir fallback aritmetico que mudaria a fronteira de
    None (D-03 — risco de regressao golden). Docstring no estilo do modulo: Cap. 14, "tendencia de
    crescimento" (explicabilidade, D-01); registrar que e o estimador robusto que substitui o CAGR
    endpoint-a-endpoint e que sera reusado pelo screening (fonte unica, D-04). Theil-Sen / media
    aparada de YoY foram rejeitados (D-02) — nao implementar. Criar tests/test_growth.py cobrindo os
    casos do bloco <behavior> com asserts numericos (tolerancia ~1e-6 para PG exata; tolerancia
    folgada onde a winsorizacao nao se aplica — aqui a serie e passada crua ao estimador).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -m pytest tests/test_growth.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def crescimento_log_linear" src/analista/core/growth.py` retorna 1 match.
    - `grep -n "import numpy as np" src/analista/core/growth.py` retorna 1 match.
    - growth.py NAO contem import de fundamentals/report/screening (sem ciclo): `grep -E "from \.+ ?import .*(fundamentals|report|screening)" src/analista/core/growth.py` vazio.
    - `python -c "from analista.core import growth; print(growth.crescimento_log_linear([100,-5,120]))"` imprime `None`.
    - `python -c "from analista.core import growth; g=growth.crescimento_log_linear([100,110,121,133.1]); print(round(g,4))"` imprime `0.1`.
    - pytest tests/test_growth.py sai com codigo 0.
  </acceptance_criteria>
  <done>Estimador log-linear puro existe, sem ciclo de import, com golden unitario verde cobrindo PG exata, serie constante, decrescente, ano <=0 -> None e len<2 -> None.</done>
</task>

<task type="auto">
  <name>Task 2: Swap do g_historico em report.py (CAGR -> log-linear)</name>
  <files>src/analista/report/report.py</files>
  <read_first>
    - src/analista/report/report.py L72-107 (bloco Crescimento + ciclo de vida; ver onde lucros/lucros_raw sao usados)
    - src/analista/core/growth.py (a funcao crescimento_log_linear recem-criada na Task 1)
    - tests/test_growth_reconciliacao.py (asserts de desigualdade sobre a.g_historico — confirmar que NAO afrouxam)
  </read_first>
  <action>
    Em report.py, no bloco Crescimento (~L78-79), trocar
    `a.g_historico = growth.cagr(lucros[0], lucros[-1], len(lucros) - 1)` por
    `a.g_historico = growth.crescimento_log_linear(lucros)` — passando a serie INTEIRA `lucros`
    (que ja e `serie_lucro_normalizada()`, L77; nenhuma mudanca de fonte de dados). Manter o guard
    `if len(lucros) >= 2:`. NAO tocar: `lucros_raw = c.serie("lucro_liquido")` (L76, CRU — D-07);
    `g_fundamentos` (L82); a precedencia/teto de `g_alto` (L93-98, incl. piso `max(0.0, min(g_alto,
    0.25))` em L97 — D-03 confia nele para o downstream de payout>=100%); os fatos per-ano
    `lucro_positivo`/`lucro_decrescente` (L103-104, leem a serie CRUA). Ajustar o comentario do bloco
    (L72-75) de "CAGR sobre a serie ... NORMALIZADA" para "tendencia log-linear (regressao de
    ln(lucro)) sobre a serie ... NORMALIZADA", mantendo a explicacao da fronteira CRU per-ano.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -m pytest tests/test_growth_reconciliacao.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "growth.crescimento_log_linear(lucros)" src/analista/report/report.py` retorna 1 match.
    - report.py NAO chama mais growth.cagr para g_historico: `grep -n "a.g_historico = growth.cagr" src/analista/report/report.py` vazio.
    - `grep -n "lucros_raw = c.serie(\"lucro_liquido\")" src/analista/report/report.py` ainda presente (serie CRUA per-ano preservada).
    - `grep -n "max(0.0, min(g_alto, 0.25))" src/analista/report/report.py` ainda presente (piso g_alto intacto).
    - pytest tests/test_growth_reconciliacao.py (asserts de DESIGUALDADE sobre a.g_historico — sobrevivem ao swap em series monotonas) sai 0. Golden de VALOR EXATO (test_report.py e quaisquer outros) NAO sao gate aqui: sao revalidados/rebaselinados DELIBERADAMENTE no 10-03/T2. Sem gate contraditorio neste plano.
  </acceptance_criteria>
  <done>g_historico usa o estimador log-linear sobre a serie normalizada inteira; g_alto/g_fundamentos/per-ano CRU intactos; desigualdades de test_growth_reconciliacao.py verdes. Golden de valor exato sao revalidados/rebaselinados no 10-03/T2 (gate movido para la — sem contradicao aqui).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serie de lucro -> estimador | Entrada numerica derivada de dados publicos (CVM/Yahoo). Nenhuma rede, auth ou input de usuario novo nesta fase (refactor de calculo puro, app local Streamlit). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Denial of Service (numerico) | crescimento_log_linear | mitigate | Guard-clauses no topo: serie None/len<2/qualquer ponto<=0 -> None ANTES de np.log/polyfit; evita log de nao-positivo (nan/-inf) e polyfit sobre entrada degenerada. |
| T-10-02 | Tampering | growth.py (import cycle) | mitigate | Estimador so importa numpy/statistics; sem import de fundamentals/report/screening (verificado por grep no acceptance_criteria), evitando carga de codigo nao-pura na primitiva. |
| T-10-03 | Information Disclosure | — | accept | N/A: dados exclusivamente publicos, sem PII/segredos, app local sem superficie de rede nova. |
</threat_model>

<verification>
- pytest tests/test_growth.py verde (golden do estimador).
- pytest tests/test_report.py e tests/test_growth_reconciliacao.py verdes OU divergencias de valor anotadas para rebaseline deliberado no Plan 03.
- Nenhum import novo alem de numpy em growth.py; sem ciclo.
</verification>

<success_criteria>
- `growth.crescimento_log_linear` existe, puro, com fronteira de None identica ao CAGR (D-03).
- report.py calcula g_historico via log-linear sobre serie_lucro_normalizada() (D-01); g_alto/g_fundamentos/per-ano CRU preservados (D-07).
- Estimador pronto para reuso pelo screening (Plan 02) — fonte unica Analisar<->Screening (D-04).
</success_criteria>

<output>
After completion, create `.planning/phases/10-crescimento-robusto-de-poison-do-screening/10-01-SUMMARY.md`
</output>
