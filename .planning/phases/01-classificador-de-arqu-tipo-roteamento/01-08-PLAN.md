---
phase: 01-classificador-de-arqu-tipo-roteamento
plan: 08
type: tdd
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - src/analista/cli.py
  - src/analista/core/comparables.py
  - tests/test_ranking_freio.py
autonomous: true
requirements: [SAN-01, ENS-01]

must_haves:
  truths:
    - "cmd_rank NÃO estampa o alvo de regressão como verdade quando a regressão é frágil (R²≈0: ITUB4/BBAS3 R²=0; n insuficiente)"
    - "cmd_rank NÃO estampa alvo cru absurdo (ROMI3 R$0,10, −98%) — suprimido/marcado pelo mesmo freio"
    - "cmd_rank aplica a suspensão por arquétipo: ticker com motor_pendente (financeira/ciclica/crescimento) não tem alvo estampado como preço-alvo"
    - "quando o alvo de regressão e a faixa DDM da MESMA ação divergem além de um limiar, cmd_rank emite AVISO de divergência (sinalização) em vez de apresentar as duas lentes como 'o preço-alvo'"
  artifacts:
    - path: "src/analista/core/comparables.py"
      provides: "limiar + helper puro de divergência entre lentes (const de módulo, padrão LIMIAR_R2)"
      contains: "divergencia"
    - path: "src/analista/cli.py"
      provides: "cmd_rank com freio de R²/n + suspensão por arquétipo + aviso de divergência"
      contains: "r2_baixo"
    - path: "tests/test_ranking_freio.py"
      provides: "goldens: ROMI3/ITUB4 (freio) + WEGE3/ITUB4 (divergência sinalizada)"
  key_links:
    - from: "src/analista/cli.py::cmd_rank"
      to: "comparables.RegressaoPL.r2_baixo / .amostra_pequena"
      via: "gate do alvo antes de imprimir"
      pattern: "r2_baixo|amostra_pequena"
    - from: "src/analista/cli.py::cmd_rank"
      to: "arquetipo.classificar / ARQUETIPO_MOTOR (motor_pendente)"
      via: "suspensão por arquétipo no Ranking (paridade com Analisar D-04)"
      pattern: "motor_pendente|ARQUETIPO_MOTOR"
---

<objective>
Fechar o Achado 3 e a parte de SINALIZAÇÃO do Achado 4 da 01-AUDIT-COERENCIA.md (Fase 3 PUXADA,
independem da Fase 2):

- **Achado 3 (freio do modo Ranking):** `cmd_rank` emite alvos de regressão crus, às vezes absurdos,
  inclusive com R²=0 (ITUB4 alvo R$33 / BBAS3 R$46 com R²=0,00 → ruído) e alvo degenerado (ROMI3
  R$0,10, −98%). O Ranking NÃO herda a suspensão por arquétipo do modo Analisar. Aplicar o freio:
  suprimir/marcar o alvo quando a regressão é frágil (R²≈0 ou n insuficiente — as propriedades
  `r2_baixo`/`amostra_pequena` de RegressaoPL JÁ EXISTEM) e aplicar a mesma suspensão por arquétipo
  (motor_pendente → não estampar alvo como verdade).
- **Achado 4 (SINALIZAÇÃO da divergência — só a sinalização):** quando as duas lentes disponíveis
  para a mesma ação (faixa DDM absoluta × alvo de regressão relativo) divergem além de um limiar
  (WEGE3 ~3×, ITUB4 ~2,2×), AVISAR o usuário em vez de apresentar as duas como "o preço-alvo".

Purpose: fecha a incoerência cross-modo (Core Value) — o mesmo número não pode aparecer barato numa
lente e ausente/caro em outra sem explicação.
NÃO ESCOPO (Fase 3, pós-Fase-2): a RECONCILIAÇÃO/ENSEMBLE real entre DDM e o motor do arquétipo —
esse motor só existe na Fase 2. Aqui é APENAS a sinalização; o must_have cobre só o aviso, não a
reconciliação final.
Output: cmd_rank com freio de fragilidade + suspensão por arquétipo + aviso de divergência.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-AUDIT-COERENCIA.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-AUDIT-COERENCIA-DATA.json

<interfaces>
<!-- Extraído de cli.py e comparables.py. Executor não precisa explorar. -->
De src/analista/core/comparables.py:
  LIMIAR_AMOSTRA = 10 ; LIMIAR_R2 = 0.5   # constantes de módulo (padrão a seguir para o limiar de divergência)
  @dataclass RegressaoPL: coeficientes, r2, n
    .amostra_pequena -> bool   # n < LIMIAR_AMOSTRA
    .r2_baixo -> bool          # r2 < LIMIAR_R2  (JÁ existe — usar no freio)
    .roe_sinal_invertido -> bool
  def preco_alvo_por_regressao(reg, dp, roe, lpa, preco_corrente) -> Optional[PrecoAlvo]
  @dataclass PrecoAlvo: pl_esperado, preco_alvo, upside, subavaliada, ...

De src/analista/cli.py::cmd_rank (linhas 132-180):
  reg = cmp.ajustar_regressao_pl(PL, DP, ROE)
  alvos = {}   # ticker -> PrecoAlvo (só quando reg existe)
  # imprime: "# TICKER NOTA ALVO R$ UPSIDE" — hoje estampa pa.preco_alvo CRU sem freio
  # empresas[i] são CompanyData já montados (têm setor, roe_valuation, serie, eh_concessionaria)

De src/analista/core/arquetipo.py:
  classificar(c, cfg) -> ResultadoArquetipo(chave, ...) ; ARQUETIPO_MOTOR.get(chave) -> motor|None
  # motor_pendente = ARQUETIPO_MOTOR.get(chave) is None  (mesma lógica de report.py:151-153)

Casos-alvo (01-AUDIT-COERENCIA-DATA.json): ROMI3 alvo=0.10 r2=0.60 n=4; ITUB4 alvo=33.44 r2=0.00 n=4
  (financeira, motor_pendente); BBAS3 alvo=45.80 r2=0.00 n=4; WEGE3 DDM 9.41-13.66 vs reg alvo=34.97 (~3×).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Freio de fragilidade (R²/n) + suspensão por arquétipo no cmd_rank</name>
  <files>src/analista/cli.py, tests/test_ranking_freio.py</files>
  <read_first>
    - src/analista/cli.py (cmd_rank, linhas 132-180; _montar linhas 44-55)
    - src/analista/core/comparables.py (RegressaoPL.r2_baixo/.amostra_pequena linhas 88-96; preco_alvo_por_regressao 149-184)
    - src/analista/core/arquetipo.py (classificar + ARQUETIPO_MOTOR — resolução de motor_pendente)
    - src/analista/report/report.py (linhas 203-219 — o padrão D-04 de suspensão a espelhar no Ranking)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-AUDIT-COERENCIA.md (Achado 3)
  </read_first>
  <behavior>
    - Test 1 (RED): regressão com r2_baixo (R²≈0, caso ITUB4/BBAS3) → cmd_rank NÃO apresenta o alvo
      como número de preço-alvo (marca como não-confiável / "—"), embora o ranque por nota siga.
    - Test 2 (RED): alvo degenerado (ROMI3 alvo≈0,10, upside −98%) → suprimido/marcado pelo freio.
    - Test 3 (RED): ticker motor_pendente (arquétipo financeira/ciclica/crescimento) → alvo NÃO
      estampado como verdade (paridade com a suspensão D-04 do modo Analisar).
    - Test 4 (guard inverso): regressão saudável (R² alto, arquétipo com motor) → alvo segue exibido.
  </behavior>
  <action>
Refatorar cmd_rank para não estampar alvo de regressão como verdade em três condições (a função
que decide "exibir o alvo?" deve ser testável — extrair um helper puro em cli.py ou comparables.py
que receba a regressão + o resultado do arquétipo e devolva se/como exibir): (1) `reg.r2_baixo` ou
`reg.amostra_pequena` → marcar o alvo como não-confiável (ex.: imprimir "—" ou o valor com um
sufixo/flag "R² baixo", não como preço-alvo cravado); (2) alvo degenerado/absurdo (upside
extremo/PrecoAlvo já retorna None quando pl_esperado<=0, mas o caso ROMI3 R$0,10 tem pl_esperado>0 —
adicionar um freio de sanidade explícito, reusando a mesma ideia de guarda-corpo, ex.: upside abaixo
de um piso ou preço_alvo << preço corrente por fator absurdo); (3) para cada empresa, resolver o
arquétipo (arquetipo.classificar(c, cfg) + ARQUETIPO_MOTOR) e, se motor_pendente, NÃO estampar o
alvo como preço-alvo — espelhar o D-04 do Analisar (o Ranking não pode afirmar um alvo por um modelo
que não serve ao perfil). Manter a NOTA do ranque (múltiplos padronizados) intacta — o freio é só
sobre a COLUNA de alvo/upside. Criar tests/test_ranking_freio.py exercitando cmd_rank (ou o helper
puro extraído) com fixtures dos casos reais do audit. Sem config.yaml: se precisar de limiares novos,
usar constantes de módulo em comparables.py (padrão LIMIAR_R2/LIMIAR_AMOSTRA já existente).
  </action>
  <verify>
    <automated>python -m pytest tests/test_ranking_freio.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Regressão com r2_baixo → o alvo não é apresentado como preço-alvo cravado (helper retorna suprimido/marcado).
    - Alvo ROMI3-shape (≈0,10 / −98%) → freado (não estampado como verdade).
    - Ticker motor_pendente → alvo não estampado como preço-alvo (paridade D-04).
    - Regressão saudável + arquétipo com motor → alvo exibido normalmente (guard inverso verde).
    - A NOTA do ranque (padronização de múltiplos) permanece inalterada nos goldens.
  </acceptance_criteria>
  <done>cmd_rank não estampa alvos frágeis/absurdos nem alvos de tickers suspensos por arquétipo; ranque por nota intacto.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Sinalização de divergência entre lentes (DDM × regressão) no Ranking</name>
  <files>src/analista/core/comparables.py, src/analista/cli.py, tests/test_ranking_freio.py</files>
  <read_first>
    - src/analista/core/comparables.py (constantes de módulo LIMIAR_* linhas 72-76 — padrão a seguir)
    - src/analista/cli.py (cmd_rank pós-Task 1)
    - src/analista/report/report.py (analisar_acao — fonte da faixa DDM a.vmin/a.vmax por ticker)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-AUDIT-COERENCIA.md (Achado 4 — tabela de divergências)
  </read_first>
  <behavior>
    - Test 1 (RED): duas estimativas da mesma ação divergindo além do limiar (WEGE3: DDM ~R$11 mid
      vs regressão R$35, ~3×) → o helper sinaliza divergência (retorna flag/razão) e cmd_rank emite
      um AVISO em vez de apresentar as duas lentes como "o preço-alvo".
    - Test 2 (RED): ITUB4 (DDM ~R$15 vs regressão R$33, ~2,2×) → sinalizado.
    - Test 3 (guard inverso): estimativas próximas (< limiar) → sem aviso.
    - O helper `divergencia_entre_lentes` é PURO e testado isoladamente (input dois números + limiar).
  </behavior>
  <action>
Adicionar em comparables.py uma constante de módulo `LIMIAR_DIVERGENCIA` (ex.: 2.0 — "maior lente >
2× a menor", coerente com o limiar de divergência do roadmap Fase 3) e um helper PURO
`divergencia_entre_lentes(v_a, v_b, limiar=LIMIAR_DIVERGENCIA) -> (bool, float)` que devolve
(divergiu, razão = max/min) tratando None/zero/negativo com segurança (retorna (False, ...) quando
alguma lente é indisponível/inválida — não inventar divergência sobre dado ausente). Em cmd_rank,
para cada empresa com alvo de regressão disponível E faixa DDM disponível, obter a estimativa DDM
(mid = (a.vmin+a.vmax)/2 de report.analisar_acao — reusar o funil já existente; se chamar
analisar_acao por ticker for pesado, extrair só a faixa DDM necessária) e comparar com o alvo de
regressão via o helper; quando divergir, imprimir um AVISO de divergência de lentes para aquele
ticker (ex.: linha/sufixo "⚠ lentes divergem ~Nx — ver Analisar; reconciliação na Fase 3") em vez de
tratar qualquer das duas como o preço-alvo definitivo. Deixar EXPLÍCITO em comentário e no SUMMARY
que isto é SINALIZAÇÃO, não reconciliação: o ensemble real (DDM × motor do arquétipo) depende da
Fase 2 e é escopo da Fase 3. Adicionar testes do helper puro + do comportamento de aviso em
tests/test_ranking_freio.py. Sem config.yaml (const de módulo).
  </action>
  <verify>
    <automated>python -m pytest tests/test_ranking_freio.py -q && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `divergencia_entre_lentes(11, 35)` retorna (True, ~3.18); `divergencia_entre_lentes(30, 33)` retorna (False, ~1.1).
    - `divergencia_entre_lentes(None, 33)` e `divergencia_entre_lentes(0, 33)` retornam (False, ...) — sem inventar divergência sobre dado ausente/inválido.
    - cmd_rank emite aviso de divergência para um caso WEGE3/ITUB4-shape e NÃO para estimativas próximas.
    - Comentário/SUMMARY declaram explicitamente que a reconciliação/ensemble é DEFERIDA à Fase 3 (pós-Fase 2).
    - `python -m pytest -q` → suíte verde, 0 failed (test_consistencia_modos inalterado).
  </acceptance_criteria>
  <done>Divergência entre as duas lentes é SINALIZADA honestamente no Ranking; reconciliação final explicitamente deferida à Fase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine → saída CLI (Ranking) | Alvo/aviso impresso ao usuário (borda de saída; freio + sinalização defensiva). |
| CVM/yfinance → regressão | Fundamentos de pares que alimentam a regressão (já ingeridos hoje). |

Superfície mínima: freio de output + helper puro de comparação. Nenhum input externo novo, nenhuma
fórmula de valuation alterada, nenhuma API/segredo/rede. Constantes de módulo (sem config novo).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0108-01 | Tampering | alvo de regressão frágil (R²=0) apresentado como preço-alvo (integridade) | mitigate | Freio por r2_baixo/amostra_pequena + suspensão por arquétipo; goldens travam ROMI3/ITUB4. |
| T-0108-02 | Repudiation | duas lentes divergentes apresentadas como uma verdade única, confundindo o usuário | mitigate | Aviso de divergência explícito (sinalização), reconciliação deferida à Fase 3 sem fingir certeza. |
| T-0108-03 | Denial of Service | helper de divergência quebra em None/zero/negativo | mitigate | Helper trata dado ausente/inválido retornando (False, ...) sem exceção; testado isoladamente. |
</threat_model>

<verification>
- `python -m pytest tests/test_ranking_freio.py -q` verde (freio + suspensão + divergência).
- `python -m pytest -q` verde, 0 failed (test_consistencia_modos/test_vulc3_regressao inalterados).
- Sem config.yaml novo (constantes de módulo em comparables.py); ddm.py/selo.py intocados.
</verification>

<success_criteria>
O modo Ranking não estampa alvos frágeis/absurdos nem alvos de tickers suspensos por arquétipo, e
sinaliza honestamente quando as duas lentes divergem além do limiar — com a reconciliação/ensemble
final explicitamente deferida à Fase 3 (pós-Fase 2).
</success_criteria>

<output>
Após completar, criar `.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-08-SUMMARY.md`.
</output>
