---
phase: 01-classificador-de-arqu-tipo-roteamento
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_arquetipo.py
  - src/analista/core/arquetipo.py
  - config.yaml
autonomous: true
gap_closure: true
requirements: [ARQ-01, ARQ-02]

must_haves:
  truths:
    - "classificar() sobre um compounder realista (>=15%/ano, réplica WEGE3) retorna chave=='crescimento' e fronteirico is False (hoje retorna 'ciclica' com confiança alta — CR-01)"
    - "Cíclicas genuínas (lucro oscilando/alternando sinal) continuam retornando chave=='ciclica'"
    - "O conflito real de sinais continua marcando fronteirico=True com 2-3 candidatos (ARQ-02 preservado)"
    - "A suíte completa permanece verde (>=354 + o novo golden)"
  artifacts:
    - path: "src/analista/core/arquetipo.py"
      provides: "_cv_lucro detrended — mede oscilação sobre retornos ano-a-ano (ou resíduos de ajuste log-linear), não sobre o nível bruto"
      contains: "def _cv_lucro"
    - path: "tests/test_arquetipo.py"
      provides: "golden com compounder realista (>=15%/ano) travando crescimento e fronteirico is False"
      contains: "crescimento"
    - path: "config.yaml"
      provides: "threshold do sinal de ciclicidade recalibrado para a métrica detrended (bloco arquetipo:)"
      contains: "ciclica_cv_min"
  key_links:
    - from: "tests/test_arquetipo.py"
      to: "arquetipo.classificar"
      via: "novo golden compounder realista"
      pattern: "chave == CRESCIMENTO"
    - from: "src/analista/core/arquetipo.py::_cv_lucro"
      to: "classificar (corte ciclica_cv_min)"
      via: "sinal de oscilação detrended alimenta o candidato ciclica"
      pattern: "cv >= ciclica_cv_min"
---

<objective>
Fechar o Gap 1 (BLOCKER, SC#1 / CR-01) da 01-VERIFICATION.md: o sinal de ciclicidade
`_cv_lucro()` mede o coeficiente de variação do NÍVEL BRUTO da série de lucro, que é
dominado pela tendência de crescimento. Qualquer compounder real (WEGE3: ROE≈25.8%, CV do
lucro cru ≈0.61) ultrapassa `ciclica_cv_min=0.40` e é roteado para `ciclica` com
`confianca='alta'` — o oposto do arquétipo `crescimento` que a própria Success Criteria
nomeia para WEGE3. A suíte está 100% verde porque o único golden de compounder
(`test_roe_alto_retencao_alta_vira_crescimento`) usa 3%/ano (CV≈0.08), mascarando o defeito.

Purpose: entregar a metade "refino quantitativo" do ARQ-01 (separar compounder de cíclica)
que hoje está estruturalmente quebrada, sem regredir a rota TAEE11/DDM nem nenhum dos 354
testes verdes.
Output: `_cv_lucro` detrended, threshold recalibrado no bloco `arquetipo:` do config, e um
golden de compounder realista que trava a regressão silenciosa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-VERIFICATION.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-REVIEW.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-CONTEXT.md
@src/analista/core/arquetipo.py
@tests/test_arquetipo.py
@config.yaml

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código. Sem exploração adicional. -->

Símbolos exportados por src/analista/core/arquetipo.py:
- FINANCEIRA, PAGADORA_REGULADA, CICLICA, CRESCIMENTO, HOLDING (constantes str)
- ARQUETIPO_MOTOR (dict chave→motor; só PAGADORA_REGULADA:"ddm" tem motor nesta fase)
- classificar(c: CompanyData, cfg: dict) -> ResultadoArquetipo
- ResultadoArquetipo(chave, fronteirico=False, candidatos=[], confianca="alta", sinais={})
- _cv_lucro(serie: list) -> Optional[float]  # ALVO do fix

Refino quantitativo atual (arquetipo.py:105-119):
- roe = c.roe_valuation(); payout = c.payout_valuation(); retencao = 1 - payout
- cv = _cv_lucro(c.serie("lucro_liquido"))
- candidato CICLICA se cv is not None and cv >= ciclica_cv_min
- candidato CRESCIMENTO se roe >= roe_alto_min E retencao >= retencao_alta_min
- se nenhum candidato → PAGADORA_REGULADA (maduro por eliminação)
- conflito (>=2 candidatos distintos) → fronteirico=True, confianca="baixa", chave=distintos[0]

Bloco config.yaml arquetipo: (linhas 177-195) — chaves relevantes:
- roe_alto_min: 0.15 ; retencao_alta_min: 0.50 ; ciclica_cv_min: 0.40

Helper de fixture em tests/test_arquetipo.py:
- _empresa(ticker, setor, lucros, *, payout=0.30, pl=5000.0, eh_concessionaria=False, anos_ini=2015)
  monta CompanyData de N anos; dividendos = payout*lucro; PL fixo (ROE ~ lucro/PL).
- _cfg() carrega config.yaml real.
- Golden atual de compounder (linha 103): lucros = [round(1000 * (1.03 ** i)) for i in range(10)]
  → só 3%/ano, CV≈0.08, NÃO exercita o caminho de crescimento realista.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): golden que reproduz o misroute do WEGE3</name>
  <files>tests/test_arquetipo.py</files>
  <read_first>
    - tests/test_arquetipo.py (helper _empresa, _cfg, golden atual test_roe_alto_retencao_alta_vira_crescimento na linha 103)
    - src/analista/core/arquetipo.py (refino quantitativo :105-119, _cv_lucro :58-69)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-VERIFICATION.md (gap 1: números reais do WEGE3 — ROE≈25.8%, CV cru≈0.61)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-REVIEW.md (CR-01)
  </read_first>
  <behavior>
    - Novo golden `test_compounder_realista_wege_vira_crescimento`: réplica de um compounder real (WEGE3-shape) usando o helper `_empresa`, com crescimento composto >=15%/ano por 10 anos (ex.: `lucros = [round(1000 * (1.18 ** i)) for i in range(10)]`), `payout` baixo o bastante para retenção >= 0.50 (payout <= 0.20) e `pl` calibrado para `roe_valuation()` >= 0.15 (WEGE3 real ≈0.258). Setor NÃO-financeiro e NÃO-concessionário (ex.: "Máquinas e Equipamentos") para cair no refino quantitativo.
    - Asserções: `r.chave == CRESCIMENTO`, `r.fronteirico is False`, `CICLICA not in r.candidatos`.
    - Este golden DEVE FALHAR contra o código atual (reproduz o misroute para 'ciclica' com confiança alta descrito no gap).
  </behavior>
  <action>Adicionar o golden `test_compounder_realista_wege_vira_crescimento` em tests/test_arquetipo.py usando o helper `_empresa` já existente e `_cfg()`. Calibrar `lucros` para >=15%/ano composto (>=1.15**i), `payout<=0.20` (retenção alta) e `pl` tal que `roe_valuation()>=roe_alto_min` (mirar ~0.258 como o WEGE3 real). Afirmar `r.chave == CRESCIMENTO`, `r.fronteirico is False` e `CICLICA not in r.candidatos`. Rodar e CONFIRMAR que o teste falha hoje (o compounder cai em 'ciclica' porque o CV do lucro cru estoura o corte por causa da tendência). NÃO alterar arquetipo.py nesta task — é a fase RED. NÃO mexer no golden existente de 3%/ano.</action>
  <verify>
    <automated>python -m pytest tests/test_arquetipo.py::test_compounder_realista_wege_vira_crescimento -x</automated>
  </verify>
  <acceptance_criteria>
    - O comando acima FALHA (RED), com a chave observada sendo 'ciclica' (ou fronteiriço), não 'crescimento' — reproduzindo o Gap 1 da 01-VERIFICATION.md.
    - O golden usa crescimento composto >=15%/ano e assertiva explícita `r.chave == CRESCIMENTO` e `r.fronteirico is False`.
    - `grep -c "1.15\|1.18\|1.20\|1.2 \*\*\|1.18 \*\*\|1.15 \*\*" tests/test_arquetipo.py` >= 1 (fixture de crescimento realista presente).
  </acceptance_criteria>
  <done>Golden de compounder realista existe e falha contra o código atual, travando a evidência do defeito antes do fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): detrend em _cv_lucro + recalibrar o corte</name>
  <files>src/analista/core/arquetipo.py, config.yaml</files>
  <read_first>
    - src/analista/core/arquetipo.py (_cv_lucro :58-69 e uso :109/:113; é o alvo)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-REVIEW.md (CR-01: fórmula sugerida — CV dos retornos ano-a-ano OU dispersão dos resíduos de ajuste log-linear)
    - config.yaml (bloco arquetipo: :177-195, corte ciclica_cv_min :195)
    - tests/test_arquetipo.py (goldens que NÃO podem regredir: test_lucro_oscilante_vira_ciclica :94, test_conflito_de_sinais_marca_fronteirico :114, test_roe_alto_retencao_alta_vira_crescimento :103)
  </read_first>
  <action>Reescrever `_cv_lucro` para medir a oscilação DETRENDED, não a dispersão do nível bruto (que carrega a tendência). Abordagem primária conforme CR-01: computar os retornos ano-a-ano `(lucro[t]-lucro[t-1])/abs(lucro[t-1])` (pulando pontos com lucro[t-1]==0) e retornar o coeficiente de variação DESSES retornos (`pstdev(ret)/abs(mean(ret))`); alternativa aceitável: dispersão dos resíduos de um ajuste log-linear. Manter a função PURA e sem I/O, manter os guards de dados faltantes existentes (`None` se <3 pontos válidos, ou métrica indefinida) para preservar `test_sinais_none_degrada_sem_typeerror`. Com o novo sinal, um compounder monotônico (retornos ~constantes) pontua BAIXO e não vira candidato `ciclica`; um cíclico que alterna sinal pontua ALTO. Atualizar o docstring de `_cv_lucro` (hoje afirma "série de lucro CRU") para descrever a métrica detrended. No config.yaml, recalibrar o VALOR de `ciclica_cv_min` (bloco `arquetipo:`, é o bloco próprio desta fase — não é rebaseline de bloco pré-existente) e seu comentário inline para a escala da nova métrica, escolhendo o corte que mantém TODOS os goldens verdes: o novo golden WEGE3-like → NÃO cíclica; `test_lucro_oscilante_vira_ciclica` e `test_conflito_de_sinais_marca_fronteirico` → continuam cíclica/fronteiriço. NÃO tocar `core/ddm.py` nem `selo.py`. NÃO alterar a árvore de decisão (hard-routes, registry, mecanismo de fronteiriço) além do sinal de oscilação e seu corte. Garantir que o WEGE3-like produza UM único candidato `crescimento` (cv abaixo do corte) — o fix é no sinal, não no desempate `distintos[0]`.</action>
  <verify>
    <automated>python -m pytest tests/test_arquetipo.py tests/test_arquetipo_roteamento.py -q && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_compounder_realista_wege_vira_crescimento` passa (GREEN): `r.chave == CRESCIMENTO`, `r.fronteirico is False`.
    - `test_lucro_oscilante_vira_ciclica`, `test_conflito_de_sinais_marca_fronteirico`, `test_roe_alto_retencao_alta_vira_crescimento`, `test_sinais_none_degrada_sem_typeerror` continuam verdes.
    - `python -m pytest -q` reporta >= 355 passed (354 anteriores + o novo golden), 0 failed.
    - `git diff --name-only` NÃO inclui src/analista/core/ddm.py nem src/analista/report/selo.py.
    - `_cv_lucro` continua pura (sem import de rede/arquivo; sem chamadas de I/O no corpo).
  </acceptance_criteria>
  <done>O sinal de ciclicidade separa "tendência de alta forte" de "oscilação em torno de média estável"; WEGE3-like classifica como crescimento e a suíte inteira segue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | O fix é um refactor de função pura (`_cv_lucro`) + recalibração de threshold em config já existente. Não introduz nova entrada externa, endpoint, API, secret ou arquivo. Os dados de lucro já vêm da ingestão CVM existente (fora do escopo desta task) e são consumidos por valor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | config.yaml `ciclica_cv_min` | accept | Threshold de calibração local, sem impacto de segurança; sem PII, sem borda de rede. Superfície de ameaça mínima documentada honestamente (refactor de função pura + valor de config). |
| T-01-03-02 | Denial of Service | `_cv_lucro` sobre série de lucro | mitigate | Função O(n) sobre série de <=10 pontos, guards `None` para <3 pontos e divisão por zero preservados; sem loop não-limitado nem recursão. |
</threat_model>

<verification>
- `python -m pytest -q` → >= 355 passed, 0 failed.
- `python -m pytest tests/test_arquetipo.py tests/test_arquetipo_roteamento.py -q` → todos verdes, incluindo o novo golden.
- `git diff --name-only` limitado a tests/test_arquetipo.py, src/analista/core/arquetipo.py, config.yaml.
- WEGE3-like → chave 'crescimento', fronteirico False; cíclicas e fronteiriço existentes intactos.
</verification>

<success_criteria>
- Gap 1 (BLOCKER) fechado: o refino quantitativo separa compounder de cíclica; WEGE3 (ticker nomeado na SC#1) não é mais roteado para 'ciclica'.
- Regressão travada por golden de compounder realista (>=15%/ano).
- Zero regressão: 354 testes anteriores + novo golden verdes; ddm.py/selo.py intocados.
</success_criteria>

<output>
After completion, create `.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-03-SUMMARY.md`
</output>
