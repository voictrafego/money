---
phase: 01-classificador-de-arqu-tipo-roteamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/core/arquetipo.py
  - config.yaml
  - tests/test_arquetipo.py
autonomous: true
requirements: [ARQ-01, ARQ-02, ENG-01]
must_haves:
  truths:
    - "roteamento híbrido: setores fortes (banco/seguradora/regulada) hard-route; resto passa pelo refino quantitativo (D-02)"
    - "classificar() sobre empresa com setor 'Bancos' retorna chave 'financeira' (confianca alta, hard-route D-02)"
    - "classificar() sobre empresa com setor '... Seguradoras ...' retorna 'financeira'"
    - "classificar() sobre empresa regulada (eh_concessionaria=True, setor 'Energia Elétrica') retorna 'pagadora_regulada'"
    - "classificar() sobre empresa 'Petróleo e Gás' com eh_concessionaria=True NÃO retorna 'pagadora_regulada' (guarda anti-Petróleo)"
    - "classificar() sobre empresa de lucro oscilante (CV >= ciclica_cv_min) inclui 'ciclica' nos candidatos"
    - "classificar() sobre empresa com sinais None degrada sem TypeError (default pagadora_regulada ou fronteiriço)"
    - "fronteiriço dispara SÓ em conflito real de sinais (>= 2 candidatos distintos); setor+quantitativo concordando crava (D-01, ARQ-02)"
    - "conflito real de sinais (>= 2 candidatos distintos) marca fronteirico=True com 2-3 candidatos (ARQ-02)"
    - "ARQUETIPO_MOTOR: 5 chaves 1:1 com os motores, mapeia pagadora_regulada->'ddm' e as outras 4 chaves->None (D-03, ENG-01)"
  artifacts:
    - path: "src/analista/core/arquetipo.py"
      provides: "classificar() + ResultadoArquetipo + ARQUETIPO_MOTOR + _cv_lucro"
      contains: "def classificar"
      min_lines: 60
    - path: "config.yaml"
      provides: "bloco arquetipo: com tokens + thresholds"
      contains: "arquetipo:"
    - path: "tests/test_arquetipo.py"
      provides: "golden do classificador (âncoras + degradação)"
      contains: "def test_"
  key_links:
    - from: "src/analista/core/arquetipo.py::classificar"
      to: "CompanyData.roe_valuation/payout_valuation/serie/eh_concessionaria/setor"
      via: "consumo de sinais canônicos (sem recalcular)"
      pattern: "roe_valuation|payout_valuation|eh_concessionaria"
    - from: "src/analista/core/arquetipo.py::classificar"
      to: "config.yaml arquetipo:"
      via: "leitura de thresholds via cfg['arquetipo']"
      pattern: "arquetipo"
---

<objective>
Erguer o classificador de arquétipo puro (`core/arquetipo.py`) e o registry arquétipo→motor,
mais o bloco de thresholds `arquetipo:` em `config.yaml`. É o coração da Fase 1: uma função pura
`classificar(c, cfg) -> ResultadoArquetipo` que lê apenas sinais que `CompanyData` já expõe e
decide entre 5 chaves (financeira, pagadora_regulada, ciclica, crescimento, holding), com
fallback honesto (fronteiriço) em conflito real de sinais.

Purpose: Sem essa etapa a ferramenta não sabe o tipo de negócio antes de valuar — é a peça que
o milestone inteiro consome. Isolá-la em `core/` (espelhando `lifecycle.py`) mantém a engine pura
testável por golden e a consistência cross-modo (mesma fonte de sinais que Analisar/Ranking).

Output: `src/analista/core/arquetipo.py` (novo), bloco `arquetipo:` em `config.yaml` (aditivo),
`tests/test_arquetipo.py` (golden). Nenhum motor novo; nenhum toque em `report.py`/`selo.py`/`ddm.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-CONTEXT.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-RESEARCH.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-PATTERNS.md

<interfaces>
<!-- Sinais canônicos que o classificador CONSOME (não recalcula). Contrato-None crítico. -->
<!-- Extraído de src/analista/core/fundamentals.py -->

CompanyData (fundamentals.py:20):
  campos: setor: str, eh_concessionaria: bool = False, beta: Optional[float],
          patrimonio_liquido: Dict[int,float], anos: List[int]
  metodos:
    setor  -> str (SETOR_ATIV legal; confiável p/ financeiras, NÃO p/ industriais)
    eh_concessionaria -> bool (build.py:68; TEM falso-positivo 'Gás' ⊂ 'Petróleo e Gás')
    roe_valuation() -> Optional[float]   # None sem PL do ano-1
    payout_valuation() -> Optional[float]  # mediana s/ clamp; pode ser >1.0; None em série vazia
    serie("lucro_liquido") -> List[float]  # série CRUA (a oscilação É o sinal)
    serie_lucro_normalizada() -> List[float]
    margem_valuation() -> Optional[float]  # None se faltar base
    anos_ordenados() -> List[int]

Analog de estrutura (lifecycle.py — copiar o estilo):
  ESTAGIOS: dict módulo-nível; classificar_estagio(...) com None-guard `g = x if x is not None else 0.0`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloco arquetipo: no config.yaml + scaffold de core/arquetipo.py</name>
  <files>config.yaml, src/analista/core/arquetipo.py</files>
  <read_first>
    - config.yaml (bloco `selo:` :160-169 e `padroes:` — padrão "bloco irmão, anti-rebaseline")
    - src/analista/core/lifecycle.py (analog exato: docstring + from __future__ + dict módulo-nível + None-guard)
    - src/analista/core/normalizacao.py (analog do helper puro `_limpar` + uso de `statistics`)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-RESEARCH.md (seção Thresholds + Pattern 1)
  </read_first>
  <action>
    Adicionar em config.yaml um bloco NOVO `arquetipo:` no FIM do arquivo (irmão de `selo:`, sem tocar
    nenhuma linha existente — Pitfall 4 anti-rebaseline). Chaves e valores iniciais: `financeiro_tokens`
    (lista: banco, "intermediação financeira", "intermediacao financeira", seguradora, "arrendamento mercantil",
    "crédito imobiliário", "credito imobiliario", factoring, securitização, securitizacao, "bolsas de valores"),
    `regulada_excluir_tokens: [petróleo, petroleo]`, `roe_alto_min: 0.15`, `retencao_alta_min: 0.50`,
    `ciclica_cv_min: 0.40`. Comentário de cabeçalho explicando que são thresholds iniciais config-driven
    (calibração empírica é BACKTEST-01/deferida).

    Criar src/analista/core/arquetipo.py com: docstring de cabeçalho ligando ao brief (roteamento por arquétipo)
    + `from __future__ import annotations`. Definir 5 constantes de chave 1:1 com os motores (D-03):
    FINANCEIRA="financeira", PAGADORA_REGULADA="pagadora_regulada", CICLICA="ciclica", CRESCIMENTO="crescimento",
    HOLDING="holding". Definir o registry `ARQUETIPO_MOTOR` (dict módulo-nível, ENG-01) mapeando
    PAGADORA_REGULADA->"ddm" e as outras 4 chaves->None (motor pendente Fase 2). Definir a dataclass
    `ResultadoArquetipo` com campos: chave: str, fronteirico: bool = False,
    candidatos: List[str] = field(default_factory=list), confianca: str = "alta",
    sinais: dict = field(default_factory=dict). Definir o helper puro privado `_cv_lucro(serie: list) -> Optional[float]`
    que filtra None, retorna None se len < 3 ou média == 0, senão `pstdev(vals)/abs(mean(vals))` (coef. de variação;
    import `from statistics import mean, pstdev`). NÃO implementar `classificar()` ainda (Task 2).
  </action>
  <verify>
    <automated>python -c "import yaml,src.analista.core.arquetipo as A; c=yaml.safe_load(open('config.yaml')); assert set(A.ARQUETIPO_MOTOR)=={'financeira','pagadora_regulada','ciclica','crescimento','holding'}; assert A.ARQUETIPO_MOTOR['pagadora_regulada']=='ddm'; assert all(A.ARQUETIPO_MOTOR[k] is None for k in A.ARQUETIPO_MOTOR if k!='pagadora_regulada'); assert 'arquetipo' in c and c['arquetipo']['ciclica_cv_min']==0.40; assert A._cv_lucro([1,1,1]) is not None; assert A._cv_lucro([1,2]) is None; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `config.yaml` tem bloco `arquetipo:` com `financeiro_tokens`, `regulada_excluir_tokens`, `roe_alto_min: 0.15`, `retencao_alta_min: 0.50`, `ciclica_cv_min: 0.40`; nenhum bloco pré-existente alterado
    - `ARQUETIPO_MOTOR` tem exatamente as 5 chaves; `pagadora_regulada`->"ddm", restante->None
    - `ResultadoArquetipo` é dataclass com `chave, fronteirico, candidatos, confianca, sinais` (mutáveis via default_factory)
    - `_cv_lucro([1,2])` retorna None (len<3); `_cv_lucro([100,100,100])` retorna 0.0; `_cv_lucro([1,None,2,3])` não levanta
    - `python -m pytest -q` continua em 338 passed (nada quebrado; módulo importável)
  </acceptance_criteria>
  <done>config.yaml com bloco arquetipo:, core/arquetipo.py com constantes/registry/dataclass/_cv_lucro; verify passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar classificar() (árvore híbrida D-01/D-02) + golden test_arquetipo.py</name>
  <files>src/analista/core/arquetipo.py, tests/test_arquetipo.py</files>
  <read_first>
    - src/analista/core/arquetipo.py (o scaffold da Task 1 — a ser completado)
    - src/analista/core/lifecycle.py (padrão de None-guard antes de comparar)
    - src/analista/core/fundamentals.py:78-162 (contrato-None de roe_valuation/payout_valuation/margem_valuation; serie :63-67)
    - tests/test_guardrails_fix06.py:16-53 (boilerplate ROOT + _cfg() + fixture CompanyData sintética de 10 anos)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-RESEARCH.md (Pattern 3 pseudocódigo + Pitfall 1/2)
  </read_first>
  <behavior>
    - setor contém token financeiro ('Bancos', '...Seguradoras...', '...Intermediação Financeira') -> ResultadoArquetipo(FINANCEIRA, confianca="alta")
    - eh_concessionaria=True E setor NÃO contém token de regulada_excluir_tokens ('petróleo') -> PAGADORA_REGULADA, confianca="alta"
    - eh_concessionaria=True MAS setor='Petróleo e Gás' -> NÃO cai em pagadora_regulada (segue p/ refino quantitativo) — guarda anti-Petróleo (Pitfall 1)
    - refino: CV(serie lucro cru) >= ciclica_cv_min -> candidato CICLICA; roe_valuation>=roe_alto_min E (1-payout_valuation)>=retencao_alta_min -> candidato CRESCIMENTO; nenhum -> PAGADORA_REGULADA default
    - >= 2 candidatos distintos -> fronteirico=True, candidatos preenchidos (2-3), confianca="baixa" (ARQ-02, D-01)
    - todos os sinais None (ex.: CompanyData só com 1 ano) -> degrada sem TypeError, retorna chave default sem crash (Pitfall 2)
  </behavior>
  <action>
    Implementar `classificar(c: CompanyData, cfg: dict) -> ResultadoArquetipo` em core/arquetipo.py seguindo a
    árvore híbrida (RESEARCH Pattern 3). Ler thresholds via `cfg.get("arquetipo", {})` com defaults (config-driven,
    Pitfall 4). Ordem: (1) HARD-ROUTE financeira — `setor.lower()` contém qualquer `financeiro_tokens` -> retorna
    FINANCEIRA confianca alta (soberano, sem quantitativo — operator decision 5). (2) HARD-ROUTE regulada —
    `c.eh_concessionaria and not any(tok in setor.lower() for tok in regulada_excluir_tokens)` -> PAGADORA_REGULADA
    confianca alta (guarda anti-Petróleo OBRIGATÓRIA — operator decision 2). (3) REFINO QUANTITATIVO para todo o
    resto: `roe = c.roe_valuation()`, `payout = c.payout_valuation()`, `retencao = (1-payout) if payout is not None
    else None`, `cv = _cv_lucro(c.serie("lucro_liquido"))`. Montar lista `candidatos`: se cv is not None e
    cv >= ciclica_cv_min -> append CICLICA; se roe is not None e roe >= roe_alto_min e retencao is not None e
    retencao >= retencao_alta_min -> append CRESCIMENTO; se vazio -> append PAGADORA_REGULADA (default maduro).
    (4) CONFLITO (D-01): se `len(set(candidatos)) >= 2` -> ResultadoArquetipo(candidatos[0], fronteirico=True,
    candidatos=list(dict.fromkeys(candidatos)), confianca="baixa"); senão ResultadoArquetipo(candidatos[0],
    confianca="alta"). Guardar CADA sinal com `is not None` ANTES de qualquer comparação `>=` (Pitfall 2).
    Popular `sinais={"roe":roe,"retencao":retencao,"cv_lucro":cv}` para debug/Fase 3. HOLDING não precisa de
    detector confiável nesta fase (Assumption A6 — motor ausente de qualquer forma); não gerar HOLDING no refino.

    Criar tests/test_arquetipo.py com boilerplate ROOT + `_cfg()` (copiar de test_guardrails_fix06.py:16-29) e
    fixtures sintéticas de 10 anos. Testes exigidos: financeira (setor="Bancos" -> FINANCEIRA), seguradora
    (setor contendo "Seguradoras" -> FINANCEIRA), regulada (setor="Energia Elétrica" + eh_concessionaria=True ->
    PAGADORA_REGULADA, motor ARQUETIPO_MOTOR[chave]=="ddm"), anti-Petróleo (setor="Petróleo e Gás" +
    eh_concessionaria=True -> chave != PAGADORA_REGULADA), cíclica (série de lucro oscilando violento, CV>=0.40 ->
    CICLICA nos candidatos), compounder (ROE>=0.15 + retenção>=0.50, série estável -> CRESCIMENTO), fronteiriço
    (sinais em conflito -> fronteirico=True com >=2 candidatos), degradação (CompanyData(ticker="VAZIA3", anos=[2024])
    -> não levanta, retorna ResultadoArquetipo válido).
  </action>
  <verify>
    <automated>python -m pytest tests/test_arquetipo.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_arquetipo.py -q` exits 0 com >= 7 testes cobrindo financeira/seguradora/regulada/anti-Petróleo/cíclica/compounder/fronteiriço/degradação
    - `classificar(CompanyData(setor="Bancos", anos=[2024]), cfg).chave == "financeira"`
    - `classificar(<Petróleo e Gás, eh_concessionaria=True>, cfg).chave != "pagadora_regulada"`
    - `classificar(CompanyData(ticker="VAZIA3", anos=[2024]), cfg)` não levanta exceção
    - conflito real -> `.fronteirico is True and len(.candidatos) >= 2 and .confianca == "baixa"`
    - `python -m pytest -q` continua 338+ passed (novos testes somam, nenhum antigo quebra)
    - NENHUM número mágico 0.15/0.40/0.50 hardcoded no corpo de classificar() — todos vêm de cfg["arquetipo"]
  </acceptance_criteria>
  <done>classificar() implementado com árvore híbrida + guarda anti-Petróleo + None-guarding; test_arquetipo.py verde; suite completa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml → classificar() | thresholds lidos de arquivo versionado em git (não input externo) |
| CompanyData (já ingerido) → classificar() | dados CVM/Yahoo/BCB já validados na borda de ingestão (fora desta fase); podem ter campos None/faltantes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | classificar() sobre CompanyData com sinais None (roe/payout/série vazia) | mitigate | Guardar cada sinal com `is not None` antes de comparar; degradar para chave default sem TypeError (Pitfall 2). Teste de degradação (VAZIA3) trava o contrato. |
| T-01-02 | Tampering | bloco `arquetipo:` ausente/malformado no config.yaml | mitigate | Ler via `cfg.get("arquetipo", {})` com defaults por chave; classificador nunca KeyError se o bloco sumir. |
| T-01-03 | Information Disclosure | `sinais` dict exposto no ResultadoArquetipo | accept | Dados de valuation já visíveis ao usuário do próprio app; sem PII, engine offline local. |
| T-01-04 | Spoofing | token de setor injetado forçando rota financeira | accept | `setor` vem do SETOR_ATIV da CVM (fonte read-only cacheada), não de input do usuário; sem superfície de injeção nesta fase. |
</threat_model>

<verification>
- `python -m pytest tests/test_arquetipo.py -q` verde (>=7 testes).
- `python -m pytest -q` mantém a baseline (338 passed) + novos testes, tudo verde.
- Nenhum toque em src/analista/report/ ou src/analista/core/ddm.py (grep confirma).
- Nenhum bloco pré-existente de config.yaml alterado (só o `arquetipo:` novo no fim).
</verification>

<success_criteria>
- `classificar()` roteia financeira/regulada por hard-route e o resto por refino quantitativo (ARQ-01).
- Guarda anti-Petróleo impede PETR4 de virar pagadora_regulada.
- Conflito real de sinais marca fronteiriço com 2-3 candidatos (ARQ-02).
- `ARQUETIPO_MOTOR` define o registry 1:1 com os 5 motores; só pagadora_regulada->ddm (ENG-01).
- Thresholds config-driven; degradação graciosa sob sinais None; suite completa verde.
</success_criteria>

<output>
After completion, create `.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-01-SUMMARY.md`
</output>
