---
phase: 01-classificador-de-arqu-tipo-roteamento
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/analista/report/report.py
  - tests/test_arquetipo_roteamento.py
  - tests/test_consistencia_modos.py
  - tests/test_selo.py
  - tests/test_guardrails_fix06.py
autonomous: true
requirements: [ARQ-01, ENG-01, ENG-06]
must_haves:
  truths:
    - "analisar_acao() popula a.arquetipo, a.motor, a.arquetipo_fronteirico, a.arquetipo_candidatos, a.motor_pendente antes do bloco de veredito"
    - "TAEE11-like (regulada, motor=='ddm') mantém veredito/vmin/vmax/multiplos IDÊNTICOS ao baseline (ENG-06)"
    - "ITUB4-like (financeira, motor pendente) tem a.veredito começando com 'VERIFICAR' e NÃO estampa selo 'evitar' (D-04)"
    - "motor_pendente=True adiciona alerta explicando a suspensão do veredito primário — para QUALQUER motor ausente (financeira/crescimento/ciclica/holding), não só financeira (D-04)"
    - "o bloco DDM (:136-152) continua rodando sempre — a.sensibilidade/a.vmin/a.vmax populados mesmo quando motor_pendente"
    - "relatorio_markdown exibe linha 'Arquétipo: X → motor Y' no cabeçalho"
    - "fixtures golden de utility regulada (setor Energia/Saneamento/Água/Gás) usadas em testes que afirmam veredito setam eh_concessionaria=True → roteiam pagadora_regulada → mantêm o veredito DDM (AAA3 continua SUBAVALIADA; VAZIA3 continua com veredito '')"
    - "fixture com sinais em conflito, através de analisar_acao, expõe a.arquetipo_fronteirico is True e len(a.arquetipo_candidatos) >= 2 (ARQ-02 pelo funil completo)"
    - "test_ddm, test_selo, test_consistencia_modos, test_guardrails_fix06, test_vulc3_regressao continuam verdes (exit 0)"
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "roteamento inserido :134→:136 + campos em AnaliseAcao + suspensão D-04 + render"
      contains: "arquetipo.classificar"
    - path: "tests/test_arquetipo_roteamento.py"
      provides: "golden e2e: TAEE11 idêntica, ITUB4 suspenso, PETR4 não-regulada, fronteiriço via funil"
      contains: "analisar_acao"
    - path: "tests/test_consistencia_modos.py"
      provides: "fidelity fix: fixtures Energia Elétrica setam eh_concessionaria=True (AAA3 rota regulada)"
      contains: "eh_concessionaria = True"
  key_links:
    - from: "src/analista/report/report.py::analisar_acao"
      to: "src/analista/core/arquetipo.py::classificar + ARQUETIPO_MOTOR"
      via: "import from ..core + lookup após CAPM (:134)"
      pattern: "arquetipo\\.classificar|ARQUETIPO_MOTOR"
    - from: "src/analista/report/report.py::analisar_acao (bloco veredito)"
      to: "selo.montar_selo overlay VERIFICAR"
      via: "prefixo 'VERIFICAR' reusado quando motor_pendente (sem tocar selo.py)"
      pattern: "VERIFICAR"
---

<objective>
Plugar o classificador da Fase 1 no funil `report.analisar_acao`: inserir o roteamento entre o
CAPM (:134) e a montagem do DDM (:136), expor o arquétipo/motor em `AnaliseAcao`, e aplicar a
suspensão D-04 no bloco do veredito reusando o prefixo "VERIFICAR" quando o motor do arquétipo
ainda não existe (RIM/normalizado/DCF/SOTP na Fase 1). A pagadora regulada (motor "ddm") roteia
normalmente e mantém números/veredito idênticos aos de hoje (ENG-06).

Como o roteamento passa a decidir se o veredito DDM é ou não suspenso, as fixtures golden de
utilities reguladas que hoje NÃO setam `eh_concessionaria` (predam o roteamento) precisam de um
**fix de fidelidade** — setar `eh_concessionaria=True` para casarem com a ingestão real
(`build.py:68` deriva Energia/Saneamento/Água/Gás) e continuarem roteando `pagadora_regulada`.
Isso mantém os goldens verdes SEM afrouxar nenhuma asserção (é fidelidade de fixture, não
enfraquecimento de teste).

Purpose: É onde o roteamento vira comportamento observável — ITUB4 deixa de ser estampado "evitar"
já na Fase 1 (via suspensão honesta), sem tocar em `ddm.py` nem no firewall `selo↛report`, e sem
mexer no caminho DDM da regulada (TAEE11 idêntica).

Output: edições cirúrgicas em `report.py` (import, campos aditivos em AnaliseAcao, roteamento,
guard D-04 no veredito, linha de render) + fix de fidelidade nas fixtures reguladas dos goldens +
`tests/test_arquetipo_roteamento.py` (golden e2e, incluindo o caminho fronteiriço pelo funil).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-CONTEXT.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-RESEARCH.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-PATTERNS.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-01-SUMMARY.md

<interfaces>
<!-- Contratos do módulo criado no Plan 01 (core/arquetipo.py) — usar diretamente. -->

core/arquetipo.py:
  classificar(c: CompanyData, cfg: dict) -> ResultadoArquetipo
  ResultadoArquetipo: chave: str, fronteirico: bool, candidatos: List[str], confianca: str, sinais: dict
  ARQUETIPO_MOTOR: dict  # pagadora_regulada->"ddm", demais->None

<!-- Âncoras exatas em report.py (VERIFICADAS linha a linha). -->
report.py:16-19  imports `from ..core import capm, ddm, growth, indicators, lifecycle, screening`
report.py:22-50  dataclass AnaliseAcao (campos aditivos vão em :49-50, após o bloco Fase 20)
report.py:133-134  fim do CAPM: `if a.g_alto is not None and a.ke is not None: a.g_alto = min(a.g_alto, a.ke)`
report.py:136   comentário `# --- DDM de dois estágios (Cap. 15/17) ---` (bloco DDM roda como HOJE)
report.py:184-207  bloco veredito (guard D-04 entra ANTES do `if a.vmin is not None ...` :184)
report.py:197-201  frase "VERIFICAR — ..." existente (copiar o estilo)
report.py:414-415  linha de cabeçalho do render (Setor/Preço/Estágio) — estender aqui
selo.py:119-122  overlay VERIFICAR: veredito.startswith("VERIFICAR") -> verificar=True, sem faixa/rótulo

<!-- Fixtures golden que representam utilities reguladas (setor Energia Elétrica) mas NÃO setam
     eh_concessionaria — predam o roteamento. Alvo do fix de fidelidade (Task 2). -->
tests/test_consistencia_modos.py:34-54   _empresa_solida        (TAEE11, "Energia Elétrica")
tests/test_consistencia_modos.py:105-129 _empresa_param         (comparáveis, "Energia Elétrica")
tests/test_consistencia_modos.py:132-159 _empresa_param_crescente (AAA3, "Energia Elétrica") ← LOAD-BEARING
tests/test_consistencia_modos.py:180,216-223 test_veredito_direcao_coerente asserta a.veredito.startswith("SUBAVALIADA") e pa.subavaliada is True
tests/test_selo.py:149-170               _empresa_solida        (TAEE11, "Energia Elétrica")
tests/test_guardrails_fix06.py:32-53     _empresa_crescente_solida (SOLID3, "Energia Elétrica")
tests/test_guardrails_fix06.py:103-111   test_banda_degrada_quando_ddm_nao_roda (VAZIA3, setor "" → DEVE cair no default pagadora_regulada p/ manter veredito == "")
tests/test_vulc3_regressao.py:41,105-109 VULC3 ("Têxtil e Vestuário") — NÃO regulada; asserta startswith("VERIFICAR") (verificar, não editar)
src/analista/ingest/build.py:68          setores_concessionaria = (Energia, Saneamento, Água, Gás) — fonte de verdade do que é "utility regulada"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campos em AnaliseAcao + import + roteamento no funil (:134→:136)</name>
  <files>src/analista/report/report.py</files>
  <read_first>
    - src/analista/report/report.py (funil :53-220; dataclass :22-50; imports :16-19; ponto de inserção :133-136)
    - src/analista/core/arquetipo.py (assinatura de classificar + ARQUETIPO_MOTOR — do Plan 01)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-01-SUMMARY.md
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-RESEARCH.md (Pattern 2 + Code Examples)
  </read_first>
  <action>
    Em report.py adicionar `arquetipo` à lista de import `from ..core import capm, ddm, growth, indicators,
    lifecycle, screening` (:16) → incluir `arquetipo`. Em AnaliseAcao (após :50, novo bloco comentado
    `# --- Fase 1 v2.2: roteamento por arquétipo (aditivo, read-only) ---`) adicionar campos:
    `arquetipo: str = ""`, `motor: str = ""`, `arquetipo_fronteirico: bool = False`,
    `arquetipo_candidatos: List[str] = field(default_factory=list)`, `motor_pendente: bool = False`.

    Inserir o roteamento ENTRE :134 (após `a.g_alto = min(a.g_alto, a.ke)`) e :136 (comentário DDM):
    chamar `arq = arquetipo.classificar(c, cfg)`; setar `a.arquetipo = arq.chave`,
    `a.arquetipo_fronteirico = arq.fronteirico`, `a.arquetipo_candidatos = arq.candidatos`;
    `motor = arquetipo.ARQUETIPO_MOTOR.get(arq.chave)`; `a.motor = motor or "pendente_fase_2"`;
    `a.motor_pendente = (motor is None)`. CRÍTICO (Pitfall 5): NÃO alterar o bloco DDM :136-152 — ele
    continua rodando sempre (popula ddm_constante/ddm_h/sensibilidade que a UI exibe como lente e que
    test_guardrails_fix06 exige). Esta task NÃO toca o veredito (:184-207) ainda — isso é a Task 3.
    Resultado esperado desta task isolada: campos populados, veredito ainda inalterado (TAEE11 e ITUB4
    ambos com o veredito DDM de hoje até a Task 3 aplicar a suspensão).
  </action>
  <verify>
    <automated>python -m pytest tests/test_consistencia_modos.py tests/test_ddm.py tests/test_guardrails_fix06.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from ..core import" src/analista/report/report.py` mostra `arquetipo` na lista de import
    - AnaliseAcao tem os 5 campos novos (arquetipo, motor, arquetipo_fronteirico, arquetipo_candidatos, motor_pendente)
    - `arquetipo.classificar(c, cfg)` chamado UMA vez entre a linha do CAPM (`min(a.g_alto, a.ke)`) e o comentário `--- DDM de dois estágios`
    - o bloco DDM `:136-152` está textualmente inalterado (grep confirma `ddm.ddm_dois_estagios` e `ddm.matriz_sensibilidade` intactos)
    - `pytest tests/test_consistencia_modos.py tests/test_ddm.py tests/test_guardrails_fix06.py -q` exits 0
      (veredito ainda inalterado nesta task → nenhum golden muda de direção)
  </acceptance_criteria>
  <done>Import + 5 campos + roteamento inseridos; DDM intacto; veredito ainda intocado; testes-trava verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix de fidelidade das fixtures reguladas (eh_concessionaria=True) — ANTES da suspensão</name>
  <files>tests/test_consistencia_modos.py, tests/test_selo.py, tests/test_guardrails_fix06.py</files>
  <read_first>
    - tests/test_consistencia_modos.py (fixtures :34-54, :105-129, :132-159; teste-alvo :162-223 — leia o assert `a.veredito.startswith("SUBAVALIADA")` :220-223 e o fixture `_empresa_param_crescente("AAA3", ...)` :180)
    - tests/test_selo.py:149-194 (fixture _empresa_solida + test_analisar_acao_popula_selo_coerente)
    - tests/test_guardrails_fix06.py:32-53 (_empresa_crescente_solida) e :103-111 (test_banda_degrada — VAZIA3, assert `a.veredito == ""`)
    - tests/test_vulc3_regressao.py:41,105-117 (VULC3 Têxtil — NÃO regulada; asserta VERIFICAR; NÃO editar, só verificar verde)
    - src/analista/core/arquetipo.py (classificar — do Plan 01) e src/analista/ingest/build.py:68 (setores_concessionaria = Energia/Saneamento/Água/Gás)
  </read_first>
  <action>
    Contexto do bug (verificado por execução): a Task 3 fará `if a.motor_pendente:` sobrepor o bloco de
    veredito DDM. A fixture `_empresa_param_crescente("AAA3", …, payout=0.35)` tem
    setor="Energia Elétrica" mas NUNCA seta `eh_concessionaria` (default False) → cai no refino quantitativo
    (roe≈0.34≥0.15, retenção=0.65≥0.50, cv≈0.37<0.40) → candidatos=["crescimento"] → motor_pendente=True →
    a Task 3 estamparia "VERIFICAR" e quebraria `test_veredito_direcao_coerente` (asserta SUBAVALIADA). Isso é
    uma fixture INCOMPLETA que predaria o roteamento, não um bug do classificador.

    FIDELITY FIX (preferido, ANTES da suspensão landar): AUDITAR cada fixture golden usada em teste que
    afirma `veredito` contra o novo classificador. Para cada fixture cujo setor é uma utility genuinamente
    regulada (Energia Elétrica / Saneamento / Água / Gás — a mesma tupla de `build.py:68`) mas que NÃO seta
    `eh_concessionaria`, adicionar `c.eh_concessionaria = True` no corpo do helper (casando a ingestão real:
    `montar_empresa` derivaria True). Isso faz a fixture rotear `pagadora_regulada` → motor "ddm" →
    motor_pendente=False → mantém o veredito DDM (AAA3 continua SUBAVALIADA). Concretamente:
      - tests/test_consistencia_modos.py: setar `c.eh_concessionaria = True` em `_empresa_solida` (:34-54),
        `_empresa_param` (:105-129) e `_empresa_param_crescente` (:132-159) — todas setor="Energia Elétrica".
      - tests/test_selo.py: setar `c.eh_concessionaria = True` em `_empresa_solida` (:149-170).
      - tests/test_guardrails_fix06.py: setar `c.eh_concessionaria = True` em `_empresa_crescente_solida` (:32-53).

    NÃO editar VAZIA3 (`test_banda_degrada`, :106): setor vazio + sem PL → o classificador degrada para o
    DEFAULT `pagadora_regulada` (candidatos vazios → PAGADORA_REGULADA no Plan 01) → motor_pendente=False →
    a suspensão NÃO dispara → `a.veredito == ""` continua valendo (o DDM não roda por falta de fundamentos).
    Apenas CONFIRMAR isso no audit; não mexer na fixture. NÃO editar VULC3 (Têxtil, não regulada): ela
    continua VERIFICAR seja pela suspensão (se classificar cíclica/crescimento) seja pelo flag DDM-FIX-05 —
    ambos startswith("VERIFICAR"); apenas confirmar verde.

    RESTRIÇÕES: (1) NÃO relaxar NENHUMA asserção — não tocar `startswith("SUBAVALIADA")`/`subavaliada is True`/
    `veredito == ""`/`startswith("VERIFICAR")`. É fidelidade de fixture, não enfraquecimento de teste.
    (2) NÃO estreitar D-04 para disparar só na financeira — crescimento/ciclica/holding também não têm motor
    na Fase 1 e DEVEM suspender igual (por isso o fix é na fixture, não na regra). (3) Se, DEPOIS do fidelity
    fix, alguma fixture NÃO-regulada ainda cruzar para um arquétipo sem motor e virasse um veredito travado,
    a 2ª escolha (não a 1ª) é recalibrar minimamente os thresholds em config.yaml
    (`roe_alto_min`/`retencao_alta_min`/`ciclica_cv_min`) e documentar o porquê no SUMMARY — mas o fidelity
    fix vem primeiro e resolve o caso AAA3.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'tests'); import yaml; import test_consistencia_modos as T; from analista.core import arquetipo as A; cfg=yaml.safe_load(open('config.yaml')); aaa=T._empresa_param_crescente('AAA3', preco=5.5, lucro_inicial=600, g=0.14, pl=5000, payout=0.35); assert aaa.eh_concessionaria is True, 'AAA3 fidelity flag ausente'; assert A.classificar(aaa,cfg).chave=='pagadora_regulada', A.classificar(aaa,cfg).chave; import test_guardrails_fix06 as G; from analista.core.fundamentals import CompanyData; vaz=CompanyData(ticker='VAZIA3', anos=[2024]); assert A.classificar(vaz,cfg).chave=='pagadora_regulada', 'VAZIA3 deve degradar p/ pagadora_regulada'; print('audit-ok')" && python -m pytest tests/test_ddm.py tests/test_selo.py tests/test_consistencia_modos.py tests/test_vulc3_regressao.py tests/test_guardrails_fix06.py -q
</automated>
  </verify>
  <acceptance_criteria>
    - `AAA3` (fixture regulada) agora seta `eh_concessionaria = True` e `classificar(AAA3, cfg).chave == "pagadora_regulada"`
    - `VAZIA3` (setor vazio, sem PL) classifica `pagadora_regulada` (default degradado) → suspensão NÃO dispara → `veredito == ""` preservado
    - `python -m pytest tests/test_ddm.py tests/test_selo.py tests/test_consistencia_modos.py tests/test_vulc3_regressao.py tests/test_guardrails_fix06.py -q` **exits 0** (suíte-trava inteira verde ANTES da suspensão landar)
    - `grep -c "eh_concessionaria = True" tests/test_consistencia_modos.py` >= 3 (as três fixtures Energia Elétrica)
    - NENHUMA asserção de veredito relaxada — `git diff tests/` não remove/afrouxa `startswith`/`is True`/`== ""` algum
    - VULC3 (Têxtil) e EXTR3 (Calçados) intocados; VAZIA3 intocada
  </acceptance_criteria>
  <done>Fixtures reguladas dos goldens setam eh_concessionaria=True (fidelidade à ingestão); AAA3 rota pagadora_regulada; VAZIA3 confirmada no default; suíte-trava verde antes da suspensão. Sem afrouxar asserção.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Suspensão D-04 no veredito (reuso "VERIFICAR") + render + golden e2e (incl. fronteiriço)</name>
  <files>src/analista/report/report.py, tests/test_arquetipo_roteamento.py</files>
  <read_first>
    - src/analista/report/report.py (bloco veredito :170-207; render :410-496; :197-201 frase VERIFICAR existente)
    - src/analista/report/selo.py:105-127 (montar_selo: overlay VERIFICAR suprime faixa/rótulo — firewall)
    - tests/test_guardrails_fix06.py:16-53,83-111 (boilerplate + fixture + asserts de degradação)
    - tests/test_vulc3_regressao.py (invariante: veredito começa com "VERIFICAR")
    - tests/test_arquetipo.py (do Plan 01 — molde de fixture fronteiriça: conflito cíclica×crescimento)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-RESEARCH.md (Pitfall 3 + Code Examples "Suspensão D-04")
  </read_first>
  <behavior>
    - motor_pendente=True -> a.veredito começa com "VERIFICAR — arquétipo {chave} usa o motor '{motor}'..." e a.alertas ganha 1 item explicando a suspensão (D-04); NENHUM prefixo SUBAVALIADA/SOBREAVALIADA estampado. Vale para QUALQUER motor ausente (financeira/crescimento/ciclica/holding) — a regra é `if a.motor_pendente`, não `if arquetipo == financeira`.
    - motor=="ddm" (pagadora_regulada, motor_pendente=False) -> veredito segue EXATAMENTE a lógica DDM atual :184-207; TAEE11 idêntica (ENG-06)
    - selo.montar_selo sobre o veredito "VERIFICAR" retorna verificar=True, faixa_preco=None, rotulo=None -> NÃO estampa 'evitar' (via prefixo reusado, sem tocar selo.py)
    - relatorio_markdown mostra "Arquétipo: {chave} → motor {motor}" no cabeçalho
    - fixture com sinais em conflito (cv>=ciclica_cv_min E roe>=roe_alto_min & retenção>=retencao_alta_min), através de analisar_acao, expõe a.arquetipo_fronteirico is True e len(a.arquetipo_candidatos) >= 2 (WARNING 3 — funil completo)
    - CompanyData degradado (só 1 ano) -> classifica + não levanta; veredito coerente com o motor resolvido (pagadora_regulada default → veredito DDM/"" )
  </behavior>
  <action>
    No bloco veredito de report.py, inserir um guard `if a.motor_pendente:` ANTES do `if a.vmin is not None
    and a.vmax is not None and a.preco_atual:` (:184). No ramo motor_pendente, setar `a.veredito` a uma string
    que COMEÇA com o prefixo "VERIFICAR" (reusar exatamente o prefixo existente — Pitfall 3: NÃO criar prefixo
    novo tipo "AGUARDANDO MOTOR", isso quebraria selo.faixa_do_veredito :88 ↔ report._veredito_token :355):
    texto no estilo "VERIFICAR — arquétipo {a.arquetipo} usa o motor '{a.motor}', que chega na Fase 2; o DDM
    abaixo é lente conservadora, não o motor deste perfil. Referências: Graham/Bazin." e
    `a.alertas.append("Roteamento: {a.arquetipo} → motor pendente (Fase 2). Veredito de preço suspenso para não
    estampar selo por um modelo que não serve a este perfil (D-04).")`. O guard é GENÉRICO por `motor_pendente`
    (NÃO condicionar a `arquetipo == financeira`): crescimento/ciclica/holding também suspendem (D-04). O
    `elif`/`else` restante do bloco (:184-207) roda quando NÃO motor_pendente — lógica DDM ATUAL intacta (TAEE11
    idêntica). Reusar "VERIFICAR" faz `selo.montar_selo` marcar verificar=True sem faixa/rótulo (selo.py:119) →
    não estampa 'evitar', sem tocar selo.py nem os prefixos existentes.

    No render `relatorio_markdown`, estender a linha de cabeçalho (:414-415) acrescentando
    `| *Arquétipo:* {a.arquetipo or '-'} → motor {a.motor or '-'}` (exposição mínima D-04; UX rica é Fase 3).

    Criar tests/test_arquetipo_roteamento.py (boilerplate ROOT+_cfg() de test_guardrails_fix06.py) com asserts e2e
    via `report.analisar_acao(c, _cfg())`: (a) REGULADA — fixture 10 anos setor="Energia Elétrica" +
    `eh_concessionaria=True`: `a.arquetipo=="pagadora_regulada"`, `a.motor=="ddm"`, `a.motor_pendente is False`,
    `a.arquetipo_fronteirico is False`, e o veredito NÃO começa com "VERIFICAR" por motor_pendente (mantém o
    prefixo DDM — TAEE11 idêntica). (b) FINANCEIRA/SUSPENSO — fixture setor="Bancos": `a.motor_pendente is True`,
    `a.veredito.startswith("VERIFICAR")`, e `selo_mod.montar_selo(70.0, a.veredito, _cfg()).verificar is True` e
    `.rotulo is None` (não estampa 'evitar'). (c) ANTI-PETRÓLEO — setor="Petróleo e Gás" + eh_concessionaria=True:
    `a.arquetipo != "pagadora_regulada"` e `a.motor_pendente is True`. (d) DEGRADAÇÃO — CompanyData(ticker="VAZIA3",
    anos=[2024], preco_atual=10.0): analisar_acao não levanta e popula a.arquetipo. (e) FRONTEIRIÇO PELO FUNIL
    (WARNING 3) — fixture 10 anos com setor NÃO-financeiro e NÃO-regulado (ex.: "Extração Mineral",
    eh_concessionaria=False) e sinais em CONFLITO: série de lucro oscilando violento (cv>=ciclica_cv_min) E ao
    mesmo tempo ROE>=roe_alto_min com retenção>=retencao_alta_min → dois candidatos (cíclica + crescimento).
    Rodar `a = report.analisar_acao(c, _cfg())` e afirmar `a.arquetipo_fronteirico is True` e
    `len(a.arquetipo_candidatos) >= 2` (ARQ-02 provado ponta-a-ponta pelo funil, não só no classificador puro).
  </action>
  <verify>
    <automated>python -m pytest tests/test_arquetipo_roteamento.py tests/test_selo.py tests/test_vulc3_regressao.py tests/test_ddm.py tests/test_consistencia_modos.py tests/test_guardrails_fix06.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_arquetipo_roteamento.py -q` exits 0 cobrindo regulada-idêntica / financeira-suspensa / anti-Petróleo / degradação / **fronteiriço-via-funil**
    - fixture financeira: `analisar_acao(...).veredito.startswith("VERIFICAR")` and `.motor_pendente is True`
    - `selo_mod.montar_selo(70.0, <veredito financeira>, cfg).verificar is True and .rotulo is None` (não estampa 'evitar')
    - fixture regulada: `.motor == "ddm"` and `.motor_pendente is False` and veredito NÃO suspenso por roteamento
    - fixture Petróleo e Gás + eh_concessionaria=True: `.arquetipo != "pagadora_regulada"`
    - fixture fronteiriça (conflito de sinais) via `analisar_acao`: `.arquetipo_fronteirico is True and len(.arquetipo_candidatos) >= 2` (WARNING 3)
    - relatorio_markdown(...) contém a substring "Arquétipo:" e "→ motor"
    - `python -m pytest tests/test_ddm.py tests/test_selo.py tests/test_consistencia_modos.py tests/test_vulc3_regressao.py tests/test_guardrails_fix06.py -q` **exits 0** (suíte-trava inteira verde COM a suspensão landada — nenhum golden rebaseado)
    - `python -m pytest -q` verde na suite completa (>= 338 + novos)
    - selo.py e core/ddm.py NÃO modificados (`git diff --name-only` não os lista)
  </acceptance_criteria>
  <done>Suspensão D-04 via "VERIFICAR" (genérica por motor_pendente) + render mínimo + golden e2e c/ fronteiriço; TAEE11 idêntica, ITUB4 suspenso sem 'evitar'; suíte-trava verde; selo.py/ddm.py intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classificar() → analisar_acao (funil) | resultado do classificador consumido no ponto único de valuation |
| report (veredito) → selo (montar_selo) | firewall testado: selo recebe só primitivos (string veredito), não importa report |
| AnaliseAcao → render/CLI/app | campos aditivos read-only exibidos ao usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | prefixo de veredito novo quebra o par faixa_do_veredito ↔ _veredito_token | mitigate | Reusar o prefixo existente "VERIFICAR" (selo.py:119 já suprime faixa/rótulo); NÃO criar token novo. test_selo trava o contrato (Pitfall 3). |
| T-01-06 | Repudiation | regressão silenciosa em TAEE11 (regulada) muda veredito/vmin/vmax sem intenção | mitigate | Golden e2e afirma regulada->motor ddm com veredito idêntico; fix de fidelidade (Task 2) garante que as fixtures reguladas roteiam pagadora_regulada; test_ddm/test_consistencia_modos/test_guardrails_fix06 verdes travam o caminho DDM (ENG-06, Pitfall 5). |
| T-01-07 | Denial of Service | analisar_acao levanta em CompanyData degradado (sinais None) via classificar() | mitigate | classificar() degrada graciosamente (Plan 01) para o default pagadora_regulada; golden VAZIA3 afirma que analisar_acao não levanta E que veredito == "" (motor_pendente=False no default). |
| T-01-08 | Elevation of Privilege | acoplamento selo→report ao suspender veredito | accept/mitigate | Suspensão feita 100% do lado do report (só string de veredito); selo.py não tocado — firewall preservado por construção. test_selo confirma. |
</threat_model>

<verification>
- `python -m pytest tests/test_arquetipo_roteamento.py -q` verde (regulada / financeira-suspensa / anti-Petróleo / degradação / fronteiriço-via-funil).
- `python -m pytest tests/test_ddm.py tests/test_selo.py tests/test_consistencia_modos.py tests/test_vulc3_regressao.py tests/test_guardrails_fix06.py -q` verde (nenhum golden rebaseado; asserções intactas).
- `python -m pytest -q` verde na suite completa.
- `git diff --name-only` NÃO lista src/analista/report/selo.py nem src/analista/core/ddm.py.
- `git diff tests/` não remove/afrouxa nenhuma asserção de veredito (fidelity fix só ADICIONA eh_concessionaria=True).
</verification>

<success_criteria>
- Roteamento inserido no funil entre CAPM e DDM; AnaliseAcao expõe arquétipo/motor/fronteiriço antes do valuation (ARQ-01, ENG-01).
- TAEE11-like (motor ddm) idêntica ao baseline (ENG-06); fixtures reguladas dos goldens setam eh_concessionaria=True (fidelidade à ingestão real).
- ITUB4-like (motor pendente) suspende o veredito primário via "VERIFICAR" e não estampa 'evitar' (D-04) — para QUALQUER motor ausente, sem tocar selo.py/ddm.py.
- Guarda anti-Petróleo confirmada e2e (PETR4-like não vira regulada).
- Fronteiriço provado ponta-a-ponta pelo funil (ARQ-02); render mínimo expõe "Arquétipo → motor"; suite completa verde.
</success_criteria>

<output>
After completion, create `.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-02-SUMMARY.md`
</output>
