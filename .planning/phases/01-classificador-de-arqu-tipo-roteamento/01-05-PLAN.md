---
phase: 01-classificador-de-arqu-tipo-roteamento
plan: 05
type: tdd
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - src/analista/core/arquetipo.py
  - config.yaml
  - tests/test_arquetipo.py
autonomous: true
requirements: [ARQ-01, ARQ-02]

must_haves:
  truths:
    - "classificar() com a série REAL de lucro de WEGE3 (cd_cvm 5410) retorna chave=='crescimento', fronteirico=False"
    - "classificar() com a série REAL de RADL3 (cd_cvm 5258) retorna chave=='crescimento' (não 'ciclica')"
    - "um terceiro compounder/defensivo real (ABEV3 5258→23264, ou LREN3 8133) NÃO é roteado para 'ciclica'"
    - "VALE3 (4170), GGBR4 (3980), SUZB3 (13986) e PETR4 (9512) — cíclicas reais — continuam chave=='ciclica'"
    - "o mecanismo fronteiriço (ARQ-02) continua disparando em conflito genuíno de sinais"
    - "python -m pytest -q → >= 356 passed, 0 failed (nenhum dos 355 regride; ddm.py/selo.py intocados)"
  artifacts:
    - path: "src/analista/core/arquetipo.py"
      provides: "sinal de ciclicidade robusto (dispersão dos resíduos de ajuste log-linear da série de lucro)"
      contains: "_cv_lucro"
    - path: "config.yaml"
      provides: "ciclica_cv_min recalibrado para a escala da nova métrica (resíduos log-lineares)"
      contains: "ciclica_cv_min"
    - path: "tests/test_arquetipo.py"
      provides: "goldens com SÉRIES REAIS de compounders e cíclicas (não progressão geométrica sintética)"
      contains: "WEGE3"
  key_links:
    - from: "src/analista/core/arquetipo.py::classificar"
      to: "_cv_lucro"
      via: "cv = _cv_lucro(c.serie('lucro_liquido'))"
      pattern: "_cv_lucro\\(c\\.serie"
    - from: "src/analista/core/arquetipo.py::classificar"
      to: "config.yaml arquetipo.ciclica_cv_min"
      via: "arq.get('ciclica_cv_min', ...)"
      pattern: "ciclica_cv_min"
---

<objective>
Fechar o Achado 1a (o gap BLOCKER reaberto na 01-VERIFICATION.md, SC#1 / CR-01): o sinal de
ciclicidade `_cv_lucro` — hoje o CV dos RETORNOS ano-a-ano — penaliza a variância da TAXA de
crescimento e continua misrouteando compounders reais de crescimento desigual (WEGE3, RADL3)
para `ciclica`/fronteiriço. A causa raiz sobreviveu ao 01-03 porque o golden usa uma progressão
geométrica perfeitamente suave (`1.18**i`) que não existe em nenhuma empresa real.

Trocar a métrica para a **dispersão dos resíduos de um ajuste log-linear** da série de lucro
(lead do CR-01 e da 01-VERIFICATION.md), recalibrar `ciclica_cv_min` para a escala nova, e
travar a correção com **goldens de séries REAIS** (compounders → crescimento; cíclicas →
ciclica), não sintéticas.

Purpose: sem este sinal correto, 18/22 ações caem em ciclica→motor_pendente→VERIFICAR e o
core-value (roteamento correto antes de valuar) não se sustenta; a Fase 2 (SC#3 "WEGE3 usa DCF")
pressupõe WEGE3=='crescimento'.
Output: `_cv_lucro` robusto, `ciclica_cv_min` recalibrado, goldens reais anti-regressão.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-AUDIT-COERENCIA.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-VERIFICATION.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-REVIEW.md

<interfaces>
<!-- Contratos que o executor consome. Extraídos do código atual — não precisa explorar. -->

De src/analista/core/arquetipo.py:
  def _cv_lucro(serie: list) -> Optional[float]   # sinal a SUBSTITUIR (hoje CV dos retornos ano-a-ano)
  def classificar(c: CompanyData, cfg: dict) -> ResultadoArquetipo
  @dataclass ResultadoArquetipo: chave, fronteirico, candidatos, confianca, sinais
  ARQUETIPO_MOTOR = {financeira:None, pagadora_regulada:"ddm", ciclica:None, crescimento:None, holding:None}
  # No refino (linhas 118-142): cv >= ciclica_cv_min -> candidato CICLICA;
  #   roe>=roe_alto_min E retencao>=retencao_alta_min -> candidato CRESCIMENTO;
  #   >=2 candidatos distintos -> fronteirico=True, chave=distintos[0] (ciclica ganha por ordem de append).

De src/analista/core/fundamentals.py (CompanyData):
  c.serie("lucro_liquido") -> list[float|None]   # série cronológica de lucro (fonte CVM)
  c.roe_valuation() / c.payout_valuation() -> Optional[float]

De config.yaml, bloco arquetipo: (linhas 177-198):
  ciclica_cv_min: 0.50   # CORTE a recalibrar. roe_alto_min:0.15  retencao_alta_min:0.50
</interfaces>

<calibration_data>
<!-- Evidência REAL levantada no planejamento (offline, cvm.fundamentos_do_ano, anos 2015-2023).
     Compara a métrica atual (CV dos retornos) vs. a proposta (pstdev dos resíduos log-lineares).
     A métrica proposta SEPARA compounders de cíclicas; a atual NÃO. Calibre contra ISTO. -->
ticker  tipo         n  cv_retornos(ATUAL)  resid_loglinear(PROPOSTA)
WEGE3   compounder   9        0.795               0.174
RADL3   compounder   9        1.460               0.156
ABEV3   defensivo    9        4.916               0.158
LREN3   ?            9        3.026               0.220
TOTS3   compounder   9        2.253               0.490   (borderline — evitar como fixture-âncora)
VALE3   ciclica      9        1.900               0.500
GGBR4   ciclica      9        1.609               0.610
SUZB3   ciclica      9        4.029               1.032
PETR4   ciclica      9        2.083               1.243
# Corte na escala nova cai na faixa ~0.45-0.50 (VALE3=0.50 ainda ciclica; WEGE3/RADL3<0.22 crescimento).
# ATENÇÃO: PETR4/GGBR4 têm ANOS DE PREJUÍZO na janela — log(≤0) é indefinido. Um ano de prejuízo
# é, por si só, forte evidência de ciclicidade: trate lucro<=0 como sinal cíclico (não descarte silencioso).
</calibration_data>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Substituir o sinal de ciclicidade por dispersão de resíduos log-lineares + recalibrar o corte</name>
  <files>src/analista/core/arquetipo.py, config.yaml, tests/test_arquetipo.py</files>
  <read_first>
    - src/analista/core/arquetipo.py (função _cv_lucro linhas 58-81; uso do corte linhas 126-127)
    - config.yaml (bloco arquetipo:, linhas 171-198 — comentário inline de ciclica_cv_min)
    - tests/test_arquetipo.py (fixture _empresa linhas 34-48; golden sintético a SUBSTITUIR linhas 112-124)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-VERIFICATION.md (bloco `missing:` — lead dos resíduos log-lineares, pstdev≈0.174 na WEGE real)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-REVIEW.md (CR-01)
  </read_first>
  <behavior>
    - RED: adicionar goldens com a SÉRIE REAL de lucro de WEGE3 (cd_cvm 5410) e RADL3 (5258)
      congelada como literais na fixture `_empresa` (derivar os números via cvm.fundamentos_do_ano
      durante o desenvolvimento e FIXAR como lista, citando cd_cvm+anos no comentário — determinístico,
      sem rede/cache no teste). Com o código atual, o golden de WEGE3 real FALHA (retorna 'ciclica'/
      fronteirico=True), reproduzindo o gap.
    - GREEN: WEGE3 real → chave=='crescimento', fronteirico==False; RADL3 real → 'crescimento'.
    - Cíclicas reais (VALE3 4170, GGBR4 3980 com ano de prejuízo) → chave=='ciclica'.
    - Remover/atualizar o golden sintético `test_compounder_realista_wege_vira_crescimento`
      (progressão 1.18**i) — substituí-lo pela série REAL de WEGE3 (a 01-VERIFICATION exige que o
      golden replique retornos ano-a-ano reais, não uma progressão geométrica perfeita).
  </behavior>
  <action>
Reescrever `_cv_lucro` (mantendo o nome e a assinatura para não quebrar o call-site em classificar)
para medir a OSCILAÇÃO como a **dispersão (pstdev) dos resíduos de um ajuste log-linear** da série
de lucro — não mais o CV dos retornos ano-a-ano. Ajuste OLS de ln(lucro) sobre o índice de tempo
sobre os anos com lucro POSITIVO; o resíduo é (ln(lucro_t) − reta_ajustada_t). Um compounder
monotônico gruda na reta (pstdev baixo ~0.15-0.22); um cíclico oscila em torno da tendência
(pstdev alto ~0.50+). Tratar ANO DE PREJUÍZO (lucro ≤ 0, log indefinido) como evidência CÍCLICA
explícita — não descartar em silêncio: se houver ano(s) de prejuízo na janela, o sinal deve
resultar em valor ACIMA do corte (empresa que dá prejuízo em algum ano é cíclica por definição
neste método). Preservar os guards de None existentes (< 3 pontos válidos → None; degrada gracioso
para o default, Pitfall 2). Função pura, O(n), sem I/O.
Recalibrar `arquetipo.ciclica_cv_min` em config.yaml para a escala nova (partir de ~0.45-0.50 usando
a tabela `<calibration_data>`: VALE3=0.500 deve ficar ≥ corte; WEGE3=0.174/RADL3=0.156 devem ficar
< corte). Atualizar o comentário inline do config para citar a escala real (compounder ~0.15-0.22;
cíclico ~0.50-1.24) em vez dos regimes sintéticos antigos. NÃO tocar roe_alto_min/retencao_alta_min.
Usar a série REAL nas fixtures (literais congelados com cd_cvm citado), NUNCA progressão geométrica.
Não usar TOTS3 como âncora (resid 0.490, borderline com VALE3 0.500).
  </action>
  <verify>
    <automated>python -m pytest tests/test_arquetipo.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `classificar()` numa fixture com a série real de WEGE3 (5410) retorna chave=='crescimento' e fronteirico==False.
    - `classificar()` numa fixture com a série real de RADL3 (5258) retorna chave=='crescimento'.
    - `classificar()` numa fixture com a série real de VALE3 (4170) e de GGBR4 (3980, com prejuízo) retorna chave=='ciclica'.
    - `_cv_lucro` sobre a série real de WEGE3 retorna valor < ciclica_cv_min; sobre VALE3 retorna >= ciclica_cv_min.
    - O golden sintético 1.18**i foi substituído por série real (grep por "1.18" em tests/test_arquetipo.py retorna 0).
    - `grep -n "ciclica_cv_min" config.yaml` mostra o novo valor com comentário na escala de resíduos.
  </acceptance_criteria>
  <done>Sinal de ciclicidade robusto a variância de taxa de crescimento; WEGE3/RADL3 reais classificam crescimento; cíclicas reais preservadas; corte recalibrado.</done>
</task>

<task type="auto">
  <name>Task 2: Golden de validação multi-ticker (compounders vs. cíclicas reais) + fronteiriço preservado</name>
  <files>tests/test_arquetipo.py</files>
  <read_first>
    - tests/test_arquetipo.py (após a Task 1 — fixtures reais já introduzidas)
    - src/analista/core/arquetipo.py (classificar — resultado final pós-Task 1)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-AUDIT-COERENCIA.md (Achado 1 — tabela de misroutes reais)
  </read_first>
  <action>
Adicionar um golden parametrizado que trava a calibração contra AMBOS os regimes com séries REAIS
(literais congelados, cd_cvm citado): compounders/defensivos {WEGE3 5410, RADL3 5258, e um terceiro
LIMPO — ABEV3 23264 (resid 0.158) ou LREN3 8133 (0.220), NÃO TOTS3} devem retornar chave != 'ciclica';
cíclicas {VALE3 4170, GGBR4 3980, SUZB3 13986, PETR4 9512} devem retornar chave == 'ciclica'.
Adicionar/preservar um caso de CONFLITO GENUÍNO que ainda dispare o fallback honesto (ARQ-02):
fronteirico==True, len(candidatos)>=2, confianca=='baixa' — garantindo que a correção do sinal NÃO
matou o mecanismo fronteiriço (o teste test_conflito_de_sinais_marca_fronteirico já existente serve;
confirmar que continua verde ou adaptar a fixture se a nova métrica mudou seu resultado). Comentar no
teste que a fonte é o audit empírico (2/setor) — a calibração é validada em >=3 compounders reais,
não num único ponto (exigência da 01-VERIFICATION).
  </action>
  <verify>
    <automated>python -m pytest tests/test_arquetipo.py -q && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Golden parametrizado cobre >=3 compounders reais (todos != 'ciclica') e >=4 cíclicas reais (todas == 'ciclica').
    - Um caso de conflito genuíno retorna fronteirico==True, len(set(candidatos))>=2, confianca=='baixa'.
    - `python -m pytest -q` → >= 356 passed, 0 failed.
    - `git log --oneline -- src/analista/core/ddm.py src/analista/report/selo.py` não mostra commit novo desta fase (intocados).
  </acceptance_criteria>
  <done>Calibração travada contra realidade multi-ticker; regressão futura do sinal é pega por golden real; fronteiriço (ARQ-02) preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml → engine | Threshold `ciclica_cv_min` (arquivo local versionado, sem input externo). |
| CVM cache → fixtures | Séries de lucro congeladas como literais no teste (offline, determinístico). |

Superfície mínima: refactor de função pura (`_cv_lucro`) + recalibração de um número em config +
goldens. Nenhum input externo novo, nenhuma API/segredo/rede introduzidos.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0105-01 | Tampering | ciclica_cv_min mal calibrado leva a misroute (integridade do veredito) | mitigate | Goldens de séries reais travam ambos os regimes (compounder<corte, cíclica>=corte); regressão pega em CI. |
| T-0105-02 | Denial of Service | série com poucos pontos / ano de prejuízo derruba `_cv_lucro` com exceção | mitigate | Guards de None preservados (< 3 pontos → None → default); prejuízo tratado como caminho explícito, sem log(≤0). |
| T-0105-03 | Information Disclosure | n/a — função pura sobre dados públicos de fundamentos, sem PII/segredo | accept | Sem dado sensível; nada a proteger. |
</threat_model>

<verification>
- `python -m pytest tests/test_arquetipo.py -q` verde (incl. novos goldens reais).
- `python -m pytest -q` → >= 356 passed, 0 failed (355 baseline + novos, nenhum regride).
- `_cv_lucro` puro (sem I/O); grep confirma corte recalibrado no config com comentário na escala nova.
- ddm.py e selo.py intocados (firewall preservado).
</verification>

<success_criteria>
WEGE3, RADL3 e um terceiro compounder reais classificam fora de 'ciclica' (WEGE3/RADL3 =='crescimento');
VALE3/GGBR4/SUZB3/PETR4 reais permanecem 'ciclica'; o fallback honesto (ARQ-02) segue funcional;
suíte >= 356 verde sem tocar ddm.py/selo.py.
</success_criteria>

<output>
Após completar, criar `.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-05-SUMMARY.md`.
</output>
