---
phase: 13-motores-contrato-de-sa-da-eng
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/analista/core/arquetipo.py
  - tests/test_arquetipo.py
  - tests/test_arquetipo_roteamento.py
  - tests/test_vulc3_regressao.py
  - tests/classificacao.yaml
autonomous: true
requirements: [ENG-03, ENG-04]
must_haves:
  truths:
    - "O classificador ganha `ARQUETIPO_ANCORA_ROE` (arquétipo → política de derivação de insumo, erro limitado) — o registry que o RIM único (Plano 03) consome"
    - "`PAGADORA_REGULADA` foi separada em `PAGADORA_MADURA` (novo default-por-eliminação) + `CONCESSAO_FINITA` (hard-route de `eh_concessionaria`)"
    - "Empresa sem sinal cai em `PAGADORA_MADURA` (RIM normal), não mais no balde da transmissora"
    - "A guarda anti-Petróleo (`_setor_casa_token` com `regulada_excluir`) permanece no hard-route de `CONCESSAO_FINITA`"
    - "O legado `ARQUETIPO_MOTOR` permanece VIVO (rótulos novos mapeados como o antigo regulada→'ddm') só para não quebrar `freio.motor_pendente` entre ondas — sua deleção é o Plano 06"
    - "Nenhuma referência viva à STRING 'pagadora_regulada' sobra em `tests/` — os testes que quebram na onda 2 pela mudança de rótulo foram migrados (rewrite) ou deletados no MESMO diff, mantendo a suíte default 0 failed"
  artifacts:
    - path: "src/analista/core/arquetipo.py"
      provides: "Registry ARQUETIPO_ANCORA_ROE (arquétipo→política) + split do arquétipo; ARQUETIPO_MOTOR legado mantido"
      contains: "ARQUETIPO_ANCORA_ROE"
    - path: "tests/classificacao.yaml"
      provides: "Entradas dos testes de roteamento renomeados/deletados no MESMO diff (0 órfão)"
  key_links:
    - from: "arquetipo.classificar"
      to: "CONCESSAO_FINITA / PAGADORA_MADURA"
      via: "split do hard-route :159-160 e do default :180"
      pattern: "CONCESSAO_FINITA|PAGADORA_MADURA"
    - from: "arquetipo.ARQUETIPO_ANCORA_ROE"
      to: "política de derivação de ROE-âncora"
      via: "dict str→política consumido pelo report.py (Plano 03)"
      pattern: "ARQUETIPO_ANCORA_ROE"
  anti_goals:
    - "NÃO consertar o `g` das transmissoras aqui — `CONCESSAO_FINITA` é rótulo/rota; a mecânica do carve-out (g_terminal) é o Plano 03"
    - "NÃO mudar o CORPO do classificador (a árvore de decisão) além das duas linhas de split — o corpo sobrevive intocado (ENG-03: sobrevive e melhora)"
    - "NÃO deletar `ARQUETIPO_MOTOR` aqui — deletar quebra `freio.motor_pendente` entre as ondas e deixa a suíte vermelha; a deleção é o Plano 06 com o último consumidor"
    - "NÃO ATUALIZAR golden de nível para um valor novo — golden/baseline de uma ROTA que deixou de existir é DELETADO (função + linha classificacao no mesmo diff)"
    - "NÃO deixar entrada órfã em classificacao.yaml — teste renomeado/deletado casa a linha no MESMO diff (quebra a coleta se driftar)"
---

<objective>
Adicionar o registry `ARQUETIPO_ANCORA_ROE` — arquétipo → **política de derivação de ROE-âncora** (erro
limitado, ENG-03), o mapa que o Plano 03 consome — e separar `PAGADORA_REGULADA` em `PAGADORA_MADURA`
(novo default-por-eliminação, RIM normal) + `CONCESSAO_FINITA` (hard-route de `eh_concessionaria`,
carve-out declarado ANTES do hold-out, ENG-04). O CORPO do classificador sobrevive intocado; só o
registry novo e as duas linhas de split mudam. O legado `ARQUETIPO_MOTOR` é MANTIDO vivo (mapeando os
rótulos novos como o antigo regulada→"ddm") para não quebrar `freio.motor_pendente` até o Plano 06.

**Correção do checker (BLOCKER, parte da onda 2):** a mudança da STRING de arquétipo (classificar deixa
de devolver "pagadora_regulada") QUEBRA, já na onda 2, todo teste que asseve essa string ao vivo — não
só em `test_arquetipo.py`, mas também em `test_arquetipo_roteamento.py` e nos capstones de
`test_vulc3_regressao.py`. Esses testes são migrados/deletados AQUI (no diff que muda o rótulo), senão a
suíte default fica vermelha na onda 2.

Purpose: dar ao Plano 03 o mapa do RIM único, tirar as empresas sem sinal do balde da transmissora, e
manter a onda 2 verde.
Output: `ARQUETIPO_ANCORA_ROE` + rótulos novos, `ARQUETIPO_MOTOR` legado, e zero referência viva à string
morta em `tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md
@.planning/spikes/eng-rim-104.md

@src/analista/core/arquetipo.py
@src/analista/core/freio.py
@tests/test_arquetipo.py
@tests/test_arquetipo_roteamento.py
@tests/test_vulc3_regressao.py
@tests/classificacao.yaml

<interfaces>
<!-- Estado atual (arquetipo.py) — o que muda e o que fica vivo -->
Constantes: FINANCEIRA, PAGADORA_REGULADA, CICLICA, CRESCIMENTO, HOLDING.
Registry atual (arquetipo.py:48): ARQUETIPO_MOTOR = {FINANCEIRA:"rim", PAGADORA_REGULADA:"ddm",
  CICLICA:"normalizado", CRESCIMENTO:"dcf", HOLDING:"nav"}.
Consumidor externo do legado (NÃO deletar aqui): freio.motor_pendente (freio.py:18) faz
  arquetipo.ARQUETIPO_MOTOR.get(arq.chave) != "ddm".
classificar (:124): hard-route regulada eh_concessionaria and not _setor_casa_token(setor,
  regulada_excluir) -> PAGADORA_REGULADA (:159-160); default candidatos.append(PAGADORA_REGULADA) (:180).
Política nova (RESEARCH Mapa de âncoras): "through_cycle" | "through_cycle_sem_g" | "normalizado" |
  "atual_fade" | "nav_piso".
Testes que asseveram a STRING de arquétipo ao vivo (quebram na onda 2): test_arquetipo.py
  (concessionaria/petroleo), test_arquetipo_roteamento.py (test_petroleo_nao_vira_pagadora_regulada,
  test_regulada_mantem_motor_ddm_e_veredito_ddm), test_vulc3_regressao.py
  (test_capstone_taee11_baseline_ddm_identico asseve a.arquetipo=="pagadora_regulada").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ARQUETIPO_ANCORA_ROE + split das constantes (ARQUETIPO_MOTOR legado mantido)</name>
  <files>src/analista/core/arquetipo.py</files>
  <read_first>
    - src/analista/core/arquetipo.py (constantes :38-41, ARQUETIPO_MOTOR :48-54, classificar :124-190, _setor_casa_token :107)
    - src/analista/core/freio.py (motor_pendente :18 — o consumidor do legado; por que ARQUETIPO_MOTOR não pode sumir agora)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md (D-01, D-03, D-05)
    - .planning/spikes/eng-rim-104.md (madura/concessão ficam sãs sob RIM único)
  </read_first>
  <action>
    Em arquetipo.py: (1) adicionar as constantes PAGADORA_MADURA = "pagadora_madura" e CONCESSAO_FINITA
    = "concessao_finita"; remover a constante PAGADORA_REGULADA (o rótulo antigo deixa de ser devolvido).
    (2) ADICIONAR ARQUETIPO_ANCORA_ROE — dict arquétipo -> política de derivação de ROE-âncora (str), por
    D-01/D-03/§"Mapa de âncoras": FINANCEIRA->"through_cycle", PAGADORA_MADURA->"through_cycle",
    CONCESSAO_FINITA->"through_cycle_sem_g" (carve-out; a mecânica do g é o Plano 03), CICLICA->
    "normalizado", CRESCIMENTO->"atual_fade", HOLDING->"nav_piso". (3) MANTER ARQUETIPO_MOTOR vivo,
    atualizando as chaves aos rótulos novos e mapeando ambos os herdeiros do regulada para "ddm"
    (PAGADORA_MADURA->"ddm", CONCESSAO_FINITA->"ddm", espelhando o antigo PAGADORA_REGULADA->"ddm"), com
    comentário de que é LEGADO a remover no Plano 06 — assim freio.motor_pendente segue idêntico.
    (4) No corpo de classificar: linha :159-160 -> devolve CONCESSAO_FINITA (mantendo
    _setor_casa_token(setor, regulada_excluir)); linha :180 -> candidatos.append(PAGADORA_MADURA). NADA
    MAIS do corpo muda.
  </action>
  <acceptance_criteria>
    - ARQUETIPO_ANCORA_ROE existe com 6 chaves; valores são strings de política (grep por "rim"/"dcf"/"nav"/"normalizado" DENTRO do dict ANCORA == 0)
    - classificar devolve CONCESSAO_FINITA no hard-route de eh_concessionaria e PAGADORA_MADURA no default (grep confirma ambos); a constante PAGADORA_REGULADA não existe mais (grep -c 'PAGADORA_REGULADA' src/analista/core/arquetipo.py == 0)
    - ARQUETIPO_MOTOR PERMANECE definido, com PAGADORA_MADURA e CONCESSAO_FINITA mapeados para "ddm" (grep confirma) — freio.motor_pendente não quebra
    - A guarda anti-Petróleo (_setor_casa_token(setor, regulada_excluir)) permanece no hard-route de concessão
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'ARQUETIPO_ANCORA_ROE' src/analista/core/arquetipo.py && grep -q 'ARQUETIPO_MOTOR' src/analista/core/arquetipo.py && test $(grep -c 'PAGADORA_REGULADA' src/analista/core/arquetipo.py) -eq 0</automated>
  </verify>
  <done>Registry de âncora adicionado, split PAGADORA_MADURA/CONCESSAO_FINITA no corpo, ARQUETIPO_MOTOR legado preserva o freio, guarda anti-Petróleo intacta.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar/deletar TODOS os testes que quebram pela string de arquétipo (onda 2) + classificacao no mesmo diff</name>
  <files>tests/test_arquetipo.py, tests/test_arquetipo_roteamento.py, tests/test_vulc3_regressao.py, tests/classificacao.yaml</files>
  <read_first>
    - `grep -rn 'pagadora_regulada\|PAGADORA_REGULADA' tests/` (o inventário EXATO dos testes que quebram na onda 2 — a fonte de verdade desta task)
    - tests/test_arquetipo.py, tests/test_arquetipo_roteamento.py, tests/test_vulc3_regressao.py (as funções listadas)
    - tests/classificacao.yaml (completude imposta na coleta pelo conftest)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§Carve-out — test_petroleo migra de rótulo, comportamento idêntico; §Pitfall 5 — golden que quebra é DELETADO)
    - CLAUDE.md (golden de nível DELETADO não atualizado; nunca afrouxar/xfail→skip)
  </read_first>
  <action>
    Tratar TODO teste que asseve a string "pagadora_regulada" ao vivo (use o grep do read_first como
    inventário canônico), com a decisão DELETE-vs-REWRITE explícita por teste:
    - **REWRITE** (invariante de roteamento que SOBREVIVE com relabel): concessionária -> CONCESSAO_FINITA;
      empresa sem sinal -> PAGADORA_MADURA; Petróleo com eh_concessionaria NÃO cai em CONCESSAO_FINITA
      (guarda anti-Petróleo, comportamento idêntico, só o rótulo-alvo muda) — ex.:
      test_arquetipo.py::test_concessionaria_vira_pagadora_regulada (-> ..._vira_concessao_finita),
      test_arquetipo_roteamento.py::test_petroleo_nao_vira_pagadora_regulada (relabel do alvo).
    - **DELETE** (baseline/golden de uma ROTA que deixou de existir): a premissa "regulada usa o motor
      DDM" foi demolida (regulada agora roda o RIM) — ex.:
      test_arquetipo_roteamento.py::test_regulada_mantem_motor_ddm_e_veredito_ddm e
      test_vulc3_regressao.py::test_capstone_taee11_baseline_ddm_identico (asseve
      a.arquetipo=="pagadora_regulada" + a.motor=="ddm"). Deletar função + linha da classificacao.yaml no
      MESMO diff (0 órfão), NUNCA atualizar o número/rótulo.
    Os capstones de test_vulc3_regressao.py que NÃO tocam a string (só motor/campos) e os invariantes a
    RESCREVER (cascata/normalização) ficam para o Plano 03 (quebram só na onda 3). Commit toca tests/ sem
    config.yaml (não dispara o hook BLIND-05).
  </action>
  <acceptance_criteria>
    - `grep -rc 'pagadora_regulada\|PAGADORA_REGULADA' tests/` soma 0 (nenhuma referência viva à string/constante morta em testes)
    - `.venv/bin/python -m pytest -q` (suíte default, onda 2) 0 failed; coleta sem CLASSIFICACAO ORFA (toda função migrada/deletada casada em classificacao.yaml)
    - Testes REWRITE asseveram os rótulos novos (grep CONCESSAO_FINITA/PAGADORA_MADURA nos arquivos de teste); testes DELETE removidos com a linha da classificacao no mesmo diff
    - Nenhuma entrada golden_nivel ATUALIZADA (só REMOÇÃO das linhas dos testes deletados; nenhum valor de nível vivo alterado)
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -rc 'pagadora_regulada\|PAGADORA_REGULADA' tests/ 2>/dev/null | awk -F: '{s+=$2} END{print s+0}') -eq 0 && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>Zero referência viva à string de arquétipo morta em tests/; REWRITE dos invariantes de roteamento, DELETE dos baselines da rota DDM; suíte default verde na onda 2; coleta sem órfão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setor/eh_concessionaria (CVM) → classificar | Sinal de setor não confiável (over-match de token) pode mis-rotear Petróleo para concessão |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering (mis-route) | hard-route de CONCESSAO_FINITA | mitigate | Guarda anti-Petróleo _setor_casa_token(setor, regulada_excluir) preservada; teste de roteamento negativo migrado (rewrite) |
| T-13-04 | Denial (crash) | classificar / freio.motor_pendente sob rótulo novo | mitigate | ARQUETIPO_MOTOR mantido com os rótulos novos (.get não vira KeyError); cada sinal guardado com is not None |
| T-13-20 | Tampering (overfit) | golden/baseline da rota DDM atualizado em vez de deletado | mitigate | Baseline de rota morta é DELETADO (função+classificacao mesmo diff); nenhum valor de nível atualizado |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` (default, onda 2) 0 failed; coleta sem CLASSIFICACAO ORFA.
- `grep -rc 'pagadora_regulada\|PAGADORA_REGULADA' tests/` soma 0; ARQUETIPO_ANCORA_ROE presente; ARQUETIPO_MOTOR legado vivo.
- Guarda anti-Petróleo intacta; split PAGADORA_MADURA/CONCESSAO_FINITA no corpo.
</verification>

<success_criteria>
O registry de âncora existe, o split PAGADORA_MADURA/CONCESSAO_FINITA está no lugar (guarda anti-Petróleo
intacta), o legado ARQUETIPO_MOTOR preserva o freio, e TODOS os testes que quebram pela mudança de rótulo
na onda 2 foram migrados/deletados no mesmo diff — suíte default verde, pronto para o Plano 03.
</success_criteria>

<output>
After completion, create `.planning/phases/13-motores-contrato-de-sa-da-eng/13-02-SUMMARY.md`
</output>
