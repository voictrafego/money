---
phase: 13-motores-contrato-de-sa-da-eng
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - src/analista/core/valuation.py
  - src/analista/report/report.py
  - tests/test_eng_contrato.py
  - tests/test_eng_ponte_pb.py
  - tests/classificacao.yaml
autonomous: true
requirements: [ENG-05, ENG-06, ENG-07, ENG-08, ENG-09]
must_haves:
  truths:
    - "A tríade SUBAVALIADA / NO INTERVALO / SOBREAVALIADA vem de `V` vs a região `[V×(1−MS), V×(1+MS)]` (do RIM único), não mais do prefixo do veredito do DDM"
    - "A região de valor simétrica é o caminho PRIMÁRIO e ÚNICO do veredito (não fallback)"
    - "A margem de segurança é parâmetro (`veredito.margem_seguranca`) consumido simetricamente — controle do usuário, nunca calibrado"
    - "A ponte auditável `P/B justo = 1 + (ROE_T − Ke)/(Ke − g)`, `V=P/B×VPA`, `payout_T = 1 − g/ROE_T` é computada e exibível, e é um TESTE de correção"
    - "Guarda-corpo em dois níveis: teste FALHA se `payout_T ∉ (0,1)` ou `P/B justo ∉ (0,6)`; runtime DEGRADA never-raise (não levanta)"
    - "A matriz de sensibilidade Ke×g vive, em torno do `a.ke` correto (herdada da Fase 12, reusada)"
  artifacts:
    - path: "src/analista/core/valuation.py"
      provides: "Helpers puros pb_justo/payout_terminal (identidade fechada do BLIND-02a)"
      contains: "def pb_justo"
    - path: "src/analista/report/report.py"
      provides: "Região da MS como caminho primário + tríade de V + ponte P/B + guard runtime"
    - path: "tests/test_eng_ponte_pb.py"
      provides: "Teste de correção da razão implícita (invariante)"
  key_links:
    - from: "report.py veredito"
      to: "região [V×(1−MS), V×(1+MS)]"
      via: "vmin/vmax simétricos sobre intrinseco_motor como caminho único"
      pattern: "margem_seguranca"
    - from: "report.py ponte P/B"
      to: "core.valuation.pb_justo / payout_terminal"
      via: "ROE_T=_roe_through_cycle, Ke=a.ke, g=g_T"
      pattern: "pb_justo|payout_terminal"
  anti_goals:
    - "NÃO calibrar a MS contra dispersão, preço ou taxa de 'compra' (Armadilha 4). A MS é escolha do usuário; default de config simétrico"
    - "NÃO inventar contrato novo (preço-teto/Bazin/Comprar-Aguardar/MS escalonada Morningstar): 0 ocorrências no livro"
    - "NÃO asserir `P/B×VPA == intrinseco_motor` — o RIM é multiestágio; a ponte é decomposição steady-state (lente auditável), o teste é sobre a RAZÃO implícita"
    - "NÃO fazer o guard P/B 'pegar' o CGRA4 a 921× (é bug de DADO, P/B implícito ~1,4 sano); o guard pega patologia de MODELO"
    - "NÃO validar o caso do livro (ITUB4=R$37,22): Fase 14. Nenhum assert com ticker+número-alvo"
    - "NÃO mudar o default `margem_seguranca` no config aqui (é co-change config+lock do Plano 05)"
---

<objective>
Entregar o **contrato de saída do livro** (Cap. 17) sobre o RIM único: (1) a tríade SUBAVALIADA / NO
INTERVALO / SOBREAVALIADA computada de `V` vs a região `[V×(1−MS), V×(1+MS)]` (D-09), com a região
simétrica como caminho **primário e único** (não fallback); (2) a **ponte auditável P/B** exibível e
como **teste de correção** da razão implícita (ENG-08); (3) o **guarda-corpo em dois níveis** (ENG-09,
D-10): teste FALHA se a razão for patológica, runtime DEGRADA never-raise; (4) confirmar a **matriz Ke×g**
(ENG-07) reusada da Fase 12 em torno do `a.ke` correto. A MS é **controle do usuário** (parâmetro),
**nunca calibrada** (ENG-06).

Purpose: o app já está "quase certo" no contrato — o trabalho é RELIGAR a banda do veredito à região da
MS sobre o RIM e expor a ponte como sanidade da razão. O número final (ITUB4) é a Fase 14.
Output: `core/valuation.py` (identidade P/B pura), região/tríade/ponte/guard em `report.py`, testes de
correção `invariante`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md

@src/analista/report/report.py
@tests/test_invariantes_v24.py

<interfaces>
<!-- Sites (medidos) e a identidade já testada -->
report.py: veredito :639-663 (árvore SUB/NO INTERVALO/SOBRE — forma JÁ certa, só muda a FONTE da banda);
  `vmin/vmax` simétricos sobre `intrinseco_motor` já existem como fallback em :608-610 (viram primário);
  `_roe_through_cycle` :184 (ROE_T); `a.ke` (Ke pronto, Fase 12); `g_T` (Fase 11); matriz Ke×g
  `ddm.matriz_sensibilidade` montada em :551. Flags de risco (VERIFICAR) :645-657 — sobrevivem.
config: `veredito.margem_seguranca` (config.yaml:118, default hoje 0.15 — o default vira 0.05/0.10 no
  Plano 05; aqui só CONSUMIR o valor de cfg).
Identidade fechada (BLIND-02a, já testada, `tests/test_invariantes_v24.py:96`):
  `pb_justo(roe,ke,g) = 1.0 + (roe - ke)/(ke - g)`  — representação steady-state (Gordon-RIM).
  Insumos da ponte = os TERMINAIS: ROE_T = _roe_through_cycle, Ke = a.ke, g = g_T.
  `payout_terminal(roe_t, g) = 1.0 - g/roe_t`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extrair a identidade P/B para core/valuation.py + teste de correção da razão</name>
  <files>src/analista/core/valuation.py, tests/test_eng_ponte_pb.py, tests/classificacao.yaml</files>
  <read_first>
    - tests/test_invariantes_v24.py (`pb_justo` :96 — a função pura já testada no BLIND-02a; reusar, não reimplementar)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Ponte P/B auditável" — nuance: o RIM é multiestágio; a ponte é decomposição steady-state, o teste é sobre a razão; §Code Examples)
    - tests/classificacao.yaml (onde registrar o teste novo como `invariante`)
  </read_first>
  <behavior>
    - `pb_justo(roe,ke,g)` == `1 + (roe−ke)/(ke−g)` exato (< 1e-9) vs a função do BLIND-02a (identidade preservada, knob-proof).
    - `payout_terminal(roe_t,g)` == `1 − g/roe_t`.
    - Guarda de borda: `ke − g > 0` já garantido por `Ke_min > g_cap` (Fase 12); com `ke − g ≤ 0` ou `roe_t == 0` a função devolve None (never-raise), não levanta.
    - Teste de correção (invariante): sobre insumos sãos, `payout_terminal ∈ (0,1)` E `pb_justo ∈ (0,6)`; fora disso = BUG do modelo (o teste deve provar que a razão-guarda pega patologia de modelo, com fixtures sintéticas — SEM nomear ticker).
  </behavior>
  <action>
    Criar `src/analista/core/valuation.py` (módulo puro, zero I/O) com `pb_justo(roe, ke, g)` e
    `payout_terminal(roe_t, g)` — a identidade fechada do BLIND-02a, com None-guard de borda
    (`ke−g>0`, `roe_t≠0`). Repontar `tests/test_invariantes_v24.py` (BLIND-02a) para importar de
    `core.valuation` em vez da cópia local — a identidade continua a MESMA (não afrouxar o assert < 1e-9).
    Criar `tests/test_eng_ponte_pb.py` (`invariante`): (a) equivalência exata com a álgebra do BLIND-02a;
    (b) `payout_terminal ∈ (0,1)` e `pb_justo ∈ (0,6)` sobre insumos sãos + RED-able: uma fixture com g
    terminal > ROE_T (payout_T negativo) e uma com spread patológico (P/B>6) FALHAM o guard — provando
    que o guard pega patologia de MODELO, não bug de dado. SEM nomear ticker; SEM asserir número-alvo do
    livro. Registrar as entradas em `classificacao.yaml` (`invariante`) no MESMO diff.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -k 'ponte_pb or invariantes_v24' -q` verde (0 failed); a identidade < 1e-9 do BLIND-02a intacta
    - `src/analista/core/valuation.py` exporta `pb_justo` e `payout_terminal`; `tests/test_invariantes_v24.py` importa de `core.valuation` (grep confirma o import; nenhuma cópia divergente)
    - O teste RED-able existe: comentar/relaxar o guard `pb∈(0,6)` faz `test_eng_ponte_pb` ficar vermelho (o guard é provado, não decorativo)
    - Coleta sem `CLASSIFICACAO ORFA`; nenhum `golden_nivel` novo (só `invariante`); commit toca `tests/` sem `config.yaml`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -k 'ponte_pb or invariantes_v24' -q</automated>
  </verify>
  <done>Identidade P/B pura em core/valuation.py, reusada pelo BLIND-02a; teste de correção da razão RED-able e verde; sem ticker.</done>
</task>

<task type="auto">
  <name>Task 2: Religar a tríade à região da MS sobre o RIM + ponte P/B + guard runtime never-raise</name>
  <files>src/analista/report/report.py</files>
  <read_first>
    - src/analista/report/report.py (veredito :639-663; `vmin/vmax` simétricos :608-610; matriz Ke×g :551; flags de risco :645-657; `_roe_through_cycle` :184)
    - src/analista/core/valuation.py (Task 1)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Contrato de saída" D-09; §"Guarda-corpo em dois níveis"; §Pitfall 6 — CGRA4 é ortogonal ao guard P/B; §Pitfall 8 — MS não pode ficar fallback)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md (D-07, D-09, D-10)
  </read_first>
  <action>
    Em `report.py`: (1) tornar `a.vmin = intrinseco_motor × (1 − ms)` / `a.vmax = intrinseco_motor ×
    (1 + ms)` o caminho **PRIMÁRIO e único** da região de valor (`ms = cfg["veredito"]["margem_seguranca"]`,
    consumido — não recalibrado), removendo o resíduo de banda ensemble; a árvore de veredito :639-663 já
    compara `preco_atual` vs `[vmin,vmax]` → mantém a forma SUB/NO INTERVALO/SOBRE, só muda a fonte da
    banda (D-09). (2) Computar a **ponte P/B** com `core.valuation.pb_justo(ROE_T, a.ke, g_T)`,
    `V_ponte = pb × vpa0`, `payout_T = payout_terminal(ROE_T, g_T)` (ROE_T=_roe_through_cycle) e expô-la
    em campos de `AnaliseAcao` para a UI (Plano 06). (3) **Guard runtime never-raise (D-10b):** quando
    `pb_justo ∉ (0,6)` ou `payout_T ∉ (0,1)` (patologia de MODELO), DEGRADAR o veredito (suprimir /
    prefixo VERIFICAR), **nunca levantar** (contrato SAN-06). O guard NÃO conserta `VPA=PL/num_acoes`
    inflado (CGRA4) — ele apenas sinaliza patologia de razão; o CGRA4 é sinalizado por SAN-01 + sanidade
    V vs preço, ORTOGONAL a este guard. (4) Confirmar que a matriz Ke×g (ENG-07) usa `a.ke` correto no
    header (reusar `ddm.matriz_sensibilidade`, não reinventar). Manter as flags de risco do livro
    (:645-657). NÃO mudar o valor do default no config (Plano 05).
  </action>
  <acceptance_criteria>
    - A região `vmin/vmax` deriva SEMPRE de `intrinseco_motor × (1 ∓ margem_seguranca)` (grep: `margem_seguranca` consumido no caminho primário; nenhum `min(...contraponto)`/`max(...contraponto)` sobrevive)
    - A tríade SUB/NO INTERVALO/SOBRE continua vindo da comparação `preco × [vmin,vmax]` (árvore :639-663 intacta na forma)
    - `report.py` importa `core.valuation` e expõe `pb_justo`/`payout_T` em `AnaliseAcao`; o guard runtime degrada (VERIFICAR/suprime) fora de faixa e NÃO levanta (test never-raise no Plano 07 / Task 3)
    - `git diff config.yaml calibracao.lock.yaml` VAZIO neste plano
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'margem_seguranca' src/analista/report/report.py && grep -q 'pb_justo\|payout_terminal' src/analista/report/report.py && test -z "$(git diff config.yaml)"</automated>
  </verify>
  <done>Região da MS é primária; tríade vem do V do RIM; ponte P/B exposta; guard runtime degrada never-raise; matriz Ke×g reusada.</done>
</task>

<task type="auto">
  <name>Task 3: Testes de contrato (tríade/região/never-raise) + classificacao</name>
  <files>tests/test_eng_contrato.py, tests/classificacao.yaml</files>
  <read_first>
    - src/analista/report/report.py (veredito/região/guard da Task 2)
    - tests/classificacao.yaml (registrar como `contrato`)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§Validation Architecture — classificar contrato/invariante; nunca golden_nivel novo)
  </read_first>
  <action>
    Criar `tests/test_eng_contrato.py` (`contrato`, fixtures sintéticas, SEM nomear ticker nem asserir
    número-alvo do livro): (a) a tríade é função da posição de `preco` vs `[V×(1−MS), V×(1+MS)]` —
    `preco < vmin` → SUBAVALIADA; `vmin ≤ preco ≤ vmax` → NO INTERVALO; `preco > vmax` → SOBREAVALIADA
    (formato, não nível); (b) a região é SIMÉTRICA e escala com a MS (dobrar a MS alarga a banda
    simetricamente); (c) never-raise: um insumo que produz razão patológica (P/B>6) → veredito degrada
    para VERIFICAR/suprimido, `analisar_acao` NÃO levanta. Registrar as entradas em `classificacao.yaml`
    (`contrato`) no MESMO diff. Commit de teste separado de qualquer edição de `config.yaml`.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -k 'eng_contrato' -q` verde; asserindo tríade por posição, simetria da região e never-raise sob razão patológica
    - `.venv/bin/python -m pytest -q` (suíte default) 0 failed; coleta sem `CLASSIFICACAO ORFA`
    - Nenhum assert nomeia ticker com número-alvo (grep `37[.,]22|ITUB4.*3[5-9]` no arquivo == 0)
    - Só entradas `contrato` adicionadas por esta task (nenhum `golden_nivel` novo)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -k 'eng_contrato' -q && ! grep -qiE '37[.,]22' tests/test_eng_contrato.py</automated>
  </verify>
  <done>Contrato de saída coberto por testes de formato/borda verdes; sem ticker; suíte default verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| intrinseco_motor + ROE_T/Ke/g → região/ponte/guard | Razão implícita patológica (spread degenerado, payout_T fora de faixa) cruza para o veredito exibido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Denial (div-by-zero/crash) | pb_justo/payout_terminal com ke−g≤0 ou roe_t=0 | mitigate | None-guard de borda; runtime never-raise degrada o veredito, não levanta (SAN-06) |
| T-13-09 | Info (overfit da MS) | veredito.margem_seguranca | mitigate | MS consumida como parâmetro do usuário; nunca calibrada; default é co-change sancionado (Plano 05), não ajuste até "ficar bonito" |
| T-13-10 | Elevation (falso guard) | guard P/B tentando pegar CGRA4 (bug de dado) | accept | Guard pega patologia de MODELO (P/B>6, payout_T fora de faixa); CGRA4 é sinalizado por SAN-01 + sanidade V×preço — ortogonal, documentado |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -k 'ponte_pb or eng_contrato or invariantes_v24' -q` verde; suíte default `pytest -q` 0 failed.
- A região `vmin/vmax` deriva sempre da MS simétrica sobre o RIM (caminho primário), a tríade vem de V.
- Ponte P/B exposta; guard runtime degrada never-raise; matriz Ke×g reusada sobre `a.ke`.
- `git diff config.yaml calibracao.lock.yaml` VAZIO.
</verification>

<success_criteria>
O contrato do livro (tríade de V vs região da MS + ponte P/B auditável + guard em dois níveis + matriz
Ke×g) está entregue na engine, com testes de correção/contrato verdes, sem calibrar a MS e sem validar o
caso do livro.
</success_criteria>

<output>
After completion, create `.planning/phases/13-motores-contrato-de-sa-da-eng/13-04-SUMMARY.md`
</output>
