---
phase: 13-motores-contrato-de-sa-da-eng
plan: 06
type: execute
wave: 5
depends_on: [03, 04]
files_modified:
  - src/analista/core/arquetipo.py
  - src/analista/core/freio.py
  - src/analista/core/comparables.py
  - src/analista/cli.py
  - app.py
  - src/analista/report/selo.py
  - tests/test_ranking_freio.py
  - tests/test_comparables.py
  - tests/test_presentation_multiticker.py
  - tests/test_comparador.py
  - tests/classificacao.yaml
autonomous: true
requirements: [ENG-05, ENG-06, ENG-07, ENG-11]
must_haves:
  truths:
    - "O Ranking foi REBAIXADO a screener por múltiplos crus (Nota, P/L, P/VP, DY, BSD): as colunas preço-alvo / upside / veredito SAÍRAM (regressão de pares é cega ao nível de preço)"
    - "O Ranking NÃO foi deletado — a Nota (Cap. 11) e o comparativo por múltiplos (Cap. 12) permanecem"
    - "A 2ª lente ensemble×DDM + divergência do ranque (cli.py:203-243) foi removida"
    - "`freio.py` (motor_pendente/alvo_regressao_confiavel) e o legado `ARQUETIPO_MOTOR` foram DELETADOS (últimos consumidores), em tasks separadas e independentemente revisáveis"
    - "A UI Analisar recebe o mínimo: widget de MS, a tríade (já da engine), a matriz Ke×g (reusada), e a ponte P/B exibida"
    - "Saíram os rótulos que nunca vieram do livro: a célula 'Evitar' e o eixo de qualidade 'Baixa' (re-rotulado neutro), mantendo VALUE TRAP/Fraca"
  artifacts:
    - path: "src/analista/cli.py"
      provides: "Ranking sem preço-alvo/upside/veredito e sem a lente ensemble×DDM"
    - path: "app.py"
      provides: "Ranking por múltiplos crus + UI Analisar mínima (MS slider, ponte P/B, matriz reusada)"
    - path: "src/analista/report/selo.py"
      provides: "_MATRIZ sem 'Evitar'; eixo qualidade sem rótulo 'Baixa'"
  key_links:
    - from: "cli.py / app.py Ranking"
      to: "comparables.ranking_por_multiplos (múltiplos crus)"
      via: "colunas Nota/P/L/P/VP/DY/BSD; sem imputar nível de preço"
      pattern: "ranking_por_multiplos"
    - from: "app.py Analisar (MS slider)"
      to: "veredito.margem_seguranca / região de valor"
      via: "st.slider alimenta a região exibida"
      pattern: "slider|margem_seguranca"
  anti_goals:
    - "NÃO deletar o Ranking (joga fora Cap. 11-12) — rebaixar e re-rotular; a Nota e os múltiplos crus ficam"
    - "NÃO deletar a função `comparables.preco_alvo_por_regressao` — é a conferência CTEEP do livro (Cap. 12, test_preco_alvo_cteep invariante); só DESCONECTAR da view do Ranking"
    - "NÃO inventar contrato novo (preço-teto/Bazin/Comprar-Aguardar): as lentes Graham/Bazin informativas (app.py:1089-1116) NÃO são o contrato e ficam intocadas (reforma de UI deferida)"
    - "NÃO calibrar a MS no widget (a MS é escolha do usuário; o slider só expõe o parâmetro, não ajusta default contra resultado)"
    - "NÃO colapsar o eixo de qualidade inteiro nem remover VALUE TRAP/Fraca — só a célula 'Evitar' e o rótulo 'Baixa'"
---

<objective>
Rebaixar o Ranking a screener por múltiplos crus (ENG-11): saem as colunas preço-alvo / upside / veredito
(imputam nível de preço — a regressão de pares é matematicamente cega ao nível), ficam Nota, P/L, P/VP,
DY, BSD (Cap. 11-12). Some a 2ª lente ensemble×DDM + divergência (cli.py:203-243), e morrem os últimos
consumidores do mundo antigo: `freio.py` e o legado `ARQUETIPO_MOTOR` (em tasks separadas, cada diff
revisável isoladamente — W1 do checker). E entregar a UI mínima (D-08): widget de MS, ponte P/B exibida,
matriz Ke×g reusada, e a remoção de "Evitar" e do rótulo de qualidade "Baixa".

Purpose: fechar o contrato do livro na tela e o screener honesto, sem inventar contrato novo e sem
deletar os Cap. 11-12.
Output: Ranking por múltiplos crus (cli + app), Analisar com MS/ponte/matriz, selo sem "Evitar"/"Baixa",
`freio.py`/`ARQUETIPO_MOTOR` deletados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md

@src/analista/core/comparables.py
@src/analista/core/freio.py
@src/analista/cli.py
@src/analista/report/selo.py

<interfaces>
<!-- Sites (medidos) do Ranking e da UI -->
cli.py: cmd_rank :168; 2a lente ensemble×DDM + divergência :203-243 (REMOVER); colunas preço-alvo :228 /
  upside :229 (REMOVER); caption R2/n :246-250 (opcional manter como contexto).
app.py: Ranking :1554-1668; Preço-alvo :1638 / Upside :1639 / Veredito :1640,1627 (REMOVER); Selo(BSD)
  :1635 (MANTER); múltiplos P/L/P/VP/DY já montados no comparador de pares :1166-1178 (reusar);
  caption regressão :1664-1668 (opcional). Analisar: manchete/veredito :918-1041; região vmin/vmax
  :1009,1054; matriz Ke×g :1430-1436 (reusar, confirmar a.ke); lentes Graham/Bazin :1089-1116 (INTOCAR).
comparables.py: ranking_por_multiplos :37 (MANTER); preco_alvo_por_regressao :181 / PrecoAlvo :169
  (DESCONECTAR do Ranking, NÃO deletar — CTEEP invariante); divergencia_entre_lentes :87 (morre com o
  ensemble — confirmar nenhum outro consumidor).
freio.py: motor_pendente :18 / alvo_regressao_confiavel :32 (DELETAR o módulo).
arquetipo.py: ARQUETIPO_MOTOR (legado do Plano 02 — DELETAR aqui; grep -rc deve zerar em src/ + app.py).
selo.py: _MATRIZ :48-55 (célula ("Baixa","Caro")="Evitar" :54 — remover); _qualidade :79-85 (rótulo
  "Baixa" -> re-rotular neutro, ex.: "Atenção"; MANTER VALUE TRAP/Fraca); faixa_do_veredito :88 (INTOCAR).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebaixar a view do Ranking (cli + app) + desconectar preço-alvo do comparables</name>
  <files>src/analista/cli.py, app.py, src/analista/core/comparables.py, tests/test_ranking_freio.py, tests/test_comparables.py, tests/classificacao.yaml</files>
  <read_first>
    - src/analista/cli.py (cmd_rank :168, lente ensemble×DDM :203-243, colunas :228-229, caption :246-250)
    - app.py (Ranking :1554-1668; colunas de preço-alvo/upside/veredito; múltiplos :1166-1178; selo :1635)
    - src/analista/core/comparables.py (ranking_por_multiplos :37, preco_alvo_por_regressao :181, divergencia_entre_lentes :87)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Rebaixamento do Ranking" — colunas exatas; §"Remoção do ensemble")
    - tests/classificacao.yaml
  </read_first>
  <action>
    (1) cli.py: remover o bloco :203-243 (ensemble_mid, divergencia_entre_lentes, avisos de divergência) e
    as colunas preço-alvo/upside/veredito do Ranking; manter a Nota (ranking_por_multiplos) e as colunas
    de múltiplos crus (P/L, P/VP, DY, BSD). As chamadas a `freio.*` que sobrarem são removidas junto (a
    deleção do MÓDULO freio.py é a Task 2). (2) app.py Ranking (:1554-1668): remover Preço-alvo/Upside/
    Veredito; manter Selo(BSD) + Nota; exibir múltiplos crus (reusar :1166-1178). Caption de regressão
    (R2/n) pode permanecer como contexto (Cap. 12) OU sair — o essencial é NENHUMA coluna imputar nível de
    preço. (3) comparables.py: DESCONECTAR preco_alvo_por_regressao da view do Ranking (NÃO deletar a
    função — conferência CTEEP do livro); deletar divergencia_entre_lentes SOMENTE se grep confirmar
    nenhum consumidor vivo fora do ensemble morto. (4) tests: deletar os `test_divergencia_*` de
    test_ranking_freio.py (função + linha da classificacao.yaml no MESMO diff); ajustar test_comparables.py
    se preco_alvo saiu do Ranking, MAS o test_preco_alvo_cteep (invariante) PERMANECE verde. Commits de
    teste sem tocar config.yaml.
  </action>
  <acceptance_criteria>
    - As colunas preço-alvo/upside/veredito não aparecem no bloco do Ranking (grep por 'preco_alvo'/'upside'/'Veredito' no ranque de cli.py/app.py == 0); Nota + P/L/P/VP/DY/BSD permanecem
    - `grep -c 'def preco_alvo_por_regressao' src/analista/core/comparables.py` == 1 (função preservada) e test_preco_alvo_cteep (invariante) verde
    - A lente ensemble×DDM (cli.py:203-243) foi removida (grep 'ensemble_mid'/'divergencia_entre_lentes' no cli.py == 0)
    - `.venv/bin/python -m pytest -k 'ranking or comparables or cteep' -q` verde; coleta sem CLASSIFICACAO ORFA
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'def preco_alvo_por_regressao' src/analista/core/comparables.py) -eq 1 && .venv/bin/python -m pytest -k 'ranking or comparables or cteep' -q</automated>
  </verify>
  <done>Ranking por múltiplos crus; preço-alvo/upside/veredito e lente ensemble fora da view; CTEEP intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Deletar freio.py + o legado ARQUETIPO_MOTOR (últimos consumidores)</name>
  <files>src/analista/core/freio.py, src/analista/core/arquetipo.py, src/analista/cli.py, app.py, tests/test_ranking_freio.py, tests/classificacao.yaml</files>
  <read_first>
    - src/analista/core/freio.py (módulo inteiro: motor_pendente :18, alvo_regressao_confiavel :32)
    - `grep -rn 'import freio\|freio\.\|ARQUETIPO_MOTOR' src/ app.py` (confirmar TODOS os consumidores vivos antes de deletar)
    - src/analista/core/arquetipo.py (o legado ARQUETIPO_MOTOR mantido pelo Plano 02)
    - tests/test_ranking_freio.py (test_freio_* / test_motor_pendente_* que exercem freio) + tests/classificacao.yaml
  </read_first>
  <action>
    (1) Remover as CHAMADAS remanescentes a `freio.motor_pendente`/`freio.alvo_regressao_confiavel` no
    cli.py/app.py (a Task 1 já tirou as colunas que dependiam delas). (2) DELETAR o módulo
    `src/analista/core/freio.py` (o predicado de suspensão por motor morre com o motor único). (3) DELETAR
    o legado `ARQUETIPO_MOTOR` de arquetipo.py — o último consumidor era o freio; grep -rc 'ARQUETIPO_MOTOR'
    em src/ + app.py deve zerar. (4) Deletar os testes de freio/motor_pendente em test_ranking_freio.py
    (função + linha da classificacao.yaml no MESMO diff, 0 órfão). Commit de teste sem tocar config.yaml.
  </action>
  <acceptance_criteria>
    - `test ! -f src/analista/core/freio.py` (módulo deletado) e `grep -rc 'import.*freio\|freio\.' src/ app.py` soma 0 (nenhum consumidor órfão)
    - `grep -rc 'ARQUETIPO_MOTOR' src/ app.py` soma 0 (legado deletado; o Plano 03 já parou de consumi-lo)
    - `.venv/bin/python -m pytest -q` (default) 0 failed; coleta sem CLASSIFICACAO ORFA; nenhum golden_nivel novo
  </acceptance_criteria>
  <verify>
    <automated>test ! -f src/analista/core/freio.py && test $(grep -rc 'ARQUETIPO_MOTOR' src/ app.py 2>/dev/null | awk -F: '{s+=$2} END{print s+0}') -eq 0 && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>freio.py e ARQUETIPO_MOTOR deletados; nenhum import órfão; suíte default verde.</done>
</task>

<task type="auto">
  <name>Task 3: UI Analisar mínima — MS slider + ponte P/B + matriz Ke×g reusada</name>
  <files>app.py</files>
  <read_first>
    - app.py (Analisar manchete/veredito :918-1041; região vmin/vmax :1009,1054; matriz Ke×g :1430-1436; lentes Graham/Bazin :1089-1116)
    - src/analista/report/report.py (campos da ponte P/B do Plano 04: pb_justo, V_ponte, payout_T; região da MS)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"UI mínima Streamlit" — escopo mínimo; NÃO tocar Graham/Bazin)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md (D-07, D-08)
  </read_first>
  <action>
    Em app.py (aba Analisar), MÍNIMO: (1) widget de MS — st.slider("Margem de segurança", 0.0, 0.20,
    default=cfg["veredito"]["margem_seguranca"], step=0.01) alimentando a região de valor exibida (a
    tríade já vem da engine, Plano 04). (2) bloco pequeno da PONTE P/B (NOVO): 3 linhas de st.metric/
    st.caption exibindo P/B justo, V = P/B×VPA e payout_T (dos campos expostos pela engine). (3) confirmar
    que a matriz Ke×g (:1430-1436) usa o a.ke correto no header (reusar, não reinventar — ENG-07). NÃO
    tocar as lentes Graham/Bazin (:1089-1116). O slider NÃO recalibra o default (só expõe o parâmetro).
  </action>
  <acceptance_criteria>
    - O widget de MS existe e lê o default de cfg (grep 'slider' + 'margem_seguranca' na aba Analisar); alterá-lo altera a região vmin/vmax exibida
    - A ponte P/B é exibida (grep por 'P/B' + 'payout' no bloco Analisar; 3 elementos st.metric/caption)
    - A matriz Ke×g permanece exibida sobre a.ke (grep confirma uso de a.ke; nenhuma reimplementação)
    - As lentes Graham/Bazin (:1089-1116) permanecem intocadas (git diff nesse range VAZIO)
    - `.venv/bin/python -c "import ast; ast.parse(open('app.py').read())"` sem erro
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import ast; ast.parse(open('app.py').read())" && grep -q 'margem_seguranca' app.py</automated>
  </verify>
  <done>UI Analisar recebe MS slider + ponte P/B + matriz reusada; Graham/Bazin intocados; sem calibrar MS.</done>
</task>

<task type="auto">
  <name>Task 4: selo.py — remover "Evitar" e o rótulo de qualidade "Baixa" + migrar badges</name>
  <files>src/analista/report/selo.py, tests/test_presentation_multiticker.py, tests/test_comparador.py, tests/classificacao.yaml</files>
  <read_first>
    - src/analista/report/selo.py (_MATRIZ :48-55, _qualidade :79-85, faixa_do_veredito :88)
    - tests/test_presentation_multiticker.py, tests/test_comparador.py (testes de badge que asseriam "Evitar"/"Baixa")
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"UI mínima" ponto 5 + Open Question 1 — recomendação: remover "Evitar" e re-rotular "Baixa" neutro, mantendo VALUE TRAP/Fraca)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md (D-08)
  </read_first>
  <action>
    Em selo.py: (1) remover a célula ("Baixa","Caro")="Evitar" do _MATRIZ (nunca veio do livro). (2)
    re-rotular o eixo de qualidade "Baixa" para um rótulo descritivo neutro (ex.: "Atenção") em _qualidade
    e nas chaves do _MATRIZ correspondentes, MANTENDO VALUE TRAP e Fraca (só o nome do eixo e a célula
    "Evitar" saem — mudança mínima, D-08). NÃO colapsar o eixo inteiro nem tocar faixa_do_veredito. (3)
    migrar os testes de badge que asseriam "Evitar"/"Baixa" para os rótulos novos + casar classificacao.yaml
    no MESMO diff (0 órfão). Badges são contrato (não golden_nivel). Commit toca tests/ sem config.yaml.
  </action>
  <acceptance_criteria>
    - `grep -c '"Evitar"' src/analista/report/selo.py` == 0 (célula removida)
    - `grep -c '"Baixa"' src/analista/report/selo.py` == 0 (rótulo re-rotulado); VALUE TRAP e Fraca permanecem (grep confirma ambos)
    - faixa_do_veredito intocado (git diff da função VAZIO)
    - `.venv/bin/python -m pytest -k 'presentation or comparador or selo' -q` verde; suíte default 0 failed; coleta sem CLASSIFICACAO ORFA
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c '"Evitar"\|"Baixa"' src/analista/report/selo.py) -eq 0 && .venv/bin/python -m pytest -k 'presentation or comparador or selo' -q</automated>
  </verify>
  <done>Selo sem "Evitar" e sem o rótulo "Baixa" (VALUE TRAP/Fraca mantidos); faixa_do_veredito intocado; badges verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entrada do usuário (slider de MS) → região de valor | Único input novo de UI desta fase; deve degradar, não quebrar, em valores de borda |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-14 | Tampering (overfit) | slider de MS | mitigate | Slider expõe o parâmetro do usuário; NÃO recalibra o default (co-change sancionado no Plano 05); MS nunca calibrada contra resultado |
| T-13-15 | Denial (import quebrado) | deleção de freio.py / ARQUETIPO_MOTOR com consumidor vivo | mitigate | Task 1 tira os consumidores da view; Task 2 grep de consumidores antes de deletar; suíte default verde prova nenhum import órfão |
| T-13-16 | Info (contrato inventado) | UI Analisar | accept | Lentes Graham/Bazin ficam como contexto informativo (não viram veredito); o contrato de saída é só a tríade do livro |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` (default) 0 failed; coleta sem CLASSIFICACAO ORFA.
- `grep -rc 'ARQUETIPO_MOTOR' src/ app.py` == 0; freio.py deletado; nenhum import órfão.
- Ranking sem preço-alvo/upside/veredito; Nota + múltiplos crus + selo permanecem; CTEEP intacto.
- selo sem "Evitar"/"Baixa"; app.py parseia; Graham/Bazin intocados.
</verification>

<success_criteria>
O Ranking está rebaixado a screener por múltiplos crus (não deletado), a UI Analisar exibe o contrato
mínimo do livro (MS/tríade/matriz/ponte), o selo perdeu "Evitar"/"Baixa", e os últimos artefatos do mundo
antigo (freio/ARQUETIPO_MOTOR) morreram — cada deleção numa task revisável isoladamente, suíte default verde.
</success_criteria>

<output>
After completion, create `.planning/phases/13-motores-contrato-de-sa-da-eng/13-06-SUMMARY.md`
</output>
