---
phase: 13-motores-contrato-de-sa-da-eng
plan: 06
type: execute
wave: 5
depends_on: [03, 04]
files_modified:
  - src/analista/core/arquetipo.py
  - src/analista/core/freio.py
  - src/analista/core/comparables.py
  - src/analista/cli.py
  - app.py
  - src/analista/report/selo.py
  - tests/test_ranking_freio.py
  - tests/test_arquetipo.py
  - tests/test_comparables.py
  - tests/test_selo.py
  - tests/test_presentation_multiticker.py
  - tests/test_comparador.py
  - tests/classificacao.yaml
autonomous: true
requirements: [ENG-05, ENG-06, ENG-07, ENG-11]
must_haves:
  truths:
    - "O Ranking foi REBAIXADO a screener por múltiplos crus (Nota, P/L, P/VP, DY, BSD): as colunas preço-alvo / upside / veredito SAÍRAM (regressão de pares é cega ao nível de preço)"
    - "O Ranking NÃO foi deletado — a Nota (Cap. 11) e o comparativo por múltiplos (Cap. 12) permanecem"
    - "A 2ª lente ensemble×DDM + divergência do ranque (cli.py:203-243) foi removida"
    - "`freio.py`, o legado `ARQUETIPO_MOTOR` e `comparables.divergencia_entre_lentes` foram DELETADOS (últimos consumidores), e o `test_ranking_freio.py` inteiro foi deletado (o import de módulo impede deleção parcial)"
    - "`test_arquetipo.py` foi repontado de `ARQUETIPO_MOTOR` para `ARQUETIPO_ANCORA_ROE` (o legado morre aqui)"
    - "A UI Analisar recebe o mínimo: widget de MS, a tríade (já da engine), a matriz Ke×g (reusada), e a ponte P/B exibida"
    - "Saíram os rótulos que nunca vieram do livro: a célula 'Evitar' e o eixo de qualidade 'Baixa' (re-rotulado neutro), mantendo VALUE TRAP/Fraca"
  artifacts:
    - path: "src/analista/cli.py"
      provides: "Ranking sem preço-alvo/upside/veredito e sem a lente ensemble×DDM"
    - path: "app.py"
      provides: "Ranking por múltiplos crus + UI Analisar mínima (MS slider, ponte P/B, matriz reusada)"
    - path: "src/analista/report/selo.py"
      provides: "_MATRIZ sem 'Evitar'; eixo qualidade sem rótulo 'Baixa'"
  key_links:
    - from: "cli.py / app.py Ranking"
      to: "comparables.ranking_por_multiplos (múltiplos crus)"
      via: "colunas Nota/P/L/P/VP/DY/BSD; sem imputar nível de preço"
      pattern: "ranking_por_multiplos"
    - from: "app.py Analisar (MS slider)"
      to: "veredito.margem_seguranca / região de valor"
      via: "st.slider alimenta a região exibida"
      pattern: "slider|margem_seguranca"
  anti_goals:
    - "NÃO deletar o Ranking (joga fora Cap. 11-12) — rebaixar e re-rotular; a Nota e os múltiplos crus ficam"
    - "NÃO deletar a função `comparables.preco_alvo_por_regressao` — é a conferência CTEEP do livro (Cap. 12, test_preco_alvo_cteep invariante); só DESCONECTAR da view do Ranking"
    - "NÃO inventar contrato novo (preço-teto/Bazin/Comprar-Aguardar): as lentes Graham/Bazin informativas (app.py:1089-1116) NÃO são o contrato e ficam intocadas (reforma de UI deferida)"
    - "NÃO calibrar a MS no widget (a MS é escolha do usuário; o slider só expõe o parâmetro, não ajusta default contra resultado)"
    - "NÃO colapsar o eixo de qualidade inteiro nem remover VALUE TRAP/Fraca — só a célula 'Evitar' e o rótulo 'Baixa'"
---

<objective>
Rebaixar o Ranking a screener por múltiplos crus (ENG-11): saem as colunas preço-alvo / upside / veredito
(imputam nível de preço — a regressão de pares é cega ao nível), ficam Nota, P/L, P/VP, DY, BSD (Cap.
11-12). Some a 2ª lente ensemble×DDM + divergência (cli.py:203-243), e morrem os últimos consumidores do
mundo antigo: `freio.py`, o legado `ARQUETIPO_MOTOR` e `comparables.divergencia_entre_lentes`. Como o
`test_ranking_freio.py` importa `freio`/`cli._motor_pendente` no topo do módulo, ele é deletado INTEIRO
(deleção parcial quebraria a coleta), e o `test_arquetipo.py` é repontado de `ARQUETIPO_MOTOR` para
`ARQUETIPO_ANCORA_ROE`. E a UI mínima (D-08): widget de MS, ponte P/B, matriz Ke×g reusada, e a remoção
de "Evitar" e do rótulo de qualidade "Baixa".

Purpose: fechar o contrato do livro na tela e o screener honesto, sem inventar contrato novo e sem
deletar os Cap. 11-12.
Output: Ranking por múltiplos crus, Analisar com MS/ponte/matriz, selo sem "Evitar"/"Baixa",
`freio.py`/`ARQUETIPO_MOTOR`/`divergencia_entre_lentes` deletados, testes do mundo antigo resolvidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md

@src/analista/core/comparables.py
@src/analista/core/freio.py
@src/analista/cli.py
@src/analista/report/selo.py

<interfaces>
<!-- Sites (medidos) do Ranking e da UI -->
cli.py: cmd_rank :168; 2a lente ensemble×DDM + divergência :203-243 (REMOVER a lente/colunas, MAS manter
  as re-exportações `_motor_pendente`/`alvo_regressao_confiavel` até a Task 2); colunas preço-alvo :228 /
  upside :229 (REMOVER).
app.py: Ranking :1554-1668 (Preço-alvo/Upside/Veredito REMOVER; Selo(BSD) :1635 MANTER; múltiplos
  :1166-1178 reusar). Analisar: veredito :918-1041; região vmin/vmax :1009,1054; matriz Ke×g :1430-1436
  (reusar, confirmar a.ke); lentes Graham/Bazin :1089-1116 (INTOCAR).
comparables.py: ranking_por_multiplos :37 (MANTER); preco_alvo_por_regressao :181 (DESCONECTAR da view,
  NÃO deletar — CTEEP invariante); divergencia_entre_lentes :87 (DELETAR na Task 2 — só o ensemble morto +
  test_ranking_freio a consomem).
freio.py: motor_pendente :18 / alvo_regressao_confiavel :32 (DELETAR o módulo — Task 2).
arquetipo.py: ARQUETIPO_MOTOR (legado do Plano 02 — DELETAR na Task 2; grep -rc deve zerar em src/+app.py).
selo.py: _MATRIZ :48-55 (célula ("Baixa","Caro")="Evitar" :54 — remover); _qualidade :79-85 (rótulo
  "Baixa" -> neutro "Atenção"; MANTER VALUE TRAP/Fraca); faixa_do_veredito :88 (INTOCAR).
Testes que quebram AQUI (onda 5): test_ranking_freio.py inteiro (importa freio/cli._motor_pendente +
  usa divergencia_entre_lentes — DELETE integral, 16 entradas classificacao); test_arquetipo.py
  (import ARQUETIPO_MOTOR :18, ARQUETIPO_MOTOR[r.chave] :112, r.chave in ARQUETIPO_MOTOR :273,:280 —
  REWRITE p/ ARQUETIPO_ANCORA_ROE); test_selo/test_presentation_multiticker/test_comparador (badges
  "Evitar"/"Baixa" — Task 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebaixar a view do Ranking (cli + app) — colunas de nível de preço e lente ensemble saem</name>
  <files>src/analista/cli.py, app.py, tests/test_comparables.py, tests/classificacao.yaml</files>
  <read_first>
    - src/analista/cli.py (cmd_rank :168, lente ensemble×DDM :203-243, colunas :228-229)
    - app.py (Ranking :1554-1668; colunas de preço-alvo/upside/veredito; múltiplos :1166-1178; selo :1635)
    - src/analista/core/comparables.py (ranking_por_multiplos :37, preco_alvo_por_regressao :181)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Rebaixamento do Ranking")
    - tests/classificacao.yaml
  </read_first>
  <action>
    (1) cli.py: remover o bloco :203-243 (a lente ensemble×DDM/divergência) e as colunas preço-alvo/upside/
    veredito do Ranking; manter a Nota (ranking_por_multiplos) + múltiplos crus (P/L, P/VP, DY, BSD).
    **MANTER** as re-exportações de módulo `_motor_pendente`/`alvo_regressao_confiavel` (a Task 2 as remove
    junto do freio — para o test_ranking_freio seguir importável até lá). (2) app.py Ranking (:1554-1668):
    remover Preço-alvo/Upside/Veredito; manter Selo(BSD) + Nota; exibir múltiplos crus (reusar :1166-1178).
    (3) DESCONECTAR preco_alvo_por_regressao da view (NÃO deletar a função — CTEEP). NÃO deletar
    divergencia_entre_lentes aqui (é a Task 2, junto do test que a exercita). (4) test_comparables.py:
    ajustar SÓ se algo saiu do Ranking, mantendo test_preco_alvo_cteep (invariante) verde; casar
    classificacao.yaml no mesmo diff se algum teste mudar. Commits de teste sem tocar config.yaml.
  </action>
  <acceptance_criteria>
    - As colunas preço-alvo/upside/veredito não aparecem no bloco do Ranking (grep por 'preco_alvo'/'upside'/'Veredito' no ranque de cli.py/app.py == 0); Nota + P/L/P/VP/DY/BSD permanecem
    - A lente ensemble×DDM (cli.py:203-243) foi removida (grep 'ensemble_mid'/'divergencia' no bloco ranque do cli.py == 0)
    - `grep -c 'def preco_alvo_por_regressao' src/analista/core/comparables.py` == 1 e test_preco_alvo_cteep verde; `cli._motor_pendente`/`cli.alvo_regressao_confiavel` ainda importáveis (grep confirma as re-exportações vivas)
    - `.venv/bin/python -m pytest -k 'ranking or comparables or cteep' -q` verde; coleta sem CLASSIFICACAO ORFA
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'def preco_alvo_por_regressao' src/analista/core/comparables.py) -eq 1 && .venv/bin/python -m pytest -k 'ranking or comparables or cteep' -q</automated>
  </verify>
  <done>Ranking por múltiplos crus; preço-alvo/upside/veredito e lente ensemble fora da view; CTEEP e re-exportações do freio intactos até a Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Deletar freio.py, ARQUETIPO_MOTOR, divergencia_entre_lentes + test_ranking_freio inteiro + repontar test_arquetipo</name>
  <files>src/analista/core/freio.py, src/analista/core/arquetipo.py, src/analista/core/comparables.py, src/analista/cli.py, app.py, tests/test_ranking_freio.py, tests/test_arquetipo.py, tests/classificacao.yaml</files>
  <read_first>
    - src/analista/core/freio.py (módulo inteiro) + `grep -rn 'import freio\|freio\.\|ARQUETIPO_MOTOR\|divergencia_entre_lentes\|_motor_pendente\|alvo_regressao_confiavel' src/ app.py tests/` (TODOS os consumidores)
    - src/analista/core/arquetipo.py (ARQUETIPO_MOTOR legado); src/analista/core/comparables.py (divergencia_entre_lentes :87)
    - tests/test_ranking_freio.py (importa freio/cli._motor_pendente no topo — deleção integral) e tests/test_arquetipo.py (import :18 + refs :112/:273/:280)
    - tests/classificacao.yaml (as 16 entradas de test_ranking_freio + as de test_arquetipo)
  </read_first>
  <action>
    (1) DELETAR o módulo `src/analista/core/freio.py` e as re-exportações `_motor_pendente`/
    `alvo_regressao_confiavel` no cli.py (e qualquer chamada residual no app.py). (2) DELETAR
    `comparables.divergencia_entre_lentes` (confirmado sem consumidor vivo após a Task 1 e a morte do
    ensemble no Plano 03). (3) DELETAR o legado `ARQUETIPO_MOTOR` de arquetipo.py — grep -rc em src/+app.py
    deve zerar. (4) DELETAR o `tests/test_ranking_freio.py` INTEIRO (o import de `freio`/`cli._motor_pendente`
    no topo do módulo impede deleção função-a-função) + as 16 entradas em classificacao.yaml no MESMO diff.
    (5) REWRITE `tests/test_arquetipo.py`: trocar o import `ARQUETIPO_MOTOR` por `ARQUETIPO_ANCORA_ROE` (:18)
    e os asserts de membership `r.chave in ARQUETIPO_MOTOR` (:273,:280) por `r.chave in ARQUETIPO_ANCORA_ROE`;
    o `ARQUETIPO_MOTOR[r.chave]=="ddm"` (:112, dentro do teste de concessão) vira `r.chave in
    ARQUETIPO_ANCORA_ROE` (drop do rótulo de motor legado) — mantendo a entrada classificacao válida.
    Commits de teste sem tocar config.yaml.
  </action>
  <acceptance_criteria>
    - `test ! -f src/analista/core/freio.py`; `grep -rc 'import.*freio\|freio\.' src/ app.py` soma 0; `grep -rc 'ARQUETIPO_MOTOR' src/ app.py tests/` soma 0; `grep -c 'def divergencia_entre_lentes' src/analista/core/comparables.py` == 0
    - `test ! -f tests/test_ranking_freio.py` (deletado inteiro) e nenhuma entrada `test_ranking_freio.py::` sobra em classificacao.yaml (0 órfão dos dois lados)
    - test_arquetipo.py importa e usa `ARQUETIPO_ANCORA_ROE` (grep) e não referencia mais `ARQUETIPO_MOTOR`
    - `.venv/bin/python -m pytest -q` (default) 0 failed; coleta sem CLASSIFICACAO ORFA; nenhum golden_nivel novo
  </acceptance_criteria>
  <verify>
    <automated>test ! -f src/analista/core/freio.py && test ! -f tests/test_ranking_freio.py && test $(grep -rc 'ARQUETIPO_MOTOR' src/ app.py tests/ 2>/dev/null | awk -F: '{s+=$2} END{print s+0}') -eq 0 && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>freio.py, ARQUETIPO_MOTOR e divergencia_entre_lentes deletados; test_ranking_freio removido inteiro; test_arquetipo repontado à ARQUETIPO_ANCORA_ROE; suíte default verde.</done>
</task>

<task type="auto">
  <name>Task 3: UI Analisar mínima — MS slider + ponte P/B + matriz Ke×g reusada</name>
  <files>app.py</files>
  <read_first>
    - app.py (Analisar manchete/veredito :918-1041; região vmin/vmax :1009,1054; matriz Ke×g :1430-1436; lentes Graham/Bazin :1089-1116)
    - src/analista/report/report.py (campos da ponte P/B do Plano 04: pb_justo, V_ponte, payout_T; região da MS)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"UI mínima Streamlit" — escopo mínimo; NÃO tocar Graham/Bazin)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md (D-07, D-08)
  </read_first>
  <action>
    Em app.py (aba Analisar), MÍNIMO: (1) widget de MS — st.slider("Margem de segurança", 0.0, 0.20,
    default=cfg["veredito"]["margem_seguranca"], step=0.01) alimentando a região de valor exibida (a
    tríade já vem da engine, Plano 04). (2) bloco pequeno da PONTE P/B (NOVO): 3 linhas de st.metric/
    st.caption exibindo P/B justo, V = P/B×VPA e payout_T. (3) confirmar que a matriz Ke×g (:1430-1436) usa
    o a.ke correto no header (reusar, não reinventar — ENG-07). NÃO tocar as lentes Graham/Bazin
    (:1089-1116). O slider NÃO recalibra o default (só expõe o parâmetro).
  </action>
  <acceptance_criteria>
    - O widget de MS existe e lê o default de cfg (grep 'slider' + 'margem_seguranca' na aba Analisar); alterá-lo altera a região vmin/vmax exibida
    - A ponte P/B é exibida (grep por 'P/B' + 'payout' no bloco Analisar; 3 elementos st.metric/caption)
    - A matriz Ke×g permanece exibida sobre a.ke (grep confirma uso de a.ke; nenhuma reimplementação)
    - As lentes Graham/Bazin (:1089-1116) permanecem intocadas (git diff nesse range VAZIO)
    - `.venv/bin/python -c "import ast; ast.parse(open('app.py').read())"` sem erro
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import ast; ast.parse(open('app.py').read())" && grep -q 'margem_seguranca' app.py</automated>
  </verify>
  <done>UI Analisar recebe MS slider + ponte P/B + matriz reusada; Graham/Bazin intocados; sem calibrar MS.</done>
</task>

<task type="auto">
  <name>Task 4: selo.py — remover "Evitar" e o rótulo de qualidade "Baixa" + migrar badges</name>
  <files>src/analista/report/selo.py, tests/test_selo.py, tests/test_presentation_multiticker.py, tests/test_comparador.py, tests/classificacao.yaml</files>
  <read_first>
    - src/analista/report/selo.py (_MATRIZ :48-55, _qualidade :79-85, faixa_do_veredito :88)
    - tests/test_selo.py (test_qualidade_por_cor :61-62 assere _qualidade=="Baixa"; test_matriz_quadrante_baixa :90 assere montar_selo(...).rotulo=="Evitar"), tests/test_presentation_multiticker.py (:208-209 selo_badge com "Baixa"/"Evitar"), tests/test_comparador.py (badges que asseriam "Evitar"/"Baixa")
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"UI mínima" ponto 5 + OQ 1 — remover "Evitar", re-rotular "Baixa" neutro, mantendo VALUE TRAP/Fraca)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md (D-08)
  </read_first>
  <action>
    Em selo.py: (1) remover a célula ("Baixa","Caro")="Evitar" do _MATRIZ. (2) re-rotular o eixo de
    qualidade "Baixa" para neutro (ex.: "Atenção") em _qualidade e nas chaves do _MATRIZ correspondentes,
    MANTENDO VALUE TRAP e Fraca. NÃO colapsar o eixo inteiro nem tocar faixa_do_veredito. (3) REWRITE os
    testes de badge que asseriam "Evitar"/"Baixa": test_selo.py::test_qualidade_por_cor (:61-62 "Baixa"→o
    novo rótulo neutro), test_selo.py::test_matriz_quadrante_baixa (:90 rotulo=="Evitar" → o rotulo novo da
    célula re-rotulada), test_presentation_multiticker.py (:208-209 passar os rótulos novos ao selo_badge),
    test_comparador.py; casar classificacao.yaml no MESMO diff (0 órfão).
    Badges são contrato. Commit toca tests/ sem config.yaml.
  </action>
  <acceptance_criteria>
    - `grep -c '"Evitar"' src/analista/report/selo.py` == 0
    - `grep -c '"Baixa"' src/analista/report/selo.py` == 0 (re-rotulado); VALUE TRAP e Fraca permanecem (grep confirma ambos)
    - faixa_do_veredito intocado (git diff da função VAZIO)
    - `.venv/bin/python -m pytest -k 'presentation or comparador or selo' -q` verde; suíte default 0 failed; coleta sem CLASSIFICACAO ORFA
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c '"Evitar"\|"Baixa"' src/analista/report/selo.py) -eq 0 && .venv/bin/python -m pytest -k 'presentation or comparador or selo' -q</automated>
  </verify>
  <done>Selo sem "Evitar" e sem o rótulo "Baixa" (VALUE TRAP/Fraca mantidos); faixa_do_veredito intocado; badges verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entrada do usuário (slider de MS) → região de valor | Único input novo de UI desta fase; deve degradar, não quebrar, em valores de borda |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-14 | Tampering (overfit) | slider de MS | mitigate | Slider expõe o parâmetro do usuário; NÃO recalibra o default (co-change sancionado no Plano 05); MS nunca calibrada contra resultado |
| T-13-15 | Denial (import quebrado) | deleção de freio.py / ARQUETIPO_MOTOR / divergencia_entre_lentes com consumidor vivo | mitigate | Task 1 tira os consumidores da view mantendo as re-exportações; Task 2 grep de TODOS os consumidores antes de deletar + deleção integral do test_ranking_freio; suíte default verde prova nenhum import órfão |
| T-13-16 | Info (contrato inventado) | UI Analisar | accept | Lentes Graham/Bazin ficam como contexto informativo (não viram veredito); o contrato de saída é só a tríade do livro |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` (default) 0 failed; coleta sem CLASSIFICACAO ORFA.
- `grep -rc 'ARQUETIPO_MOTOR' src/ app.py tests/` == 0; freio.py e test_ranking_freio.py deletados; divergencia_entre_lentes removida; test_arquetipo repontado.
- Ranking sem preço-alvo/upside/veredito; Nota + múltiplos crus + selo permanecem; CTEEP intacto.
- selo sem "Evitar"/"Baixa"; app.py parseia; Graham/Bazin intocados.
</verification>

<success_criteria>
O Ranking está rebaixado a screener por múltiplos crus (não deletado), a UI Analisar exibe o contrato
mínimo do livro (MS/tríade/matriz/ponte), o selo perdeu "Evitar"/"Baixa", e os últimos artefatos do mundo
antigo (freio/ARQUETIPO_MOTOR/divergencia_entre_lentes/test_ranking_freio) morreram — cada bloco numa task
revisável, suíte default verde.
</success_criteria>

<output>
After completion, create `.planning/phases/13-motores-contrato-de-sa-da-eng/13-06-SUMMARY.md`
</output>
