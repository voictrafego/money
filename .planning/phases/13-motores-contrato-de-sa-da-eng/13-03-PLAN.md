---
phase: 13-motores-contrato-de-sa-da-eng
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/analista/report/report.py
  - src/analista/core/motores.py
  - tests/test_report.py
  - tests/test_guardrails_ddm.py
  - tests/test_arquetipo_roteamento.py
  - tests/test_vulc3_regressao.py
  - tests/test_motores.py
  - tests/test_backtest_bancos.py
  - tests/classificacao.yaml
autonomous: true
requirements: [ENG-01, ENG-02, ENG-03, ENG-04]
must_haves:
  truths:
    - "Existe UM único caminho de valor: `_intrinseco_por_motor` colapsou num caminho que sempre chama `motores.rim(...)` com o insumo derivado pela política do arquétipo"
    - "Nenhum ticker roteia para `ddm`/`seguradora`/`normalizado`/`dcf`/`nav` como motor primário; a rota `seguradora` e sua chave `MOTOR_ROTULO['seguradora']` foram removidas"
    - "O ensemble morreu: `_guarda_san01`, `_guarda_faixa_ddm`, `_veredito_fronteirico`, `_hipotese_divergencia` e os campos de ensemble de `AnaliseAcao` foram REMOVIDOS (não portados)"
    - "`CONCESSAO_FINITA` usa modelo de ativo financeiro: o `g_terminal` NÃO recebe o g de inflação (carve-out declarado, conforme o spike do Plano 01)"
    - "`dcf_crescimento` foi DELETADO como motor (não substituído por FCFE); `lucro_normalizado`/`nav_contabil` sobrevivem só como derivadores de insumo"
    - "O invariante estrutural WR-04 da seguradora (intrínseco FINITO e > 0 para a franquia capital-light) SOBREVIVE como REWRITE sobre a âncora FINANCEIRA→RIM — não é morto em silêncio"
    - "TODO teste não-quarentenado que asseve campo de ensemble / rótulo de motor / função deletada foi DELETADO (baseline do método antigo) ou RESCRITO (invariante), com a entrada em classificacao.yaml casada no mesmo diff"
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "Dispatch RIM único (_valor_rim/_derivar_insumo) + ensemble removido"
      contains: "motores.rim"
    - path: "src/analista/core/motores.py"
      provides: "dcf_crescimento removido; rota+chave seguradora removidas; rim é a única fórmula"
  key_links:
    - from: "report._intrinseco_por_motor (colapsado)"
      to: "motores.rim"
      via: "chamada única com roe0/roe_terminal/g_terminal derivados pela política do arquétipo"
      pattern: "motores\\.rim"
    - from: "ARQUETIPO_ANCORA_ROE[a.arquetipo]"
      to: "_derivar_insumo"
      via: "política → (roe0, roe_terminal, g_terminal)"
      pattern: "ARQUETIPO_ANCORA_ROE|_derivar_insumo"
  anti_goals:
    - "NÃO 'consertar' o `dcf_crescimento` com FCFE (`lpa × payout`): vira DDM por teorema (Armadilha 2, WEGE3 0,58→0,26). Ele MORRE"
    - "NÃO aplicar o `g_cap` (que embute inflação) ao terminal de `CONCESSAO_FINITA`: double-count de IPCA sob ICPC 01 (Armadilha)"
    - "NÃO portar `_guarda_san01`/`_guarda_faixa_ddm`/ensemble 'por segurança': são cicatrizes do viés"
    - "NÃO MATAR EM SILÊNCIO o invariante WR-04 da seguradora (aviso Fase-7): o finite-positive SOBREVIVE como rewrite sobre a âncora FINANCEIRA→RIM; só o `a.motor=='seguradora'` (rota morta) sai. O `test_setor_de_banco_nao_casa_o_token_seguradora` (token puro) fica INTOCADO"
    - "NÃO recomputar `a.ke` (Fase 12) nem recalibrar `g_cap`/`g_T` (Fase 11): já prontos; tocar apaga o diagnóstico"
    - "NÃO validar o caso do livro (ITUB4=R$37,22): Fase 14. Nenhum assert com ticker+número-alvo"
    - "NÃO ATUALIZAR golden de nível para o valor novo: golden/baseline que quebra é DELETADO (função + linha classificacao no mesmo diff); nunca afrouxar tolerância nem trocar xfail→skip"
---

<objective>
Colapsar os 4 motores num **RIM único** (ENG-01) e **matar o ensemble** (ENG-02). `_intrinseco_por_motor`
deixa de ter 6 ramos (`rim`/`normalizado`/`dcf`/`nav`/`ddm` + rota `seguradora`) e passa a: (1) derivar
o insumo pela política `ARQUETIPO_ANCORA_ROE` do arquétipo (Plano 02); (2) chamar **sempre**
`motores.rim(...)` com `a.ke` (Fase 12) e `g_T` (Fase 11) prontos. O `dcf_crescimento` é **deletado**;
`lucro_normalizado`/`nav_contabil` viram derivadores; a **rota `seguradora`** e sua chave
`MOTOR_ROTULO["seguradora"]` são removidas (a seguradora vira uma FINANCEIRA no RIM único).
`CONCESSAO_FINITA` recebe o carve-out (g terminal sem inflação, spike do Plano 01). Guardas-cicatriz,
divergência e fronteiriço **removidos**.

**Correção do checker (BLOCKER, onda 3 — sweep exaustivo):** a deleção dos campos de `AnaliseAcao`, dos
rótulos de motor (`ddm`/`seguradora`/`normalizado`/`dcf`/`nav`) e das funções `_guarda_*` QUEBRA, na
onda 3, todo teste não-quarentenado que os asseve ao vivo. O inventário empírico (grep do read_first)
cobre: `test_report.py`, `test_guardrails_ddm.py`, `test_vulc3_regressao.py`, `test_arquetipo_roteamento.py`,
`test_motores.py` (seguradora WR-04) e `test_backtest_bancos.py` (MOTOR_ROTULO). Cada um recebe decisão
DELETE-vs-REWRITE explícita, no MESMO diff da deleção. (`test_ranking_freio.py` NÃO quebra aqui — depende
de `freio`/`ARQUETIPO_MOTOR`, vivos até o Plano 06.)

Purpose: uma fórmula, um caminho, erro limitado — e a suíte default 0 failed na onda 3.
Output: dispatch RIM único; `motores.py` sem `dcf_crescimento`-motor e sem a chave `seguradora`; ensemble
deletado; testes do mundo antigo resolvidos (delete/rewrite); o invariante WR-04 da seguradora preservado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md
@.planning/spikes/eng-rim-104.md

@src/analista/report/report.py
@src/analista/core/motores.py
@src/analista/core/arquetipo.py

<interfaces>
<!-- Sites exatos (medidos) -->
report.py: `AnaliseAcao` (:54-77; campos de ensemble a REMOVER: motor_pendente :58, contraponto_valor
  :66, banda_do_motor :67, divergencia_ativa :68, divergencia_razao/hipotese :69-70, arquetipo_incerto
  :72, candidatos_intrinsecos :73, veredito_range :74; TAMBÉM `san01_reetiquetado` e
  `arquetipo_fronteirico` — asseridos em testes, confirmar no dataclass); `_guarda_faixa_ddm` :77-105 +
  :589; `_guarda_san01` :108-182 + :706; `_roe_through_cycle` :184 (MANTER); `_intrinseco_por_motor`
  :201-327 (COLAPSAR); `_veredito_fronteirico` :329-395 + :697 (REMOVER); dispatch :519; ensemble
  motor×contraponto :591-629; `_hipotese_divergencia` :874-890 (REMOVER); render divergência/incerto
  :1071-1103 (REMOVER); flags de risco (VERIFICAR) :645-657 (MANTER).
motores.py: `rim` :66 (MANTER); `lucro_normalizado` :149 → derivador; `dcf_crescimento` :161 → DELETAR;
  `nav_contabil` :195 → derivador; `MOTOR_ROTULO` :33 (dict — REMOVER a chave "seguradora"; MANTER "rim");
  terminal :128-129 (g_terminal=None ⇒ vp_terminal=0).
arquetipo: ARQUETIPO_ANCORA_ROE[chave] (Plano 02); ARQUETIPO_MOTOR legado (NÃO consumir; morre no 06).
Insumos PRONTOS (não recomputar): a.ke; g_cap; g_T = max(0, min(ROE_T×retenção, g_cap)).

<!-- INVENTÁRIO EMPÍRICO dos testes que quebram na onda 3 (grep 2026-07-19; classif. entre []) -->
test_report.py [contrato]: test_ensemble_banda_do_motor_financeira (:98), o teste de :120 (motor=="ddm"
  + banda_do_motor False), o de :140 (contraponto None/banda), test_divergencia_hipotese_preenchida...
  (:161), o de :181 (contraponto no md), :198, :241 — todos asseveram banda_do_motor/contraponto_valor/
  divergencia_*. [DELETE, é a máquina do ensemble]
test_guardrails_ddm.py [contrato + 1 golden]: 5 de `_guarda_faixa_ddm` (:48-83), 7 de `_guarda_san01`/
  `san01_reetiquetado` (:199-286), + golden test_san01_reetiqueta_aberracao_itub4_like. [DELETE tudo]
test_vulc3_regressao.py [contrato/invariante]: test_capstone_vale3_motor_normalizado (:268),
  test_capstone_wege3_motor_dcf_sem_faixa_lixo (:280), o(s) capstone(s) asserindo banda_do_motor/
  contraponto (:234-238,:269,:281) [DELETE]; test_vulc3_cascata_estrutural_sobrevive [invariante, banda=
  matriz min/max] e test_vulc3_normalizacao_doma_roe_e_dy [invariante clean-surplus] [REWRITE]. (o
  test_capstone_taee11, :250 a.arquetipo=="pagadora_regulada", já é DELETADO no Plano 02 — onda 2.)
test_arquetipo_roteamento.py [contrato + 1 golden]: os fronteiriço (:302-380 arquetipo_incerto/
  candidatos_intrinsecos/veredito_range), os VER-01/banda (:162,:181,:228-273), motor=="normalizado"/
  "dcf"/"nav"/"ddm" (:242,:252,:269,:279), golden test_financeira_rim_destrava... [DELETE]; +
  test_financeira_veredito_real_do_motor_nunca_evitar e test_degradacao_um_ano_nao_levanta [REWRITE].
  (o :145-148 regulada-ddm baseline já é DELETADO no Plano 02.)
test_motores.py [contrato/invariante]: test_rota_seguradora_roteia_e_da_intrinseco_finito_positivo
  (:217 a.motor=="seguradora") [REWRITE — WR-04 finite>0 sobre FINANCEIRA→RIM];
  test_setor_de_banco_nao_casa_o_token_seguradora (:234-236 _setor_casa_token puro) [INTOCADO — sobrevive].
test_backtest_bancos.py [contrato]: test_backtest_rotulo_do_motor_consistente (:130 motor=="seguradora",
  :131 MOTOR_ROTULO["seguradora"], :138 MOTOR_ROTULO["rim"]) [REWRITE — sob RIM único todos → "rim"].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Colapsar o dispatch num RIM único + remover a rota/chave seguradora</name>
  <files>src/analista/report/report.py, src/analista/core/motores.py</files>
  <read_first>
    - src/analista/report/report.py (`_intrinseco_por_motor` :201-327; ramo `rim` :255-268; ramo `normalizado` :285-309; rota `seguradora` dentro do dispatch; `_roe_through_cycle` :184; dispatch :519)
    - src/analista/core/motores.py (`rim` :66-146, `lucro_normalizado` :149, `dcf_crescimento` :161, `nav_contabil` :195, `MOTOR_ROTULO` :33; terminal :128-129)
    - src/analista/core/arquetipo.py (ARQUETIPO_ANCORA_ROE do Plano 02)
    - .planning/spikes/eng-rim-104.md (a escolha do g_terminal do carve-out)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Pattern 2", §"Mapa de âncoras", §"Carve-out CONCESSAO_FINITA")
  </read_first>
  <behavior>
    - Todo ticker com dado suficiente resolve `intrinseco_motor` finito e > 0 via `motores.rim` (ou None never-raise).
    - `a.motor`/rótulo NUNCA é ddm/seguradora/normalizado/dcf/nav como motor primário — sempre o RIM (ou a política de âncora).
    - through_cycle: roe0=c.roe_valuation(), roe_terminal=_roe_through_cycle.
    - normalizado (cíclica): roe0 ≈ lpa_norm_deflacionado / vpa0 (série deflacionada por IPCA, PRIM-04).
    - atual_fade (crescimento): roe0=c.roe_qualidade_atual(), fade sobre n_fade; dcf_crescimento não é chamado.
    - through_cycle_sem_g (CONCESSAO_FINITA): g_terminal = a escolha do spike (None fade-only recomendado).
    - A seguradora capital-light (FINANCEIRA) produz intrínseco FINITO e > 0 pelo RIM (ROE alto compensa o book pequeno) — o invariante WR-04 sobrevive sem a rota própria.
  </behavior>
  <action>
    Reescrever `_intrinseco_por_motor` (renomear para `_valor_rim` se ajudar) para: derivar o insumo por
    `_derivar_insumo(politica, c, cfg, g_cap, rim_cfg)` -> (roe0, roe_terminal, g_terminal, base_book)
    conforme ARQUETIPO_ANCORA_ROE[a.arquetipo], e chamar SEMPRE motores.rim(...) com ke=a.ke e g_T. Para
    CONCESSAO_FINITA passar g_terminal conforme o spike (None fade-only). **Remover a rota `seguradora` E
    o passthrough `ddm`** do dispatch (a seguradora, classificada FINANCEIRA, roda o RIM). Em motores.py:
    DELETAR dcf_crescimento (NÃO FCFE) E DELETAR motores.lucro_normalizado (Gordon-P/L obsoleto — o
    derivador da cíclica computa `lpa_norm` INLINE via norm.media_ciclo + mult.lpa, não pela função);
    MANTER motores.nav_contabil (= lentes.vpa) como derivador de piso patrimonial; **remover
    a chave `"seguradora"` de `MOTOR_ROTULO`** (a rota morreu; MANTER a chave "rim"). Continuar lendo
    cfg["motores"]["ciclica"]["anos_media"] (o rename do knob é o Plano 05). NÃO tocar a.ke/g_cap/g_T. NÃO
    consumir ARQUETIPO_MOTOR (legado; morre no Plano 06) — usar ARQUETIPO_ANCORA_ROE.
  </action>
  <acceptance_criteria>
    - `grep -nE "'seguradora'|\"seguradora\"|motor == .ddm|== .normalizado.|== .dcf.|== .nav." src/analista/report/report.py` no dispatch == 0 (nenhum ramo por nome de motor; rota seguradora removida)
    - `grep -c 'def dcf_crescimento' src/analista/core/motores.py` == 0 e `grep -c '"seguradora"' src/analista/core/motores.py` == 0 (chave MOTOR_ROTULO removida); `MOTOR_ROTULO["rim"]` permanece
    - `grep -c 'def lucro_normalizado' src/analista/core/motores.py` == 0 (Gordon obsoleto DELETADO) e `grep -c 'def nav_contabil' src/analista/core/motores.py` == 1 (piso SOBREVIVE)
    - motores.rim é chamado no caminho único de report.py (grep) e a.ke/g_cap não são recomputados; ARQUETIPO_MOTOR não é consumido em report.py
    - `.venv/bin/python -m pytest -k 'not golden_nivel' -q` coleta sem CLASSIFICACAO ORFA (falhas de ensemble esperadas até a Task 3)
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'def dcf_crescimento' src/analista/core/motores.py) -eq 0 && test $(grep -c 'def lucro_normalizado' src/analista/core/motores.py) -eq 0 && test $(grep -c 'def nav_contabil' src/analista/core/motores.py) -eq 1 && test $(grep -c '"seguradora"' src/analista/core/motores.py) -eq 0 && grep -q 'motores.rim' src/analista/report/report.py</automated>
  </verify>
  <done>Um só caminho RIM; dcf e rota/chave seguradora mortos; carve-out da concessão aplicado; Ke/g intocados; ARQUETIPO_MOTOR não consumido.</done>
</task>

<task type="auto">
  <name>Task 2: Remover ensemble, guardas-cicatriz e render de divergência (não portar)</name>
  <files>src/analista/report/report.py</files>
  <read_first>
    - src/analista/report/report.py (AnaliseAcao :54-77 incl. san01_reetiquetado/arquetipo_fronteirico; _guarda_faixa_ddm :77-105 + :589; _guarda_san01 :108-182 + :706; ensemble :591-629; _veredito_fronteirico :329-395 + :697; _hipotese_divergencia :874-890; render :1071-1103; flags de risco :645-657)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Remoção do ensemble")
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md (D-04)
  </read_first>
  <action>
    DELETAR (não portar): _guarda_faixa_ddm + chamada; _guarda_san01 + chamada; _veredito_fronteirico +
    chamada; _hipotese_divergencia (+ _HIPOTESE_DIVERGENCIA); TODOS os campos de ensemble de AnaliseAcao
    (motor_pendente, contraponto_valor, banda_do_motor, divergencia_ativa/razao/hipotese, arquetipo_incerto,
    arquetipo_fronteirico, san01_reetiquetado, candidatos_intrinsecos, veredito_range); o render de
    "Classificação incerta"/"Bandeira de divergência" (:1071-1103). O bloco ensemble motor×contraponto
    (:591-629) sai — deixar vmin/vmax na FORMA SIMÉTRICA sobre intrinseco_motor (o fallback :608-610 vira
    o caminho; o Plano 04 formaliza como região da MS primária). NÃO deletar cfg["veredito"]["san01"] do
    config.yaml aqui (é o Plano 05 — evita o hook BLIND-05). NÃO tocar as flags de risco do veredito
    (:645-657 — do livro, sobrevivem).
  </action>
  <acceptance_criteria>
    - `grep -cE 'def _guarda_san01|def _guarda_faixa_ddm|def _veredito_fronteirico|def _hipotese_divergencia' src/analista/report/report.py` == 0
    - `grep -cE 'banda_do_motor|divergencia_ativa|contraponto_valor|candidatos_intrinsecos|veredito_range|arquetipo_incerto|arquetipo_fronteirico|san01_reetiquetado|motor_pendente' src/analista/report/report.py` == 0
    - As flags de risco do veredito (VERIFICAR) permanecem (grep confirma)
    - `cfg["veredito"]["san01"]` NÃO removido do config.yaml neste plano (git diff config.yaml VAZIO)
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -cE 'def _guarda_san01|def _guarda_faixa_ddm|def _veredito_fronteirico|banda_do_motor|divergencia_ativa|candidatos_intrinsecos|san01_reetiquetado' src/analista/report/report.py) -eq 0 && test -z "$(git diff config.yaml)"</automated>
  </verify>
  <done>Ensemble/guardas/divergência/fronteiriço e TODOS os campos removidos; região simétrica no lugar; flags de risco intactas; config.yaml não tocado.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep exaustivo — delete/rewrite de TODO teste que asseve campo/motor/função deletados (onda 3)</name>
  <files>tests/test_report.py, tests/test_vulc3_regressao.py, tests/test_guardrails_ddm.py, tests/test_arquetipo_roteamento.py, tests/test_motores.py, tests/test_backtest_bancos.py, tests/classificacao.yaml</files>
  <read_first>
    - `grep -rnE 'banda_do_motor|contraponto_valor|divergencia_(ativa|razao|hipotese)|_hipotese_divergencia\(|arquetipo_(fronteirico|incerto)|san01_reetiquetado|_guarda_san01\(|_guarda_faixa_ddm\(|cfg\["veredito"\]\["san01"\]|veredito_range|candidatos_intrinsecos|\.dcf_crescimento\(|\.lucro_normalizado\(|MOTOR_ROTULO\["seguradora"\]|\.motor *== *"(ddm|seguradora|normalizado|dcf|nav)"' tests/ | sed 's/#.*//' | grep -vE ':\s*$'` (o inventário canônico VIVO — fora de comentário — desta task)
    - tests/test_report.py, tests/test_vulc3_regressao.py, tests/test_guardrails_ddm.py, tests/test_arquetipo_roteamento.py, tests/test_motores.py, tests/test_backtest_bancos.py
    - tests/classificacao.yaml (completude na coleta)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Remoção do ensemble" tabela; §Pitfall 5); CLAUDE.md (golden DELETADO; nunca afrouxar/xfail→skip)
  </read_first>
  <action>
    Percorrer o inventário do grep acima e decidir por teste (DELETE-vs-REWRITE):
    - **DELETE (máquina de ensemble/dispatch por motor / guarda deletada — baseline do método antigo, função
      + linha da classificacao no MESMO diff):**
      test_report.py: os que asseveram banda_do_motor/contraponto_valor/divergencia_* (test_ensemble_*,
      test_divergencia_hipotese_*, os de :120/:140/:181/:198/:241). test_guardrails_ddm.py: os 5 de
      `_guarda_faixa_ddm`, os 7 de `_guarda_san01`/san01_reetiquetado, + o golden
      test_san01_reetiqueta_aberracao_itub4_like. test_vulc3_regressao.py: test_capstone_vale3_motor_
      normalizado, test_capstone_wege3_motor_dcf_sem_faixa_lixo e o(s) capstone(s) com banda_do_motor/
      contraponto. test_arquetipo_roteamento.py: os fronteiriço (arquetipo_incerto/candidatos_intrinsecos/
      veredito_range), os VER-01/banda_do_motor, os motor=="normalizado"/"dcf"/"nav"/"ddm", + o golden
      test_financeira_rim_destrava_vs_ddm_e_alimenta_veredito.
    - **REWRITE (invariante estrutural que SOBREVIVE — repontar à nova arquitetura, NÃO afrouxar):**
      test_vulc3_regressao.py::test_vulc3_cascata_estrutural_sobrevive → banda deixou de ser min/max da
      matriz; asseverar a região da MS (vmin==V×(1−ms), vmax==V×(1+ms)), preservando finito/never-raise/
      cascata. test_vulc3_regressao.py::test_vulc3_normalizacao_doma_roe_e_dy → o clean-surplus/ROE domado
      SOBREVIVE; drop do assert de rótulo de motor, mantendo a domesticação do ROE/DY sob o RIM único.
      test_arquetipo_roteamento.py::test_financeira_veredito_real_do_motor_nunca_evitar → financeira roteia
      RIM, veredito ∈ {SUBAVALIADA, NO INTERVALO, SOBREAVALIADA}, drop de banda_do_motor.
      test_arquetipo_roteamento.py::test_degradacao_um_ano_nao_levanta → never-raise (SAN-06) SOBREVIVE;
      drop dos asserts de motor_pendente/banda/fronteirico. **test_motores.py::test_rota_seguradora_roteia_
      e_da_intrinseco_finito_positivo → REWRITE (WR-04, aviso Fase-7 — NÃO deletar em silêncio): a
      seguradora capital-light (FINANCEIRA) produz intrínseco FINITO e > 0 pelo RIM único; drop de
      a.motor=="seguradora" (rota morta), MANTER o assert finite>0 (sem nível em reais, BLIND-04a).**
      **test_backtest_bancos.py::test_backtest_rotulo_do_motor_consistente → REWRITE: sob RIM único todos
      os tickers têm a.motor_rotulo == MOTOR_ROTULO["rim"]; remover o ramo seguradora (motor=="seguradora"
      e MOTOR_ROTULO["seguradora"]).**
    - **DELETE (funções deletadas — os greps de call-form as pegam; iter-3):** test_motores.py::
      test_dcf_crescimento_positivo_finito_e_modelo_h_conservador e ::test_dcf_crescimento_never_raise
      (chamam motores.dcf_crescimento — motor deletado; a invariante finite/never-raise da crescimento
      é coberta pelo RIM/atual_fade na regressão dos 104 do Plano 07); test_motores.py::
      test_lucro_normalizado_usa_media_e_ignora_pico_vale e ::test_lucro_normalizado_never_raise (chamam
      motores.lucro_normalizado — Gordon obsoleto; a média through-cycle vive em norm.media_ciclo, testada
      à parte); test_report.py::test_hipotese_divergencia_financeira_motor_acima e ::test_hipotese_
      divergencia_fallback_generico (chamam report._hipotese_divergencia — deletado na Task 2). **O
      test_guardrails_ddm.py inteiro é DELETADO** (todos os _guarda_faixa_ddm/_guarda_san01/san01_
      reetiquetado/e2e MAIS **test_config_tem_bloco_san01 [B3]** que lê cfg["veredito"]["san01"] — o
      bloco de config morre no Plano 05/onda 5, mas o teste sai AQUI, onda 3, porque o Plano 05 exclui
      tests/ por BLIND-05; deletar na onda 3 é seguro: enquanto o bloco existe o teste-deletado não roda).
      test_arquetipo_roteamento.py::test_ciclica_roteia_lucro_normalizado (asseve motor=='normalizado').
    - **INTOCADO (colisão de nome — NÃO deletar):** test_motores.py::test_nav_contabil_igual_vpa e
      ::test_nav_contabil_never_raise (nav_contabil SOBREVIVE como piso). test_sanidade_checks.py /
      test_sanidade_pipeline.py `checar_san01` = SAN-01 da Fase 8 (dado), NÃO o `_guarda_san01` do
      veredito — INTOCADOS. Os asserts `"Evitar" not in ...` (ausência) sobrevivem por construção.
    - **INTOCADO:** test_motores.py::test_setor_de_banco_nao_casa_o_token_seguradora (`_setor_casa_token`
      puro, não referencia rota/campo) — permanece verde sem edição.
    Cada DELETE remove função + linha da classificacao no MESMO diff (0 órfão). Cada REWRITE mantém a
    entrada válida (invariante/contrato). NUNCA afrouxar tolerância, xfail casual ou xfail→skip. Commits de
    teste sem tocar config.yaml. (test_ranking_freio.py NÃO é tocado aqui — quebra no Plano 06.)
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` (suíte default, onda 3) 0 failed; `-m golden_nivel` sem CLASSIFICACAO ORFA; `xfail_estritos()` == 0; só o skip do jackknife sobra
    - Inventário VIVO zerado (comment-aware): `grep -rnE 'banda_do_motor|contraponto_valor|divergencia_ativa|san01_reetiquetado|veredito_range|candidatos_intrinsecos|_guarda_san01|_guarda_faixa_ddm|\.motor *== *"(ddm|seguradora|normalizado|dcf|nav)"|MOTOR_ROTULO\["seguradora"\]' tests/ | sed 's/#.*//' | grep -cE 'banda_do_motor|contraponto_valor|divergencia_ativa|_hipotese_divergencia\(|san01_reetiquetado|_guarda_san01\(|_guarda_faixa_ddm\(|veredito_range|candidatos_intrinsecos|\.dcf_crescimento\(|\.lucro_normalizado\(|== *"(ddm|seguradora|normalizado|dcf|nav)"|MOTOR_ROTULO' ` == 0
    - Os 4 REWRITE + o WR-04 da seguradora asseveram a NOVA arquitetura (grep: cascata usa 'margem'/região; seguradora usa finite>0 sem 'seguradora'); test_setor_de_banco_nao_casa_o_token_seguradora INTOCADO (git diff vazio nele)
    - Nenhum golden_nivel ATUALIZADO (só REMOÇÃO das linhas dos deletados); nenhum assert afrouxado; nenhum xfail→skip novo
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -rnE 'banda_do_motor|contraponto_valor|divergencia_ativa|_hipotese_divergencia\(|san01_reetiquetado|_guarda_san01\(|_guarda_faixa_ddm\(|cfg\["veredito"\]\["san01"\]|veredito_range|candidatos_intrinsecos|\.dcf_crescimento\(|\.lucro_normalizado\(|MOTOR_ROTULO\["seguradora"\]' tests/ | sed 's/#.*//' | grep -cE 'banda_do_motor|contraponto_valor|divergencia_ativa|_hipotese_divergencia\(|san01_reetiquetado|_guarda_san01\(|_guarda_faixa_ddm\(|veredito_range|candidatos_intrinsecos|\.dcf_crescimento\(|\.lucro_normalizado\(|MOTOR_ROTULO') -eq 0 && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>Sweep da onda 3 fechado: ensemble/guarda/rota deletados, invariantes (cascata/normalização/WR-04 seguradora) RESCRITOS, token-test intocado; suíte default verde; coleta sem órfão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CompanyData (dado curado, mas com escalas absurdas residuais) → RIM único | O único caminho de valor precisa degradar, não quebrar, sob razão patológica |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Denial (crash) | _valor_rim / _derivar_insumo sobre dado faltante ou absurdo | mitigate | Never-raise (SAN-06): degrada para None; motores.rim já é never-raise; sinais guardados com is not None |
| T-13-06 | Elevation (bug oculto) | carve-out CONCESSAO_FINITA aplicando g de inflação | mitigate | g_terminal=None (fade-only nativo) conforme spike; nenhuma reintrodução de g_cap no terminal da concessão |
| T-13-07 | Tampering (overfit) | golden de nível atualizado em vez de deletado | mitigate | Golden que quebra é DELETADO; invariantes sobreviventes (incl. WR-04 seguradora) RESCRITOS, não afrouxados nem mortos em silêncio |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` (default, onda 3) 0 failed; `-m golden_nivel` sem CLASSIFICACAO ORFA.
- Nenhum ramo de dispatch por nome de motor; rota/chave seguradora removidas; `motores.rim` é o único chamado; `dcf_crescimento` deletado.
- Campos de ensemble/`_guarda_*` ausentes; inventário VIVO (comment-aware) de símbolos deletados == 0 em tests/.
- WR-04 da seguradora RESCRITO (finite>0 sobre FINANCEIRA→RIM), token-test intocado; cascata/normalização repontados à região da MS/RIM.
- `git diff config.yaml calibracao.lock.yaml` VAZIO (knob-cut é o Plano 05).
</verification>

<success_criteria>
Um único caminho RIM alimentado pela política do arquétipo; ensemble, guardas-cicatriz e rota/chave
seguradora removidos; dcf morto; carve-out da concessão aplicado; TODO teste não-quarentenado que asseve
campo/motor/função deletado foi DELETADO (baseline) ou RESCRITO (invariante, incl. o WR-04 da seguradora)
— suíte default verde na onda 3, sem tocar Ke/g/config.
</success_criteria>

<output>
After completion, create `.planning/phases/13-motores-contrato-de-sa-da-eng/13-03-SUMMARY.md`
</output>
