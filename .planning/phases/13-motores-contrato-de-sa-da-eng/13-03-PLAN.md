---
phase: 13-motores-contrato-de-sa-da-eng
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/analista/report/report.py
  - src/analista/core/motores.py
  - tests/test_report.py
  - tests/test_guardrails_ddm.py
  - tests/test_arquetipo_roteamento.py
  - tests/test_vulc3_regressao.py
  - tests/test_ranking_freio.py
  - tests/classificacao.yaml
autonomous: true
requirements: [ENG-01, ENG-02, ENG-03, ENG-04]
must_haves:
  truths:
    - "Existe UM único caminho de valor: `_intrinseco_por_motor` colapsou num caminho que sempre chama `motores.rim(...)` com o insumo derivado pela política do arquétipo"
    - "Nenhum ticker roteia para `ddm`/`seguradora`/`normalizado`/`dcf`/`nav` como motor primário"
    - "O ensemble morreu: `_guarda_san01`, `_guarda_faixa_ddm`, `_veredito_fronteirico`, `_hipotese_divergencia` e os campos de ensemble de `AnaliseAcao` foram REMOVIDOS (não portados)"
    - "`CONCESSAO_FINITA` usa modelo de ativo financeiro: o `g_terminal` NÃO recebe o g de inflação (carve-out declarado, conforme o spike do Plano 01)"
    - "`dcf_crescimento` foi DELETADO como motor (não substituído por FCFE); `lucro_normalizado`/`nav_contabil` sobrevivem só como derivadores de insumo"
    - "TODO teste que asseve campos de ensemble/rótulo de motor deletados foi DELETADO (baseline do método antigo) ou RESCRITO contra a nova arquitetura (RIM único + região da MS), com a entrada em classificacao.yaml casada no mesmo diff"
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "Dispatch RIM único (_valor_rim/_derivar_insumo) + ensemble removido"
      contains: "motores.rim"
    - path: "src/analista/core/motores.py"
      provides: "dcf_crescimento removido como motor; rim é a única fórmula de valor"
  key_links:
    - from: "report._intrinseco_por_motor (colapsado)"
      to: "motores.rim"
      via: "chamada única com roe0/roe_terminal/g_terminal derivados pela política do arquétipo"
      pattern: "motores\\.rim"
    - from: "ARQUETIPO_ANCORA_ROE[a.arquetipo]"
      to: "_derivar_insumo"
      via: "política → (roe0, roe_terminal, g_terminal)"
      pattern: "ARQUETIPO_ANCORA_ROE|_derivar_insumo"
  anti_goals:
    - "NÃO 'consertar' o `dcf_crescimento` com FCFE (`lpa × payout`): vira DDM por teorema (Armadilha 2, WEGE3 0,58→0,26). Ele MORRE"
    - "NÃO aplicar o `g_cap` (que embute inflação) ao terminal de `CONCESSAO_FINITA`: double-count de IPCA sob ICPC 01 (Armadilha)"
    - "NÃO portar `_guarda_san01`/`_guarda_faixa_ddm`/ensemble 'por segurança': são cicatrizes do viés"
    - "NÃO recomputar `a.ke` (Fase 12) nem recalibrar `g_cap`/`g_T` (Fase 11): já prontos; tocar apaga o diagnóstico"
    - "NÃO validar o caso do livro (ITUB4=R$37,22): Fase 14. Nenhum assert com ticker+número-alvo"
    - "NÃO ATUALIZAR golden de nível para o valor novo: golden/baseline que quebra é DELETADO (função + linha classificacao no mesmo diff); nunca afrouxar tolerância nem trocar xfail→skip"
---

<objective>
Colapsar os 4 motores num **RIM único** (ENG-01) e **matar o ensemble** (ENG-02). `_intrinseco_por_motor`
deixa de ter 6 ramos (`rim`/`normalizado`/`dcf`/`nav`/`ddm` + rota `seguradora`) e passa a: (1) derivar
o insumo (`roe0`/`roe_terminal`/`g_terminal`/base de book) pela política `ARQUETIPO_ANCORA_ROE` do
arquétipo (Plano 02); (2) chamar **sempre** `motores.rim(...)` com `a.ke` (Fase 12) e `g_T` (Fase 11)
prontos. O `dcf_crescimento` é **deletado**; `lucro_normalizado`/`nav_contabil` sobrevivem só como
derivadores. `CONCESSAO_FINITA` recebe o carve-out (g terminal sem inflação, conforme o spike do Plano
01). Todas as guardas-cicatriz e o bloco de divergência/fronteiriço são **removidos**.

**Correção do checker (BLOCKER, onda 3):** a deleção dos campos de `AnaliseAcao` e dos rótulos de motor
QUEBRA, na onda 3, os testes que os asseveram ao vivo em `test_report.py` e `test_vulc3_regressao.py`
(além dos de `test_arquetipo_roteamento.py`/`test_guardrails_ddm.py` já listados). Esses testes são
DELETADOS (baseline do método antigo) ou RESCRITOS (invariantes que sobrevivem) AQUI, no mesmo diff da
deleção, com decisão explícita por teste.

Purpose: uma fórmula, um caminho, erro limitado — e a suíte default 0 failed na onda 3.
Output: dispatch RIM único; `motores.py` sem `dcf_crescimento`-motor; ensemble deletado; testes do
mundo antigo resolvidos (delete/rewrite).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md
@.planning/spikes/eng-rim-104.md

@src/analista/report/report.py
@src/analista/core/motores.py
@src/analista/core/arquetipo.py

<interfaces>
<!-- Sites exatos (medidos), o executor NÃO precisa explorar -->
report.py: `AnaliseAcao` (:54-77; campos de ensemble a REMOVER: `motor_pendente` :58, `contraponto_valor`
  :66, `banda_do_motor` :67, `divergencia_ativa` :68, `divergencia_hipotese` :70, `arquetipo_incerto`
  :72, `candidatos_intrinsecos` :73, `veredito_range` :74); `_guarda_faixa_ddm` (:77-105, chamada :589);
  `_guarda_san01` (:108-182, chamada :706); `_roe_through_cycle` (:184 — MANTER); `_intrinseco_por_motor`
  (:201-327 — COLAPSAR); `_veredito_fronteirico` (:329-395, chamada :697 — REMOVER); dispatch :519; bloco
  ensemble motor×contraponto (:591-629); `_hipotese_divergencia` (:874-890 — REMOVER); render de
  divergência/incerto (:1071-1103 — REMOVER); flags de risco do veredito (VERIFICAR) :645-657 — MANTER.
motores.py: `rim` (:66 — MANTER); `lucro_normalizado` (:149 → derivador); `dcf_crescimento` (:161 →
  DELETAR); `nav_contabil` (:195 → derivador de piso). Terminal: :128-129 (g_terminal=None ⇒ vp_terminal=0).
arquetipo: ARQUETIPO_ANCORA_ROE[chave] (Plano 02); ARQUETIPO_MOTOR legado (NÃO consumir aqui; morre no 06).
Insumos PRONTOS (não recomputar): a.ke (Fase 12); g_cap; g_T = max(0, min(ROE_T×retenção, g_cap)).
Testes que quebram na onda 3 (asseveram CAMPOS/rótulo de motor, não a string de arquétipo — essa foi o
  Plano 02): test_report.py (test_ensemble_*, test_hipotese_divergencia_* x3, test_render_bandeira_
  divergencia_*, test_ver01_* x2, test_cr01_motor_degrada...); test_vulc3_regressao.py (test_capstone_
  itub4_*, test_capstone_vale3_motor_normalizado, test_capstone_wege3_motor_dcf_sem_faixa_lixo,
  test_vulc3_cascata_estrutural_sobrevive [invariante, banda=min/max matriz], test_vulc3_normalizacao_
  doma_roe_e_dy [invariante clean-surplus/ROE]); test_arquetipo_roteamento.py (fronteiriço + test_financeira_
  veredito_real_do_motor_nunca_evitar, test_degradacao_um_ano_nao_levanta); test_guardrails_ddm.py (5 de
  _guarda_faixa_ddm + o golden test_san01_reetiqueta...).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Colapsar o dispatch num RIM único alimentado pela política do arquétipo</name>
  <files>src/analista/report/report.py, src/analista/core/motores.py</files>
  <read_first>
    - src/analista/report/report.py (`_intrinseco_por_motor` :201-327; ramo `rim` :255-268; ramo `normalizado` :285-309; `_roe_through_cycle` :184; dispatch :519)
    - src/analista/core/motores.py (`rim` :66-146, `lucro_normalizado` :149, `dcf_crescimento` :161, `nav_contabil` :195; terminal :128-129)
    - src/analista/core/arquetipo.py (ARQUETIPO_ANCORA_ROE do Plano 02)
    - .planning/spikes/eng-rim-104.md (a escolha do g_terminal do carve-out)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Pattern 2", §"Mapa de âncoras", §"Carve-out CONCESSAO_FINITA", §Code Examples)
  </read_first>
  <behavior>
    - Todo ticker com dado suficiente resolve `intrinseco_motor` finito e > 0 via `motores.rim` (ou None never-raise) — nenhum NaN/inf/exceção.
    - `a.motor`/rótulo NUNCA é ddm/seguradora/normalizado/dcf/nav como motor primário — sempre o RIM (ou o nome da política de âncora).
    - through_cycle: roe0=c.roe_valuation(), roe_terminal=_roe_through_cycle (baseline; bancos não regridem).
    - normalizado (cíclica): roe0 ≈ lpa_norm_deflacionado / vpa0 (série deflacionada por IPCA, PRIM-04).
    - atual_fade (crescimento): roe0=c.roe_qualidade_atual(), fade sobre n_fade; dcf_crescimento não é chamado.
    - through_cycle_sem_g (CONCESSAO_FINITA): g_terminal = a escolha do spike (None fade-only recomendado).
  </behavior>
  <action>
    Reescrever `_intrinseco_por_motor` (renomear para `_valor_rim` se ajudar) para: derivar o insumo por
    um helper `_derivar_insumo(politica, c, cfg, g_cap, rim_cfg)` -> (roe0, roe_terminal, g_terminal,
    base_book) conforme ARQUETIPO_ANCORA_ROE[a.arquetipo], e chamar SEMPRE motores.rim(vpa0=lentes.vpa(PL,
    num_acoes), roe0, ke=a.ke, retencao=1−payout, n=rim_cfg["n_fade"], excesso_sustentavel=..., g_terminal,
    ke_g_spread_min=..., roe_terminal). Para CONCESSAO_FINITA passar g_terminal conforme o spike (None
    fade-only; motores.py:128-129 zera o terminal nativamente). Remover a rota seguradora e o passthrough
    ddm do dispatch. Em motores.py: DELETAR dcf_crescimento (NÃO FCFE); manter rim; converter
    lucro_normalizado em helper de derivação do lpa_norm (ou inlinar) e nav_contabil em derivador de piso.
    Continuar lendo cfg["motores"]["ciclica"]["anos_media"] (o rename do knob é o Plano 05). NÃO tocar
    a.ke/g_cap/g_T. NÃO consumir ARQUETIPO_MOTOR (legado; morre no Plano 06) — usar ARQUETIPO_ANCORA_ROE.
  </action>
  <acceptance_criteria>
    - `grep -nE "'seguradora'|\"seguradora\"|motor == .ddm|== .normalizado.|== .dcf.|== .nav." src/analista/report/report.py` no dispatch == 0
    - `grep -c 'def dcf_crescimento' src/analista/core/motores.py` == 0 (motor deletado)
    - motores.rim é chamado no caminho único de report.py (grep) e a.ke/g_cap não são recomputados (nenhum capm.ke_local dentro do dispatch); ARQUETIPO_MOTOR não é consumido em report.py
    - `.venv/bin/python -m pytest -k 'not golden_nivel' -q` coleta sem CLASSIFICACAO ORFA (falhas de testes de ensemble esperadas até a Task 3; a suíte fecha no fim do plano)
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'def dcf_crescimento' src/analista/core/motores.py) -eq 0 && grep -q 'motores.rim' src/analista/report/report.py</automated>
  </verify>
  <done>Um só caminho RIM alimentado pela política do arquétipo; dcf morto; carve-out da concessão aplicado; Ke/g intocados; ARQUETIPO_MOTOR não consumido.</done>
</task>

<task type="auto">
  <name>Task 2: Remover ensemble, guardas-cicatriz e render de divergência (não portar)</name>
  <files>src/analista/report/report.py</files>
  <read_first>
    - src/analista/report/report.py (AnaliseAcao :54-77; _guarda_faixa_ddm :77-105 + chamada :589; _guarda_san01 :108-182 + chamada :706; bloco ensemble :591-629; _veredito_fronteirico :329-395 + chamada :697; _hipotese_divergencia :874-890; render :1071-1103; flags de risco :645-657)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Remoção do ensemble")
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md (D-04)
  </read_first>
  <action>
    DELETAR (não portar): _guarda_faixa_ddm + chamada; _guarda_san01 + chamada; _veredito_fronteirico +
    chamada; _hipotese_divergencia (+ _HIPOTESE_DIVERGENCIA); os campos de ensemble de AnaliseAcao
    (motor_pendente, contraponto_valor, banda_do_motor, divergencia_ativa/razao/hipotese, arquetipo_incerto,
    candidatos_intrinsecos, veredito_range); o render de "Classificação incerta"/"Bandeira de divergência"
    (:1071-1103). O bloco ensemble motor×contraponto (:591-629) sai — nesta task deixar vmin/vmax
    derivando da FORMA SIMÉTRICA sobre intrinseco_motor (o fallback :608-610 vira o caminho, para o
    pipeline não quebrar; o Plano 04 formaliza como região da MS primária). NÃO deletar cfg["veredito"]
    ["san01"] do config.yaml aqui (é o Plano 05 — evita o hook BLIND-05). NÃO tocar as flags de risco do
    veredito (:645-657 — são do livro, sobrevivem).
  </action>
  <acceptance_criteria>
    - `grep -cE 'def _guarda_san01|def _guarda_faixa_ddm|def _veredito_fronteirico|def _hipotese_divergencia' src/analista/report/report.py` == 0
    - `grep -cE 'banda_do_motor|divergencia_ativa|contraponto_valor|candidatos_intrinsecos|veredito_range|arquetipo_incerto|motor_pendente' src/analista/report/report.py` == 0
    - As flags de risco do veredito (VERIFICAR por payout>100%/DY alto/div em prejuízo) permanecem (grep confirma)
    - `cfg["veredito"]["san01"]` NÃO removido do config.yaml neste plano (git diff config.yaml VAZIO)
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -cE 'def _guarda_san01|def _guarda_faixa_ddm|def _veredito_fronteirico|banda_do_motor|divergencia_ativa|candidatos_intrinsecos' src/analista/report/report.py) -eq 0 && test -z "$(git diff config.yaml)"</automated>
  </verify>
  <done>Ensemble/guardas/divergência/fronteiriço removidos; região simétrica no lugar; flags de risco intactas; config.yaml não tocado.</done>
</task>

<task type="auto">
  <name>Task 3: Delete/rewrite dos testes que quebram na onda 3 (campos/motor) + classificacao no mesmo diff</name>
  <files>tests/test_report.py, tests/test_vulc3_regressao.py, tests/test_guardrails_ddm.py, tests/test_arquetipo_roteamento.py, tests/test_ranking_freio.py, tests/classificacao.yaml</files>
  <read_first>
    - `grep -rn 'banda_do_motor\|contraponto_valor\|divergencia_\|arquetipo_incerto\|candidatos_intrinsecos\|veredito_range\|motor_pendente\|a\.motor ==\|== .ddm.\|== .normalizado.\|== .dcf.' tests/` (inventário canônico dos que quebram na onda 3)
    - tests/test_report.py, tests/test_vulc3_regressao.py, tests/test_arquetipo_roteamento.py, tests/test_guardrails_ddm.py, tests/test_ranking_freio.py
    - tests/classificacao.yaml (completude na coleta)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Remoção do ensemble" tabela de testes; §Pitfall 5)
    - CLAUDE.md (golden que quebra é DELETADO; nunca afrouxar/xfail→skip)
  </read_first>
  <action>
    Decisão explícita DELETE-vs-REWRITE por teste (use o grep do read_first como inventário):
    - **DELETE (máquina de ensemble/dispatch por motor — baseline do método antigo):**
      test_report.py: test_ensemble_banda_do_motor_financeira, test_ensemble_ddm_intocado_regulada,
      test_ensemble_fallback_margem_seguranca, test_hipotese_divergencia_* (x3), test_render_bandeira_
      divergencia_*, test_ver01_* (x2), test_cr01_motor_degrada_mas_ddm_sobrevive_rotula_ddm.
      test_vulc3_regressao.py: test_capstone_itub4_* (asseve motor/banda), test_capstone_vale3_motor_
      normalizado, test_capstone_wege3_motor_dcf_sem_faixa_lixo. test_guardrails_ddm.py: os 5 de
      _guarda_faixa_ddm + o golden_nivel test_san01_reetiqueta_aberracao_itub4_like (DELETAR, não
      atualizar). test_arquetipo_roteamento.py: os fronteiriço (test_fronteirico_*, test_render_fronteirico_*)
      e test_arquetipo_roteamento.py::test_financeira_rim_destrava_vs_ddm_e_alimenta_veredito (golden_nivel,
      o ensemble RIM/DDM morre). test_ranking_freio.py: os test_divergencia_* que dependem só do ensemble
      (a função comparables.divergencia_entre_lentes é desconectada no Plano 06 — aqui só os que quebram
      por campo).
    - **REWRITE (invariante estrutural que SOBREVIVE — repontar à nova arquitetura, NÃO afrouxar):**
      test_vulc3_regressao.py::test_vulc3_cascata_estrutural_sobrevive — a banda deixou de ser min/max da
      matriz; RESCREVER para asseverar a região da MS (vmin==V×(1−ms), vmax==V×(1+ms)), preservando o
      intento estrutural (finito/never-raise/cascata sobrevive). test_vulc3_regressao.py::test_vulc3_
      normalizacao_doma_roe_e_dy — o invariante de normalização/clean-surplus (ROE domado) SOBREVIVE;
      repontar ao caminho RIM único (drop do assert de rótulo de motor, mantendo a asserção de domesticação
      do ROE/DY). test_arquetipo_roteamento.py::test_financeira_veredito_real_do_motor_nunca_evitar ->
      RESCREVER: financeira roteia RIM, veredito ∈ {SUBAVALIADA, NO INTERVALO, SOBREAVALIADA} (nunca
      "Evitar" — que sai no Plano 06), drop de banda_do_motor. test_arquetipo_roteamento.py::test_
      degradacao_um_ano_nao_levanta -> RESCREVER: o never-raise (SAN-06) SOBREVIVE; drop dos asserts de
      motor_pendente/banda/fronteirico, manter "analisar_acao não levanta".
    Cada DELETE remove função + linha da classificacao.yaml no MESMO diff (0 órfão). Cada REWRITE mantém a
    entrada válida (invariante/contrato). NUNCA afrouxar tolerância, xfail casual ou xfail→skip. Commits de
    teste sem tocar config.yaml.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` (suíte default, onda 3) 0 failed; `-m golden_nivel` sem CLASSIFICACAO ORFA; `xfail_estritos()` == 0 (doenças curadas nas Fases 10/12); só o skip do jackknife sobra
    - `grep -rc 'banda_do_motor\|contraponto_valor\|divergencia_ativa\|candidatos_intrinsecos\|veredito_range' tests/` soma 0 (nenhum teste ainda asseve campo deletado)
    - Os 2 invariantes RESCRITOS asseveram a NOVA arquitetura: test_vulc3_cascata_estrutural_sobrevive checa vmin==V×(1−ms)/vmax==V×(1+ms) (grep 'margem' / '1 -' no teste); test_vulc3_normalizacao_doma_roe_e_dy não asseve mais rótulo de motor
    - Nenhum golden_nivel ATUALIZADO (só REMOÇÃO das linhas dos deletados); nenhum assert afrouxado; nenhum xfail→skip novo
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -rc 'banda_do_motor\|contraponto_valor\|divergencia_ativa\|candidatos_intrinsecos\|veredito_range' tests/ 2>/dev/null | awk -F: '{s+=$2} END{print s+0}') -eq 0 && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>Testes de ensemble/dispatch DELETADOS (função+classificacao juntas); invariantes de cascata/normalização RESCRITOS contra a região da MS/RIM único; suíte default verde na onda 3; coleta sem órfão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CompanyData (dado curado, mas ainda com escalas absurdas residuais) → RIM único | O único caminho de valor precisa degradar, não quebrar, sob razão patológica |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Denial (crash) | _valor_rim / _derivar_insumo sobre dado faltante ou absurdo | mitigate | Never-raise (SAN-06): degrada para None; motores.rim já é never-raise; guardar sinais com is not None |
| T-13-06 | Elevation (bug oculto) | carve-out CONCESSAO_FINITA aplicando g de inflação | mitigate | g_terminal=None (fade-only nativo) conforme spike; nenhuma reintrodução de g_cap no terminal da concessão |
| T-13-07 | Tampering (overfit) | golden de nível atualizado em vez de deletado | mitigate | Golden que quebra é DELETADO (função+classificacao mesmo diff); invariantes sobreviventes RESCRITOS, não afrouxados |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` (default, onda 3) 0 failed; `-m golden_nivel` sem CLASSIFICACAO ORFA.
- Nenhum ramo de dispatch por nome de motor; `motores.rim` é o único chamado; `dcf_crescimento` deletado.
- `_guarda_san01`/`_guarda_faixa_ddm`/`_veredito_fronteirico`/campos de ensemble ausentes; nenhum teste asseve campo deletado.
- Invariantes de cascata/normalização RESCRITOS contra a região da MS/RIM único.
- `git diff config.yaml calibracao.lock.yaml` VAZIO (knob-cut é o Plano 05).
</verification>

<success_criteria>
Um único caminho RIM alimentado pela política do arquétipo, ensemble e guardas-cicatriz removidos, dcf
morto, carve-out da concessão aplicado; TODO teste que asseve campo/motor deletado foi DELETADO (baseline)
ou RESCRITO (invariante) contra a nova arquitetura — suíte default verde na onda 3, sem tocar Ke/g/config.
</success_criteria>

<output>
After completion, create `.planning/phases/13-motores-contrato-de-sa-da-eng/13-03-SUMMARY.md`
</output>
