---
phase: 13-motores-contrato-de-sa-da-eng
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/analista/report/report.py
  - src/analista/core/motores.py
  - tests/test_guardrails_ddm.py
  - tests/test_arquetipo_roteamento.py
  - tests/test_ranking_freio.py
  - tests/classificacao.yaml
autonomous: true
requirements: [ENG-01, ENG-02, ENG-03, ENG-04]
must_haves:
  truths:
    - "Existe UM único caminho de valor: `_intrinseco_por_motor` colapsou num caminho que sempre chama `motores.rim(...)` com o insumo derivado pela política do arquétipo"
    - "Nenhum ticker roteia para `ddm`/`seguradora`/`normalizado`/`dcf`/`nav` como motor primário"
    - "O ensemble morreu: `_guarda_san01`, `_guarda_faixa_ddm`, `_veredito_fronteirico`, `_hipotese_divergencia` e os campos de ensemble de `AnaliseAcao` foram REMOVIDOS (não portados)"
    - "`CONCESSAO_FINITA` usa modelo de ativo financeiro: o `g_terminal` NÃO recebe o g de inflação (carve-out declarado, conforme o spike do Plano 01)"
    - "`dcf_crescimento` foi DELETADO como motor (não substituído por FCFE); `lucro_normalizado`/`nav_contabil` sobrevivem só como derivadores de insumo"
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "Dispatch RIM único (_valor_rim/_derivar_insumo) + ensemble removido"
      contains: "motores.rim"
    - path: "src/analista/core/motores.py"
      provides: "dcf_crescimento removido como motor; rim é a única fórmula de valor"
  key_links:
    - from: "report._intrinseco_por_motor (colapsado)"
      to: "motores.rim"
      via: "chamada única com roe0/roe_terminal/g_terminal derivados pela política do arquétipo"
      pattern: "motores\\.rim"
    - from: "ARQUETIPO_ANCORA_ROE[a.arquetipo]"
      to: "_derivar_insumo"
      via: "política → (roe0, roe_terminal, g_terminal)"
      pattern: "ARQUETIPO_ANCORA_ROE|_derivar_insumo"
  anti_goals:
    - "NÃO 'consertar' o `dcf_crescimento` com FCFE (`lpa × payout`): vira DDM por teorema (Armadilha 2, WEGE3 0,58→0,26). Ele MORRE"
    - "NÃO aplicar o `g_cap` (que embute inflação) ao terminal de `CONCESSAO_FINITA`: double-count de IPCA sob ICPC 01 (Armadilha)"
    - "NÃO portar `_guarda_san01`/`_guarda_faixa_ddm`/ensemble 'por segurança': são cicatrizes do viés"
    - "NÃO recomputar `a.ke` (Fase 12) nem recalibrar `g_cap`/`g_T` (Fase 11): já prontos; tocar apaga o diagnóstico"
    - "NÃO validar o caso do livro (ITUB4=R$37,22): Fase 14. Nenhum assert com ticker+número-alvo"
    - "NÃO atualizar golden_nivel para o valor novo: golden que quebra é DELETADO (função + linha classificacao no mesmo diff)"
---

<objective>
Colapsar os 4 motores num **RIM único** (ENG-01) e **matar o ensemble** (ENG-02). `_intrinseco_por_motor`
deixa de ter 6 ramos (`rim`/`normalizado`/`dcf`/`nav`/`ddm` + rota `seguradora`) e passa a: (1) derivar
o insumo (`roe0`/`roe_terminal`/`g_terminal`/base de book) pela política `ARQUETIPO_ANCORA_ROE` do
arquétipo (Plano 02); (2) chamar **sempre** `motores.rim(...)` com `a.ke` (Fase 12) e `g_T` (Fase 11)
prontos. O `dcf_crescimento` é **deletado**; `lucro_normalizado`/`nav_contabil` sobrevivem só como
derivadores. `CONCESSAO_FINITA` recebe o carve-out (g terminal sem inflação, conforme o spike do Plano
01). Todas as guardas-cicatriz e o bloco de divergência/fronteiriço são **removidos**.

Purpose: a dispersão medida (0,81/0,63/0,63/0,48) era a assinatura dos bugs já curados nas Fases 9-12 —
não divergência de método. Uma fórmula, um caminho, erro limitado.
Output: dispatch RIM único em `report.py`; `motores.py` sem `dcf_crescimento`-motor; ensemble deletado;
goldens de nível do ensemble apagados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md
@.planning/spikes/eng-rim-104.md

@src/analista/report/report.py
@src/analista/core/motores.py
@src/analista/core/arquetipo.py

<interfaces>
<!-- Sites exatos (medidos), o executor NÃO precisa explorar -->
report.py: `AnaliseAcao` (:54-77; campos de ensemble a REMOVER: `motor_pendente` :58, `contraponto_valor`
  :66, `banda_do_motor` :67, `divergencia_ativa` :68, `divergencia_hipotese` :70, `arquetipo_incerto`
  :72, `candidatos_intrinsecos` :73, `veredito_range` :74); `_guarda_faixa_ddm` (:77-105, chamada :589);
  `_guarda_san01` (:108-182, chamada :706); `_roe_through_cycle` (:184 — a âncora through-cycle, MANTER);
  `_intrinseco_por_motor` (:201-327 — COLAPSAR); `_veredito_fronteirico` (:329-395, chamada :697 —
  REMOVER); dispatch `a.intrinseco_motor = _intrinseco_por_motor(...)` (:519); bloco ensemble
  motor×contraponto (:591-629 — a banda vira região da MS no Plano 04); `_hipotese_divergencia` (:874-890
  — REMOVER); render de divergência/incerto (:1071-1103 — REMOVER).
motores.py: `rim` (:66 — MANTER, única fórmula); `lucro_normalizado` (:149 → derivador de lpa_norm ou
  inlinado); `dcf_crescimento` (:161 → DELETAR como motor); `nav_contabil` (:195 → derivador de piso).
  Terminal do rim: `motores.py:128-129` libera terminal só com `g_terminal is not None` E `ke − g_terminal
  ≥ ke_g_spread_min`; com `g_terminal=None` ⇒ `vp_terminal=0.0` (fade-only nativo).
arquetipo.ANCORA (Plano 02): ARQUETIPO_ANCORA_ROE[chave] ∈ {"through_cycle","through_cycle_sem_g",
  "normalizado","atual_fade","nav_piso"}.
Insumos PRONTOS (não recomputar): `a.ke` (Fase 12); `g_cap = (1+cfg["macro"]["pi_ciclo"])*(1+cfg["ddm"]
  ["pib_real"])−1`; `g_T = max(0, min(ROE_T×retenção, g_cap))`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Colapsar o dispatch num RIM único alimentado pela política do arquétipo</name>
  <files>src/analista/report/report.py, src/analista/core/motores.py</files>
  <read_first>
    - src/analista/report/report.py (`_intrinseco_por_motor` :201-327 — todos os ramos atuais; ramo `rim` :255-268; ramo `normalizado` :285-309 — a derivação do lpa_norm deflacionado; `_roe_through_cycle` :184; dispatch :519)
    - src/analista/core/motores.py (`rim` :66-146, `lucro_normalizado` :149, `dcf_crescimento` :161, `nav_contabil` :195; terminal :128-129)
    - src/analista/core/arquetipo.py (ARQUETIPO_ANCORA_ROE do Plano 02)
    - .planning/spikes/eng-rim-104.md (a escolha do g_terminal do carve-out)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Pattern 2", §"Mapa de âncoras", §"Carve-out CONCESSAO_FINITA", §Code Examples)
  </read_first>
  <behavior>
    - Todo ticker com dado suficiente resolve `intrinseco_motor` finito e > 0 via `motores.rim` (ou None never-raise) — nenhum NaN/inf/exceção.
    - `a.motor`/rótulo de motor NUNCA é `ddm`/`seguradora`/`normalizado`/`dcf`/`nav` como motor primário — sempre o RIM (ou o nome da política de âncora).
    - `through_cycle`: `roe0=c.roe_valuation()`, `roe_terminal=_roe_through_cycle(...)` (baseline; bancos/financeira não regridem vs o ramo `rim` atual).
    - `normalizado` (cíclica): `roe0 ≈ lpa_norm_deflacionado / vpa0` (série deflacionada por IPCA, PRIM-04), `roe_terminal=_roe_through_cycle`.
    - `atual_fade` (crescimento): `roe0=c.roe_qualidade_atual()`, fade sobre `n_fade` (nativo do rim); `dcf_crescimento` não é chamado.
    - `through_cycle_sem_g` (CONCESSAO_FINITA): `g_terminal` = a escolha do spike (`None` fade-only recomendado) — o g de inflação NÃO entra.
  </behavior>
  <action>
    Reescrever `_intrinseco_por_motor` (renomear para `_valor_rim` se ajudar a leitura) para: derivar o
    insumo por um helper `_derivar_insumo(politica, c, cfg, g_cap, rim_cfg)` que devolve
    `(roe0, roe_terminal, g_terminal, base_book)` conforme `ARQUETIPO_ANCORA_ROE[a.arquetipo]` (mapa do
    §"Mapa de âncoras"), e chamar SEMPRE `motores.rim(vpa0=lentes.vpa(PL, num_acoes), roe0=roe0,
    ke=a.ke, retencao=1−payout, n=rim_cfg["n_fade"], excesso_sustentavel=rim_cfg["excesso_sustentavel"],
    g_terminal=g_terminal, ke_g_spread_min=rim_cfg["ke_g_spread_min"], roe_terminal=roe_terminal)`.
    Para `CONCESSAO_FINITA` passar `g_terminal` conforme a decisão do spike (`None` fade-only) — o RIM já
    zera o terminal nativamente (`motores.py:128-129`). Remover a rota `seguradora` e o passthrough `ddm`
    do dispatch (todos passam pelo RIM). Em `motores.py`: DELETAR `dcf_crescimento` (NÃO substituir por
    FCFE); manter `rim`; converter `lucro_normalizado` em helper de derivação do `lpa_norm` (ou inlinar
    no `_derivar_insumo`) e `nav_contabil` em derivador de piso patrimonial. Continuar lendo
    `cfg["motores"]["ciclica"]["anos_media"]` para a janela de normalização (o rename do knob é o Plano
    05). NÃO tocar `a.ke`/`g_cap`/`g_T`.
  </action>
  <acceptance_criteria>
    - `grep -nE "'seguradora'|\"seguradora\"|motor == .ddm|== .normalizado.|== .dcf.|== .nav." src/analista/report/report.py` no dispatch == 0 (nenhum ramo por nome de motor sobrevive)
    - `grep -c 'def dcf_crescimento' src/analista/core/motores.py` == 0 (motor deletado)
    - `motores.rim` é chamado no caminho único de `report.py` (grep confirma) e `a.ke`/`g_cap` não são recomputados (nenhuma reconstrução de Ke via `capm.ke_local` dentro do dispatch)
    - `.venv/bin/python -m pytest -k 'not golden_nivel' -q` roda a coleta sem `CLASSIFICACAO ORFA` (falhas de testes de ensemble são esperadas até as Tasks 2-3; a suíte fecha no fim do plano)
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'def dcf_crescimento' src/analista/core/motores.py) -eq 0 && grep -q 'motores.rim' src/analista/report/report.py</automated>
  </verify>
  <done>Um só caminho RIM alimentado pela política do arquétipo; dcf morto; carve-out da concessão aplicado; Ke/g intocados.</done>
</task>

<task type="auto">
  <name>Task 2: Remover ensemble, guardas-cicatriz e render de divergência (não portar)</name>
  <files>src/analista/report/report.py</files>
  <read_first>
    - src/analista/report/report.py (`AnaliseAcao` :54-77 campos de ensemble; `_guarda_faixa_ddm` :77-105 + chamada :589; `_guarda_san01` :108-182 + chamada :706; bloco ensemble motor×contraponto :591-629; `_veredito_fronteirico` :329-395 + chamada :697; `_hipotese_divergencia` :874-890; render divergência/incerto :1071-1103)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Remoção do ensemble" — inventário dos sites)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md (D-04)
  </read_first>
  <action>
    DELETAR (não portar): `_guarda_faixa_ddm` e sua chamada (:589); `_guarda_san01` e sua chamada (:706);
    `_veredito_fronteirico` e sua chamada (:697); `_hipotese_divergencia` (+ `_HIPOTESE_DIVERGENCIA` se
    existir); os campos de ensemble de `AnaliseAcao` (`motor_pendente`, `contraponto_valor`,
    `banda_do_motor`, `divergencia_ativa/razao/hipotese`, `arquetipo_incerto`, `candidatos_intrinsecos`,
    `veredito_range`); o bloco de render de "Classificação incerta"/"Bandeira de divergência"
    (:1071-1103). O bloco ensemble motor×contraponto (:591-629) que hoje computa `vmin/vmax` por
    min/max(motor, contraponto) sai — a região de valor vira `[V×(1−MS), V×(1+MS)]` no Plano 04; nesta
    task deixar `vmin/vmax` derivando da forma simétrica sobre `intrinseco_motor` (o fallback simétrico
    que já existe em :608-610 vira o caminho, para o pipeline não quebrar entre planos). NÃO deletar
    `cfg["veredito"]["san01"]` do config.yaml aqui (é o Plano 05, config-only, evita o hook BLIND-05).
    NÃO tocar as flags de risco do veredito (payout>100%, DY>15%, div em prejuízo → VERIFICAR,
    :645-657) — são do livro (Cap. 6), sobrevivem.
  </action>
  <acceptance_criteria>
    - `grep -cE 'def _guarda_san01|def _guarda_faixa_ddm|def _veredito_fronteirico|def _hipotese_divergencia' src/analista/report/report.py` == 0
    - `grep -cE 'banda_do_motor|divergencia_ativa|contraponto_valor|candidatos_intrinsecos|veredito_range|arquetipo_incerto|motor_pendente' src/analista/report/report.py` == 0
    - As flags de risco do veredito (VERIFICAR por payout>100%/DY alto/div em prejuízo) permanecem (grep confirma o bloco de risco)
    - `cfg["veredito"]["san01"]` NÃO é removido do config.yaml neste plano (git diff config.yaml VAZIO neste plano)
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -cE 'def _guarda_san01|def _guarda_faixa_ddm|def _veredito_fronteirico|banda_do_motor|divergencia_ativa|candidatos_intrinsecos' src/analista/report/report.py) -eq 0 && test -z "$(git diff config.yaml)"</automated>
  </verify>
  <done>Ensemble/guardas/divergência/fronteiriço removidos; flags de risco do livro intactas; config.yaml não tocado.</done>
</task>

<task type="auto">
  <name>Task 3: Deletar goldens de nível e testes de ensemble mortos (função + classificacao no mesmo diff)</name>
  <files>tests/test_guardrails_ddm.py, tests/test_arquetipo_roteamento.py, tests/test_ranking_freio.py, tests/classificacao.yaml</files>
  <read_first>
    - tests/classificacao.yaml (localizar as entradas dos testes abaixo; regra de completude na coleta)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Remoção do ensemble" tabela "Testes que morrem/mudam"; §Pitfall 5)
    - CLAUDE.md (golden de nível que quebra é DELETADO, não atualizado; nunca xfail casual / xfail→skip / afrouxar tolerância)
  </read_first>
  <action>
    DELETAR (função + linha da `classificacao.yaml` no MESMO diff, zero órfão): os goldens de nível
    `test_guardrails_ddm.py::test_san01_reetiqueta_aberracao_itub4_like` e
    `test_arquetipo_roteamento.py::test_financeira_rim_destrava_vs_ddm_e_alimenta_veredito` (o ensemble
    RIM/DDM morre — é o golden de nível que esta fase apaga; NÃO atualizar o número); os 5 testes de
    `_guarda_faixa_ddm` em `test_guardrails_ddm.py`; os testes de `_veredito_fronteirico`/render fronteiriço
    em `test_arquetipo_roteamento.py`; os testes de `divergencia_entre_lentes` em `test_ranking_freio.py`
    QUE dependem só do ensemble (confirmar por leitura antes de deletar; a função `comparables.
    divergencia_entre_lentes` em si é desconectada no Plano 06). Para cada deleção, remover a entrada
    correspondente em `classificacao.yaml`. Este commit toca `tests/` mas NÃO `config.yaml` → não dispara
    o hook BLIND-05. NUNCA afrouxar tolerância, marcar xfail casual ou trocar xfail por skip.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` (suíte default) fecha com 0 failed e `-m golden_nivel` sem `CLASSIFICACAO ORFA`; `xfail_estritos()` continua 0 (as duas doenças já curadas nas Fases 10/12)
    - `grep -c 'test_san01_reetiqueta_aberracao_itub4_like\|test_financeira_rim_destrava' tests/` (recursivo) == 0 E as mesmas chaves ausentes de classificacao.yaml (0 órfão dos dois lados)
    - Nenhum golden_nivel foi ATUALIZADO (o diff de classificacao.yaml só REMOVE entradas dos testes deletados; não altera valor de assert de nível vivo)
    - `git diff config.yaml` VAZIO neste plano (commits de teste separados de config — hook BLIND-05)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <done>Goldens de nível do ensemble DELETADOS (função + linha juntas); suíte default verde; coleta sem órfão; config intocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CompanyData (dado curado, mas ainda com escalas absurdas residuais) → RIM único | O único caminho de valor precisa degradar, não quebrar, sob razão patológica |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Denial (crash) | _valor_rim / _derivar_insumo sobre dado faltante ou absurdo | mitigate | Never-raise (SAN-06): degrada para None; `motores.rim` já é never-raise; guardar sinais com `is not None` |
| T-13-06 | Elevation (bug oculto) | carve-out CONCESSAO_FINITA aplicando g de inflação | mitigate | g_terminal=None (fade-only nativo) conforme spike; nenhuma reintrodução de g_cap no terminal da concessão |
| T-13-07 | Tampering (overfit) | golden de nível atualizado em vez de deletado | mitigate | Golden que quebra é DELETADO (função+classificacao mesmo diff); nenhum valor de nível atualizado |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` (default) verde; `-m golden_nivel` sem `CLASSIFICACAO ORFA`.
- Nenhum ramo de dispatch por nome de motor; `motores.rim` é o único chamado; `dcf_crescimento` deletado.
- `_guarda_san01`/`_guarda_faixa_ddm`/`_veredito_fronteirico`/campos de ensemble ausentes.
- `git diff config.yaml calibracao.lock.yaml` VAZIO (fronteira: knob-cut é o Plano 05).
</verification>

<success_criteria>
Um único caminho RIM alimentado pela política do arquétipo, ensemble e guardas-cicatriz removidos, dcf
morto, carve-out da concessão aplicado, goldens de nível do ensemble deletados — suíte default verde, sem
tocar Ke/g/config.
</success_criteria>

<output>
After completion, create `.planning/phases/13-motores-contrato-de-sa-da-eng/13-03-SUMMARY.md`
</output>
