---
phase: 13-motores-contrato-de-sa-da-eng
plan: 07
type: execute
wave: 6
depends_on: [04, 05, 06]
files_modified:
  - tests/test_eng_validacao.py
  - tests/test_cli_rank_consistencia.py
  - tests/classificacao.yaml
autonomous: true
requirements: [ENG-01, ENG-08, ENG-09]
must_haves:
  truths:
    - "A correção do colapso é PROVADA POR EXECUÇÃO da regressão dos 104 tickers ao vivo (oráculo herdado de KE-04), não por 'suíte verde' genérica"
    - "Todo ticker com dado suficiente resolve `intrinseco_motor` finito e > 0 (ou None never-raise) — zero NaN/inf/exceção"
    - "Nenhuma explosão: V < ~50× preço por distribuição (sem nomear ticker)"
    - "Razão P/B sã por construção sobre a cesta real: `payout_T ∈ (0,1)` e `P/B justo ∈ (0,6)` — o guard D-10a provado por execução"
    - "Um só caminho: nenhum ticker roteia para ddm/seguradora/normalizado/dcf/nav como motor primário — todos passam pelo RIM único"
    - "Cross-menu (WR-03): analyze e rank produzem o mesmo intrinseco_motor/a.ke/matriz para a mesma ação"
  artifacts:
    - path: "tests/test_eng_validacao.py"
      provides: "Regressão dos 104 pós-colapso (invariante, BLIND-04a-safe, sem ticker)"
      contains: "CAMINHO_SNAPSHOT_LIMPO"
  key_links:
    - from: "tests/test_eng_validacao.py"
      to: "report.analisar_acao sobre os 104"
      via: "hs.CAMINHO_SNAPSHOT_LIMPO + β setorial carimbado"
      pattern: "analisar_acao|CAMINHO_SNAPSHOT_LIMPO"
  anti_goals:
    - "NÃO validar o caso do livro (ITUB4 = R$ 37,22): Fase 14. A prova é por DISTRIBUIÇÃO dos 104, nenhum ticker nomeado num assert"
    - "NÃO usar V/preço como métrica de acerto (espelho do mercado); aqui V/preço é só o TETO de sanidade (não explosão), não alvo"
    - "NÃO introduzir clamp/guard novo para 'passar' a regressão — se um V explode, o bug é ROE_T/spread; investigar, não clampar"
    - "NÃO criar golden_nivel novo — os asserts são invariantes/contrato por distribuição"
    - "NÃO afrouxar tolerância nem trocar assert por skip para a suíte ficar verde"
---

<objective>
Provar o colapso **por execução** — o oráculo herdado de KE-04 (`test_regressao_104_sem_explosao`),
agora sobre o RIM único. Rodar `report.analisar_acao` sobre os 104 reais (`hs.CAMINHO_SNAPSHOT_LIMPO`,
β setorial carimbado, Ke offline idêntico ao app) e asseverar, por distribuição e sem nomear ticker:
(1) intrínseco finito > 0 ou None (never-raise); (2) nenhuma explosão (V < ~50× preço); (3) razão P/B
sã (`payout_T ∈ (0,1)`, `P/B justo ∈ (0,6)`) — o guard D-10a provado sobre a cesta real; (4) um só
caminho (nenhum motor primário ddm/seguradora/normalizado/dcf/nav); (5) cross-menu (WR-03): analyze e
rank batem.

Purpose: "suíte verde" não prova blindagem — rodar a regressão prova (memória
`guardrails-devem-ser-provados-por-execucao`). É o gate de fechamento da fase.
Output: `tests/test_eng_validacao.py` (invariante, BLIND-04a-safe) + `test_cli_rank_consistencia`
adaptado ao caminho novo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md

@tests/test_ke_validacao.py
@tests/test_cli_rank_consistencia.py
@tests/helpers_sanidade.py
@src/analista/report/report.py

<interfaces>
<!-- O padrão do oráculo herdado (KE-04) a espelhar -->
tests/test_ke_validacao.py::test_regressao_104_sem_explosao — carrega hs.CAMINHO_SNAPSHOT_LIMPO,
  carimba β setorial (macro.carimbar_beta_setorial), roda report.analisar_acao offline nos 104; assere
  todo Ke>=Ke_min>g_cap, intrinseco_motor finito>0, V<50× preço (max medido 4,7×), None aceitável.
Campos pós-fase: a.intrinseco_motor (RIM único); a.motor/rótulo (deve ser sempre RIM/política, nunca
  ddm/seguradora/normalizado/dcf/nav); os campos da ponte P/B (pb_justo, payout_T) do Plano 04.
test_cli_rank_consistencia.py — assere beta_setorial E a.ke idênticos entre analyze e rank (D-06 da
  Fase 12); adaptar para também casar intrinseco_motor/matriz no caminho novo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regressão dos 104 pós-colapso (finito, sem explosão, P/B sã, caminho único)</name>
  <files>tests/test_eng_validacao.py, tests/classificacao.yaml</files>
  <read_first>
    - tests/test_ke_validacao.py (o padrão a espelhar: loader, carimbo de β, laço nos 104, asserts por distribuição/never-raise, BLIND-04a-safe)
    - tests/helpers_sanidade.py (loader offline dos 104)
    - src/analista/report/report.py (a.intrinseco_motor do RIM único; a.motor/rótulo; campos da ponte P/B pb_justo/payout_T)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Validation Architecture" — os 5 invariantes; §Pitfall 6 CGRA4 ortogonal; §"Fronteira dura com a Fase 14")
  </read_first>
  <action>
    Criar `tests/test_eng_validacao.py` (classificação `invariante`, BLIND-04a-safe — SEM nomear ticker,
    SEM asserir número-alvo do livro), espelhando `test_ke_validacao.py`: carregar
    `hs.CAMINHO_SNAPSHOT_LIMPO`, carimbar β setorial, rodar `report.analisar_acao` offline nos 104 e
    asseverar por distribuição: (1) todo `intrinseco_motor` finito e > 0 OU None (never-raise); zero
    NaN/inf/exceção; (2) nenhuma explosão — `V < 50× preço` (teto de sanidade, sem nomear ticker); (3)
    para todo ticker com razão computável, `payout_T ∈ (0,1)` e `P/B justo ∈ (0,6)` (o guard D-10a
    provado por execução; ticker fora da faixa deve ter sido DEGRADADO/sinalizado, não ter quebrado); (4)
    caminho único — `a.motor`/rótulo nunca é ddm/seguradora/normalizado/dcf/nav como motor primário
    (todos passam pelo RIM). Registrar a entrada em `classificacao.yaml` (`invariante`) no MESMO diff.
    Se algum ticker explodir/rotear errado, o teste FALHA — investigar ROE_T/spread (não clampar).
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -k 'eng_validacao' -q` verde: os 4 invariantes passam sobre os 104 reais (finito/None, sem explosão, P/B sã, caminho único)
    - O teste roda `report.analisar_acao` sobre `hs.CAMINHO_SNAPSHOT_LIMPO` (grep confirma o snapshot e a função)
    - Nenhum ticker nomeado num assert e nenhum número-alvo do livro (`grep -iE '37[.,]22|ITUB4.*3[5-9]' tests/test_eng_validacao.py` == 0) — BLIND-04a-safe
    - Classificação `invariante` registrada; coleta sem CLASSIFICACAO ORFA; nenhum golden_nivel novo
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -k 'eng_validacao' -q && ! grep -qiE '37[.,]22' tests/test_eng_validacao.py</automated>
  </verify>
  <done>A regressão dos 104 prova por execução: finito/sem explosão/P/B sã/caminho único, sem nomear ticker nem validar o livro.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-menu (WR-03) — analyze e rank batem no caminho novo</name>
  <files>tests/test_cli_rank_consistencia.py, tests/classificacao.yaml</files>
  <read_first>
    - tests/test_cli_rank_consistencia.py (assere beta_setorial + a.ke idênticos entre analyze e rank, D-06)
    - src/analista/cli.py (o caminho rank rebaixado do Plano 06)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Validation Architecture" ponto 5 — cross-menu)
  </read_first>
  <action>
    Adaptar `test_cli_rank_consistencia.py` ao caminho novo: além de `beta_setorial`/`a.ke` idênticos
    entre `analyze` e `rank`, casar `intrinseco_motor` (e a matriz Ke×g quando exposta) para a mesma ação
    — a fonte única cross-modo (WR-03) sob o RIM único. Como o Ranking foi rebaixado (Plano 06, sem
    preço-alvo/veredito), remover do teste qualquer asserção sobre colunas que saíram e manter a
    igualdade dos números carimbados. Casar `classificacao.yaml` no MESMO diff (contrato). SEM nomear
    ticker com número-alvo.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -k 'cli_rank_consistencia' -q` verde: analyze e rank batem em beta_setorial/a.ke/intrinseco_motor
    - O teste não assere colunas removidas do Ranking (preço-alvo/upside/veredito) — só os números carimbados
    - `.venv/bin/python -m pytest -q` (suíte default completa) fecha 0 failed; `-m golden_nivel` sem CLASSIFICACAO ORFA; `xfail_estritos()` == 0; só o skip do jackknife permanece (Fase 14)
    - Coleta limpa; nenhum golden_nivel novo
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -k 'cli_rank_consistencia' -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>Cross-menu provado sob o RIM único; suíte default completa 0 failed; só o skip do jackknife sobra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot dos 104 (com sujeira residual, ex.: 921×) → RIM único ao vivo | A prova de robustez do contrato never-raise/degrade-not-crash vive aqui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-17 | Denial (crash) | analisar_acao sobre os 104 (ratios absurdos) | mitigate | Assert de never-raise: None aceitável, zero exceção; V<50× por distribuição; guard runtime degrada (não levanta) |
| T-13-18 | Tampering (overfit oculto) | assert nomeando ticker / número do livro | mitigate | BLIND-04a: prova por distribuição, sem ticker; grep barra 37,22; hook + test de justificativa a montante |
| T-13-19 | Elevation (clamp escondido) | 'consertar' a regressão com guard novo | mitigate | Se um V explode, o teste FALHA e o bug é ROE_T/spread — investigar, nunca clampar (fronteira KE-04) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -k 'eng_validacao or cli_rank_consistencia' -q` verde.
- `.venv/bin/python -m pytest -q` (default completa) 0 failed; `-m golden_nivel` sem CLASSIFICACAO ORFA; xfail_estritos()==0; 1 skipped (jackknife, Fase 14).
- Regressão dos 104: finito/None, sem explosão, P/B sã, caminho único, cross-menu — sem nomear ticker, sem validar o caso do livro.
</verification>

<success_criteria>
O colapso está provado por EXECUÇÃO da regressão dos 104 (finito, sem explosão, razão P/B sã, um só
caminho, cross-menu consistente), com a suíte default completa verde — o motor está pronto para a
validação soberana da Fase 14, sem que esta fase tenha tocado no caso do livro.
</success_criteria>

<output>
After completion, create `.planning/phases/13-motores-contrato-de-sa-da-eng/13-07-SUMMARY.md`
</output>
