---
phase: 13-motores-contrato-de-sa-da-eng
plan: 05
type: execute
wave: 5
depends_on: [03, 04]
files_modified:
  - config.yaml
  - calibracao.lock.yaml
  - src/analista/report/report.py
autonomous: true
requirements: [ENG-06, ENG-10]
must_haves:
  truths:
    - "O bloco `motores:` do config.yaml foi de 7 folhas para ≤5, e a contagem é o critério de verificação (regra dura C)"
    - "Os sub-blocos `motores.ciclica` e `motores.crescimento` colapsaram: `ciclica.anos_media` virou política de input do RIM (movida), `ciclica.winsor` e `crescimento.n_anos_explicito` DELETADAS"
    - "Toda folha que saiu do config saiu da partição do `calibracao.lock.yaml` no MESMO commit; `test_knobs_batem_com_o_lock` reflete"
    - "O orçamento continua em EXATAMENTE 3 graus de liberdade (ERP, n_fade, PIB_real) — nenhum grau novo"
    - "O default de `veredito.margem_seguranca` mudou de 0.15 para simétrico 5-10% (co-change config+lock no user_control), realizando o ENG-06"
    - "O bloco morto `veredito.san01` do config foi removido (o `_guarda_san01` já morreu no Plano 03)"
  artifacts:
    - path: "config.yaml"
      provides: "Bloco motores trimado (≤5 folhas) + margem_seguranca em 5-10% + san01 removido"
    - path: "calibracao.lock.yaml"
      provides: "Partição reescrita: escopo 26→24 folhas (motores 5), congelados atualizados, 3 graus intactos"
  key_links:
    - from: "config.yaml motores:"
      to: "calibracao.lock.yaml partição"
      via: "co-change no mesmo commit (folhas do escopo)"
      pattern: "motores"
    - from: "report._derivar_insumo (cíclica)"
      to: "cfg['motores']['rim']['anos_ciclica']"
      via: "consumidor da knob movida, no mesmo diff da mudança de config"
      pattern: "anos_ciclica|anos_media"
  anti_goals:
    - "NÃO introduzir um 4º grau de liberdade — o orçamento é 3 (ERP, n_fade, PIB_real); `n_fade` DEVE sobreviver"
    - "NÃO editar `tests/` neste plano (config+lock+report não disparam o hook BLIND-05; misturar tests/classificacao.yaml aqui dispara)"
    - "NÃO mencionar ticker na justificativa do knob (trailer `Knob-Change-Justification:` + teste `-k justificativa` + hook commit-msg)"
    - "NÃO calibrar a MS até 'ficar bonito': o default 5-10% é a regra do livro (MS simétrica do usuário), não ajuste contra dispersão/preço/compra (Armadilha 4)"
    - "NÃO recalibrar `g_cap`/ERP/n_fade — o corte é de folhas mortas dos ex-motores, não de nível de knob vivo"
---

<objective>
Executar o **corte contado de knobs** (ENG-10, regra dura C): o bloco `motores:` do `config.yaml` vai de
**7 folhas para ≤5**, com o `calibracao.lock.yaml` reescrito **no mesmo commit** e o **orçamento intacto
em 3 graus** (ERP, n_fade, PIB_real). Como `dcf_crescimento`/`lucro_normalizado` deixaram de ser motores
(Plano 03), os sub-blocos `motores.ciclica`/`motores.crescimento` colapsam. E realizar o **ENG-06**: o
default de `veredito.margem_seguranca` passa de 0.15 para **simétrico 5-10%** (co-change config+lock no
`user_control`). Remover o bloco morto `veredito.san01`.

Purpose: sem número contável, a deleção não acontece (post-mortem do v2.3). Superfície de knob menor,
orçamento provado em 3 graus.
Output: config+lock coesos (knob-cut + MS default), com o único consumidor da knob movida
(`report._derivar_insumo` da cíclica) no mesmo diff — sem tocar `tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md
@CLAUDE.md

@config.yaml
@calibracao.lock.yaml
@src/analista/report/report.py
@.githooks/commit-msg

<interfaces>
<!-- Estado medido do bloco motores (config.yaml:245-277) — 7 folhas hoje -->
motores.rim.n_fade                    ← GRAU (n_fade) — NÃO pode sair
motores.rim.excesso_sustentavel       ← congelado — MANTER
motores.rim.ke_g_spread_min           ← congelado — MANTER
motores.rim.roe_terminal_stat         ← congelado — MANTER
motores.ciclica.anos_media            ← MOVER para motores.rim.anos_ciclica (política de input do RIM)
motores.ciclica.winsor                ← DELETAR (inerte desde PRIM-02)
motores.crescimento.n_anos_explicito  ← DELETAR (dcf morreu; RIM usa n_fade)
Resultado alvo: motores: 7 → 5 folhas.
Lock (calibracao.lock.yaml): escopo "26 folhas (motores 7 ...)" → "24 folhas (motores 5 ...)" nos 3
  lugares (:34/header/partição); congelados :138-155 removem winsor + n_anos_explicito e renomeiam
  anos_media; graus_de_liberdade :60-... INTOCADO (ERP, n_fade :74, PIB_real).
MS: veredito.margem_seguranca (config.yaml:118, 0.15) declarada no user_control do lock
  (calibracao.lock.yaml:104-119, "ENG-06 (Fase 13) — vira controle do usuário").
Testes de orçamento (NÃO editar; devem refletir): test_orcamento_de_knobs_e_exatamente_3
  (test_blindagem_orcamento.py:44 — partição folhas==graus|congelados), test_knobs_batem_com_o_lock (:119).
Dead config: veredito.san01 (config.yaml:126-129) — _guarda_san01 morto no Plano 03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Knob-cut motores 7→5 (config + lock + consumidor da knob movida) — commit sancionado coeso</name>
  <files>config.yaml, calibracao.lock.yaml, src/analista/report/report.py</files>
  <read_first>
    - config.yaml (bloco `motores:` :245-277)
    - calibracao.lock.yaml (escopo :34, graus :60, congelados :127-156)
    - src/analista/report/report.py (o consumidor da cíclica: hoje lê `cfg["motores"]["ciclica"]["anos_media"]`)
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Corte contado de knobs" — set recomendado, reescrita coesa do lock; §"Classificação de testes + hook" — config+lock não dispara o hook)
    - CLAUDE.md (justificativa de knob nunca menciona ticker; knob de valuation muda COM o lock no mesmo diff)
  </read_first>
  <action>
    No MESMO commit: (1) `config.yaml` bloco `motores:` — MANTER `rim.{n_fade, excesso_sustentavel,
    ke_g_spread_min, roe_terminal_stat}`; MOVER `ciclica.anos_media` → `motores.rim.anos_ciclica`
    (política de input do ROE-âncora da cíclica); DELETAR `ciclica.winsor` e `crescimento.n_anos_explicito`;
    remover os sub-blocos `motores.ciclica` e `motores.crescimento` inteiros. Resultado: 5 folhas.
    (2) `calibracao.lock.yaml` — atualizar a contagem "26 folhas (motores 7 ...)" → "24 folhas (motores 5
    ...)" nos 3 lugares (escopo/header/partição); em `congelados`, remover `motores.ciclica.winsor` e
    `motores.crescimento.n_anos_explicito` e renomear `motores.ciclica.anos_media` → `motores.rim.anos_ciclica`;
    `graus_de_liberdade` INTOCADO (ERP, n_fade, PIB_real). (3) `report._derivar_insumo` (cíclica) passa a
    ler `cfg["motores"]["rim"]["anos_ciclica"]` (a knob movida) — no MESMO diff, para não haver estado
    quebrado. Commit com trailer `Knob-Change-Justification:` de razão econômica **sem ticker**
    (colapso dos ex-motores em política de input; superfície de knob menor). Este commit toca config+lock+
    report.py, NÃO `tests/` → passa o hook sem `--no-verify`. Garantir `git config core.hooksPath .githooks`.
  </action>
  <acceptance_criteria>
    - O bloco `motores:` do config.yaml tem ≤5 folhas CONTADAS (contar as folhas escalares sob `motores:`; ignorar linhas de comentário — `grep -vE '^\s*#' | grep -cE '^\s{4,}\w+:'` sob o bloco == 5) e os sub-blocos `motores.ciclica`/`motores.crescimento` não existem mais
    - `.venv/bin/python -m pytest -k 'orcamento or knobs_batem_com_o_lock' -q` verde (partição consistente; 3 graus; nenhuma folha órfã dos dois lados)
    - `n_fade` sobrevive (grau) e `graus_de_liberdade` do lock está intocado (`git diff calibracao.lock.yaml` não altera o bloco de graus)
    - `report._derivar_insumo` lê `motores.rim.anos_ciclica` (grep) e a suíte default `pytest -q` fica 0 failed (o consumidor migrou no mesmo diff)
    - Commit sem `--no-verify`; hook commit-msg passou; `.venv/bin/python -m pytest -k justificativa -q` verde (trailer sem ticker)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import yaml,sys; m=yaml.safe_load(open('config.yaml'))['motores']; n=sum(len(v) if isinstance(v,dict) else 1 for v in m.values()); print('motores leaves:',n); sys.exit(0 if n<=5 else 1)" && .venv/bin/python -m pytest -k 'orcamento or knobs_batem_com_o_lock or justificativa' -q && test -z "$(git diff tests/)"</automated>
  </verify>
  <done>motores: 7→5 contado, lock reescrito no mesmo commit, 3 graus intactos, consumidor migrado, trailer sem ticker, hook verde.</done>
</task>

<task type="auto">
  <name>Task 2: MS default 5-10% (co-change config+lock) + remover config morto veredito.san01</name>
  <files>config.yaml, calibracao.lock.yaml</files>
  <read_first>
    - config.yaml (`veredito.margem_seguranca` :118 = 0.15; `veredito.san01` :126-129)
    - calibracao.lock.yaml (`user_control` :104-119 — a anotação "ENG-06 (Fase 13)")
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Margem de segurança" — default 0.05/0.10, co-change sancionado; o livro usa ±5% no ITUB4)
    - .planning/REQUIREMENTS.md (ENG-06: MS simétrica, default 5-10%, nunca calibrada)
  </read_first>
  <action>
    (1) Mudar o default de `veredito.margem_seguranca` de 0.15 para dentro de **5-10%** (recomendado 0.05
    ou 0.10; o livro usa ±5% no caso ITUB4) — co-change no `user_control` do `calibracao.lock.yaml` (a
    folha mora lá), MESMO commit. Justificativa: fidelidade ao livro (MS simétrica escolhida pelo usuário,
    Cap. 17), **não** ajuste contra dispersão/preço/compra. (2) Remover o bloco morto `veredito.san01`
    (config.yaml:126-129) — o `_guarda_san01` foi deletado no Plano 03; `veredito` está fora do escopo do
    lock (exceto `margem_seguranca` no user_control), então a remoção do `san01` não mexe na partição do
    lock. Commit com trailer `Knob-Change-Justification:` **sem ticker**; toca config+lock, NÃO `tests/`.
  </action>
  <acceptance_criteria>
    - `veredito.margem_seguranca` está em [0.05, 0.10] no config.yaml E o valor bate no `user_control` do lock (grep confirma o mesmo número nos dois arquivos)
    - `grep -c 'san01' config.yaml` == 0 (bloco morto removido)
    - `.venv/bin/python -m pytest -q` (default) 0 failed; `-k 'orcamento or knobs_batem_com_o_lock'` verde (partição intacta; MS segue em user_control, fora do orçamento de 3 graus)
    - Commit sem `--no-verify`; trailer sem ticker; `git diff tests/` VAZIO neste plano
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'san01' config.yaml) -eq 0 && .venv/bin/python -m pytest -k 'orcamento or knobs_batem_com_o_lock' -q && test -z "$(git diff tests/)"</automated>
  </verify>
  <done>MS default em 5-10% (co-change config+lock), san01 morto removido, orçamento intacto, sem tocar tests, sem ticker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edição de knob (config.yaml/lock) → orçamento de 3 graus | Uma folha órfã ou um 4º grau escondido corrompe o orçamento (a assinatura do overfit do v2.3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Tampering (overfit) | commit de knob com ticker na justificativa | mitigate | Trailer `Knob-Change-Justification:` sem ticker; hook `.githooks/commit-msg` + `test_nenhuma_justificativa_de_knob_menciona_ticker` |
| T-13-12 | Elevation (4º grau) | remoção/renome de folhas do lock | mitigate | `test_orcamento_de_knobs_e_exatamente_3` (partição folhas==graus|congelados) reflete no mesmo diff; n_fade preservado |
| T-13-13 | Tampering (co-change escondido) | config.yaml + tests no mesmo commit | mitigate | Plano toca só config+lock+report; nenhuma edição em `tests/` (não dispara o hook; separação garante o diff revisável) |
</threat_model>

<verification>
- Bloco `motores:` com ≤5 folhas contadas; `motores.ciclica`/`motores.crescimento` inexistentes.
- `pytest -k 'orcamento or knobs_batem_com_o_lock or justificativa' -q` verde; suíte default 0 failed.
- 3 graus intactos (ERP, n_fade, PIB_real); MS em 5-10% (co-change user_control); `veredito.san01` removido.
- Commits sancionados sem `--no-verify`, trailer sem ticker; `git diff tests/` VAZIO.
</verification>

<success_criteria>
`motores:` cortado de 7 para ≤5 folhas CONTADAS, lock reescrito no mesmo commit, orçamento provado em 3
graus, MS default em 5-10% — tudo por commits sancionados sem tocar `tests/` e sem nomear ticker.
</success_criteria>

<output>
After completion, create `.planning/phases/13-motores-contrato-de-sa-da-eng/13-05-SUMMARY.md`
</output>
