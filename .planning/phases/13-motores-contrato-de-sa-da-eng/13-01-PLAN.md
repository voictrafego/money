---
phase: 13-motores-contrato-de-sa-da-eng
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/spike_eng_rim_104.py
  - .planning/spikes/eng-rim-104.md
autonomous: true
requirements: [ENG-01, ENG-04]
must_haves:
  truths:
    - "A distribuição de V/preço, P/B justo e payout_T dos tickers regulada e cíclica sob o RIM único proposto está MEDIDA sobre os 104, não assumida"
    - "A escolha do g_terminal do carve-out CONCESSAO_FINITA (None vs PIB_real) está decidida por medição e registrada"
    - "Nenhum arquivo de produção foi tocado (spike puro)"
  artifacts:
    - path: ".planning/spikes/eng-rim-104.md"
      provides: "Medição do RIM único sobre regulada/cíclica + decisão do carve-out"
      contains: "g_terminal"
    - path: "scripts/spike_eng_rim_104.py"
      provides: "Script offline throwaway que aplica o mapa de âncoras proposto e chama motores.rim sobre o snapshot limpo"
  key_links:
    - from: "scripts/spike_eng_rim_104.py"
      to: "hs.CAMINHO_SNAPSHOT_LIMPO"
      via: "loader offline dos 104"
      pattern: "CAMINHO_SNAPSHOT_LIMPO"
    - from: "scripts/spike_eng_rim_104.py"
      to: "motores.rim"
      via: "chamada direta com insumo derivado pela política do arquétipo"
      pattern: "motores\\.rim|from.*motores"
  anti_goals:
    - "NÃO validar o caso do livro (ITUB4 = R$ 37,22): queima o hold-out da Fase 14. Nenhum assert/print nomeando ITUB4 com número-alvo"
    - "NÃO nomear ticker em nenhuma justificativa registrada (BLIND-04a): a medição é por distribuição/coorte, não por caso"
    - "NÃO editar produção nem config.yaml/calibracao.lock.yaml: git diff em src/ e config deve ficar VAZIO ao fim do spike"
    - "NÃO calibrar knob para 'acertar' um ticker — a decisão g_terminal é medição de sanidade de coorte, não ajuste"
---

<objective>
Medir, ANTES de fechar o mapa de âncoras, como os arquétipos regulada e cíclica se comportam sob o
RIM único (confiança MEDIUM no research: hoje regulada→DDM e cíclica→lucro_normalizado/Gordon-P/L
mudam para o RIM). Fixar por execução — não por suposição — a escolha do `g_terminal` do carve-out
`CONCESSAO_FINITA` (`None` fade-only vs `PIB_real` pinado). É o oráculo herdado de KE-04
(`test_regressao_104_sem_explosao`), aplicado como spike de medição.

Purpose: de-riscar as Ondas 2-4. O mapa de âncoras (Plano 02/03) e o carve-out (Plano 03) precisam
ser declarados ANTES do hold-out (Fase 14) e provados por distribuição dos 104, não por caso.
Output: `.planning/spikes/eng-rim-104.md` com a medição por coorte + a decisão do carve-out; script
throwaway offline. Zero produção tocada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-CONTEXT.md
@.planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md

# Fonte do oráculo herdado (padrão a espelhar) e do RIM
@tests/test_ke_validacao.py
@src/analista/core/motores.py
@src/analista/report/report.py
@tests/helpers_sanidade.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script de medição offline do RIM único sobre os 104</name>
  <files>scripts/spike_eng_rim_104.py</files>
  <read_first>
    - tests/test_ke_validacao.py (o padrão `test_regressao_104_sem_explosao`: carrega `hs.CAMINHO_SNAPSHOT_LIMPO`, carimba β setorial via `macro.carimbar_beta_setorial`, roda offline idêntico ao app)
    - src/analista/core/motores.py (`rim` :66 — assinatura: vpa0, roe0, ke, retencao, n, excesso_sustentavel, g_terminal, ke_g_spread_min, fade_para, roe_terminal; e `motores.py:128-129` — terminal só libera com g_terminal não-None E ke−g_terminal ≥ ke_g_spread_min)
    - src/analista/report/report.py (`_roe_through_cycle` :184; ramo `rim` :255-268 e ramo `normalizado` :285-309 — a mecânica de derivação do lpa_norm deflacionado; `_intrinseco_por_motor` :201)
    - src/analista/core/arquetipo.py (`classificar` :124, `ARQUETIPO_MOTOR` :48 — as chaves de arquétipo atuais)
  </read_first>
  <action>
    Criar `scripts/spike_eng_rim_104.py` (offline, throwaway, NÃO importado por produção). Carregar os
    104 do snapshot limpo (mesmo loader offline de `test_ke_validacao.py`, `hs.CAMINHO_SNAPSHOT_LIMPO`,
    β setorial carimbado). Para cada ticker: classificar arquétipo (`arquetipo.classificar`), derivar o
    insumo do RIM pela política proposta no §"Mapa de âncoras" do RESEARCH — financeira/madura →
    `roe0=c.roe_valuation()`, `roe_terminal=_roe_through_cycle`; cíclica → `roe0 = lpa_norm_deflacionado /
    vpa0` (série deflacionada por IPCA, PRIM-04, como o ramo `normalizado` faz); crescimento →
    `roe0=c.roe_qualidade_atual()` com fade `n_fade` — e chamar `motores.rim(...)` com `ke=a.ke` (pronto,
    Fase 12), `g_terminal=g_T` onde `g_T = max(0, min(ROE_T×retenção, g_cap))`. Para o coorte
    CONCESSAO_FINITA (as que hoje casam `eh_concessionaria`), rodar DUAS variantes: `g_terminal=None`
    (fade-only) e `g_terminal=PIB_real` (2,0% real puro). Coletar por COORTE (financeira, madura,
    concessão, cíclica, crescimento, holding): distribuição de `V/preço` (mediana, p10, p90, max), de
    `P/B justo = 1 + (roe_T − ke)/(ke − g_T)`, e de `payout_T = 1 − g_T/roe_T`. NÃO nomear ticker no
    output agregado; imprimir por coorte + contagem de ofensores (V/preço > 50×, P/B fora de (0,6),
    payout_T fora de (0,1), NaN/inf). Never-raise: ticker que degrada conta como `None`, não aborta.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python scripts/spike_eng_rim_104.py` executa sem exceção e imprime tabela por coorte (V/preço, P/B justo, payout_T; contagem de ofensores) para as duas variantes de g_terminal
    - O script lê `hs.CAMINHO_SNAPSHOT_LIMPO` e chama `motores.rim` diretamente (grep confirma ambos os símbolos no arquivo)
    - `git diff --stat src/ config.yaml calibracao.lock.yaml` VAZIO após rodar (spike não toca produção)
    - Nenhum literal de ticker cravado num assert/print de número-alvo (grep por `37,22`/`37.22`/`ITUB4.*3[579]` retorna 0 no script)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python scripts/spike_eng_rim_104.py && test -z "$(git diff --stat src/ config.yaml calibracao.lock.yaml)"</automated>
  </verify>
  <done>O script roda offline, imprime a medição por coorte para as duas variantes, e não altera produção.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar a medição + decidir o g_terminal do carve-out</name>
  <files>.planning/spikes/eng-rim-104.md</files>
  <read_first>
    - .planning/phases/13-motores-contrato-de-sa-da-eng/13-RESEARCH.md (§"Carve-out CONCESSAO_FINITA" — direção HIGH: não aplicar o g de inflação; escolha None vs PIB_real a MEDIR; §"Mapa de âncoras" observação load-bearing sobre TAEE11/regulada; Assumptions A2/A6; Open Question 3)
    - .planning/spikes/san-07-ihcd-at1-fvoci.md (formato de spike doc do projeto: pergunta → medição → resposta fundamentada, sem knob movido)
  </read_first>
  <action>
    Escrever `.planning/spikes/eng-rim-104.md` no formato de spike do projeto. Registrar: (1) a
    distribuição por coorte medida na Task 1; (2) se regulada/madura e cíclica ficam SÃS sob o RIM único
    (V/preço não explode — teto de sanidade ~50× medido na KE-04; P/B justo ∈ (0,6); payout_T ∈ (0,1)) —
    resposta por coorte, sem nomear ticker; (3) a DECISÃO do carve-out `CONCESSAO_FINITA`: `g_terminal =
    None` (fade-only, recomendação do research) OU `g_terminal = PIB_real`, com a evidência de coorte que
    a sustenta (qual das duas variantes mantém a concessão sã sem subvalorizar grosseiramente). A
    justificativa é econômica (ICPC 01: book já é VP da RAP; g_cap embute inflação → double-count) e
    **não menciona ticker**. Marcar explicitamente que este spike NÃO valida o caso do livro (Fase 14) e
    que nenhum knob foi movido (git diff config vazio).
  </action>
  <acceptance_criteria>
    - `.planning/spikes/eng-rim-104.md` existe e contém a seção de decisão com a string `g_terminal` e a escolha explícita (None ou PIB_real) fundamentada por coorte
    - O doc afirma por coorte se regulada/cíclica ficam sãs sob RIM único (finito, P/B∈(0,6), payout_T∈(0,1), sem explosão) — a entrada para o mapa de âncoras dos Planos 02/03
    - `grep -iE '37[.,]22|R\$ ?3[5-9]' .planning/spikes/eng-rim-104.md` retorna 0 (não valida o caso do livro; fronteira dura com a Fase 14)
    - O doc registra "nenhum knob movido" e a justificativa do carve-out não contém nome de ticker (grep por tickers B3 no bloco de justificativa == 0)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/spikes/eng-rim-104.md && grep -q "g_terminal" .planning/spikes/eng-rim-104.md && ! grep -qiE '37[.,]22' .planning/spikes/eng-rim-104.md</automated>
  </verify>
  <done>A decisão do carve-out e a sanidade de coorte de regulada/cíclica estão registradas por medição; a fronteira com a Fase 14 é respeitada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot dos 104 → motores.rim | Dado financeiro cru (possivelmente sujo: escalas absurdas tipo 921×) cruza para a fórmula de valor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Denial (crash) | scripts/spike_eng_rim_104.py sobre dado absurdo | mitigate | Never-raise por ticker: degrada para None e conta como ofensor, nunca aborta a varredura (contrato SAN-06) |
| T-13-02 | Info disclosure (overfit) | justificativa do carve-out | mitigate | Justificativa econômica sem ticker; medição por coorte/distribuição (BLIND-04a); doc não valida o caso do livro |
</threat_model>

<verification>
- `.venv/bin/python scripts/spike_eng_rim_104.py` roda offline sem exceção.
- `git diff --stat src/ config.yaml calibracao.lock.yaml` VAZIO (spike puro).
- `.planning/spikes/eng-rim-104.md` registra a decisão do g_terminal e a sanidade por coorte, sem nomear ticker e sem validar o caso do livro.
</verification>

<success_criteria>
A escolha do `g_terminal` do carve-out `CONCESSAO_FINITA` e a sanidade de coorte de regulada/cíclica sob
o RIM único estão MEDIDAS e registradas, prontas para alimentar o mapa de âncoras dos Planos 02/03 — sem
tocar produção, sem calibrar knob, sem validar o caso do livro.
</success_criteria>

<output>
After completion, create `.planning/phases/13-motores-contrato-de-sa-da-eng/13-01-SUMMARY.md`
</output>
