---
phase: 08-sanidade-dos-dados-san
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/ingest/cvm.py
  - src/analista/ingest/prices.py
  - src/analista/core/fundamentals.py
  - src/analista/ingest/build.py
  - tests/test_sanidade_insumos.py
  - tests/classificacao.yaml
autonomous: true
requirements: [SAN-01, SAN-02, SAN-03, SAN-04]
must_haves:
  truths:
    - "cvm.fundamentos_do_ano devolve o lucro do controlador (3.11.01) e a participação de não-controladores no PL, separados do consolidado"
    - "cvm.fundamentos_do_ano devolve, em paralelo e SEM tocar o campo sujo, os proventos da DFC casados por filtro AMPLO (dividendo OU juros sobre capital) — o insumo que mede o JCP perdido sem depender de num_acoes"
    - "prices.coletar_mercado devolve marketCap, impliedSharesOutstanding e o histórico COMPLETO de splits"
    - "Um CompanyData recém-construído nasce com confianca == 'nao_avaliada' e avisos == []"
    - "CompanyData carrega a origem de cada ano de num_acoes (cvm vs yahoo_fallback)"
    - "Nenhum valor que os motores já consomem muda: lucro_liquido, patrimonio_liquido, num_acoes e dividendos são bit-a-bit idênticos aos de antes"
    - "O comentário BUG-JCP de build.py:104 — que tem a direção INVERTIDA — é corrigido com os números medidos (corrigir comentário errado não é consertar dado)"
  artifacts:
    - path: "src/analista/ingest/cvm.py"
      provides: "leitura das contas 3.11.01, dos minoritários no PL e dos proventos por filtro AMPLO (o JCP que o filtro atual perde)"
      contains: "lucro_controlador"
    - path: "src/analista/ingest/prices.py"
      provides: "marketCap, impliedSharesOutstanding e splits em DadosMercado"
      contains: "implied_shares_outstanding"
    - path: "src/analista/core/fundamentals.py"
      provides: "campos de diagnóstico + avisos/confianca (D-01/D-03)"
      contains: "confianca"
    - path: "tests/test_sanidade_insumos.py"
      provides: "prova de que os insumos novos existem e degradam sem levantar"
  key_links:
    - from: "src/analista/ingest/build.py"
      to: "CompanyData.lucro_controlador / .market_cap / .splits / .origem_num_acoes"
      via: "montar_empresa carimba os insumos de diagnóstico"
      pattern: "c\\.(lucro_controlador|market_cap|splits|origem_num_acoes)"
---

<objective>
Wave 0 dos insumos. Os checks SAN-01, SAN-02 e SAN-04 são **impossíveis** com o que o pipeline lê
hoje: falta a conta CVM `3.11.01` (lucro do controlador), a participação de não-controladores no PL,
e faltam `marketCap` / `impliedSharesOutstanding` / `tk.splits` (histórico completo) do Yahoo.

E o **SAN-03 é impossível de diagnosticar corretamente** com o que existe hoje: o insumo que mede
o **JCP perdido** não existe, e a única razão disponível (`Σ dividendos_CVM` vs `Σ DPA × num_acoes`)
depende de `num_acoes` — que está quebrado em 205.000× no BRSR6. Ela dispararia **pelo motivo errado**
e não diagnosticaria JCP nenhum: diagnosticaria escala.

🔴 **A DIREÇÃO DO BUG-JCP ESTÁ INVERTIDA NO REPO — medido, não suposto.**
O comentário de `build.py:104-107` afirma que *"o histórico do Yahoo perde o JCP... a DFC da CVM traz
div + JCP completos → fonte preferida"*. **É o contrário.**
- **O lado CVM PERDE o JCP.** O BRSR6 fila o JCP na linha `6.03.04 "Juros sobre o Capital Próprio
  Pagos"` — **sem a substring "dividendo"**. O filtro de `cvm.py:169`
  (`ds.str.contains("dividendo")`) **não casa**. Medido: `_distribuicoes_proventos()` devolve R$ 36,0 M
  em 2025 contra R$ 620,0 M de JCP fora do filtro — **subestimação de 18×** (e 19×/24×/25×/5× em
  2021-2024).
- **O lado Yahoo é o lado BOM — ele INCLUI o JCP.** Medido: `DPA_yahoo × contagem real de ações` bate
  com o provento real (div + JCP) do BRSR6 com erro **< 5% em 4 anos** (1,00× / 1,00× / 1,00×).
- Os 4 grandes bancos **escapam por acidente**: filam numa linha `"Dividendos E Juros sobre o Capital
  Próprio Pagos"`, que casa o filtro. Por isso o bug só aparece nas ~13 empresas do DATA-01.

**O código prefere a CVM exatamente onde a CVM está quebrada.** Consertar isso é **DATA-01 (Fase 9)** —
aqui a gente só **mede** e **corrige o comentário mentiroso**, para que quem for consertar não conserte
o lado errado.

Este plano **LÊ campos novos**. Não conserta nada.

**Ler conta nova ≠ consertar dado.** `num_acoes` continua nascendo de `|LL / LPA|` (`build.py:87`),
com todos os seus 41 tickers quebrados. `lucro_liquido` continua sendo o **consolidado**.
`_fator_unit` continua devolvendo 5 para a ALUP11 quando o certo é 3. Nada disso se toca — esses
bugs **são** o teste de regressão da Fase 9, e apagá-los aqui destrói a prova.

Purpose: sem estes 6 insumos, metade dos checks da fase não existe — e o SAN-03 nasceria medindo
escala em vez de JCP, esvaziando o teste de regressão do DATA-01.
Output: `cvm.py`, `prices.py`, `fundamentals.py` e `build.py` expondo os insumos de diagnóstico;
o comentário invertido do BUG-JCP corrigido; um teste que prova que tudo isso existe e degrada sem
levantar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-sanidade-dos-dados-san/08-RESEARCH.md
@.planning/phases/08-sanidade-dos-dados-san/08-CONTEXT.md
@CLAUDE.md
@src/analista/ingest/cvm.py
@src/analista/ingest/prices.py
@src/analista/ingest/build.py
@src/analista/core/fundamentals.py
@tests/classificacao.yaml

<interfaces>
Contratos que o executor precisa e que já existem no repo — use-os, não os reinvente.

`src/analista/ingest/cvm.py`:
    def _valor_conta(df, cd_cvm, codigos: list, nomes: list,
                     nome_primeiro: bool = False, aplicar_escala: bool = True) -> Optional[float]
    def fundamentos_do_ano(cd_cvm: int, ano: int) -> Dict[str, Optional[float]]
      # chaves hoje: lucro_liquido, vendas_liquidas, patrimonio_liquido, ativo_circulante,
      #              passivo_circulante, divida_lp, ativo_intangivel, fco, lpa,
      #              dividendos_distribuidos

    def _distribuicoes_proventos(df, cd_cvm) -> Optional[float]   # cvm.py:149-174
      # filtra a DFC 6.03.* por DS_CONTA contendo "dividendo"  <-- 🔴 O FILTRO ESTREITO, QUE PERDE O JCP
      #   incluir = ds.str.contains("dividendo", na=False)
      #   excluir = ds.str.contains("nao control|minoritar|recebid", na=False, regex=True)
      # NÃO TOQUE NESTA FUNÇÃO. Ela continua suja — consertá-la é DATA-01 (Fase 9).

`src/analista/ingest/prices.py`:
    def _yf()                      # import tardio de yfinance — PONTO DE INJEÇÃO dos testes
    def _fetch_info(tk) -> dict    # {} em qualquer falha — PONTO DE INJEÇÃO dos testes
    @dataclass DadosMercado:
        ticker, preco_atual, num_acoes, setor, nome, volume_financeiro_diario, beta,
        desempenho_relativo_6m, dividendos_por_ano, dpa_trailing_12m, ano_dpa,
        serie_precos, ohlc, ohlc_ajustado, serie_precos_ajustada
    def coletar_mercado(ticker: str, meses_beta: int = 60) -> DadosMercado

`src/analista/core/fundamentals.py`:
    @dataclass CompanyData:
        ticker, nome, setor, anos, lucro_liquido, patrimonio_liquido, fco,
        vendas_liquidas, dividendos, num_acoes, ...  (séries {ano: float})
        # NÃO tem nenhum campo de aviso ou confiança hoje

`src/analista/ingest/build.py`:
    def montar_empresa(ticker, ano_base, n_anos=10, setores_concessionaria=(...)) -> Optional[CompanyData]
      # build.py:99-102 — a bifurcação da origem de num_acoes:
      #   if ano in contagem_cvm:  c.num_acoes[ano] = contagem_cvm[ano] / fator   -> origem "cvm"
      #   elif acoes_atual:        c.num_acoes[ano] = acoes_atual                 -> origem "yahoo_fallback"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cvm.py lê o lucro do controlador e os minoritários do PL</name>

  <read_first>
    - src/analista/ingest/cvm.py (o arquivo modificado — leia `_valor_conta` inteiro para entender
      `nome_primeiro` e `aplicar_escala` antes de escrever qualquer chamada)
    - .planning/phases/08-sanidade-dos-dados-san/08-RESEARCH.md §"Achado 4" (as contas exatas,
      medidas) e §"Achado 6" (por que o PL dos bancos NÃO é `2.03`)
    - src/analista/ingest/cvm.py, função `_distribuicoes_proventos` (linhas 149-174) — o filtro
      estreito que **perde o JCP**. Leia-o inteiro: você vai **copiar a forma** dele, **sem tocá-lo**.
  </read_first>

  <action>
Adicione **duas chaves novas** ao dicionário devolvido por `fundamentos_do_ano` (`cvm.py:212-246`).
Não altere nenhuma chave existente.

1. `"lucro_controlador"` — o lucro atribuído aos **sócios da empresa controladora**, extraído da DRE
   (o mesmo `df` que já alimenta `lucro_liquido`). Código: `3.11.01`. Nomes de fallback:
   `"Atribuído a Sócios da Empresa Controladora"`, `"Atribuído a Sócios da Empresa Controlada"`.
   `aplicar_escala=True` (a conta vem em R$ mil, exatamente como o `3.11`).

2. `"pl_nao_controladores"` — a participação de acionistas não-controladores dentro do PL, extraída
   do BPP (o mesmo `df` que já alimenta `patrimonio_liquido`). O **código varia por template** —
   medido: `2.03.09` (empresas), `2.07.02` (BBAS3/BBDC4/BRSR6), `2.08.09` (ITUB4). Passe os três na
   lista de códigos. Nomes de fallback: `"Participação dos Acionistas Não Controladores"`,
   `"Participação dos Não Controladores"`. Use `nome_primeiro=True` — é a mesma defesa que o
   `patrimonio_liquido` já usa (`cvm.py:224-228`) contra o template dos bancos.

Ambas devolvem `None` quando a empresa não tem a linha (empresa sem subsidiária consolidada). `None`
é resposta válida — **não** invente zero.

3. `"proventos_filtro_amplo"` — **o insumo que mede o JCP perdido, sem depender de `num_acoes`.**
   Crie uma função nova `_distribuicoes_proventos_amplo(df, cd_cvm)`, **irmã** de
   `_distribuicoes_proventos` (`cvm.py:149-174`), idêntica em tudo — mesma varredura da DFC `6.03.*`,
   **mesmas exclusões** (`nao control|minoritar|recebid`), mesmo `abs()`, mesma escala — **exceto** no
   filtro de inclusão, que passa a casar **`"dividendo"` OU `"juros sobre capital"`**:

       incluir = ds.str.contains("dividendo|juros sobre capital", na=False, regex=True)

   **Por que uma função nova e não um parâmetro em `_distribuicoes_proventos`:** a função existente é
   consumida por `build.py:88` para popular `c.dividendos`, que os motores usam. Ela precisa continuar
   devolvendo **exatamente o mesmo número sujo de hoje** — é o teste de regressão do DATA-01. Um
   parâmetro com default seria um convite a alguém "só ligar" o filtro amplo e apagar a prova.

   **Isto é leitura diagnóstica, não conserto.** `_distribuicoes_proventos` fica **intocada**;
   `c.dividendos` continua sujo e continua vindo do filtro estreito. O valor novo alimenta **só** o
   detector do SAN-03 — exatamente o mesmo padrão do `lucro_controlador` no SAN-04.

   Verificação de sanidade contra o medido: no BRSR6, `proventos_filtro_amplo / dividendos_distribuidos`
   fica entre **5×** e **25×** em 2021-2025. Nos 4 grandes bancos fica em **1,00×** (eles filam numa
   linha `"Dividendos E Juros sobre o Capital Próprio Pagos"`, que já casa o filtro estreito — **escapam
   por acidente**).

**Regra dura:** a chave `"lucro_liquido"` continua lendo `3.11`/`3.13`/`3.09` (o **consolidado**), a
chave `"lpa"` continua lendo `3.99.01.01` **exatamente como hoje, com o bug intacto**, e a chave
`"dividendos_distribuidos"` continua saindo de `_distribuicoes_proventos` (o **filtro estreito**, que
perde o JCP). Os valores novos alimentam **só** o detector.

Documente no docstring/comentário do módulo, em uma frase, que `3.11.01` é insumo de **diagnóstico**
(SAN-04) e que o consolidado permanece a fonte de `lucro_liquido` — para o leitor da Fase 9 não achar
que o conserto já foi feito aqui.
  </action>

  <acceptance_criteria>
    - `grep -c "3.11.01" src/analista/ingest/cvm.py` retorna ≥ 1
    - `grep -c "pl_nao_controladores" src/analista/ingest/cvm.py` retorna ≥ 1
    - `grep -c "_distribuicoes_proventos_amplo" src/analista/ingest/cvm.py` retorna ≥ 2 (def + uso)
    - `grep -c "juros sobre capital" src/analista/ingest/cvm.py` retorna ≥ 1
    - `git diff src/analista/ingest/cvm.py` NÃO contém alteração nas listas de códigos de
      `lucro_liquido` (`["3.11", "3.13", "3.09"]`), de `patrimonio_liquido` (`["2.03", "2.08"]`) nem
      de `lpa` (`["3.99.01.01", "3.99.01"]`)
    - `git diff src/analista/ingest/cvm.py` **NÃO altera nenhuma linha do corpo de
      `_distribuicoes_proventos`** — em especial, a linha `incluir = ds.str.contains("dividendo", ...)`
      permanece intacta (é o bug que a Fase 9 conserta e que o SAN-03 mede)
    - **O detector de JCP perdido funciona, medido:** no BRSR6 (CD_CVM 1210), em 2025,
      `proventos_filtro_amplo / dividendos_distribuidos` > 5 (o medido é ~18×); no ITUB4 (19348) a
      razão é ~1,0 (o grande banco escapa por acidente)
  </acceptance_criteria>

  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -c "
from analista.ingest import cvm
f = cvm.fundamentos_do_ano(19348, 2025)
assert f['lucro_controlador'] is not None and f['pl_nao_controladores'] is not None
assert f['lucro_liquido'] != f['lucro_controlador']
assert abs(f['proventos_filtro_amplo'] / f['dividendos_distribuidos'] - 1) < 0.05, 'grande banco deveria escapar'
b = cvm.fundamentos_do_ano(1210, 2025)
assert b['proventos_filtro_amplo'] / b['dividendos_distribuidos'] > 5, 'JCP perdido do BRSR6 nao foi detectado'
print('OK — o filtro estreito perde o JCP e o amplo o enxerga')"</automated>
  </verify>

  <done>`fundamentos_do_ano` devolve `lucro_controlador`, `pl_nao_controladores` e `proventos_filtro_amplo`; `_distribuicoes_proventos` está intocada e continua perdendo o JCP; nenhuma chave preexistente mudou de valor.</done>
</task>

<task type="auto">
  <name>Task 2: prices.py lê marketCap, impliedSharesOutstanding e o histórico completo de splits</name>

  <read_first>
    - src/analista/ingest/prices.py (o arquivo modificado — leia `coletar_mercado` inteiro; note o
      retry do `_fetch_info` e o bloco `try/except` dos dividendos em `prices.py:190-207`, que é o
      padrão never-raise a copiar)
    - .planning/phases/08-sanidade-dos-dados-san/08-RESEARCH.md §"Achado 5" (a forma exata do
      `tk.splits`), §"Achado 3" (por que `impliedSharesOutstanding` e NUNCA `sharesOutstanding`) e
      §"R-07"/"R-08" (dtype=object, tz-aware, chamada de rede extra, dado mutável)
  </read_first>

  <action>
Adicione **três campos** ao dataclass `DadosMercado` e preencha-os em `coletar_mercado`.

1. `market_cap: Optional[float] = None` ← `info.get("marketCap")`
2. `implied_shares_outstanding: Optional[float] = None` ← `info.get("impliedSharesOutstanding")`
3. `splits: Dict[str, float] = field(default_factory=dict)` ← histórico **completo** de splits

Sobre o item 3 — **três armadilhas medidas**:
- `dm.ohlc["Stock Splits"]` **NÃO SERVE**: o `tk.history(period="5y")` de `prices.py:147` cobre
  2021→2026 e o split do ITUB4 de `2018-11-21` (o único que a isenção do D-12 realmente precisa)
  fica **fora** da janela. É obrigatório chamar `tk.splits` (histórico completo) — **uma chamada de
  rede a mais**.
- `tk.splits` devolve uma `pandas.Series` **nunca `None`**; quando não há split, vem **vazia e com
  `dtype=object`** (não `float64`) — operação numérica direta nela quebra. Trate o caso vazio antes
  de qualquer aritmética.
- O index é `DatetimeIndex` **tz-aware** (`America/Sao_Paulo`) — comparar com `datetime` naive
  levanta `TypeError`. **Não compare datas.** Itere com `.items()` (é o que `prices.py:196` já faz
  para dividendos) e serialize a chave como **string ISO** `"YYYY-MM-DD"` via
  `data.strftime("%Y-%m-%d")`. Para o SAN-02 só o **ano** importa.

Crie um ponto de injeção `_fetch_splits(tk) -> Dict[str, float]` no mesmo espírito do `_fetch_info`
(`prices.py:32-37`): envolve tudo em `try/except Exception` e devolve `{}` em **qualquer** falha —
inclusive o 404 do MRFG3. Chame-o de `coletar_mercado`. **Nada aqui pode levantar exceção** (SAN-06).

**Regra dura:** `dm.num_acoes = info.get("sharesOutstanding")` fica **INTACTO**. Trocar por
`impliedSharesOutstanding` seria consertar dado (é literalmente o DATA-02 da Fase 9) e apagaria o
teste de regressão. O `implied_shares_outstanding` é um campo **novo e paralelo**, consumido só pelo
detector.
  </action>

  <acceptance_criteria>
    - `grep -c "impliedSharesOutstanding" src/analista/ingest/prices.py` retorna ≥ 1
    - `grep -c "marketCap" src/analista/ingest/prices.py` retorna ≥ 1
    - `grep -c "_fetch_splits" src/analista/ingest/prices.py` retorna ≥ 2 (def + chamada)
    - `git diff src/analista/ingest/prices.py` mantém a linha `dm.num_acoes = info.get("sharesOutstanding")` inalterada
    - Nenhuma comparação de `datetime` com o index de `tk.splits` aparece no diff
  </acceptance_criteria>

  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -c "
from analista.ingest import prices
dm = prices.DadosMercado(ticker='X')
assert dm.market_cap is None and dm.implied_shares_outstanding is None and dm.splits == {}
class TkQuebrado:
    @property
    def splits(self): raise RuntimeError('404')
assert prices._fetch_splits(TkQuebrado()) == {}
print('OK')"</automated>
  </verify>

  <done>`DadosMercado` carrega `market_cap`, `implied_shares_outstanding` e `splits` (dict de string ISO → fator); `_fetch_splits` devolve `{}` em qualquer falha; `sharesOutstanding` intacto.</done>
</task>

<task type="auto">
  <name>Task 3: CompanyData ganha avisos/confianca e os campos de diagnóstico; build.py os carimba</name>

  <read_first>
    - src/analista/core/fundamentals.py (o arquivo modificado — o dataclass `CompanyData`, linhas
      19-53; NÃO toque em nenhum método de cálculo abaixo da linha 55)
    - src/analista/ingest/build.py (o arquivo modificado — `montar_empresa`; a bifurcação de origem
      de `num_acoes` está em build.py:99-102)
    - tests/classificacao.yaml (o teste novo desta task PRECISA de entrada aqui, senão a coleta
      inteira da suíte quebra com `pytest.UsageError` — R-10)
    - tests/conftest.py (o mecanismo que impõe isso, e o escape `BLIND_BOOTSTRAP=1`)
    - tests/test_ingest_unit.py (padrão de teste de ingest com monkeypatch — reaproveite a forma)
    - .planning/phases/08-sanidade-dos-dados-san/08-RESEARCH.md §"Achado 2" item (c) — por que a
      origem de cada ano precisa ser carimbada
  </read_first>

  <action>
**(a) `core/fundamentals.py` — `CompanyData` ganha os campos.** Todos com default, para não quebrar
nenhum construtor existente (inclusive o `backtest.carregar_snapshot` dos bancos):

- Diagnóstico D-01/D-03:
  - `avisos: List["Aviso"] = field(default_factory=list)` — tipada como `List` genérica (`List[Any]`
    ou string forward-ref); **não** importe `core.sanidade` aqui, para não criar ciclo de import.
  - `confianca: str = "nao_avaliada"` — **este é o default e ele é inegociável (D-03)**. Um
    `CompanyData` construído à mão não pode nascer parecendo limpo.
- Insumos dos checks (séries `{ano: float}`, todas `default_factory=dict`):
  - `lucro_controlador` (SAN-04), `pl_nao_controladores` (SAN-04), `lpa_cvm` (o LPA **cru** da CVM,
    pré-divisão — é a causa-raiz), `dpa_por_ano` (o DPA do Yahoo, insumo do SAN-03),
    `proventos_filtro_amplo` (**o insumo do detector de JCP perdido** — DFC `6.03.*` casada por
    "dividendo" **OU** "juros sobre capital"; SAN-03),
    `origem_num_acoes: Dict[int, str]` (valores: `"cvm"` ou `"yahoo_fallback"`).
- Insumos de mercado (escalares/dict):
  - `market_cap: Optional[float] = None`, `implied_shares_outstanding: Optional[float] = None`,
    `splits: Dict[str, float] = field(default_factory=dict)`.

**(b) `ingest/build.py` — `montar_empresa` carimba os campos.** Sem mudar uma linha de cálculo:
- No laço de anos (build.py:75-89), guarde `c.lucro_controlador[ano]`, `c.pl_nao_controladores[ano]`,
  `c.lpa_cvm[ano]` e `c.proventos_filtro_amplo[ano]` a partir de `f["lucro_controlador"]`,
  `f["pl_nao_controladores"]`, `f["lpa"]` e `f["proventos_filtro_amplo"]` (só quando não-`None`).
  ⚠️ `c.dividendos[ano]` continua saindo de `dist_cvm[ano]` (o **filtro estreito**, sujo) exatamente
  como hoje — `c.proventos_filtro_amplo` é um campo **paralelo**, que os motores **não** consomem.
- No laço do Passo 2 (build.py:98-113), **na mesma bifurcação que já existe**, carimbe
  `c.origem_num_acoes[ano] = "cvm"` no ramo `if ano in contagem_cvm` e `"yahoo_fallback"` no ramo
  `elif acoes_atual`.
- Depois de `c.preco_atual = dm.preco_atual` (build.py:58), copie `c.market_cap = dm.market_cap`,
  `c.implied_shares_outstanding = dm.implied_shares_outstanding`, `c.splits = dm.splits` e
  `c.dpa_por_ano = dict(dm.dividendos_por_ano)`.

**Regra dura:** `contagem_cvm[ano] = abs(f["lucro_liquido"] / lpa_cvm)` (build.py:87), o `_fator_unit`
(build.py:24-37), a atribuição de `c.num_acoes` e a de `c.dividendos` ficam **byte a byte iguais**.
Nenhum número que os motores consomem pode mudar neste plano.

**(b.2) Corrigir o comentário BUG-JCP de `build.py:104-107` — que está com a DIREÇÃO INVERTIDA.**
Ele afirma hoje: *"o histórico do Yahoo perde o JCP (metade da distribuição dos bancos nos anos
antigos)... A DFC da CVM traz div + JCP completos e auditáveis → fonte preferida; Yahoo é fallback"*.
**Medido: é o contrário.** O filtro de `cvm.py:169` casa só `"dividendo"`, e o BRSR6 fila o JCP em
`6.03.04 "Juros sobre o Capital Próprio Pagos"` → a CVM devolve R$ 36,0 M em 2025 contra R$ 620,0 M
reais (**18× a menos**; 19×/24×/25×/5× em 2021-2024). E o **DPA do Yahoo INCLUI o JCP**: bate com o
provento real do BRSR6 com erro **< 5% em 4 anos**. Os 4 grandes bancos escapam por acidente (filam
numa linha `"Dividendos E Juros sobre o Capital Próprio Pagos"`).

Reescreva o comentário com os números medidos e a direção certa, deixando **explícito** que o código
**prefere a CVM exatamente onde a CVM está quebrada**, e que **consertar isso é o DATA-01 da Fase 9** —
não aqui. **Só o comentário muda; a lógica fica idêntica.** (Corrigir um comentário factualmente errado
não é consertar dado — é impedir que a Fase 9 conserte o lado errado por confiar nele.)

**(c) `tests/test_sanidade_insumos.py`** — novo, categoria `contrato`:
- `test_companydata_nasce_nao_avaliada` — um `CompanyData(ticker=...)` cru tem
  `confianca == "nao_avaliada"` e `avisos == []` (D-03).
- `test_dados_mercado_degrada_sem_levantar_quando_o_yahoo_falha` — monkeypatch em `prices._yf` /
  `prices._fetch_info` / `prices._fetch_splits` simulando falha total: `coletar_mercado` devolve um
  `DadosMercado` com `market_cap is None`, `implied_shares_outstanding is None`, `splits == {}` e
  **não levanta** (SAN-06).
- `test_montar_empresa_carimba_a_origem_de_num_acoes` — com `prices.coletar_mercado`
  monkeypatchado (CVM do cache real, offline), o `CompanyData` resultante tem
  `set(c.origem_num_acoes.values()) <= {"cvm", "yahoo_fallback"}` e uma chave de origem para **cada**
  ano presente em `c.num_acoes`.
- `test_montar_empresa_carimba_o_lucro_do_controlador` — `c.lucro_controlador` não é vazio e, para
  o último ano, `c.lucro_controlador[ult] != c.lucro_liquido[ult]` num ticker com minoritários.
- `test_o_filtro_estreito_da_cvm_perde_o_jcp` — num ticker de JCP perdido (**BRSR6**), o
  `c.proventos_filtro_amplo[ult]` é **maior** que o `c.dividendos[ult]`; e num grande banco (**ITUB4**)
  os dois são **iguais** (ele escapa por acidente). Asserts de **comparação entre dois campos** — sem
  nenhuma constante numérica (o BLIND-04a fica longe).

**BLIND-04a (o meta-teste anti-golden) — não acorde o detector:** um teste é OFENSOR quando contém
**ao mesmo tempo** um literal de ticker conhecido **E** uma constante numérica não-trivial (∉ {0.0,
1.0, 0.5, 2.0}) chegando a um assert. Os asserts acima são todos de presença/identidade/conjunto —
**não introduza nenhum limiar numérico neste arquivo**. Se precisar de um ticker, use um literal de
ticker real, mas mantenha os asserts sem constante numérica.

**(d) `tests/classificacao.yaml` — NA MESMA TASK.** Um teste novo sem entrada aqui não fica vermelho:
ele **quebra a coleta inteira** (`pytest.UsageError`, a suíte toda não roda). Colha os nodeids exatos
com o escape oficial (que não executa teste nenhum, logo não dispara o
`test_bootstrap_do_blind01_nao_esta_ligado`):
`BLIND_BOOTSTRAP=1 .venv/bin/python -m pytest --collect-only -q -k sanidade_insumos`
e adicione cada nodeid como `'tests/test_sanidade_insumos.py::<nome>': contrato`, num bloco novo no
fim do YAML com um comentário de cabeçalho identificando a Fase 8 / plano 08-01.
  </action>

  <acceptance_criteria>
    - `grep -c 'confianca: str = "nao_avaliada"' src/analista/core/fundamentals.py` retorna 1
    - `grep -c "origem_num_acoes" src/analista/ingest/build.py` retorna ≥ 1
    - `git diff src/analista/ingest/build.py` NÃO altera as linhas `contagem_cvm[ano] = abs(...)`,
      `_fator_unit`, nem `c.num_acoes[ano] = ...`
    - `grep -c "test_sanidade_insumos" tests/classificacao.yaml` retorna ≥ 5 (uma linha por teste novo)
    - `grep -c "proventos_filtro_amplo" src/analista/ingest/build.py src/analista/core/fundamentals.py` retorna ≥ 2
    - `git diff src/analista/ingest/build.py` mostra alteração no **comentário** BUG-JCP (linhas
      104-107) e **nenhuma** alteração na lógica `if ano in dist_cvm: c.dividendos[ano] = dist_cvm[ano]`
    - `.venv/bin/python -m pytest -k sanidade_insumos` sai 0, sem `UsageError` de coleta
    - `.venv/bin/python -m pytest` sai 0 (0 failed) — a suíte inteira ainda coleta e passa
  </acceptance_criteria>

  <verify>
    <automated>.venv/bin/python -m pytest -k "sanidade_insumos" -q && .venv/bin/python -m pytest -q</automated>
  </verify>

  <done>`CompanyData` nasce `nao_avaliada`; `montar_empresa` carimba os 9 insumos de diagnóstico e a origem de cada ano; o comentário invertido do BUG-JCP está corrigido com os números medidos; nenhum número consumido pelos motores mudou; a suíte inteira continua verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yfinance → `prices.coletar_mercado` | resposta de terceiro (Yahoo), não confiável: pode 404, vir vazia, mudar de dtype ou de timezone |
| ZIP da CVM (cache local) → `cvm._ler_demonstracao` | arquivo de terceiro; conta ausente é normal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | `_fetch_splits` (chamada de rede extra por ticker) | mitigate | `try/except Exception` → `{}`; nunca levanta e nunca aborta o pipeline (contrato SAN-06). O custo de rede fica confinado à captura do snapshot (plano 08-03), não ao caminho quente do app. |
| T-08-02 | Tampering | dado mutável do Yahoo (`.splits` mudou durante a própria pesquisa) | mitigate | O detector nunca lê `.splits` ao vivo nos testes — o snapshot congelado do plano 08-03 é a única fonte de verdade dos testes. |
| T-08-03 | Information disclosure | — | accept | Nenhum segredo, nenhum PII, nenhuma superfície de rede exposta: o projeto só consome APIs públicas e gratuitas. |
</threat_model>

<verification>
1. `.venv/bin/python -m pytest -q` → 0 failed (a suíte inteira coleta e passa)
2. `PYTHONPATH=src .venv/bin/python -c "from analista.ingest import cvm; f=cvm.fundamentos_do_ano(19348, 2025); assert f['lucro_controlador'] and f['pl_nao_controladores']"`
3. `git diff --stat` mostra apenas os 6 arquivos de `files_modified`
4. `git diff src/analista/ingest/build.py | grep -E "^\-.*(contagem_cvm\[ano\]|_fator_unit|c\.num_acoes\[ano\])"` → **vazio** (nenhuma linha de cálculo removida/alterada)
</verification>

<success_criteria>
- `cvm.fundamentos_do_ano` devolve `lucro_controlador`, `pl_nao_controladores` e `proventos_filtro_amplo`
- `_distribuicoes_proventos` (o filtro estreito) está **intocada** e continua perdendo o JCP — é o bug que o SAN-03 mede e que a Fase 9 conserta
- O comentário BUG-JCP de `build.py:104` está **corrigido** (a direção estava invertida): a CVM é que perde o JCP; o Yahoo é que o tem
- `prices.coletar_mercado` devolve `market_cap`, `implied_shares_outstanding` e `splits` (histórico completo, chaves ISO)
- `CompanyData` tem `avisos` (vazio) e `confianca == "nao_avaliada"` por default
- `montar_empresa` carimba os insumos e a origem de cada ano de `num_acoes`
- **Zero conserto de dado:** `num_acoes`, `_fator_unit`, `lpa`, `lucro_liquido` e `dividendos` produzem exatamente os mesmos números de antes (só o **comentário** mudou)
- Todos os testes novos têm entrada em `tests/classificacao.yaml`
</success_criteria>

<output>
Ao concluir, crie `.planning/phases/08-sanidade-dos-dados-san/08-01-SUMMARY.md`.
</output>
