---
phase: 08-sanidade-dos-dados-san
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spikes/san-07-ihcd-at1-fvoci.md
  - .planning/spikes/MANIFEST.md
  - scripts/spike_san07_bancos.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SAN-07]
must_haves:
  truths:
    - "O spike SAN-07 responde por escrito, com números por banco, as duas perguntas — e as duas respostas são NÃO"
    - "O documento corrige a conta: o PL dos bancos é 2.07 (BBAS3/BBDC4/BRSR6) ou 2.08 (ITUB4), nunca 2.03"
    - "O documento declara a anomalia do 'Ajustes de Avaliação Patrimonial = 0,00' em vez de escondê-la"
    - "O número fantasma 'ITUB4 2019 = 1.131×' sai do REQUIREMENTS.md e do ROADMAP.md, substituído pelo salto MEDIDO"
    - "A Fase 9 herda, escrito, o achado do composicao_capital com as duas armadilhas (chave por CNPJ, escala inconsistente)"
    - "Nenhum knob se move por causa do spike"
  artifacts:
    - path: ".planning/spikes/san-07-ihcd-at1-fvoci.md"
      provides: "a resposta escrita do SAN-07 com a medição nos 4 bancos"
      contains: "2.08"
    - path: "scripts/spike_san07_bancos.py"
      provides: "re-emissão offline dos números do spike a partir do cache CVM"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SAN-02 e SAN-07 corrigidos; DATA-03 herda o achado do composicao_capital"
  key_links:
    - from: ".planning/spikes/san-07-ihcd-at1-fvoci.md"
      to: "scripts/spike_san07_bancos.py"
      via: "o doc cita o script que re-emite os números"
      pattern: "spike_san07_bancos"
---

<objective>
Fechar o SAN-07 (D-15) e **corrigir os números errados que estão escritos nos artefatos de
planejamento**.

O SAN-07 já está respondido pela medição do `08-RESEARCH.md` (§Achado 6), e as duas respostas são
**NÃO**:
1. **IHCD/AT1 no PL dos bancos?** NÃO. E a premissa do requisito está errada de saída: **`2.03` não é
   o PL de banco nenhum** — é "Passivos Financeiros ao Custo Amortizado" (ITUB4) ou "Provisões"
   (BBAS3/BBDC4/BRSR6). O PL real é `2.08` (ITUB4) e `2.07` (os outros três). E não há linha de
   AT1/IHCD dentro de nenhum deles.
2. **Dirty surplus FVOCI material?** NÃO. O OCI anual (DRA, conta `4.02`) fica entre **0,03% e 0,59%
   do PL** nos 4 bancos. Contra o clean surplus, é ruído.

**Conclusão: o terceiro bug de dados NÃO existe.** A Fase 9 não herda dúvida — herda um "não"
fundamentado. **Nenhum knob se move** (D-15, e a Armadilha 3 do ROADMAP).

Este plano também mata **dois números fantasma** que a pesquisa mediu e derrubou (R-01, R-02) e
registra, para a Fase 9, o achado mais valioso da pesquisa (R-09: `composicao_capital`).

Purpose: um requisito escrito contra uma conta que não existe produz uma resposta sem sentido; e um
limiar calibrado para um fator que não existe (1,131×) geraria falso positivo em toda bonificação da
B3 — exatamente o que desinstala a guarda.
Output: o doc do spike, o script que re-emite os números, e o REQUIREMENTS/ROADMAP corrigidos.

Este plano é **independente** do restante da fase (só toca `.planning/` e um script novo) e roda em
paralelo com o 08-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-sanidade-dos-dados-san/08-RESEARCH.md
@.planning/phases/08-sanidade-dos-dados-san/08-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/spikes/CONVENTIONS.md
@.planning/spikes/MANIFEST.md
@src/analista/ingest/cvm.py

<interfaces>
`src/analista/ingest/cvm.py` — as funções que o script do spike reaproveita (offline, cache completo
2015–2025 em `data/cvm/`):

    @lru_cache
    def _ler_demonstracao(ano: int, prefixo: str) -> Optional[pd.DataFrame]
      # prefixo é o miolo do nome do CSV dentro do ZIP:
      #   "BPP_con"  -> dfp_cia_aberta_BPP_con_2025.csv   (o balanço patrimonial passivo)
      #   "DRA_con"  -> dfp_cia_aberta_DRA_con_2025.csv   (a DRA — o parser NUNCA abriu este arquivo)
      # já filtra ORDEM_EXERC == "ÚLTIMO"; colunas: CD_CVM, CD_CONTA, DS_CONTA, VL_CONTA, ESCALA_MOEDA

    def _valor_conta(df, cd_cvm, codigos, nomes, nome_primeiro=False, aplicar_escala=True) -> Optional[float]

CD_CVM dos 4 bancos (de `data/ticker_map.json` — resolva pelo arquivo, não os cole no código):
ITUB4, BBAS3, BBDC4, BRSR6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: escrever o spike SAN-07 e o script que re-emite os números</name>

  <read_first>
    - .planning/phases/08-sanidade-dos-dados-san/08-RESEARCH.md §"Achado 6" (as duas respostas, as
      tabelas por banco, a composição do PL do ITUB4 e a anomalia do `Ajustes de Avaliação
      Patrimonial = 0,00`) e §"R-02"
    - .planning/spikes/CONVENTIONS.md e .planning/spikes/MANIFEST.md (os arquivos modificados —
      formato dos spikes existentes)
    - src/analista/ingest/cvm.py (`_ler_demonstracao` e `_valor_conta` — o script do spike os usa
      diretamente, sem tocar em cvm.py)
    - .planning/REQUIREMENTS.md §SAN-07 (o texto que está errado)
  </read_first>

  <action>
**(a) `scripts/spike_san07_bancos.py`** — script standalone (mesmo padrão de
`scripts/capturar_snapshot_bancos.py`: `sys.path.insert` do `src`, sem depender de `pip install -e`),
**100% offline** (o cache CVM 2015–2025 já está em `data/cvm/`). Para cada um dos 4 bancos (ITUB4,
BBAS3, BBDC4, BRSR6), no ano-base `2025`, ele imprime:

1. **Qual conta é de fato o PL** — varre o `BPP_con` da empresa e imprime o `CD_CONTA` cuja `DS_CONTA`
   casa "Patrimônio Líquido Consolidado", junto do que a conta `2.03` **realmente** é naquele banco
   (código + nome + valor). Esta é a prova de que a premissa do requisito está errada.
2. **A composição do PL** — todas as subcontas imediatas do bloco do PL (`2.07.*` / `2.08.*`), com
   código, nome e valor, para o leitor ver **com os próprios olhos** que não existe linha de
   IHCD / AT1 / "Instrumentos Elegíveis ao Capital" / "dívida perpétua".
3. **O dirty surplus** — do `DRA_con` (arquivo que o parser **nunca abriu**): `4.01` (LL), `4.02`
   (Outros Resultados Abrangentes) e a razão **OCI / PL**, em %.

Nada é escrito em disco; nada é consertado; nenhum knob é lido. O script é evidência re-executável,
não parte do pipeline.

**(b) `.planning/spikes/san-07-ihcd-at1-fvoci.md`** — o documento do D-15. Estrutura:

- **Pergunta** (as duas, como o REQUIREMENTS as escreveu) e **Veredito** em cima: **NÃO** e **NÃO**.
  **O terceiro bug de dados não existe.**
- **Correção da premissa** — `2.03` não é o PL de banco nenhum. Tabela: por banco, o que `2.03`
  realmente é × onde o PL realmente está (`2.08` no ITUB4, `2.07` nos outros três). Nota de que
  `cvm.py:224-228` só sobrevive porque casa pelo **nome** (`nome_primeiro=True`) — **o parser está
  certo; o texto do requisito é que estava errado**.
- **Pergunta 1 — IHCD/AT1 no PL?** A composição medida do PL do ITUB4 (`2.08.01`..`2.08.09`) e a
  constatação de ausência nos 4 bancos. No BRSR6 o instrumento aparece do lado do **passivo**
  (`2.01.01 Dívida Subordinada`). **Ressalva honesta obrigatória:** nas demonstrações IFRS *próprias*
  do Itaú os perpétuos AT1 **são** classificados em equity — o que a medição prova é que a **DFP da
  CVM não os expõe assim**, e é a DFP que este pipeline lê.
  → **Consequência para a Fase 9: o `B0` que o RIM consome NÃO está inflado por AT1.**
- **Pergunta 2 — dirty surplus FVOCI é material?** Tabela dos 4 bancos: LL (`4.01`), OCI (`4.02`), PL,
  e **OCI/PL** (0,03% / 0,04% / 0,59% / 0,21%). Nota de que as pernas individuais são grandes
  (BBAS3 tem +4,713 bi de ganhos não realizados brutos) mas **se cancelam no líquido** (efeito
  tributário e conversão cambial).
  → **Consequência: nenhum knob se move.** Contra o clean surplus, 0,6%/ano é ruído.
- **Anomalia declarada (honestidade > completude):** a conta de **estoque** `Ajustes de Avaliação
  Patrimonial` (`2.08.06` no ITUB4, `2.07.01.06` nos demais) lê **0,00 nos quatro bancos**, enquanto
  a DRA mostra um **fluxo** de OCI não-zero. Estoque zerado com fluxo não-zero é inconsistente. Duas
  leituras possíveis (o filer não preenche a linha padronizada e aloca em "Reservas de Lucros"; ou o
  valor é pequeno demais para a precisão do print). **Em qualquer dos casos a conclusão de
  materialidade não muda** — o fluxo da DRA já limita o efeito a < 0,6% do PL/ano. Declarar, não
  esconder.
- **Como reproduzir:** `PYTHONPATH=src .venv/bin/python scripts/spike_san07_bancos.py` (offline, do
  cache CVM).
- **O que a Fase 9 consome:** um "não" fundamentado. Sem knob movido, sem `B0` corrigido, sem
  `resultado abrangente` substituindo o LL.

**(c) `.planning/spikes/MANIFEST.md`** — acrescente a linha do spike na tabela `## Spikes`
(`| san-07 | ihcd-at1-fvoci | contábil | IHCD/AT1 no PL dos bancos? dirty surplus FVOCI material? |
❌ REFUTADO (as duas respostas são NÃO) | contabilidade, bancos, rim, fase-8 |`), preservando a
estrutura do arquivo. Adicione ao `## Idea`/`## Requirements` só se for necessário para o arquivo
continuar coerente — **não reescreva** o manifesto do "Modo Trading".

**Proibido neste plano:** mexer em `config.yaml`, em `calibracao.lock.yaml` ou em qualquer motor.
O spike **responde uma pergunta contábil, não move um knob** (Armadilha 3 do ROADMAP; D-15).
  </action>

  <acceptance_criteria>
    - `PYTHONPATH=src .venv/bin/python scripts/spike_san07_bancos.py` sai 0 e imprime, para os 4
      bancos, o código real do PL, as subcontas do PL e a razão OCI/PL
    - `grep -c "2.08" .planning/spikes/san-07-ihcd-at1-fvoci.md` retorna ≥ 1
    - `grep -ci "ajustes de avaliação patrimonial" .planning/spikes/san-07-ihcd-at1-fvoci.md` retorna ≥ 1
      (a anomalia está declarada)
    - `grep -c "san-07" .planning/spikes/MANIFEST.md` retorna ≥ 1
    - `git diff --name-only` NÃO inclui `config.yaml` nem `calibracao.lock.yaml`
    - O documento contém, por banco, os quatro números de OCI/PL e a identificação da conta de PL
  </acceptance_criteria>

  <verify>
    <automated>PYTHONPATH=src .venv/bin/python scripts/spike_san07_bancos.py && test -f .planning/spikes/san-07-ihcd-at1-fvoci.md && grep -q "2.08" .planning/spikes/san-07-ihcd-at1-fvoci.md && echo OK</automated>
  </verify>

  <done>O spike existe, responde NÃO às duas perguntas com número por banco, corrige a conta do PL, declara a anomalia, e o script re-emite tudo offline.</done>
</task>

<task type="auto">
  <name>Task 2: corrigir os números fantasma no REQUIREMENTS e no ROADMAP</name>

  <read_first>
    - .planning/REQUIREMENTS.md (o arquivo modificado — §SAN-01..SAN-07, e o bloco DATA logo abaixo)
    - .planning/ROADMAP.md (o arquivo modificado — §"Phase 8", critérios de sucesso 1 e 5; §"Phase 9")
    - .planning/phases/08-sanidade-dos-dados-san/08-RESEARCH.md §"R-01" (o "1.131×" não existe — o
      salto real do ITUB4 2019 é ÷1000), §"R-02" (a conta `2.03`) e §"R-09" (o `composicao_capital`)
  </read_first>

  <action>
Três correções, **no mesmo diff**, cada uma com a nota de que o valor foi **medido, não lembrado**
(cite `08-RESEARCH.md` §R-01 / §R-02 / §R-09 no texto).

**(1) O número fantasma do ITUB4 2019 (R-01).** `REQUIREMENTS.md` §SAN-02 e `ROADMAP.md` §Phase 8
critério 1 dizem *"Pega ITUB4 2019 (1.131×)"*. **Esse salto não existe.** O medido:
`2018: 10.015.234.375 → 2019: 10.004.676` = **÷1000** (o LPA de 2019 veio como `2780,00`, a doença do
×1000), e a recuperação em 2020 = **×780**. O "1,131×" é quase certamente o salto **2024→2025**
(1,1286×) — uma **bonificação real e legítima** do Itaú — mal-rotulado como 2019.

Reescreva SAN-02 para: *"Detecta salto de `num_acoes` ano-a-ano sem evento societário, com limiar
**simétrico** `max(r, 1/r) ≥ 3×`. Pega ITUB4 2019 (÷1000) e 2020 (×780), BRSR6 2020/21 (×205.000),
CGRA4 2025 (÷1000) e MRFG3 (4,7×) — e ignora toda bonificação real da B3 (que raramente passa de 2×).
Números **medidos** contra o cache CVM em 2026-07-14 (`08-RESEARCH.md` §Achado 2/§R-01); o "1.131×"
que constava aqui era um número fantasma."*
Faça a correção equivalente no critério de sucesso 1 da Phase 8 do ROADMAP. **Por que isso importa e
não é cosmético:** um limiar calibrado para 1,13× dispararia em **toda** bonificação de 10% da B3 —
falso positivo em massa — e deixaria a guarda inteira dependendo da qualidade do `.splits` do Yahoo
para **não** gritar. O 3× simétrico torna o SAN-02 robusto **por construção**. A isenção por `.splits`
(D-12) **fica mantida** — apenas deixa de ser load-bearing.

**(2) A conta do SAN-07 (R-02).** `REQUIREMENTS.md` §SAN-07 e `ROADMAP.md` §Phase 8 critério 5 dizem
*"IHCD/AT1 entram no PL dos bancos (`2.03`)"*. **`2.03` não é o PL de banco nenhum.** Corrija para
`2.07` (BBAS3/BBDC4/BRSR6) / `2.08` (ITUB4) — ou, melhor, para o **nome** "Patrimônio Líquido
Consolidado", que é como o `cvm.py` já casa. Marque o SAN-07 como **[x] respondido** e aponte para
`.planning/spikes/san-07-ihcd-at1-fvoci.md`, com o veredito de uma linha: **as duas respostas são NÃO;
o terceiro bug de dados não existe; nenhum knob se move.**

**(3) O legado para a Fase 9 (R-09) — o achado mais valioso da pesquisa.** No `REQUIREMENTS.md`,
junto ao **DATA-03** (a contagem de ações), acrescente uma nota de insumo:
*"O `dfp_cia_aberta_composicao_capital_{ano}.csv` vive **dentro do ZIP que o projeto já baixa** e traz
a contagem **oficial** de ações (`QT_ACAO_TOTAL_CAP_INTEGR`, `QT_ACAO_TOTAL_TESOURO`) — é o insumo que
resolve o DATA-03. **Duas armadilhas medidas:** (i) é chaveado por **`CNPJ_CIA`, não por `CD_CVM`**
(exige join via `cad_cia_aberta.csv`); (ii) a **escala é inconsistente entre empresas** — ITUB4 e
BRSR6 vêm em **MILHARES** de ações, GOAU4/CGRA4/CSNA3/EQTL3/ALUP11/MRFG3 vêm em **unidades**. Usá-lo
cru **reintroduziria a doença do ×1000 por outro caminho**. Validação cruzada: ele confirma o
`impliedSharesOutstanding` do Yahoo com erro < 0,3% em 5 de 5 tickers (`08-RESEARCH.md` §Achado 4)."*
**Não** use isso na Fase 8 — usá-lo aqui para consertar `num_acoes` é exatamente o erro proibido.

**(4) A direção do BUG-JCP está INVERTIDA no repo, e o DATA-01 herdaria o erro.** No
`REQUIREMENTS.md` §**DATA-01**, acrescente a nota medida:
*"⚠️ **A direção do bug é o contrário do que `build.py:104-107` afirma.** O filtro de `cvm.py:169`
casa só `"dividendo"`; o BRSR6 fila o JCP em `6.03.04 "Juros sobre o Capital Próprio Pagos"`, que
**não casa** → `_distribuicoes_proventos` devolve **R$ 36,0 M em 2025 contra R$ 620,0 M reais (18× a
menos)**; e 19×/24×/25×/5× em 2021-2024. Os 4 grandes bancos **escapam por acidente** (filam numa
linha `"Dividendos E Juros sobre o Capital Próprio Pagos"`, que casa o filtro) — por isso o bug só
atinge as ~13 empresas. **E o DPA do Yahoo JÁ INCLUI o JCP:** validado contra o provento real do BRSR6
com erro **< 5% em 4 anos** (2022-2024). Ou seja, **o código prefere a CVM exatamente onde a CVM está
quebrada**, e o Yahoo — que o comentário chama de fallback inferior — é o lado bom. Isso **muda a
estratégia de conserto**: a correção mínima é ampliar o filtro da DFC para `"dividendo" OU "juros
sobre capital"`, não trocar de fonte. Medido em 2026-07-14 contra o cache CVM (Fase 8, plano 08-01,
que já corrige o comentário e adiciona o insumo de detecção `proventos_filtro_amplo`)."*

**Não toque** na lista "NÃO fazer nesta fase" da Phase 8, nem no bloco `**Plans**:` (o orquestrador
cuida dele).
  </action>

  <acceptance_criteria>
    - `grep -c "1.131" .planning/REQUIREMENTS.md` retorna 0
    - `grep -c "1.131" .planning/ROADMAP.md` retorna 0
    - `grep -c "composicao_capital" .planning/REQUIREMENTS.md` retorna ≥ 1
    - `grep -ci "juros sobre o capital próprio pagos" .planning/REQUIREMENTS.md` retorna ≥ 1
      (a direção invertida do BUG-JCP está registrada no DATA-01)
    - `grep -c "CNPJ_CIA" .planning/REQUIREMENTS.md` retorna ≥ 1 (a armadilha da chave está escrita)
    - `grep -c "san-07-ihcd-at1-fvoci" .planning/REQUIREMENTS.md` retorna ≥ 1
    - O texto do SAN-02 no REQUIREMENTS contém "3×" (ou "3x") e a palavra "simétrico"
    - `git diff --name-only` NÃO inclui `config.yaml` nem `calibracao.lock.yaml`
  </acceptance_criteria>

  <verify>
    <automated>test $(grep -c "1.131" .planning/REQUIREMENTS.md .planning/ROADMAP.md | awk -F: '{s+=$2} END {print s}') -eq 0 && grep -q "composicao_capital" .planning/REQUIREMENTS.md && grep -q "CNPJ_CIA" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>

  <done>O "1.131×" sumiu dos dois arquivos, substituído pelos saltos medidos; o SAN-07 aponta para o spike e para a conta certa; a Fase 9 herda o `composicao_capital` com as duas armadilhas escritas E a direção correta do BUG-JCP (a CVM é que perde o JCP; o Yahoo é que o tem).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ZIP da CVM (cache local) → `spike_san07_bancos.py` | arquivo de terceiro; conta ausente é resposta válida |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | o spike virar justificativa para mover um knob | mitigate | O doc declara **explicitamente** "nenhum knob se move" e o veredito é **negativo**; o plano não toca `config.yaml` nem `calibracao.lock.yaml` (verificado por `git diff --name-only` no acceptance) |
| T-08-05 | Repudiation | número não-reproduzível no doc | mitigate | `scripts/spike_san07_bancos.py` re-emite todos os números do doc offline, do cache CVM; o doc cita o comando |
| T-08-06 | Denial of Service | — | accept | Script offline, sem rede, sem escrita em disco |
</threat_model>

<verification>
1. `PYTHONPATH=src .venv/bin/python scripts/spike_san07_bancos.py` → sai 0, imprime os 4 bancos
2. `grep -c "1.131" .planning/REQUIREMENTS.md .planning/ROADMAP.md` → 0 em ambos
3. `.venv/bin/python -m pytest -q` → 0 failed (este plano não toca em teste nenhum; a suíte tem que
   continuar exatamente como estava)
4. `git diff --name-only` → apenas os 5 arquivos de `files_modified`
</verification>

<success_criteria>
- `.planning/spikes/san-07-ihcd-at1-fvoci.md` existe, responde **NÃO** às duas perguntas com número por banco, corrige a conta do PL e declara a anomalia do `Ajustes de Avaliação Patrimonial = 0,00`
- `scripts/spike_san07_bancos.py` re-emite os números offline
- `.planning/REQUIREMENTS.md` e `.planning/ROADMAP.md` não contêm mais o "1.131×"; o SAN-02 fixa o limiar simétrico de 3×
- O `composicao_capital` (com as duas armadilhas) está registrado para a Fase 9
- A **direção invertida do BUG-JCP** está registrada no DATA-01, com os números medidos — a Fase 9 não vai consertar o lado errado
- **Nenhum knob se moveu; `config.yaml` e `calibracao.lock.yaml` não aparecem no diff**
</success_criteria>

<output>
Ao concluir, crie `.planning/phases/08-sanidade-dos-dados-san/08-02-SUMMARY.md`.
</output>
