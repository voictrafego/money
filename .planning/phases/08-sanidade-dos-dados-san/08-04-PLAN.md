---
phase: 08-sanidade-dos-dados-san
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - src/analista/core/sanidade.py
  - tests/test_sanidade_checks.py
  - tests/test_sanidade_limiares.py
  - tests/classificacao.yaml
autonomous: true
requirements: [SAN-01, SAN-02, SAN-03, SAN-04, SAN-05]
must_haves:
  truths:
    - "SAN-01 flaga GOAU4 (2,97×) e CGRA4 (0,001×) e devolve 'não avaliável' para o MRFG3, sem exceção"
    - "SAN-02 é SIMÉTRICO (max(r,1/r) >= 3) e flaga ITUB4 2019/2020, BRSR6 2020/21 e CGRA4 2025"
    - "SAN-02 isenta o salto quando o .splits registra um desdobramento compatível naquele ano (D-12)"
    - "SAN-02 não confunde troca de fonte com salto de dado: par de anos com origem diferente não é avaliável"
    - "SAN-03 tem DOIS sinais: (a) o detector de JCP perdido, 100% interno à CVM, imune ao num_acoes quebrado — pega o BRSR6; e (b) a reconciliação CVM vs Yahoo×num_acoes, que é sinal de consistência, não veredito de quem está certo"
    - "O checar_san03 NÃO assume qual lado é a verdade — ele reporta DIVERGÊNCIA"
    - "_bucket nunca levanta com fator <= 0: o bucket usa abs(fator), e o sinal invertido vive num campo próprio do Aviso"
    - "SAN-04 flaga MRFG3, CSNA3, ALUP11 e EQTL3 pela divergência 3.11 vs 3.11.01, inclusive o caso de sinal invertido do CSNA3"
    - "SAN-05 reporta a violação do clean surplus como DADO, não como exceção"
    - "Um teste congela os valores dos 5 limiares (D-11) e não menciona nenhum ticker"
  artifacts:
    - path: "src/analista/core/sanidade.py"
      provides: "os 5 checks aritméticos + os limiares (D-10) + o bucket de ordem de grandeza (D-07)"
      exports: ["Aviso", "LIMIAR_SAN01", "LIMIAR_SAN02", "LIMIAR_SAN03", "LIMIAR_SAN03_JCP", "LIMIAR_SAN04", "LIMIAR_SAN05", "TOLERANCIA_SPLIT"]
      min_lines: 120
    - path: "tests/test_sanidade_checks.py"
      provides: "os checks provados contra o snapshot congelado, nos tickers que o ROADMAP nomeia"
    - path: "tests/test_sanidade_limiares.py"
      provides: "os limiares congelados — afrouxar um fica vermelho no diff (D-11)"
  key_links:
    - from: "src/analista/core/sanidade.py"
      to: "CompanyData.market_cap / .splits / .lucro_controlador / .origem_num_acoes"
      via: "os checks leem os insumos carimbados pelo plano 08-01"
      pattern: "c\\.(market_cap|splits|lucro_controlador|origem_num_acoes)"
---

<objective>
Os sete checks viram código. Este plano entrega **os cinco checks aritméticos** (SAN-01..SAN-05), os
limiares (D-10, constantes no módulo — **não** vão para `config.yaml` e **não** entram no
`calibracao.lock.yaml`), e o teste que congela esses limiares (D-11). O SAN-06 (never-raise
estrutural) e o `aplicar_sanidade` ficam no plano 08-05.

**Nada aqui conserta nada.** Os checks **leem** o dado sujo e **relatam**. Se um check flagar um
ticker, isso é o sistema funcionando — não um bug para corrigir.

Dois insights de desenho que saíram da medição e que precisam morar **no docstring do módulo**:

- **SAN-01 e SAN-02 são o MESMO bug visto de dois ângulos** (nível vs. série temporal). Ambos nascem
  de `cvm.py:242` lendo a conta `3.99.01.01` sem validar escala nem semântica, e de `build.py:87`
  dividindo `LL / LPA` para inventar `num_acoes`. Escrever isso no módulo é o que impede a Fase 9 de
  "consertar um" e achar que consertou os dois.
- **O SAN-01 roda sobre `c.num_acoes` (pós-`_fator_unit`), não sobre a contagem crua.** É o campo que
  os motores consomem — e é exatamente onde o mascaramento acontece: o `_fator_unit` da ALUP11
  devolve **5** quando o certo é **3**, e a divisão excessiva **cancela** o erro de minoritários de
  1,41× → o fator SAN-01 pós-unit cai para ≈0,98 e **a ALUP11 não é flagada pelo SAN-01**. Isso está
  **correto**: o REQUIREMENTS atribui a ALUP11 ao **SAN-04**. **NÃO "conserte" o `_fator_unit` para
  fazer o SAN-01 acender** — seria consertar dado, e é Fase 9.

Purpose: os asserts **são** o teste de regressão da Fase 9. Precisam existir, disparar e **ser vistos
disparando** antes de qualquer conserto.
Output: `core/sanidade.py` com os 5 checks + os limiares; os testes que os provam nos tickers-alvo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-sanidade-dos-dados-san/08-RESEARCH.md
@.planning/phases/08-sanidade-dos-dados-san/08-CONTEXT.md
@.planning/phases/08-sanidade-dos-dados-san/08-01-SUMMARY.md
@.planning/phases/08-sanidade-dos-dados-san/08-03-SUMMARY.md
@CLAUDE.md
@src/analista/core/fundamentals.py
@src/analista/core/normalizacao.py
@tests/helpers_sanidade.py
@tests/classificacao.yaml

<interfaces>
Insumos disponíveis em `CompanyData` **depois do plano 08-01** (todos com default; ausência é normal):

    ticker, anos, ultimo_ano()
    lucro_liquido        {ano: float}   # CONSOLIDADO (3.11) — sujo, permanece sujo
    lucro_controlador    {ano: float}   # 3.11.01 — NOVO, insumo do SAN-04
    patrimonio_liquido   {ano: float}
    pl_nao_controladores {ano: float}   # NOVO
    lpa_cvm              {ano: float}   # o LPA CRU da CVM — a causa-raiz, pré-divisão
    num_acoes            {ano: float}   # pós-_fator_unit — O CAMPO QUE OS MOTORES CONSOMEM
    origem_num_acoes     {ano: "cvm" | "yahoo_fallback"}   # NOVO

    # 🔴 ATENÇÃO — A DIREÇÃO DO BUG-JCP É O CONTRÁRIO DO QUE O REPO DIZ (medido):
    dividendos           {ano: float}   # DFC da CVM pelo filtro ESTREITO ("dividendo") -> **PERDE O JCP**
                                        #   BRSR6 2025: R$ 36,0 M contra R$ 620,0 M reais (18× a menos).
                                        #   ITUB4/BBAS3/BBDC4 escapam por acidente (linha "Dividendos E
                                        #   Juros sobre o Capital Próprio Pagos" casa o filtro).
                                        #   É SUJO. Continua sujo. Consertar = DATA-01, Fase 9.
    proventos_filtro_amplo {ano: float} # NOVO (08-01): DFC 6.03.* por "dividendo" OU "juros sobre
                                        #   capital" -> **ENXERGA O JCP**. Só o detector consome.
    dpa_por_ano          {ano: float}   # DPA do Yahoo -> **INCLUI O JCP** (bate com o provento real do
                                        #   BRSR6 com erro < 5% em 4 anos). É o lado BOM.
    preco_atual, market_cap, implied_shares_outstanding    # NOVO (SAN-01)
    splits               {"YYYY-MM-DD": fator}             # NOVO, histórico COMPLETO (D-12)
    avisos: list, confianca: str = "nao_avaliada"

Snapshot congelado (plano 08-03): `tests/helpers_sanidade.carregar_snapshot_sanidade()` →
`Dict[ticker, CompanyData]`, offline.

Números MEDIDOS contra os dados reais (para o executor conferir o que os checks têm que produzir):
  SAN-01 fator = num_acoes[ult] × preco / market_cap
    GOAU4 2,969 · CGRA4 0,001 · CSNA3 0,752 · EQTL3 1,496 · ALUP11 ≈0,98 (mascarado) ·
    ITUB4 1,027 · BRSR6 1,001 (sadios) · MRFG3 SEM DADO (404)
  SAN-02 salto = num_acoes[t] / num_acoes[t-1]
    ITUB4 2019 = 0,0010 · ITUB4 2020 = 780,15 · ITUB4 2018 = 1,5187 (split REAL de 1,5 em 2018-11-21)
    BRSR6 2020 ≈ 0,0000 · BRSR6 2021 = 205.099,96 (o .splits do BRSR6 é VAZIO)
    CGRA4 2025 = 0,0010 · ITUB4 2025 = 1,1286 (bonificação REAL: 1,1 × 1,03 = 1,133)
  SAN-03 (JCP) razão = proventos_filtro_amplo / dividendos   (o filtro estreito perde o JCP)
    BRSR6: 19,1× (2021) · 24,1× (2022) · 25,3× (2023) · 5,4× (2024) · 18,2× (2025)
    ITUB4 / BBAS3 / BBDC4: ≈ 1,00× (escapam por acidente)
  SAN-04 razão = lucro_liquido / lucro_controlador  (2025)
    GOAU4 2,967 · EQTL3 1,492 · ALUP11 1,426 · CSNA3 0,752 (SINAL INVERTIDO: LL=-1,51bi, contr.=-2,00bi)
    bancos limpos: ITUB4 minoritários = 4,9% do PL · BBAS3 2,3% · BBDC4 0,3% · BRSR6 0,04%
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: core/sanidade.py — limiares, Aviso, bucket, SAN-01 e SAN-02</name>

  <read_first>
    - src/analista/core/normalizacao.py (o módulo-espelho: funções puras sobre séries, sem I/O — é o
      padrão arquitetural que `sanidade.py` copia)
    - src/analista/core/fundamentals.py (os campos que os checks leem)
    - .planning/phases/08-sanidade-dos-dados-san/08-RESEARCH.md §"Achado 1" (a causa-raiz),
      §"Achado 2" (a série real e a simetria), §"Achado 3" (SAN-01 medido), §"Achado 5" (a forma do
      `.splits` e a tolerância de 20%), §"Achado 8" (o `_fator_unit` corrompido) e §"R-07"
    - .planning/phases/08-sanidade-dos-dados-san/08-CONTEXT.md (D-07, D-09, D-10, D-12)
  </read_first>

  <action>
Novo módulo `src/analista/core/sanidade.py` — **funções puras** sobre `CompanyData`, sem I/O, sem
rede, espelhando `core/normalizacao.py`. Nenhuma dependência nova (`pandera`/`great-expectations`
estão **proibidos**: são 5 asserts aritméticos).

**Os limiares (D-10 — constantes de MÓDULO).** Não vão para `config.yaml` e **não entram no
`calibracao.lock.yaml`**: limiar de detecção **não é knob de valuation** — não move `Ke`, não move
`g`, não move preço. O lock tem exatamente 3 graus de liberdade (`ERP`, `n_fade`, `PIB_real`); um 4º
deixa a suíte vermelha por construção.

- `LIMIAR_SAN01 = 1.5` — **simétrico**: flaga quando `max(f, 1/f) >= 1.5` (desvio > 50%, D-09).
- `LIMIAR_SAN02 = 3.0` — **simétrico**: flaga quando `max(r, 1/r) >= 3.0`. **DECISÃO DO USUÁRIO,
  medida.** Pega ITUB4 2019 (÷1000), ITUB4 2020 (×780), BRSR6 (×205.000), CGRA4 2025 (÷1000) e MRFG3
  (4,7×) — **e ignora toda bonificação real da B3**, que raramente passa de 2×. O "1.131×" que o
  REQUIREMENTS antigo citava **não existe** (é um número fantasma; o plano 08-02 o corrige). Um limiar
  de ~1,13 dispararia em toda bonificação de 10% e desinstalaria a guarda.
- `LIMIAR_SAN03 = 1.5` — simétrico, sobre a razão das **somas** da janela (o sinal de *consistência*
  CVM↔Yahoo — ver Task 2).
- `LIMIAR_SAN03_JCP = 1.10` — o **detector direto de JCP perdido**: flaga quando
  `Σ proventos_filtro_amplo / Σ dividendos > 1.10`. Apertado de propósito, e pode ser: este check é
  **100% interno à CVM**, não passa por `num_acoes` nem pelo Yahoo, e por isso **não tem ruído de
  escala nem de preço**. O medido: BRSR6 entre **5,4×** e **25,3×**; os grandes bancos em **1,00×**.
  Margem enorme dos dois lados.
- `LIMIAR_SAN04 = 0.10` — flaga `|LL/LL_controlador − 1| > 10%`. Pega os 4 nomeados (26%..197%) e
  deixa os 4 bancos limpos (0,04%..4,9%) com margem enorme.
- `LIMIAR_SAN05 = 0.10` — resíduo mediano do clean surplus > 10% do PL.
- `TOLERANCIA_SPLIT = 0.20` — a isenção do D-12 casa quando `|salto / Πfatores_do_ano − 1| < 20%`.
  **Um `==` exato não isentaria nada**: o salto medido do ITUB4 2018 é 1,5187 contra um split
  registrado de 1,5 (1,2% de diferença).

**`Aviso`** — dataclass leve: `check: str` (`"SAN-01"`..), `ano: Optional[int]`, `fator: float`
(adimensional — **nunca** um R$), `bucket: str`, `detalhe: str`.

**`Aviso`** precisa de um campo `sinal_invertido: bool = False` — ver o `_bucket` abaixo e o SAN-04.

**`_bucket(fator) -> str`** (D-07 — ordem de grandeza, **não** magnitude exata): devolve
`f"~1e{round(math.log10(abs(fator)))}"`. GOAU4 2,969 → `"~1e0"`; CGRA4 0,001 → `"~1e-3"`; BRSR6
205.099 → `"~1e5"`. **String, sempre** — o baseline vai serializar isso, e string **não é constante
numérica**, logo o detector do BLIND-04a nem se aproxima. Um re-download do Yahoo mexendo no terceiro
decimal **não** pode virar teste vermelho; silenciar a flag sem corrigir a escala **tem** que quebrar.

🔴 **`_bucket` NÃO PODE LEVANTAR com fator ≤ 0.** `math.log10` de um número **negativo** levanta
`ValueError`, e `log10(0)` levanta `ValueError` também. E o `checar_san04` **precisa** flagar
justamente o caso de **sinal invertido** (CSNA3: `LL = −1,51 bi` contra `LL_controlador = −2,00 bi`).
Se o `_bucket` estourar ali, o `try/except` do `aplicar_sanidade` (plano 08-05) **engoliria a exceção
e converteria uma detecção REAL em "não avaliável"** — o oposto exato do requisito, e em silêncio.
Portanto:
- `_bucket` opera sobre **`abs(fator)`**;
- `fator == 0` → devolve a string `"~0"` (**nunca** chama `log10`);
- o sinal vive **fora** do bucket, no campo `Aviso.sinal_invertido: bool`.
Escreva um teste para isso no arquivo dos limiares (Task 3): `_bucket` sobre um fator negativo e sobre
zero **não levanta**.

**`checar_san01(c) -> Optional[Aviso]` — escala: `num_acoes × preço ≈ market cap`.**
- Referência = `c.market_cap` (o Yahoo satisfaz exatamente `marketCap = preço ×
  impliedSharesOutstanding`; e o `impliedSharesOutstanding` bate com a contagem oficial da CVM com
  erro **< 0,3% em 5 de 5** — a referência é sólida, não é palpite).
- `fator = c.num_acoes[ultimo_ano] × c.preco_atual / c.market_cap`. **Sobre `c.num_acoes`
  pós-`_fator_unit`** — o campo que os motores consomem.
- Se `market_cap`, `preco_atual` ou `num_acoes[ult]` faltarem ou forem zero → devolve `None`
  (**não avaliável**). **Nem flag, nem exceção.** Este é o caso do **MRFG3** (404 no Yahoo) e é o caso
  de teste vivo do SAN-06.
- Flaga quando `max(fator, 1/fator) >= LIMIAR_SAN01`.
- ⚠️ **NUNCA use `sharesOutstanding` como referência.** Ele é só a classe negociada (ITUB4: 5,4bn = só
  as PN); o `impliedSharesOutstanding` é ON+PN (11,02bn), que é a base da contagem da CVM (`LL/LPA`).
  Comparar com `sharesOutstanding` produziria falso positivo de ~2× em **toda** empresa com PN.

**`checar_san02(c) -> List[Aviso]` — salto de `num_acoes` ano-a-ano sem evento societário.**
- Para cada par de anos consecutivos com `num_acoes` em ambos: `r = n[t] / n[t-1]`.
- **SIMÉTRICO** (`max(r, 1/r) >= LIMIAR_SAN02`). Isto é obrigatório e foi medido: o salto aparece
  **duas vezes** — na entrada e na saída do ano quebrado (BRSR6: `0,0000×` em 2020 e `205.099×` em
  2021; ITUB4: `0,0010×` em 2019 e `780×` em 2020). Um check que só olhe *aumento* pega o ano **são**
  (o seguinte) e deixa o ano **doente** passar.
- **Fronteira de fonte (Achado 2c):** se `c.origem_num_acoes[t] != c.origem_num_acoes[t-1]`, o par
  **não é avaliável** — pule sem flagar. O EQTL3 mistura três bases (CVM 2016-17, fallback Yahoo
  2018-24, CVM 2025) e a troca de fonte produz um salto **artificial** que não é bug de dado; é troca
  de fonte. Confundir os dois envenena o baseline.
- **Isenção por split (D-12):** o salto é **isento** quando o `c.splits` registra desdobramento(s) no
  ano `t` cujo produto `Πfatores` satisfaz `|r / Πfatores − 1| < TOLERANCIA_SPLIT`. Extraia o ano da
  chave ISO com `int(chave[:4])` — **não construa `datetime`** e **não compare datas**: o index
  original é tz-aware e o `.splits` vazio vem com `dtype=object`; para o SAN-02 só o **ano** importa.
  `c.splits` vazio (`{}`) → **nenhuma isenção**, o salto dispara. Falha na direção segura: se o
  `.splits` faltar, o falso positivo é **visível**, não silencioso.
- `bucket` do aviso usa o `r` **cru** (direcional), não o simétrico — assim `"~1e-3"` (÷1000) e
  `"~1e3"` (×1000) continuam distinguíveis no baseline.
- Verificação de sanidade do próprio check, contra os números medidos: a isenção é **load-bearing
  exatamente uma vez** (ITUB4 2018 = 1,5187×, split real de 1,5 em `2018-11-21`) — e com o limiar de
  3× esse caso **já fica abaixo da linha** e nem precisa dela. O ITUB4 2019 (`.splits` **não registra
  nada em 2019**) e o BRSR6 (`.splits` **vazio**) **disparam e não são isentos**. ✅

**Docstring do módulo (obrigatório):** declare (a) que SAN-01 e SAN-02 são o mesmo bug em dois
ângulos, com a causa-raiz nomeada (`cvm.py:242` + `build.py:87`); (b) que os limiares **não são knobs
de valuation** e por isso não entram no lock; (c) que **nada aqui conserta nada** — os checks são o
teste de regressão da Fase 9 e apagar uma flag consertando o dado aqui destrói a prova.
  </action>

  <acceptance_criteria>
    - `grep -c "^LIMIAR_SAN0[12] = " src/analista/core/sanidade.py` retorna 2
    - `grep -c "^TOLERANCIA_SPLIT = " src/analista/core/sanidade.py` retorna 1
    - `grep -c "def checar_san01\|def checar_san02\|def _bucket" src/analista/core/sanidade.py` retorna 3
    - `grep -c "sharesOutstanding" src/analista/core/sanidade.py` retorna 0 (a referência é o `market_cap`)
    - `grep -c "import yfinance\|requests\|open(" src/analista/core/sanidade.py` retorna 0 (funções puras, zero I/O)
    - `git diff --name-only` NÃO inclui `calibracao.lock.yaml` nem `config.yaml`
    - `PYTHONPATH=src .venv/bin/python -c "from analista.core import sanidade; print(sanidade._bucket(0.001), sanidade._bucket(2.969), sanidade._bucket(205099.0))"` imprime `~1e-3 ~1e0 ~1e5`
    - `_bucket` sobre um fator **negativo** e sobre **zero** **não levanta** `ValueError`
    - `grep -c "sinal_invertido" src/analista/core/sanidade.py` retorna ≥ 1
  </acceptance_criteria>

  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -c "
from analista.core import sanidade as s
assert s._bucket(0.001) == '~1e-3' and s._bucket(2.969) == '~1e0' and s._bucket(205099.0) == '~1e5'
assert s.LIMIAR_SAN02 == 3.0 and s.LIMIAR_SAN01 == 1.5
assert s._bucket(-0.752) == '~1e0'   # sinal invertido (CSNA3) NAO pode estourar
assert s._bucket(0.0) == '~0'        # zero NAO pode estourar
print('OK')"</automated>
  </verify>

  <done>`sanidade.py` existe com os limiares, o `Aviso` (com `sinal_invertido`), o `_bucket` em string que **nunca levanta** com fator ≤ 0, o SAN-01 (não avaliável sem market_cap) e o SAN-02 simétrico com isenção por split e fronteira de fonte.</done>
</task>

<task type="auto">
  <name>Task 2: SAN-03, SAN-04 e SAN-05</name>

  <read_first>
    - src/analista/core/sanidade.py (o arquivo modificado — o que a Task 1 escreveu)
    - src/analista/ingest/cvm.py (`_distribuicoes_proventos` — de onde vem o `dividendos` da CVM, com
      JCP incluído) e src/analista/ingest/build.py:104-113 (o BUG-JCP já documentado: o Yahoo perde o
      JCP)
    - .planning/phases/08-sanidade-dos-dados-san/08-RESEARCH.md §"Achado 4" (a mecânica dos
      minoritários, provada, e o caso de sinal invertido do CSNA3), §"Achado 7" (SAN-03/SAN-05) e
      §"R-06" (os checks se contaminam — e isso é correto)
  </read_first>

  <action>
Três checks a mais no mesmo módulo. Todos puros, todos never-raise, todos devolvendo `None` quando o
insumo falta (**não avaliável ≠ limpo**).

**`checar_san03(c) -> List[Aviso]` — proventos. DOIS sinais, e a ordem importa.**

🔴 **LEIA ISTO ANTES DE CODAR — a premissa que o repo escreve está INVERTIDA (medido).**
O comentário de `build.py:104-107` afirma que a CVM traz *"div + JCP completos"* e que *"o Yahoo perde
o JCP"*. **É o contrário.** `cvm.py:169` casa só `"dividendo"`, e o BRSR6 fila o JCP em
`6.03.04 "Juros sobre o Capital Próprio Pagos"` → `c.dividendos` sai **18× menor** que o real em 2025.
E o **DPA do Yahoo INCLUI o JCP** (bate com o real do BRSR6 a **< 5% em 4 anos**). O plano 08-01
corrige esse comentário e adiciona o insumo. **Não escreva o check assumindo que a CVM é a verdade.**

**Sinal (a) — o detector direto de JCP perdido. É o que vale. 100% interno à CVM.**
- `r_jcp = Σ c.proventos_filtro_amplo / Σ c.dividendos` (somas sobre a janela, só anos com ambos).
- Flaga `SAN-03` quando `r_jcp > LIMIAR_SAN03_JCP`.
- **Por que este é o sinal bom:** ele **não passa por `num_acoes`** e **não passa pelo Yahoo**. É
  imune à contaminação cruzada do R-06. No BRSR6, cujo `num_acoes` está quebrado em **205.000×**, o
  sinal (b) abaixo dispararia **pelo motivo errado** (escala, não JCP) — e a Fase 9 consertaria o JCP,
  veria o SAN-03 **continuar vermelho** por causa do `num_acoes`, e **não conseguiria provar nada
  ticker a ticker**. Isso esvaziaria o teste de regressão do DATA-01. O sinal (a) resolve isso: ele
  **só** apaga quando o JCP for capturado.
- Denominador vazio ou zero → **não avaliável** (não entra na lista).

**Sinal (b) — a reconciliação de consistência CVM ↔ Yahoo.** (É o que o REQUIREMENTS SAN-03 pede
literalmente; mantenha-o, mas **rotulado pelo que ele é**.)
- `r = Σ c.dividendos / Σ (c.dpa_por_ano[ano] × c.num_acoes[ano])`, só anos com ambos. Razão das
  **SOMAS**, não ano-a-ano: a data de **pagamento** do Yahoo e o ano do **fluxo de caixa** da CVM não
  casam exatamente, e uma razão anual ruidosa produziria falso positivo. A soma da janela cancela o
  descasamento.
- Flaga quando `max(r, 1/r) >= LIMIAR_SAN03`.
- 🔴 **O check NÃO afirma qual lado está certo.** Ele reporta **divergência**. O `detalhe` do `Aviso`
  deve dizer *"CVM e Yahoo divergem — a causa pode ser JCP perdido (lado CVM) OU `num_acoes` quebrado
  (lado do produto). Cruze com SAN-01/SAN-02."* Escrever "a CVM está certa" aqui é reproduzir o erro
  do `build.py:104`.
- **Espere cascata, e ela é legítima:** um `num_acoes` quebrado envenena o denominador → o sinal (b)
  dispara nos tickers de escala quebrada. O dado *está* inconsistente nessa dimensão. Registre; não
  invente exceção para "descontaminar". **Mas é por isso que o sinal (a) existe.**

**`checar_san04(c) -> Optional[Aviso]` — `PL` e `lucro` na mesma base (o bug dos minoritários).**
- No último ano com **ambos** `lucro_liquido` e `lucro_controlador`: `razao = LL / LL_controlador`.
- Flaga quando `abs(razao − 1) > LIMIAR_SAN04` **ou** quando os **sinais divergem**
  (`LL * LL_controlador < 0`). O **CSNA3** é exatamente o caso de sinal invertido: os minoritários
  tiveram lucro (+0,496 bi) enquanto o controlador teve prejuízo (−2,002 bi), e o LL consolidado sai
  *menos negativo* que o do controlador → a razão dá **0,752** e a contagem de ações sai para
  **baixo**, não para cima. Um check que só olhasse `razao > 1` deixaria o CSNA3 passar.
- `lucro_controlador` ausente (empresa sem subsidiária consolidada) → `None`, **não avaliável**.
- 🔴 **O `Aviso` do sinal invertido:** `bucket = _bucket(abs(razao))` e `sinal_invertido = True`.
  **Nunca** passe uma razão negativa para o `_bucket` esperando que ele se vire — `log10` de negativo
  levanta `ValueError`, e o `try/except` do `aplicar_sanidade` (plano 08-05) **engoliria a exceção e
  transformaria a detecção do CSNA3 em "não avaliável"**, em silêncio. Seria o oposto do requisito.
- **A prova de que este check está certo, medida:** a razão dos minoritários do GOAU4 é **2,967×** e o
  fator SAN-01 medido do GOAU4 é **2,969×** — é o **mesmo número**. O "3× do GOAU4" **É** o bug de
  minoritários. Idem CSNA3: **0,752×** dos dois lados.
- **O MRFG3 é pego aqui** (o SAN-04 é 100% CVM e não depende do Yahoo), e **a ALUP11 também** — é
  onde o REQUIREMENTS a atribui, e é por isso que o SAN-01 não precisa flagá-la.

**`checar_san05(c) -> Optional[Aviso]` — clean surplus (`ΔB ≈ LL − DIV`).**
- Para cada par de anos consecutivos com `patrimonio_liquido`, `lucro_liquido` e `dividendos`:
  `residuo[t] = ((PL[t] − PL[t−1]) − (LL[t] − DIV[t])) / abs(PL[t−1])` (adimensional).
- Flaga quando a **mediana** dos `abs(residuo)` da janela `> LIMIAR_SAN05` (mediana, não máximo:
  um único ano de recompra grande não pode acender a flag sozinho).
- Menos de 2 anos utilizáveis → `None` (não avaliável).
- **Reportado como DADO, não como exceção** (REQUIREMENTS SAN-05, ROADMAP critério 3): o SAN-05 é
  simultaneamente **detector de bug** e **pré-condição de validade do RIM**. O `Aviso` carrega o
  resíduo mediano como `fator` (adimensional) e o `bucket` — **nunca** um R$.
- **Espere cascata:** num ticker de `num_acoes` quebrado, o resíduo do clean surplus acusa violação
  por causa do **bug de base** (Achado 4), não por dirty surplus. **Isso é feature, não bug.** E o
  spike SAN-07 (plano 08-02) já mediu que o dirty surplus real dos bancos é **0,03%–0,59% do PL** —
  ou seja, quando o SAN-05 dispara em cima do limiar de 10%, **não é FVOCI**: é bug de dado.
  Escreva essa frase no docstring do check — ela poupa a Fase 9 de uma caça ao fantasma.
  </action>

  <acceptance_criteria>
    - `grep -c "def checar_san03\|def checar_san04\|def checar_san05" src/analista/core/sanidade.py` retorna 3
    - `grep -c "^LIMIAR_SAN0[345] = \|^LIMIAR_SAN03_JCP = " src/analista/core/sanidade.py` retorna 4
    - `grep -c "proventos_filtro_amplo" src/analista/core/sanidade.py` retorna ≥ 1 (o sinal (a) do SAN-03 existe)
    - `grep -ci "a CVM está certa\|CVM é a fonte correta\|CVM traz.*completo" src/analista/core/sanidade.py` retorna 0
      (o check reporta **divergência**, não veredito de quem está certo)
    - `grep -vc '^#' src/analista/core/sanidade.py` > 0 e `grep -v '^#' src/analista/core/sanidade.py | grep -c "raise "` retorna 0 (nenhum check levanta)
    - `git diff --name-only` NÃO inclui `calibracao.lock.yaml` nem `config.yaml`
  </acceptance_criteria>

  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -c "
from analista.core import sanidade as s
from analista.core.fundamentals import CompanyData
vazio = CompanyData(ticker='ZZZZ9')
for fn in (s.checar_san01, s.checar_san04, s.checar_san05):
    assert fn(vazio) is None, fn.__name__
assert s.checar_san02(vazio) == [] and s.checar_san03(vazio) == []
print('OK — nenhum check levanta sobre CompanyData vazio')"</automated>
  </verify>

  <done>Os 5 checks existem, são puros, nunca levantam, e devolvem "não avaliável" quando o insumo falta.</done>
</task>

<task type="auto">
  <name>Task 3: provar os 5 checks no snapshot e congelar os limiares</name>

  <read_first>
    - src/analista/core/sanidade.py (o que as Tasks 1 e 2 escreveram)
    - tests/helpers_sanidade.py (o loader offline do snapshot congelado — plano 08-03)
    - tests/classificacao.yaml (os testes novos PRECISAM de entrada aqui, senão a coleta inteira
      quebra — R-10)
    - tests/helpers_blindagem.py, funções `detectar_ticker_com_valor_cravado` (linhas 271-325) e
      `_tem_nivel_cravado` (linha 240) — **a regra exata do BLIND-04a**. Leia antes de escrever
      qualquer assert.
    - .planning/phases/08-sanidade-dos-dados-san/08-RESEARCH.md §"BLIND-04a" (a tabela de armadilhas)
  </read_first>

  <action>
**(a) `tests/test_sanidade_checks.py`** — categoria `contrato`. Roda **sobre o snapshot congelado**
(offline, determinístico — teste que chama a rede não é teste).

Os alvos que o ROADMAP nomeia, e que este arquivo tem que provar:
- `test_san01_flaga_os_tickers_de_escala_quebrada` — GOAU4 e CGRA4 têm aviso SAN-01.
- `test_san01_nao_flaga_os_tickers_sadios` — ITUB4 e BRSR6 (fator 1,03 e 1,00 em 2025) **não** têm
  aviso SAN-01. **Zero falso positivo é metade do valor da guarda.**
- `test_san01_e_nao_avaliavel_sem_dado_de_mercado` — o **MRFG3** (404 no Yahoo) devolve `None`:
  **nem flag, nem exceção**. É o caso de teste vivo do never-raise.
- `test_san02_flaga_o_salto_de_escala` — ITUB4 e BRSR6 têm aviso SAN-02.
- `test_san02_isenta_o_desdobramento_real` — o ano do split real de 1,5 do ITUB4 (2018) **não**
  produz aviso; nem a bonificação de 2025.
- `test_san03_flaga_o_jcp_perdido` — o **BRSR6** (JCP filado em `6.03.04 "Juros sobre o Capital
  Próprio Pagos"`, fora do filtro `"dividendo"`) acende **SAN-03**. É o ticker real do bug, não um
  genérico. **Cita ticker → NENHUMA constante numérica no assert:** `assert "SAN-03" in
  {a.check for a in avisos}`. **Jamais** `assert razao > 5`.
- `test_san03_nao_flaga_o_grande_banco_que_escapa_por_acidente` — o **ITUB4** (que fila numa linha
  `"Dividendos E Juros sobre o Capital Próprio Pagos"`, casando o filtro estreito) **não** acende o
  sinal de JCP perdido. Zero falso positivo.
- `test_san04_flaga_a_base_divergente` — MRFG3, CSNA3, ALUP11 e EQTL3 têm aviso SAN-04.
- `test_san04_nao_flaga_os_bancos` — ITUB4, BBAS3, BBDC4 e BRSR6 **não** têm aviso SAN-04.
- `test_san05_reporta_o_clean_surplus_como_dado` — o check devolve um `Aviso` (ou `None`), **nunca**
  levanta, mesmo num ticker com PL/lucro sujos.

**🔴 A REGRA QUE QUEBRA A SUÍTE (BLIND-04a).** Um teste é **OFENSOR** quando o corpo contém
**AO MESMO TEMPO**: (i) um literal de ticker que é chave do `data/ticker_map.json` — ou um nome de
**constante de módulo** cujo valor contenha um (uma linha `TICKERS = ["GOAU4"]` no topo **contamina
toda função** que use o nome `TICKERS`) — **E** (ii) uma constante numérica **não-trivial**
(∉ {0.0, 1.0, 0.5, 2.0}) que chegue a um assert (direto, via variável local, via constante de módulo,
ou via helper que contenha um assert).

Consequência concreta e **medida**: `assert len(sujos) == 41` num teste que cite um ticker é
**OFENSOR** e quebra a suíte.

**Regra prática para este arquivo inteiro: os asserts são de PERTINÊNCIA, não de valor.**
`assert "SAN-01" in {a.check for a in avisos}` ✅ (tem ticker, **zero** constante numérica).
`assert fator > 1.5` ❌ (é ofensor). **Nenhum número não-trivial neste arquivo.** Se você sentir
necessidade de assertar sobre um fator, asserte sobre o **bucket** (que é `str`).

**(b) `tests/test_sanidade_limiares.py`** — categoria `invariante`. **D-11: congela os valores dos
limiares.** Afrouxar um limiar até a flag calar é o atalho mais tentador da Fase 9, e é um diff de uma
linha que ninguém nota. Aqui ele fica **vermelho** e tem que ser encarado.
- `test_limiares_de_sanidade_estao_congelados` — os 5 limiares + a `TOLERANCIA_SPLIT` batem com os
  valores esperados.
- `test_limiar_do_san02_e_simetrico` — prova a simetria: para `r` e `1/r`, o check enxerga o mesmo
  fator.
- `test_limiares_nao_vivem_no_config_nem_no_lock` (D-10) — nenhuma chave `LIMIAR_SAN*` aparece em
  `config.yaml` nem em `calibracao.lock.yaml`, e o `calibracao.lock.yaml` continua com exatamente os
  3 graus de liberdade de sempre. **Limiar de detecção não é knob de valuation.**

**🔴 Neste arquivo, o número é permitido — o TICKER é que é PROIBIDO.** A condição (i) do detector é
o literal de ticker; sem ele, a condição nunca fecha e o teste é seguro, por mais número que tenha.
**`tests/test_sanidade_limiares.py` NÃO PODE CONTER NENHUM LITERAL DE TICKER.** Nem em comentário de
código gerado por engano, nem em nome de variável, nem em docstring. Se precisar de um `CompanyData`
sintético, use um ticker **fictício** que não exista no `ticker_map.json` (ex.: `"ZZZZ9"`).

**(c) `tests/classificacao.yaml` — NA MESMA TASK.** Colha os nodeids com
`BLIND_BOOTSTRAP=1 .venv/bin/python -m pytest --collect-only -q -k "sanidade_checks or sanidade_limiares"`
e adicione cada um: `tests/test_sanidade_checks.py::*` → **`contrato`**;
`tests/test_sanidade_limiares.py::*` → **`invariante`**. Bloco novo, com cabeçalho de comentário
identificando Fase 8 / plano 08-04 e explicando **por que** os limiares são `invariante` (são a trava
que denuncia o afrouxamento — se saírem daqui, afrouxar limiar volta a ser invisível).

**Se algum check não flagar o ticker que o ROADMAP nomeia:** o que está errado é o **check**, não o
teste. **NUNCA** afrouxe o limiar, marque `xfail` casual, troque `xfail` por `skip` nem delete o
assert para a suíte ficar verde. (A única exceção já conhecida e **correta** é a ALUP11 no SAN-01 —
ela é pega pelo SAN-04, como o REQUIREMENTS manda.)
  </action>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest -k "sanidade_checks"` sai 0
    - `.venv/bin/python -m pytest -k "sanidade_limiares"` sai 0
    - `.venv/bin/python -m pytest -k "nenhum_teste_de_calibracao_crava_ticker_em_reais"` sai 0
      (o meta-teste do BLIND-04a continua verde — os testes novos não são ofensores)
    - `grep -c "test_sanidade_checks\|test_sanidade_limiares" tests/classificacao.yaml` retorna ≥ 14
    - `.venv/bin/python -m pytest -k "san03_flaga_o_jcp_perdido"` sai 0 (o BRSR6 acende SAN-03)
    - `pytest -k limiares` cobre `_bucket` com fator **negativo** e com **zero**, sem `ValueError`
    - `grep -f <(python3 -c "import json;[print(t) for t in json.load(open('data/ticker_map.json'))]") tests/test_sanidade_limiares.py | wc -l` retorna 0
      (nenhum literal de ticker no arquivo dos limiares)
    - `.venv/bin/python -m pytest` sai 0 (0 failed) — a suíte inteira coleta e passa
    - `git diff --name-only` NÃO inclui `calibracao.lock.yaml` nem `config.yaml`
  </acceptance_criteria>

  <verify>
    <automated>.venv/bin/python -m pytest -k "sanidade_checks or sanidade_limiares or nenhum_teste_de_calibracao_crava_ticker_em_reais" -q && .venv/bin/python -m pytest -q</automated>
  </verify>

  <done>Os 5 checks estão provados nos tickers que o ROADMAP nomeia; os limiares estão congelados por teste `invariante`; o BLIND-04a continua verde; a suíte inteira passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot YAML (dado sujo, de terceiro) → `core/sanidade.py` | valores ausentes, zero, negativos e absurdos (`num_acoes = 20.797`) são o **caso esperado** — é o que a fase existe para detectar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Denial of Service | divisão por zero / `log10(0)` / `None` nos checks sobre dado sujo | mitigate | Todo check devolve `None` (não avaliável) quando o insumo falta ou o denominador é zero; `_bucket` só é chamado sobre fator estritamente positivo. Provado por `pytest -k sanidade_checks` sobre os 104 tickers do snapshot, MRFG3 incluído. |
| T-08-11 | Tampering | afrouxar um limiar até a flag calar (o atalho da Fase 9) | mitigate | `tests/test_sanidade_limiares.py` (categoria `invariante`) congela os 6 valores; o diff de uma linha fica **vermelho**. |
| T-08-12 | Elevation of privilege | limiar de detecção virar knob de valuation | mitigate | `test_limiares_nao_vivem_no_config_nem_no_lock` (D-10): nenhum `LIMIAR_SAN*` em `config.yaml` nem no `calibracao.lock.yaml`; os 3 graus de liberdade permanecem 3. |
</threat_model>

<verification>
1. `.venv/bin/python -m pytest -q` → 0 failed
2. `.venv/bin/python -m pytest -k "sanidade_checks or sanidade_limiares" -q` → verde, **offline**
3. `.venv/bin/python -m pytest -k blindagem -q` → verde (BLIND-04a e o orçamento de knobs intactos)
4. `git diff --name-only` → apenas os 4 arquivos de `files_modified`; **nunca** `config.yaml` nem `calibracao.lock.yaml`
5. `git diff src/analista/ingest/` → **vazio** (este plano não toca o ingest: nada de conserto de dado)
</verification>

<success_criteria>
- `core/sanidade.py` entrega os 5 checks como funções puras, sem I/O, sem dependência nova
- SAN-01 pega GOAU4 e CGRA4, não pega ITUB4/BRSR6 sadios, e é **não avaliável** no MRFG3
- SAN-02 é **simétrico (3×)**, isenta desdobramento real e não confunde troca de fonte com salto
- SAN-04 pega MRFG3, CSNA3, ALUP11, EQTL3 (inclusive o sinal invertido do CSNA3) e deixa os bancos limpos
- SAN-03 tem **dois sinais**: o detector direto de JCP perdido (interno à CVM, imune ao `num_acoes` quebrado — pega o **BRSR6**, deixa o **ITUB4** limpo) e a reconciliação de consistência CVM↔Yahoo, que **reporta divergência sem eleger um lado como verdade**
- SAN-05 reporta o clean surplus **como dado**
- `_bucket` **nunca levanta** com fator ≤ 0; o sinal invertido do CSNA3 vira `Aviso.sinal_invertido`, não uma exceção engolida
- Os limiares estão congelados por teste `invariante` e **não** entram no `calibracao.lock.yaml`
- **Zero conserto de dado:** `git diff src/analista/ingest/` é vazio
- Todos os testes novos têm entrada em `tests/classificacao.yaml`
</success_criteria>

<output>
Ao concluir, crie `.planning/phases/08-sanidade-dos-dados-san/08-04-SUMMARY.md`.
</output>
