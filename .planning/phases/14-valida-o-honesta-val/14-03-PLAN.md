---
phase: 14-valida-o-honesta-val
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - scripts/montar_cesta_holdout.py
  - tests/fixtures/holdout_v24.yaml
  - tests/test_holdout_cesta.py
  - tests/classificacao.yaml
autonomous: true
requirements: [VAL-02, VAL-03, VAL-05]

must_haves:
  truths:
    - "A cesta é estratificada por arquétipo (≥6 por estrato quando o universo permite) + 10 difíceis deliberados disjuntos da cota, montada por regra determinística escrita ANTES."
    - "CONCESSAO_FINITA existe como estrato (19 membros) porque o montador replica eh_concessionaria (build.py:168); sem isso o carve-out ficaria vazio e a cesta validaria um roteamento que a produção não usa."
    - "CRESCIMENTO tem só 4 nomes no universo — usa os 4 e MARCA cota mínima não atingida (honesto, não inventa)."
    - "COMMIT 1: holdout_v24.yaml nasce SÓ com fair_value (faixa Graham+Bazin), fonte/data, e cabeçalho com regra de seleção + snapshot-hash — ZERO v_modelo, ZERO excecao_nota."
  artifacts:
    - path: "scripts/montar_cesta_holdout.py"
      provides: "Montador determinístico: snapshot + eh_concessionaria + classificar + Graham/Bazin + seleção D-05/D-06/D-07"
      contains: "eh_concessionaria"
    - path: "tests/fixtures/holdout_v24.yaml"
      provides: "Fixture do hold-out — Commit 1 (fair_value only, linhas separadas para git blame)"
      contains: "fair_value"
    - path: "tests/test_holdout_cesta.py"
      provides: "Teste de composição da cesta (VAL-02): ≥6/arquétipo, 10 difíceis, CRESCIMENTO marcado"
      contains: "def test"
  key_links:
    - from: "scripts/montar_cesta_holdout.py"
      to: "src/analista/core/arquetipo.py::classificar"
      via: "estratificação usa a fonte única de classificação (com eh_concessionaria replicado)"
      pattern: "arquetipo\\.classificar"
    - from: "scripts/montar_cesta_holdout.py"
      to: "src/analista/core/lentes.py"
      via: "fair_value = faixa [min,max] de preco_justo_graham + preco_teto_bazin"
      pattern: "preco_justo_graham|preco_teto_bazin"
---

<objective>
Montar a cesta estratificada do hold-out por regra determinística e gravar o **Commit 1**:
`holdout_v24.yaml` SÓ com `fair_value` (faixa Graham+Bazin), sem `v_modelo`. É o primeiro dos dois
commits load-bearing do D-09 — o `git log` vai provar que os fair values foram cravados ANTES de
rodar o modelo. Reusa peças prontas (`carregar_snapshot_sanidade`, `arquetipo.classificar`,
`core/lentes`); o único risco é reconstruir divergente (`eh_concessionaria` faltando).

Purpose: um substrato de validação honesto e independente do modelo/sell-side (Graham+Bazin partem
de lucro/dividendo real, não de preço), estratificado para incluir os difíceis (onde o modelo pode
falhar), não só o meio da distribuição.

Output: `montar_cesta_holdout.py` (builder), `holdout_v24.yaml` (Commit 1, fair_value only),
`test_holdout_cesta.py` (composição).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-valida-o-honesta-val/14-RESEARCH.md
@.planning/phases/14-valida-o-honesta-val/14-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contratos verificados no repo (2026-07-20). O montador é ~template de scripts/spike_eng_rim_104.py -->

scripts/spike_eng_rim_104.py — TEMPLATE quase pronto (imports, loop, eh_concessionaria mirror, cfg offline):
  _ROOT/.../ sys.path.insert(src, tests); import helpers_blindagem as hb; import helpers_sanidade as hs
  from analista.core import arquetipo, lentes, motores; from analista.report import report
  from analista.ingest import macro

tests/helpers_sanidade.py — carregar_snapshot_sanidade(CAMINHO_SNAPSHOT_LIMPO), CAMINHO_SNAPSHOT_LIMPO,
  falhas_do_snapshot(...)   # NÃO popula c.eh_concessionaria (fica False) — mirror obrigatório abaixo

MIRROR OBRIGATÓRIO de build.py:168 (senão CONCESSAO_FINITA fica vazio):
  SETORES_CONCESSIONARIA = ("energia", "saneamento", "água", "gás")
  c.eh_concessionaria = any(t in (c.setor or "").lower() for t in SETORES_CONCESSIONARIA)

src/analista/core/lentes.py — fair_value (mesmo padrão de backtest._graham/_bazin):
  preco_justo_graham(lpa, vpa) -> Number     # exige lpa>0 e vpa>0, senão indefinido
  vpa(patrimonio_liquido, num_acoes) -> Number
  dpa_medio(dpas, n=5) -> Number
  preco_teto_bazin(dpa_med, dy_minimo=BAZIN_DY_MIN) -> Number   # exige dpa>0, senão indefinido

src/analista/core/arquetipo.py — classificar(c, cfg).chave  → estrato (arquétipos vivos:
  FINANCEIRA, PAGADORA_MADURA, CICLICA, CRESCIMENTO, CONCESSAO_FINITA)

Contagens MEDIDAS (RESEARCH, com eh_concessionaria): ciclica 53 / concessao_finita 19 / financeira 17 /
  pagadora_madura 11 / crescimento 4 (<6 → MARCAR). Lentes: 93 ambas / 7 só Bazin / 3 só Graham / 1
  nenhuma (AZUL4 → D-03: fica na cesta SEM fair_value, fora do jackknife, reportado).
Baldes difíceis MEDIDOS: P/B<1=32, prejuízo≤3a=19, payout>100%=SÓ 2 (AGRO3,AURE3 — balde raso),
  book pequeno vários (2 negativos). A regra NÃO pode exigir N fixo por balde.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Montador determinístico da cesta (scripts/montar_cesta_holdout.py)</name>
  <files>scripts/montar_cesta_holdout.py</files>
  <read_first>
    - .planning/phases/14-valida-o-honesta-val/14-PATTERNS.md §"Montador da cesta estratificada" (template + landmine eh_concessionaria + loop core + ordenação D-05)
    - .planning/phases/14-valida-o-honesta-val/14-RESEARCH.md §"Cesta estratificada" (contagens reais + regra de seleção + os 10 difíceis + landmine crítica)
    - scripts/spike_eng_rim_104.py (o template: imports, cfg offline, eh_concessionaria mirror, loop dos 104)
    - src/analista/core/lentes.py (preco_justo_graham, preco_teto_bazin, dpa_medio, vpa — condições de indefinição)
  </read_first>
  <action>
    Crie `scripts/montar_cesta_holdout.py` espelhando `scripts/spike_eng_rim_104.py` (imports, cfg
    offline com `macro.carimbar_beta_setorial`, loop sobre `carregar_snapshot_sanidade` pulando
    `falhas_do_snapshot`). **LANDMINE OBRIGATÓRIA:** antes de classificar, replique `build.py:168`:
    `c.eh_concessionaria = any(t in (c.setor or "").lower() for t in ("energia","saneamento","água","gás"))`
    — sem isso CONCESSAO_FINITA fica vazio e a cesta valida um roteamento que a produção não usa.
    Para cada ticker: (a) `arquetipo.classificar(c, cfg).chave` = estrato; (b) `fair_value` = faixa
    `[min(Graham,Bazin), max(Graham,Bazin)]` entre as lentes DEFINIDAS (D-02) — Graham indefinido se
    lpa≤0/vpa≤0; Bazin indefinido se dpa≤0; **nenhuma definida → sem fair_value** (D-03, degradação
    reportada, NÃO exclusão silenciosa). **Seleção determinística (D-05):** por estrato, ordene por
    `market_cap` desc (desempate alfabético por ticker), pegue os **6 primeiros** (todos os 4 de
    CRESCIMENTO, MARCANDO cota<6 — D-07). **10 difíceis (D-06):** 4 baldes (`P/B<1`, prejuízo≤0 em
    qualquer dos últimos 3 anos, payout>100%, menor `patrimonio_liquido[ult]`); pegue TODOS do balde
    raso (payout>100% só tem 2) e complete até ≥10 pelos extremos dos baldes fartos — os difíceis são
    **disjuntos** da cota (monte as cotas PRIMEIRO, escolha difíceis FORA do já selecionado). Regra
    "≥N por balde" NÃO fecha no balde raso — use ordinais (extremos), não thresholds mágicos (evita 4º
    grau de liberdade). O montador aceita um flag/modo `--fair-value-only` (default) que emite SÓ
    fair_value (Commit 1) e um modo `--fill-v-modelo` (Plano 04). NÃO rode o modelo aqui. Never-raise:
    ticker que degrada → sem fair_value, nunca aborta.
  </action>
  <verify>
    <automated>bash -c 'python3 scripts/montar_cesta_holdout.py --fair-value-only --dry-run 2>/dev/null | python3 -c "import sys,yaml; d=yaml.safe_load(sys.stdin) or {}; ent=[v for v in d.values() if isinstance(v,dict)]; import collections; c=collections.Counter(v.get(\"arquetipo\") for v in ent); print(\"ESTRATOS\", dict(c)); assert c.get(\"concessao_finita\",0) >= 1, \"CONCESSAO vazio → eh_concessionaria faltando\"; assert sum(1 for v in ent if v.get(\"dificil\")) >= 10; print(\"OK difíceis\", sum(1 for v in ent if v.get(\"dificil\")))"'</automated>
  </verify>
  <acceptance_criteria>
    - CONCESSAO_FINITA tem ≥1 membro no output (prova que `eh_concessionaria` foi replicado; warning sign do RESEARCH: 0 membros).
    - Cada estrato com universo ≥6 tem ≥6 representantes; CRESCIMENTO tem 4 e vem MARCADO cota<6.
    - Há ≥10 difíceis, disjuntos das cotas (nenhum ticker aparece como cota E difícil).
    - AZUL4 (D-03) aparece na cesta SEM `fair_value` (degradação), nunca omitido.
    - A regra de seleção (ordenação market_cap desc, desempate alfabético) está escrita no cabeçalho/docstring — reproduzível, não olha o resultado do modelo.
  </acceptance_criteria>
  <done>O montador é determinístico, replica o roteamento de produção (eh_concessionaria), estratifica por regra escrita ANTES e marca honestamente a cota faltante.</done>
</task>

<task type="auto">
  <name>Task 2: COMMIT 1 — gravar holdout_v24.yaml (fair_value only) + teste de composição</name>
  <files>tests/fixtures/holdout_v24.yaml, tests/test_holdout_cesta.py, tests/classificacao.yaml</files>
  <read_first>
    - .planning/phases/14-valida-o-honesta-val/14-PATTERNS.md §"tests/fixtures/holdout_v24.yaml" (schema recomendado + linhas separadas para git blame)
    - .planning/phases/14-valida-o-honesta-val/14-RESEARCH.md §"Contrato do fixture" (o teste que acorda espera fair_value ESCALAR)
    - tests/test_blindagem_meta.py (linhas 145-172 — o contrato que o fixture precisa satisfazer no Plano 04)
  </read_first>
  <action>
    Rode `montar_cesta_holdout.py --fair-value-only` e grave `tests/fixtures/holdout_v24.yaml`. Schema
    por ticker (D-02/D-09), **cada campo em sua PRÓPRIA LINHA** (git blame é por linha): `fair_value`
    (ESCALAR = ponto médio da faixa, para casar o teste que acorda em test_blindagem_meta.py:160-165
    sem reescrevê-lo), `fair_value_min`, `fair_value_max` (bordas auditáveis), `lentes` (quais
    valeram), `arquetipo` (estrato), `dificil` (bool), `fonte: "lentes Graham+Bazin (core/lentes)"`,
    `data`. **ZERO `v_modelo`** (é o Commit 2, Plano 04). **ZERO `excecao_nota`** (a lavanderia morreu
    no Plano 01 — o fixture novo não nasce com ela). AZUL4 entra SEM `fair_value` (D-03). Cabeçalho do
    YAML: a **regra de seleção** (market_cap desc, 6/estrato, desempate alfabético, difíceis por
    extremos de balde) + o **snapshot-hash** (hash do arquivo do snapshot) — prova que a cesta não foi
    montada olhando o resultado. Crie `tests/test_holdout_cesta.py` com
    `test_holdout_estratificado_composicao` (`@pytest.mark.contrato`): valida ≥6 por arquétipo com
    universo suficiente, CRESCIMENTO marcado cota<6, ≥10 difíceis disjuntos, e que NENHUMA entrada tem
    chave `v_modelo` nem `excecao_nota` (Commit 1 puro). Adicione a entrada
    `'tests/test_holdout_cesta.py::test_holdout_estratificado_composicao': contrato` em
    `classificacao.yaml`. **Disciplina D-09:** este é o Commit 1 — commit ISOLADO, sem `v_modelo`, sem
    squash com o Plano 04.
  </action>
  <verify>
    <automated>bash -c 'pytest -k "holdout_estratificado or blindagem_meta" -p no:cacheprovider -q && python3 -c "import yaml; d=yaml.safe_load(open(\"tests/fixtures/holdout_v24.yaml\")); ent=[v for v in d.values() if isinstance(v,dict)]; assert all(\"v_modelo\" not in v for v in ent), \"Commit 1 nao pode ter v_modelo\"; assert all(\"excecao_nota\" not in v for v in ent), \"fixture nao nasce com excecao_nota\"; print(\"COMMIT1_LIMPO\", len(ent), \"entradas\")"'</automated>
  </verify>
  <acceptance_criteria>
    - `holdout_v24.yaml` existe; NENHUMA entrada tem `v_modelo` (Commit 1 é fair_value only).
    - NENHUMA entrada tem `excecao_nota` (o fixture não nasce com a lavanderia — VAL-06).
    - `fair_value`, `fair_value_min`, `fair_value_max` estão em LINHAS SEPARADAS por ticker (pré-condição do git blame do Plano 04).
    - O cabeçalho grava a regra de seleção + o snapshot-hash.
    - `pytest -k holdout_estratificado` verde; `test_nenhum_ticker_e_load_bearing` ainda `skipped` (falta v_modelo — acorda só no Plano 04).
    - Este task produz UM commit isolado (Commit 1 do D-09), sem v_modelo — não misturar com o Plano 04.
  </acceptance_criteria>
  <done>Commit 1 gravado: os fair values Graham+Bazin estão cravados ANTES do modelo, em linhas separadas, com regra+hash no cabeçalho; nenhuma v_modelo/excecao_nota.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot dos 104 → cesta estratificada | Onde a cesta pode divergir da produção (eh_concessionaria faltando → carve-out vazio) ou ser montada olhando o resultado (cherry-pick). Adversário = viés de "validar só o meio da distribuição". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Spoofing (roteamento falso) | montar_cesta_holdout.py | mitigate | Replica `build.py:168` (eh_concessionaria); teste assere CONCESSAO_FINITA ≥1 (warning sign: 0 membros). A cesta valida o roteamento REAL do app. |
| T-14-09 | Tampering (cherry-pick) | seleção da cesta | mitigate | Regra determinística escrita ANTES (market_cap desc, ordinal, desempate alfabético) + snapshot-hash no cabeçalho + Commit 1 datado (git prova que não olhou o resultado). |
| T-14-10 | Repudiation (viés de meio) | difíceis / cota faltante | mitigate | 10 difíceis por filtro de atributo disjunto; CRESCIMENTO<6 MARCADO (não inventado); AZUL4 sem lente reportado (D-03), nunca omitido silenciosamente. |
| T-14-04b | Elevation of Privilege | holdout_v24.yaml | mitigate | Fixture não nasce com `excecao_nota` (teste de composição assere ausência) — nenhuma exceção pode salvar ticker via o novo substrato. |
</threat_model>

<verification>
- `pytest -k "holdout_estratificado or blindagem"` verde.
- `holdout_v24.yaml`: 0 `v_modelo`, 0 `excecao_nota`, campos em linhas separadas, cabeçalho com regra+hash.
- CONCESSAO_FINITA não vazio; CRESCIMENTO marcado cota<6; ≥10 difíceis disjuntos; AZUL4 sem fair_value.
- Nenhum knob tocado (`git diff config.yaml calibracao.lock.yaml` VAZIO).
</verification>

<success_criteria>
- Cesta estratificada determinística montada por regra escrita ANTES, replicando o roteamento de produção.
- Commit 1 (fair_value only) gravado, isolado, datado — o substrato do hold-out honesto.
- Composição validada por teste (≥6/arquétipo quando possível, 10 difíceis, cota<6 marcada).
</success_criteria>

<output>
After completion, create `.planning/phases/14-valida-o-honesta-val/14-03-SUMMARY.md`
</output>
