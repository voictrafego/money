---
phase: 14-valida-o-honesta-val
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/helpers_blindagem.py
  - tests/test_blindagem_meta.py
  - tests/classificacao.yaml
autonomous: true
requirements: [VAL-05]

must_haves:
  truths:
    - "LIMIAR_JACKKNIFE_PP é uma função de n derivada de um null neutro (Monte-Carlo seed-fixo), não a constante 0,01 [ASSUMIDO]."
    - "A função é overfit-proof por construção: só olha n (contagem) e a forma do null, NUNCA os valores de v_modelo — commitada antes de existir qualquer v_modelo."
    - "Um teste prova que a função mede o que promete: num sample saudável o desvio do jackknife fica abaixo do LIMIAR; num sample com ponto load-bearing (ponte) o desvio o excede."
  artifacts:
    - path: "tests/helpers_blindagem.py"
      provides: "LIMIAR_JACKKNIFE_PP(n) puro, determinístico, seed-fixo, derivado de null neutro"
      contains: "def LIMIAR_JACKKNIFE_PP"
    - path: "tests/test_blindagem_meta.py"
      provides: "Teste que valida a função no null + call-site convertido de constante para função"
      contains: "LIMIAR_JACKKNIFE_PP"
  key_links:
    - from: "tests/test_blindagem_meta.py"
      to: "tests/helpers_blindagem.py::LIMIAR_JACKKNIFE_PP"
      via: "chamada LIMIAR_JACKKNIFE_PP(len(razoes)) no lugar da constante"
      pattern: "LIMIAR_JACKKNIFE_PP\\("
    - from: "tests/helpers_blindagem.py::LIMIAR_JACKKNIFE_PP"
      to: "tests/helpers_blindagem.py::mediana_jackknife"
      via: "o LIMIAR é o percentil do estatístico de mediana_jackknife num null neutro"
      pattern: "mediana_jackknife"
---

<objective>
Substituir a constante `LIMIAR_JACKKNIFE_PP = 0.01 [ASSUMIDO]` por uma **função de n** derivada de um
null neutro por Monte-Carlo com seed fixo — o item de MAIOR INCERTEZA da fase. É uma tarefa de
DERIVAÇÃO estatística: a função precisa medir "quanto um único ponto PODE mover a mediana numa
distribuição saudável de n pontos", independente dos valores reais, e ser commitada ANTES de existir
qualquer `v_modelo` (D-10 — overfit-proof por construção e por timestamp).

Purpose: dar ao jackknife (D-11) um gate honesto — se um ticker mover a mediana além do que a
suavidade de n pontos explica, ele é load-bearing (a calibração está apoiada nele).

Output: `LIMIAR_JACKKNIFE_PP(n)` puro + um teste que o valida no null; a constante e o parágrafo
`[ASSUMIDO]` removidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-valida-o-honesta-val/14-RESEARCH.md
@.planning/phases/14-valida-o-honesta-val/14-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contratos verificados no repo (2026-07-20). -->

tests/helpers_blindagem.py:327 — o estatístico do jackknife (ANALOG de forma):
  def mediana_jackknife(valores: Sequence[float]) -> tuple[float, float]:
      # devolve (mediana, desvio_max) onde
      # desvio_max = max_v |mediana(amostra - {v}) - mediana(amostra)|
      # levanta ValueError se len < 3

tests/test_blindagem_meta.py:30 — LIMIAR_JACKKNIFE_PP = 0.01   (constante [ASSUMIDO], linhas 30-35)
tests/test_blindagem_meta.py:88 — test_mediana_jackknife_e_robusta_por_construcao (ANALOG de teste:
      homogênea → desvio baixo; ponte → desvio explode)
tests/test_blindagem_meta.py:133-172 — test_nenhum_ticker_e_load_bearing:
      :154-158 skip se HOLDOUT_V24 não existe (auto-acorda no Plano 04)
      :160-165 razoes = [v_modelo/fair_value para entradas com ambos]
      :168     assert desvio_max <= LIMIAR_JACKKNIFE_PP  ← CALL-SITE a converter para função

Env: numpy disponível; stdlib (random + statistics) basta. Sem rede — fase determinística.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derivar LIMIAR_JACKKNIFE_PP(n) do null neutro (Monte-Carlo seed-fixo)</name>
  <files>tests/helpers_blindagem.py</files>
  <read_first>
    - .planning/phases/14-valida-o-honesta-val/14-RESEARCH.md §"LIMIAR_JACKKNIFE_PP(n) — a estatística" (teoria order-statistic + o caminho honesto + a landmine de escala)
    - .planning/phases/14-valida-o-honesta-val/14-PATTERNS.md §"Função LIMIAR_JACKKNIFE_PP(n)"
    - tests/helpers_blindagem.py (linhas ~320-360 — mediana_jackknife, o estatístico que o LIMIAR calibra)
  </read_first>
  <action>
    Escreva `LIMIAR_JACKKNIFE_PP(n: int) -> float` em `tests/helpers_blindagem.py`, ao lado de
    `mediana_jackknife`, pura e determinística. Derivação (D-10, caminho honesto do RESEARCH):
    (1) defina um **null neutro** — `n` pontos iid de uma distribuição suave unimodal representando
    uma distribuição de `V/FairValue` SEM ponto load-bearing (lognormal ou uniforme numa faixa
    plausível); o σ (dispersão saudável) é a ÚNICA premissa de modelagem e vem de crença prévia,
    NUNCA do hold-out observado. (2) Simule M draws (ex. 10 000) com **seed fixo** (`random.Random(seed)`
    ou `numpy.random.default_rng(seed)` — seed constante literal no código), compute `mediana_jackknife`
    de cada, tome um **percentil alto** (95 ou 99). (3) O LIMIAR é esse percentil. **Landmine de
    escala:** o desvio do jackknife escala com a dispersão dos dados; para o LIMIAR depender só de `n`
    (e não de "que dispersão a cesta acabou tendo"), **normalize o estatístico por uma escala robusta
    do próprio sample** (MAD ou IQR) — derive o LIMIAR do estatístico normalizado, escala-invariante.
    Se isso exigir `mediana_jackknife` devolver também a escala, adicione uma função-irmã
    (ex. `desvio_jackknife_normalizado(valores)`) SEM quebrar a assinatura existente de
    `mediana_jackknife` (o teste :88 depende dela). Documente no docstring: a premissa do null, o seed,
    M, o percentil, e a normalização escolhida — para ser auditável. **Não olhe nenhum `v_modelo`**
    (ele nem existe ainda — Plano 04). Cachear por `n` (functools.lru_cache) é aceitável (determinístico).
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0,'tests'); import helpers_blindagem as h; a=h.LIMIAR_JACKKNIFE_PP(23); b=h.LIMIAR_JACKKNIFE_PP(23); print('DET', a==b); assert a==b and a>0 and a<1; print('MONOTONIA', h.LIMIAR_JACKKNIFE_PP(11) >= h.LIMIAR_JACKKNIFE_PP(99))"</automated>
  </verify>
  <acceptance_criteria>
    - `LIMIAR_JACKKNIFE_PP(n)` é **determinística**: duas chamadas com o mesmo `n` dão bit-a-bit o mesmo float (seed fixo).
    - Retorna float em `(0, 1)` para `n` típicos (≥ 3).
    - É **monótona não-crescente em n** (mais pontos → um ponto move menos a mediana): `LIMIAR(11) >= LIMIAR(99)`.
    - O docstring declara: distribuição do null, seed literal, M, percentil, e a normalização (MAD/IQR) — tudo auditável.
    - `grep -n "def LIMIAR_JACKKNIFE_PP" tests/helpers_blindagem.py` retorna a definição; nenhuma referência a `v_modelo` ou ao fixture na função.
  </acceptance_criteria>
  <done>A função existe, é pura/determinística/seed-fixa, depende só de n e da forma do null, e nunca olha os dados reais.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teste do LIMIAR no null + converter call-site de constante para função</name>
  <files>tests/test_blindagem_meta.py, tests/classificacao.yaml</files>
  <read_first>
    - .planning/phases/14-valida-o-honesta-val/14-RESEARCH.md §"LIMIAR" (recomendação: 2 entregáveis — função + teste que a valida no null)
    - tests/test_blindagem_meta.py (linhas 25-45 — a constante [ASSUMIDO]; 85-130 — o analog de teste por construção; 145-172 — o call-site)
  </read_first>
  <behavior>
    - Num sample SAUDÁVEL (homogêneo, sem ponto load-bearing) de n pontos, o desvio do jackknife
      (normalizado) fica ABAIXO de `LIMIAR_JACKKNIFE_PP(n)`.
    - Num sample com um ponto LOAD-BEARING deliberado (ponte/outlier que domina a mediana), o desvio
      (normalizado) EXCEDE `LIMIAR_JACKKNIFE_PP(n)` — o gate reprova corretamente.
    - O call-site `:168` passa a usar `LIMIAR_JACKKNIFE_PP(len(razoes))`, não a constante.
  </behavior>
  <action>
    Remova a constante `LIMIAR_JACKKNIFE_PP = 0.01` e o parágrafo `[ASSUMIDO]` de
    `tests/test_blindagem_meta.py:30-35`, e o parágrafo "NA FASE 14: fixar LIMIAR..." (linhas ~151-152).
    Importe/use `helpers_blindagem.LIMIAR_JACKKNIFE_PP`. Converta o call-site em
    `test_nenhum_ticker_e_load_bearing` (:168) para comparar o desvio contra
    `LIMIAR_JACKKNIFE_PP(len(razoes))`. **CRÍTICO — mesma escala nos dois lados:** se a Task 1 derivou o
    LIMIAR do estatístico NORMALIZADO (MAD/IQR), então o `desvio_max` do call-site (hoje `desvio_max`
    cru de `mediana_jackknife(razoes)` em :166-167) DEVE ser recomputado com a MESMA normalização —
    troque a chamada `mediana_jackknife(razoes)` por `desvio_jackknife_normalizado(razoes)` (a
    função-irmã da Task 1) para não comparar unidades diferentes. Esta edição das linhas :166-167 está
    EXPLICITAMENTE autorizada (é o par do LIMIAR). NÃO reescreva a lógica de construção de razões
    (:160-165 — as entradas com v_modelo+fair_value) — só a fonte do limiar e a normalização do desvio. Escreva `test_limiar_jackknife_mede_o_que_promete` (`@pytest.mark.invariante`),
    espelhando `test_mediana_jackknife_e_robusta_por_construcao`: (a) sample saudável → desvio <
    LIMIAR(n); (b) sample com ponto load-bearing → desvio > LIMIAR(n). Adicione a entrada
    `'tests/test_blindagem_meta.py::test_limiar_jackknife_mede_o_que_promete': invariante` em
    `tests/classificacao.yaml`, comentário sem ticker. `test_nenhum_ticker_e_load_bearing` permanece
    `skip` (o fixture nasce no Plano 03/04) — não force nada aqui.
  </action>
  <verify>
    <automated>pytest -k "limiar_jackknife or mediana_jackknife or blindagem_meta" -p no:cacheprovider -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -k limiar_jackknife` passa: o teste prova AMBAS as direções (saudável abaixo, load-bearing acima).
    - `grep -c "LIMIAR_JACKKNIFE_PP = 0.01" tests/test_blindagem_meta.py` == 0 (constante removida).
    - `grep -c "ASSUMIDO" tests/test_blindagem_meta.py` == 0 (parágrafo removido).
    - `grep -n "LIMIAR_JACKKNIFE_PP(" tests/test_blindagem_meta.py` mostra o call-site como CHAMADA de função (com argumento).
    - `pytest` (coleta completa) sem CLASSIFICACAO ORFA; `test_nenhum_ticker_e_load_bearing` ainda `skipped` (fixture ausente).
  </acceptance_criteria>
  <done>O LIMIAR virou função validada por construção; o call-site do jackknife consome `LIMIAR_JACKKNIFE_PP(n)`; nada olha os dados reais.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| null neutro (crença prévia) → gate do jackknife | Onde a premissa de "dispersão saudável" pode ser escolhida olhando os dados (overfit). O adversário é o viés de confirmação: escolher σ/percentil depois de ver a distribuição de `v_modelo/fair_value`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Information Disclosure | LIMIAR_JACKKNIFE_PP(n) | mitigate | Derivado SÓ de `n` + forma do null (crença prévia); seed literal fixo; commitado no Wave 2, antes de qualquer `v_modelo` (Plano 04, Wave 4). Nenhuma referência a `v_modelo`/fixture na função (grep). |
| T-14-05b | Tampering | escala do estatístico | mitigate | Normalização por MAD/IQR torna o LIMIAR escala-invariante — imune a "que dispersão a cesta acabou tendo"; o teste no null prova as duas direções. |
| T-14-07 | Repudiation | constante 0,01 [ASSUMIDO] | mitigate | A constante mágica e o parágrafo [ASSUMIDO] são removidos; o número passa a ter proveniência documentada (null + seed + percentil). |
</threat_model>

<verification>
- `pytest -k "limiar_jackknife or mediana_jackknife or blindagem_meta"` verde.
- `LIMIAR_JACKKNIFE_PP(n)` determinística e monótona em n (prova por execução direta).
- `grep ASSUMIDO tests/test_blindagem_meta.py == 0`; constante 0,01 removida.
- Nenhum knob de valuation tocado (`git diff config.yaml calibracao.lock.yaml` VAZIO).
</verification>

<success_criteria>
- LIMIAR_JACKKNIFE_PP(n) derivado do null neutro, seed-fixo, pré-registrado (Wave 2 < v_modelo Wave 4).
- Teste prova as duas direções (saudável abaixo / load-bearing acima).
- Call-site do jackknife consome a função; constante [ASSUMIDO] extinta.
</success_criteria>

<output>
After completion, create `.planning/phases/14-valida-o-honesta-val/14-02-SUMMARY.md`
</output>
