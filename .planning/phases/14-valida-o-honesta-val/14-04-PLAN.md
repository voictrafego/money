---
phase: 14-valida-o-honesta-val
plan: 04
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - tests/fixtures/holdout_v24.yaml
  - tests/test_holdout_ordem_git.py
  - tests/classificacao.yaml
autonomous: true
requirements: [VAL-03, VAL-04, VAL-05]

must_haves:
  truths:
    - "COMMIT 2: v_modelo (V do RIM via report.analisar_acao) é preenchido rodando o modelo sobre a MESMA cesta, em linhas SEPARADAS, sem re-tocar as linhas fair_value do Commit 1."
    - "test_nenhum_ticker_e_load_bearing acorda com o fixture completo e passa: nenhum ticker move a mediana de V/FairValue além de LIMIAR_JACKKNIFE_PP(n) — jackknife robusto (D-11)."
    - "O git blame prova a ordem: toda linha fair_value tem author-time ANTERIOR a toda linha v_modelo — os fair values foram cravados antes de rodar o modelo (VAL-03)."
    - "PASS do hold-out (D-11) provado por EXECUÇÃO: (1) VAL-01 verdadeiro, (2) nenhum ticker load-bearing, (3) nenhuma exceção salvou ticker; mediana V/FairValue reportada como DETECTOR, nunca gate."
  artifacts:
    - path: "tests/fixtures/holdout_v24.yaml"
      provides: "Commit 2 — v_modelo por ticker (linhas separadas, fair_value intocado)"
      contains: "v_modelo"
    - path: "tests/test_holdout_ordem_git.py"
      provides: "Prova de ordem por git blame --line-porcelain (VAL-03/D-09)"
      contains: "line-porcelain"
  key_links:
    - from: "tests/fixtures/holdout_v24.yaml (v_modelo)"
      to: "src/analista/report/report.py::analisar_acao"
      via: "v_modelo = V do RIM (fonte única, não reimplementado)"
      pattern: "analisar_acao"
    - from: "tests/test_holdout_ordem_git.py"
      to: "tests/fixtures/holdout_v24.yaml"
      via: "git blame author-time por linha: max(fair_value) < min(v_modelo)"
      pattern: "author-time"
    - from: "tests/test_blindagem_meta.py::test_nenhum_ticker_e_load_bearing"
      to: "tests/fixtures/holdout_v24.yaml"
      via: "acorda automaticamente quando o fixture completo existe; usa LIMIAR_JACKKNIFE_PP(n)"
      pattern: "HOLDOUT_V24"
---

<objective>
Fechar a Fase 14: gravar o **Commit 2** (`v_modelo` rodando o modelo sobre a mesma cesta), acordar o
jackknife (`test_nenhum_ticker_e_load_bearing`), provar a ordem por `git blame` (VAL-03/D-09), e
validar o **PASS do hold-out por EXECUÇÃO** (D-11). Depende do Commit 1 (Plano 03, fair_value) e do
LIMIAR (Plano 02) — a ordem é load-bearing: fair_value ANTES de v_modelo.

Purpose: provar o marco sem se enganar — o modelo roda UMA vez contra âncoras cravadas antes; se
falhar, re-arquiteta, NÃO recalibra (VAL-04). A mediana V/FairValue é detector de viés, nunca alvo.

Output: `holdout_v24.yaml` completo (Commit 2), `test_holdout_ordem_git.py`, jackknife verde,
regressão ao vivo dos 104 confirmando o PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-valida-o-honesta-val/14-RESEARCH.md
@.planning/phases/14-valida-o-honesta-val/14-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contratos verificados no repo (2026-07-20). -->

src/analista/report/report.py — analisar_acao(c, cfg) → AnaliseAcao (V do RIM = intrinseco_motor;
  FONTE ÚNICA; NÃO reimplementar a fórmula). Mesmo cfg offline do montador (β setorial carimbado, D-06).

tests/test_blindagem_meta.py:133-172 — test_nenhum_ticker_e_load_bearing:
  :154-158 skip se HOLDOUT_V24 não existe → ACORDA automaticamente quando o Commit 2 completar o fixture
  :160-165 razoes = [v_modelo/fair_value para entradas com AMBOS] (AZUL4 sem fair_value cai fora sozinho)
  :168 assert desvio_max <= LIMIAR_JACKKNIFE_PP(len(razoes))   (call-site convertido no Plano 02)

git blame --line-porcelain -- tests/fixtures/holdout_v24.yaml  → "author-time <epoch>" por linha
  (VERIFIED). Landmines: NÃO usar `git log --grep` (falso positivo com commits 13-0x); shallow clone
  (CI --depth=1) quebra blame → precisa fetch-depth: 0; squash/amend colapsa os 2 commits.

scripts/montar_cesta_holdout.py — modo --fill-v-modelo (Plano 03) preenche v_modelo SEM re-tocar
  fair_value.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: COMMIT 2 — preencher v_modelo (rodar o modelo) e acordar o jackknife</name>
  <files>tests/fixtures/holdout_v24.yaml</files>
  <read_first>
    - .planning/phases/14-valida-o-honesta-val/14-RESEARCH.md §"Contrato do fixture" + §"Ordem por git" (schema load-bearing: v_modelo em linha separada, não re-tocar fair_value)
    - .planning/phases/14-valida-o-honesta-val/14-PATTERNS.md §"Modified Files → test_blindagem_meta.py"
    - scripts/montar_cesta_holdout.py (o modo --fill-v-modelo criado no Plano 03)
    - tests/fixtures/holdout_v24.yaml (o Commit 1 — os fair_value que NÃO podem ser re-tocados)
  </read_first>
  <action>
    Rode o modelo (`report.analisar_acao(c, cfg)` com o MESMO cfg offline do montador — β setorial
    carimbado, D-06) sobre a MESMA cesta do Commit 1 e preencha `v_modelo` (= V do RIM =
    `intrinseco_motor`) por ticker. **CRÍTICO (D-09):** insira `v_modelo` como uma **linha NOVA** ao
    fim de cada bloco de ticker, deixando as linhas `fair_value`/`fair_value_min`/`fair_value_max` do
    Commit 1 **byte-a-byte intocadas** (se você reescrever o arquivo inteiro, TODAS as linhas ganham o
    timestamp do Commit 2 e a prova de ordem evapora). Use `--fill-v-modelo` que edita cirurgicamente,
    ou insira as linhas manualmente sem tocar as de fair_value. Ticker sem `fair_value` (AZUL4, D-03)
    pode receber `v_modelo` (a degradação é a ausência de RAZÃO, não a ausência de V) — o teste do
    jackknife o ignora sozinho por não ter `fair_value`. NÃO reimplemente a fórmula RIM (fonte única).
    **Disciplina D-09:** este é o Commit 2 — commit ISOLADO, POSTERIOR ao Commit 1, **sem squash**.
    A mediana V/FairValue resultante é **detector reportado** (registre no SUMMARY), NUNCA um alvo:
    se estiver longe de 1, é ALERTA escrito, jamais motivo para mexer knob (VAL-05/D-11).
  </action>
  <verify>
    <automated>bash -c 'pytest -k "nenhum_ticker_e_load_bearing or blindagem_meta" -p no:cacheprovider -q && python3 -c "import yaml; d=yaml.safe_load(open(\"tests/fixtures/holdout_v24.yaml\")); ent=[v for v in d.values() if isinstance(v,dict)]; nvm=sum(1 for v in ent if v.get(\"v_modelo\")); print(\"V_MODELO preenchidos:\", nvm); assert nvm>0"'</automated>
  </verify>
  <acceptance_criteria>
    - `test_nenhum_ticker_e_load_bearing` ACORDOU (não mais `skipped`) e PASSA: `desvio_max <= LIMIAR_JACKKNIFE_PP(n)` — nenhum ticker load-bearing (D-11).
    - As linhas `fair_value*` permanecem byte-idênticas ao Commit 1 (`git blame` ainda as atribui ao Commit 1 — verificado no Task 2).
    - `v_modelo` está em linha SEPARADA por ticker; AZUL4 sem `fair_value` fica fora do jackknife automaticamente.
    - `git log --oneline -2 -- tests/fixtures/holdout_v24.yaml` mostra DOIS commits distintos (Commit 1 e Commit 2), sem squash.
    - `git diff config.yaml calibracao.lock.yaml` VAZIO (nenhum knob tocado para "arrumar" a mediana).
  </acceptance_criteria>
  <done>Commit 2 gravado: v_modelo rodado sobre a cesta cravada, jackknife acordou e passou; fair_value intocado; mediana reportada como detector.</done>
</task>

<task type="auto">
  <name>Task 2: Prova de ordem por git blame (VAL-03/D-09)</name>
  <files>tests/test_holdout_ordem_git.py, tests/classificacao.yaml</files>
  <read_first>
    - .planning/phases/14-valida-o-honesta-val/14-RESEARCH.md §"Ordem por git (D-09) — mecanismo" (blame por linha; landmines)
    - .planning/phases/14-valida-o-honesta-val/14-PATTERNS.md §"Teste de ordem por git (D-09)"
    - Memória do projeto: historia-git-tem-fase-13-superseded (NÃO usar git log --grep; usar timestamps das linhas reais)
  </read_first>
  <action>
    Crie `tests/test_holdout_ordem_git.py` com `test_holdout_ordem_por_git`
    (`@pytest.mark.contrato`): rode `git blame --line-porcelain -- tests/fixtures/holdout_v24.yaml`
    via `subprocess`, extraia `author-time <epoch>` por linha, mapeie cada linha ao seu campo (parse
    por indentação/chave do YAML), e asserte
    `max(author-time das linhas fair_value/fair_value_min/fair_value_max) < min(author-time das linhas
    v_modelo)`. **NÃO** use `git log --grep` de mensagem (falso positivo com os commits de trading
    `13-0x` — memória). Se o `git blame` falhar por história ausente (shallow clone), `pytest.skip`
    com mensagem clara pedindo `fetch-depth: 0` (não falso-reprovar em ambiente sem história) — mas
    documente que localmente e na CI com história completa o teste RODA. Adicione a entrada
    `'tests/test_holdout_ordem_git.py::test_holdout_ordem_por_git': contrato` em `classificacao.yaml`,
    comentário sem ticker. Documente no docstring: dois commits sem squash + `git push` (história
    remota congelada) é a proteção; rebase preserva author-time (não quebra); squash/amend quebra.
  </action>
  <verify>
    <automated>pytest -k "ordem_por_git or holdout" -p no:cacheprovider -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -k ordem_por_git` PASSA localmente (história presente): toda linha fair_value é anterior a toda linha v_modelo por author-time.
    - O teste NÃO usa `git log --grep` (`grep -c "\-\-grep" tests/test_holdout_ordem_git.py` == 0).
    - Em shallow clone (sem história), o teste faz `skip` com instrução `fetch-depth: 0`, nunca falso-reprova.
    - Entrada em `classificacao.yaml` presente; coleta sem CLASSIFICACAO ORFA.
  </acceptance_criteria>
  <done>O git prova a ordem por linha: fair_value cravado antes de v_modelo (VAL-03), imune a git log --grep e robusto a shallow clone.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — PASS do hold-out provado por EXECUÇÃO (D-11)</name>
  <files>tests/fixtures/holdout_v24.yaml</files>
  <read_first>
    - .planning/phases/14-valida-o-honesta-val/14-CONTEXT.md §D-11 (o que é PASS) + §"Escopo negativo" (re-arquiteta não recalibra)
    - .planning/phases/14-valida-o-honesta-val/14-RESEARCH.md §"Validation Architecture → Sampling Rate / Phase gate"
    - Memória do projeto: guardrails-devem-ser-provados-por-execucao (a regressão ao vivo é o oráculo, não a suíte verde)
  </read_first>
  <action>
    Prove o PASS do hold-out por EXECUÇÃO (não só por suíte verde). Rode e registre no SUMMARY as três
    condições do D-11: **(1)** VAL-01 verdadeiro (`pytest -k soberano_itub4` verde — ITUB4 ∈ [35,39]);
    **(2)** nenhum ticker load-bearing (`pytest -k nenhum_ticker_e_load_bearing` verde —
    `desvio_max <= LIMIAR_JACKKNIFE_PP(n)`); **(3)** nenhuma exceção salvou ticker
    (`grep -rn excecao_nota src/ tests/ scripts/` fora de comentário == 0, do Plano 01). Rode a suíte
    default completa (`pytest`) e confirme **0 failed** e coleta sem CLASSIFICACAO ORFA. Confirme a
    **fronteira vazia**: `git diff config.yaml calibracao.lock.yaml` VAZIO (orçamento em 3 graus
    intacto — nenhum knob tocado nesta fase). Reporte a **mediana e o IQR de V/FairValue por estrato +
    pooled** (D-07), com CONCESSAO_FINITA isolado (não contamina o pooled) e a lista de degradação
    (AZUL4). Se qualquer condição do PASS falhar: **NÃO recalibra** — documente como falha de
    arquitetura (VAL-04) e pare para re-arquitetar. A mediana longe de 1 é ALERTA escrito, nunca gate.
    Este task NÃO edita produção — é validação pura; o único artefato tocado é o SUMMARY/relatório.
  </action>
  <verify>
    <automated>bash -c 'pytest -k "soberano_itub4 or nenhum_ticker_e_load_bearing or ordem_por_git or holdout or blindagem" -p no:cacheprovider -q && test -z "$(git diff --name-only config.yaml calibracao.lock.yaml 2>/dev/null)" && echo FRONTEIRA_VAZIA && grep -rn excecao_nota src/ tests/ scripts/ | grep -vi "\.md:" | grep -v "^\s*#" | grep -c excecao_nota | grep -qx 0 && echo ZERO_EXCECAO'</automated>
  </verify>
  <acceptance_criteria>
    - As TRÊS condições do D-11 verdes por execução: VAL-01 (região [35,39]), jackknife robusto (sem load-bearing), zero exceção viva.
    - `pytest` (suíte default) 0 failed, coleta sem CLASSIFICACAO ORFA.
    - `git diff config.yaml calibracao.lock.yaml` VAZIO — orçamento em 3 graus intacto (re-arquiteta ≠ recalibra).
    - Distribuição V/FairValue reportada por estrato + pooled; CONCESSAO_FINITA isolado; AZUL4 na lista de degradação.
    - Mediana V/FairValue documentada como DETECTOR (alerta se longe de 1), NUNCA usada como gate/alvo.
  </acceptance_criteria>
  <done>O marco v2.4 está provado sem se enganar: caso do livro passa, jackknife robusto, zero exceção, zero knob tocado — PASS por execução, não por promessa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Commit 1 (fair_value) → Commit 2 (v_modelo) | Onde a ordem load-bearing pode ser burlada (squash, re-toque de fair_value, shallow clone) — a assinatura exata de "calibrei o knob até o teste passar". Adversário = viés de confirmação / contribuidor futuro. |
| suíte verde → PASS real | Onde "suíte verde" pode ser confundida com blindagem; o oráculo é a regressão ao vivo, não o teste passar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Repudiation (ordem falsa) | git blame / holdout_v24.yaml | mitigate | Blame por LINHA (author-time), nunca `git log --grep`; v_modelo em linha nova sem re-tocar fair_value; dois commits sem squash + push; teste assere max(fair_value) < min(v_modelo). |
| T-14-02b | Tampering (recalibrar disfarçado) | config.yaml / calibracao.lock.yaml | mitigate | Gate `git diff config.yaml calibracao.lock.yaml` VAZIO; mediana V/FairValue é DETECTOR reportado, nunca alvo; falha → re-arquiteta (VAL-04), não recalibra. |
| T-14-05c | Information Disclosure (LIMIAR pós-hoc) | LIMIAR_JACKKNIFE_PP(n) | mitigate | O LIMIAR foi commitado no Wave 2 (Plano 02), ANTES do v_modelo (Wave 4) — timestamp + por-construção; o call-site só recebe `n`, nunca os valores. |
| T-14-11 | Repudiation (suíte verde ≠ blindagem) | phase gate | mitigate | PASS provado por EXECUÇÃO (regressão ao vivo dos 104 + as 3 condições D-11), não só por `pytest` verde (memória guardrails-devem-ser-provados-por-execucao). |
</threat_model>

<verification>
- `pytest -k "soberano_itub4 or nenhum_ticker_e_load_bearing or ordem_por_git or holdout or blindagem"` verde.
- `pytest` (suíte default) 0 failed, sem CLASSIFICACAO ORFA; `test_nenhum_ticker_e_load_bearing` acordado e verde.
- `git log --oneline -2 -- tests/fixtures/holdout_v24.yaml` = 2 commits distintos (Commit 1 antes de Commit 2), sem squash.
- `git diff config.yaml calibracao.lock.yaml` VAZIO; `grep excecao_nota` na árvore viva == 0.
</verification>

<success_criteria>
- Commit 2 (v_modelo) gravado sobre a cesta cravada, sem re-tocar fair_value; jackknife acordou e passou.
- Prova de ordem por git blame verde (fair_value antes de v_modelo).
- PASS do hold-out provado por execução: VAL-01 + jackknife robusto + zero exceção; orçamento em 3 graus intacto.
- Mediana V/FairValue reportada como detector, nunca gate; distribuição por estrato + pooled.
</success_criteria>

<output>
After completion, create `.planning/phases/14-valida-o-honesta-val/14-04-SUMMARY.md`
</output>
