---
phase: 14-valida-o-honesta-val
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers_blindagem.py
  - tests/test_soberano_itub4.py
  - tests/classificacao.yaml
  - src/analista/backtest.py
  - tests/test_backtest_bancos.py
  - scripts/backtest_bancos.py
  - .planning/decisions/VAL-07-backtest-temporal.md
autonomous: true
requirements: [VAL-01, VAL-06, VAL-07]

must_haves:
  truths:
    - "Injetando o Ke do livro (0,1248) no motor RIM do ITUB4, V cai na região [35, 39] — o critério de aceite soberano do marco passa por execução."
    - "O símbolo excecao_nota não existe mais como chave/atributo vivo em src/, tests/ ou scripts/ — nenhuma regra de exceção pode salvar um ticker."
    - "A decisão VAL-07 (não fazer backtest temporal) está registrada de forma durável e auditável, com âncora no código do backtest."
  artifacts:
    - path: "tests/test_soberano_itub4.py"
      provides: "Teste closed-form VAL-01: motores.rim com Ke=0,1248 → assert 35<=V<=39"
      contains: "35"
    - path: "tests/helpers_blindagem.py"
      provides: "Helper insumos_itub4_livro (literal ITUB4 + insumos Cap.17 FORA de função test_, higiene BLIND-04a)"
      contains: "insumos_itub4_livro"
    - path: ".planning/decisions/VAL-07-backtest-temporal.md"
      provides: "ADR: não fazer backtest temporal (PIT honesto inviável com dados gratuitos)"
      contains: "point-in-time"
  key_links:
    - from: "tests/test_soberano_itub4.py"
      to: "src/analista/core/motores.py::rim"
      via: "chamada closed-form com ke=0.1248"
      pattern: "motores\\.rim|from analista.core import motores"
    - from: "src/analista/backtest.py"
      to: ".planning/decisions/VAL-07-backtest-temporal.md"
      via: "comentário-âncora apontando para o ADR"
      pattern: "VAL-07"
---

<objective>
Landar os três artefatos de validação que NÃO dependem do fixture do hold-out: o teste soberano
VAL-01 (o critério de aceite do marco), a subtração do `excecao_nota` (VAL-06) e a decisão escrita
VAL-07. São aditivos/subtrativos, sem `holdout_v24.yaml`, e abrem a Fase 14.

Purpose: provar por execução que o motor reproduz o caso do livro (V ∈ [35,39]), fechar por
construção a lavanderia de overfit do v2.3 (`excecao_nota`), e cravar a decisão de não-fazer-
backtest-temporal de forma auditável.

Output: `test_soberano_itub4.py` (verde), `excecao_nota` morto na árvore viva, ADR VAL-07 + âncora
no `backtest.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-valida-o-honesta-val/14-CONTEXT.md
@.planning/phases/14-valida-o-honesta-val/14-RESEARCH.md
@.planning/phases/14-valida-o-honesta-val/14-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contratos verificados no repo (2026-07-20). Use diretamente — sem explorar o código. -->

src/analista/core/motores.py:60 — assinatura do motor único:
  def rim(vpa0, roe0, ke, retencao, n, excesso_sustentavel=0.0,
          g_terminal=None, ke_g_spread_min=0.03, fade_para=None,
          roe_terminal=None) -> Optional[ResultadoRIM]
  ResultadoRIM.valor_intrinseco : float   (motores.py:47)

tests/helpers_blindagem.py:640 — empresa_itub4(empresas)  (ANALOG: literal "ITUB4" fora de test_)
tests/helpers_blindagem.py:270 — detectar_ticker_com_valor_cravado(...) (guarda BLIND-04a; varre só test_*.py)

src/analista/backtest.py:179 — "excecao_nota": fv.get("excecao_nota"),  (passthrough a REMOVER)
tests/test_backtest_bancos.py:56  — test_nenhuma_rota_diferente_de_rim_e_silenciosa (DELETAR)
tests/test_backtest_bancos.py:72  — test_nenhuma_nota_de_excecao_e_orfa (DELETAR)
tests/test_backtest_bancos.py:96  — test_backtest_determinismo (PRESERVAR)
tests/test_backtest_bancos.py:119 — test_backtest_rotulo_do_motor_consistente (PRESERVAR)
scripts/backtest_bancos.py:79 — r["excecao_nota"] or "-",  (coluna a REMOVER, senão KeyError)
tests/classificacao.yaml:48-49 — entradas dos 2 testes deletados (REMOVER no mesmo diff)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teste soberano VAL-01 (ITUB4 → V ∈ [35,39] com Ke do livro)</name>
  <files>tests/helpers_blindagem.py, tests/test_soberano_itub4.py, tests/classificacao.yaml</files>
  <read_first>
    - .planning/phases/14-valida-o-honesta-val/14-RESEARCH.md §"VAL-01" (números VERIFIED por execução)
    - .planning/phases/14-valida-o-honesta-val/14-PATTERNS.md §"Teste soberano VAL-01"
    - tests/helpers_blindagem.py (linhas ~630-660 — o analog empresa_itub4; linhas ~270-320 — o detector)
    - src/analista/core/motores.py (linhas 38-135 — ResultadoRIM + assinatura rim)
  </read_first>
  <behavior>
    - Injetando ke=0.1248 (constante do livro, Cap.17) em motores.rim com os insumos do ITUB4
      (vpa0=19.0, roe0=0.1798, retencao=0.5331, n=10, excesso_sustentavel=0.045,
      g_terminal=0.0728, roe_terminal=0.1798), valor_intrinseco ∈ [35, 39].
    - O teste assere a REGIÃO (35 <= V <= 39), NUNCA o ponto == 37,22 (== seria golden de nível).
    - O literal "ITUB4" e os números do Cap.17 vivem no helper insumos_itub4_livro em
      helpers_blindagem.py (FORA de qualquer função test_) — o detector BLIND-04a não dispara.
  </behavior>
  <action>
    Adicione em `tests/helpers_blindagem.py` (fora de qualquer função `test_`, ao lado de
    `empresa_itub4`) um helper `insumos_itub4_livro()` que devolve os insumos do Cap.17 do livro:
    `vpa0=19.0, roe0=0.1798, ke=0.1248, retencao=0.5331, n=10, excesso_sustentavel=0.045,
    g_terminal=0.0728, roe_terminal=0.1798`. Documente a proveniência: `ke=0,1248` é a constante do
    livro (Cap.17); `roe0/retencao/vpa0` vêm do snapshot do ITUB4; o `g` do livro (10,24%) entra por
    `roe0×retencao ≈ 9,58%`, NÃO por um parâmetro `g_alto` (o RIM não tem esse argumento). NÃO mexa
    em `excesso_sustentavel=0,045` para "chegar em 37,22" — é knob travado no lock (Pitfall 2).
    Crie `tests/test_soberano_itub4.py` com `test_soberano_itub4_reproduz_caso_do_livro`
    (`@pytest.mark.contrato`): chama `motores.rim(**insumos_itub4_livro())`, lê `valor_intrinseco`,
    asserta `35.0 <= V <= 39.0`. Zero literal de ticker + número no corpo do teste (tudo no helper).
    Adicione a entrada `'tests/test_soberano_itub4.py::test_soberano_itub4_reproduz_caso_do_livro': contrato`
    em `tests/classificacao.yaml`, sem citar ticker no comentário (hook `-k justificativa`).
  </action>
  <verify>
    <automated>pytest -k "soberano_itub4 or blindagem_meta" -p no:cacheprovider -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -k soberano_itub4` passa (1 passed); o assert é `35.0 <= V <= 39.0`.
    - `grep -n "insumos_itub4_livro" tests/helpers_blindagem.py` retorna a definição (helper fora de test_).
    - `grep -c "ITUB4" tests/test_soberano_itub4.py` == 0 (literal só no helper).
    - `pytest -k "detectar_ticker or blindagem_meta"` verde (o detector BLIND-04a não flagra o novo teste).
    - `pytest -k soberano_itub4` NÃO usa `==` sobre 37,22 (asserção de região; `grep "37" tests/test_soberano_itub4.py` == 0).
  </acceptance_criteria>
  <done>VAL-01 verde por execução: o motor RIM reproduz o caso do livro na região [35,39]; higiene BLIND-04a intacta.</done>
</task>

<task type="auto">
  <name>Task 2: Matar o excecao_nota (VAL-06) — subtração fechada por construção</name>
  <files>src/analista/backtest.py, tests/test_backtest_bancos.py, scripts/backtest_bancos.py, tests/classificacao.yaml</files>
  <read_first>
    - .planning/phases/14-valida-o-honesta-val/14-RESEARCH.md §"VAL-06" (localização exaustiva por grep)
    - .planning/phases/14-valida-o-honesta-val/14-PATTERNS.md §"Modified Files" (pontos exatos)
    - src/analista/backtest.py (linhas ~160-190 — o dict de rodar_cesta)
    - tests/test_backtest_bancos.py (linhas 1-130 — docstring + os 4 testes; 2 a deletar, 2 a preservar)
    - tests/classificacao.yaml (linhas 45-52 — as 2 entradas a remover)
  </read_first>
  <action>
    Remova a linha `"excecao_nota": fv.get("excecao_nota"),` do dict de resultado de `rodar_cesta`
    em `src/analista/backtest.py:179`. Delete os dois testes de `tests/test_backtest_bancos.py`:
    `test_nenhuma_rota_diferente_de_rim_e_silenciosa` (:56) e `test_nenhuma_nota_de_excecao_e_orfa`
    (:72) — sob o RIM único (Fase 13) todo ticker roteia para `rim`, a bijeção nota⟺rota-não-rim é
    vacuamente satisfeita e protege uma máquina do v2.3 que esta fase aposenta. PRESERVE
    `test_backtest_determinismo` (:96) e `test_backtest_rotulo_do_motor_consistente` (:119). Scrube a
    menção a `excecao_nota` na docstring do arquivo (linha ~19). Remova a coluna
    `r["excecao_nota"] or "-",` de `scripts/backtest_bancos.py:79` (senão o script quebra com KeyError
    após a remoção do passthrough). Remova as DUAS entradas órfãs em `tests/classificacao.yaml:48-49`
    NO MESMO diff (senão a coleta quebra com CLASSIFICACAO ORFA). Feche por construção: nenhuma
    referência viva a `excecao_nota` sobra em `src/`, `tests/`, `scripts/`.
  </action>
  <verify>
    <automated>bash -c 'grep -rn "excecao_nota" src/ tests/ scripts/ | grep -vi "\.md:" | grep -v "^\s*#" | grep -c "excecao_nota" | grep -qx 0 && echo GREP_LIMPO && pytest -k "backtest_bancos or backtest" -p no:cacheprovider -q'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "excecao_nota" src/ tests/ scripts/` (excluindo comentários e `.md`) retorna 0 — símbolo morto na árvore viva.
    - `pytest -k backtest_bancos` verde com os 2 testes preservados (determinismo + rotulo) e SEM os 2 deletados.
    - `python3 -c "import ast; ast.parse(open('scripts/backtest_bancos.py').read())"` OK (script não quebra).
    - `pytest` (coleta completa) NÃO dispara CLASSIFICACAO ORFA (entradas removidas no mesmo diff).
    - `tests/fixtures/fair_values_bancos.yaml` NÃO é deletado (é referência anti-padrão; só deixa de ser lido).
  </acceptance_criteria>
  <done>Nenhuma regra de exceção pode salvar um ticker: excecao_nota removido do passthrough, dos 2 testes, do script e das entradas de classificação — fechado por grep na árvore viva.</done>
</task>

<task type="auto">
  <name>Task 3: ADR VAL-07 — decisão de não fazer backtest temporal, durável e auditável</name>
  <files>.planning/decisions/VAL-07-backtest-temporal.md, src/analista/backtest.py</files>
  <read_first>
    - .planning/phases/14-valida-o-honesta-val/14-CONTEXT.md §D-08 (justificativa completa)
    - .planning/phases/14-valida-o-honesta-val/14-RESEARCH.md §"VAL-07"
    - .planning/phases/14-valida-o-honesta-val/14-PATTERNS.md §"ADR VAL-07"
    - src/analista/backtest.py (topo do módulo / perto de carregar_snapshot — onde ancorar o comentário)
  </read_first>
  <action>
    Crie o diretório `.planning/decisions/` e o arquivo `VAL-07-backtest-temporal.md` como ADR leve:
    **Contexto** (o marco v2.4 pergunta se há backtest temporal; o v2.3 gastou ~8 graus sobre 4 obs);
    **Decisão = NÃO fazer** backtest temporal nesta fase; **Justificativa** — PIT honesto exige mapear
    a data de disponibilidade de cada DFP (lag de ~2–3 meses; a DFP de 2022 só existiu em mar/2023) e
    reconstruir preço/rf da época, inviável de forma confiável só com dados gratuitos; um backtest
    ingênuo (DFP no fechamento, não na publicação) é vazamento de futuro → número confiante e falso,
    pior que nenhum; **Consequência** = registrar o desenho do PIT correto como Future Requirement
    (v2.5+), sem gastar orçamento desta fase. Adicione em `src/analista/backtest.py` (topo do módulo
    ou perto de `carregar_snapshot`) um comentário-âncora citando `VAL-07` e apontando para
    `.planning/decisions/VAL-07-backtest-temporal.md` — o código é onde um futuro implementador de
    backtest tropeça primeiro.
  </action>
  <verify>
    <automated>bash -c 'test -f .planning/decisions/VAL-07-backtest-temporal.md && grep -qi "point-in-time\|vazamento de futuro" .planning/decisions/VAL-07-backtest-temporal.md && grep -q "VAL-07" src/analista/backtest.py && python3 -c "import ast; ast.parse(open(\"src/analista/backtest.py\").read())" && echo VAL07_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/decisions/VAL-07-backtest-temporal.md` existe com as 4 seções (contexto, decisão, justificativa, consequência).
    - O ADR menciona "point-in-time" e "vazamento de futuro" (a razão técnica, não vaga).
    - `grep -n "VAL-07" src/analista/backtest.py` retorna o comentário-âncora.
    - `backtest.py` compila (ast.parse OK) após a adição do comentário.
  </acceptance_criteria>
  <done>VAL-07 satisfeito pela decisão ESCRITA: ADR durável em .planning/decisions/ + âncora no backtest.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| método pretendido → código de teste | Onde um viés de confirmação vira um golden de nível disfarçado ou uma exceção que salva um ticker. Não há rede/auth/usuário nesta fase; o "adversário" é o viés do próprio implementador (ou um contribuidor futuro que afrouxa o gate). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | test_soberano_itub4.py | mitigate | Asserta REGIÃO [35,39], nunca `==37,22`; literal do ticker + números no helper fora de `test_` (higiene BLIND-04a). `grep "37" test_soberano_itub4.py == 0`. |
| T-14-02 | Tampering | motores.rim / calibracao.lock.yaml | mitigate | VAL-01 injeta SÓ `ke=0,1248`; `excesso_sustentavel=0,045` intocado; `git diff config.yaml calibracao.lock.yaml` deve ficar VAZIO (nenhum knob para "chegar em 37,22"). |
| T-14-04 | Elevation of Privilege | excecao_nota (backtest.py:179) | mitigate | Símbolo removido do passthrough, dos 2 testes e do script; gate `grep excecao_nota == 0` na árvore viva fecha por construção (nenhuma exceção pode salvar ticker). |
| T-14-06 | Repudiation | ADR VAL-07 | accept | A decisão de não-fazer é registrada de forma durável (ADR + âncora no código); risco residual = alguém implementar backtest ingênuo ignorando o ADR — mitigado pela âncora no ponto de tropeço (`carregar_snapshot`). |
</threat_model>

<verification>
- `pytest -k "soberano_itub4 or backtest_bancos or blindagem"` verde.
- `pytest` (suíte default) coleta sem CLASSIFICACAO ORFA e sem regressão (0 failed).
- `grep -rn excecao_nota src/ tests/ scripts/` (fora de comentário/`.md`) == 0.
- `git diff config.yaml calibracao.lock.yaml` VAZIO (zero knob tocado).
</verification>

<success_criteria>
- VAL-01 verde por execução: `35 <= V <= 39` com o Ke do livro injetado.
- VAL-06: `excecao_nota` morto na árvore viva; nenhuma exceção pode salvar ticker.
- VAL-07: ADR + âncora no código; decisão durável registrada.
- Orçamento intacto em 3 graus (diff config/lock vazio).
</success_criteria>

<output>
After completion, create `.planning/phases/14-valida-o-honesta-val/14-01-SUMMARY.md`
</output>
