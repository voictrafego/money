---
phase: 09-ingest-o-correta-data
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - scripts/capturar_snapshot_limpo.py
  - tests/fixtures/snapshot_sanidade_limpo_2026-07-15.yaml
  - tests/helpers_sanidade.py
  - tests/test_sanidade_baseline.py
  - tests/classificacao.yaml
autonomous: true
requirements: [DATA-06]
must_haves:
  truths:
    - "O snapshot SUJO congelado e o baseline dos sujos são PRESERVADOS intactos (régua + evidência)"
    - "Existe um snapshot LIMPO novo, gerado pelo código consertado (bilhões de ações no ITUB4 2019)"
    - "A medição de 'hoje' lê o snapshot limpo; os testes de detecção seguem lendo o sujo"
    - "A monotonicidade ENCOLHE: pares_hoje ⊂ pares_baseline (progresso, não tautologia)"
    - "Os pares consertados (BRSR6 SAN-03; GOAU4/CGRA4 SAN-01; ITUB4/BRSR6 SAN-02; CSNA3/ALUP11/EQTL3/MRFG3 SAN-04) somem de pares_hoje"
    - "snapshot_bancos NÃO é regenerado aqui — decisão travada: fica para a Fase 10"
  artifacts:
    - path: "scripts/capturar_snapshot_limpo.py"
      provides: "captura do snapshot limpo a partir do código consertado (live)"
      min_lines: 20
    - path: "tests/fixtures/snapshot_sanidade_limpo_2026-07-15.yaml"
      provides: "dado LIMPO congelado, medido de 'hoje'"
    - path: "tests/helpers_sanidade.py"
      provides: "caminho do snapshot limpo desacoplado do sujo"
      contains: "LIMPO"
  key_links:
    - from: "_pares_e_buckets_de_hoje (test_sanidade_baseline.py)"
      to: "snapshot LIMPO"
      via: "carregar_snapshot_sanidade(path=CAMINHO_SNAPSHOT_LIMPO)"
      pattern: "LIMPO"
    - from: "test_sanidade_checks.py"
      to: "snapshot SUJO (intacto)"
      via: "carregar_snapshot_sanidade() default"
      pattern: "snapshot_sanidade_2026-07-14"
---

<objective>
DATA-06 — a régua enxerga o progresso. Regenerar o snapshot de teste de modo que a
monotonicidade da Fase 8 (`pares_hoje ⊆ pares_baseline`) deixe de ser uma TAUTOLOGIA
(dirty-vs-dirty) e passe a ENCOLHER, provando ticker-a-ticker os consertos dos planos 09-01/02/03.

Purpose: Sem isto, o executor conserta o dado e não vê progresso nenhum (Pitfall 1). A régua
(baseline dos sujos), o objeto medido (snapshot sujo) e o objeto consertado (dado limpo novo) NÃO
podem ser o mesmo arquivo. **Decisão de arquitetura travada (09-RESEARCH §DATA-06):** preservar o
sujo + gerar um limpo novo + desacoplar o loader — NÃO rodar live na medição (perde offline/
determinismo). **Decisão travada 2:** `snapshot_bancos` NÃO é regenerado aqui (fica para a Fase 10,
onde o golden ITUB4 32,88 é formalmente deletado — regenerar aqui só antecipa uma quebra
quarentenada).
Output: snapshot limpo + loader desacoplado + monotonicidade encolhendo, tudo verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ingest-o-correta-data/09-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- O mecanismo de regressão VERIFICADO — três artefatos, três papéis. -->

tests/fixtures/snapshot_sanidade_2026-07-14.yaml — dado SUJO congelado (104 tickers). PRESERVAR.
tests/fixtures/baseline_sanidade.yaml — o BASELINE DOS SUJOS (régua). PRESERVAR intacto.

tests/helpers_sanidade.py:
  carregar_snapshot_sanidade(path=None) — JÁ aceita path (default = CAMINHO_SNAPSHOT sujo).
    Reconstrói CompanyData por ticker das séries congeladas (_SERIES_NUM inclui lucro_liquido,
    lucro_controlador, patrimonio_liquido, pl_nao_controladores, dividendos, proventos_filtro_amplo,
    num_acoes, dpa_por_ano). O desacoplamento é só adicionar CAMINHO_SNAPSHOT_LIMPO e passá-lo.

tests/test_sanidade_baseline.py:
  _pares_e_buckets_de_hoje() (linha 65) — HOJE lê o sujo via carregar_snapshot_sanidade().
    É AQUI que a medição de "hoje" deve passar a ler o LIMPO.
  _pares_e_buckets_do_baseline() — lê o baseline sujo (a régua). NÃO mexer.
  test_baseline_de_sujos_so_encolhe (invariante) — pares_hoje ⊆ pares_baseline.
  test_bucket_nao_muda_sem_a_flag_sumir (invariante) — bucket igual p/ par persistente.

tests/test_sanidade_checks.py — lê o SUJO via default; PROVA que os detectores disparam. NÃO mexer.

scripts/capturar_snapshot_sujo.py — a FORMA provada da captura (universo ticker_map.json,
  degradação por ticker never-raise, zero R$, congela _SERIES_NUM + market_cap + splits).
  O limpo reusa essa forma com novo DATA_BASE e novo DESTINO.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capturar o snapshot LIMPO do código consertado (preservar o sujo)</name>
  <read_first>
    - scripts/capturar_snapshot_sujo.py — a forma inteira (universo, never-raise por ticker, SERIES_NUM, bloco falhas:, zero R$ derivado). É o molde.
    - tests/helpers_sanidade.py:26,45-92 — CAMINHO_SNAPSHOT e como as séries são reconstruídas.
    - .planning/phases/09-ingest-o-correta-data/09-RESEARCH.md §DATA-06 (recomendação 1-3) + Pitfall 2.
    - config.yaml:5-6 — ano_base=2025, anos_historico=10.
  </read_first>
  <action>
    Criar `scripts/capturar_snapshot_limpo.py` reusando a forma de `capturar_snapshot_sujo.py`
    (mesmo universo de ticker_map.json, mesma degradação por ticker never-raise, mesmas SERIES_NUM,
    zero R$ derivado, congela market_cap+splits+origem_num_acoes), com `DATA_BASE = "2026-07-15"` e
    `DESTINO = tests/fixtures/snapshot_sanidade_limpo_2026-07-15.yaml`. Ajustar o cabeçalho/docstring:
    este é o dado LIMPO (produto do código consertado), NÃO a evidência congelada — pode ser
    regenerado. Rodar o script (live) para gerar o fixture limpo. **NÃO sobrescrever nem tocar**
    `snapshot_sanidade_2026-07-14.yaml` (sujo) nem `baseline_sanidade.yaml` (Pitfall 2 — regenerar o
    sujo faz test_sanidade_checks falhar). Tickers 404 do Yahoo (MRFG3 etc.) degradam para o bloco
    falhas: com a CVM intacta, como no sujo.
  </action>
  <verify>
    <automated>test -f tests/fixtures/snapshot_sanidade_limpo_2026-07-15.yaml && git diff --quiet tests/fixtures/snapshot_sanidade_2026-07-14.yaml tests/fixtures/baseline_sanidade.yaml && echo OK-SUJO-INTACTO</automated>
  </verify>
  <done>Snapshot limpo gerado do código consertado; sujo e baseline intactos (git diff vazio).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Desacoplar o loader + provar a monotonicidade encolhendo</name>
  <read_first>
    - tests/helpers_sanidade.py:25-49 — onde CAMINHO_SNAPSHOT vive e como adicionar CAMINHO_SNAPSHOT_LIMPO.
    - tests/test_sanidade_baseline.py:65-107 — _pares_e_buckets_de_hoje (a rewirar) e as duas guardas de monotonicidade/bucket.
    - tests/test_sanidade_checks.py — confirmar que segue no sujo (NÃO mexer).
    - .planning/phases/09-ingest-o-correta-data/09-RESEARCH.md §mecanismo de regressão + a tabela "par que DEVE sumir".
    - CLAUDE.md — teste novo precisa de entrada em classificacao.yaml no mesmo commit; BLIND-04a (tupla de strings, sem número de nível).
  </read_first>
  <behavior>
    - _pares_e_buckets_de_hoje passa a ler o snapshot LIMPO → pares_hoje é subconjunto ESTRITO de pares_baseline.
    - Somem de pares_hoje: (BRSR6,SAN-03), (GOAU4,SAN-01), (CGRA4,SAN-01), (ITUB4,SAN-02), (BRSR6,SAN-02), (CSNA3,SAN-04), (ALUP11,SAN-04), (EQTL3,SAN-04), (MRFG3,SAN-04).
    - test_sanidade_checks (sujo) segue verde — os detectores ainda disparam no dado sujo congelado.
    - Nenhum par ressuscita; nenhum bucket empurrado.
  </behavior>
  <action>
    Em helpers_sanidade.py adicionar `CAMINHO_SNAPSHOT_LIMPO = RAIZ_REPO/"tests"/"fixtures"/
    "snapshot_sanidade_limpo_2026-07-15.yaml"`. Em test_sanidade_baseline.py, alterar SÓ
    `_pares_e_buckets_de_hoje` para carregar via `hs.carregar_snapshot_sanidade(path=hs.CAMINHO_SNAPSHOT_LIMPO)`
    — a régua (`_pares_e_buckets_do_baseline`) e os detectores (test_sanidade_checks) ficam no sujo.
    Adicionar um teste novo `test_os_alvos_consertados_sumiram_de_hoje` (categoria `contrato`) que afirma
    que os pares consertados NÃO estão em pares_hoje (tuplas de STRINGS, sem constante numérica — BLIND-04a),
    espelhando o positivo de test_o_baseline_contem_os_alvos_do_roadmap. Registrar a entrada em
    `tests/classificacao.yaml` NO MESMO commit. NÃO regenerar `snapshot_bancos_2026-07-12.yaml` (decisão
    travada: Fase 10) — deixar uma nota no summary registrando a decisão.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "sanidade" -q</automated>
  </verify>
  <done>pares_hoje ⊂ pares_baseline (monotonicidade encolhe); alvos consertados somem; detecção no sujo segue verde; suíte verde (0 failed); snapshot_bancos intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo/CVM → snapshot limpo (captura live) | dado de terceiros; alguns tickers 404 (degradação por ticker) |
| Fixtures de teste → suíte | régua (sujo) e objeto medido (limpo) não podem colidir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering (régua corrompida) | snapshot sujo / baseline | mitigate | Preservar intactos (git diff vazio); gerar arquivo NOVO para o limpo (Pitfall 2). |
| T-09-12 | Denial (Yahoo 404 na captura) | captura live do limpo | mitigate | Degradação por ticker never-raise (bloco falhas:), CVM intacta — a captura não aborta. |
| T-09-13 | Repudiation (tautologia mascarando falta de progresso) | _pares_e_buckets_de_hoje | mitigate | Rewirar para o limpo; teste explícito de que os alvos consertados sumiram. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 0 failed (golden_nivel quarentenados, 2 xfailed, 1 skipped).
- `git diff --quiet tests/fixtures/snapshot_sanidade_2026-07-14.yaml tests/fixtures/baseline_sanidade.yaml` → sujo/baseline intactos.
- `snapshot_bancos_2026-07-12.yaml` inalterado (decisão travada: Fase 10).
- `git diff config.yaml calibracao.lock.yaml` VAZIO (nenhum knob).
- Novo teste em classificacao.yaml (coleta não quebra).
</verification>

<success_criteria>
- Snapshot limpo gerado; sujo/baseline preservados; loader desacoplado.
- Monotonicidade encolhe (pares_hoje ⊂ pares_baseline); alvos consertados somem.
- snapshot_bancos não regenerado (Fase 10); suíte verde v2.4.
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-ingest-o-correta-data/09-05-SUMMARY.md`.
</output>
