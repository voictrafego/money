---
phase: 09-ingest-o-correta-data
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/report/presentation.py
  - src/analista/glossario.py
  - tests/test_dy_base.py
  - tests/classificacao.yaml
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "O DY declara explicitamente sua base — é BRUTO (antes de IRRF), sem ambiguidade"
    - "O rótulo/glossário do DY diz 'bruto' onde o usuário lê o número"
    - "Nenhum cálculo de imposto especulativo é introduzido (a Lei 15.270/2025 não foi verificada juridicamente)"
  artifacts:
    - path: "src/analista/report/presentation.py"
      provides: "header_dy declara a base bruta"
      contains: "bruto"
    - path: "src/analista/glossario.py"
      provides: "verbete do DY declara base bruta"
      contains: "bruto"
    - path: "tests/test_dy_base.py"
      provides: "teste de contrato de que a base do DY é declarada"
  key_links:
    - from: "presentation.header_dy"
      to: "usuário"
      via: "label/help declarando base bruta"
      pattern: "bruto"
---

<objective>
DATA-05 — o DY passa a DECLARAR sua base. Hoje `DY = DPA/Preço` (proventos brutos, sem imposto)
sem dizer isso ao usuário. A saída de MENOR RISCO (decisão desta fase) é declarar explicitamente
que o DY é BRUTO — satisfaz "o DY declara sua base" sem afirmar um cálculo de imposto não
verificado.

Purpose: A Lei 15.270/2025 (IRRF 17,5% sobre JCP, suposta vigência 01/01/2026) **NÃO foi
verificada em fonte oficial** (09-RESEARCH Assumption A2); há ainda a reforma da tributação de
dividendos que pode mexer na base. Aplicar imposto exigiria decompor JCP por ano e cravar
alíquota/vigência frágeis. **Decisão travada: declarar bruto, não calcular imposto especulativo.**
Output: rótulo + glossário declarando a base bruta + teste de contrato. Nenhum número de valuation
muda; nenhum motor tocado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ingest-o-correta-data/09-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Onde o DY é montado e explicado ao usuário (VERIFICADO). -->

presentation.header_dy(dy_recorrente, dy_atual)  (presentation.py:49-79) — monta o dict do
  st.metric do DY: chaves label, value, delta, delta_color, help, fallback. É o ponto de
  declaração da base para o usuário do app.

glossario.py — verbetes do DY (linhas ~32-33 e ~57-58): "Dividend Yield (DY) — proventos por
  ação ÷ preço da ação, em %". É onde a base bruta deve ser explicitada.

multiples.dividend_yield(dpa, preco)  (multiples.py:97-99) = DPA/Preço, sem imposto. NÃO alterar
  o cálculo (a decisão é rótulo, não conta).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declarar a base BRUTA do DY no header e no glossário</name>
  <read_first>
    - src/analista/report/presentation.py:41-79 — header_dy (as chaves help/label onde a base entra).
    - src/analista/glossario.py:30-60 — os verbetes do DY.
    - .planning/phases/09-ingest-o-correta-data/09-RESEARCH.md §DATA-05 + Open Question 4 + Assumption A2.
  </read_first>
  <action>
    Em `header_dy`, acrescentar ao texto `help` (ambos os caminhos, normal e fallback) a declaração
    explícita de que o DY é BRUTO — antes de IRRF (ex.: proventos brutos sobre o preço; o imposto de
    renda sobre dividendos/JCP não é descontado), per DATA-05. Em `glossario.py`, atualizar o(s)
    verbete(s) do DY para dizer que é a base BRUTA. NÃO alterar `multiples.dividend_yield` nem nenhum
    número. Não citar alíquota específica (a Lei 15.270/2025 não foi verificada juridicamente) —
    declarar "bruto" é a afirmação segura e suficiente.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "presentation or glossario or report" -q</automated>
  </verify>
  <done>header_dy e o glossário declaram a base bruta do DY; nenhum cálculo/valor mudou; suíte verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teste de contrato da base do DY (classificado)</name>
  <read_first>
    - tests/test_presentation_multiticker.py — padrão de teste do header (categoria contrato).
    - tests/classificacao.yaml — formato de entrada; completude imposta na coleta.
    - CLAUDE.md — todo teste novo precisa de entrada em classificacao.yaml NO MESMO commit; BLIND-04a (sem ticker+número não-trivial no mesmo assert).
  </read_first>
  <behavior>
    - O teste afirma que o help do header_dy (e/ou o verbete do glossário) contém a palavra "bruto" — asserção de contrato sobre string, sem literal de ticker + número.
  </behavior>
  <action>
    Criar `tests/test_dy_base.py` com um teste de contrato que chama `presentation.header_dy(...)` e
    afirma que a base bruta está declarada no `help` (substring "bruto", case-insensitive), nos dois
    caminhos (recorrente e fallback). Opcionalmente afirmar o mesmo no verbete do glossário. Sem
    constante numérica de nível e sem literal de ticker no mesmo assert (BLIND-04a). Adicionar a
    entrada em `tests/classificacao.yaml` como `contrato` NO MESMO commit.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "dy_base" -q</automated>
  </verify>
  <done>Teste novo verde e classificado; a coleta não quebra; suíte verde (0 failed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | Mudança de rótulo/texto; não cruza fonte de dado externa nem input de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Repudiation (afirmação incorreta ao usuário) | rótulo do DY | mitigate | Declarar "bruto" (fato verificável: DPA/Preço sem imposto); NÃO afirmar alíquota/líquido não verificados juridicamente. |
| T-09-10 | Information disclosure | — | accept | Texto informativo público; sem dado sensível. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 0 failed; coleta OK (novo teste classificado).
- `git diff` restrito a presentation.py, glossario.py, tests/test_dy_base.py, classificacao.yaml.
- `git diff config.yaml calibracao.lock.yaml multiples.py` VAZIO (nenhum knob, nenhum cálculo).
</verification>

<success_criteria>
- O DY declara sua base (bruto), sem ambiguidade, no ponto onde o usuário lê o número.
- Nenhum imposto especulativo calculado; nenhum valor de valuation alterado.
- Suíte verde v2.4.
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-ingest-o-correta-data/09-04-SUMMARY.md`.
</output>
