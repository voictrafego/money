---
phase: 09-ingest-o-correta-data
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - .planning/spikes/data-04-degrau-split.md
  - src/analista/ingest/prices.py
  - tests/test_ingest_split.py
  - tests/classificacao.yaml
autonomous: true
requirements: [DATA-04]
must_haves:
  truths:
    - "O site atual do degrau artificial de ~13% no ITUB4 está LOCALIZADO por medição, não suposto"
    - "O duplo ajuste de split é removido — a série por-ação do ITUB4 não tem degrau na fronteira da bonificação"
    - "Existe um teste novo que falharia se o degrau reaparecesse"
  artifacts:
    - path: ".planning/spikes/data-04-degrau-split.md"
      provides: "medição do onde-está-o-degrau-hoje (a ref prices.py:71-111 está obsoleta)"
      min_lines: 15
    - path: "tests/test_ingest_split.py"
      provides: "teste de que a série por-ação do ITUB4 não tem degrau artificial"
  key_links:
    - from: "spike (medição)"
      to: "conserto do site correto"
      via: "localizar antes de editar (Pitfall 6)"
      pattern: "degrau"
---

<objective>
DATA-04 — remover o duplo ajuste de split que cria um degrau artificial de ~13% no ITUB4.
**A referência do requisito (`prices.py:71-111`) está OBSOLETA** — o módulo foi reescrito nas
Fases 3-4. Este plano ataca em duas etapas obrigatórias: (1) um SPIKE de localização que mede
onde o degrau aparece hoje; (2) o conserto do site real + um teste que trava a regressão.

Purpose: É o único DATA sem site confirmado e sem assert SAN dedicado. Escrever a task de
conserto às cegas sobre a linha 71-111 (que hoje é o dataclass DadosMercado) não endereça nada
(Pitfall 6). Medir primeiro. Pode estar parcialmente mitigado pelos refactors das Fases 3-4 —
confirmar por medição.
Output: spike documentado + conserto (se o degrau existir) + teste novo classificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ingest-o-correta-data/09-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- O estado atual do split, VERIFICADO na pesquisa (a ref do requisito não bate mais). -->

prices._ajustar_por_split(hist)  (prices.py:93-133) — ajuste split-only derivado da coluna
  "Stock Splits" (auto_adjust=False). Produz dm.ohlc_ajustado, consumido SÓ pelos indicadores
  técnicos (core/indicators.py, Fase 5), NÃO pelo motor de valuation. serie_precos é nominal.

Hipótese medida (09-RESEARCH §DATA-04, confiança MEDIUM): o degrau de ~13% ≈ produto dos splits
  de 2025 do ITUB4 (1,1 × 1,03 = 1,133) e nasce da interação entre a série num_acoes da CVM
  (que carrega os degraus REAIS de bonificação) e alguma série por-ação/preço ajustada por split
  — double-count da bonificação 2024→2025 (1,1286×).

⚠ Após o plano 09-02, num_acoes vem da contagem oficial da CVM (não mais LL/LPA) — o degrau pode
  ter mudado de forma. Re-medir sobre o estado JÁ consertado (por isso este plano é wave 3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPIKE — localizar o degrau de ~13% no ITUB4 (a ref é obsoleta)</name>
  <read_first>
    - src/analista/ingest/prices.py:93-236 — _ajustar_por_split, coletar_mercado, serie_precos vs serie_precos_ajustada, dividendos_por_ano.
    - src/analista/core/fundamentals.py — como a série por-ação (LPA/DPA/ROE por ano) usa num_acoes e preço; procurar onde preço-por-ação e num_acoes se cruzam.
    - .planning/phases/09-ingest-o-correta-data/09-RESEARCH.md §DATA-04 + Assumption A1 + Pitfall 6.
    - git log de prices.py (Fases 3-4: c4c4b7a, 8964d55) — confirmar o refactor.
  </read_first>
  <action>
    Escrever um script/medição de diagnóstico (pode viver em scripts/ ou como célula do spike) que
    reconstrói a série por-ação histórica do ITUB4 (2015-2025) sobre o estado JÁ consertado pelos
    planos 09-01/09-02, e localiza se/onde existe um degrau artificial de ~13% na fronteira da
    bonificação 2024→2025 (1,1286×). Comparar: (a) série num_acoes oficial; (b) série de preço nominal;
    (c) qualquer série ajustada por split que a alimente. Registrar em
    `.planning/spikes/data-04-degrau-split.md`: o site REAL do degrau (arquivo:linha), se ele ainda
    existe pós-Fases 3-4 e pós-09-02, o mecanismo exato, e a magnitude medida. Se o degrau NÃO existir
    mais, documentar isso explicitamente (o conserto pode ser um teste-guarda apenas). NÃO editar código
    de produção nesta task.
  </action>
  <verify>
    <automated>test -f .planning/spikes/data-04-degrau-split.md && grep -qiE "degrau|site|itub4" .planning/spikes/data-04-degrau-split.md</automated>
  </verify>
  <done>O spike documenta o site real (arquivo:linha) do degrau, se ainda existe, o mecanismo e a magnitude — por medição, não suposição.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conserto do duplo split + teste-guarda (classificado)</name>
  <read_first>
    - `.planning/spikes/data-04-degrau-split.md` (produzido na Task 1) — o site real a corrigir.
    - src/analista/ingest/prices.py (o site apontado pelo spike) e tests/test_ingest_ohlc.py:286-297 (padrão de teste de split existente, categoria invariante/contrato).
    - tests/classificacao.yaml — o formato das entradas (completude imposta na coleta).
    - CLAUDE.md — pytest -k; todo teste novo precisa de entrada em classificacao.yaml NO MESMO commit.
  </read_first>
  <behavior>
    - Após o conserto, a série por-ação do ITUB4 não apresenta degrau artificial na fronteira da bonificação 2024→2025.
    - Se o spike concluir que o degrau já não existe: o teste apenas TRAVA essa ausência (guarda de regressão), sem edição de produção.
  </behavior>
  <action>
    Com base no site localizado pelo spike, remover o duplo ajuste de split (o Close do Yahoo já vem
    ajustado; não re-ajustar por num_acoes que já carrega o degrau real). Criar
    `tests/test_ingest_split.py` com um teste que falharia se o degrau de ~13% reaparecesse na série
    por-ação do ITUB4 (asserta sobre a AUSÊNCIA de salto na fronteira da bonificação — sem estampar R$
    de ticker: usar razão adimensional entre anos, respeitando BLIND-04a). Adicionar a entrada
    correspondente em `tests/classificacao.yaml` (categoria `contrato` ou `invariante`) NO MESMO commit.
    Se o spike concluiu que não há mais degrau, o conserto é só o teste-guarda + nota no summary.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "split or ingest_ohlc or indicators" -q</automated>
  </verify>
  <done>Duplo split removido (ou provado ausente); teste novo verde e classificado; suíte verde (0 failed); coleta não quebra (classificacao completa).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo (Close / Stock Splits) → série por-ação | preço de terceiros; auto_adjust pode mudar; splits históricos móveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering (double-count) | ajuste de split × num_acoes | mitigate | Medir o site antes de editar (spike); remover o ajuste duplicado; teste-guarda de regressão. |
| T-09-08 | Denial | Yahoo 404 / série vazia | accept | prices.py já degrada (never-raise) para None; fora do escopo desta correção. |
</threat_model>

<verification>
- Spike existe e localiza o site por medição (não pela ref obsoleta 71-111).
- `.venv/bin/python -m pytest -q` → 0 failed; coleta OK (novo teste em classificacao.yaml).
- `git diff config.yaml calibracao.lock.yaml` VAZIO (nenhum knob).
</verification>

<success_criteria>
- O degrau de ~13% do ITUB4 está localizado e removido (ou provado inexistente pós-refactors).
- Teste-guarda novo, verde e classificado.
- Suíte verde v2.4.
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-ingest-o-correta-data/09-03-SUMMARY.md`.
</output>
