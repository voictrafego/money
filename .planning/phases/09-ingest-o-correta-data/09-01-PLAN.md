---
phase: 09-ingest-o-correta-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/ingest/cvm.py
  - src/analista/ingest/build.py
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "c.dividendos inclui o JCP nas ~13 empresas que hoje o perdem (BRSR6 payout 10,3% → ~55,9%)"
    - "O par (BRSR6, SAN-03) deixa de disparar quando medido sobre dado consertado"
    - "c.lucro_liquido usa a base do controlador (3.11.01) quando presente, com fallback ao consolidado"
    - "c.patrimonio_liquido subtrai os minoritários junto com o lucro — nunca uma base cruzada nova"
    - "Os pares (MRFG3/CSNA3/ALUP11/EQTL3, SAN-04) deixam de disparar sobre dado consertado"
  artifacts:
    - path: "src/analista/ingest/cvm.py"
      provides: "dividendos_distribuidos apontado para o filtro amplo (dividendo OU JCP)"
      contains: "_distribuicoes_proventos_amplo"
    - path: "src/analista/ingest/build.py"
      provides: "lucro_liquido e patrimonio_liquido na base do controlador com fallback"
      contains: "lucro_controlador"
  key_links:
    - from: "cvm.fundamentos_do_ano"
      to: "c.dividendos (build.py)"
      via: "dividendos_distribuidos → dist_cvm → c.dividendos"
      pattern: "dividendos_distribuidos"
    - from: "build.montar_empresa"
      to: "c.lucro_liquido / c.patrimonio_liquido"
      via: "promoção dos campos de controlador a fonte de valuation"
      pattern: "lucro_controlador"
---

<objective>
Consertar as duas fontes de dado da CVM que os motores consomem: (1) DATA-01 — o filtro
estreito de proventos que descarta o JCP em ~13 empresas; (2) DATA-02 — a base consolidada
(com minoritários) de lucro e PL, que deveria ser a base do controlador.

Purpose: Curar a metade da Doença 2 que vive na fonte CVM. Os insumos limpos já foram lidos
de propósito na Fase 8 (`proventos_filtro_amplo`, `lucro_controlador`, `pl_nao_controladores`);
esta fase os PROMOVE a fonte de verdade. **Não inventa cálculo novo, não toca motor.**
Output: `c.dividendos`, `c.lucro_liquido` e `c.patrimonio_liquido` corretos, provando o
conserto pelos asserts SAN-03/SAN-04 da Fase 8 pararem de disparar (a prova formal vem no
plano 09-05, que regenera o snapshot limpo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ingest-o-correta-data/09-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Insumos LIMPOS já lidos na Fase 8 (leitura, não conserto). Esta fase os promove a fonte. -->

cvm.fundamentos_do_ano(cd_cvm, ano) devolve dict com (entre outras) as chaves:
  - "lucro_liquido"            : consolidado (3.11/3.13/3.09) — a fonte SUJA de hoje
  - "lucro_controlador"        : 3.11.01 (controlador) — insumo limpo, hoje só diagnóstico
  - "patrimonio_liquido"       : PL consolidado (2.03/2.08) — inclui minoritários
  - "pl_nao_controladores"     : minoritários no PL (2.03.09/2.07.02/2.08.09) — insumo limpo
  - "dividendos_distribuidos"  : _distribuicoes_proventos (filtro ESTREITO "dividendo") — SUJO
  - "proventos_filtro_amplo"   : _distribuicoes_proventos_amplo (dividendo OU JCP) — CORRETO

cvm._distribuicoes_proventos_amplo(df, cd_cvm)  (cvm.py:183) — já existe, já testado (Fase 8);
  regex de inclusão: "dividendo|juros sobre.*capital". É a implementação correta do DATA-01.

build.montar_empresa (build.py:83-101): c.lucro_liquido[ano]=f["lucro_liquido"];
  c.patrimonio_liquido via loop; c.lucro_controlador/c.pl_nao_controladores são diagnóstico
  paralelo. c.dividendos vem de dist_cvm (build.py:94-95,131-132).

Detectores da Fase 8 que provam o conserto (core/sanidade.py):
  - SAN-03 sinal (a): Σ proventos_filtro_amplo / Σ dividendos > 1,10 (interno à CVM)
  - SAN-04: razao = lucro_liquido / lucro_controlador no último ano; flaga |razao-1|>0,10
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DATA-01 — apontar dividendos_distribuidos para o filtro amplo (JCP capturado)</name>
  <read_first>
    - src/analista/ingest/cvm.py — LER `_distribuicoes_proventos` (155-180), `_distribuicoes_proventos_amplo` (183-214) e a chave `dividendos_distribuidos` em `fundamentos_do_ano` (286). A gêmea ampla é idêntica exceto o regex de inclusão (linha 209).
    - src/analista/ingest/build.py:120-132 — o comentário BUG-JCP já corrigido na Fase 8 (a CVM perde o JCP, não o Yahoo) e o fluxo dist_cvm → c.dividendos.
    - .planning/phases/09-ingest-o-correta-data/09-RESEARCH.md §DATA-01 — a armadilha de contagem em dobro (A4) e a prova via SAN-03 sinal (a).
    - src/analista/core/sanidade.py:179-213 — checar_san03, sinal (a): razão Σ amplo / Σ estreito.
    - tests/test_sanidade_checks.py:64-73 — os asserts SAN-03 (BRSR6 dispara; ITUB4 não).
  </read_first>
  <behavior>
    - Após o conserto, para o BRSR6: Σ proventos_filtro_amplo ≈ Σ dividendos → SAN-03 sinal (a) ≈ 1 (deixa de flagar sobre dado consertado).
    - Para os 4 grandes bancos (ITUB4/BBDC4/BBAS3) que já capturavam JCP por acidente: c.dividendos NÃO muda materialmente (sem contagem em dobro).
    - c.proventos_filtro_amplo continua populado (o detector segue existindo).
  </behavior>
  <action>
    Em cvm.py `fundamentos_do_ano`, trocar a fonte da chave `dividendos_distribuidos` de
    `_distribuicoes_proventos(dfc, cd_cvm)` para `_distribuicoes_proventos_amplo(dfc, cd_cvm)`
    (linha ~286). Manter `_distribuicoes_proventos` no módulo (é consumida por
    `test_cvm_distribuicoes` como referência do filtro estreito) — apenas deixa de alimentar
    `c.dividendos`. Atualizar a docstring da chave para dizer que agora casa "dividendo OU JCP"
    (per DATA-01). NÃO trocar de fonte para o Yahoo — a correção é ampliar o filtro DENTRO da CVM.
    Verificar por medição (contra o cache CVM) que nos 4 grandes bancos não há linha única casando
    ambos os termos somada em dobro: a soma é por LINHA da DFC 6.03.*, cada linha conta uma vez.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "sanidade or cvm_distribuicoes or ingest_unit" -q</automated>
  </verify>
  <done>c.dividendos passa a incluir o JCP; a suíte segue verde (0 failed); os 4 bancos não somam JCP em dobro (medido).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DATA-02 — base do controlador para lucro E PL (mover os dois juntos)</name>
  <read_first>
    - src/analista/ingest/build.py:83-101 — onde c.lucro_liquido e c.patrimonio_liquido são populados, e onde c.lucro_controlador/c.pl_nao_controladores já são lidos (diagnóstico).
    - src/analista/core/sanidade.py:237-272 — checar_san04: razao = lucro_liquido/lucro_controlador; sinal_invertido (CSNA3).
    - tests/test_sanidade_checks.py:77-93 — os asserts SAN-04 (MRFG3/CSNA3/ALUP11/EQTL3 disparam; ITUB4/BBDC4/BRSR6 não).
    - .planning/phases/09-ingest-o-correta-data/09-RESEARCH.md §DATA-02 + Pitfall 3 — o fallback obrigatório e a proibição de base cruzada nova.
  </read_first>
  <behavior>
    - Empresa COM linha de controlador (consolida minoritários): c.lucro_liquido = lucro_controlador; c.patrimonio_liquido = PL_consolidado − pl_nao_controladores. SAN-04 razao → 1 (deixa de flagar).
    - CSNA3 (sinal_invertido): c.lucro_liquido passa a refletir o prejuízo do controlador (muda de sinal) — isso é o conserto, não regressão.
    - Empresa single-entity (lucro_controlador None): c.lucro_liquido = consolidado; PL inalterado (fallback). Sem regressão nos limpos.
  </behavior>
  <action>
    Em build.py `montar_empresa`, no loop por ano (~83-89): definir `c.lucro_liquido[ano]` como
    `f["lucro_controlador"]` quando `f["lucro_controlador"] is not None`, senão `f["lucro_liquido"]`
    (fallback consolidado, per DATA-02). Para o PL: `c.patrimonio_liquido[ano] = f["patrimonio_liquido"]
    − (f["pl_nao_controladores"] or 0)` — subtrair minoritários SOMENTE quando o lucro também foi para o
    controlador, para NUNCA deixar lucro no controlador e PL no consolidado (base cruzada nova = a doença
    que o SAN-04 detecta, Pitfall 3). Manter c.lucro_controlador/c.pl_nao_controladores como diagnóstico
    (linhas 99-101) intactos. Comentar só o "porquê" do acoplamento lucro↔PL.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "sanidade or cvm_single_entity or fundamentals_consistencia or ingest_unit" -q</automated>
  </verify>
  <done>c.lucro_liquido e c.patrimonio_liquido na base do controlador com fallback; suíte verde (0 failed); bancos limpos (ITUB4/BBDC4/BRSR6) seguem sem SAN-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CVM ZIP → parser | ZIP público de terceiros; schema de conta pode mudar, valores podem faltar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering (integridade de dado) | `_distribuicoes_proventos_amplo` / soma da DFC 6.03.* | mitigate | Somar por LINHA (cada linha conta uma vez); confirmar por medição que os 4 bancos não somam JCP em dobro. Fonte já validada na Fase 8. |
| T-09-02 | Denial (linha ausente) | `lucro_controlador` / `pl_nao_controladores` None | mitigate | Fallback explícito ao consolidado quando a linha do controlador está ausente; never-raise do `aplicar_sanidade` preserva resposta. |
| T-09-03 | Information disclosure | — | accept | Ingestão read-only de dado público; sem PII, sem segredo, sem rede de escrita. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 0 failed (golden_nivel quarentenados, 2 xfailed, 1 skipped).
- Nenhum knob mexido: `git diff calibracao.lock.yaml config.yaml` VAZIO.
- `git diff` restrito a `src/analista/ingest/cvm.py` e `src/analista/ingest/build.py`.
- A prova ticker-a-ticker (pares SAN-03/SAN-04 sumindo) é formalizada no plano 09-05 (snapshot limpo). Aqui o gate é a suíte verde + nenhum motor/knob tocado.
</verification>

<success_criteria>
- DATA-01: c.dividendos inclui o JCP; sem contagem em dobro nos 4 bancos (medido).
- DATA-02: lucro e PL na base do controlador com fallback; sem base cruzada nova.
- Suíte verde v2.4 (0 failed); zero mudança em `config.yaml`/`calibracao.lock.yaml`.
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-ingest-o-correta-data/09-01-SUMMARY.md`.
</output>
