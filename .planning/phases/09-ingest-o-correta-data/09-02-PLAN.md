---
phase: 09-ingest-o-correta-data
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/analista/ingest/cvm.py
  - src/analista/ingest/build.py
autonomous: true
requirements: [DATA-03]
must_haves:
  truths:
    - "num_acoes deixa de ser derivado de LL/LPA — a fonte por ano é a contagem oficial da CVM"
    - "A contagem oficial vem do composicao_capital (QT_ACAO_TOTAL_CAP_INTEGR − QT_ACAO_TOTAL_TESOURO)"
    - "A escala (milhares×unidades) é detectada cruzando com impliedSharesOutstanding, nunca usada crua"
    - "O fallback usa impliedSharesOutstanding (ON+PN), nunca sharesOutstanding (só uma classe)"
    - "Os pares (GOAU4/CGRA4, SAN-01) e (ITUB4/BRSR6, SAN-02) deixam de disparar sobre dado consertado"
  artifacts:
    - path: "src/analista/ingest/cvm.py"
      provides: "leitura nova de composicao_capital com join CNPJ→CD_CVM via cad_cia_aberta.csv"
      contains: "composicao_capital"
    - path: "src/analista/ingest/build.py"
      provides: "num_acoes da contagem oficial; _fator_unit refeito/aposentado"
      contains: "implied_shares_outstanding"
  key_links:
    - from: "cvm (novo leitor composicao_capital)"
      to: "build.num_acoes"
      via: "contagem oficial por ano, com escala detectada"
      pattern: "QT_ACAO_TOTAL"
    - from: "build.num_acoes"
      to: "SAN-01 / SAN-02 (sanidade.py)"
      via: "num_acoes[ult] × preço ≈ market_cap; sem salto artificial"
      pattern: "num_acoes"
---

<objective>
DATA-03 — parar de derivar `num_acoes` de `LL/LPA` com bases cruzadas (a causa-raiz da dispersão
em 41 dos 104 tickers). A fonte por ano passa a ser a contagem OFICIAL da CVM
(`dfp_cia_aberta_composicao_capital_{ano}.csv`, já dentro do ZIP baixado), validada/ancorada pelo
`impliedSharesOutstanding` (ON+PN) do Yahoo.

Purpose: É o conserto de maior alavancagem da fase (41/104 tickers). O `impliedSharesOutstanding`
já é lido (Fase 8); esta fase adiciona a leitura nova do `composicao_capital` e reescreve a
derivação de `num_acoes` no build. **Não toca motor, não toca knob.**
Output: `c.num_acoes` correto por ano, provado pelos SAN-01/SAN-02 pararem de disparar (formalizado
no plano 09-05). É a única leitura NOVA da fase — as 4 armadilhas medidas são obrigatórias.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ingest-o-correta-data/09-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Insumo NOVO (dentro do ZIP já baixado) + âncora limpa já lida na Fase 8. -->

Arquivo dentro de data/cvm/dfp_cia_aberta_{ano}.zip:
  dfp_cia_aberta_composicao_capital_{ano}.csv  (sep=";", encoding latin-1)
  Colunas (MEDIDAS): CNPJ_CIA, DT_REFER, VERSAO, DENOM_CIA,
    QT_ACAO_ORDIN_CAP_INTEGR, QT_ACAO_PREF_CAP_INTEGR, QT_ACAO_TOTAL_CAP_INTEGR,
    QT_ACAO_ORDIN_TESOURO, QT_ACAO_PREF_TESOURO, QT_ACAO_TOTAL_TESOURO
  Contagem = QT_ACAO_TOTAL_CAP_INTEGR − QT_ACAO_TOTAL_TESOURO.
  Chaveado por CNPJ_CIA — NÃO por CD_CVM (armadilha 2).
  ⚠ ITUB4 vem em MILHARES (QT_ACAO_TOTAL_CAP_INTEGR=11026869 = 11,02 bn); GOAU4/CGRA4/CSNA3/
    EQTL3/ALUP11/MRFG3 vêm em UNIDADES (armadilha 3).

data/cvm/cad_cia_aberta.csv (sep=";", latin-1) — colunas incluem CNPJ_CIA, CD_CVM.
  É o mapa CNPJ↔CD_CVM para o join (o projeto já baixa este arquivo).

Insumo limpo já lido (prices.py:164 → CompanyData.implied_shares_outstanding): ON+PN,
  NUNCA sharesOutstanding (só a classe negociada). Bate com a contagem oficial <0,3% em 5/5.
  É valor ÚNICO (snapshot atual), não série — usar como âncora do ÚLTIMO ano e validador de escala.

Site atual (VERIFICADO): build.py:91-93 (contagem_cvm=abs(LL/lpa)), build.py:25-38 (_fator_unit
  CORROMPIDO: devolve 5 p/ ALUP11, verdadeiro 3), build.py:109-118 (fallback sharesOutstanding).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cvm.py — leitor de composicao_capital com join CNPJ→CD_CVM</name>
  <read_first>
    - src/analista/ingest/cvm.py — `_ler_demonstracao` (80-101, padrão de leitura de CSV dentro do ZIP), `_norm`, `baixar_dfp`, e o formato de `fundamentos_do_ano`.
    - data/cvm/dfp_cia_aberta_2025.zip (arquivo composicao_capital) e data/cvm/cad_cia_aberta.csv — as colunas listadas em <interfaces>.
    - .planning/phases/09-ingest-o-correta-data/09-RESEARCH.md §DATA-03 (as 4 armadilhas) + Pitfall 4.
  </read_first>
  <behavior>
    - Para ITUB4 (CD_CVM via cad), a contagem bruta lida é 11026869 (milhares); após detecção de escala vira ~11,02 bn, batendo com implied_shares_outstanding <0,3%.
    - Para GOAU4/CGRA4 (unidades), a contagem crua já está em unidades — fator de escala = 1.
    - CNPJ ausente no cad → contagem None (não avaliável, never-raise).
  </behavior>
  <action>
    Adicionar em cvm.py: (1) um leitor `_composicao_capital(ano)` que abre
    `dfp_cia_aberta_composicao_capital_{ano}.csv` de dentro do ZIP (mesmo padrão de `_ler_demonstracao`,
    latin-1, sep=";"), devolvendo por CNPJ_CIA a contagem `QT_ACAO_TOTAL_CAP_INTEGR − QT_ACAO_TOTAL_TESOURO`;
    (2) um mapa CNPJ→CD_CVM lido de `cad_cia_aberta.csv` (cacheado com lru_cache), para casar por CD_CVM
    (armadilha 2 — chave é CNPJ_CIA, não CD_CVM). Expor uma função `contagem_oficial_do_ano(cd_cvm, ano)`
    devolvendo a contagem CRUA por ano (sem escala aplicada — a escala é decidida no build, onde o implied
    está disponível). None quando o CNPJ não casa ou o arquivo falta (never-raise). NÃO aplicar escala aqui.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "cvm or ingest_unit or ingest_resolucao" -q</automated>
  </verify>
  <done>cvm expõe a contagem oficial por ano (crua, sem escala), casada via CNPJ→CD_CVM; suíte verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build.py — num_acoes da contagem oficial + escala detectada + _fator_unit refeito</name>
  <read_first>
    - src/analista/ingest/build.py:25-38 (_fator_unit corrompido), 76-118 (acoes_atual, contagem_cvm, fallback), e como origem_num_acoes é carimbada (linhas 114-118).
    - src/analista/core/sanidade.py:88-114 (SAN-01) e 117-168 (SAN-02) — o que precisa virar ~1 / sem salto.
    - tests/test_sanidade_checks.py:30-60 — os alvos SAN-01/SAN-02.
    - .planning/phases/09-ingest-o-correta-data/09-RESEARCH.md §DATA-03 armadilhas 1, 3, 4 + Pitfalls 4 e 5.
  </read_first>
  <behavior>
    - num_acoes[ano] passa a vir da contagem oficial da CVM por ano, com fator de escala inferido do último ano contra implied_shares_outstanding e aplicado à série inteira.
    - ITUB4 2019 passa a ter bilhões de ações (não milhões); o salto artificial ÷1000 some (SAN-02 limpo).
    - GOAU4/CGRA4: num_acoes[ult] × preço ≈ market_cap (SAN-01 limpo).
    - Ano sem contagem oficial → fallback impliedSharesOutstanding (NÃO sharesOutstanding), carimbando origem.
    - ALUP11: fator de unit derivado corretamente (3, não 5) OU dispensável — o _fator_unit velho não consome a contagem nova.
  </behavior>
  <action>
    Em build.py: (1) trocar a fonte de `num_acoes` por ano da derivação `LL/lpa` (linha 91-93) para
    `cvm.contagem_oficial_do_ano(cd_cvm, ano)`. (2) Detectar a escala (armadilha 3): calcular o fator
    entre a contagem oficial do ÚLTIMO ano e `dm.implied_shares_outstanding` (ON+PN); arredondar para a
    potência de 1000 mais próxima (1 ou 1000) e aplicar à série inteira — nunca usar a contagem crua
    (reintroduziria o ×1000, Pitfall 4). (3) Fallback (armadilha 1): quando falta contagem oficial num
    ano, usar `dm.implied_shares_outstanding`, NUNCA `dm.num_acoes` (=sharesOutstanding, só a classe);
    carimbar `origem_num_acoes`. (4) Refazer/aposentar `_fator_unit` (armadilha 4/Pitfall 5): com a
    contagem oficial ON+PN, o fator de unit é derivável corretamente (988.880.601/329.626.866 = 3,000) ou
    dispensável — NÃO deixar o `_fator_unit` velho (que devolve 5 p/ ALUP11) consumindo a contagem nova.
    Preservar `c.lpa_cvm` como diagnóstico (não é mais fonte de num_acoes).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "sanidade or ingest_unit or fundamentals_consistencia or ingest_resolucao" -q</automated>
  </verify>
  <done>num_acoes vem da contagem oficial escalada; _fator_unit não corrompe a contagem nova; fallback usa implied (ON+PN); suíte verde (0 failed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CVM ZIP (composicao_capital) → parser | novo CSV de terceiros; escala inconsistente entre empresas (milhares×unidades) |
| Yahoo (impliedSharesOutstanding) → âncora | valor único, pode faltar (404) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering (escala) | `composicao_capital` cru | mitigate | Detectar escala cruzando com impliedSharesOutstanding do último ano; jamais usar contagem crua (Pitfall 4). |
| T-09-05 | Tampering (chave errada) | join CNPJ×CD_CVM | mitigate | Join explícito via cad_cia_aberta.csv (armadilha 2); CNPJ ausente → None, never-raise. |
| T-09-06 | Denial (âncora ausente) | implied None (MRFG3 404) | mitigate | Sem âncora → escala não confirmável; degrada para não-avaliável sem abortar (SAN-06). |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 0 failed (quarentenados fora, 2 xfailed, 1 skipped).
- `git diff` restrito a `cvm.py` e `build.py`; `config.yaml`/`calibracao.lock.yaml` VAZIOS.
- Medição manual: ITUB4 2019 tem bilhões (não milhões) de ações; GOAU4 num_acoes×preço/market_cap ≈ 1.
- A prova formal (pares SAN-01/SAN-02 sumindo) vem no plano 09-05.
</verification>

<success_criteria>
- num_acoes vem da contagem oficial da CVM por ano, com escala detectada; fallback = impliedSharesOutstanding.
- _fator_unit refeito/aposentado — não divide por 5 a ALUP11.
- Suíte verde v2.4; zero mudança em knob.
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-ingest-o-correta-data/09-02-SUMMARY.md`.
</output>
