---
phase: 10-primitivas-sem-vi-s-prim
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/analista/core/fundamentals.py
  - tests/test_fundamentals_valuation.py
  - tests/test_normalizacao.py
  - tests/classificacao.yaml
autonomous: true
requirements: [PRIM-02, PRIM-03]
must_haves:
  truths:
    - "roe_valuation devolve a mediana da série de ROEs anuais (roe(a)), não mais o cruzamento base_lucro_normalizada ÷ PL do último ano."
    - "Para o ITUB4 (snapshot de bancos) roe_valuation cai ≈18,0%; a direção vinda do dado limpo é levemente PARA BAIXO (~19,8→18,5), NÃO de 16,1 para 18,0."
    - "roe_valuation e roe_terminal (_roe_through_cycle) passam a usar a MESMA estatística (mediana dos roe(a)) — não divergem mais."
    - "serie_lucro_normalizada devolve a série CRUA de lucro (sem winsorização temporal); a função norm.serie_winsorizada continua viva para o screening."
  artifacts:
    - path: "src/analista/core/fundamentals.py"
      provides: "roe_valuation = median(roe(a)); serie_lucro_normalizada = série crua"
      contains: "median"
    - path: "tests/test_fundamentals_valuation.py"
      provides: "testes de roe_valuation==median(roe(a)) e serie crua"
  key_links:
    - from: "src/analista/core/fundamentals.py roe_valuation"
      to: "self.roe(a) sobre anos_ordenados + statistics.median"
      via: "espelha payout_valuation / _roe_through_cycle"
      pattern: "roe\\(a\\)|roe\\(ano\\)"
    - from: "src/analista/core/fundamentals.py serie_lucro_normalizada"
      to: 'self.serie("lucro_liquido") cru'
      via: "remove o wrapper norm.serie_winsorizada"
      pattern: "serie\\(.lucro_liquido.\\)"
---

<objective>
PRIM-02 e PRIM-03 — duas primitivas de `fundamentals.py`, independentes do estimador do Plano 01.

PRIM-02: `roe_valuation` hoje cruza bases temporais (base de lucro de 3a ÷ PL do último ano, `fundamentals.py:160-168`). Passa a ser a **mediana da série de ROEs anuais**, reusando `roe(ano)` (lucro_t ÷ PL médio(t-1,t)) — exatamente o que `_roe_through_cycle` (report.py:184-198) já computa. Espelha `payout_valuation`. Ganho de consistência: o `roe0` e o `roe_terminal` do RIM passam a usar a mesma estatística. É a mudança que, sozinha, move o RIM do ITUB4 de 32,88 → 31,52 (quebra o golden — o Plano 04 o deleta).

PRIM-03: `serie_lucro_normalizada` para de winsorizar a série temporal — devolve a série crua. A Fase 10 apenas REMOVE o viés da winsorização; o desenho do `g` robusto é Fase 11 (fronteira limpa). `norm.serie_winsorizada` continua viva (screening.py:253,272 ainda a usa, Cap. 8 elegibilidade, fora do escopo PRIM).

Purpose: tirar o cruzamento de bases do ROE e o clamp de tendência da winsorização temporal, sem tocar em motor/knob.
Output: `fundamentals.py` com roe_valuation por mediana-de-ROEs e serie_lucro_normalizada crua.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/10-primitivas-sem-vi-s-prim/10-RESEARCH.md
@.planning/phases/10-primitivas-sem-vi-s-prim/10-PATTERNS.md

<interfaces>
<!-- Extraído do código. -->
fundamentals.py:
  def roe(self, ano) -> Optional[float]       # :110 — lucro_t ÷ PL médio(t-1,t); None se falta PL-1 (base ÚNICA, WR-01)
  def anos_ordenados(self) -> List[int]       # :74
  def payout_valuation(self) -> Optional[float]  # :96-108 — SHAPE a espelhar: [payout(a) for a in anos_ordenados()] → norm.mediana_payout(serie)
  def base_lucro_normalizada(anos_media=3, winsor=0.10)  # :140-143
  def roe_valuation(anos_media=3, winsor=0.10) -> Optional[float]  # :155-168 — o cruzamento a MATAR
  def serie_lucro_normalizada(winsor=0.10) -> List[float]  # :145-148 — hoje norm.serie_winsorizada(serie("lucro_liquido"))

report.py:
  _roe_through_cycle(c, rim_cfg)  # :184-198 — JÁ faz median([c.roe(a) for a in c.anos_ordenados()]) com <3→None
  report.py:243 roe0=c.roe_valuation()  ·  report.py:250 roe_terminal=_roe_through_cycle(...)
  report.py:378 lucros = c.serie_lucro_normalizada() → growth.crescimento_log_linear (g_historico)

Fronteira a preservar: roe_valuation()/lpa_valuation()/serie_lucro_normalizada() chamáveis SEM args nas 3 superfícies (Analisar + Ranking app + Ranking cli).
screening.py:253,272 usa norm.serie_winsorizada — NÃO remover a função.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes de roe_valuation==median(roe(a)) e serie crua (RED)</name>
  <read_first>
    - tests/test_normalizacao.py (test_mediana_payout_* 88-128 como template de teste de agregação)
    - src/analista/core/fundamentals.py (roe 110-125, payout_valuation 96-108, roe_valuation 155-168, serie_lucro_normalizada 145-148)
    - src/analista/report/report.py (_roe_through_cycle 184-198 — a estatística-gêmea)
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-RESEARCH.md §"PRIM-02" (tabela de números medidos; ITUB4 18,0% exato no snapshot de bancos) e Pitfall 1 (números-âncora obsoletos)
    - tests/classificacao.yaml + tests/conftest.py (completude na coleta)
    - tests/helpers_sanidade.py (carregar_snapshot_sanidade — loader offline dos 104, se precisar de um CompanyData real)
  </read_first>
  <behavior>
    - roe_valuation() == median([roe(a) for a in anos_ordenados() se roe(a) is not None]) para um CompanyData sintético/de snapshot — asserção de MÉTODO, não de R$/%.
    - Consistência: roe_valuation() usa a MESMA estatística que _roe_through_cycle (mediana da série de roe(a)); um CompanyData com série conhecida devolve o mesmo número nos dois.
    - Fronteira None: série sem nenhum roe(a) válido → None; roe_valuation continua chamável sem args.
    - Ano de prejuízo ignorado pela mediana (CSNA3-like: a mediana não vira negativa por 1 ano ruim).
    - serie_lucro_normalizada() devolve a série CRUA (== self.serie("lucro_liquido")); um outlier NÃO é mais clampado (contraste com a série winsorizada).
  </behavior>
  <action>
Criar `tests/test_fundamentals_valuation.py` com os testes de método acima (espelhando `test_mediana_payout_*`), usando um `CompanyData` sintético com série de ROE/lucro conhecida (ou um ticker do loader offline dos 104). Adicionar um teste de PRIM-03 (série crua ≠ série winsorizada num caso com outlier ≥5 pontos) — pode viver em `tests/test_normalizacao.py` ou no arquivo novo. Registrar cada teste novo em `tests/classificacao.yaml` (classe `invariante` para as igualdades de método; `contrato` para fronteiras None/sem-args) no MESMO diff. Rodar → RED. Escrever os asserts como MÉTODO (igualdade estrutural), nunca como golden de nível `ticker == valor`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "fundamentals_valuation or serie_lucro or roe_valuation" -o addopts="" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Testes novos existem e FALHAM antes da Task 2 (RED).
    - Nenhum assert é `ticker == R$/%` (BLIND-04a): são igualdades de método (`roe_valuation == median(roe(a))`).
    - Coleta sem órfão: `.venv/bin/python -m pytest -k fundamentals_valuation --collect-only` sem `CLASSIFICACAO ORFA`.
  </acceptance_criteria>
  <done>Testes de método de PRIM-02/PRIM-03 escritos, classificados e vermelhos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: roe_valuation = median(roe(a)) + serie_lucro_normalizada crua (GREEN)</name>
  <read_first>
    - src/analista/core/fundamentals.py (roe_valuation 155-168; serie_lucro_normalizada 145-148; payout_valuation 96-108 a espelhar; roe 110-125 a reusar)
    - src/analista/report/report.py (_roe_through_cycle 184-198 — considerar extrair helper compartilhado p/ roe0 e roe_terminal não divergirem)
    - src/analista/core/screening.py:253,272 (confirmar que norm.serie_winsorizada CONTINUA sendo usada aqui — não remover a função)
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-PATTERNS.md §"roe_valuation" e §"serie_lucro_normalizada"
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-RESEARCH.md §"Don't Hand-Roll" (reusar roe(ano), não inventar 2ª semântica de ROE)
  </read_first>
  <action>
Reescrever `roe_valuation` (fundamentals.py:155-168) para: `serie = [self.roe(a) for a in self.anos_ordenados()]`; `validos = [r for r in serie if r is not None]`; `return float(median(validos)) if validos else None`. Reusar `roe(ano)` — NÃO inventar segunda semântica de ROE (Fronteira FIX-04). Manter a assinatura `roe_valuation(anos_media=3, winsor=0.10)` chamável sem args (os params ficam inertes/aceitos por compatibilidade das 3 superfícies). Opcional recomendado: extrair um helper compartilhado com `_roe_through_cycle` para que `roe0` e `roe_terminal` não possam divergir (RESEARCH §Don't-Hand-Roll).
Reescrever `serie_lucro_normalizada` (145-148) para devolver `self.serie("lucro_liquido")` CRU (sem `norm.serie_winsorizada`). NÃO remover a função `norm.serie_winsorizada` (screening.py ainda a usa). NÃO desenhar o g robusto (Fase 11). NÃO tocar em motor/knob/config/lock.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "fundamentals_valuation or serie_lucro or roe_valuation or mediana_payout" 2>&1 | tail -20 && .venv/bin/python -m pytest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `roe_valuation` não contém mais `base_lucro_normalizada`/`roe_medio`/`pl_ini`/`pl_fim`: `grep -n "roe_medio\|pl_ini" src/analista/core/fundamentals.py` não aponta para o corpo de roe_valuation (só para roe(ano)).
    - `serie_lucro_normalizada` devolve cru: `grep -n "serie_winsorizada" src/analista/core/fundamentals.py` retorna vazio (o wrapper saiu de fundamentals); `grep -c "serie_winsorizada" src/analista/core/screening.py` >= 1 (função preservada para o screening).
    - Os testes da Task 1 passam (GREEN); `test_mediana_payout_*` continuam verdes (payout intocado).
    - Suíte default `.venv/bin/python -m pytest` → 0 failed. (O golden ITUB4 32,88 é `golden_nivel`, deselecionado por default — sua quebra é confirmada e tratada no Plano 04.)
    - `git diff config.yaml calibracao.lock.yaml` VAZIO neste plano (nenhum knob movido).
  </acceptance_criteria>
  <done>roe_valuation = mediana de roe(a); serie_lucro_normalizada crua; norm.serie_winsorizada preservada para o screening; suíte default 0 failed; zero edição de knob.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Reescrita de duas primitivas puras sobre dados já ingeridos. Sem nova entrada externa, auth ou rede. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Information Disclosure | — | accept | Nenhum dado sensível; cálculo determinístico local. Sem superfície nova. |
| T-10-04 | Denial of Service | roe_valuation sobre série vazia/só-None | mitigate | Fronteira `None` explícita (`if validos else None`); chamável sem args preservado. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest` → 0 failed.
- roe_valuation == mediana de roe(a) provado por teste de método; consistência com _roe_through_cycle.
- serie_lucro_normalizada crua; norm.serie_winsorizada ainda usada pelo screening.
- `git diff config.yaml calibracao.lock.yaml` vazio.
</verification>

<success_criteria>
PRIM-02 e PRIM-03 entregues: ROE de valuation por mediana da série anual (consistente com o roe_terminal), série de lucro crua para o CAGR, sem tocar em motor, knob ou lock.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/10-primitivas-sem-vi-s-prim/10-02-SUMMARY.md`.
</output>
