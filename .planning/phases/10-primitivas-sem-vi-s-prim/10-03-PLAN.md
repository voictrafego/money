---
phase: 10-primitivas-sem-vi-s-prim
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - src/analista/ingest/macro.py
  - src/analista/report/report.py
  - src/analista/cli.py
  - app.py
  - src/analista/backtest.py
  - config.yaml
  - tests/test_macro_ipca.py
  - tests/test_deflacao_ciclica.py
  - tests/classificacao.yaml
autonomous: true
requirements: [PRIM-04]
must_haves:
  truths:
    - "A base do motor cíclico é expressa em reais do último ano (deflacionada por IPCA), então uma série inflacionária tem base real > base nominal."
    - "macro.ipca_deflatores_anuais devolve {ano: fator para o último ano} da série anual do IPCA do BCB, com degradação graciosa (falha de rede → [] / fallback), como _selic_historico."
    - "analisar_acao permanece OFFLINE e determinística — os deflatores são resolvidos nos entry points (cli/app) e carimbados em cfg, exatamente como rf_local; o backtest lê o valor carimbado do snapshot."
    - "O deflator do IPCA é dado macro objetivo do BCB (como o rf), NÃO um knob de valuation — nenhum 4º grau de liberdade, nenhuma escolha livre de ano-base (D-03: reais do último ano)."
  artifacts:
    - path: "src/analista/ingest/macro.py"
      provides: "ipca_deflatores_anuais(anos) — fetch BCB SGS anual + retry, copiando _selic_historico"
      contains: "def ipca_deflatores_anuais"
    - path: "src/analista/report/report.py"
      provides: "ramo 'normalizado' deflaciona a série antes de norm.media_ciclo, lendo cfg['macro']['ipca_deflatores']"
      contains: "ipca_deflatores"
    - path: "config.yaml"
      provides: "bloco macro.ipca_deflatores (default vazio; entry points sobrescrevem)"
  key_links:
    - from: "src/analista/cli.py :77 / app.py :866 (entry points)"
      to: "cfg['macro']['ipca_deflatores'] = macro.ipca_deflatores_anuais(...)"
      via: "stamping no entry point (rede só aqui), espelha rf_local"
      pattern: "ipca_deflatores"
    - from: "src/analista/report/report.py ramo 'normalizado'"
      to: "cfg['macro']['ipca_deflatores'] (lido offline)"
      via: "deflaciona lucro antes de media_ciclo — NUNCA chama macro/requests"
      pattern: "ipca_deflatores"
    - from: "src/analista/backtest.py carregar_snapshot"
      to: "ipca_deflatores carimbado no snapshot"
      via: "leitura offline como rf_local (:55)"
      pattern: "ipca_deflatores"
---

<objective>
PRIM-04 — deflacionar a base do motor cíclico por IPCA. Hoje o ramo `"normalizado"` de `_intrinseco_por_motor` (report.py:253-262, `anos_media=10`) soma reais NOMINAIS de anos diferentes (reais de 2015 com 2024; IPCA acumulado ~58%), subvalorizando cíclicas só por nominalidade (CSNA3 sai 31,8% subvalorizada só por isso; a base cíclica sobe +49,6% quando deflacionada).

O padrão é o do `rf_local` EXATAMENTE (RESEARCH Pattern 3 + PATTERNS §"Offline entry-point stamping"): uma nova função `macro.ipca_deflatores_anuais` copia o esqueleto de retry/degradação de `_selic_historico`; os entry points (cli.py, app.py) resolvem os deflatores UMA vez e carimbam em `cfg["macro"]["ipca_deflatores"]`; a engine lê offline (nunca chama `requests`); o snapshot de teste carrega o valor carimbado como `rf_local`. O motor cíclico continua usando a MÉDIA through-cycle (`norm.media_ciclo` do Plano 01), não o endpoint — só recebe a série já deflacionada.

Purpose: tirar o viés de somar reais nominais no motor cíclico, mantendo a engine determinística/offline e sem adicionar knob.
Output: `macro.ipca_deflatores_anuais`, deflação no ramo cíclico, stamping nos entry points e no snapshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/10-primitivas-sem-vi-s-prim/10-RESEARCH.md
@.planning/phases/10-primitivas-sem-vi-s-prim/10-PATTERNS.md

<interfaces>
<!-- Extraído do código. -->
macro.py:
  SGS_URL = "...bcdata.sgs.{codigo}/dados/ultimos/{n}?formato=json"   # :15
  SELIC_META = 432 ; IPCA_12M = 13522                                 # :18-19 (IPCA 12m acumulado %)
  _selic_historico(anos=10) -> List[float]  # :59-84 — ESQUELETO a copiar: date-range dataInicial/dataFinal + 3 retries + time.sleep + degrada p/ []
  selic_ciclo_para_capm(fallback, anos=10)  # :87-100 — resolver do entry point (rede aqui)
  ipca_12m() -> Optional[float]             # :41-44 — convenção de fração (/100.0)

report.py ramo "normalizado" (:253-262):
  cic = mot_cfg.get("ciclica", {})
  lpa_mid = mult.lpa(norm.media_ciclo(c.serie("lucro_liquido"), anos_media=cic.get("anos_media",10), winsor=...), c.num_acoes.get(ult))
  # (media_ciclo veio do Plano 01)
  c.anos_ordenados() -> List[int] ; c.lucro_liquido: Dict[int,float]

cli.py :77-79   cfg["capm"]["rf_local"] = macro.selic_ciclo_para_capm(cfg["capm"]["selic_fallback"], cfg["capm"].get("rf_ciclo_anos",10))
app.py :866-868 CFG["capm"]["rf_local"] = rf_capm(CFG["capm"]["selic_fallback"], CFG["capm"].get("rf_ciclo_anos",10))   # rf_capm em app.py :246 → macro.selic_ciclo_para_capm
backtest.py :55  rf_local = float(snap["rf_local"])   # top-level key global do snapshot (_CHAVES_GLOBAIS :33)
backtest.py :117 cfg = {**cfg, "capm": {**cfg.get("capm",{}), "rf_local": rf_local}}   # injeta em CÓPIA, não muta

BCB SGS — código do IPCA anual: usar SGS 13522 amostrado em DEZEMBRO (IPCA 12m acumulado no fechamento do ano = IPCA do ano-calendário). Alternativa documentada: SGS 433 (IPCA mensal %), compondo prod(1+m/100) por ano-calendário. RESEARCH Assumption A1 / Open Q2 — o executor confirma o código na primeira execução contra o BCB.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: macro.ipca_deflatores_anuais + teste offline (RED→GREEN)</name>
  <read_first>
    - src/analista/ingest/macro.py (todo — _selic_historico 59-84 é o esqueleto exato; ipca_12m 41-44; SGS_URL/constantes 15-19)
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-PATTERNS.md §"macro.py ipca_deflatores_anuais (NEW)"
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-RESEARCH.md §"PRIM-04" e Open Q2 (código SGS) e §"Knob Budget" (não é knob)
    - tests/classificacao.yaml + tests/conftest.py (completude)
  </read_first>
  <behavior>
    - ipca_deflatores_anuais(anos) devolve um dict {ano: fator} onde o fator do último ano é 1.0 e anos anteriores têm fator > 1.0 (compõem a inflação acumulada até o último ano) — D-03: reais do último ano, NÃO ano-base fixo.
    - Convenção de fração: IPCA lido /100.0 (como ipca_12m).
    - Degradação graciosa: falha de rede / série vazia → devolve {} (ou fallback vazio), nunca levanta — espelha _selic_historico → [].
    - Determinismo do teste: o teste NÃO toca a rede — monta um dict de IPCA anual sintético (mockando o fetch ou testando a função pura de composição dos fatores) e verifica a composição {ano: prod(1+ipca[y]) para y in (ano+1..T)}.
  </behavior>
  <action>
Adicionar `ipca_deflatores_anuais(anos: int)` a `macro.py`, copiando o esqueleto date-range + 3 retries + `time.sleep(0.5*(t+1))` + degradação para vazio de `_selic_historico`. Buscar a série anual do IPCA (SGS 13522 amostrado em dezembro, ou 433 mensal composto por ano — confirmar o código contra o BCB) e construir `{ano: prod(1+ipca[y]) for y in (ano+1..T)}`, `defl[T]=1.0`, `T` = último ano da janela. Separar a lógica PURA de composição dos fatores (recebe {ano: ipca_frac}) da parte de rede, para permitir o teste offline. Escrever `tests/test_macro_ipca.py` (offline, sem rede) verificando a composição e a fronteira (série vazia → {}). Registrar os testes em `tests/classificacao.yaml` (classe `contrato`/`invariante`) no mesmo diff. É dado objetivo do BCB, NÃO knob — nenhuma escolha livre de ano-base.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "macro_ipca or deflator" -o addopts="" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def ipca_deflatores_anuais" src/analista/ingest/macro.py` == 1.
    - O teste offline passa e NÃO toca a rede (sem `requests` real): `.venv/bin/python -m pytest -k macro_ipca` verde.
    - Composição correta: fator do último ano == 1.0; fator de anos anteriores > 1.0 para IPCA positivo.
    - Coleta sem órfão em classificacao.yaml.
  </acceptance_criteria>
  <done>ipca_deflatores_anuais implementada (fetch + composição + degradação), com teste offline verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deflacionar o ramo cíclico + bloco macro no config (RED→GREEN)</name>
  <read_first>
    - src/analista/report/report.py (ramo "normalizado" 253-262 — já usa norm.media_ciclo após o Plano 01; anos_ordenados/lucro_liquido em fundamentals.py:74/lucro dict)
    - config.yaml (bloco motores.ciclica 265-267; onde adicionar o bloco macro)
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-PATTERNS.md §"report.py _intrinseco_por_motor 'normalizado'" e §"Offline entry-point stamping"
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-RESEARCH.md Pattern 3 e Pitfall 4 (deflacionar dentro da engine quebra determinismo)
  </read_first>
  <behavior>
    - No ramo "normalizado", a série de lucro é multiplicada pelos deflatores ANTES de norm.media_ciclo: `serie_real = [c.lucro_liquido[a] * defl[a] for a in c.anos_ordenados() if a in c.lucro_liquido and a in defl]`.
    - Para uma série inflacionária conhecida + deflatores conhecidos, a base cíclica deflacionada > base nominal (direção medida: CSNA3 +49,6% na base).
    - A engine lê `cfg["macro"]["ipca_deflatores"]` — NUNCA chama macro/requests (offline/determinística).
    - Fallback: se `cfg["macro"]["ipca_deflatores"]` estiver vazio/ausente, o ramo degrada para a série nominal (comportamento antigo) sem levantar.
    - media_ciclo continua sendo a MÉDIA through-cycle (não o endpoint) — motores.ciclica.anos_media:10 / winsor:0.10 congelados intocados.
  </behavior>
  <action>
Editar o ramo `"normalizado"` de `_intrinseco_por_motor` (report.py) para deflacionar `c.serie("lucro_liquido")` para reais do último ano usando `cfg["macro"]["ipca_deflatores"]` (carimbado — lido offline) ANTES de `norm.media_ciclo(...)`. Manter `anos_media=cic.get("anos_media",10)`/`winsor=cic.get("winsor",0.10)`. Degradar para a série nominal se o dict de deflatores estiver ausente/vazio (never-raise). Adicionar o bloco `macro:` ao `config.yaml` com `ipca_deflatores: {}` default (comentário: os entry points sobrescrevem; a engine lê; NÃO é knob de valuation). NÃO chamar `requests`/`macro` de dentro da engine. NÃO deflacionar a base do CAGR/`g` (isso é Fase 11). NÃO tocar motores.ciclica no lock. Escrever `tests/test_deflacao_ciclica.py` (offline, deflatores carimbados à mão) provando base real > nominal e a fronteira de fallback; registrar em classificacao.yaml.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "deflacao_ciclica or normalizado or ciclica" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ipca_deflatores" src/analista/report/report.py` >= 1 e o ramo NÃO importa/chama macro/requests: `grep -n "requests\|macro\." src/analista/report/report.py` sem novas chamadas de rede.
    - `grep -c "^macro:\|ipca_deflatores" config.yaml` >= 1 (bloco default presente).
    - Teste offline: base cíclica deflacionada > nominal para série inflacionária; fallback nominal quando deflatores vazios; ambos verdes.
    - `git diff` NÃO altera `motores.ciclica.anos_media`/`winsor` no config.yaml nem no calibracao.lock.yaml (congelados intactos; orçamento 3 graus).
  </acceptance_criteria>
  <done>Ramo cíclico deflaciona a série antes da média through-cycle, offline via cfg carimbado, com fallback nominal; nenhum congelado movido.</done>
</task>

<task type="auto">
  <name>Task 3: Stamping nos entry points + snapshot (offline, espelho do rf_local)</name>
  <read_first>
    - src/analista/cli.py (:73-80 — stamping do rf_local)
    - app.py (:860-870 — stamping do rf_local via rf_capm; rf_capm em :244-246)
    - src/analista/backtest.py (:33 _CHAVES_GLOBAIS, :46-75 carregar_snapshot, :108-117 injeção em cópia do cfg)
    - src/analista/ingest/macro.py (ipca_deflatores_anuais da Task 1)
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-PATTERNS.md §"cli.py/app.py entry point" e §"backtest.py carregar_snapshot"
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-RESEARCH.md §"Runtime State Inventory" (NÃO regenerar snapshots; carimbar é aditivo)
  </read_first>
  <action>
Em `cli.py` (junto ao stamping do rf_local, ~:77): resolver e carimbar `cfg["macro"] = {**cfg.get("macro",{}), "ipca_deflatores": macro.ipca_deflatores_anuais(<janela de anos do universo>)}`. Em `app.py` (~:866, dentro do `st.status`, cacheado): idem com `CFG`, reusando o cache de rede do rf (uma chamada por execução; app.py segue read-only). Em `backtest.py`: adicionar `"ipca_deflatores"` a `_CHAVES_GLOBAIS` e ler o valor carimbado do snapshot em `carregar_snapshot` (espelhando `rf_local = float(snap["rf_local"])` :55), devolvendo-o ao caller; injetar em `cfg["macro"]["ipca_deflatores"]` numa CÓPIA do cfg em `run_backtest` (espelhando :117). Adicionar a chave top-level `ipca_deflatores:` (dict {ano: fator}) ao(s) snapshot(s) de teste que exercitam o motor cíclico — é edição ADITIVA de um INPUT (como rf_local), NÃO regeneração dos dados dos tickers. NÃO regenerar snapshot. Confirmar por execução que `analisar_acao` permanece offline (nenhuma chamada de rede dentro da engine).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "backtest or determinismo or offline or ciclica" 2>&1 | tail -20 && .venv/bin/python -m pytest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ipca_deflatores" src/analista/cli.py` >= 1 e `grep -c "ipca_deflatores" app.py` >= 1 (stamping nos dois entry points).
    - `grep -c "ipca_deflatores" src/analista/backtest.py` >= 1 e `ipca_deflatores` está em `_CHAVES_GLOBAIS`.
    - `test_backtest_determinismo` (invariante) continua verde — a engine permanece determinística/offline.
    - Suíte default `.venv/bin/python -m pytest` → 0 failed.
    - `git diff` dos snapshots mostra APENAS a adição da chave `ipca_deflatores` (dados de ticker inalterados).
  </acceptance_criteria>
  <done>Deflatores resolvidos nos entry points e carimbados; backtest lê do snapshot; engine offline determinística; suíte default 0 failed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entry point (cli/app) → BCB SGS (IPCA) | Fetch HTTP público, resolvido SÓ no entry point. Mesma superfície já existente do `_selic_historico`/`rf_local` — nenhuma superfície nova de fato. |
| engine ← cfg carimbado | A engine lê só valores já resolvidos em cfg; nunca toca a rede (mantém determinismo/offline). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | resposta do BCB SGS (IPCA) | accept | Mesma origem confiável já usada para a Selic; parsing defensivo (try/except → {}) copiado de _selic_historico; sem elevação de superfície. |
| T-10-06 | Denial of Service | timeout/intermitência do SGS | mitigate | 3 retries + time.sleep + degradação graciosa para {} → engine cai na série nominal (never-raise), como o rf degrada para fallback. |
| T-10-07 | Tampering | determinismo da engine | mitigate | Rede confinada aos entry points; a engine lê cfg carimbado; `test_backtest_determinismo` prova offline. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest` → 0 failed.
- Base cíclica deflacionada > nominal (teste offline) e fallback nominal quando deflatores vazios.
- Engine offline: `test_backtest_determinismo` verde; nenhuma chamada de rede dentro de report.py.
- Congelados motores.ciclica intactos; orçamento 3 graus; snapshots só com a chave aditiva.
</verification>

<success_criteria>
PRIM-04 entregue: o motor cíclico consome uma série deflacionada a reais do último ano, resolvida offline via stamping igual ao rf_local, sem adicionar knob e sem quebrar o determinismo da engine.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/10-primitivas-sem-vi-s-prim/10-03-SUMMARY.md`.
</output>
