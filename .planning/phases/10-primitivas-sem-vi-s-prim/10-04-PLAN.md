---
phase: 10-primitivas-sem-vi-s-prim
plan: 04
type: execute
wave: 4
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - tests/test_backtest_bancos.py
  - tests/test_motores.py
  - tests/test_vulc3_regressao.py
  - tests/classificacao.yaml
  - tests/test_guardrails_ddm.py
  - tests/test_arquetipo_roteamento.py
autonomous: false
requirements: [PRIM-05]
must_haves:
  truths:
    - "O golden ITUB4 32,88 ± 0,20 (test_backtest_alvos_recalibrados) NÃO existe mais no repositório — este é o CRITÉRIO DE SAÍDA da fase."
    - "Cada golden de nível deletado teve sua função de teste E sua linha em tests/classificacao.yaml removidas no MESMO diff (completude da coleta preservada, zero órfão)."
    - "As fixtures anti-pattern do BLIND-04a (helpers_blindagem.py) e o BLIND-02b (Fase 12) permanecem INTOCADOS."
    - "A suíte default continua 0 failed; o meta-teste BLIND-04a permanece verde (deletar remove o teste de ofensores E de tolerados simultaneamente)."
  artifacts:
    - path: "tests/classificacao.yaml"
      provides: "sem as linhas dos goldens deletados; sem órfãos"
  key_links:
    - from: "tests/test_backtest_bancos.py::test_backtest_alvos_recalibrados"
      to: "(ausente do repo)"
      via: "deleção da função + linha 63 do classificacao.yaml"
      pattern: "test_backtest_alvos_recalibrados"
---

<objective>
PRIM-05 — o CRITÉRIO DE SAÍDA da fase. O golden `ITUB4 = 32,88 ± 0,20` foi calibrado para cancelar o haircut de −9,1% da normalização (dois erros se anulando). Com as primitivas consertadas (Planos 01-03; PRIM-02 sozinho já move o RIM do ITUB4 de 32,88 → 31,52), ele quebra — e **é DELETADO, não atualizado** (Armadilha 3: atualizar mantém o reflexo vivo; regra do CLAUDE.md v2.4: golden de nível que quebra é DELETADO). A fase não está concluída enquanto ele existir no repositório.

Mecânica obrigatória (Pitfall 5): deletar a função de teste E sua linha em `tests/classificacao.yaml` no MESMO diff — uma linha órfã quebra a coleta exatamente como uma entrada faltante. Não existe accept-list separada: deletar os dois remove o teste de `ofensores` (o AST não o vê mais) e de `tolerados` (não é mais golden_nivel) ao mesmo tempo → o meta-teste BLIND-04a fica verde.

Purpose: cumprir o critério de saída sem manter vivo o reflexo do overfit do v2.3.
Output: os goldens de nível ITUB4 removidos (teste + linha YAML), suíte verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/10-primitivas-sem-vi-s-prim/10-RESEARCH.md
@.planning/phases/10-primitivas-sem-vi-s-prim/10-PATTERNS.md

<interfaces>
<!-- Golden Disambiguation DEFINITIVA (RESEARCH §Golden Disambiguation + PATTERNS §delete map). -->
DELETAR (função + linha classificacao.yaml no mesmo diff):
  1. tests/test_backtest_bancos.py::test_backtest_alvos_recalibrados      ← O EXIT GOLDEN (ITUB4 32,88 ±0,20)   · classificacao.yaml:63
  2. tests/test_backtest_bancos.py::test_backtest_cesta_rota_por_ticker   (ITUB4 30-40)                          · classificacao.yaml:61
  3. tests/test_backtest_bancos.py::test_backtest_gate_quorum_e_anotacao  (quórum fair_values_bancos)            · classificacao.yaml:62
  4. tests/test_motores.py::test_rim_itub4_honesto_maior_que_ddm          (ITUB4 36-42)                          · classificacao.yaml:332
  5. tests/test_motores.py::test_rim_itub4_live_alvo_32_40                (ITUB4 32-40)                          · classificacao.yaml:333
  6. tests/test_motores.py::test_rota_seguradora_nao_pega_banco           (ITUB4 30-40)                          · classificacao.yaml:348
  7. tests/test_vulc3_regressao.py::test_rim_itub4_dispatch_banda         (ITUB4 > 30)                           · classificacao.yaml:467

SOBREVIVEM em test_backtest_bancos.py (NÃO deletar — não asseriam nível R$):
  test_backtest_determinismo (invariante) · test_backtest_rotulo_do_motor_consistente (contrato)

CHECKPOINT (RESEARCH A3 — item 1 é o único requisito DURO de PRIM-05; 8-9 podem ir p/ a Fase 13):
  8. tests/test_guardrails_ddm.py::test_san01_reetiqueta_aberracao_itub4_like            · classificacao.yaml:162
  9. tests/test_arquetipo_roteamento.py::test_financeira_rim_destrava_vs_ddm_e_alimenta_veredito · classificacao.yaml:49 (tag "Fase 10/13")

NÃO TOCAR:
  tests/test_invariantes_v24.py::test_invariancia_inflacao_engine_itub4   (BLIND-02b xfail — vira verde na Fase 12)
  tests/helpers_blindagem.py:157,215 (ALVOS={"ITUB4":32.88}) — fixtures anti-pattern do detector BLIND-04a
  tests/fixtures/snapshot_bancos_2026-07-12.yaml:296 intrinseco_motor_observado: 32.88 — observação registrada
  comentários-prosa em test_backtest_bancos.py:10,117 ("32,88 INALTERADO — o cap satura") — propriedade da Fase 12
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deletar os 7 goldens de nível ITUB4 (função + linha YAML no mesmo diff)</name>
  <read_first>
    - tests/test_backtest_bancos.py (as 3 funções golden E os comentários-prosa a PRESERVAR nas linhas 10,117)
    - tests/test_motores.py (as 3 funções golden nas ~332/333/348)
    - tests/test_vulc3_regressao.py (test_rim_itub4_dispatch_banda ~467)
    - tests/classificacao.yaml (linhas 61,62,63,332,333,348,467 — confirmar cada nodeid por grep antes de remover)
    - tests/conftest.py (completude bidirecional na coleta — órfão quebra igual a faltante)
    - tests/helpers_blindagem.py (:157,215 — NÃO tocar; confirmar que são fixtures do detector)
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-RESEARCH.md §"Golden Disambiguation" e §"BLIND-04a meta-test interaction"
  </read_first>
  <action>
Remover as 7 funções de teste da lista DELETAR (itens 1-7) dos arquivos `tests/test_backtest_bancos.py`, `tests/test_motores.py`, `tests/test_vulc3_regressao.py` E remover a linha correspondente de cada uma em `tests/classificacao.yaml` — no MESMO diff/commit (Pitfall 5: linha órfã quebra a coleta). Confirmar cada nodeid por grep antes de apagar (os números de linha podem ter deslocado). NÃO deletar o arquivo inteiro `test_backtest_bancos.py` (test_backtest_determinismo e test_backtest_rotulo_do_motor_consistente sobrevivem). NÃO atualizar nenhum número (proibido — Armadilha 3). NÃO tocar em helpers_blindagem.py, no BLIND-02b, no snapshot, nem nos comentários-prosa "32,88 INALTERADO". Se o commit tocar tests/ mas não config.yaml, o hook BLIND-05 não dispara (não há par knob+golden); mensagem de commit sem ticker (ex.: "deleta os goldens de nível de valuation quebrados pelo conserto das primitivas — critério de saída PRIM-05").
  </action>
  <verify>
    <automated>cd "$PWD" && test $(grep -c "test_backtest_alvos_recalibrados" tests/test_backtest_bancos.py tests/classificacao.yaml | awk -F: '{s+=$2} END{print s}') -eq 0 && .venv/bin/python -m pytest --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "test_backtest_alvos_recalibrados" tests/` retorna VAZIO (função + linha YAML ausentes) — critério de saída duro.
    - Cada um dos 7 nodeids da lista DELETAR está ausente tanto do arquivo de teste quanto de `tests/classificacao.yaml`.
    - `.venv/bin/python -m pytest --collect-only` NÃO acusa `CLASSIFICACAO ORFA` nem nodeid órfão (completude preservada).
    - `test_backtest_determinismo` e `test_backtest_rotulo_do_motor_consistente` ainda existem e coletam.
    - `git diff tests/helpers_blindagem.py` VAZIO; `grep -c "32.88" tests/helpers_blindagem.py` inalterado (fixtures do detector intactas).
    - Suíte default `.venv/bin/python -m pytest` → 0 failed; meta-teste BLIND-04a verde (`-k nenhum_teste_de_calibracao_crava_ticker`).
  </acceptance_criteria>
  <done>Os 7 goldens de nível ITUB4 removidos (função + linha YAML juntas); o exit golden 32,88 ausente do repo; coleta e suíte default verdes.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decidir o destino dos goldens 8-9 (Fase 10 vs Fase 13)</name>
  <decision>Deletar agora os itens 8-9 (test_san01_reetiqueta_aberracao_itub4_like, test_financeira_rim_destrava_vs_ddm_e_alimenta_veredito) ou deixá-los para a Fase 13.</decision>
  <context>
RESEARCH A3: o único requisito DURO de PRIM-05 é o item 1 (já deletado na Task 1). Os itens 8-9 são diferentes dos 7 puros:
- Item 8 (test_san01_reetiqueta_aberracao_itub4_like) é um guardrail de RE-ETIQUETAGEM (SAN-01) que exibe um nível ITUB4-like no veredito — deletar cedo remove uma guarda antes da substituta.
- Item 9 (test_financeira_rim_destrava...) está TAGGED "Fase 10/13" no classificacao.yaml:49 — a própria classificação hesita.
Ambos são golden_nivel (deselecionados por default; sua quebra não afeta a suíte default). A decisão é sobre higiene de guardas, não sobre o critério de saída (que já está cumprido pela Task 1).
  </context>
  <options>
    <option id="deletar-agora">
      <name>Deletar 8-9 agora (com linha YAML)</name>
      <pros>Fecha todos os goldens de nível ITUB4 numa fase só; nada de nível ITUB4 sobra quebrado.</pros>
      <cons>Remove uma guarda de re-etiquetagem/roteamento antes da substituta da Fase 13; risco de perder um invariante estrutural cedo (lição do gap WR-04).</cons>
    </option>
    <option id="adiar-fase-13">
      <name>Deixar 8-9 para a Fase 13 (recomendado pela RESEARCH)</name>
      <pros>Item 1 (o critério duro) já cumprido; preserva a guarda até a Fase 13 reescrever o contrato de saída; menor risco de matar invariante em silêncio.</pros>
      <cons>Dois goldens de nível ITUB4 (quarentenados) continuam no repo até a Fase 13.</cons>
    </option>
  </options>
  <resume-signal>Escolha: deletar-agora ou adiar-fase-13. Se deletar-agora, o executor remove função + linha YAML dos itens 8-9 no mesmo diff e re-verifica coleta/suíte.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Deleção de testes. Nenhuma mudança de código de produção, nenhuma entrada externa, auth ou rede. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Repudiation | remoção de guarda de teste sem rastro | mitigate | Deleção casada função+linha YAML num único commit com justificativa sem ticker; itens 8-9 (guardas estruturais) sob checkpoint explícito, não removidos às cegas. |
| T-10-09 | Tampering | quebra silenciosa da coleta por órfão | mitigate | conftest impõe completude bidirecional; verify roda `--collect-only` e o meta-teste BLIND-04a. |
</threat_model>

<verification>
- `grep -rn "test_backtest_alvos_recalibrados" tests/` VAZIO (critério de saída).
- `.venv/bin/python -m pytest --collect-only` sem órfão; `.venv/bin/python -m pytest` → 0 failed.
- Meta-teste BLIND-04a verde; helpers_blindagem.py e BLIND-02b intocados.
</verification>

<success_criteria>
PRIM-05 cumprido: o golden ITUB4 32,88 e os demais goldens de nível ITUB4 puros (itens 1-7) estão ausentes do repositório (função + linha YAML), a coleta e a suíte default permanecem verdes, e o destino dos itens 8-9 foi decidido explicitamente. A fase está concluída.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/10-primitivas-sem-vi-s-prim/10-04-SUMMARY.md`.
</output>
