---
phase: 10-primitivas-sem-vi-s-prim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/core/normalizacao.py
  - src/analista/report/report.py
  - tests/test_normalizacao.py
  - tests/test_invariantes_v24.py
  - tests/classificacao.yaml
  - config.yaml
  - calibracao.lock.yaml
autonomous: false
requirements: [PRIM-01]
must_haves:
  truths:
    - "Uma série de lucro +10%/ano PURA (zero outlier) produz base normalizada >= último ano menos inflação — o teste BLIND-03 fica verde."
    - "base_normalizada devolve o endpoint de uma tendência robusta (Theil-Sen) no ano atual, não a mediana do ano-do-meio."
    - "O motor cíclico (anos_media=10) continua usando a MÉDIA through-cycle (não o endpoint) — CSNA3 não vira base negativa."
    - "Séries curtas/negativas degradam sem quebrar: vazio→None, N=1→valor, N=2→média, endpoint<=0→mediana."
    - "Rastreabilidade CONTEXT — D-01: base_normalizada troca o median()-de-3 pelo endpoint de regressão robusta Theil-Sen no ano atual; D-01a: o estimador foi validado contra o mapa de 104 tickers (RESEARCH) antes do planejamento; D-01b: fallback de série curta implementado (N=1→valor, N=2→média, vazio→None)."
  artifacts:
    - path: "src/analista/core/normalizacao.py"
      provides: "base_normalizada = endpoint Theil-Sen + guard; media_ciclo = a média/mediana antiga (para o cíclico)"
      contains: "theilslopes"
    - path: "src/analista/report/report.py"
      provides: "ramo 'normalizado' do _intrinseco_por_motor consumindo norm.media_ciclo"
      contains: "media_ciclo"
    - path: "tests/test_invariantes_v24.py"
      provides: "BLIND-03 sem o decorator xfail (vira invariante normal)"
  key_links:
    - from: "src/analista/report/report.py :253-262 (ramo 'normalizado')"
      to: "norm.media_ciclo"
      via: "chamada direta (NÃO base_normalizada)"
      pattern: "media_ciclo"
    - from: "src/analista/core/fundamentals.py :143 base_lucro_normalizada"
      to: "norm.base_normalizada (endpoint)"
      via: "chamada existente, inalterada"
      pattern: "base_normalizada"
---

<objective>
PRIM-01 + a divisão do estimador (a descoberta que mais molda a fase). Hoje `normalizacao.base_normalizada` faz `median()` de 3 anos = o ano-do-meio, que pune crescimento (haircut medido de −9,1% num crescedor de 10%). Substituir por um **endpoint de regressão robusta (Theil-Sen)** que reflete o ano atual e é robusto a 1 exercício atípico (D-01/D-01a/D-01b).

O ponto estrutural decisivo (RESEARCH §Shared Pattern "Estimator split"): `base_normalizada` é chamada por DOIS consumidores que querem estimadores OPOSTOS — a base de valuation de 3 anos quer o endpoint; o motor cíclico de 10 anos quer a MÉDIA through-cycle (CSNA3: média = +1.270M vs endpoint Theil-Sen = −891M). **NÃO editar uma função compartilhada para Theil-Sen** — dividir o estimador.

Purpose: tirar o viés que pune crescimento na base de valuation E preservar o comportamento correto do motor cíclico, num único commit que também remove o `xfail` do BLIND-03 (Pitfall 6: `xfail_strict=true` transforma um xfail que passa em XPASS=FAIL).
Output: `normalizacao.py` com dois estimadores separados; `report.py` cíclico repontado; BLIND-03 verde sem xfail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/10-primitivas-sem-vi-s-prim/10-CONTEXT.md
@.planning/phases/10-primitivas-sem-vi-s-prim/10-RESEARCH.md
@.planning/phases/10-primitivas-sem-vi-s-prim/10-PATTERNS.md

<interfaces>
<!-- Extraído do código. O executor usa isto direto — sem exploração. -->

normalizacao.py (site a mudar, linhas 58-75):
  def base_normalizada(valores, anos_media=3, winsor=0.10) -> Number
    hoje: _limpar → vazio→None → janela=limpos[-anos_media:] → n==1→valor → n<5→median → else→media_winsorizada
  def media_winsorizada(valores, winsor=0.10) -> Number   # ladder-N já existente a copiar
  def mediana_payout(valores) -> Number                    # NÃO tocar

report.py :253-262 (ramo "normalizado" de _intrinseco_por_motor):
  chama norm.base_normalizada(c.serie("lucro_liquido"), anos_media=cic.get("anos_media",10), winsor=cic.get("winsor",0.10))

fundamentals.py :143 base_lucro_normalizada → norm.base_normalizada(..., anos_media=3)  # consumidor de VALUATION (endpoint)
fundamentals.py :179 margem_valuation → norm.base_normalizada(serie("vendas_liquidas"), anos_media=3)  # também VALUATION (endpoint)

scipy.stats.theilslopes(y, x) -> (slope, intercept, low, high)   # scipy 1.17.1 já instalado
  endpoint = intercept + slope * (n-1)

BLIND-03 xfail: tests/test_invariantes_v24.py ~linha 161, @pytest.mark.xfail(strict=True) sobre test_normalizacao_nao_pune_crescimento (fica @pytest.mark.invariante)
config.yaml:57  normalizacao.anos_media: 3   ·  config.yaml:58 normalizacao.winsor: 0.10
calibracao.lock.yaml:194 normalizacao.anos_media: 3  (congelado)
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decidir a janela do Theil-Sen (3 vs 5)</name>
  <decision>Janela do endpoint Theil-Sen para a base de valuation: manter 3 ou passar para 5.</decision>
  <context>
RESEARCH Open Q1 pede confirmação do usuário (é o único ponto em que a fase toca o lock). Medido nos 104 tickers:
- Janela 3: distribuição mais estreita (endpoint/último mediana 1,00), 2 endpoints negativos, MAS fraca contra outlier terminal (um pico 2× no penúltimo ano → 159 vs 146). NÃO edita o lock (caminho recomendado A4).
- Janela 5: robusta ao pico (→144,7), 5 endpoints negativos (o guard cobre), satisfaz melhor D-01 "robusto a 1 exercício atípico". EXIGE co-commit `config.yaml:57` + `calibracao.lock.yaml:194` (mudança de congelado visível, o hook BLIND-05 permite o par).
BLIND-03 passa nas duas janelas — a escolha é só sobre robustez geral a pico terminal.
  </context>
  <options>
    <option id="janela-3">
      <name>Manter anos_media=3</name>
      <pros>Zero edição de lock (menor blast radius); distribuição transversal mais estreita.</pros>
      <cons>Theil-Sen com 3 pontos persegue um pico terminal (não separa outlier de tendência).</cons>
    </option>
    <option id="janela-5">
      <name>Mudar para anos_media=5 (recomendado pela RESEARCH)</name>
      <pros>Robusto a 1 exercício atípico (requisito literal de D-01); pico terminal suavizado.</pros>
      <cons>Exige editar config.yaml:57 + calibracao.lock.yaml:194 no MESMO commit (justificativa sem ticker).</cons>
    </option>
  </options>
  <resume-signal>Escolha: janela-3 ou janela-5</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes unitários do endpoint Theil-Sen + guard + fallback (RED)</name>
  <read_first>
    - tests/test_normalizacao.py (todo — é o template; ver test_mediana_payout_* linhas 88-128, e os testes de estimador antigo linhas 21-65 que serão reescritos)
    - src/analista/core/normalizacao.py (base_normalizada 58-75, media_winsorizada 39-55, _limpar 34-36)
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-RESEARCH.md §"Validation Architecture" (tabela break/rewrite/survive) e §"Empirical Validation" (números-alvo do endpoint)
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-PATTERNS.md §"Tests — break/rewrite/delete map"
    - tests/classificacao.yaml (mecânica de completude — todo teste novo precisa de entrada)
    - tests/conftest.py (completude imposta na coleta: entrada órfã OU teste sem entrada quebra a coleta)
  </read_first>
  <behavior>
    - Endpoint sobre série +10%/ano pura `[100,110,121,133.1,146.41]` (janela escolhida na Task 1) reflete o crescimento (≈145,8 na janela 3 / ≈144,2 na janela 5), NÃO a mediana do ano-do-meio (133,1).
    - Série plana `[100,100,100]` → endpoint == 100 (test_serie_estavel_base_igual_ao_valor SOBREVIVE).
    - Guard de degeneração: série com endpoint <= 0 (ex.: `[100, 50, -80]` em queda) → cai para `median(janela)`, nunca devolve negativo.
    - Fallback de série curta (D-01b): vazio/só-None → None; N=1 → o próprio valor; N=2 → média dos dois.
    - Robustez ao pico terminal: um pico atípico injetado NÃO estoura a base (escrever o assert coerente com a janela escolhida — janela 5 → pico suavizado).
    - A primitiva continua PURA: `test_primitiva_e_pura_sem_import_de_fundamentals` verde (scipy não é import de fundamentals).
  </behavior>
  <action>
Escrever os testes unitários novos em `tests/test_normalizacao.py` cobrindo endpoint + guard endpoint<=0 + fallback N=0/1/2, espelhando o formato de `test_mediana_payout_*`. REESCREVER (não afrouxar) os 2-3 testes que codificam o estimador ANTIGO (mediana), conforme a tabela da RESEARCH: `test_outlier_alto_suavizado_pela_mediana` (asseria base==105 pela mediana → reescrever para a invariância de robustez ao pico na janela escolhida) e `test_none_ignorado_antes_de_normalizar` (manter o assert de None-skip, remover o nível-105 da mediana). `test_apenas_os_ultimos_anos_media_entram_na_base` sobrevive se a janela for mantida no código. Adicionar as entradas dos testes NOVOS em `tests/classificacao.yaml` (classe `invariante` para o endpoint/guard/robustez; `contrato` para as fronteiras None/vazio) no MESMO diff. Rodar e ver os novos falharem (RED — o código ainda é a mediana). NÃO reescrever asserts para o valor antigo; NÃO trocar xfail por skip.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "normalizacao" -o addopts="" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Os testes novos do endpoint/guard/fallback existem em `tests/test_normalizacao.py` e FALHAM antes da Task 3 (RED).
    - Toda função de teste nova tem entrada correspondente em `tests/classificacao.yaml` (coleta sem `CLASSIFICACAO ORFA`): `.venv/bin/python -m pytest -k normalizacao --collect-only` não acusa órfão.
    - Nenhum assert antigo foi afrouxado; os testes de estimador-antigo foram REESCRITOS para a nova invariância, não deletados às cegas.
  </acceptance_criteria>
  <done>Testes do novo estimador escritos, classificados e vermelhos (RED); testes do estimador-antigo reescritos para as novas invariâncias.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dividir o estimador + swap para Theil-Sen + remover xfail do BLIND-03 (GREEN)</name>
  <read_first>
    - src/analista/core/normalizacao.py (base_normalizada 58-75; copiar a ladder-N de media_winsorizada 39-55)
    - src/analista/report/report.py (ramo "normalizado" 253-262; _roe_through_cycle 184-198 como referência de estilo)
    - src/analista/core/fundamentals.py (base_lucro_normalizada 143, margem_valuation 179 — consumidores de valuation que ganham o endpoint automaticamente)
    - src/analista/core/motores.py:180 (docstring que cita base_normalizada do cíclico — atualizar a referência se apontar para o estimador)
    - tests/test_invariantes_v24.py (cabeçalho 1-18 e o xfail ~161 sobre test_normalizacao_nao_pune_crescimento)
    - config.yaml (bloco normalizacao 56-58; bloco motores.ciclica 265-267)
    - calibracao.lock.yaml (194-202 normalizacao; 156-158 motores.ciclica; §escopo 45-49; §congelados)
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-RESEARCH.md §"Architecture Patterns" Pattern 1 e §"Knob Budget Integrity"
    - .planning/phases/10-primitivas-sem-vi-s-prim/10-PATTERNS.md §"normalizacao.py base_normalizada" e §"Shared Patterns / Estimator split"
  </read_first>
  <action>
Em `normalizacao.py`: (1) adicionar a função `media_ciclo(valores, anos_media, winsor)` contendo EXATAMENTE a lógica de média/mediana atual de `base_normalizada` (ladder: vazio→None, N=1→valor, N<5→median, else→media_winsorizada) — é o estimador que o motor cíclico precisa; (2) reescrever o corpo de `base_normalizada` para o endpoint Theil-Sen via `scipy.stats.theilslopes` sobre `np.arange(n)`, preservando a mesma ladder-N nas fronteiras (`_limpar`→vazio→None, N==1→valor, N==2→média dos dois) e o GUARD `endpoint <= 0 → median(janela)` (RESEARCH Pattern 1 / D-01b). Manter os parâmetros `anos_media`/`winsor` na assinatura (integridade do orçamento de knobs). Preservar a promessa da docstring "primitiva pura — só numpy/statistics/scipy, sem import de fundamentals".
Em `report.py` ramo "normalizado" (256): trocar `norm.base_normalizada(...)` por `norm.media_ciclo(...)` mantendo `anos_media=cic.get("anos_media",10)`/`winsor=cic.get("winsor",0.10)` — o cíclico NÃO recebe o endpoint (anti-pattern decisivo). NÃO deflacionar aqui (isso é o Plano 03).
Em `tests/test_invariantes_v24.py`: REMOVER o decorator `@pytest.mark.xfail(strict=True)` de `test_normalizacao_nao_pune_crescimento` no MESMO commit do conserto (mantém `@pytest.mark.invariante`). Nunca trocar por skip, nunca afrouxar o limiar.
Se a Task 1 escolheu janela-5: editar `config.yaml:57` `normalizacao.anos_media: 5` E `calibracao.lock.yaml:194` no MESMO commit, com mensagem de justificativa SEM ticker (ex.: "janela=5 torna a base robusta a um exercício atípico terminal — requisito de D-01"). Se janela-3: NÃO tocar config/lock.
NÃO mexer em `g_cap`/`Ke`/`ke_teto`/`ke_piso`/ERP/beta/motor. NÃO adicionar knob compensatório.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "normalizacao or nao_pune_crescimento or knobs or orcamento_de_knobs or knobs_batem" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "theilslopes" src/analista/core/normalizacao.py` retorna >= 1 e existe o guard `endpoint <= 0` caindo em `median`.
    - `grep -c "media_ciclo" src/analista/core/normalizacao.py` >= 1 e `grep -c "media_ciclo" src/analista/report/report.py` >= 1 (ramo cíclico repontado).
    - `.venv/bin/python -m pytest -k nao_pune_crescimento` sai 0 SEM marcador xfail nesse teste (`grep -n xfail tests/test_invariantes_v24.py` não lista test_normalizacao_nao_pune_crescimento).
    - `.venv/bin/python -m pytest -k "orcamento_de_knobs or knobs_batem_com_o_lock"` verde: o orçamento continua exatamente 3 graus (ERP, n_fade, PIB_real); Theil-Sen é parameter-free (nenhum 4º grau).
    - Se janela-5: `git diff --name-only` do commit inclui config.yaml E calibracao.lock.yaml juntos; a mensagem de justificativa NÃO menciona ticker (`-k justificativa` verde).
    - Suíte default verde: `.venv/bin/python -m pytest` → 0 failed (golden_nivel deselecionado, BLIND-02b ainda xfailed, jackknife skipped).
  </acceptance_criteria>
  <done>base_normalizada = endpoint Theil-Sen + guard; media_ciclo alimenta o cíclico; BLIND-03 verde sem xfail; orçamento de 3 knobs intacto; suíte default 0 failed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Fase de métodos numéricos puros sobre dados já ingeridos e localmente confiáveis. Nenhuma nova entrada externa, auth ou rede neste plano (o fetch BCB fica no Plano 03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | scipy.theilslopes sobre série numérica | accept | Biblioteca vetada já instalada (scipy 1.17.1); entrada é série interna já validada na ingestão (Fase 8/9). Sem superfície de ataque nova. |
| T-10-02 | Denial of Service | endpoint negativo/degenerado em série de prejuízo | mitigate | Guard `endpoint<=0→median` + ladder N=0/1/2 impedem NaN/negativo silencioso propagando ao RIM/DCF. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest` (default) → 0 failed.
- `.venv/bin/python -m pytest -k nao_pune_crescimento` verde sem xfail (BLIND-03 curado).
- Orçamento de knobs intacto (3 graus) via `-k "orcamento or knobs_batem"`.
- `grep -c "media_ciclo" src/analista/report/report.py` >= 1 (split efetivo).
</verification>

<success_criteria>
PRIM-01 entregue: a base de valuation usa o endpoint Theil-Sen (reflete o ano recente), o motor cíclico mantém a média through-cycle, o BLIND-03 vira verde e o orçamento de 3 knobs permanece intacto.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/10-primitivas-sem-vi-s-prim/10-01-SUMMARY.md`.
</output>
