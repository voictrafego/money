---
phase: 18-home-watchlist-noticias
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified: [src/analista/core/home_feed.py, app.py]
autonomous: true
requirements: [NEWS-01, NEWS-02]
user_setup: []
must_haves:
  truths:
    - "A Home exibe um feed de noticias do mercado com manchete + submanchete + fonte + horario (America/Sao_Paulo)"
    - "Clicar numa noticia abre o site original da fonte em nova aba; nunca reproduz o texto completo"
    - "O feed vem de RSS aberto (InfoMoney + Google News BR) com dedupe/sort por data; auto-atualiza ~5-15min via fragment com cache compartilhado"
    - "Se uma fonte cair, o feed degrada sem quebrar; custo-zero (sem API paga)"
  artifacts:
    - path: "src/analista/core/home_feed.py"
      provides: "noticias() real: feedparser InfoMoney + Google News, User-Agent, try/except por feed, dedupe, sort por data desc, TZ America/Sao_Paulo"
      contains: "feedparser"
    - path: "app.py"
      provides: "wrapper @st.cache_data(ttl=600) _noticias + fragment @st.fragment(run_every=600) _render_noticias + render seguro (st.markdown texto + st.link_button)"
      contains: "st.link_button"
  key_links:
    - from: "app.py _render_noticias (fragment)"
      to: "home_feed.noticias"
      via: "wrapper _noticias() @st.cache_data(ttl=600)"
      pattern: "noticias"
    - from: "home_feed.noticias"
      to: "InfoMoney RSS + Google News RSS"
      via: "feedparser.parse(url, agent=UA) por feed, try/except por feed"
      pattern: "feedparser.parse"
---

<objective>
Preencher o feed de noticias da Home (NEWS-01 + NEWS-02): implementar home_feed.noticias() (feedparser
sobre InfoMoney + Google News RSS, User-Agent, try/except por feed, dedupe por titulo, sort por data desc,
pubDate para America/Sao_Paulo) e, no app.py, o wrapper cacheado _noticias (ttl=600), o fragment
_render_noticias (run_every=600) e o render SEGURO de cada item (titulo/resumo como texto escapado,
st.link_button para o site original em nova aba). Substitui o placeholder de noticias do render_home.

Purpose: feed agregador custo-zero, cache compartilhado (D-05), zona segura de copyright (so manchete + link).
Output: home_feed.noticias real + bloco de noticias funcional no render_home.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-home-watchlist-noticias/18-CONTEXT.md
@.planning/phases/18-home-watchlist-noticias/18-RESEARCH.md
@.planning/phases/18-home-watchlist-noticias/18-PATTERNS.md

<interfaces>
home_feed (planos 01/02) — noticias() a implementar; render_home() (placeholder de noticias a substituir)
intraday.py:27,61-73 — _TZ_B3 = "America/Sao_Paulo" e a conversao de TZ a reusar
app.py:58-68 — modelo de wrapper @st.cache_data(ttl=) com import tardio
app.py:912-915 — modelo de @st.fragment(run_every=)
Feeds verificados ao vivo (18-RESEARCH.md Q6):
  InfoMoney: https://www.infomoney.com.br/mercados/feed/  (submanchete boa via description)
  Google News BR: https://news.google.com/rss/search?q=mercado+financeiro+bolsa+dividendos+when:1d&hl=pt-BR&gl=BR&ceid=BR:pt-419
  User-Agent: Mozilla/5.0 (compatible; AnalistaDividendos/1.0)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: home_feed.noticias() real — feedparser multi-feed + dedupe + sort + TZ</name>
  <files>src/analista/core/home_feed.py</files>
  <read_first>
    - src/analista/ingest/intraday.py:27,61-73 (_TZ_B3 + conversao de TZ a reusar)
    - 18-RESEARCH.md (Code Examples: Parse de noticias multi-feed; Pitfalls 3 throttle, 6 TZ; Security V5/V7)
    - 18-PATTERNS.md (contrato never-raise; RSS untrusted; No Analog Found: RSS)
  </read_first>
  <behavior>
    - noticias() retorna list de dict, cada um com fonte/titulo/resumo/link/quando.
    - Itens ordenados por data desc; titulos duplicados (normalizados) aparecem uma vez.
    - quando e datetime em America/Sao_Paulo (do pubDate UTC) ou None se o feed nao trouxe data.
    - Feed que cai NAO derruba os demais (try/except por feed); se tudo falhar, retorna list vazia (never-raise).
    - link so entra se comecar com https:// (V5, esquema seguro).
  </behavior>
  <action>
    Implementar noticias() com feedparser (import tardio). Dict de feeds InfoMoney + Google News BR (URLs da secao
    interfaces) e a constante User-Agent de browser (Pitfall 3, InfoMoney throttla). Para cada feed: feedparser.parse(url,
    agent=UA) em try/except (feed que cai e pulado). Para cada entry (limitar a ~20 por feed): montar dict fonte /
    titulo (e.title) / resumo (e.summary) / link (e.link) / quando. Converter published_parsed (UTC struct_time) para
    datetime em _TZ_B3 (reusando a logica de intraday), nunca hora crua (Pitfall 6). Tratar titulo/resumo/link como UNTRUSTED
    (manter como texto; a UI escapa). So aceitar link que comeca com https://. Ao final: dedupe por titulo normalizado
    (strip+lower) e sort por data desc (itens sem data vao para o fim). NUNCA levantar. Nota A3: registrar no SUMMARY a
    qualidade do Google News (ajustar a query se houver ruido; InfoMoney e a fonte primaria de submanchete).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` sai 0 (283 passed).
    - noticias() retorna list; cada item tem chaves fonte/titulo/resumo/link/quando; nao levanta nem offline (checar com um -c curto).
    - Ha try/except por feed (uma fonte cair nao derruba as outras); links aceitos so se comecam com https://; hora convertida para America/Sao_Paulo.
    - Firewall D-06 intacto (sem import de engines em home_feed.py).
  </acceptance_criteria>
  <done>noticias() implementada: multi-feed com UA, dedupe/sort, TZ B3, never-raise, links https.</done>
</task>

<task type="auto">
  <name>Task 2: Bloco de noticias no app.py — cache wrapper + fragment + render seguro</name>
  <files>app.py</files>
  <read_first>
    - app.py:58-68 (wrapper @st.cache_data), app.py:912-915 (fragment), e o render_home dos planos 01/02
    - 18-RESEARCH.md (Render seguro de item de noticia Q7; Security V7 output encoding)
    - 18-PATTERNS.md (Shared Patterns: Output encoding seguro RSS untrusted)
  </read_first>
  <action>
    No app.py, substituir o placeholder de noticias do render_home por um bloco real:
    - Wrapper cacheado process-global: def _noticias() decorado com @st.cache_data(show_spinner=False, ttl=600) que importa
      home_feed (import tardio) e retorna home_feed.noticias(). Nunca st.cache_data.clear() global (D-05).
    - Fragment @st.fragment(run_every=600) def _render_noticias(): chama _noticias() e renderiza a lista densa.
    - Render SEGURO de cada item (V7, RSS untrusted): st.markdown do titulo em negrito (SEM unsafe_allow_html; nunca
      components.html com conteudo do feed), st.caption com fonte + horario formatado (dia/mes hora), e, so se o link comeca
      com https://, st.link_button para abrir no site original em nova aba (ancora nativa segura, sem tabnabbing). O resumo
      (submanchete) tambem via texto escapado. NUNCA reproduzir texto completo (so manchete/trecho + link).
    - Estado vazio: se a lista vier vazia (todas as fontes cairam), exibir um aviso curto de "sem noticias no momento"
      em vez de quebrar.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - app.py define _noticias com @st.cache_data(ttl=600) e _render_noticias com @st.fragment(run_every=600).
    - Cada item renderiza titulo via st.markdown (texto, sem unsafe_allow_html) + fonte/horario via st.caption + st.link_button so quando link comeca com https://.
    - Nenhum texto completo de noticia e reproduzido; estado vazio tratado sem quebra.
    - `.venv/bin/python -m pytest -q` sai 0 (283 passed); os 4 branches existentes seguem inalterados.
  </acceptance_criteria>
  <done>Feed de noticias funcional: multi-feed cacheado, auto-refresh ~10min, render seguro (texto + link_button), estado vazio tratado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSS externo -> app | Titulo/resumo/link vindos de feed sao conteudo NAO confiavel |
| link da noticia -> nova aba | Navegacao para dominio externo da fonte |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Tampering/Info | XSS via titulo/resumo RSS malicioso | mitigate | Renderizar como texto (st.markdown SEM unsafe_allow_html; nunca components.html com conteudo do feed) — defesa em profundidade sobre a sanitizacao do feedparser |
| T-18-07 | Tampering | link RSS com esquema perigoso (javascript:) | mitigate | So passar para st.link_button links que comecam com https:// |
| T-18-08 | Tampering | tabnabbing no link em nova aba | mitigate | st.link_button monta ancora nativa com rel seguro (nao usar ancora crua target=_blank) |
| T-18-09 | DoS | poll de feeds x N usuarios / throttle InfoMoney | mitigate | @st.cache_data(ttl=600) (poucos hits) + User-Agent de browser + try/except por feed |

Severidade residual: baixa apos mitigacoes (o vetor block-worthy seria XSS, mitigado por render de texto).
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` sai 0 (283 passed) — engines/goldens intactos.
- home_feed.noticias never-raise, dedupe/sort, TZ B3, links https.
- Bloco de noticias com cache ttl=600, fragment run_every=600, render seguro (texto + link_button), estado vazio tratado.
</verification>

<success_criteria>
- NEWS-01: feed manchete + submanchete + fonte + horario; clique abre o site original em nova aba; so RSS aberto.
- NEWS-02: auto-refresh ~5-15min com cache compartilhado; degrada sem quebrar se uma fonte cair; custo-zero.
</success_criteria>

<output>
Apos concluir, criar .planning/phases/18-home-watchlist-noticias/18-03-SUMMARY.md (registrar a validacao A3 do Google News,
os feeds efetivamente incluidos e a confirmacao dos 283 goldens verdes).
</output>
