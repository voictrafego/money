---
phase: 18-home-watchlist-noticias
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, src/analista/core/home_feed.py, app.py]
autonomous: true
requirements: [HOME-01]
user_setup: []
must_haves:
  truths:
    - "Ao abrir o app, a Home (Inicio) e a primeira tela / opcao default do radio"
    - "Os 4 menus atuais (Analisar/Garimpar/Ranking/Swing) continuam no radio e nao mudam de comportamento"
    - "Existe core/home_feed.py read-only com o contrato cotacoes()/noticias() (never-raise), sem importar engines"
    - "As deps novas (feedparser [+ streamlit-local-storage]) estao pinadas no requirements.txt e a A2 foi validada"
  artifacts:
    - path: "src/analista/core/home_feed.py"
      provides: "Modulo agregador read-only: cotacoes(tickers)->list, noticias()->list, DEFAULT_WATCHLIST/MAX_WATCHLIST, validar_ticker"
      contains: "def cotacoes"
    - path: "app.py"
      provides: "Opcao Inicio como 1o item do radio (default) + render_home() thin no dispatch"
      contains: "render_home"
    - path: "requirements.txt"
      provides: "feedparser pinado (+ streamlit-local-storage conforme A2)"
      contains: "feedparser=="
    - path: ".planning/phases/18-home-watchlist-noticias/.phase-base-sha"
      provides: "SHA-base fixo da fase para o diff de invariancia dos goldens/engines no plano 04"
  key_links:
    - from: "app.py st.sidebar.radio"
      to: "render_home()"
      via: "branch aditivo antes do branch Analisar"
      pattern: "modo.startswith"
    - from: "app.py render_home"
      to: "core/home_feed.py"
      via: "import tardio dentro dos wrappers @st.cache_data"
      pattern: "home_feed"
---

<objective>
Estabelecer o esqueleto da Home como landing default (HOME-01): prepender a opcao "Inicio" como 1o
item do radio (vira default; radio stateless), adicionar um branch aditivo render_home() thin ANTES dos
4 branches existentes (que ficam identicos), e criar o modulo read-only core/home_feed.py com os
CONTRATOS que 02 (watchlist) e 03 (noticias) preenchem. Fixa deps novas (feedparser + validacao A2) e
grava o SHA-base da fase.

Purpose: interface-first — roteamento e contrato existem antes das implementacoes. HOME-01 satisfeito.
Output: requirements.txt, core/home_feed.py (esqueleto never-raise), app.py (radio + scaffold), .phase-base-sha.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-home-watchlist-noticias/18-CONTEXT.md
@.planning/phases/18-home-watchlist-noticias/18-RESEARCH.md
@.planning/phases/18-home-watchlist-noticias/18-PATTERNS.md

<interfaces>
prices.py:27-42 — def _yf() (import tardio yfinance); def yahoo_symbol(ticker)->str (sufixo .SA idempotente, .upper().strip()); _MAX_TENTATIVAS=3; _BACKOFF_SEG=(0.5,1.0)
intraday.py:8-14,27 — contrato never-raise (falha vira estado, nunca raise/None); _TZ_B3="America/Sao_Paulo"
app.py:377-382 — st.sidebar.radio SEM key= (stateless, 1o item e default); branches comecam em app.py:400
app.py:58-68 — modelo de wrapper @st.cache_data(ttl=) process-global com import tardio (frame_intraday)
app.py:82-97 — helpers fmt_pct, fmt_num, fmt_rs, esc_md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixar SHA-base + deps novas no requirements.txt (validar A2)</name>
  <files>.planning/phases/18-home-watchlist-noticias/.phase-base-sha, requirements.txt</files>
  <read_first>
    - requirements.txt (bloco: yfinance>=0.2.40, streamlit>=1.30, plotly>=6.0)
    - 18-RESEARCH.md (Standard Stack, Assumptions Log A2, Installation)
  </read_first>
  <action>
    ANTES de mudar codigo: gravar `git rev-parse HEAD` em `.planning/phases/18-home-watchlist-noticias/.phase-base-sha`
    (base fixa para o diff do plano 04 — nunca HEAD~N). Depois:
    1. Append `feedparser==6.0.12` ao requirements.txt (pin exato).
    2. Instalar: `.venv/bin/pip install "feedparser==6.0.12"` e `.venv/bin/pip install "streamlit-local-storage==0.0.25"`.
    3. VALIDAR A2 (streamlit-local-storage x streamlit 1.58): tentar `from streamlit_local_storage import LocalStorage`.
       Se importar: append `streamlit-local-storage==0.0.25` ao requirements.txt (plano 02 usa p/ persistencia cross-session).
       Se falhar: NAO adicionar; registrar no SUMMARY que 02 usara fallback session_state (persistencia best-effort).
    So append; nao alterar deps existentes. Usar .venv/bin/pip e .venv/bin/python (sem `python` pelado).
  </action>
  <verify>
    <automated>test -s .planning/phases/18-home-watchlist-noticias/.phase-base-sha && grep -q "feedparser==6.0.12" requirements.txt && .venv/bin/python -c "import feedparser"</automated>
  </verify>
  <acceptance_criteria>
    - `.phase-base-sha` existe com um SHA de 40 hex; deps pre-existentes inalteradas.
    - requirements.txt contem feedparser==6.0.12; import feedparser funciona no venv.
    - Decisao A2 registrada no SUMMARY (dep adicionada OU fallback session_state).
  </acceptance_criteria>
  <done>SHA-base fixado; feedparser pinado e importavel; decisao A2 registrada.</done>
</task>

<task type="auto">
  <name>Task 2: core/home_feed.py (esqueleto never-raise) + roteamento Home no app.py</name>
  <files>src/analista/core/home_feed.py, app.py</files>
  <read_first>
    - app.py:1-32 (imports do topo) e app.py:372-400 (titulo/caption + radio + inicio dos branches)
    - src/analista/ingest/prices.py:27-42 (_yf, yahoo_symbol)
    - src/analista/ingest/intraday.py:8-14 (contrato never-raise)
    - 18-PATTERNS.md (home_feed firewall D-06; app.py roteamento excerto 1)
  </read_first>
  <action>
    Criar src/analista/core/home_feed.py read-only (firewall D-06): NAO importar
    report/build/indicators/multiples/screening/comparables/grafico — so feedparser e prices por import
    TARDIO dentro das funcoes. Definir:
    - DEFAULT_WATCHLIST = ("BBSE3","TAEE11","EGIE3","ITUB4","BBAS3") e MAX_WATCHLIST = 5.
    - validar_ticker(t): .strip().upper(), devolve o ticker se casar o regex de 4 a 6 chars A-Z0-9, senao None (defesa V5; nunca levanta).
    - cotacoes(tickers) -> list[dict]: assinatura + docstring never-raise; esqueleto retorna lista vazia (corpo no plano 02).
    - noticias() -> list[dict]: idem; esqueleto retorna lista vazia (corpo no plano 03).
    Docstring do modulo: read-only, never-raise (degrada por item, espelha FrameOHLC/coletar_mercado), firewall D-06, custo-zero.
    No app.py:
    - Prepender "Inicio" (com o icone de casa) como 1o item da lista do st.sidebar.radio (stateless, vira default; NAO usar index=).
      Os 4 itens seguintes ficam identicos, na mesma ordem.
    - Definir def render_home() (thin): subheader/intro + dois blocos placeholder "Minha watchlist" e
      "Noticias do mercado" (ex.: st.info de carregando), substituidos por 02/03. Sem recalculo de metodo.
    - Antes do branch Analisar (app.py:400), adicionar o branch da Home que chama render_home(). Os 4 branches existentes
      permanecem inalterados. st.title/st.caption e a sidebar Selic/aviso seguem globais (nao mover).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q &amp;&amp; grep -q "def render_home" app.py &amp;&amp; grep -q "def cotacoes" src/analista/core/home_feed.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` sai com codigo 0 (283 passed) — scaffold nao regride goldens.
    - home_feed.cotacoes(("BBSE3",)) e home_feed.noticias() retornam list sem levantar; MAX_WATCHLIST==5; validar_ticker("bbse3")=="BBSE3" e validar_ticker("bad;x") is None.
    - home_feed.py nao tem import (top-level nem tardio) de report/build/indicators/multiples/screening/comparables/grafico.
    - app.py contem a string Inicio como 1o item do radio e def render_home; o branch da Home aparece antes do branch Analisar.
  </acceptance_criteria>
  <done>home_feed.py existe (never-raise, firewall D-06); Home e o item default do radio e roteia para render_home(); 283 goldens verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> venv | Deps novas (feedparser, streamlit-local-storage) entram na arvore de execucao |
| usuario -> radio | Escolha de menu (conjunto fechado de opcoes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | supply-chain das deps novas | mitigate | Pin exato (feedparser==6.0.12, streamlit-local-storage==0.0.25) verificado no RESEARCH; A2 validada por import antes de confiar |
| T-18-02 | Elevation | firewall do modulo novo | mitigate | home_feed.py proibido de importar engines (aceite verifica imports); mantem as 283 goldens e a aba Analisar isoladas |

Severidade residual: baixa. Este plano nao expoe input de rede novo (fetch real vem em 02/03).
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` sai 0 (283 passed).
- Contrato/firewall de home_feed.py conforme acceptance_criteria.
- Inicio e o 1o item do radio (default) e roteia para render_home(); os 4 branches existentes inalterados.
</verification>

<success_criteria>
- HOME-01: Home e a landing default; os 4 menus continuam acessiveis e sem mudanca de comportamento.
- core/home_feed.py criado (read-only, never-raise, firewall D-06) com os contratos para 02/03.
- Deps novas pinadas; A2 decidida (dep ou fallback).
</success_criteria>

<output>
Apos concluir, criar .planning/phases/18-home-watchlist-noticias/18-01-SUMMARY.md (registrar a decisao A2,
o SHA-base gravado e a confirmacao dos 283 goldens verdes).
</output>
