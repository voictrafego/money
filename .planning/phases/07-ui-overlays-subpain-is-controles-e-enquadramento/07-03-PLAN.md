---
phase: 07-ui-overlays-subpain-is-controles-e-enquadramento
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/grafico.py
  - tests/test_grafico_ui.py
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]
must_haves:
  truths:
    - "A lógica de QUAIS overlays desenhar no painel de preço deriva puramente do estado dos toggles + a.sinais (UI-01/UI-03)."
    - "A lógica de QUAIS subpainéis criar (RSI/MACD/ADX) é dinâmica e devolve SPEC COMPLETO (série(s) + níveis de referência), não só nomes: subpainel só entra quando o toggle está ON e a série existe (não toda-NaN) (UI-02)."
    - "Os marcadores de evento (golden/death cross, rompimento Donchian) saem nas datas EXATAS lendo as séries de a.sinais + close (UI-04), nomeando o evento."
    - "O estado padrão tem o técnico OFF (UI-06): por padrão nenhum overlay/subpainel é desenhado."
  artifacts:
    - path: "src/analista/grafico.py"
      provides: "Funções puras + dataclasses de spec: estado_padrao(), overlays_preco()->[OverlaySpec], subpaineis_ativos()->[SubpainelSpec(nome, series, referencias)], layout_subplots(), marcadores_eventos()->[Marcador], leitura_tecnica_disponivel()"
      min_lines: 90
      exports: ["estado_padrao", "overlays_preco", "subpaineis_ativos", "layout_subplots", "marcadores_eventos", "leitura_tecnica_disponivel", "OverlaySpec", "SubpainelSpec", "Marcador"]
    - path: "tests/test_grafico_ui.py"
      provides: "Golden das funções de montagem (overlays/subpainéis com série+níveis/marcadores/layout/degradação)"
  key_links:
    - from: "src/analista/grafico.py (marcadores_eventos)"
      to: "a.sinais.tendencia.sma50/sma200 + close"
      via: "detecção de mudança de sinal (cross) e close×donchian"
      pattern: "sma50|donchian"
    - from: "src/analista/grafico.py (subpaineis_ativos)"
      to: "SubpainelSpec com a.sinais.{forca.adx, momentum.rsi/macd/macd_sinal/macd_hist} + referencias [20,25]/[30,70]/[0]"
      via: "toggle ON e série com ponto não-NaN, mapeamento nome→série no módulo puro"
      pattern: "SubpainelSpec"
---

<objective>
Extrair TODA a lógica de montagem do gráfico técnico para funções puras e testáveis em
`src/analista/grafico.py`, deixando o app.py (Plans 04/05) como fina camada de render. Isto
satisfaz a regra do CONTEXT/deep_work_rules: a verificação visual de Streamlit é difícil de
automatizar, então a decisão de QUAIS overlays/subpainéis/marcadores, em que ordem e com quais
dados e níveis de referência, vive em funções puras cobertas por golden.

Este plano é o CONTRATO de interface que os planos de app.py consomem (estado dos toggles,
specs de overlays, specs de subpainéis — incluindo série(s) e linhas de referência —, marcadores,
layout). Read-only sobre a.sinais — nenhuma fórmula de indicador é recalculada.

Simetria de testabilidade (WARNING do checker): tanto `overlays_preco` quanto `subpaineis_ativos`
devolvem SPECS completos (série(s) + estilo/níveis). O app.py NÃO mapeia nome→série nem hardcoda
níveis de referência (20/25, 30/70, 0); tudo é golden-coberto aqui.

Purpose: UI-01/UI-02/UI-03/UI-04 viram lógica determinística testável; o redesenho sem
recompute (UI-03) é só re-chamar essas funções com outro estado.
Output: `grafico.py` (funções puras + dataclasses de spec) + `test_grafico_ui.py` (golden).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
src/analista/core/indicators.py — SinaisTecnicos (já existe; séries pd.Series por família):
  tendencia: sma20/sma50/sma200, ema20/ema50/ema200, posicao_mm200, cruzamento
  canais:    donchian_sup/inf (20), donchian_sup_55/inf_55, bb_sup/bb_med/bb_inf, largura_bb, squeeze_pct, rompimento_donchian, toque_bollinger, squeeze
  forca:     adx, pdi, ndi, regressao_slope_ann, regressao_r2, forca_adx
  momentum:  rsi, macd, macd_sinal, macd_hist, nivel_rsi, cruzamento_macd
  close:     pd.Series  (close split-adjusted usada — exposta no Plan 01; passar explicitamente às funções)

Rótulos discretos: posicao_mm200∈{"acima","abaixo","indisponivel"} ; cruzamento∈{"golden_cross","death_cross","nenhum","indisponivel"} ; rompimento_donchian∈{"nova_maxima","perda_minima","nenhum","indisponivel"}.

config.yaml indicadores: sma_emas:[20,50,200], donchian:[20,55], bollinger{janela:20,sigma:2.0}, rsi_faixas:[30,70].

Invocação de testes (padrão do projeto): usar `./.venv/bin/python -m pytest` (tests/ não é package;
`./.venv/bin/pytest` puro dá ModuleNotFoundError e coleta 0).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contrato de estado + overlays + subpainéis (com série+níveis) + layout (UI-01/UI-02/UI-03)</name>
  <files>src/analista/grafico.py, tests/test_grafico_ui.py</files>
  <read_first>
    - src/analista/core/indicators.py (SinaisTecnicos e famílias — bloco interfaces acima)
    - app.py:138-174 (gráfico go.Figure atual — paleta/estilo a herdar)
    - .planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-CONTEXT.md (controles HÍBRIDOS + técnico OFF por padrão)
  </read_first>
  <behavior>
    - estado_padrao() devolve dict com técnico OFF: tendencia{on:False, tipo:"sma", janelas:[20,50,200]},
      canais{donchian_on:False, donchian_janela:20, bollinger_on:False}, forca{on:False}, momentum{rsi_on:False, macd_on:False}.
    - overlays_preco(estado, sinais) -> list[OverlaySpec(nome, serie, estilo)]: estado OFF ⇒ lista vazia.
      tendencia.on com tipo "ema" ⇒ usa ema*; "sma" ⇒ sma*. janelas controla quais MMs (ex.: [50,200] ⇒ só
      sma50/sma200). canais.donchian_janela 55 ⇒ donchian_sup_55/inf_55. bollinger_on ⇒ bb_sup/bb_med/bb_inf.
    - subpaineis_ativos(estado, sinais) -> list[SubpainelSpec(nome, series, referencias)] na ordem fixa
      ["adx","rsi","macd"]; inclui só os toggles ON cuja série principal tem ≥1 ponto não-NaN. O SPEC carrega
      o mapeamento nome→série(s) E os níveis de referência (no módulo puro, não no app.py):
        * adx → series=[("ADX", forca.adx)], referencias=[20.0, 25.0]
        * rsi → series=[("RSI", momentum.rsi)], referencias=[30.0, 70.0]
        * macd → series=[("MACD", momentum.macd), ("Sinal", momentum.macd_sinal), ("Histograma", momentum.macd_hist)], referencias=[0.0]
      Séries todo-NaN (histórico curto) NÃO criam subpainel (degradação, DATA-03).
    - layout_subplots(n): rows=1+n; row_heights com preço dominante e osciladores proporcionais (somam 1.0).
  </behavior>
  <action>
    Criar `src/analista/grafico.py` com as dataclasses `OverlaySpec(nome, serie, estilo)` e
    `SubpainelSpec(nome, series, referencias)` (series = lista de (rotulo, pd.Series); referencias = list[float]),
    e as funções puras `estado_padrao()`, `overlays_preco(estado, sinais)`, `subpaineis_ativos(estado, sinais)`,
    `layout_subplots(n_subpaineis)`. Sem dependência de streamlit/plotly no módulo (devolve dados/specs, não
    objetos de figura). Reaproveitar a nomenclatura das famílias do SinaisTecnicos. Estilo (cores/larguras)
    como dica leve, herdando a paleta do gráfico atual (ex.: preço #1f77b4). Escrever os golden em
    test_grafico_ui.py construindo um SinaisTecnicos sintético (séries pequenas) e afirmando overlays,
    subpainéis (nome, séries e referencias) e layout.
  </action>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_grafico_ui.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - estado_padrao() tem todas as famílias OFF (overlays_preco/subpaineis_ativos com esse estado ⇒ vazios).
    - Com tendencia.on e tipo "ema", overlays_preco usa as séries ema*; com tipo "sma", as sma*.
    - subpaineis_ativos devolve SubpainelSpec com as séries corretas e as referencias ([20,25] p/ adx, [30,70]
      p/ rsi, [0] p/ macd) — golden afirma nome, rótulos das séries e os níveis; NÃO inclui oscilador ON com
      série toda-NaN; inclui quando há ponto válido.
    - layout_subplots(n): retorna 1+n linhas e row_heights somando 1.0 com o preço como maior fração.
  </acceptance_criteria>
  <done>Contrato de estado + montagem de overlays e subpainéis (série+níveis) + layout, puro e testado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Marcadores de evento nas datas exatas + degradação (UI-04)</name>
  <files>src/analista/grafico.py, tests/test_grafico_ui.py</files>
  <read_first>
    - src/analista/core/indicators.py (_tendencia: lógica de cross sma50−sma200 ; _canais: close×donchian shift(1))
    - .planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-CONTEXT.md (UI-04: triângulos/setas nas datas exatas + hover)
  </read_first>
  <behavior>
    - marcadores_eventos(sinais, close): varre a série inteira (não só a última barra):
        * golden_cross = data onde sign(sma50−sma200) vira de ≤0 para >0 ; death_cross = vira para <0.
        * nova_maxima = data onde close > donchian_sup (shiftado) ; perda_minima = close < donchian_inf.
      Devolve lista de Marcador(data, y, tipo, rotulo), y na própria série lida (sma no cross; nível do canal
      no rompimento), rotulo nomeando o evento em PT ("Golden cross (MM50×MM200)", "Rompimento da máxima",...).
      Séries indisponíveis/curtas ⇒ lista vazia (sem exceção).
    - leitura_tecnica_disponivel(sinais): False quando posicao_mm200=="indisponivel" (ou sinais None); True caso contrário.
  </behavior>
  <action>
    Acrescentar a `grafico.py` `marcadores_eventos(sinais, close)` e `leitura_tecnica_disponivel(sinais)`.
    A detecção de cross espelha a regra discreta de `_tendencia` mas aplicada a TODA a série (mudança de sinal
    de sma50−sma200, dropna). A detecção de rompimento espelha `_canais` (close vs donchian_sup/inf), usando a
    `close` passada (= a.sinais.close do Plan 01). Definir dataclass `Marcador(data, y, tipo, rotulo)`. Funções
    puras (sem plotly) — devolvem dados que o app.py converte em traces de marcador (símbolo/cor = discrição,
    seguindo o estilo atual). Golden em test_grafico_ui.py: série com um cross conhecido e um rompimento
    conhecido ⇒ afirmar a data e o tipo exatos do(s) marcador(es); série achatada ⇒ lista vazia.
  </action>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_grafico_ui.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Para uma série com golden cross numa data conhecida, marcadores_eventos retorna um Marcador com tipo
      "golden_cross" e essa data exata; idem para um rompimento de máxima do Donchian.
    - Série achatada/curta ⇒ marcadores_eventos retorna [] e leitura_tecnica_disponivel retorna False.
  </acceptance_criteria>
  <done>Marcadores derivados puramente de a.sinais+close, nas datas exatas, com hover nomeado; degradação detectável.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Módulo puro de lógica; recebe dataclasses já calculados, sem rede/IO/eval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | estado de toggle inválido/inesperado vindo da sessão | mitigate | funções tratam estado parcial via defaults de estado_padrao(); golden cobre estado OFF |
| T-07-05 | Denial of Service | séries todo-NaN / vazias | mitigate | subpaineis_ativos e marcadores_eventos degradam para vazio sem exceção (testado) |
</threat_model>

<verification>
- `./.venv/bin/python -m pytest tests/test_grafico_ui.py -q` verde.
- `grep -n "import streamlit\|import plotly" src/analista/grafico.py` retorna 0 (módulo puro, sem UI).
</verification>

<success_criteria>
- Funções de montagem puras e testadas cobrindo UI-01/UI-02/UI-03/UI-04; subpainéis com série+níveis no módulo; técnico OFF por padrão; degradação para vazio.
</success_criteria>

<output>
Após concluir, crie `.planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-03-SUMMARY.md`.
</output>
