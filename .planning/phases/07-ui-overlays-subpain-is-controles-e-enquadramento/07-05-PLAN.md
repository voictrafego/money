---
phase: 07-ui-overlays-subpain-is-controles-e-enquadramento
plan: 05
type: execute
wave: 3
depends_on: [04, 03, 01]
files_modified:
  - app.py
autonomous: false
requirements: [UI-01, UI-02, UI-04]
must_haves:
  truths:
    - "O gráfico de preço migra de go.Figure para make_subplots: 1 painel de preço (com a banda DDM preservada) + N subpainéis criados SÓ para os osciladores ativos (UI-01/UI-02)."
    - "Os overlays ativos (MMs/Donchian/Bollinger) são desenhados no eixo de preço lendo a.sinais via grafico.overlays_preco (UI-01)."
    - "Os subpainéis são montados a partir do SubpainelSpec (série(s)+níveis de referência) vindo do módulo puro — app.py não hardcoda mapeamento nome→série nem níveis."
    - "Os marcadores de evento (golden/death cross, rompimento Donchian) aparecem nas datas exatas com hover nomeando o evento (UI-04)."
    - "Mudar os toggles redesenha o subconjunto escolhido sem recomputar a engine (UI-03 observável)."
    - "Um leitor novo numa tela 'VERIFICAR/cara + timing bullish' reconhece o fundamento como decisório (critério de aceite UI-06)."
  artifacts:
    - path: "app.py"
      provides: "make_subplots dinâmico dirigido por grafico.* (overlays, subpaineis_ativos->SubpainelSpec, layout_subplots, marcadores_eventos) preservando a banda DDM e o rangeselector"
      contains: "make_subplots"
  key_links:
    - from: "app.py (gráfico)"
      to: "grafico.subpaineis_ativos / overlays_preco / marcadores_eventos / layout_subplots"
      via: "render dirigido por st.session_state['tec_estado'] + a.sinais (specs consumidos, nada hardcoded)"
      pattern: "make_subplots|overlays_preco|marcadores_eventos"
    - from: "app.py (marcadores)"
      to: "a.sinais.close"
      via: "marcadores_eventos(a.sinais, a.sinais.close)"
      pattern: "a\\.sinais\\.close"
---

<objective>
Migrar o gráfico de preço de `go.Figure` para `make_subplots` dinâmico, dirigido pelas funções
puras do `grafico.py` (Plan 03) e pelo estado dos toggles (Plan 04): overlays no painel de preço
(UI-01), subpainéis de osciladores criados só quando ativos (UI-02), e marcadores de evento nas
datas exatas (UI-04). A banda do valor intrínseco (DDM) e o rangeselector do gráfico atual são
preservados. app.py permanece READ-ONLY (lê a.sinais, não recomputa) e NÃO hardcoda o mapeamento
nome→série nem os níveis de referência dos subpainéis — consome os SubpainelSpec do módulo puro.

Fecha a fase com um checkpoint humano: o fresh-reader test do UI-06 (STATE/Blockers) — um leitor
novo numa tela "VERIFICAR/cara + timing bullish" deve reconhecer o fundamento como decisório —
incluindo um ticker COM split (ITSA4) para validar o alinhamento visual dos overlays/marcadores.

Purpose: entregar o gráfico técnico completo (overlays + subpainéis + marcadores) com o
redesenho-sem-recompute (UI-03 observável) e validar o enquadramento por verificação humana.
Output: app.py com make_subplots dinâmico; UI-01/UI-02/UI-04 visíveis; UI-06 verificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-CONTEXT.md
@.planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-03-PLAN.md

<interfaces>
app.py (gráfico atual ~138-174): go.Figure com trace de preço (serie=c.serie_precos, daily nominal #1f77b4),
  add_hrect da banda DDM (a.vmin..a.vmax), rangeselector 30D/6M/1A/5A, update_layout(height=400).
  Guarda de série indisponível ~141-146 (st.info) — preservar.

src/analista/grafico.py (Plan 03):
  estado_padrao(), overlays_preco(estado, sinais)->[OverlaySpec(nome, serie, estilo)],
  subpaineis_ativos(estado, sinais)->[SubpainelSpec(nome, series, referencias)]  # series=[(rotulo, serie)]; referencias=[float]
  layout_subplots(n)->(rows, row_heights), marcadores_eventos(sinais, close)->[Marcador(data,y,tipo,rotulo)],
  leitura_tecnica_disponivel(sinais)->bool.
src/analista/core/indicators.py: a.sinais.close (Plan 01) — close split-adjusted p/ os marcadores.

NOTA de alinhamento (CR-01, locked): o painel de preço usa a close NOMINAL diária (c.serie_precos)
para alinhar com a banda DDM; os overlays/subpainéis leem a.sinais (split-adjusted, base_temporal do
config — semanal por padrão). É o tradeoff travado em CR-01/DATA-02; não tentar reconciliar/recompute.
O efeito visual só aparece em ticker COM split na janela — por isso o checkpoint inclui ITSA4.

Invocação de testes (padrão do projeto): `./.venv/bin/python -m pytest` (tests/ não é package).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: make_subplots dinâmico — overlays no preço + subpainéis (via SubpainelSpec) dos osciladores ativos (UI-01/UI-02)</name>
  <files>app.py</files>
  <read_first>
    - app.py (gráfico go.Figure ~138-174 — preço, banda DDM, rangeselector, layout)
    - src/analista/grafico.py (overlays_preco, subpaineis_ativos->SubpainelSpec, layout_subplots, leitura_tecnica_disponivel)
  </read_first>
  <action>
    Trocar `import plotly.graph_objects as go` por também `from plotly.subplots import make_subplots`.
    Substituir o bloco do go.Figure: ler `estado = st.session_state["tec_estado"]`; computar
    `specs = grafico.subpaineis_ativos(estado, a.sinais)` e `rows, heights = grafico.layout_subplots(len(specs))`;
    criar `make_subplots(rows=rows, cols=1, shared_xaxes=True, row_heights=heights, vertical_spacing=...)`.
    Row 1 = preço (c.serie_precos, mesmo trace/estilo atual) + a banda DDM via add_hrect no row 1 (preservar
    a.vmin/a.vmax) + cada OverlaySpec de `grafico.overlays_preco(estado, a.sinais)` como go.Scatter no row 1
    (usa spec.nome/spec.serie/spec.estilo; hovertemplate de data/valor). Cada SubpainelSpec de `specs`, na ordem,
    vira um row subsequente: iterar spec.series (cada (rotulo, serie) → go.Scatter no row) e desenhar uma linha
    de referência (add_hline) para cada nível em spec.referencias. NÃO hardcodar nome→série nem os níveis (20/25,
    30/70, 0): tudo vem do spec. Quando `not grafico.leitura_tecnica_disponivel(a.sinais)` ou nenhum toggle ativo
    (specs vazio), renderizar só o painel de preço + banda (comportamento atual), sem subpainéis. Preservar o
    rangeselector e a guarda de série indisponível (st.info). Ajustar height proporcional ao nº de linhas.
    Read-only: nenhum recompute de indicador.
  </action>
  <verify>
    <automated>./.venv/bin/python -c "import ast; ast.parse(open('app.py').read())" && grep -q "make_subplots" app.py && grep -q "overlays_preco\|subpaineis_ativos" app.py && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - app.py usa make_subplots; row 1 mantém preço + banda DDM + rangeselector.
    - Os overlays vêm de grafico.overlays_preco e os subpainéis de grafico.subpaineis_ativos (SubpainelSpec):
      app.py itera spec.series e spec.referencias — sem mapeamento nome→série nem níveis hardcoded.
    - Estado técnico OFF/degradado ⇒ só o painel de preço (paridade com o gráfico atual).
  </acceptance_criteria>
  <done>Gráfico make_subplots dinâmico: overlays no preço (UI-01) + subpainéis só dos osciladores ativos, montados via SubpainelSpec (UI-02).</done>
</task>

<task type="auto">
  <name>Task 2: Marcadores de evento nas datas exatas (UI-04)</name>
  <files>app.py</files>
  <read_first>
    - app.py (bloco make_subplots da Task 1)
    - src/analista/grafico.py (marcadores_eventos, Marcador)
  </read_first>
  <action>
    No row do preço, adicionar `for m in grafico.marcadores_eventos(a.sinais, a.sinais.close)`: um go.Scatter
    mode="markers" em (m.data, m.y) com símbolo/cor por m.tipo (triângulo-up verde p/ golden_cross e nova_maxima;
    triângulo-down vermelho p/ death_cross e perda_minima — discrição de estilo), `hovertext=m.rotulo` e
    `hovertemplate` nomeando o evento e a data. Quando a leitura degrada (lista vazia), nenhum marcador é
    adicionado (sem erro). Read-only: só lê a.sinais/close.
  </action>
  <verify>
    <automated>./.venv/bin/python -c "import ast; ast.parse(open('app.py').read())" && grep -q "marcadores_eventos" app.py && grep -q "a.sinais.close" app.py && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Os marcadores vêm de grafico.marcadores_eventos(a.sinais, a.sinais.close) (datas exatas, não recomputadas).
    - Cada marcador tem hover nomeando o evento; degradação ⇒ sem marcadores e sem exceção.
  </acceptance_criteria>
  <done>Eventos marcados nas datas exatas com hover nomeado (UI-04).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana — overlays/subpainéis/marcadores + alinhamento split + fresh-reader UI-06</name>
  <what-built>
    Gráfico make_subplots com overlays no preço, subpainéis dinâmicos de RSI/MACD/ADX, marcadores de
    evento nas datas exatas, controles HÍBRIDOS por sessão e enquadramento subordinado ao veredito.
  </what-built>
  <how-to-verify>
    1. Rode `./.venv/bin/streamlit run app.py` e abra a aba "🔎 Analisar uma ação".
    2. Analise TAEE11 (ou EGIE3): confirme o gráfico de preço + banda DDM no topo, igual ao anterior.
    3. Abra "⚙️ Indicadores técnicos (consultivo)". Ligue Tendência (SMA, janelas 50/200): as MMs aparecem
       no painel de preço. Alterne SMA⇄EMA e janelas: o gráfico redesenha sem recoletar dados (sem spinner
       de rede). Ligue ADX/RSI/MACD: subpainéis aparecem só para os ativos, com as linhas de referência
       (ADX 20/25, RSI 30/70, MACD 0); desligue: somem.
    4. Ligue Canais (Donchian 20 e 55, Bollinger): overlays no preço. Confirme marcadores nas datas de
       golden/death cross e rompimento, com hover nomeando o evento.
    5. ALINHAMENTO COM SPLIT (CR-01/DATA-02): analise ITSA4 (ticker com split conhecido na janela) com
       Tendência + Canais + marcadores ligados. Confirme que os overlays/marcadores (split-adjusted, vindos
       de a.sinais) ficam num nível coerente com a linha de preço NOMINAL — e que, se houver descolamento
       vertical na época do split, ele é o tradeoff esperado (não um bug de plotagem/datas trocadas). As
       DATAS dos marcadores devem cair onde o evento ocorre, sem deslocamento temporal.
    6. Passe o mouse no "?" de cada controle/indicador: o tooltip de glossário aparece.
    7. FRESH-READER (UI-06): analise um papel com veredito "VERIFICAR" ou "SOBREAVALIADA" e ligue indicadores
       que mostrem timing de alta. Pergunta de aceite: numa olhada, o FUNDAMENTO (veredito no topo) é o que
       decide — o timing técnico parece consultivo/secundário, não uma ordem de compra?
    8. Analise um papel sem histórico suficiente (ou base curta): confirme a mensagem
       "Leitura técnica indisponível — histórico insuficiente para os indicadores" sem quebrar a aba.
  </how-to-verify>
  <resume-signal>Digite "approved" se tudo confere, ou descreva os ajustes (ex.: cor/símbolo de marcador, row_heights, hierarquia visual do enquadramento, alinhamento split).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → app (ticker, toggles) | Ticker normalizado e tratado por montar(); toggles só mudam o que se desenha (sem rede). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Spoofing/Integrity | timing técnico parecer ordem de compra/venda | mitigate | marcadores/subpainéis são consultivos; veredito mantém o banner decisório; fresh-reader checkpoint valida UI-06 |
| T-07-09 | Denial of Service | a.sinais/close vazios (Yahoo instável) | mitigate | guarda de série indisponível preservada + degradação para só-preço sem exceção |
</threat_model>

<verification>
- `./.venv/bin/python -c "import ast; ast.parse(open('app.py').read())"` (sintaxe ok).
- `./.venv/bin/python -m pytest -q` (133+ verdes — app.py não muda a engine).
- Checkpoint humano aprovado (overlays/subpainéis/marcadores + alinhamento split ITSA4 + fresh-reader UI-06 + degradação).
</verification>

<success_criteria>
- make_subplots dinâmico com overlays (UI-01), subpainéis só-ativos via SubpainelSpec (UI-02), marcadores nas datas exatas (UI-04).
- Redesenho sem recompute observável (UI-03); enquadramento aprovado no fresh-reader test (UI-06).
</success_criteria>

<output>
Após concluir, crie `.planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-05-SUMMARY.md`.
</output>
