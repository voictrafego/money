---
phase: 07-ui-overlays-subpain-is-controles-e-enquadramento
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - app.py
autonomous: true
requirements: [UI-03, UI-05, UI-06]
must_haves:
  truths:
    - "O veredito fundamentalista (incl. 'VERIFICAR') segue no topo como selo decisório; o bloco técnico é seção SECUNDÁRIA, off por padrão, em linguagem consultiva (UI-06)."
    - "Há um st.expander('⚙️ Indicadores técnicos (consultivo)', expanded=False) com 4 toggles por família + sub-opções (SMA⇄EMA + janelas; Donchian 20/55; Bollinger), com estado em st.session_state (UI-03)."
    - "Cada controle/indicador tem tooltip '?' via help=h('tec_*') (UI-05)."
    - "Quando a leitura técnica degrada, a seção mostra 'Leitura técnica indisponível — histórico insuficiente para os indicadores' sem quebrar a aba."
  artifacts:
    - path: "app.py"
      provides: "Expander de controles técnicos (session_state) + seção consultiva subordinada (timing_resumo/matriz_leitura/alerta) + mensagem de degradação"
      contains: "session_state"
  key_links:
    - from: "app.py (expander de controles)"
      to: "st.session_state['tec_estado']"
      via: "toggles escrevem o estado iniciado por grafico.estado_padrao()"
      pattern: "session_state"
    - from: "app.py (controles/seção técnica)"
      to: "analista.glossario.h"
      via: "help=h('tec_*')"
      pattern: "h\\(\"tec_"
---

<objective>
Construir, no app.py, o ANDAIME da seção técnica: o expander de controles (UI-03) com estado em
`st.session_state`, os tooltips de glossário (UI-05), e o ENQUADRAMENTO subordinado (UI-06) — o
veredito fundamentalista continua o selo decisório no topo; a leitura técnica consultiva
(timing_resumo / matriz_leitura / alerta_reverificacao) vira seção secundária abaixo, com a
mensagem de degradação graciosa quando o read técnico não está disponível.

O gráfico em si (make_subplots, overlays, subpainéis, marcadores) é o Plan 05 — aqui o gráfico
permanece o atual; o que muda é o enquadramento, os controles e o estado da sessão que o Plan 05
vai consumir. app.py permanece READ-ONLY sobre a engine (só lê a.* e escreve session_state).

Purpose: garantir a hierarquia fundamento > técnico (UI-06) e o contrato de estado dos controles
(UI-03) antes de ligar o gráfico ao estado.
Output: app.py com expander de controles + seção consultiva subordinada + degradação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-CONTEXT.md
@.planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-03-PLAN.md

<interfaces>
app.py (modo "🔎 Analisar", linhas ~85-242):
  - a = report.analisar_acao(c, CFG)  ; veredito colorido ~110-116 ; métricas m1..m5 ~120-125
  - gráfico go.Figure ~148-174 (preço + banda DDM) ; tabs ~176 em diante.
  - já importa: from analista.glossario import h  ; helpers fmt_pct/fmt_rs/esc_md.
  - a.timing_resumo (str) ; a.matriz_leitura (str) ; a.alerta_reverificacao (Optional[str]) ; a.sinais.

src/analista/grafico.py (Plan 03):
  estado_padrao() -> dict (técnico OFF) ; leitura_tecnica_disponivel(sinais) -> bool.

src/analista/glossario.py (Plan 02): chaves tec_indicadores, tec_mm, tec_cross, tec_donchian,
  tec_bollinger, tec_squeeze, tec_rsi, tec_macd, tec_adx, tec_regressao, tec_timing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expander de controles técnicos com estado em session_state (UI-03/UI-05)</name>
  <files>app.py</files>
  <read_first>
    - app.py (bloco do modo Analisar ~85-242, em especial o gráfico ~138-174)
    - src/analista/grafico.py (estado_padrao — contrato do dict de estado)
    - src/analista/glossario.py (chaves tec_*)
  </read_first>
  <action>
    Importar `from analista import grafico`. Inicializar `st.session_state.setdefault("tec_estado",
    grafico.estado_padrao())`. Logo abaixo do gráfico de preço, criar
    `st.expander("⚙️ Indicadores técnicos (consultivo)", expanded=False)` contendo os 4 toggles por
    família (Tendência / Canais / Força / Momentum) com as sub-opções HÍBRIDAS do CONTEXT:
    Tendência → st.toggle on/off + st.radio SMA⇄EMA + st.multiselect janelas {20,50,200};
    Canais → st.toggle Donchian on/off + st.radio janela {20,55} + st.toggle Bollinger;
    Força → st.toggle ADX; Momentum → st.toggle RSI + st.toggle MACD. Cada widget escreve no dict
    `st.session_state["tec_estado"]` (mesmas chaves de estado_padrao). Cada controle recebe
    `help=h("tec_...")` correspondente (UI-05). NÃO desenhar overlays aqui (é o Plan 05) — só capturar
    o estado. app.py read-only sobre a engine.
  </action>
  <verify>
    <automated>./.venv/bin/python -c "import ast; ast.parse(open('app.py').read()); print('app.py ok')" && grep -q "estado_padrao\|tec_estado" app.py && grep -q "Indicadores técnicos (consultivo)" app.py && echo gates-ok</automated>
  </verify>
  <acceptance_criteria>
    - app.py parseia (AST) e contém o expander "⚙️ Indicadores técnicos (consultivo)" com expanded=False.
    - st.session_state["tec_estado"] é iniciado por grafico.estado_padrao() e atualizado pelos toggles.
    - Cada controle novo tem help=h("tec_*") (paridade de tooltip, UI-05).
  </acceptance_criteria>
  <done>Controles HÍBRIDOS persistidos por sessão, com tooltips de glossário; estado pronto para o gráfico consumir.</done>
</task>

<task type="auto">
  <name>Task 2: Enquadramento subordinado — seção consultiva + degradação (UI-06)</name>
  <files>app.py</files>
  <read_first>
    - app.py (veredito ~110-116, métricas ~120-125, gráfico ~138-174)
    - src/analista/grafico.py (leitura_tecnica_disponivel)
    - .planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-CONTEXT.md (UI-06: técnico subordinado, linguagem consultiva)
  </read_first>
  <action>
    Manter o veredito colorido + métricas no TOPO (selo decisório, sem mudança — inclui o estado
    "VERIFICAR" já tratado pelo ramo st.warning). Abaixo do gráfico/controles, adicionar a seção
    consultiva SECUNDÁRIA (clearly subordinate — st.markdown discreto, não st.success/error que
    competiria com o veredito): se `grafico.leitura_tecnica_disponivel(a.sinais)` e `a.timing_resumo`,
    exibir `**Timing (consultivo):** {a.timing_resumo}`, depois `a.matriz_leitura` (fundamento-primeiro),
    e `a.alerta_reverificacao` (quando não-None) como st.info/caption com voz de reverificação — NUNCA
    venda. Caso contrário (degradação holística do Plan 01: timing_resumo==""), exibir
    `st.caption("Leitura técnica indisponível — histórico insuficiente para os indicadores")`. Usar
    help=h("tec_timing"). Linguagem consultiva em toda a seção; o fundamento permanece o decisório.
  </action>
  <verify>
    <automated>./.venv/bin/python -c "import ast; ast.parse(open('app.py').read())" && grep -q "Leitura técnica indisponível" app.py && grep -q "leitura_tecnica_disponivel\|timing_resumo" app.py && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - O veredito fundamentalista permanece no topo (st.success/error/warning), inalterado.
    - A seção técnica é visivelmente secundária (markdown/caption discreto, não banner de veredito).
    - Quando a leitura degrada, aparece exatamente "Leitura técnica indisponível — histórico insuficiente para os indicadores".
    - Nenhum texto da seção técnica usa "compre/venda" (linguagem consultiva).
  </acceptance_criteria>
  <done>Enquadramento subordinado: fundamento decisório no topo, técnico consultivo abaixo, degradação graciosa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → app (ticker, toggles) | Entrada do usuário; ticker já normalizado (strip/upper) e tratado por montar(); toggles só alteram o que se desenha. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Spoofing/Integrity | seção técnica passar como veredito decisório | mitigate | técnico renderizado como caption/markdown secundário; veredito mantém o banner colorido (UI-06) |
| T-07-07 | Tampering | session_state com estado parcial | accept | estado iniciado por estado_padrao(); widgets só sobrescrevem chaves conhecidas |
</threat_model>

<verification>
- `./.venv/bin/python -c "import ast; ast.parse(open('app.py').read())"` (sintaxe ok).
- Inspeção: expander expanded=False; veredito no topo; seção técnica abaixo; mensagem de degradação presente.
</verification>

<success_criteria>
- Controles HÍBRIDOS em session_state (UI-03) + tooltips (UI-05) + enquadramento subordinado e degradação (UI-06).
</success_criteria>

<output>
Após concluir, crie `.planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-04-SUMMARY.md`.
</output>
