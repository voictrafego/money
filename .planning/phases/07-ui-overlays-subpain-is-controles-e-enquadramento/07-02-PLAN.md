---
phase: 07-ui-overlays-subpain-is-controles-e-enquadramento
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/glossario.py
  - tests/test_glossario.py
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "Cada indicador técnico novo (MMs, Donchian, Bollinger, squeeze, RSI, MACD, ADX, regressão, cruzamentos/rompimentos, timing) tem uma definição de glossário acessível via h()."
    - "As definições são fiéis ao enquadramento consultivo (linguagem 'timing', nunca 'compre/venda')."
  artifacts:
    - path: "src/analista/glossario.py"
      provides: "Novas chaves de tooltip dos indicadores técnicos no dict G"
      contains: "tec_"
    - path: "tests/test_glossario.py"
      provides: "Teste afirmando que h() devolve texto não-vazio para todas as chaves técnicas e que nenhuma usa linguagem de ordem"
  key_links:
    - from: "app.py (controles/seção técnica — Plan 04)"
      to: "analista.glossario.h"
      via: "help=h('tec_*')"
      pattern: "h\\(\"tec_"
---

<objective>
Adicionar ao glossário as definições dos indicadores técnicos novos (UI-05), para que cada
controle/indicador da seção técnica exiba um tooltip "?" com definição acessível, em paridade
com o glossário fundamentalista já existente.

Purpose: UI-05 exige tooltip de glossário para CADA indicador novo; centralizar os textos em
`glossario.py` (mesmo helper `h()`) mantém a paridade e deixa o app.py como fina camada.
Output: chaves novas em `glossario.G` + teste travando a existência e o tom consultivo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
src/analista/glossario.py:
  G: dict[str, str]   # chave curta -> texto markdown do tooltip
  h(chave: str) -> str | None   # retorna G.get(chave)
  Estilo existente: textos curtos, markdown, citam o capítulo do livro quando aplicável.
  Para a Fase 7 a fonte das definições é o glossário do método técnico já discutido (consultivo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Definições de glossário dos indicadores técnicos</name>
  <files>src/analista/glossario.py</files>
  <read_first>
    - src/analista/glossario.py (dict G inteiro e a função h())
    - .planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-CONTEXT.md (decisão UI-05/UI-06: linguagem consultiva)
  </read_first>
  <action>
    Acrescentar ao dict G chaves prefixadas `tec_` cobrindo TODOS os indicadores novos da seção
    técnica, uma por conceito exibível: `tec_indicadores` (intro consultiva da seção),
    `tec_mm` (médias móveis SMA/EMA 20/50/200 + posição vs MM200), `tec_cross` (golden/death cross),
    `tec_donchian` (canal 20/55 + rompimento de máxima/mínima), `tec_bollinger` (bandas 20/2σ + toque),
    `tec_squeeze` (estreitamento = baixa volatilidade), `tec_rsi` (RSI 14 Wilder, faixas 30/70),
    `tec_macd` (MACD 12/26/9 + cruzamento de sinal), `tec_adx` (força da tendência <20/>25, Wilder),
    `tec_regressao` (inclinação anualizada + R²), `tec_timing` (resumo consultivo de timing).
    Tom OBRIGATÓRIO: consultivo — falar de "timing"/"força"/"tendência"/"reverificar fundamentos";
    NUNCA "compre", "venda", "compre agora". Textos curtos em markdown, no estilo das chaves existentes.
  </action>
  <verify>
    <automated>./.venv/bin/python -c "from analista.glossario import h; ks=['tec_indicadores','tec_mm','tec_cross','tec_donchian','tec_bollinger','tec_squeeze','tec_rsi','tec_macd','tec_adx','tec_regressao','tec_timing']; assert all(h(k) for k in ks), 'faltam chaves tec_'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `h(k)` devolve string não-vazia para cada uma das 11 chaves `tec_*` listadas.
    - Nenhum texto `tec_*` contém as substrings "compre", "venda" ou "compre agora" (case-insensitive).
  </acceptance_criteria>
  <done>Glossário técnico completo e consultivo, acessível por h('tec_*').</done>
</task>

<task type="auto">
  <name>Task 2: Teste do glossário técnico (existência + tom consultivo)</name>
  <files>tests/test_glossario.py</files>
  <read_first>
    - src/analista/glossario.py (chaves tec_* recém-criadas)
    - tests/test_indicators.py (estilo mínimo de teste/imports do projeto)
  </read_first>
  <action>
    Criar `tests/test_glossario.py` com dois testes: (1) `test_chaves_tec_presentes` — itera a lista
    canônica das 11 chaves `tec_*` e afirma `h(k)` não-vazio; (2) `test_tom_consultivo` — afirma que
    nenhum valor de chave `tec_*` contém "compre"/"venda" (case-insensitive), travando a regra
    consultiva (UI-06). Lista canônica de chaves definida no próprio teste (fonte da verdade do contrato).
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_glossario.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Os dois testes passam; remover qualquer chave `tec_*` ou introduzir linguagem de ordem faz o teste falhar.
  </acceptance_criteria>
  <done>Contrato do glossário técnico travado por teste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Conteúdo estático de texto; sem entrada do usuário, sem rede, sem IO. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | texto de tooltip induzindo ordem de compra/venda | mitigate | teste trava tom consultivo (proíbe "compre"/"venda") |
</threat_model>

<verification>
- `./.venv/bin/pytest tests/test_glossario.py -q` verde.
</verification>

<success_criteria>
- 11 chaves `tec_*` presentes e consultivas; teste de contrato verde.
</success_criteria>

<output>
Após concluir, crie `.planning/phases/07-ui-overlays-subpain-is-controles-e-enquadramento/07-02-SUMMARY.md`.
</output>
