---
phase: 07-blindagem-processual-blind
plan: 04
type: execute
wave: 4
depends_on: [01, 03]
files_modified:
  - .githooks/commit-msg
  - tests/test_blindagem_hook.py
  - tests/helpers_blindagem.py
  - tests/classificacao.yaml
autonomous: true
requirements: [BLIND-05]

must_haves:
  truths:
    - "Um commit que toque config.yaml e um golden/fixture ao mesmo tempo e' BLOQUEADO — e' a assinatura exata de 'calibrei o knob ate o teste passar'."
    - "O hook e' VERSIONADO: sobrevive a um git clone (nao vive em .git/hooks, que nao e' versionado)."
    - "A escapatoria existe, mas e' BARULHENTA e auditavel para sempre: o trailer Knob-Change-Justification: fica no git log."
    - "Uma justificativa de knob que menciona um TICKER e' bloqueada — a regra escrita do ROADMAP, executavel."
    - "O hook e' bypassavel com --no-verify; por isso existe um BACKSTOP em teste que varre o historico."
  artifacts:
    - path: ".githooks/commit-msg"
      provides: "Bloqueio do co-change config.yaml + golden/fixture (BLIND-05)"
      contains: "Knob-Change-Justification"
    - path: "tests/test_blindagem_hook.py"
      provides: "Backstop: core.hooksPath instalado + varredura do historico do marco"
      contains: "core.hooksPath"
  key_links:
    - from: "git (core.hooksPath)"
      to: ".githooks/commit-msg"
      via: "git config core.hooksPath .githooks"
      pattern: "hooksPath"
    - from: "tests/test_blindagem_hook.py"
      to: "git config --get core.hooksPath"
      via: "subprocess — falha se a protecao estiver DESINSTALADA (estado local, nao versionado)"
      pattern: "githooks"
---

<objective>
Bloquear, no ato do commit, a assinatura exata do overfit do v2.3: **`config.yaml` + um
golden/fixture no MESMO commit**. E — porque hooks são bypassáveis e este repo **não tem CI** —
instalar um **backstop em teste**, para que a proteção não vire fantasma.

Purpose: a varredura dos **676 commits** do repo achou **5 co-changes** de `config.yaml` + `tests/`.
Um deles é `5cd3b61` ("ROE through-cycle no report injetado via `roe_terminal` + knob") — **é
literalmente o overfit do v2.3**. Outros **2 de 5 são legítimos** (primitiva nova + knob + teste
nascendo juntos) → o hook **precisa** de escapatória, e ela precisa ser **auditável**, não um
`--no-verify` silencioso.
Output: `.githooks/commit-msg`, `core.hooksPath` configurado, `tests/test_blindagem_hook.py`.

**ESTE PLANO NÃO MOVE NENHUM NÚMERO.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md
@.planning/phases/07-blindagem-processual-blind/07-01-SUMMARY.md
@.planning/REQUIREMENTS.md
</context>

<interfaces>
<!-- Fatos verificados no repo hoje. Não re-derivar. -->

- `git ls-files .git/hooks` → **0 arquivos**. `.git/hooks` **não é versionado** → um hook solto lá
  **não protege o repositório** (some no próximo clone).
- `git config --get core.hooksPath` → **vazio** hoje (não configurado).
- git **2.39.5**; `core.hooksPath` existe desde a **2.9**. Testado num repo descartável: o hook
  versionado **rodou e bloqueou o commit**.
- **`.github/workflows` NÃO existe** → **não há CI**. O backstop de `--no-verify` tem que ser um
  **teste**.
- ⚠️ **O hook `pre-commit` NÃO recebe o path da mensagem de commit** — quem recebe é o **`commit-msg`**
  (`$1` = arquivo da mensagem), e ele **também** consegue ler o índice via `git diff --cached`.
  → **Usar `.githooks/commit-msg`**, um hook só. O nome "pre-commit" no requisito BLIND-05 é
  **descritivo, não normativo**.
- **Framework `pre-commit` (pypi) está REJEITADO**: dep nova + ambientes isolados baixados por rede,
  para 15 linhas de shell. Viola o custo-zero declarado do projeto. **Nenhum `pip install` nesta fase.**

**Escopo do bloqueio:** `config.yaml` **E** (`tests/fixtures/*` **ou** `tests/test_*`) no mesmo
commit. ⚠️ **`calibracao.lock.yaml` (plano 07-05) fica na RAIZ, fora de `tests/` — de propósito**:
`config.yaml` + lock no mesmo commit é o caminho **sancionado** (a mudança de knob fica visível e
revisável no diff), e não pode ser bloqueado.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: .githooks/commit-msg + instalacao via core.hooksPath + prova E2E de que bloqueia</name>
  <files>.githooks/commit-msg</files>

  <read_first>
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` § **"BLIND-05 — o hook:
      core.hooksPath, não o framework pre-commit"** (o script do hook está escrito lá, já validado —
      **usar, não reinventar**), § **"O hook é bypassável"**, § **"Prova histórica"** (a tabela dos
      5 co-changes) e § **"Security Domain"** (a regra do `sh`: **sem `eval`**)
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` § **"Pitfall 6 — O hook que não
      protege ninguém"**
    - `data/ticker_map.json` (o hook valida a menção a ticker — ver nota abaixo)
  </read_first>

  <action>
Criar `.githooks/commit-msg` (`chmod +x`), em `sh` puro:

1. `staged=$(git diff --cached --name-only)`.
2. Se **não** tocar `config.yaml` → `exit 0`. Se **não** tocar `tests/fixtures/` nem `tests/test_` →
   `exit 0`. (Só o **co-change** interessa.)
3. Tocou os dois → exige o trailer na mensagem (`$1` = arquivo da mensagem):
   `Knob-Change-Justification: <razao ECONOMICA, sem citar ticker>`.
   Sem o trailer → **BLOQUEIA** com uma mensagem que: nomeia o BLIND-05, diz que é a assinatura de
   *"calibrei o knob até o golden passar"* (post-mortem do v2.3), manda **separar em dois commits**,
   e mostra o formato do trailer para o caso legítimo (primitiva nova + knob + teste nascendo juntos).
4. Com o trailer → aplicar a **regra escrita do ROADMAP, executável**: *"uma justificativa legítima
   de knob nunca menciona um ticker"*. Se a justificativa contiver um ticker → **BLOQUEIA**, citando
   `config.yaml:238` ("Move ITUB4 ~R$2") como o contra-exemplo canônico.
   ⚠️ **Não usar regex nua `[A-Z]{4}[0-9]{1,2}` sozinha** — ela casa `MACD12` (falso positivo real).
   O hook é `sh`: usar a regex como **primeiro filtro** e validar o candidato contra
   `data/ticker_map.json` (`grep -qF "\"$cand\""`) antes de bloquear.

🔒 **Segurança do shell:** o hook processa a mensagem de commit com `printf '%s' "$msg" | grep`.
**Nunca `eval`, nunca expansão de comando sobre a mensagem.**

5. **Instalar:** `git config core.hooksPath .githooks`.

6. **Provar E2E que bloqueia** (sem isto, é um hook de enfeite). Num branch descartável ou com
   `git stash` ao final — **sem sujar a árvore**:
   - (a) `touch`-ar uma alteração trivial em `config.yaml` (um espaço em comentário) + qualquer
     `tests/test_*.py`, `git add` os dois, `git commit -m "teste"` → **BLOQUEADO** (exit ≠ 0).
   - (b) mesmo stage, com `Knob-Change-Justification: ITUB4 estava baixo` → **BLOQUEADO** pela regra
     do ticker.
   - (c) mesmo stage, com `Knob-Change-Justification: primitiva nova de normalizacao nasce com knob e teste`
     → **PASSA**.
   - **Reverter tudo** (`git reset --hard` do commit de teste / `git checkout -- config.yaml`) — a
     árvore precisa terminar limpa e o `config.yaml` **bit-idêntico** ao original.
  </action>

  <verify>
    <automated>test -x .githooks/commit-msg && git config --get core.hooksPath && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `test -x .githooks/commit-msg` → sucesso (executável)
    - `git config --get core.hooksPath` → **`.githooks`**
    - `git ls-files .githooks/commit-msg` → **1 linha** (o hook está **versionado**, não em `.git/hooks`)
    - `grep -c "Knob-Change-Justification" .githooks/commit-msg` → **≥ 2**
    - `grep -c "eval" .githooks/commit-msg` → **0**
    - `grep -c "ticker_map.json" .githooks/commit-msg` → **≥ 1** (a validação de ticker não é regex nua)
    - As **três** provas E2E (a/b/c) foram executadas e suas saídas estão registradas no SUMMARY:
      bloqueio sem trailer, bloqueio com ticker na justificativa, passagem com justificativa econômica
    - `git status --porcelain config.yaml` → **vazio** ao fim da task (o `config.yaml` voltou
      bit-idêntico — **nenhum número foi movido**)
  </acceptance_criteria>

  <done>O hook versionado está instalado e comprovadamente bloqueia os dois casos ofensivos e libera o legítimo.</done>
</task>

<task type="auto">
  <name>Task 2: Backstop — o hook nao pode estar silenciosamente desinstalado nem ser burlado por --no-verify</name>
  <files>tests/test_blindagem_hook.py, tests/classificacao.yaml</files>

  <read_first>
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` §§ **"O hook é bypassável —
      precisa de backstop"** e **"Runtime State Inventory"** (a pergunta canônica: *depois dos 6
      requisitos no repo, que estado runtime ainda tem a proteção desligada?* → **exatamente um:
      `core.hooksPath`**)
    - `.githooks/commit-msg` (criado na Task 1)
  </read_first>

  <action>
Criar `tests/test_blindagem_hook.py`:

1. `test_hook_do_blind05_esta_instalado` — marcador `contrato`:
   - `subprocess.run(["git","config","--get","core.hooksPath"], capture_output=True, text=True)`
   - `assert out.strip() == ".githooks"`, com mensagem acionável: *"o hook do BLIND-05 está INATIVO.
     Rode: `git config core.hooksPath .githooks`"*.
   - Docstring: `core.hooksPath` é **estado LOCAL** (`.git/config`), **não versionado** → um `git
     clone` novo **não tem a proteção**. Este teste é a única coisa que transforma isso em erro
     ruidoso em vez de proteção fantasma (Pitfall 6).

2. `test_hook_do_blind05_e_versionado_e_executavel` — marcador `contrato`:
   - `git ls-files .githooks/commit-msg` devolve o arquivo (versionado)
   - `os.access(".githooks/commit-msg", os.X_OK)` → True

3. `test_historico_do_v24_sem_co_change_knob_e_golden` — marcador `contrato` — o **backstop de
   `--no-verify`**:
   - varre os commits **do marco v2.4** (de uma `BASE_V24` — constante nomeada no topo, o SHA do
     commit do ROADMAP v2.4 — até `HEAD`) com `git log --format=%H` + `git show --name-only`
   - um commit que toque `config.yaml` **e** (`tests/fixtures/` ou `tests/test_`) só é aceito se a
     mensagem tiver o trailer `Knob-Change-Justification:` **e** a justificativa **não** mencionar
     ticker (mesma regra do hook — reusar `helpers_blindagem.tickers_conhecidos()`)
   - **Só o marco v2.4**, não os 676 commits: os 5 co-changes históricos são **passado auditado** (a
     pesquisa já os classificou), e falhar sobre eles tornaria o teste impossível de ficar verde —
     que é exatamente o Pitfall 5 pela porta dos fundos.
   - Se o repo estiver em estado sem histórico (shallow clone / CI ausente): `pytest.skip` com razão
     explícita — nunca um `assert True` silencioso.

Registrar os 3 testes novos em `tests/classificacao.yaml`.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest -q -rs tests/test_blindagem_hook.py</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_blindagem_hook.py` → **3 passed**, 0 failed
    - **Prova de que o backstop morde:** `git config --unset core.hooksPath` →
      `.venv/bin/python -m pytest -q -k hook_do_blind05_esta_instalado` → **1 failed** com a mensagem
      mandando rodar `git config core.hooksPath .githooks` → **reinstalar** (`git config core.hooksPath .githooks`)
      → volta a **1 passed**. Registrar as duas saídas no SUMMARY.
    - `grep -c "BASE_V24" tests/test_blindagem_hook.py` → **≥ 2** (constante nomeada + uso)
    - `.venv/bin/python -m pytest -q` (suíte inteira) → **0 failed**, **2 xfailed**, **1 skipped**,
      0 xpassed, 0 errors
    - `git diff --stat -- src/ config.yaml` → **vazio**
  </acceptance_criteria>

  <done>Os 3 testes de backstop passam; desinstalar o `core.hooksPath` comprovadamente deixa a suíte vermelha.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mensagem de commit → shell do hook | Único ponto do marco em que **texto livre** entra num interpretador (`sh`). |
| `--no-verify` → hook ignorado | O hook é **bypassável por design** do git; o teste-backstop é a rede. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Elevation | `.githooks/commit-msg` (`sh` sobre a mensagem) | mitigate | `printf '%s' "$msg" \| grep` — **sem `eval`**, sem expansão de comando sobre a mensagem. Verificado por `grep -c eval → 0` |
| T-07-08 | Repudiation | bypass silencioso via `--no-verify` | mitigate | `test_historico_do_v24_sem_co_change_knob_e_golden` varre o histórico do marco; o trailer `Knob-Change-Justification:` fica no `git log` **para sempre** |
| T-07-09 | Tampering | `core.hooksPath` desinstalado (estado local) | mitigate | `test_hook_do_blind05_esta_instalado` → suíte vermelha se a proteção sumir |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q -rs` → 0 failed, 2 xfailed, 1 skipped, 0 xpassed
- `git config --get core.hooksPath` → `.githooks`
- As 3 provas E2E do hook (bloqueio sem trailer / bloqueio com ticker / passagem com justificativa)
  e a prova do backstop (unset → vermelho → reinstalar → verde) estão registradas
- `git diff --stat -- src/ config.yaml` → vazio
</verification>

<success_criteria>
- BLIND-05 satisfeito: hook **versionado** (`.githooks/commit-msg`), **instalado** (`core.hooksPath`),
  **provado E2E**, com escapatória **auditável** (trailer no `git log`) e **backstop em teste** contra
  o `--no-verify`.
- Nenhum número movido; `config.yaml` bit-idêntico.
</success_criteria>

<output>
Criar `.planning/phases/07-blindagem-processual-blind/07-04-SUMMARY.md`.

Registrar: as 3 saídas E2E do hook; a prova do backstop (unset → vermelho → reinstalar → verde); e o
`BASE_V24` (SHA) escolhido como início da varredura. Nota durável: **`core.hooksPath` é estado local
por clone** — todo clone novo precisa de `git config core.hooksPath .githooks`, e o teste é o que
avisa.
</output>
</content>
