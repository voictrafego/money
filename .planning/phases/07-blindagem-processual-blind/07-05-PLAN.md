---
phase: 07-blindagem-processual-blind
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - calibracao.lock.yaml
  - config.yaml
  - tests/test_blindagem_orcamento.py
  - tests/helpers_blindagem.py
  - tests/classificacao.yaml
  - CLAUDE.md
autonomous: true
requirements: [BLIND-06]

must_haves:
  truths:
    - "O sistema tem EXATAMENTE 3 graus de liberdade declarados (ERP, n_fade, PIB_real) — e um teste falha se aparecer um 4o."
    - "Mudar QUALQUER knob da superficie de valuation exige mexer no calibracao.lock.yaml no MESMO diff — a mudanca de knob deixa de ser invisivel."
    - "veredito.margem_seguranca esta congelada e etiquetada user_control (nao grau_de_liberdade) — o 4o grau escondido esta fechado ate o ENG-06."
    - "Nenhuma justificativa de knob no config.yaml menciona um ticker."
    - "A suite NOVA consegue reprovar: dobrar o ERP move o V em mais de 5% e o canario pega."
    - "'Suite verde' passa a significar: 0 failed, goldens em quarentena, 2 xfailed (as duas doencas) — e o CLAUDE.md diz isso."
  artifacts:
    - path: "calibracao.lock.yaml"
      provides: "Orcamento de knobs: escopo declarado, 3 graus de liberdade, user_control, 27 congelados"
      contains: "graus_de_liberdade"
    - path: "tests/test_blindagem_orcamento.py"
      provides: "BLIND-06: orcamento==3, knobs batem com o lock, nenhuma justificativa cita ticker, canario"
      contains: "orcamento"
      min_lines: 80
  key_links:
    - from: "tests/test_blindagem_orcamento.py"
      to: "calibracao.lock.yaml + config.yaml"
      via: "compara folha a folha; qualquer divergencia deixa a suite vermelha"
      pattern: "calibracao.lock"
    - from: "tests/test_blindagem_orcamento.py"
      to: "report.analisar_acao com erp_local dobrado"
      via: "canario: a suite reage a um knob grosseiramente errado"
      pattern: "erp_local"
---

<objective>
Travar o **orçamento de knobs** (BLIND-06): **exatamente 3 graus de liberdade** (`ERP`, `n_fade`,
`PIB_real`) sobre uma superfície de valuation **declarada**; tornar toda mudança de knob **visível no
diff**; executar a regra escrita do ROADMAP (*"uma justificativa legítima de knob nunca menciona um
ticker"*); e fechar a fase com o **canário** — a prova de que a suíte nova **consegue reprovar**.

Purpose: sem orçamento declarado, "3 graus de liberdade" não é testável. O v2.3 gastou **~8 graus de
liberdade sobre 4 observações** e ninguém contou. E uma suíte com 448 testes que não consegue ficar
vermelha é a mesma suíte decorativa numa fantasia nova.
Output: `calibracao.lock.yaml`, `tests/test_blindagem_orcamento.py`, comentários com ticker removidos
do `config.yaml`, `CLAUDE.md` atualizado.

**ESTE PLANO NÃO MOVE NENHUM NÚMERO.** O `config.yaml` é tocado **apenas em comentários**. Se algum
**valor** do `config.yaml` mudar, saiu do escopo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md
@.planning/phases/07-blindagem-processual-blind/07-01-SUMMARY.md
@.planning/phases/07-blindagem-processual-blind/07-02-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@config.yaml
</context>

<interfaces>
<!-- MEDIDO NO REPO HOJE. A pesquisa esbocou o lock com valores ASPIRACIONAIS (os alvos das Fases
     11-13). Se o executor copiar aquele esboco, o teste `knobs_batem_com_o_lock` fica VERMELHO na
     primeira escrita — e a "correcao" obvia seria editar o config.yaml, ou seja, MOVER UM NUMERO,
     violando a regra dura desta fase. E a armadilha mais perigosa deste plano. -->

**O lock registra o estado de HOJE, nao o alvo:**

| Grau de liberdade | Caminho no `config.yaml` hoje | Valor hoje | Alvo (fase futura) |
|---|---|---|---|
| `ERP` | `capm.erp_local` | **0.06** | 0.045 (KE-02, Fase 12) |
| `n_fade` | `motores.rim.n_fade` | **10** | — |
| `PIB_real` | **`ddm.pib_real` NAO EXISTE hoje.** O valor esta digitado em `ddm.g_estavel` | **0.025** (2,5% de PIB **real** — e a Doenca 1) | `g_cap` derivado = 7,28% (GROW-01, Fase 11) |

**Superficie de valuation (o `escopo` do orcamento) — 30 folhas, contadas:**
`motores` (**11** folhas — **nao ~20**; a regra dura C do ROADMAP esta mal-calibrada na origem. A meta
"<=5" continua valida e continua sendo um corte real (11 -> 5), mas e problema da **Fase 13/ENG-10**,
nao desta) + `capm` (12) + `ddm` (5) + `normalizacao` (2) = **30**.
Os outros 80 knobs (`screening` 33, `indicadores` 16, `score` 11, `padroes` 6, `selo` 3, `arquetipo` 5,
`veredito` 4, `universo` 2) **nao afetam `V`** -> fora do escopo. Total do arquivo: **110 folhas**.

**D-04 (decisao travada):** `veredito.margem_seguranca` (**0.15**) **multiplica o `V`** e e, ate o
ENG-06, um **4o grau de liberdade escondido** (e a Armadilha 4). O lock **congela-a explicitamente**,
etiquetada **`user_control`** — **NAO** como `grau_de_liberdade`.

**Comentarios com ticker no `config.yaml` (10 linhas medidas contra `data/ticker_map.json`):**
linhas **233, 238, 240, 242, 255-257, 259** — todas nos blocos de **valuation** (8 das 10 em
`motores.rim`, a superficie exata do overfit v2.3) — **mais** 218-219 (bloco `arquetipo`, ranges
ilustrativos de beta). **O escopo do teste E da limpeza sao os 4 blocos de valuation** -> as linhas
218-219 ficam **de fora** (evita limpeza cosmetica sem valor).
O ROADMAP e o `PITFALLS.md` citam `config.yaml:237` e `:258` — **sao off-by-one**: as linhas reais sao
**238** e **259**.

**Do plano 07-01 (reusar, nao reimplementar):**
```
helpers_blindagem.tickers_conhecidos() -> set[str]     # data/ticker_map.json, 105 tickers
helpers_blindagem.cfg_e_empresas_do_snapshot()         # (07-02) config de producao + snapshot
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: calibracao.lock.yaml — escopo declarado, 3 graus de liberdade, user_control, 27 congelados</name>
  <files>calibracao.lock.yaml</files>

  <read_first>
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` § **"BLIND-06 — o orçamento de
      knobs"** inteira (a correção das 11 chaves, a tabela do escopo, o esboço do lock e dos 3 testes)
      e § **"Open Questions → Q4"** (a `margem_seguranca` como 4º grau escondido)
    - `.planning/REQUIREMENTS.md` → **BLIND-06**, **ENG-06** (a MS vira controle do usuário),
      **ENG-10** (o `motores:` vai a ≤5 chaves na Fase 13)
    - `config.yaml` — os blocos `motores`, `capm`, `ddm`, `normalizacao`, `veredito`
    - a tabela `<interfaces>` **deste plano** — os valores de **HOJE**, não os do esboço da pesquisa
  </read_first>

  <action>
Criar `calibracao.lock.yaml` **na raiz do repo**. Fora de `tests/` **de propósito**: o hook do
BLIND-05 bloqueia `config.yaml` + `tests/*` no mesmo commit, e `config.yaml` + lock é o caminho
**sancionado** (a mudança de knob fica visível e revisável no diff, não bloqueada).

Estrutura:

- `escopo:` — os 4 blocos que afetam `V`: `motores`, `capm`, `ddm`, `normalizacao`. Com comentário
  explicando **por que** os outros 8 blocos ficam fora (não afetam `V`).
- `graus_de_liberdade:` — **exatamente 3**, cada um com `caminho`, `valor` (o de **HOJE**) e `fonte`:
  - `ERP` → `capm.erp_local` = **0.06**. Comentário: hoje existem **dois ERPs simultâneos** no
    sistema (`capm.erp_local` **e** `motores.rim.erp_banco`) — o segundo está em `congelados`, e o
    KE-02 (Fase 12) unifica.
  - `n_fade` → `motores.rim.n_fade` = **10**. Comentário: é **este** knob que impõe o piso estrutural
    de **−4,68%** do BLIND-02(b) — a janela explícita não é invariante à inflação; a perpetuidade é.
  - `PIB_real` → `ddm.g_estavel` = **0.025**. Comentário **obrigatório**: *"hoje o PIB real está
    digitado direto em `ddm.g_estavel` (2,5% REAL contra um `Ke` NOMINAL — é a Doença 1). O GROW-01
    (Fase 11) troca isto por `g_cap` derivado = (1+pi_ciclo)x(1+PIB_real)-1 = 7,28%. Quando isso
    acontecer, ATUALIZE O CAMINHO AQUI — isso não é afrouxar o orçamento, é o orçamento sendo
    mantido."*
- `user_control:` — **D-04**: `veredito.margem_seguranca: 0.15`. Comentário: *"multiplica o `V` → de
  fato seria um 4º grau de liberdade. NÃO é grau de liberdade: o ENG-06 (Fase 13) a transforma em
  controle explícito do usuário (o livro: 'se 5%, 10% ou qualquer outro valor, é você quem decide').
  Até lá fica CONGELADA aqui. NUNCA calibrar contra dispersão, preço ou taxa de compra — é assim que
  a Armadilha 4 morre por construção."*
- `congelados:` — **todas** as demais folhas do `escopo` (30 − 3 = **27**), cada uma com o **valor de
  hoje**. Onde for conhecida, anotar a fase que a deleta/deriva. Mínimo obrigatório:
  - `motores.rim.ke_teto` → *"KE-04, Fase 12: REMOVIDO. Hoje satura e absorve a perna do `rf` do
    BLIND-02(b). A justificativa 'Blume' do `config.yaml:235` é aritmeticamente FALSA (Blume daria
    15,9%)."*
  - `motores.rim.ke_piso` → *"KE-04, Fase 12: REMOVIDO."*
  - `motores.rim.erp_banco` → *"KE-02, Fase 12: o 2º ERP do sistema morre; sobra um só."*
  - `normalizacao.anos_media` → *"PRIM-01, Fase 10. **NÃO é grau de liberdade** — é a trava do
    BLIND-03: mudar para 1 faria aquele teste passar sem consertar `normalizacao.py`."*
  - `motores.rim.excesso_sustentavel` e `motores.rim.ke_g_spread_min` → *"GROW-05, Fase 11: hoje
    decorativos, viram LOAD-BEARING quando o spread `Ke-g` cair de 10,5pp para ~5,5pp."*
- Cabeçalho com a regra escrita: *"uma justificativa legítima de knob nunca menciona um ticker"*.

O valor de **cada** folha vem de `yaml.safe_load("config.yaml")` lido do repo — **nunca** digitado de
memória nem copiado do esboço da pesquisa.
  </action>

  <verify>
    <automated>.venv/bin/python -c "import yaml; cfg=yaml.safe_load(open('config.yaml',encoding='utf-8')); lock=yaml.safe_load(open('calibracao.lock.yaml',encoding='utf-8')); g=lambda d,p:[d:=d[k] for k in p.split('.')][-1]; print('graus:',len(lock['graus_de_liberdade'])); print('batem:', all(g(cfg,s['caminho'])==s['valor'] for s in lock['graus_de_liberdade'].values())); print('congelados:',len(lock['congelados'])); print('user_control:',lock['user_control'])"</automated>
  </verify>

  <acceptance_criteria>
    - O comando acima imprime `graus: 3`, `batem: True`, `congelados: 27`, e um `user_control`
      contendo `veredito.margem_seguranca` com valor `0.15`
    - `grep -c "ddm.g_estavel" calibracao.lock.yaml` → **≥ 1** (o caminho do `PIB_real` é o de HOJE;
      **`ddm.pib_real` não existe** — se aparecer no lock, está errado)
    - `grep -c "pib_real" calibracao.lock.yaml` → **0** ocorrências como **caminho** de config
      (a palavra pode aparecer no nome do grau `PIB_real` e nos comentários, mas nunca como
      `caminho: ddm.pib_real`)
    - `grep -c "0.045" calibracao.lock.yaml` → **0** (0.045 é o **alvo** do KE-02, não o valor de hoje;
      o `capm.erp_local` de hoje é **0.06**)
    - `grep -c "user_control" calibracao.lock.yaml` → **≥ 1** e `grep -c "grau_de_liberdade\|graus_de_liberdade" calibracao.lock.yaml` → **≥ 1**
    - 3 + 27 = **30** = a contagem de folhas dos 4 blocos do `escopo` (nenhuma folha da superfície de
      valuation ficou de fora do lock)
    - `git diff config.yaml` → **vazio** nesta task
  </acceptance_criteria>

  <done>`calibracao.lock.yaml` existe, bate folha a folha com o `config.yaml` de hoje, declara 3 graus + 27 congelados + a MS como `user_control`.</done>
</task>

<task type="auto">
  <name>Task 2: Os 3 testes do BLIND-06 + limpeza dos comentarios com ticker no config.yaml</name>
  <files>tests/test_blindagem_orcamento.py, tests/helpers_blindagem.py, config.yaml, tests/classificacao.yaml</files>

  <read_first>
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` § **"BLIND-06 → `calibracao.lock.yaml`
      + o teste"** (os 3 testes esboçados) e § **"O teste do ticker falha hoje — e é bom que falhe"**
      (a tabela das 10 linhas ofensoras)
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` § **"Assumptions Log → A4"**
      (limpar comentário não muda valor — mas **confirmar** que nenhum é lido por código; em YAML,
      não são)
    - `config.yaml` linhas **215-265** (os comentários ofensores e o bloco `motores`)
    - `calibracao.lock.yaml` (Task 1)
  </read_first>

  <action>
Criar `tests/test_blindagem_orcamento.py` com 3 testes (todos marcador `contrato`):

1. `test_orcamento_de_knobs_e_exatamente_3` — a **partição** é completa e o orçamento não estourou:
   - `folhas_do_escopo(cfg, lock["escopo"])` (helper novo em `helpers_blindagem.py`: caminha os 4
     blocos e devolve o conjunto de caminhos-folha, tratando **lista como uma folha só**)
   - `declaradas = {caminhos dos graus_de_liberdade} | {caminhos dos congelados}`
   - `assert folhas_do_escopo == declaradas` — **nenhuma folha órfã** (knob novo não declarado) e
     **nenhuma declaração órfã** (knob deletado do config e esquecido no lock). Mensagem de erro
     lista as diferenças dos dois lados.
   - `assert len(lock["graus_de_liberdade"]) == 3`, com mensagem: *"orçamento estourado. Um 4º grau
     de liberdade é como o v2.3 gastou ~8 sobre 4 observações. Se um knob PRECISA ser calibrável,
     outro precisa morrer."*
   - `assert "veredito.margem_seguranca" in lock["user_control"]` — **D-04**: a MS está congelada e
     etiquetada, não solta.

2. `test_knobs_batem_com_o_lock` — **o dente do requisito**: para **cada** folha do escopo (os 3 graus
   **e** os 27 congelados), o valor no `config.yaml` é **idêntico** ao registrado no lock.
   - Mensagem de erro: *"o knob `<caminho>` mudou de `<lock>` para `<config>` sem atualizar o
     `calibracao.lock.yaml`. Toda mudança de knob tem que aparecer no MESMO diff — é o que a torna
     revisável. Se a mudança é legítima, atualize o lock no mesmo commit."*
   - Docstring: este teste é o que transforma "mexer num knob" de invisível em **evento revisável**.

3. `test_nenhuma_justificativa_de_knob_menciona_ticker` — a regra escrita do ROADMAP, **executável**:
   - varre os **comentários** do `config.yaml` **dentro dos 4 blocos do `escopo`** (parse por linha:
     as chaves de topo estão na coluna 0 → o bloco vai do `motores:` até a próxima chave de coluna 0)
   - candidato a ticker por regex **validado contra `helpers_blindagem.tickers_conhecidos()`**
     (⚠️ regex nua casa **`MACD12`** — falso positivo real em `config.yaml:134`)
   - `assert not ofensores`, listando linha + trecho.
   - **Este teste FALHA HOJE (10 linhas).** É bom que falhe — e a correção **não** é afrouxar o teste:

**Limpar os comentários ofensores do `config.yaml`** (linhas ~233, 238, 240, 242, 255-257, 259):
   - reescrever cada comentário mantendo a **justificativa econômica** e **removendo a menção ao
     ticker**. Exemplos:
     - `# Move ITUB4 ~R$2` → `# desloca o valor terminal em ~R$2 num banco grande tipico`
     - `# NAO mexer nos knobs acima: mudariam o ITUB4` → **deletar a linha inteira** (é a instrução
       escrita de cometer a Armadilha 3 — o repo literalmente diz ao próximo executor para calibrar
       contra um ticker)
     - `# E a alavanca que destrava o ITUB4 do 'evitar'` → `# alavanca do excesso de ROE sustentavel`
   - ⚠️ **SOMENTE comentários.** Nenhum `valor` do `config.yaml` pode mudar. Prova obrigatória no
     acceptance criteria abaixo.
   - As linhas **218-219** (bloco `arquetipo`) ficam **fora** do escopo → não tocar.

Registrar os 3 testes novos em `tests/classificacao.yaml`.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_blindagem_orcamento.py</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_blindagem_orcamento.py` → **3 passed**, 0 failed
    - **PROVA DE QUE NENHUM VALOR MUDOU** (o critério mais importante desta task):
      `.venv/bin/python -c "import yaml,subprocess; novo=yaml.safe_load(open('config.yaml',encoding='utf-8')); velho=yaml.safe_load(subprocess.run(['git','show','HEAD:config.yaml'],capture_output=True,text=True).stdout); print('config semanticamente identico:', novo==velho)"`
      → **`True`**. (O YAML carregado ignora comentários → se os dicts forem iguais, só comentários
      mudaram.)
    - `git diff --numstat config.yaml` mostra alterações **apenas** em linhas que começam com `#` ou
      contêm comentário inline — verificar o diff à mão
    - **Prova de que o lock morde** (o canário do BLIND-06): alterar temporariamente
      `motores.rim.n_fade` de 10 para 9 no `config.yaml` → `.venv/bin/python -m pytest -q -k knobs_batem`
      → **1 failed** com a mensagem citando o caminho → **reverter** (`git checkout -- config.yaml`,
      depois reaplicar a limpeza de comentários) → volta a **3 passed**. Registrar as saídas no SUMMARY.
    - `.venv/bin/python -m pytest -q -k justificativa` → **1 passed** (as 10 linhas ofensoras sumiram)
    - `grep -c "ITUB4\|BBAS3\|BBDC4\|BBSE3" config.yaml` → **0** ocorrências nos blocos de valuation
      (as linhas 218-219 do bloco `arquetipo` podem manter WEGE3/VALE3 etc. — estão fora do escopo)
  </acceptance_criteria>

  <done>Os 3 testes do BLIND-06 passam; o `config.yaml` está semanticamente idêntico (só comentários mudaram); mexer num knob deixa a suíte vermelha.</done>
</task>

<task type="auto">
  <name>Task 3: O canario (a suite CONSEGUE reprovar) + portao da fase + CLAUDE.md</name>
  <files>tests/test_blindagem_orcamento.py, tests/classificacao.yaml, CLAUDE.md</files>

  <read_first>
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` §§ **"Code Examples → O canário
      (PITFALLS P5.4)"**, **"Estado esperado da suíte ao fim da Fase 7"** e **"Sampling Rate → Portão
      da fase"**
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` § **"Nota sobre o conflito
      CLAUDE.md × ROADMAP"** (por que o `CLAUDE.md` precisa ser atualizado ou a próxima sessão lê a
      regra velha e "conserta" o teste)
    - `./CLAUDE.md` — a linha de Constraints: *"testes golden existentes em `tests/` devem continuar
      passando após as correções"*
  </read_first>

  <action>
1. **O canário** — acrescentar a `tests/test_blindagem_orcamento.py`:
   `test_a_suite_reage_a_um_knob_grosseiramente_errado` (marcador `contrato`):
   - `empresas, cfg = helpers_blindagem.cfg_e_empresas_do_snapshot()`
   - `sabotado = deepcopy(cfg)`; `sabotado["capm"]["erp_local"] *= 2` — um erro **grosseiro** de método
   - `assert abs(V_sabotado / V_base - 1) > 0.05` sobre o ITUB4
   - Docstring **obrigatório**: *"Sem este teste, '448 verdes' pode significar '448 deselecionados'.
     Este é o canário do PITFALLS P5.4: ele prova que a blindagem **constrange** algo — que a suíte
     nova não é MAIS decorativa que a velha. Se ele um dia falhar, é porque a engine parou de reagir
     ao custo de capital, e nenhum outro teste vai te contar isso."*
   - Registrar o teste em `tests/classificacao.yaml`.

2. **`CLAUDE.md`** — atualizar a linha de **Compatibility** em Constraints. A regra velha
   (*"testes golden existentes em `tests/` devem continuar passando após as correções"*) foi escrita
   no v1.x, quando os goldens codificavam o método **pretendido**. O v2.4 provou que eles codificam um
   método **errado** (o golden `ITUB4 32,88` existe para cancelar um haircut de −9,1% — **dois erros
   se anulando**). Substituir por, em substância:
   - **"Suíte verde" (a partir do v2.4)** = `pytest` → **0 failed**, com os `golden_nivel` em
     **quarentena** (deselecionados) e **2 xfailed** (BLIND-02b e BLIND-03 — as duas doenças escritas
     como código). `pytest -m ""` roda tudo.
   - **Golden de nível quebrou? DELETE, não atualize.** Atualizar mantém vivo o reflexo que produziu o
     overfit do v2.3. A classificação vive em `tests/classificacao.yaml`; o orçamento de knobs em
     `calibracao.lock.yaml` (**3 graus de liberdade**: `ERP`, `n_fade`, `PIB_real`).
   - **Nunca** afrouxar tolerância, marcar `xfail` casual ou deletar assert para a suíte ficar verde.
   - Instalar o hook: `git config core.hooksPath .githooks` (estado **local por clone**).
   Manter o resto do `CLAUDE.md` intacto (edição cirúrgica, não reescrita).

3. **Portão da fase** — rodar e registrar as 4 verificações finais (ver acceptance criteria).
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest -q -rxXs 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q -k suite_reage` → **1 passed** (o canário confirma que a engine
      reage ao ERP dobrado em > 5%)
    - **PORTÃO DA FASE (as 4 verificações, todas registradas no SUMMARY):**
      1. `.venv/bin/python -m pytest -q -rxXs` → **0 failed, 0 errors, 0 xpassed**, **exatamente
         2 xfailed** (BLIND-02b + BLIND-03), **1 skipped** (jackknife, aguarda a Fase 14), e
         `N passed, Q deselected` com `N + Q = 448 + <testes novos da fase>`
      2. `.venv/bin/python -m pytest -q -m golden_nivel 2>&1 | tail -1` → os quarentenados **rodam sob
         demanda** (podem passar hoje — o ponto é que **rodam**, e que não bloqueiam o default)
      3. `.venv/bin/python -m pytest -q -m "" 2>&1 | tail -1` → **tudo** é coletado (nada foi perdido,
         só deselecionado)
      4. `git config --get core.hooksPath` → **`.githooks`**
    - `grep -c "quarentena\|golden_nivel" CLAUDE.md` → **≥ 1** e `grep -c "DELETE\|DELETAR" CLAUDE.md` → **≥ 1**
    - `grep -c "devem continuar passando" CLAUDE.md` → **0** (a regra velha, que faria a próxima sessão
      "consertar" o teste, saiu)
    - **PROVA FINAL — nenhum número foi movido na fase inteira:**
      `.venv/bin/python -c "import yaml,subprocess; base=subprocess.run(['git','show','<SHA_ANTES_DA_FASE_7>:config.yaml'],capture_output=True,text=True).stdout; print('config semanticamente identico ao inicio da fase:', yaml.safe_load(open('config.yaml',encoding='utf-8'))==yaml.safe_load(base))"`
      → **`True`**
    - `git diff --stat <SHA_ANTES_DA_FASE_7>..HEAD -- src/ tests/test_motores.py tests/test_backtest_bancos.py tests/fixtures/` → **vazio**
      (zero edições em produção, nos testes existentes e nas fixtures — o `V` de nenhum ticker mudou)
  </acceptance_criteria>

  <done>O canário passa; o portão da fase (4 verificações) está registrado; o `CLAUDE.md` define "suíte verde" pela regra nova.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config.yaml` / `calibracao.lock.yaml` → testes | Arquivos versionados e revisados (trusted). Sem rede, sem input externo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | knob alterado sem rastro | mitigate | `test_knobs_batem_com_o_lock` — divergência entre `config.yaml` e o lock deixa a suíte **vermelha**; a mudança tem que aparecer no mesmo diff |
| T-07-11 | Tampering | leitura de `config.yaml` / lock | mitigate | `yaml.safe_load`, nunca `yaml.load` |
| T-07-12 | Repudiation | limpeza de comentários mudando um valor por acidente | mitigate | Acceptance criteria compara os **dicts carregados** (antes x depois) — igualdade semântica obrigatória |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q -rxXs` → 0 failed, 0 errors, 0 xpassed, **2 xfailed**, **1 skipped**
- `.venv/bin/python -m pytest -q -m golden_nivel` → os quarentenados rodam sob demanda
- `git config --get core.hooksPath` → `.githooks`
- `config.yaml` **semanticamente idêntico** ao início da fase (só comentários mudaram)
- `git diff --stat -- src/ tests/test_motores.py tests/test_backtest_bancos.py tests/fixtures/` → vazio
</verification>

<success_criteria>
- BLIND-06 satisfeito: orçamento de **exatamente 3 graus de liberdade** travado por teste, sobre um
  **escopo declarado** (30 folhas), com a `margem_seguranca` congelada como `user_control` (D-04) e
  **zero** justificativa de knob mencionando ticker.
- O **canário** prova que a suíte nova consegue reprovar (não é decorativa numa fantasia nova).
- O `CLAUDE.md` define "suíte verde" pela regra nova — a próxima sessão não vai "consertar" o teste.
- **Nenhum número movido na fase inteira.**
</success_criteria>

<output>
Criar `.planning/phases/07-blindagem-processual-blind/07-05-SUMMARY.md`.

Registrar:
- as 4 verificações do **portão da fase** (com as saídas)
- a saída do **canário do lock** (n_fade 10→9 → vermelho → revertido → verde)
- a **prova de igualdade semântica** do `config.yaml` antes/depois da fase
- **Handoffs duráveis para o marco:**
  1. **Golden master dos 104 tickers** (PITFALLS P5.2 / RESEARCH Q3) — **fora do escopo da Fase 7**
     (não é requisito BLIND, e `out/` é gitignored → não existe baseline hoje). É **pré-requisito das
     Fases 8/9**: sem baseline, "os asserts viram verde ticker a ticker" não é mensurável.
  2. **BLIND-02(b) vira verde na Fase 12, não na 11** (o `ke_teto` satura até lá). A regra dura A
     (não fundir 11 e 12) **continua válida** — ela é sobre a ordem do conserto, não sobre onde um
     teste fica verde.
  3. **`LIMIAR_JACKKNIFE_PP` está `[ASSUMIDO]`** — fixar na **Fase 14 (VAL-02)** com a distribuição
     real da cesta estratificada.
  4. **`motores:` tem 11 chaves, não ~20** — a regra dura C do ROADMAP está mal-calibrada na origem.
     A meta ≤5 (ENG-10, Fase 13) continua sendo um corte real (11 → 5), mas a **contagem de partida é
     11**.
  5. **`core.hooksPath` é estado local por clone** — todo clone novo precisa de
     `git config core.hooksPath .githooks`; o teste do 07-04 é o que avisa.
</output>
</content>
