---
phase: 07-blindagem-processual-blind
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/helpers_blindagem.py
  - tests/classificacao.yaml
  - scripts/bootstrap_classificacao.py
autonomous: true
requirements: [BLIND-01]

must_haves:
  truths:
    - "Rodar `pytest` deixa de significar 'o método atual está preservado' — os goldens de nível não bloqueiam mais o marco."
    - "Cada um dos 448 testes tem exatamente uma categoria (invariante / golden_nivel / contrato) num arquivo commitado."
    - "Um teste novo sem classificação QUEBRA a coleta — a classificação não pode driftar em silêncio."
    - "Os três goldens de nível do ITUB4 estão marcados para DELEÇÃO na Fase 10, não para atualização."
    - "Os quarentenados continuam rodabilizáveis sob demanda (`pytest -m golden_nivel`)."
  artifacts:
    - path: "tests/classificacao.yaml"
      provides: "Classificação commitada dos 448 testes (BLIND-01)"
      contains: "golden_nivel"
    - path: "tests/conftest.py"
      provides: "Aplicação dinâmica dos marcadores + imposição de completude na coleta"
      contains: "pytest_collection_modifyitems"
    - path: "tests/helpers_blindagem.py"
      provides: "Substrato compartilhado: detector AST de ticker+valor, loaders de YAML"
      contains: "def detectar_ticker_com_valor_cravado"
    - path: "pyproject.toml"
      provides: "Marcadores registrados + quarentena via addopts + xfail_strict"
      contains: "golden_nivel"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/classificacao.yaml"
      via: "yaml.safe_load na coleta, aplica item.add_marker"
      pattern: "classificacao\\.yaml"
    - from: "pyproject.toml"
      to: "marcador golden_nivel"
      via: "addopts = -m 'not golden_nivel'"
      pattern: "not golden_nivel"
---

<objective>
Redefinir o que "suíte verde" significa. Hoje 448 testes ficam verdes sobre um snapshot em que o
ITUB4 tem 10 milhões de ações — a suíte é decorativa. Este plano classifica os 448 testes em
**invariante / golden_nivel / contrato** num arquivo commitado, e põe os `golden_nivel` em
**quarentena** (deselecionados do default, rodabilizáveis sob demanda).

Purpose: sem isto, os consertos das Fases 9–13 quebram goldens de um método errado, e o executor
"conserta" o número em vez do código — foi exatamente assim que nasceu o overfit do v2.3.
Output: `tests/classificacao.yaml` (448 entradas), `tests/conftest.py`, `tests/helpers_blindagem.py`,
`scripts/bootstrap_classificacao.py`, `pyproject.toml` atualizado.

**ESTE PLANO NÃO MOVE NENHUM NÚMERO.** Zero edições nos 448 arquivos de teste existentes. Zero
edições em `src/`. Se o `V` de qualquer ticker mudar, saiu do escopo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

**Runner do projeto:** `.venv/bin/python -m pytest` (o `python`/`python3` do sistema **não** tem as
deps — `python3 -m pytest` colhe 28 erros de import). Todos os comandos abaixo usam `.venv/bin/python`.

**Baseline medido hoje:** `448 tests collected in 1.00s` · `448 passed in ~4,2s`.
</context>

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do repo hoje — não explorar o codebase. -->

`pyproject.toml` — bloco existente (ACRESCENTAR, não substituir):
```toml
[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
```

`data/ticker_map.json` — 105 tickers, versionado. É a **única** fonte válida para decidir se uma
string é um ticker. ⚠️ Regex nua `[A-Z]{4}\d{1,2}` casa `MACD12` (falso positivo real, medido em
`config.yaml:134`) — ver RESEARCH § Pitfall 7.

Fatos do repo (verificados, não re-derivar):
- `tests/` tem **0 classes** `class Test*` → nodeids são sempre `arquivo::funcao` ou `arquivo::funcao[param]`.
- **1 arquivo** usa `parametrize`: `tests/test_arquetipo.py`. 443 `FunctionDef` `test_*` → **448** nodeids.
- `tests/conftest.py` **não existe**.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Substrato compartilhado — detector AST + loaders (helpers_blindagem.py) e o script de bootstrap</name>
  <files>tests/helpers_blindagem.py, scripts/bootstrap_classificacao.py</files>

  <read_first>
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` §§ **"Inventário Real da Suíte"**,
      **"BLIND-04 → 4a. A proibição"** (a regra exata do detector), **"Don't Hand-Roll"**, **"Pitfall 7"**
    - `data/ticker_map.json` (a lista de tickers válidos)
    - `tests/test_motores.py` e `tests/test_backtest_bancos.py` (os dois arquivos onde golden e
      invariante coabitam — é a razão de a quarentena ser por teste, não por arquivo)
  </read_first>

  <action>
Criar `tests/helpers_blindagem.py` — módulo **sem prefixo `test_`** (pytest não coleta; como não há
`__init__.py` em `tests/`, o pytest põe `tests/` no `sys.path` → os testes fazem
`import helpers_blindagem` direto). É o substrato compartilhado dos planos 07-01, 07-03 e 07-05.

Funções exportadas (nomes exatos — os planos seguintes dependem deles):

- `tickers_conhecidos() -> set[str]` — lê `data/ticker_map.json`. **Nunca** regex.
- `detectar_ticker_com_valor_cravado(raiz: pathlib.Path = Path("tests")) -> set[str]` — devolve
  identificadores **em nível de função**, no formato `tests/arquivo.py::nome_da_funcao` (SEM
  `[param]`). Regra exata (RESEARCH § BLIND-04a): `ast.walk` sobre cada `FunctionDef` cujo nome
  começa com `test_`; casa quando o corpo contém **(i)** um literal string que é chave de
  `ticker_map.json` **E** **(ii)** um `ast.Constant` float **não-trivial** (∉ {0, 1, 0.5, 2}) dentro
  de um `ast.Compare` ou `ast.Assert`. Hoje isto acha **47** funções (número de referência da
  pesquisa — recontar, não confiar).
- `importa_caminho_de_valuation(caminho: pathlib.Path) -> bool` — True se o arquivo importa
  qualquer um de: `report`, `motores`, `ddm`, `capm`, `growth`, `normalizacao`, `comparables`,
  `lentes`, `selo`, `multiples`, `screening`, `freio`, `arquetipo`, `backtest`. (Hoje: 271 testes.)
- `carregar_classificacao() -> dict[str, str]` — `yaml.safe_load` de `tests/classificacao.yaml`.
  **`safe_load`, nunca `load`** (o repo já é `safe_load` em todos os call-sites).
- `nodeid_para_funcao(nodeid: str) -> str` — corta o `[param]`: `s.split("[", 1)[0]`. É a ponte entre
  os nodeids do pytest (com params) e os identificadores do detector AST (sem params).
- `quarentenados() -> set[str]` — as chaves de `carregar_classificacao()` com valor `golden_nivel`,
  já passadas por `nodeid_para_funcao`.

Criar `scripts/bootstrap_classificacao.py` — gerador da **primeira versão** do YAML (o executor
audita depois, na Task 3). Ele **não** é a fonte de verdade; o YAML commitado é.

- A lista autoritativa de nodeids vem do **pytest**, não do AST:
  `subprocess` → `.venv/bin/python -m pytest --collect-only -q` (448 linhas de nodeid). Isto garante
  que os `[param]` do `test_arquetipo.py` entrem com o id exato que o pytest usa.
- A **proposta** de categoria vem do AST, casando o nodeid via `nodeid_para_funcao`:
  1. está em `detectar_ticker_com_valor_cravado()` → `golden_nivel`
  2. arquivo importa caminho de valuation **e** a função tem float não-trivial em assert (mas sem
     ticker) → escreve a categoria com um sufixo de comentário **` # REVISAR`**
  3. arquivo ∈ {`test_indicators.py`, `test_glossario.py`, `test_grafico_ui.py`, `test_setup_report.py`}
     → `invariante` para `test_indicators.py` (matemática pura), `contrato` para os outros três
  4. resto → `contrato`
- Preserva/ignora o arquivo existente se já houver um (idempotente: reexecutar não sobrescreve
  decisões humanas — imprime só as entradas **novas** e as **órfãs**).
- ⚠️ A palavra `REVISAR` **não pode aparecer no cabeçalho/prosa** do YAML — ela é o marcador de
  pendência e a Task 3 fecha com `grep -c REVISAR → 0`.
  </action>

  <verify>
    <automated>.venv/bin/python -c "import sys; sys.path.insert(0,'tests'); import helpers_blindagem as h; print('tickers:', len(h.tickers_conhecidos())); print('detector:', len(h.detectar_ticker_com_valor_cravado())); print('MACD12 nao e ticker:', 'MACD12' not in h.tickers_conhecidos())"</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'tests'); import helpers_blindagem as h; print(len(h.tickers_conhecidos()))"` → imprime **105**
    - o mesmo import + `len(h.detectar_ticker_com_valor_cravado())` → imprime um inteiro **≥ 47**
      (a pesquisa mediu 47; se divergir, é porque `tests/` mudou — registrar o número medido no SUMMARY)
    - `'MACD12' not in h.tickers_conhecidos()` → `True` (Pitfall 7 fechado)
    - `.venv/bin/python scripts/bootstrap_classificacao.py --dry-run` imprime **448** nodeids e a
      contagem por bucket, sem escrever arquivo
    - `grep -rn "yaml.load(" tests/helpers_blindagem.py scripts/bootstrap_classificacao.py` → **0 linhas**
    - `git diff --stat src/` → **vazio** (nenhum arquivo de produção tocado)
  </acceptance_criteria>

  <done>`helpers_blindagem.py` importável e com o detector medindo ≥47; `bootstrap_classificacao.py` roda em dry-run listando 448 nodeids.</done>
</task>

<task type="auto">
  <name>Task 2: pyproject (marcadores + quarentena + xfail_strict) e conftest.py (marcadores dinâmicos + completude)</name>
  <files>pyproject.toml, tests/conftest.py</files>

  <read_first>
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` § **"Mecanismo de Quarentena
      (BLIND-01) — decisão recomendada"** (o `pyproject.toml` e o `conftest.py` estão escritos lá,
      já validados E2E em pytest 9.0.3 — **use-os, não reinvente**) e § **"Comportamento verificado"**
    - `pyproject.toml` (o bloco `[tool.pytest.ini_options]` existente — **acrescentar**, não substituir)
    - `tests/helpers_blindagem.py` (criado na Task 1)
  </read_first>

  <action>
**`pyproject.toml`** — acrescentar ao bloco `[tool.pytest.ini_options]` existente (preservando
`pythonpath` e `testpaths`):
- `xfail_strict = true` — XPASS vira FAIL globalmente. É a defesa contra o precedente registrado em
  `tests/test_backtest_bancos.py:19` ("o `xfail(strict=True)` que travava a reprovação foi REMOVIDO
  ao cruzar o quórum") — este repo **já projetou um teste para deixar de reprovar**.
- `addopts = "-m 'not golden_nivel' --strict-markers"` — a quarentena + marcador desconhecido = erro.
- `markers = [...]` — as três categorias, com a descrição de cada uma. A do `golden_nivel` **precisa**
  conter a palavra **DELETAR** e a proibição de atualizar (é o texto que o próximo executor vai ler
  quando o golden quebrar na Fase 10).

**`tests/conftest.py`** (arquivo novo) — `pytest_collection_modifyitems`:
- lê `tests/classificacao.yaml` via `helpers_blindagem.carregar_classificacao()`
- para cada item coletado: busca `item.nodeid` no mapa → `item.add_marker(getattr(pytest.mark, cat))`
- categoria fora de `{invariante, golden_nivel, contrato}` → `pytest.UsageError`
- **teste sem entrada** → acumula em `sem_classe` → `pytest.UsageError` listando os nodeids, com a
  mensagem `TESTE NAO CLASSIFICADO (BLIND-01) — adicione a tests/classificacao.yaml`
- **entrada órfã** (está no YAML mas o teste sumiu) → `pytest.UsageError` com
  `CLASSIFICACAO ORFA — o teste sumiu mas a entrada ficou`

O check de órfão é o que torna a **deleção da Fase 10 auditável**: apagar a função de teste sem
apagar a linha do YAML quebra a coleta → a deleção não pode ser esquecida pela metade.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest --collect-only -q 2>&1 | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - Nesta task o `classificacao.yaml` ainda não existe/está incompleto → `.venv/bin/python -m pytest -q`
      **DEVE** terminar em `UsageError` com a mensagem `TESTE NAO CLASSIFICADO (BLIND-01)`.
      **Isto é o resultado esperado da Task 2** — a suíte fica verde na Task 3.
    - `grep -c "xfail_strict = true" pyproject.toml` → **1**
    - `grep -c "not golden_nivel" pyproject.toml` → **1**
    - `grep -c "strict-markers" pyproject.toml` → **1**
    - `grep -c "DELETAR" pyproject.toml` → **≥ 1** (a descrição do marcador `golden_nivel`)
    - `grep -c "pythonpath" pyproject.toml` → **1** e `grep -c "testpaths" pyproject.toml` → **1**
      (o bloco existente foi **preservado**, não substituído)
  </acceptance_criteria>

  <done>`pyproject.toml` com os 3 marcadores + `addopts` + `xfail_strict`; `conftest.py` falhando a coleta por falta de classificação (comportamento correto até a Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Gerar e AUDITAR tests/classificacao.yaml (448 entradas) e provar os 3 modos da quarentena</name>
  <files>tests/classificacao.yaml</files>

  <read_first>
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` §§ **"Inventário Real da Suíte"**
      (por que "~150 goldens" **não se reproduz**: os números defensáveis são **47** e **271**),
      **"Localização exata dos artefatos"** (os **três** goldens do ITUB4), **"Pitfall 3"**, **"Pitfall 4"**
    - `tests/test_backtest_bancos.py` (linhas 49-50, 78, 117-125) e `tests/test_motores.py` (linha 238)
      — os três goldens de nível do ITUB4, que precisam sair marcados com `-> DELETAR na Fase 10`
    - os arquivos de teste do caminho de valuation, conforme a auditoria for exigindo
  </read_first>

  <action>
1. Rodar `.venv/bin/python scripts/bootstrap_classificacao.py` para gerar a primeira versão de
   `tests/classificacao.yaml` (448 entradas).

2. **Auditar.** A regra de detecção que este plano ADOTA e DECLARA (o "~150" do ROADMAP é uma
   estimativa não-verificada — não persegui-la):
   - **`golden_nivel`** ⟺ o teste falharia **por causa de um NÍVEL** se o método mudasse: contém
     assert que compara uma saída do caminho de valuation (`intrinseco*`, `rim`, `ddm`,
     `preco_justo`, `vpa`, `lpa`, `roe`, `g`, `ke`, `payout`, alvo/faixa/banda) contra uma
     **constante numérica não-trivial**, ancorada num **ticker real** (validado contra
     `ticker_map.json`) **ou** numa **fixture de ticker real** (`snapshot_bancos*`, `fair_values_bancos*`).
   - **`invariante`** ⟺ verdade **algébrica/estrutural**, verificável sem ticker e sem knob
     (monotonicidade, identidade fechada, idempotência, simetria, referência matemática publicada —
     ex.: RSI de Wilder).
   - **`contrato`** ⟺ formato / borda / never-raise / roteamento (tipo, chave presente, `None` na
     borda, não levanta exceção, rótulo escolhido).
   - **Precedência na dúvida: `golden_nivel` > `invariante` > `contrato`.** Quarentenar um invariante
     por engano custa um teste a menos rodando por default (e ele continua acessível em `-m ""`);
     deixar um golden solto custa o marco (o executor da Fase 10 "atualiza" o número).

3. **Auditoria obrigatória, linha a linha, dos candidatos a `golden_nivel`** (o bucket pequeno —
   ~47 a ~80). Os demais (`invariante`/`contrato`) podem ser resolvidos por bloco/arquivo, EXCETO
   as entradas marcadas `# REVISAR` pelo bootstrap, que precisam de decisão individual.
   ⚠️ Rodar o detector também nos 125 testes "outros" (indicators/UI/home/glossário) — a pesquisa
   achou **3 goldens escondidos em `test_home_feed.py`** (Assumption A3).

4. **Os TRÊS goldens do ITUB4** (Pitfall 4 — o ROADMAP só nomeia um) precisam sair com comentário
   inline `# ITUB4 <o que trava> -> DELETAR na Fase 10 (PRIM-05), NUNCA atualizar`:
   - `tests/test_backtest_bancos.py::test_backtest_alvos_recalibrados` (o `32.88 ± 0.20`)
   - `tests/test_backtest_bancos.py::test_backtest_cesta_rota_por_ticker` (a banda 30–40)
   - `tests/test_motores.py::test_rota_seguradora_nao_pega_banco` (a banda 30–40)
   Confirmar o nome exato de cada função lendo os arquivos — não confiar nesta lista de memória.

5. Cabeçalho do YAML: as 3 definições + a regra "**quarentenar agora, DELETAR quando a fase chegar —
   NUNCA atualizar para o valor novo**".

**PROIBIDO nesta task:** editar qualquer arquivo em `tests/test_*.py` (a quarentena é por YAML
justamente para que **zero** teste existente seja tocado), editar `src/`, editar `config.yaml`,
mudar qualquer número.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest -q 2>&1 | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` → **0 failed, 0 errors**, e a linha final reporta
      `N passed, Q deselected` com `N + Q = 448`
    - `.venv/bin/python -m pytest -q -m golden_nivel --collect-only 2>&1 | tail -1` → colhe **Q ≥ 47**
      testes (os quarentenados rodáveis sob demanda)
    - `.venv/bin/python -m pytest -q -m "" --collect-only 2>&1 | tail -1` → **448 tests collected**
      (o `-m ""` sobrepõe o `addopts` e continua vendo tudo)
    - `grep -c REVISAR tests/classificacao.yaml` → **0** (toda pendência do bootstrap foi resolvida)
    - `grep -c "DELETAR na Fase 10" tests/classificacao.yaml` → **≥ 3** (os três goldens do ITUB4)
    - `grep -v '^#' tests/classificacao.yaml | grep -c 'golden_nivel'` → igual ao Q medido acima
    - **Prova de que a completude é imposta:** criar um teste temporário
      `tests/test_tmp_canario.py::test_tmp` → `.venv/bin/python -m pytest -q` termina em
      `UsageError: TESTE NAO CLASSIFICADO (BLIND-01)` → **deletar o arquivo temporário** e a suíte
      volta a verde. Registrar a saída no SUMMARY.
    - `git diff --stat -- tests/test_*.py src/ config.yaml` → **vazio** (nada de produção nem de teste
      existente foi tocado — o `V` de nenhum ticker mudou)
  </acceptance_criteria>

  <done>448 testes classificados; suíte verde com os goldens deselecionados; os 3 goldens do ITUB4 marcados para deleção na Fase 10; a coleta quebra se um teste novo não for classificado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML do repo → parser | `tests/classificacao.yaml` é arquivo **versionado e revisado** (trusted). Sem entrada externa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `helpers_blindagem.carregar_classificacao` | mitigate | `yaml.safe_load`, **nunca `yaml.load`** — desserialização de objetos arbitrários fica fora de alcance mesmo se o arquivo for corrompido |
| T-07-02 | Elevation | `scripts/bootstrap_classificacao.py` (subprocess) | mitigate | `subprocess.run` com **lista de argumentos**, sem `shell=True`, sem interpolação de string |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → verde, `N passed + Q deselected = 448`, 0 failed
- `.venv/bin/python -m pytest -q -m ""` → 448 rodam (nada foi perdido, só deselecionado)
- `git diff --stat -- src/ config.yaml tests/test_*.py` → vazio
</verification>

<success_criteria>
- BLIND-01 satisfeito: os 448 testes classificados em arquivo commitado; `golden_nivel` em quarentena.
- Um teste novo sem classificação quebra a coleta (provado com o canário temporário).
- Uma entrada órfã quebra a coleta (o mecanismo que torna a deleção da Fase 10 auditável).
- **Nenhum número mudou.** Zero diff em `src/`, `config.yaml` e nos testes existentes.
</success_criteria>

<output>
Criar `.planning/phases/07-blindagem-processual-blind/07-01-SUMMARY.md`.

Registrar no SUMMARY (números **medidos**, não os da pesquisa):
- `Q` = quantos testes foram para a quarentena, e `N` = quantos rodam por default (`N + Q = 448`)
- quantos vieram do detector AST vs. quantos foram promovidos na auditoria manual
- a saída do canário temporário (a prova de que a completude é imposta)
- **Fora de escopo desta fase (handoff):** o *golden master* dos 104 tickers (PITFALLS P5.2 /
  RESEARCH Q3) **não é** requisito BLIND e **não** foi feito aqui. Ele é **pré-requisito das Fases
  8/9** — sem baseline, "os asserts viram verde ticker a ticker" não é mensurável. Sinalizar ao
  planner do marco.
</output>
</content>
</invoke>
