---
phase: 07-blindagem-processual-blind
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - tests/test_blindagem_meta.py
  - tests/helpers_blindagem.py
  - tests/classificacao.yaml
autonomous: true
requirements: [BLIND-04]

must_haves:
  truths:
    - "Nenhum teste NOVO pode afirmar 'ticker == valor em reais' — a porta pela qual o overfit voltaria está fechada por um teste."
    - "Os testes que hoje cravam ticker+R$ só são tolerados porque estão na quarentena (têm data de morte declarada)."
    - "Existe o harness do jackknife (função pura, testada com dados sintéticos) pronto para a Fase 14."
    - "O veredito do jackknife NÃO é inventado hoje: sem a cesta estratificada da Fase 14, o teste SKIPa em vez de fingir."
  artifacts:
    - path: "tests/test_blindagem_meta.py"
      provides: "BLIND-04a (meta-teste AST) e BLIND-04b (jackknife: invariante + skip até a Fase 14)"
      contains: "load_bearing"
    - path: "tests/helpers_blindagem.py"
      provides: "mediana_jackknife(valores) -> (mediana, desvio_max_ao_remover_1)"
      contains: "def mediana_jackknife"
  key_links:
    - from: "tests/test_blindagem_meta.py"
      to: "tests/classificacao.yaml (conjunto golden_nivel)"
      via: "ofensores - quarentenados; um teste novo com ticker+R$ nao esta na quarentena -> falha"
      pattern: "quarentenados"
    - from: "tests/test_blindagem_meta.py"
      to: "tests/fixtures/holdout_v24.yaml"
      via: "pytest.skip se o fixture nao existir (nasce na Fase 14)"
      pattern: "holdout_v24"
---

<objective>
Fechar a porta pela qual o overfit voltaria: **nenhum teste de calibração pode afirmar
`ticker == valor em reais`** (BLIND-04a), e preparar o harness de validação por **distribuição +
jackknife** que substitui o golden por ticker (BLIND-04b).

Purpose: `assert V(ITUB4) == 32.88` é **calibração disfarçada de teste**. O 07-01 quarentenou os que
já existem; este plano impede que **novos** nasçam. O jackknife é o substituto legítimo — mas ele
**não tem sobre o que operar hoje** (4 tickers no fixture; jackknife sobre 4 observações é vazio), e
inventar um veredito sem dado seria calibrar um knob contra dado inexistente.
Output: `tests/test_blindagem_meta.py` + `mediana_jackknife` em `tests/helpers_blindagem.py`.

**ESTE PLANO NÃO MOVE NENHUM NÚMERO.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md
@.planning/phases/07-blindagem-processual-blind/07-01-SUMMARY.md
@.planning/REQUIREMENTS.md
@tests/helpers_blindagem.py
@tests/classificacao.yaml
</context>

<interfaces>
<!-- Já criado no plano 07-01. Reusar, não reimplementar. -->
```
helpers_blindagem.detectar_ticker_com_valor_cravado(raiz=Path("tests")) -> set[str]
    # devolve "tests/arquivo.py::funcao" (SEM [param]); regra: literal de ticker validado contra
    # data/ticker_map.json  E  float nao-trivial (∉ {0,1,0.5,2}) em Compare/Assert
helpers_blindagem.quarentenados() -> set[str]        # chaves golden_nivel, ja sem [param]
helpers_blindagem.nodeid_para_funcao(nodeid) -> str  # corta o [param]
```
⚠️ Os dois conjuntos precisam ser comparados **no mesmo espaço de nomes** (nível de função, sem
`[param]`) — é por isso que `quarentenados()` já aplica `nodeid_para_funcao`.

**Fixture do hold-out (BLIND-04b):** `tests/fixtures/holdout_v24.yaml` **não existe** e **não é
criado nesta fase** — ele nasce na **Fase 14 (VAL-02)**: cesta estratificada, ≥6 por arquétipo + 10
"difíceis" deliberados. `tests/fixtures/fair_values_bancos.yaml` tem só **4 tickers** (a cesta do
overfit v2.3) — **não serve** de substrato.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: BLIND-04a — meta-teste AST: nenhum teste crava ticker == R$ fora da quarentena</name>
  <files>tests/test_blindagem_meta.py, tests/classificacao.yaml</files>

  <read_first>
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` § **"BLIND-04 → 4a. A proibição"**
      (o teste está escrito lá, com a regra exata de detecção) e § **"Pitfall 7"** (por que regex nua
      não serve: casa `MACD12`)
    - `tests/helpers_blindagem.py` (o detector, criado no 07-01)
    - `tests/classificacao.yaml` (o conjunto `golden_nivel` — a quarentena)
  </read_first>

  <action>
Criar `tests/test_blindagem_meta.py` com `test_nenhum_teste_de_calibracao_crava_ticker_em_reais`
(marcador `contrato`):

- `ofensores = helpers_blindagem.detectar_ticker_com_valor_cravado(Path("tests"))`
- `novos = ofensores - helpers_blindagem.quarentenados()`
- `assert not novos`, com mensagem que **lista** os ofensores e explica a correção: *"um teste que
  crava `ticker == R$` é calibração disfarçada de teste. Ou ele é classificado como `golden_nivel` em
  `tests/classificacao.yaml` (quarentena, com data de morte declarada), ou ele não existe. Não
  afrouxe este teste."*
- Docstring: este é o mecanismo que impede o overfit de **voltar**. Os que já existem estão
  tolerados **porque estão na quarentena** — quarentena é dívida declarada, não permissão.

⚠️ **O teste deve estar VERDE ao fim desta task.** Se algum ofensor ficar de fora da quarentena, é
porque a auditoria do 07-01 deixou passar → a correção é **classificar o teste como `golden_nivel`**
no `classificacao.yaml` (com o comentário da fase em que morre), **NUNCA** afrouxar o detector nem
excluir arquivos da varredura.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest -q -k "crava_ticker"</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q -k "crava_ticker"` → **1 passed**, 0 failed
    - **Prova de que o teste morde** (o canário deste requisito): criar um teste temporário
      `tests/test_tmp_ofensor.py` com `assert 32.88 == 32.88` e a string `"ITUB4"` no corpo,
      classificá-lo como `contrato` no YAML (para a coleta não quebrar) → `pytest -k crava_ticker`
      **FALHA** listando `tests/test_tmp_ofensor.py::test_...`. **Deletar** o arquivo e a entrada do
      YAML → volta a verde. Registrar as duas saídas no SUMMARY.
    - `grep -c "MACD12\|re.compile\|re.search\|re.match" tests/test_blindagem_meta.py` → **0**
      (nada de regex para achar ticker — a detecção é AST + `ticker_map.json`)
  </acceptance_criteria>

  <done>O meta-teste passa hoje e comprovadamente falha quando um teste novo crava ticker+R$ fora da quarentena.</done>
</task>

<task type="auto">
  <name>Task 2: BLIND-04b — harness do jackknife (invariante hoje) + test_nenhum_ticker_e_load_bearing (skip ate a Fase 14)</name>
  <files>tests/helpers_blindagem.py, tests/test_blindagem_meta.py, tests/classificacao.yaml</files>

  <read_first>
    - `.planning/phases/07-blindagem-processual-blind/07-RESEARCH.md` § **"BLIND-04 → 4b. O jackknife —
      não tem sobre o que operar hoje"** e § **"Assumptions Log → A1"** (o limiar de 1pp é
      **[ASSUMIDO]** e deve ser fixado na Fase 14, com a distribuição real na mão)
    - `.planning/REQUIREMENTS.md` → **BLIND-04**, **VAL-02** (a cesta estratificada da Fase 14) e
      **VAL-05** (a métrica é `V/FairValue`, **nunca** `V/preço`)
  </read_first>

  <action>
Acrescentar a `tests/helpers_blindagem.py`:

- `mediana_jackknife(valores: Sequence[float]) -> tuple[float, float]` — função **pura**: devolve
  `(mediana_completa, desvio_max_ao_remover_1)`, onde o segundo é
  `max(|mediana(valores - {v}) - mediana(valores)|)` sobre todo `v`. Sem I/O, sem config.

Acrescentar a `tests/test_blindagem_meta.py`:

1. `test_mediana_jackknife_e_robusta_por_construcao` — marcador `invariante`, **passa hoje**:
   verdades algébricas sobre dados **sintéticos** (nenhum ticker):
   - numa amostra homogênea grande, remover 1 ponto move a mediana pouco;
   - injetando **um outlier dominante**, o `desvio_max_ao_remover_1` **explode** → o harness
     **detecta** um ponto load-bearing. É a prova de que a função mede o que promete.

2. `test_nenhum_ticker_e_load_bearing` — marcador `contrato`:
   - `LIMIAR_JACKKNIFE_PP = 0.01` — constante nomeada no topo, com comentário **obrigatório**:
     *"[ASSUMIDO] — 1 pp. NÃO é um número medido: não existe dado para medi-lo hoje. É FIXADO NA
     FASE 14 (VAL-02), com a distribuição real da cesta estratificada na mão. Fixá-lo agora seria
     calibrar um knob contra dado inexistente — exatamente o que este marco combate."*
   - Se `tests/fixtures/holdout_v24.yaml` **não existir** → `pytest.skip("cesta estratificada nasce
     na Fase 14 (VAL-02); jackknife sobre os 4 tickers do v2.3 e' estatisticamente vazio")`.
     ⚠️ **`skip`, não `xfail`**: a ausência do fixture **não é uma doença a curar**, é uma dependência
     de fase. Um `xfail` aqui viraria XPASS na Fase 14 e confundiria o sinal.
   - Quando o fixture existir: métrica é **`V/FairValue`** (VAL-05 — **nunca** `V/preço`; um modelo
     com mediana `V/preço = 1,00` é um espelho do mercado e não serve para nada); assert
     `desvio_max_ao_remover_1 <= LIMIAR_JACKKNIFE_PP`.

Registrar os 3 testes novos em `tests/classificacao.yaml`.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest -q -rs -k "jackknife or load_bearing"</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q -rs -k "jackknife"` → **1 passed**
    - `.venv/bin/python -m pytest -q -rs -k "load_bearing"` → **1 skipped**, com a razão citando
      **Fase 14** / **VAL-02** (0 failed, 0 xfailed)
    - `grep -c "ASSUMIDO" tests/test_blindagem_meta.py` → **≥ 1** (o limiar carrega a etiqueta)
    - `grep -c "FASE 14\|Fase 14" tests/test_blindagem_meta.py` → **≥ 2**
    - `grep -v '^#' tests/test_blindagem_meta.py | grep -c "V/preco\|v_preco"` → **0** (a métrica é
      `V/FairValue`, VAL-05)
    - `.venv/bin/python -m pytest -q` (suíte inteira) → **0 failed**, **2 xfailed**, **1 skipped**,
      0 xpassed, 0 errors
  </acceptance_criteria>

  <done>Jackknife implementado e provado em dados sintéticos; o veredito SKIPa aguardando a Fase 14, com o limiar declarado [ASSUMIDO].</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fonte dos testes → parser AST | `tests/*.py` são arquivos do repo (trusted). O AST **parseia**, nunca **executa**. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Elevation | detector de ofensores | mitigate | `ast.parse` sobre o texto do arquivo — **nunca** `exec`/`eval`/`import` do módulo sob análise |
| T-07-06 | Tampering | `holdout_v24.yaml` (Fase 14) | accept | Artefato versionado e revisado; `yaml.safe_load` já cobre o risco de desserialização |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q -rs` → 0 failed, 2 xfailed, 1 skipped, 0 xpassed
- O canário do BLIND-04a foi executado (ofensor temporário → vermelho → deletado → verde)
- `git diff --stat -- src/ config.yaml` → vazio
</verification>

<success_criteria>
- BLIND-04 satisfeito: (a) meta-teste AST verde e comprovadamente mordente; (b) harness do jackknife
  pronto e testado, com o veredito honestamente adiado para a Fase 14.
- O limiar do jackknife está marcado `[ASSUMIDO]` — **não** foi inventado um número contra dado
  inexistente.
</success_criteria>

<output>
Criar `.planning/phases/07-blindagem-processual-blind/07-03-SUMMARY.md`.

Registrar: quantos ofensores o detector acha hoje e que **todos** estão na quarentena; a saída do
canário do BLIND-04a (vermelho → verde); e o handoff explícito para a Fase 14: **fixar
`LIMIAR_JACKKNIFE_PP` com a distribuição real da cesta estratificada (VAL-02)**.
</output>
</content>
