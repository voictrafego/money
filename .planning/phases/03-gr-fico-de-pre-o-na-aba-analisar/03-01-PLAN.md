---
phase: 03-gr-fico-de-pre-o-na-aba-analisar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/ingest/prices.py
  - src/analista/ingest/build.py
  - src/analista/core/fundamentals.py
  - requirements.txt
autonomous: true
requirements: [GRAF-01, GRAF-02, GRAF-03]

must_haves:
  truths:
    - "A série diária de close (5a) baixada em prices.py é preservada em vez de descartada"
    - "A série viaja DadosMercado → CompanyData via build.montar_empresa, como os demais campos de mercado"
    - "plotly está em requirements.txt e instalado no venv"
    - "pytest continua verde — nenhum golden test quebra (nenhuma fórmula de valuation alterada)"
  artifacts:
    - path: "src/analista/ingest/prices.py"
      provides: "Campo serie_precos em DadosMercado + preservação de hist['Close'] no fetch existente"
      contains: "serie_precos"
    - path: "src/analista/ingest/build.py"
      provides: "Cópia c.serie_precos = dm.serie_precos em montar_empresa"
      contains: "c.serie_precos = dm.serie_precos"
    - path: "src/analista/core/fundamentals.py"
      provides: "Campo serie_precos em CompanyData (snapshot de mercado)"
      contains: "serie_precos"
    - path: "requirements.txt"
      provides: "Pin de plotly"
      contains: "plotly"
  key_links:
    - from: "src/analista/ingest/prices.py"
      to: "DadosMercado.serie_precos"
      via: "atribuição dm.serie_precos = hist['Close'].dropna() dentro do if hist existente"
      pattern: "dm\\.serie_precos = hist"
    - from: "src/analista/ingest/build.py"
      to: "CompanyData.serie_precos"
      via: "cópia direta dm→c no bloco de campos de mercado"
      pattern: "c\\.serie_precos = dm\\.serie_precos"
---

<objective>
Preservar a série diária de preço de fechamento (5 anos) que `ingest/prices.py` já baixa
(`tk.history(period="5y", auto_adjust=True)`) e hoje descarta, e conduzi-la pela cadeia
`DadosMercado → build.montar_empresa → CompanyData` — exatamente como os campos de mercado
existentes (`preco_atual`, `beta`, `dpa_trailing_12m`). Também adicionar `plotly` ao
`requirements.txt` e instalá-lo no venv (preparação para o render no Plano 02).

Purpose: Disponibilizar a série na estrutura `CompanyData` (já em escopo na Tela 1 do app.py)
sem nova chamada de rede — backbone de GRAF-01/02/03. Garantir o ambiente (plotly) e a
imunidade dos golden tests.
Output: Campo `serie_precos` nos dois dataclasses, threading em `build.py`, `plotly>=6.0`
no requirements e instalado, `pytest` verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-CONTEXT.md
@.planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-RESEARCH.md
@.planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-PATTERNS.md

<interfaces>
<!-- Formas exatas a copiar, extraídas do codebase. Não explorar — copiar a forma. -->

DadosMercado (src/analista/ingest/prices.py:45-57) — campos Optional de mercado:
  preco_atual: Optional[float] = None
  beta: Optional[float] = None
  desempenho_relativo_6m: Optional[float] = None
  dpa_trailing_12m: Optional[float] = None
  ano_dpa: Optional[int] = None
  (typing importa Dict, Optional na linha 14; pandas NÃO é importado no topo)

Bloco de preservação no fetch (prices.py:98-102) — já existe, guardado por `if hist is not None and not hist.empty:`:
  if dm.preco_atual is None:
      dm.preco_atual = float(hist["Close"].iloc[-1])
  ult_ano = hist.tail(252)
  dm.volume_financeiro_diario = float((ult_ano["Close"] * ult_ano["Volume"]).mean())

Threading dm→c (src/analista/ingest/build.py:35-41):
  c.preco_atual = dm.preco_atual
  c.beta = dm.beta
  c.dpa_trailing_12m = dm.dpa_trailing_12m
  c.ano_dpa = dm.ano_dpa
  c.eh_concessionaria = any(...)

CompanyData snapshot de mercado (src/analista/core/fundamentals.py:38-48):
  preco_atual: Optional[float] = None
  beta: Optional[float] = None
  ...
  dpa_trailing_12m: Optional[float] = None
  ano_dpa: Optional[int] = None
  (typing importa Dict, List, Optional na linha 13; pandas NÃO é importado)

requirements.txt — pins existentes: pandas>=2.0, ..., streamlit>=1.30
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preservar a série em prices.py (campo + atribuição no fetch existente)</name>
  <files>src/analista/ingest/prices.py</files>
  <read_first>
    - src/analista/ingest/prices.py (arquivo sendo modificado; dataclass DadosMercado linhas 45-57, bloco de fetch linhas 92-102)
    - .planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-PATTERNS.md (seção "src/analista/ingest/prices.py" — padrão de campo Optional e preservação no fetch)
    - .planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-RESEARCH.md (Pattern 1; Pitfall 3 gaps/NaN; Pitfall 4 import pesado)
  </read_first>
  <action>
    Em `DadosMercado` (prices.py ~45-57), adicionar o campo `serie_precos: Optional["pd.Series"] = None`
    junto aos demais campos Optional de mercado, com comentário curto explicando o "porquê"
    (ex.: "close diário 5a, índice = datas"). Usar forward-ref em string `Optional["pd.Series"]`
    para NÃO forçar `import pandas` no topo do módulo (Pitfall 4 — a engine deve ficar leve;
    `pandas` não está importado no topo, só `Dict, Optional` na linha 14).
    Dentro de `coletar_mercado`, no bloco JÁ existente `if hist is not None and not hist.empty:`
    (~98-102), adicionar UMA linha: `dm.serie_precos = hist["Close"].dropna()`. Não criar
    novo try/except — o try/except existente (93-96) já define `hist = None` em falha do Yahoo,
    deixando `serie_precos = None` (estado que o fallback D-05/GRAF-03 consome). Nenhuma nova
    chamada de rede: a série sai de `hist["Close"]` que já foi baixado.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && grep -n 'serie_precos' src/analista/ingest/prices.py | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - `DadosMercado` declara `serie_precos: Optional["pd.Series"] = None` (forward-ref em string).
    - `coletar_mercado` atribui `dm.serie_precos = hist["Close"].dropna()` DENTRO do bloco
      `if hist is not None and not hist.empty:` existente (sem novo try/except, sem nova chamada de rede).
    - Nenhum `import pandas`/`import plotly` adicionado ao topo de prices.py.
    - `cd "<projeto>" && ./.venv/bin/python -c "import analista.ingest.prices"` importa sem erro.
  </acceptance_criteria>
  <done>O campo serie_precos existe em DadosMercado e é preenchido com a série de close já baixada; módulo importa sem novas deps no topo.</done>
</task>

<task type="auto">
  <name>Task 2: Thread serie_precos em build.py e CompanyData; plotly em requirements.txt</name>
  <files>src/analista/core/fundamentals.py, src/analista/ingest/build.py, requirements.txt</files>
  <read_first>
    - src/analista/core/fundamentals.py (dataclass CompanyData, bloco "snapshot atual / mercado" linhas 38-48; imports linha 13)
    - src/analista/ingest/build.py (bloco de cópia dm→c linhas 35-41)
    - requirements.txt (pins existentes)
    - .planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-PATTERNS.md (seções build.py, fundamentals.py, requirements.txt)
    - .planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-RESEARCH.md (Standard Stack / Installation; Pitfall 4)
  </read_first>
  <action>
    Em `CompanyData` (fundamentals.py, bloco "snapshot atual / mercado" ~38-48), adicionar
    `serie_precos: Optional["pd.Series"] = None` junto de `preco_atual`/`beta`, com comentário curto.
    Usar forward-ref em string (`Optional["pd.Series"]`) — NÃO adicionar `import pandas` ao topo
    (imports atuais: `from typing import Dict, List, Optional` na linha 13; Pitfall 4: os testes
    carregam este módulo, mantê-lo leve). Não adicionar métodos novos ao dataclass.
    Em `build.montar_empresa` (build.py), adicionar `c.serie_precos = dm.serie_precos` no bloco
    de campos de mercado (após a linha 39 `c.dpa_trailing_12m = ...` / junto de `c.ano_dpa`),
    mesma forma e indentação dos demais — cópia direta de Optional, sem lógica condicional.
    Em `requirements.txt`, adicionar `plotly>=6.0` (estilo "minor floor"); colocar junto de
    `streamlit>=1.30` (ambos camada de apresentação). Em seguida instalar no venv:
    `./.venv/bin/pip install "plotly>=6.0"`.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && grep -n 'serie_precos' src/analista/core/fundamentals.py src/analista/ingest/build.py | grep -v '^#' && grep -c plotly requirements.txt && ./.venv/bin/python -c "import plotly; print(plotly.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `CompanyData` declara `serie_precos: Optional["pd.Series"] = None` no bloco de mercado (forward-ref string).
    - `build.montar_empresa` contém `c.serie_precos = dm.serie_precos` no bloco de campos de mercado.
    - `requirements.txt` contém uma linha `plotly>=6.0`.
    - `./.venv/bin/python -c "import plotly"` executa sem erro (plotly instalado).
    - Nenhum `import pandas`/`import plotly` adicionado ao topo de fundamentals.py ou build.py.
  </acceptance_criteria>
  <done>serie_precos existe em CompanyData e é copiado em build.py; plotly está no requirements e instalado no venv.</done>
</task>

<task type="auto">
  <name>Task 3: Garantir golden tests verdes após o threading</name>
  <files>tests/</files>
  <read_first>
    - .planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-RESEARCH.md (Pitfall 4: testes não importam app.py; 62 testes offline; campo Optional novo é imune a construção por keyword args)
    - .planning/ROADMAP.md (Phase 3 Success Criterion #4: pytest verde, nenhuma fórmula alterada)
  </read_first>
  <action>
    Rodar a suíte completa de testes para confirmar que o campo opcional novo nos dataclasses
    NÃO quebrou nenhum golden (test_ddm, test_multiples, test_comparables, test_screening,
    test_consistencia_modos, test_fundamentals_consistencia, test_ingest_resolucao). Nenhuma
    fórmula de valuation foi alterada nesta fase — esta task é a trava de regressão (TEST-02/SC #4).
    NÃO editar testes para "passar"; se algum quebrar, a causa é uma mudança indevida nas tasks
    anteriores e deve ser corrigida na origem (não no teste). Confirmar também que pytest não
    importa plotly (a engine não importa plotly em lugar nenhum).
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && ./.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `./.venv/bin/python -m pytest -q` sai com código 0 (todos os golden tests verdes).
    - Nenhum arquivo em `tests/` foi modificado para forçar a passagem.
    - `cd "<projeto>" && ./.venv/bin/python -m pytest -q 2>&1 | grep -i 'ModuleNotFoundError.*plotly'` não retorna nada (engine não importa plotly).
  </acceptance_criteria>
  <done>pytest verde; nenhuma fórmula alterada; engine não depende de plotly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo Finance → prices.py | Entrada externa não confiável (série de preços). JÁ é a borda existente; esta fase só preserva um campo a mais do mesmo fetch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering/DoS | prices.py série do Yahoo (NaN/gaps/vazio) | mitigate | `hist["Close"].dropna()` na origem remove NaN; o `if hist is not None and not hist.empty` existente já trata vazio/None (vira `serie_precos=None`, consumido pelo fallback do Plano 02). |
| T-03-02 | Information disclosure | serie_precos em dataclass (pickle do cache) | accept | Série é dado público de preço (read-only display), sem PII/segredos. Cache é local em memória (ttl 1h). Sem superfície nova. |

Nota honesta: esta fase praticamente não tem superfície de segurança — reusa um fetch que já
existe, não adiciona input do usuário, rede nova, auth ou segredos. A série é dado de exibição
read-only.
</threat_model>

<verification>
- `grep serie_precos` retorna ocorrências em prices.py, fundamentals.py e build.py.
- `./.venv/bin/python -c "import plotly"` funciona; `plotly>=6.0` em requirements.txt.
- `./.venv/bin/python -m pytest -q` sai 0 (golden verde, SC #4 / TEST-02).
- Engine importa sem `import pandas`/`import plotly` novos no topo dos módulos.
</verification>

<success_criteria>
- A série de close 5a é preservada em DadosMercado e copiada para CompanyData (sem nova rede).
- plotly instalado e pinado em requirements.txt.
- pytest verde — nenhuma fórmula de valuation alterada (SC #4).
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-01-SUMMARY.md`.
</output>
