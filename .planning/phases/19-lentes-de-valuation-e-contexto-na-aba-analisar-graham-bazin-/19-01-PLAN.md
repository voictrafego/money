---
phase: 19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/core/lentes.py
  - tests/test_lentes.py
autonomous: true
requirements: [VAL-01, VAL-02, RET-01, PEER-01]

must_haves:
  truths:
    - "Chamar preco_justo_graham(lpa, vpa) com LPA e VPA positivos retorna √(22,5×LPA×VPA); LPA≤0 ou VPA≤0 retorna None (never-raise)"
    - "Chamar preco_teto_bazin(dpa_medio) retorna DPA médio ÷ 0,06; sem dividendos (dpa_medio None/≤0) retorna None"
    - "Chamar retorno_periodo(serie_adj, anos) retorna R$ final de R$1.000 via Adj Close; histórico insuficiente retorna None (nunca exceção)"
    - "tabela_pares(metricas, ticker_alvo) marca a linha do alvo e reporta se há pares suficientes"
    - "Os 296 goldens existentes seguem verdes; nenhum módulo de método é alterado em comportamento"
  artifacts:
    - path: "src/analista/core/lentes.py"
      provides: "Fórmulas de referência puras (Graham, Bazin, retorno, comparador) — módulo novo aditivo"
      contains: "def preco_justo_graham"
    - path: "tests/test_lentes.py"
      provides: "Golden tests de valores conhecidos das 4 lentes + degradação never-raise"
      contains: "def test_graham"
  key_links:
    - from: "src/analista/core/lentes.py"
      to: "src/analista/core/multiples.py"
      via: "reuso de preco_lucro/earnings_yield/dividend_yield (sem duplicar fórmula)"
      pattern: "from . import multiples"
---

<objective>
Criar o módulo puro `core/lentes.py` com as quatro lentes de valuation/contexto da Fase 19
(Graham, Bazin, "quanto teria rendido", comparador de pares), todas como funções puras
testáveis por golden — espelhando o padrão de `ddm.py`/`multiples.py`. NÃO toca em `app.py`
nem em nenhum módulo de método existente.

Purpose: isolar as fórmulas novas na engine (regra locked: lógica na engine, UI só lê) e
travar sua correção com testes golden de valores conhecidos antes de qualquer render.
Output: `src/analista/core/lentes.py` + `tests/test_lentes.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-/19-CONTEXT.md

<interfaces>
<!-- Contratos da engine que as lentes reusam. Executor NÃO precisa explorar o codebase. -->

De src/analista/core/multiples.py (funções puras, retornam None quando indefinido):
- preco_lucro(preco, lpa) -> Optional[float]      # P/L = preço / LPA
- earnings_yield(lpa, preco) -> Optional[float]
- dividend_yield(dpa, preco) -> Optional[float]
- _safe_div(num, den) -> Optional[float]          # divisão segura (den None/0 -> None)

De src/analista/core/fundamentals.py — dataclass CompanyData (campos/métodos públicos usados):
- preco_atual: Optional[float]
- patrimonio_liquido: Dict[int, float]   # {ano: PL}
- num_acoes: Dict[int, float]            # {ano: nº ações base UNIT}
- ultimo_ano() -> Optional[int]
- lpa_valuation() -> Optional[float]     # LPA canônico normalizado (o que o Analisar exibe)
- roe_valuation() -> Optional[float]
- dy_atual() -> Optional[float]
- dpa(ano) -> Optional[float]            # dividendos[ano]/num_acoes[ano]
- anos_ordenados() -> List[int]

De src/analista/core/ddm.py — padrão de módulo puro golden-testável (analog de estilo).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fórmulas Graham + Bazin + retorno + VPA/upside (funções puras)</name>
  <read_first>
    - src/analista/core/multiples.py (reusar _safe_div/preco_lucro; NÃO reescrever fórmula)
    - src/analista/core/ddm.py (padrão de módulo puro: docstrings com conferência de valores, `Number = Optional[float]`)
    - .planning/phases/19-.../19-CONTEXT.md (fórmulas locked VAL-01/VAL-02/RET-01)
  </read_first>
  <behavior>
    - Graham: preco_justo_graham(lpa=3.0, vpa=10.0) == sqrt(22.5*3*10) ≈ 25.98
    - Graham degrada: lpa<=0 OR vpa<=0 OR None -> None (fórmula não vale sem lucro/PL positivo)
    - VPA: vpa(patrimonio_liquido=1000.0, num_acoes=100.0) == 10.0; num_acoes None/0 -> None
    - dpa_medio([1.0, 2.0, 3.0], n=5) == 2.0; pula None; lista vazia/só None -> None; usa só os últimos n
    - Bazin: preco_teto_bazin(dpa_medio=1.2, dy_minimo=0.06) == 20.0; dpa_medio None/<=0 -> None
    - upside(referencia=30.0, preco_atual=20.0) == 0.5; preco_atual None/0 -> None
    - retorno_periodo(serie_adj, anos=1, valor_inicial=1000.0): 1000 * ultimo/primeiro_na_janela;
      série vazia/curta demais p/ a janela -> None (never-raise)
  </behavior>
  <action>
    Criar src/analista/core/lentes.py como módulo puro (mesmo cabeçalho/estilo de ddm.py:
    `from __future__ import annotations`, `Number = Optional[float]`, docstring citando as
    fórmulas do livro/concorrente). Reusar `from . import multiples as mult` — NÃO reimplementar
    divisão (usar mult._safe_div ou guardas locais equivalentes).

    Constantes de módulo: GRAHAM_K = 22.5 (VAL-01) e BAZIN_DY_MIN = 0.06 (VAL-02, DY-mínimo 6%).

    Funções:
    - preco_justo_graham(lpa, vpa) -> Number: retorna math.sqrt(GRAHAM_K * lpa * vpa) SOMENTE se
      lpa e vpa não-None e ambos > 0; caso contrário None (VAL-01 degradação: não serve p/ empresa
      sem lucro/PL positivo).
    - vpa(patrimonio_liquido, num_acoes) -> Number: PL / num_acoes via _safe_div (VPA do ano-base).
    - dpa_medio(dpas, n=5) -> Number: média aritmética dos ÚLTIMOS n valores não-None de `dpas`
      (Sequence[Optional[float]]); se <5 anos usa o período disponível (VAL-02: espelha nota do
      concorrente); None se não houver nenhum DPA.
    - preco_teto_bazin(dpa_med, dy_minimo=BAZIN_DY_MIN) -> Number: dpa_med / dy_minimo se dpa_med
      não-None e > 0; senão None (VAL-02 degradação: só vale p/ boas pagadoras).
    - upside(referencia, preco_atual) -> Number: referencia/preco_atual - 1.0 via guarda (preço
      None/0 -> None). Usado por Graham e Bazin p/ o "upside vs preço atual".
    - retorno_periodo(serie_adj, anos, valor_inicial=1000.0) -> Number: recebe uma pd.Series de
      Adj Close indexada por data (já embute reinvestimento de dividendos, RET-01). Determina a
      data-corte = último índice - `anos` anos (usar pd.DateOffset/Timedelta); pega o primeiro
      preço em/ou-após a corte e o último preço; retorna valor_inicial * ultimo/primeiro. Retorna
      None se a série for None/vazia OU se não houver dado tão antigo quanto a janela pedida
      (histórico insuficiente) — NUNCA levanta exceção. Import pandas dentro do módulo.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_lentes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/analista/core/lentes.py existe e exporta preco_justo_graham, vpa, dpa_medio,
      preco_teto_bazin, upside, retorno_periodo
    - `grep -c "GRAHAM_K = 22.5" src/analista/core/lentes.py` == 1 e `grep -c "BAZIN_DY_MIN = 0.06"` == 1
    - Nenhuma fórmula de divisão duplicada: `grep -q "import multiples" src/analista/core/lentes.py`
    - Todas as funções retornam None (não exceção) para entradas degeneradas (None/≤0/série curta)
  </acceptance_criteria>
  <done>As 4 lentes puras (Graham, Bazin, retorno, mais VPA/upside/dpa_medio auxiliares) existem, com degradação never-raise; os testes da Task 3 cobrindo estas funções passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comparador de pares — metricas_par + tabela_pares (PEER-01)</name>
  <read_first>
    - src/analista/core/fundamentals.py (CompanyData: lpa_valuation/roe_valuation/dy_atual/preco_atual/patrimonio_liquido/num_acoes/ultimo_ano)
    - src/analista/core/multiples.py (preco_lucro para P/L consistente com o Analisar)
    - src/analista/core/comparables.py (analog de estilo do módulo de pares; reusa multiples)
    - src/analista/core/lentes.py (o arquivo desta fase, criado na Task 1 — adicionar aqui)
  </read_first>
  <behavior>
    - metricas_par(c) devolve dict com pl, pvp, roe, dy, valor_mercado do ticker c
    - pvp usa vpa() do ano-base (preco_atual / (PL[ult]/num_acoes[ult]))
    - valor_mercado = preco_atual * num_acoes[ult]; campos None quando insumo ausente (never-raise)
    - tabela_pares(metricas, ticker_alvo): cada linha ganha alvo=True só na do ticker_alvo
    - pares_suficientes(pares) [lista já marcada] True apenas com >= 2 linhas `not .alvo` (senão UI mostra msg neutra)
  </behavior>
  <action>
    Adicionar a src/analista/core/lentes.py um dataclass e duas funções puras (PEER-01):
    - dataclass ParComparavel(ticker: str, pl: Number, pvp: Number, roe: Number, dy: Number,
      valor_mercado: Number, alvo: bool = False).
    - metricas_par(c) -> ParComparavel: recebe um CompanyData; ult = c.ultimo_ano();
      pl = mult.preco_lucro(c.preco_atual, c.lpa_valuation()) (MESMO LPA canônico do Analisar,
      Core Value de consistência entre menus); pvp = mult._safe_div(c.preco_atual,
      vpa(c.patrimonio_liquido.get(ult), c.num_acoes.get(ult))); roe = c.roe_valuation();
      dy = c.dy_atual(); valor_mercado = mult._safe_div(...) NÃO — usar multiplicação guardada:
      preco_atual * num_acoes[ult] se ambos não-None senão None. alvo=False (a UI marca depois,
      ou passar ticker_alvo). Importar CompanyData de `.fundamentals` (sem ciclo: fundamentals
      não importa lentes).
    - tabela_pares(metricas, ticker_alvo) -> List[ParComparavel]: recebe uma lista de
      ParComparavel (ou de CompanyData — aceite List[CompanyData] e chame metricas_par internamente);
      seta alvo=True na linha cujo ticker == ticker_alvo (case-insensitive, .upper()); preserva a
      ordem de entrada. NÃO ordena nem recomenda (PEER-01: só contexto).
    - pares_suficientes(pares) -> bool: recebe a lista JÁ MARCADA por tabela_pares (List[ParComparavel]
      com o flag .alvo setado). True se houver ao menos 2 linhas com `not p.alvo` e com pelo menos
      P/L OU DY não-None (evita tabela vazia). Contar pelo flag .alvo (não por string de ticker) remove
      ambiguidade. Usado pela UI p/ decidir entre tabela e mensagem neutra de degradação.
    NÃO alterar comparables.py (mantém 296 goldens); reuso é por import de multiples.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_lentes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - lentes.py exporta ParComparavel, metricas_par, tabela_pares, pares_suficientes
    - tabela_pares marca exatamente uma linha alvo=True quando o ticker_alvo está presente
    - `grep -q "import comparables" src/analista/core/comparables.py` NÃO se aplica — comparables.py permanece intocado (`git diff --quiet src/analista/core/comparables.py`)
    - metricas_par com CompanyData sem preço/num_acoes devolve campos None sem levantar
  </acceptance_criteria>
  <done>O comparador de pares puro existe (5 métricas P/L, P/VP, ROE, DY, Valor de Mercado por ticker, com flag alvo e checagem de suficiência); testes da Task 3 passam.</done>
</task>

<task type="auto">
  <name>Task 3: Suíte golden test_lentes.py + 296 goldens verdes</name>
  <read_first>
    - tests/test_ddm.py (padrão de golden: valores conhecidos, tolerância abs, comentário citando a fonte)
    - tests/test_multiples.py (padrão de teste de funções puras + casos de None)
    - src/analista/core/lentes.py (assinaturas criadas nas Tasks 1-2)
    - src/analista/core/fundamentals.py (montar um CompanyData mínimo p/ testar metricas_par)
  </read_first>
  <action>
    Criar tests/test_lentes.py cobrindo TODAS as funções com valores conhecidos e degradação:
    - test_graham: preco_justo_graham(3.0, 10.0) ≈ 25.98 (abs < 0.01); lpa<=0/vpa<=0/None -> None.
    - test_vpa: vpa(1000.0, 100.0) == 10.0; num_acoes 0/None -> None.
    - test_dpa_medio: [1,2,3] -> 2.0; com Nones pula; janela n limita aos últimos n; vazio -> None.
    - test_bazin: preco_teto_bazin(1.2) == 20.0 (DY 6%); dpa_med None/<=0 -> None.
    - test_upside: upside(30, 20) == 0.5; preço 0/None -> None.
    - test_retorno_periodo: construir pd.Series de Adj Close sintética (datas diárias ~2 anos,
      ex.: preço dobra) e checar retorno_periodo(serie, anos=1, 1000.0) contra o cálculo manual
      1000*ultimo/primeiro_na_janela; série curta (30 dias) com anos=5 -> None; série vazia -> None.
    - test_tabela_pares: montar 3 CompanyData mínimos (ticker/preco/PL/num_acoes/lucro etc.) e
      verificar metricas_par (P/L, P/VP, ROE, DY, valor_mercado coerentes) + tabela_pares marca o
      alvo + pares_suficientes True com 2 pares, False com só o alvo.
    Rodar a suíte inteira e garantir 296 (existentes) + novos verdes.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` sai com exit 0 e a contagem = 296 + nº de novos testes de test_lentes.py
    - test_lentes.py tem ao menos um teste por função pública de lentes.py (graham, vpa, dpa_medio, bazin, upside, retorno_periodo, tabela_pares)
    - Nenhum golden pré-existente falha (engine de método intacta)
  </acceptance_criteria>
  <done>tests/test_lentes.py cobre as 4 lentes com valores conhecidos e degradação; a suíte completa fica verde (296 + novos).</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest -q` verde (296 existentes + novos de test_lentes.py).
- `git diff --quiet src/analista/core/comparables.py src/analista/core/multiples.py src/analista/core/ddm.py src/analista/report/report.py` — módulos de método intocados.
- app.py NÃO foi tocado neste plano.
</verification>

<success_criteria>
- core/lentes.py entrega as 4 lentes como funções puras com degradação never-raise.
- Golden tests de valores conhecidos travam Graham (√22,5×LPA×VPA), Bazin (DPA médio÷6%), retorno (Adj Close) e comparador (5 métricas + alvo).
- Zero dependência nova; nenhum módulo de método alterado; 296 goldens verdes.
</success_criteria>

<output>
Após completar, criar `.planning/phases/19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-/19-01-SUMMARY.md`
</output>
