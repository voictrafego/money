---
phase: 19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - app.py
autonomous: true
requirements: [VAL-01, VAL-02, RET-01, PEER-01]

must_haves:
  truths:
    - "Na aba Analisar, o usuário vê o Preço-Justo de Graham como card ao lado do DDM, com upside vs preço atual"
    - "O usuário vê o Preço-Teto de Bazin como card, com upside vs preço atual"
    - "O usuário vê 'quanto teria rendido R$1.000' em 1a e 5a (reinvestimento via Adj Close)"
    - "O usuário vê a tabela comparadora de pares (P/L, P/VP, ROE, DY, Valor de Mercado) com a linha do ticker analisado destacada"
    - "Cada lente degrada para 'indisponível'/mensagem neutra sem quebrar a aba; copy exibe, nunca recomenda"
  artifacts:
    - path: "app.py"
      provides: "Render read-only das 4 lentes no branch Analisar (cards Graham/Bazin, retorno, comparador)"
      contains: "lentes"
  key_links:
    - from: "app.py (branch Analisar)"
      to: "src/analista/core/lentes.py"
      via: "lentes.preco_justo_graham/preco_teto_bazin/retorno_periodo/tabela_pares"
      pattern: "lentes\\."
    - from: "app.py (retorno)"
      to: "c.serie_precos_ajustada"
      via: "retorno_periodo(c.serie_precos_ajustada, anos)"
      pattern: "serie_precos_ajustada"
---

<objective>
Renderizar as 4 lentes da Fase 19 na aba **Analisar** como camada fina read-only: cards de
Graham e Bazin ao lado do valor intrínseco (DDM), bloco "quanto teria rendido" (1a/5a) e tabela
comparadora de pares com o ticker analisado destacado. `app.py` só LÊ a engine (`lentes.*` +
campos de CompanyData/AnaliseAcao) — nenhuma fórmula na view, nenhum recálculo de método.

Purpose: entregar ao usuário as features de alto apelo (Graham, Bazin, retorno histórico,
comparador) que motivaram a fase, mantendo a regra locked `app.py` read-only e a fronteira
"exibe, nunca recomenda".
Output: branch `if modo.startswith("Analisar")` de app.py enriquecido, read-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-/19-CONTEXT.md
@.planning/phases/19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-/19-01-PLAN.md

<interfaces>
<!-- Engine que a UI LÊ (criada nos Planos 01/02). Zero fórmula na view. -->

De src/analista/core/lentes.py (Plano 01):
- preco_justo_graham(lpa, vpa) -> Optional[float]     # None quando LPA/VPA <=0
- vpa(patrimonio_liquido, num_acoes) -> Optional[float]
- dpa_medio(dpas, n=5) -> Optional[float]
- preco_teto_bazin(dpa_med, dy_minimo=0.06) -> Optional[float]
- upside(referencia, preco_atual) -> Optional[float]
- retorno_periodo(serie_adj, anos, valor_inicial=1000.0) -> Optional[float]
- metricas_par(c) -> ParComparavel ; tabela_pares(metricas_ou_companies, ticker_alvo) -> list
- pares_suficientes(metricas) -> bool
- ParComparavel(ticker, pl, pvp, roe, dy, valor_mercado, alvo)

De src/analista/core/fundamentals.py — CompanyData `c` (já em escopo no branch):
- c.lpa_valuation(), c.patrimonio_liquido, c.num_acoes, c.ultimo_ano()
- c.dpa(ano), c.anos_ordenados(), c.serie_precos_ajustada (Adj Close 5a, Plano 02)
- c.preco_atual

De app.py (helpers já existentes; usar, não recriar):
- montar(ticker, ANO_BASE, N_ANOS)  # @st.cache_data. NOTA: PEER-01 é a ÚNICA exceção à regra "zero rede nova": buscar pares NÃO-cacheados dispara fetch (Yahoo/CVM), exatamente como a aba Ranking já faz hoje (app.py:1109-1118). É comportamento intencional e aceito (não é violação). As outras 3 lentes (Graham/Bazin/retorno) permanecem 100% sem rede nova.
- fmt_rs(x), fmt_pct(x), fmt_num(x), esc_md(s), h("chave_de_ajuda")
- a.preco_atual, a.vmin, a.vmax (AnaliseAcao já calculada no branch, ~linha 779)

Âncora de inserção: branch `if modo.startswith("Analisar")` (app.py ~linha 749); a linha de
métricas m1..m5 está ~linha 795-802; o gráfico e as tabs (Múltiplos/Valuation/Fundamentos)
vêm depois (~linha 986+).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cards Graham + Bazin ao lado do DDM (VAL-01, VAL-02)</name>
  <read_first>
    - app.py branch Analisar (linha 749-1044): entender m1..m5, a variável `a` (AnaliseAcao) e `c` (CompanyData), uso de fmt_rs/fmt_pct/esc_md/h
    - src/analista/core/lentes.py (preco_justo_graham, vpa, dpa_medio, preco_teto_bazin, upside)
    - .planning/.../19-CONTEXT.md (VAL-01/VAL-02: fórmulas, disclaimers de limitação)
  </read_first>
  <action>
    No branch Analisar, LOGO APÓS a linha de métricas m1..m5 (~linha 802) e antes do gráfico,
    inserir uma seção read-only "Lentes de referência (além do DDM)" com dois cards lado a lado
    (usar st.columns). NÃO calcular fórmula em app.py — só chamar lentes.*.

    Graham (VAL-01): ult = c.ultimo_ano(); vpa_val = lentes.vpa(c.patrimonio_liquido.get(ult),
    c.num_acoes.get(ult)); graham = lentes.preco_justo_graham(c.lpa_valuation(), vpa_val).
    Se graham não-None: st.metric("Preço-Justo (Graham)", fmt_rs(graham),
    delta=fmt_pct(lentes.upside(graham, a.preco_atual)) + " vs preço", delta_color="off",
    help=h(...) ou caption explicando √(22,5×LPA×VPA)). Se None: exibir "indisponível" + caption
    de limitação ("fórmula não vale para empresa sem lucro/PL positivo" — VAL-01 degradação).

    Bazin (VAL-02): dpas = [c.dpa(ano) for ano in c.anos_ordenados()]; dpa_med =
    lentes.dpa_medio(dpas, n=5); bazin = lentes.preco_teto_bazin(dpa_med). Se não-None:
    st.metric("Preço-Teto (Bazin)", fmt_rs(bazin), delta=fmt_pct(lentes.upside(bazin,
    a.preco_atual)) + " vs preço", delta_color="off", caption "DPA médio 5a ÷ DY-mínimo 6%").
    Se None: "indisponível" + caption "só vale para boas pagadoras de dividendos".

    Adicionar `from analista.core import lentes` no topo de app.py (junto dos outros imports do
    pacote). Copy 100% neutra: rótulos de referência/estudo, jamais "compre/venda". Usar esc_md
    em qualquer string derivada de dado externo.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast; ast.parse(open('app.py').read()); print('app.py OK')" && grep -c "preco_justo_graham\|preco_teto_bazin" app.py</automated>
  </verify>
  <acceptance_criteria>
    - app.py importa `lentes` e chama lentes.preco_justo_graham e lentes.preco_teto_bazin
    - Nenhuma aritmética de Graham/Bazin em app.py (`grep -c "22.5\|sqrt\|0.06" app.py` referente a essas lentes == 0 — a UI só lê o retorno da engine)
    - Ramo de degradação presente para ambos os cards (texto "indisponível")
    - `python -c "import ast; ast.parse(...)"` valida sintaxe
  </acceptance_criteria>
  <done>Graham e Bazin aparecem como cards ao lado do DDM com upside; degradam para "indisponível" com disclaimer, sem quebrar a aba.</done>
</task>

<task type="auto">
  <name>Task 2: "Quanto teria rendido" (RET-01) + comparador de pares (PEER-01)</name>
  <read_first>
    - app.py branch Analisar + branch Ranking (~linha 1105-1141: padrão de montar() por ticker + tabela de comparáveis)
    - src/analista/core/lentes.py (retorno_periodo, metricas_par, tabela_pares, pares_suficientes)
    - src/analista/core/fundamentals.py (c.serie_precos_ajustada — Plano 02)
  </read_first>
  <action>
    RET-01 — bloco "Quanto teria rendido": adicionar (numa nova sub-seção ou dentro da tab
    "Múltiplos & Crescimento", à discrição) o cálculo read-only:
    r1 = lentes.retorno_periodo(c.serie_precos_ajustada, anos=1);
    r5 = lentes.retorno_periodo(c.serie_precos_ajustada, anos=5).
    Exibir para cada janela não-None: "R$ 1.000 há 1 ano valeriam <fmt_rs(r1)> hoje (com
    dividendos reinvestidos)"; idem 5 anos. Janela com None (histórico insuficiente): OCULTAR
    aquela linha (RET-01 degradação — não exibir "indisponível" quebrado). Se ambas None: caption
    "histórico de preços insuficiente para o cálculo de rentabilidade". Incluir a nota honesta
    "rentabilidade passada não garante retorno futuro" (caption).

    PEER-01 — comparador de pares: numa nova sub-seção "Comparador de pares (contexto)" (expander),
    reusar o padrão do Ranking. Campo st.text_input de comparáveis EDITÁVEL pelo usuário (mesma UX do
    Ranking) com uma lista default genérica hardcoded "TAEE11, EGIE3, CMIG4, ALUP11, CPFE3" — NÃO tentar
    lookup dinâmico de setor→tickers (não existe índice reverso; data/ticker_map.json é só ticker→CD_CVM).
    SEMPRE incluir o ticker_ativo na lista. Para cada ticker:
    cp = montar(t, ANO_BASE, N_ANOS) (cache — reuso, o ticker analisado já está em cache); coletar
    os CompanyData válidos. metricas = [lentes.metricas_par(cp) for cp válidos];
    tabela = lentes.tabela_pares(metricas, ticker_ativo). Chamar lentes.pares_suficientes(tabela)
    APÓS a marcação (usa o flag .alvo p/ excluir o próprio ticker da contagem de ≥2 pares — não passar a lista pré-tag `metricas`).
    Se suficiente: montar um pd.DataFrame com colunas Ticker, P/L, P/VP, ROE, DY, Valor de Mercado — esta última
    escalada para R$ bi (valor / 1e9, formato "R$ X,X B"). Destacar (negrito/realce) a linha onde .alvo é True.
    formatadas (fmt_num/fmt_pct/fmt_rs) e DESTACAR a linha alvo (ex.: prefixo "➤ " no ticker OU
    st.dataframe com column_config/styler realçando alvo=True). Senão: st.info neutro "pares
    insuficientes do mesmo setor para comparar" (PEER-01 degradação). NÃO emitir veredito/nota nem
    ordenar por ranking — só contexto (PEER-01: não recomenda).

    Toda string derivada de ticker/dado externo passa por esc_md. app.py permanece read-only.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast; ast.parse(open('app.py').read()); print('OK')" && grep -c "retorno_periodo\|tabela_pares\|pares_suficientes\|serie_precos_ajustada" app.py</automated>
  </verify>
  <acceptance_criteria>
    - app.py chama lentes.retorno_periodo(c.serie_precos_ajustada, ...) para janelas 1a e 5a
    - app.py chama lentes.tabela_pares + lentes.pares_suficientes e destaca a linha do ticker analisado
    - Colunas da tabela de pares: Ticker, P/L, P/VP, ROE, DY, Valor de Mercado
    - Degradação: janela de retorno None é ocultada; pares insuficientes -> st.info neutro (sem exceção)
    - Nenhum "compre/venda"/veredito na seção de pares (`grep -i "compre\|venda\|recomend" app.py` não retorna linha nova nessas seções)
  </acceptance_criteria>
  <done>Retorno histórico (1a/5a via Adj Close) e comparador de pares com alvo destacado renderizam read-only, degradando sem quebrar e sem recomendar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → app (text_input ticker/comparáveis) | tickers digitados entram no fetch/montar e nas strings renderizadas |
| Yahoo/CVM → engine → UI | dados externos (nome, setor, preços) chegam à camada de render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering/Injection | app.py render de strings de ticker/nome via markdown | mitigate | passar toda string derivada de dado externo por `esc_md` antes de st.markdown (padrão já usado no branch) |
| T-19-02 | DoS | comparador chama montar() por comparável | mitigate | montar() é @st.cache_data (1 fetch/ticker/intervalo); limitar a lista de comparáveis a poucos tickers no default; degrada sem quebrar se um ticker falhar |
| T-19-03 | Info disclosure | nenhum dado sensível/segredo nesta superfície | accept | app custo-zero, dados públicos de mercado; sem PII/credenciais |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('app.py').read())"` — app.py sintaticamente válido.
- app.py só LÊ lentes.* e campos de c/a; nenhuma fórmula (22.5/sqrt/0.06/÷DY) na view.
- Nenhum outro branch (Garimpar/Ranking/Swing/Início) alterado; método/veredito intactos.
</verification>

<success_criteria>
- As 4 lentes aparecem na aba Analisar (Graham+Bazin cards, retorno 1a/5a, comparador com alvo destacado).
- Cada lente degrada graciosamente; copy exibe, nunca recomenda; app.py read-only.
</success_criteria>

<output>
Após completar, criar `.planning/phases/19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-/19-03-SUMMARY.md`
</output>
