---
phase: 13-piv-s-contexto-de-tend-ncia-e-n-veis
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - src/analista/core/indicators.py
  - tests/test_indicators.py
autonomous: true
requirements: [TREND-01, TREND-02]
must_haves:
  truths:
    - "A engine rotula a tendência do ticker no diário como alta/baixa/lateral via sequência de Dow sobre pivôs"
    - "O desempate de sequência ambígua usa MM/ADX já existentes e cai para lateral (D-05)"
    - "A engine deriva o semanal por resample W-FRI do diário (sem nova rede) e rotula o alinhamento semanal→diário (D-04)"
    - "Conflito multi-TF é rotulado (conflito) mas NUNCA bloqueia/zera o setup (apenas será modulador do score na Fase 15) (D-06)"
  artifacts:
    - path: "src/analista/core/indicators.py"
      provides: "dataclass ContextoTendencia + função _contexto (Dow + resample W-FRI + alinhamento) + campo aditivo em SinaisTecnicos"
      contains: "W-FRI"
    - path: "tests/test_indicators.py"
      provides: "goldens de Dow (alta/baixa/lateral), alinhamento (alinhado_alta/baixa/conflito) e degradação"
      contains: "dow"
  key_links:
    - from: "indicators._contexto"
      to: "_pivos + adx_wilder + SMA"
      via: "consumo dos pivôs do plano 01 e dos rótulos de força/MM existentes"
      pattern: "_pivos\\(|adx_wilder\\("
    - from: "indicators._contexto"
      to: "resample W-FRI do ohlc_ajustado"
      via: "derivação do semanal sem nova chamada de rede"
      pattern: "resample\\(.W-FRI"
---

<objective>
Derivar o **contexto de tendência** sobre os pivôs do plano 01: rótulo de Dow no diário (alta/baixa/lateral — TREND-01) e **alinhamento semanal→diário** (alinhado_alta/alinhado_baixa/conflito — TREND-02), com o semanal obtido por resample W-FRI do próprio diário (D-04, sem nova rede).

Purpose: Dow + multi-TF é o contexto que domina o score da Fase 15. Conflito multi-TF deve apenas MODULAR (penalizar) e nunca bloquear (D-06) — então aqui ele é só um rótulo aditivo e auditável.
Output: `ContextoTendencia` dataclass + `_contexto`, campo aditivo em `SinaisTecnicos`, novos goldens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-CONTEXT.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-PATTERNS.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-01-SUMMARY.md

<interfaces>
<!-- Do plano 01 (já implementado quando este rodar): -->
`SinaisTecnicos.pivos: Pivos = None` com campos `pivot_high`, `pivot_low` (pd.Series, preço nos pivôs confirmados, NaN nas demais), `ultimo_topo`, `ultimo_fundo` (Number), `n` (int).

<!-- Reuso obrigatório (D-05: NÃO reimplementar ADX/SMA): -->
- `adx_wilder(ohlc, length) -> (adx, pdi, ndi)` (force_adx limiares: <20 sem_tendencia, >25 forte).
- SMAs em `_tendencia` / `Tendencia` (sma20/sma50/sma200, posicao_mm200 "acima"/"abaixo").
- `regressao_trailing(close, win) -> (slope_ann, r2)` (inclinação anualizada %/ano causal).

<!-- Resample W-FRI canônico (já golden-coberto em test_report.py::test_resample_semanal_w_fri): -->
ohlc.resample("W-FRI").agg({"Open":"first","High":"max","Low":"min","Close":"last"}).dropna()
Guard antes do resample: só em DatetimeIndex com as colunas OHLC; senão degrada.

Vocabulário de label estável (strings minúsculas; "indisponivel" para degradação): nunca copy natural.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rótulo de Dow no diário (sequência de pivôs + desempate MM/ADX) — TREND-01</name>
  <files>src/analista/core/indicators.py, tests/test_indicators.py</files>
  <read_first>
    - src/analista/core/indicators.py (`_forca` lines 330-353 para reuso de `adx_wilder`/limiares; `_tendencia` lines 117-154 para SMAs e o padrão de label discreto; o novo `_pivos`/`Pivos` do plano 01)
    - tests/test_indicators.py (fixtures `_frame_ohlc`/`_cfg_ind`; goldens de força/tendência existentes)
    - .planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-CONTEXT.md (D-05)
  </read_first>
  <behavior>
    - Sequência de topos/fundos CONFIRMADOS (do `Pivos`): topos crescentes E fundos crescentes (HH+HL) → "alta"; topos decrescentes E fundos decrescentes (LH+LL) → "baixa".
    - Ambíguo (ex.: HH+LL, ou < 2 topos/2 fundos confirmados): desempatar pela inclinação da SMA/ADX já existentes — slope claramente positivo + ADX≥limiar → "alta"; negativo + ADX≥limiar → "baixa"; caso contrário → "lateral".
    - Frame curto/sem pivôs suficientes e sem ADX válido → "indisponivel".
    - Determinístico: mesma entrada → mesmo rótulo; rótulo é string do vocabulário {"alta","baixa","lateral","indisponivel"}.
  </behavior>
  <action>
    Adicionar dataclass `ContextoTendencia` (campos preenchidos parcialmente aqui, completados na Task 2): `dow_diario: str`, `alinhamento_mtf: str` (default este último será setado na Task 2). Implementar a lógica de Dow numa função/helper `_dow(pivos, ohlc, cfg) -> str` que: (a) extrai a sequência ordenada de topos confirmados (`pivot_high` não-NaN) e fundos confirmados (`pivot_low` não-NaN); (b) compara os 2 últimos topos e os 2 últimos fundos para HH/HL→"alta", LH/LL→"baixa"; (c) no ambíguo, desempata reusando `adx_wilder(ohlc, ind["adx_janela"])` + a inclinação (sinal de `regressao_trailing(close, ind["regressao_janela"])` ou slope da SMA50) — claro→"alta"/"baixa", senão→"lateral"; (d) degradação→"indisponivel". NÃO reimplementar ADX/SMA (D-05). Usar o vocabulário de label estável (lines 41-42, 54-56). Documentar no docstring a regra de Dow e o critério de desempate.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_indicators.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Golden com fixture HH/HL sintética → `dow_diario == "alta"`; LH/LL → `"baixa"`; série lateral/ruidosa sem ADX → `"lateral"`; frame curto → `"indisponivel"`.
    - `_dow` consome `adx_wilder`/SMA existentes (não há nova implementação de ADX/SMA — `grep` não introduz nova fórmula de ADX).
    - `.venv/bin/python -m pytest tests/ -q` 100% verde (baseline 202).
  </acceptance_criteria>
  <done>`dow_diario` rotulado deterministicamente a partir de pivôs com desempate por MM/ADX; goldens verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Alinhamento semanal→diário via resample W-FRI — TREND-02</name>
  <files>src/analista/core/indicators.py, tests/test_indicators.py</files>
  <read_first>
    - src/analista/core/indicators.py (o `_dow` da Task 1; `calcular` lines 406-425 para a montagem aditiva)
    - tests/test_indicators.py + (referência do idioma resample) o padrão W-FRI descrito em 13-PATTERNS.md §"Alinhamento semanal→diário"
    - .planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-CONTEXT.md (D-04, D-06)
  </read_first>
  <behavior>
    - Semanal = `ohlc.resample("W-FRI").agg({Open:first,High:max,Low:min,Close:last}).dropna()` do frame recebido; recalcular pivôs + Dow no semanal reusando `_pivos`/`_dow`.
    - Alinhamento: diário "alta" e semanal "alta" → "alinhado_alta"; ambos "baixa" → "alinhado_baixa"; qualquer divergência (incluindo um lado "lateral") → "conflito".
    - Conflito é apenas um RÓTULO (D-06): a função NÃO zera, NÃO levanta, NÃO altera os demais campos — a penalização do score é da Fase 15.
    - Guard: índice não-DatetimeIndex, sem colunas OHLC, ou semanal com poucas barras → "indisponivel" (cai no frame original / degrada, espelhando o guard de report.py).
  </behavior>
  <action>
    Completar `_contexto(ohlc, cfg) -> ContextoTendencia`: computar `dow_diario` via `_dow` no frame recebido; derivar o semanal por resample W-FRI (idioma exato do `report.py` lines 253-255), guardado por `isinstance(ohlc.index, pd.DatetimeIndex)` + colunas OHLC presentes (lines 246-252) — D-04 PROÍBE buscar `1wk` do Yahoo. Recomputar `_pivos`+`_dow` sobre o semanal e rotular `alinhamento_mtf` em {"alinhado_alta","alinhado_baixa","conflito","indisponivel"}. Montar `_contexto` dentro de `calcular` como campo aditivo opcional `contexto: ContextoTendencia = None` no fim de `SinaisTecnicos`. Documentar no docstring que conflito MODULA e nunca bloqueia (D-06).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_indicators.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Golden: diário alta + semanal alta → `alinhamento_mtf == "alinhado_alta"`; diário alta + semanal baixa/lateral → `"conflito"`; frame não-datetime → `"indisponivel"`.
    - Teste prova que em "conflito" os demais campos de `SinaisTecnicos` permanecem normalmente preenchidos (D-06: não bloqueia).
    - `grep -n "1wk" src/analista/core/indicators.py` vazio (D-04: sem fetch semanal separado).
    - `SinaisTecnicos.contexto` populado; `.venv/bin/python -m pytest tests/ -q` 100% verde (baseline 202).
  </acceptance_criteria>
  <done>`contexto` com `dow_diario` + `alinhamento_mtf` populado; resample W-FRI sem rede; goldens verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FrameOHLC (Fase 12) → indicators.calcular | Engine pura; sem rede, sem input direto. O resample W-FRI é transformação local do frame já validado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Denial | `_contexto` sobre frame sem DatetimeIndex ou semanal curto demais | mitigate | Guard `isinstance(index, DatetimeIndex)` + colunas OHLC antes do resample; degradação para "indisponivel" sem exceção (espelha report.py). |
| T-13-05 | Tampering | Conflito multi-TF bloqueando/zerando indevidamente o setup | mitigate | D-06 explícito no código e em golden: conflito é só rótulo; demais campos intactos. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -q` → 100% verde (baseline 202).
- `grep -n "1wk" src/analista/core/indicators.py` vazio.
</verification>

<success_criteria>
- `dow_diario` ∈ {alta,baixa,lateral,indisponivel} derivado de pivôs + desempate MM/ADX reusados.
- `alinhamento_mtf` ∈ {alinhado_alta,alinhado_baixa,conflito,indisponivel} via resample W-FRI; conflito não bloqueia.
- Contrato 100% aditivo; suíte verde.
</success_criteria>

<output>
Após completar, criar `.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-02-SUMMARY.md`.
</output>
