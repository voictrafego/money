---
phase: 13-piv-s-contexto-de-tend-ncia-e-n-veis
plan: 03
type: tdd
wave: 3
depends_on: [01]
files_modified:
  - src/analista/core/indicators.py
  - config.yaml
  - tests/test_indicators.py
autonomous: true
requirements: [LEVEL-01, VOL-01]
must_haves:
  truths:
    - "A engine produz S/R como faixas/zonas (clusters de pivôs por proximidade < k×ATR), nunca pontos exatos"
    - "Donchian 20/55 entra como faixa externa de referência para S/R"
    - "Níveis de preço (S/R) são calculados sobre o frame nominal quando ele é fornecido (D-02 herdado)"
    - "A família Volume (MM de volume + flag rompimento com volume) é adicionada de forma aditiva ao SinaisTecnicos"
    - "Frame sem coluna Volume / curto degrada graciosamente (zonas vazias, flag desligada) sem exceção"
  artifacts:
    - path: "src/analista/core/indicators.py"
      provides: "dataclass Niveis (S/R zonas) + dataclass Volume + clustering por k×ATR + param opcional ohlc_nominal em calcular"
      contains: "cluster_k"
    - path: "config.yaml"
      provides: "params cluster_k e volume_janela no bloco indicadores"
      contains: "volume_janela"
    - path: "tests/test_indicators.py"
      provides: "goldens de zonas S/R (cluster), Donchian externo, MM volume e flag de rompimento + degradação"
      contains: "suporte"
  key_links:
    - from: "indicators.calcular"
      to: "_niveis_sr (cluster de pivôs) + atr_wilder"
      via: "clustering por proximidade < cluster_k × ATR sobre pivôs confirmados"
      pattern: "cluster_k"
    - from: "indicators._volume"
      to: "donchian_sup + MM de volume"
      via: "flag rompimento com volume acima da média"
      pattern: "rompimento_com_volume"
---

<objective>
Produzir **zonas de Suporte/Resistência** (LEVEL-01) por clustering de pivôs com largura adaptada à volatilidade (proximidade < `cluster_k`×ATR), com Donchian 20/55 como faixa externa de referência; e adicionar a família **Volume** (VOL-01): MM de volume + flag booleana "rompimento com volume acima da média". Introduz o parâmetro opcional `ohlc_nominal` em `calcular` para honrar D-02 (níveis de preço sobre frame nominal) de forma aditiva.

Purpose: S/R em zonas (nunca pontos) é mais robusto entre papéis B3 distintos que um % fixo; o Volume confirma rompimentos. Ambos são aditivos ao contrato e alimentam a Fase 16 (overlays) e a Fase 14 (padrões).
Output: `Niveis` (S/R) + `Volume` dataclasses, param `ohlc_nominal`, novos params em config, novos goldens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-CONTEXT.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-PATTERNS.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-01-SUMMARY.md

<interfaces>
<!-- Do plano 01: -->
`SinaisTecnicos.pivos: Pivos` com `pivot_high`/`pivot_low` (pd.Series, preço nos pivôs confirmados, NaN nas demais), `ultimo_topo`/`ultimo_fundo` (Number), `n` (int).
`atr_wilder(ohlc, length) -> pd.Series` (ATR de Wilder, 1º válido no índice `length`); `Forca.atr` já preenchido.

<!-- Já existente em Canais (D-10: Donchian como faixa externa de S/R): -->
`Canais.donchian_sup_55`/`donchian_inf_55` (faixa longa 55, causal `.shift(1)`); `donchian_sup`/`donchian_inf` (20).

<!-- Entry-point a estender de forma aditiva: -->
`calcular(ohlc, cfg) -> SinaisTecnicos`. Guard de borda já roteia None/vazio/sem-colunas.
D-02 herdado (Fase 12): níveis de PREÇO usam o frame NOMINAL (`FrameOHLC.ohlc`); indicadores usam o split-adjusted (`FrameOHLC.ohlc_ajustado`).

Convenção de label estável (strings minúsculas; "indisponivel" para degradação). Volume golden precisa de coluna `Volume` na fixture — ver test_ingest_intraday._frame_5min como referência de OHLCV com Volume.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zonas de S/R por cluster de pivôs (k×ATR) + Donchian externo + param ohlc_nominal — LEVEL-01</name>
  <files>src/analista/core/indicators.py, config.yaml, tests/test_indicators.py</files>
  <read_first>
    - src/analista/core/indicators.py (`_canais` lines 185-252 para `donchian_sup_55`/`donchian_inf_55`; `calcular` lines 406-425 para introduzir `ohlc_nominal`; o `_pivos`/`Pivos` e `atr_wilder` do plano 01)
    - tests/test_indicators.py (`_cfg_ind`/`_frame_ohlc`; padrão de degradação graciosa)
    - .planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-CONTEXT.md (D-02 herdado, D-10)
  </read_first>
  <behavior>
    - Cluster: a partir dos preços de pivôs CONFIRMADOS (pivot_high + pivot_low), ordenar e fundir preços cuja distância < `cluster_k`×ATR_atual numa zona `(low, high)`. Resultado: lista de zonas (faixas), nunca pontos.
    - Classificação relativa ao close atual: zonas abaixo do close → suportes; acima → resistências (ordenadas por proximidade ao preço).
    - Donchian externo: `donchian_inf_55`/`donchian_sup_55` (ponta) entram como faixa externa de referência (campos dedicados na `Niveis`).
    - `ohlc_nominal` fornecido → pivôs e zonas calculados sobre o nominal; ausente → usa o próprio `ohlc` (goldens existentes inalterados).
    - Degradação: sem pivôs confirmados / ATR NaN / frame curto → listas de zonas vazias e referências None, sem exceção.
  </behavior>
  <action>
    Adicionar `ohlc_nominal: pd.DataFrame | None = None` como parâmetro OPCIONAL ao final de `calcular`; internamente `nominal = ohlc_nominal if ohlc_nominal is not None else ohlc` e rotear as famílias de PREÇO (pivôs + níveis) por `nominal`, mantendo indicadores/contexto por `ohlc` (split-adjusted) — D-02. Isso é aditivo: chamadas existentes (goldens) não passam `ohlc_nominal` e continuam idênticas. Adicionar dataclass `Niveis` com (parte preenchida aqui, resto no plano 04): `suportes: list = field(default_factory=list)` e `resistencias: list = field(default_factory=list)` (cada elemento uma tupla `(low, high)` de zona), `donchian_externo_inf: Number = None`, `donchian_externo_sup: Number = None`. Implementar `_niveis_sr(pivos, ohlc, atr, donchian_inf_55, donchian_sup_55, cfg)`: ler `k = ind["cluster_k"]`, pegar o último ATR válido (`atr.dropna().iloc[-1]`), agrupar os preços de pivôs por gap < k×ATR em zonas `(min,max)`, classificar vs `close.iloc[-2]` (barra fechada, D-04 herdado) em suportes/resistências, e setar as referências Donchian externas a partir da ponta de `donchian_*_55`. Montar `Niveis` dentro de `calcular` como campo aditivo opcional `niveis: Niveis = None`. Em config.yaml acrescentar `cluster_k: 1.0` ao bloco `indicadores:` com comentário "largura da zona S/R = k×ATR — adapta à volatilidade do papel (D-10); Donchian 20/55 já entra como faixa externa". Degradação graciosa (listas vazias / None).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_indicators.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Golden: fixture com pivôs próximos (< k×ATR) os funde numa única zona `(low,high)` com `low < high` (faixa, não ponto); pivôs distantes geram zonas separadas.
    - Golden prova que `suportes` ficam abaixo e `resistencias` acima do close da barra fechada, e que `donchian_externo_sup/inf` batem a ponta de `donchian_*_55`.
    - Teste prova que `calcular(ohlc_ajustado, cfg)` (sem `ohlc_nominal`) é idêntico ao comportamento anterior, e que passar `ohlc_nominal` diferente muda os preços das zonas (D-02).
    - Degradação: frame sem pivôs / curto → `suportes==[]` e `resistencias==[]`, sem exceção.
    - `_cfg_ind()` expõe `indicadores.cluster_k`. `.venv/bin/python -m pytest tests/ -q` 100% verde (baseline 202).
  </acceptance_criteria>
  <done>`SinaisTecnicos.niveis` com zonas S/R (faixas) + Donchian externo; param `ohlc_nominal` aditivo; goldens verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Família Volume — MM de volume + flag rompimento com volume — VOL-01</name>
  <files>src/analista/core/indicators.py, config.yaml, tests/test_indicators.py</files>
  <read_first>
    - src/analista/core/indicators.py (`_canais` `rompimento_donchian` lines 198-211 — base da flag de rompimento; padrão `min_periods=janela`; `calcular` para montagem)
    - tests/test_indicators.py + tests/test_ingest_intraday.py (`_frame_5min` lines ~26-32: referência de fixture OHLCV com coluna `Volume`)
    - .planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-CONTEXT.md (D-11)
  </read_first>
  <behavior>
    - `volume_mm` = média móvel de volume com janela `volume_janela` e `min_periods=volume_janela` (NaN com histórico curto, não valor parcial).
    - `rompimento_com_volume` (bool) = True somente quando a barra FECHADA (`iloc[-2]`) rompe a banda Donchian superior (`rompimento_donchian == "nova_maxima"` na lógica causal) E `Volume[barra fechada] > volume_mm[barra fechada]`.
    - Sem coluna `Volume` no frame, ou frame curto → `volume_mm = None` e `rompimento_com_volume = False` (degradação), sem exceção.
    - 100% aditivo: nenhum campo existente de `SinaisTecnicos` muda.
  </behavior>
  <action>
    Adicionar dataclass `Volume` com `volume_mm: pd.Series = None` e `rompimento_com_volume: bool = False`. Implementar `_volume(ohlc, cfg) -> Volume`: se `"Volume"` não está nas colunas → retornar `Volume()` (degradado); senão `vmm = ohlc["Volume"].rolling(ind["volume_janela"], min_periods=ind["volume_janela"]).mean()`; flag = (rompimento da Donchian superior na barra fechada) AND (volume da barra fechada > `vmm` na barra fechada), protegendo contra NaN. Reusar a lógica de rompimento Donchian causal já existente em `_canais` (não duplicar o `.shift(1)`; computar a partir do `donchian_sup` já disponível ou reproduzir o mesmo idioma `.shift(1)`). Montar `_volume` dentro de `calcular` como campo aditivo opcional `volume: Volume = None`. Em config.yaml acrescentar `volume_janela: 20` ao bloco `indicadores:` com comentário "janela da MM de volume p/ confirmar rompimentos (VOL-01)".
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_indicators.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Golden com fixture OHLCV (coluna `Volume`) onde a barra fechada rompe a Donchian superior com volume acima da média → `volume.rompimento_com_volume is True`; rompimento sem volume → `False`.
    - Golden de degradação: frame SEM coluna `Volume` → `volume.volume_mm is None` e `rompimento_com_volume is False`, sem exceção; frame curto → `volume_mm` todo-NaN.
    - `volume_mm` usa `min_periods == volume_janela`.
    - `_cfg_ind()` expõe `indicadores.volume_janela`. `.venv/bin/python -m pytest tests/ -q` 100% verde (baseline 202 — nenhum golden existente, que não tem coluna Volume, quebra).
  </acceptance_criteria>
  <done>`SinaisTecnicos.volume` populado de forma aditiva; flag de rompimento com volume golden-coberta; suíte verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FrameOHLC (Fase 12) → indicators.calcular | Engine pura; sem rede/usuário. `ohlc_nominal` é outro frame já validado pela Fase 12, não input externo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Denial | `_niveis_sr` com ATR NaN / sem pivôs / divisão na largura da zona | mitigate | Checagem de `atr.dropna()` vazio e ausência de pivôs → zonas vazias; sem div-zero (k×ATR só com ATR válido). |
| T-13-07 | Tampering | Frame sem coluna `Volume` (goldens existentes) levantando KeyError | mitigate | `_volume` checa `"Volume" in ohlc.columns` antes de tudo; degrada para `Volume()` default. |
| T-13-08 | Tampering | Repaint da flag de volume por usar a barra viva | mitigate | Flag avaliada na barra FECHADA (`iloc[-2]`, D-04 herdado) + Donchian causal `.shift(1)`. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -q` → 100% verde (baseline 202; goldens existentes sem coluna Volume não quebram).
- Zonas S/R são faixas `(low,high)` com `low<high`, nunca pontos.
</verification>

<success_criteria>
- `SinaisTecnicos.niveis` com `suportes`/`resistencias` como zonas + Donchian externo; clustering por k×ATR.
- `SinaisTecnicos.volume` com MM de volume + flag de rompimento com volume, aditivo e degradável.
- `config.yaml` ganha `cluster_k` e `volume_janela`; param `ohlc_nominal` opcional honra D-02; suíte verde.
</success_criteria>

<output>
Após completar, criar `.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-03-SUMMARY.md`.
</output>
