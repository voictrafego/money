---
phase: 13-piv-s-contexto-de-tend-ncia-e-n-veis
plan: 04
type: tdd
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - src/analista/core/indicators.py
  - tests/test_indicators.py
autonomous: true
requirements: [LEVEL-02, LEVEL-03, LEVEL-04, RR-01]
must_haves:
  truths:
    - "A engine produz zona de entrada por retração de Fibonacci (38,2/50/61,8%) ancorada em dois pivôs documentados"
    - "A engine produz alvo por extensão de Fibonacci 161,8% no mesmo impulso confirmado"
    - "O stop técnico é o mais conservador (mais distante) entre o swing estrutural e ATR×m"
    - "A relação Risco:Retorno é uma razão formatada (ex.: '1 : 2,5') e degrada para 'indisponivel' quando o risco é zero/indefinido (sem infinito)"
    - "Os dois pivôs âncora ficam documentados no contrato para auditabilidade"
  artifacts:
    - path: "src/analista/core/indicators.py"
      provides: "campos de níveis Fibonacci (entrada/alvo), stop técnico e R:R em Niveis + função de níveis geométricos"
      contains: "risco_retorno"
    - path: "tests/test_indicators.py"
      provides: "goldens de Fibonacci ancorado, stop conservador, R:R formatado e degradação para indisponivel"
      contains: "risco_retorno"
  key_links:
    - from: "indicators._niveis_fib"
      to: "pivos.ultimo_topo/ultimo_fundo + contexto.dow_diario"
      via: "ancoragem no último impulso confirmado (par de pivôs)"
      pattern: "ultimo_topo|ultimo_fundo"
    - from: "indicators (stop / R:R)"
      to: "atr_wilder + swing estrutural"
      via: "stop = mais conservador entre swing e ATR×m; R:R com np.errstate"
      pattern: "stop_atr_m|errstate"
---

<objective>
Fechar os níveis geométricos sobre os pivôs e o contexto: **zona de entrada** por retração de Fibonacci (38,2/50/61,8% — LEVEL-02), **alvo** por extensão de Fibonacci 161,8% (LEVEL-04), **stop técnico** = o mais conservador entre swing estrutural e ATR×m (LEVEL-03), e a **relação Risco:Retorno** formatada com degradação para "indisponivel" (RR-01). Tudo ancorado no último impulso confirmado, com os dois pivôs âncora documentados (D-07).

Purpose: São os níveis de estudo que a Fase 16 renderiza e que a Fase 15 usa no score (R:R como gate). Devem ser determinísticos, no-repaint (ancorados em pivôs CONFIRMADOS) e jamais propagar infinito/exceção. "Exibe, nunca recomenda" continua sendo gate da Fase 16 — aqui são apenas níveis numéricos neutros.
Output: extensão da dataclass `Niveis` (entrada/stop/alvo/âncora/R:R) + função de níveis geométricos, novos goldens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-CONTEXT.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-PATTERNS.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-01-SUMMARY.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-02-SUMMARY.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-03-SUMMARY.md

<interfaces>
<!-- Acumulado dos planos 01-03 (já implementado quando este rodar): -->
`SinaisTecnicos.pivos: Pivos` → `pivot_high`/`pivot_low` (pd.Series), `ultimo_topo`/`ultimo_fundo` (Number), `n`.
`SinaisTecnicos.contexto: ContextoTendencia` → `dow_diario` ∈ {"alta","baixa","lateral","indisponivel"}, `alinhamento_mtf`.
`SinaisTecnicos.niveis: Niveis` → já tem `suportes`/`resistencias` (zonas), `donchian_externo_inf/sup`. ESTENDER esta mesma dataclass.
`atr_wilder(ohlc, length) -> pd.Series`; `Forca.atr` preenchido. `ind["stop_atr_m"]` (1.5) já em config.
`calcular(ohlc, cfg, ohlc_nominal=None)` — níveis de preço usam o frame nominal (D-02).

<!-- Proteção div-zero (idioma do módulo): -->
`with np.errstate(divide="ignore", invalid="ignore"): ...` (ver rsi_wilder lines 172-177, _canais lines 231-232).

Vocabulário de label: R:R é string formatada com vírgula decimal BR (ex.: "1 : 2,5") OU "indisponivel".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fibonacci ancorado no último impulso — entrada (retração) + alvo (extensão) — LEVEL-02, LEVEL-04</name>
  <files>src/analista/core/indicators.py, tests/test_indicators.py</files>
  <read_first>
    - src/analista/core/indicators.py (`Niveis` dataclass e `_niveis_sr` do plano 03; `Pivos`/`_pivos` do plano 01; `ContextoTendencia`/`_contexto` do plano 02; `calcular`)
    - tests/test_indicators.py (`_frame_ohlc`/`_cfg_ind`; gate no-repaint dos planos anteriores)
    - .planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-CONTEXT.md (D-07)
  </read_first>
  <behavior>
    - Âncora = último impulso CONFIRMADO = o par de pivôs mais recente. Em tendência de alta (dow_diario "alta"): fundo→topo (último fundo confirmado e o topo confirmado seguinte). Em baixa: topo→fundo. `pivos_ancora` documenta os dois (timestamps + preços).
    - Retração (entrada) em alta: níveis = topo − (topo−fundo)×{0.382, 0.5, 0.618}; `entrada_zona = (nível 61,8%, nível 38,2%)` como faixa; `fib_retracoes` guarda os 3 níveis nomeados.
    - Extensão (alvo) em alta: `alvo = fundo + (topo−fundo)×1.618`. Em baixa, espelhar (retração medida a partir do fundo para cima; alvo projetado para baixo).
    - Degradação: sem par de pivôs confirmado, ou `dow_diario` ∈ {"lateral","indisponivel"} → `entrada_zona=None`, `alvo=None`, `pivos_ancora=None` (níveis direcionais não fazem sentido sem tendência) — sem exceção.
  </behavior>
  <action>
    Estender a dataclass `Niveis` (do plano 03) com campos aditivos: `entrada_zona: tuple = None` (faixa 61,8%↔38,2%), `fib_retracoes: dict = None` (chaves "382"/"500"/"618" → preço), `alvo: Number = None`, `pivos_ancora: dict = None` (ex.: `{"fundo_ts","fundo_preco","topo_ts","topo_preco"}` — auditabilidade D-07). Implementar `_niveis_fib(pivos, contexto, ohlc_nominal, cfg)`: selecionar o par de pivôs mais recente coerente com `contexto.dow_diario` (alta→fundo então topo posterior; baixa→topo então fundo posterior), calcular retrações 38,2/50/61,8% e extensão 161,8% sobre `(topo−fundo)`, preencher `entrada_zona`/`fib_retracoes`/`alvo`/`pivos_ancora`. Usar PREÇOS do frame nominal (D-02). Chamar `_niveis_fib` dentro de `calcular`, mesclando no objeto `Niveis` já construído pelo `_niveis_sr`. Documentar no docstring qual par de pivôs ancora e por quê (no-repaint: pivôs confirmados nunca mudam → níveis nunca repaint). Degradação para None quando lateral/indisponivel/sem par.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_indicators.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Golden com impulso de alta sintético (fundo F→topo T confirmados): `entrada_zona` ≈ `(T-(T-F)*0.618, T-(T-F)*0.382)` (`abs` tolerante), `fib_retracoes["500"]` ≈ `T-(T-F)*0.5`, `alvo` ≈ `F+(T-F)*1.618`.
    - Golden de baixa espelha corretamente a direção.
    - `pivos_ancora` traz os dois pivôs (timestamps + preços) usados — auditável.
    - Degradação: `dow_diario == "lateral"` ou sem par confirmado → `entrada_zona is None and alvo is None`, sem exceção.
    - `.venv/bin/python -m pytest tests/ -q` 100% verde (baseline 202).
  </acceptance_criteria>
  <done>`Niveis` com entrada (retração) + alvo (extensão) Fibonacci ancorados e documentados; goldens verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stop técnico (mais conservador entre swing e ATR×m) + Risco:Retorno — LEVEL-03, RR-01</name>
  <files>src/analista/core/indicators.py, tests/test_indicators.py</files>
  <read_first>
    - src/analista/core/indicators.py (o `_niveis_fib` da Task 1; `atr_wilder`/`Forca.atr` do plano 01; idioma `np.errstate` em `rsi_wilder` lines 172-177 e `_canais` lines 231-232)
    - tests/test_indicators.py (goldens dos planos anteriores; helpers)
    - .planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-CONTEXT.md (D-08, D-09)
  </read_first>
  <behavior>
    - Stop em alta: `swing` = preço do fundo âncora (swing-low estrutural); `atr_stop` = entrada_ref − ATR×m; `stop = min(swing, atr_stop)` (o mais distante/conservador para baixo). Em baixa, espelhar com `max` (mais distante para cima). `entrada_ref` = ponto 50% da `entrada_zona` (ou close da barra fechada se zona ausente).
    - `m = cfg["indicadores"]["stop_atr_m"]`; ATR = último valor válido de `atr_wilder`.
    - R:R: `risco = |entrada_ref − stop|`, `retorno = |alvo − entrada_ref|`, `razao = retorno/risco` formatada como `"1 : {razao com vírgula decimal}"` (ex.: "1 : 2,5"). Risco == 0, NaN, ou alvo/stop ausentes → `risco_retorno = "indisponivel"` (NUNCA infinito/divisão por zero — usar `np.errstate`).
    - Degradação: sem `entrada_zona`/`alvo` (lateral/indisponivel) → `stop=None`, `risco_retorno="indisponivel"`, sem exceção.
  </behavior>
  <action>
    Estender `Niveis` com `stop: Number = None` e `risco_retorno: str = "indisponivel"`. Implementar o cálculo do stop dentro do fluxo de níveis (após `_niveis_fib`): obter `m = ind["stop_atr_m"]` e o último ATR válido (`atr.dropna().iloc[-1]`); `swing` = preço do pivô âncora estrutural; `atr_stop = entrada_ref ∓ m*ATR`; `stop` = o mais conservador (mais distante de `entrada_ref`) — `min` em alta, `max` em baixa. Calcular R:R com `np.errstate(divide="ignore", invalid="ignore")`: se `risco<=0` ou NaN ou `alvo`/`stop` None → `"indisponivel"`; senão formatar `"1 : {valor}"` com vírgula decimal BR (1 casa). NÃO recalcular ATR (reusar `atr_wilder` — D-08). Documentar a regra "mais conservador" e a proteção div-zero no docstring.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_indicators.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Golden onde o swing-low é mais distante que ATR×m → `stop == swing`; onde ATR×m é mais distante → `stop == atr_stop` (prova do "mais conservador", D-08).
    - Golden de R:R: entrada/stop/alvo conhecidos → `risco_retorno == "1 : 2,5"` (formato com vírgula) para razão 2,5.
    - Golden de borda: `risco == 0` (stop == entrada_ref) → `risco_retorno == "indisponivel"`; `np.isinf` nunca aparece. `dow lateral` → `stop is None` e `risco_retorno == "indisponivel"`.
    - `grep -n "errstate" src/analista/core/indicators.py` cobre o cálculo de R:R.
    - `.venv/bin/python -m pytest tests/ -q` 100% verde (baseline 202).
  </acceptance_criteria>
  <done>`Niveis` com `stop` (mais conservador) e `risco_retorno` formatado/degradável; nunca infinito; goldens verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FrameOHLC (Fase 12) → indicators.calcular | Engine pura; sem rede/usuário. Níveis derivam de pivôs já confirmados (no-repaint). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Denial | R:R com `risco == 0` → divisão por zero / infinito vazando para a Fase 15/16 | mitigate | D-09: `np.errstate` + guarda `risco<=0 or NaN` → string "indisponivel"; golden de borda obrigatório. |
| T-13-10 | Tampering | Níveis Fibonacci repaint por ancorar em pivô não confirmado | mitigate | Âncora SEMPRE em par de pivôs CONFIRMADOS (D-07); pivôs confirmados são imutáveis (gate do plano 01). |
| T-13-11 | Denial | Tendência lateral/indisponível produzindo níveis direcionais sem sentido | mitigate | Degradação explícita: `entrada_zona/alvo/stop = None`, `risco_retorno = "indisponivel"` quando dow ∈ {lateral, indisponivel}. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -q` → 100% verde (baseline 202).
- R:R nunca infinito; `risco<=0` → "indisponivel" (golden de borda verde).
- Níveis Fibonacci ancorados em pivôs confirmados (no-repaint), com âncora documentada.
</verification>

<success_criteria>
- `Niveis` com `entrada_zona`/`fib_retracoes`/`alvo` (Fibonacci ancorado), `stop` (mais conservador), `pivos_ancora` (auditável) e `risco_retorno` formatado/degradável.
- Contrato 100% aditivo; suíte verde; nenhuma divisão por zero/infinito.
</success_criteria>

<output>
Após completar, criar `.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-04-SUMMARY.md`.
</output>
