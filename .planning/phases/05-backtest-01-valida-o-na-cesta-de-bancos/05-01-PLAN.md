---
phase: 05-backtest-01-valida-o-na-cesta-de-bancos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/capturar_snapshot_bancos.py
  - tests/fixtures/snapshot_bancos_2026-07-12.yaml
autonomous: true
requirements: [VAL-01]

must_haves:
  truths:
    - "Um snapshot congelado (VPA/ROE/preço/beta raw + rf_local) dos 4 bancos existe versionado, com data-base carimbada"
    - "Reconstruir os 4 CompanyData do snapshot e rodar analisar_acao offline reproduz o intrínseco RIM da Fase 4 (ITUB4 ~R$32,87)"
    - "A rota arquétipo→motor observada na captura ao vivo está registrada por ticker (antecipa a exceção BBSE3, D-08)"
  artifacts:
    - path: "tests/fixtures/snapshot_bancos_2026-07-12.yaml"
      provides: "Raw fundamentals + preco_atual + beta + setor + rf_local + a.motor observado, por ticker"
      contains: "ITUB4"
    - path: "scripts/capturar_snapshot_bancos.py"
      provides: "Script de captura ao vivo one-time (D-05) via build.montar_empresa, reproduzível/auditável"
  key_links:
    - from: "scripts/capturar_snapshot_bancos.py"
      to: "ingest.build.montar_empresa"
      via: "captura CVM+Yahoo+BCB por ticker"
      pattern: "montar_empresa"
    - from: "tests/fixtures/snapshot_bancos_2026-07-12.yaml"
      to: "report.analisar_acao"
      via: "reconstrução de CompanyData reproduz a.intrinseco_motor"
      pattern: "intrinseco_motor"
---

<objective>
Capturar UMA vez ao vivo (D-05) os fundamentos dos 4 bancos da cesta (ITUB4, BBAS3, BBSE3, BBDC4) via `build.montar_empresa` e congelá-los num fixture YAML versionado (D-04), para que o backtest rode 100% offline e determinístico. O snapshot é a fonte de reprodutibilidade exigida por VAL-01.

Purpose: Sem um snapshot congelado, o teste quebraria a cada oscilação de preço / queda de Yahoo/CVM. Congelar raw fundamentals (não valores derivados) mantém o teste imune a mudança de assinatura interna de `motores.rim` — se o loop D-12 recalibrar a Fase 4, re-roda-se o snapshot, não se reescreve o teste.
Output: `scripts/capturar_snapshot_bancos.py` + `tests/fixtures/snapshot_bancos_2026-07-12.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-CONTEXT.md
@.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-RESEARCH.md
@.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-PATTERNS.md
@.planning/phases/04-rim-com-valor-terminal-ke-revisado/04-01-SUMMARY.md

<interfaces>
<!-- Contratos que o executor consome. Extraídos da RESEARCH/PATTERNS (file:line verificados). -->
De src/analista/ingest/build.py:40 — montar_empresa(ticker: str, ano_base: int, n_anos: int = 10) -> Optional[CompanyData]
De src/analista/cli.py:52-63 — padrão _montar: ano_base = cfg["universo"]["ano_base"] (2025); n = cfg["universo"]["anos_historico"] (10)
De src/analista/core/fundamentals.py:19-53 — CompanyData: dataclass mutável; séries anuais Dict[int,float]; campos de mercado escalares (preco_atual, beta)
De src/analista/report/report.py:312 — analisar_acao(c, cfg) -> AnaliseAcao; campos a.arquetipo, a.motor, a.intrinseco_motor (report.py:59)
Campos MÍNIMOS a congelar por ticker (RESEARCH Q2): ticker, nome, setor, anos(List[int]), lucro_liquido, patrimonio_liquido, num_acoes, dividendos (Dict[int,float]), preco_atual, beta (escalares); opcionais display: vendas_liquidas, fco, dpa_trailing_12m
Global carimbado: rf_local (escalar, o usado na captura), data_base: "2026-07-12"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever o script de captura one-time e capturar os 4 bancos ao vivo</name>
  <files>scripts/capturar_snapshot_bancos.py</files>
  <read_first>
    - src/analista/cli.py:32-63 (carregar_config + padrão _montar — ano_base/n de cfg["universo"])
    - src/analista/ingest/build.py:40-117 (montar_empresa: assinatura e o que popula)
    - src/analista/core/fundamentals.py:19-53 (campos de CompanyData a extrair)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-RESEARCH.md §Q2 (tabela de campos mínimos), §Q9 (landmine rf_local)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-PATTERNS.md §snapshot fixture (como gerar, D-05)
  </read_first>
  <action>
    Criar `scripts/` (dir novo) e o script `capturar_snapshot_bancos.py`. Carregar `config.yaml` via `yaml.safe_load` (mesmo padrão de `cli.carregar_config`). Para cada ticker em `["ITUB4","BBAS3","BBSE3","BBDC4"]`, chamar `build.montar_empresa(t, cfg["universo"]["ano_base"], cfg["universo"]["anos_historico"])`. Para cada `CompanyData` retornado, extrair APENAS os campos mínimos da tabela Q2: `ticker`, `nome`, `setor`, `anos`, e as séries `lucro_liquido`, `patrimonio_liquido`, `num_acoes`, `dividendos` (mais `vendas_liquidas`, `fco` se presentes), e os escalares `preco_atual`, `beta`, `dpa_trailing_12m`. Congelar também, GLOBAL: o `rf_local` usado na captura (ler de `cfg["capm"]["rf_local"]`, valor shipado 0.105 — NÃO chamar `macro.selic_ciclo_para_capm`; a rede fica só na captura de fundamentos) e `data_base: "2026-07-12"`. Para cada ticker, rodar `report.analisar_acao(c, cfg)` na captura e registrar no snapshot `motor_observado = a.motor`, `arquetipo_observado = a.arquetipo` e `intrinseco_motor_observado = a.intrinseco_motor` (carimbo da rota — crítico para o roteamento BBSE3 / D-08; nunca assumir "rim"). O script grava o YAML final em `tests/fixtures/snapshot_bancos_2026-07-12.yaml`. Serializar raw fundamentals — NÃO congelar `vpa0/roe0/ke` derivados (anti-pattern RESEARCH). Não editar nenhum arquivo de motor.
  </action>
  <verify>
    <automated>python scripts/capturar_snapshot_bancos.py && test -f tests/fixtures/snapshot_bancos_2026-07-12.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/capturar_snapshot_bancos.py` existe e roda sem exceção (exit 0)
    - A execução grava `tests/fixtures/snapshot_bancos_2026-07-12.yaml` com uma entrada por ticker dos 4 bancos
    - Cada entrada de ticker contém as chaves: setor, anos, lucro_liquido, patrimonio_liquido, num_acoes, dividendos, preco_atual, beta, motor_observado, arquetipo_observado
    - O YAML tem as chaves globais data_base: "2026-07-12" e rf_local (escalar)
    - O script NÃO importa/chama macro.selic_ciclo_para_capm nem toca core/motores.py, core/ddm.py, report/selo.py, core/lentes.py, config.yaml (`git diff --name-only` não lista esses arquivos)
  </acceptance_criteria>
  <done>Snapshot YAML gerado ao vivo com raw fundamentals + rf_local + rota observada por ticker; nenhum arquivo de motor tocado.</done>
</task>

<task type="auto">
  <name>Task 2: Validar que o snapshot reproduz os números RIM da Fase 4 offline</name>
  <files>tests/fixtures/snapshot_bancos_2026-07-12.yaml</files>
  <read_first>
    - tests/fixtures/snapshot_bancos_2026-07-12.yaml (o snapshot gerado na Task 1)
    - tests/test_vulc3_regressao.py:20-36,160-188 (molde: _cfg + reconstrução offline de CompanyData + analisar_acao + assert de bounds)
    - src/analista/report/report.py:312-420 (analisar_acao → a.intrinseco_motor; landmine rf_local do caller cli.py:77-79)
    - .planning/phases/04-rim-com-valor-terminal-ke-revisado/04-01-SUMMARY.md §Verified Numbers (ITUB4 live R$32,87)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-RESEARCH.md §Q2 landmine rf_local
  </read_first>
  <action>
    Escrever um verificador ad-hoc (pode ser um bloco `python -c` ou um trecho temporário — não precisa versionar código extra): carregar `config.yaml` via `yaml.safe_load`, carregar o snapshot YAML, reconstruir cada `CompanyData` preenchendo os dicts por ano (mesmo padrão de `_itub4_live_like` em test_vulc3_regressao.py:160-175), injetar o `rf_local` congelado em `cfg["capm"]["rf_local"]` ANTES de chamar `report.analisar_acao(c, cfg)`, e imprimir por ticker `a.arquetipo`, `a.motor`, `a.intrinseco_motor`. Confirmar que ITUB4 roteia `financeira`→`rim` e reproduz `a.intrinseco_motor` na faixa ~R$32–40 (bate o live R$32,87 do SUMMARY 04-01, dentro de tolerância de bounds — convenção do repo é bounds absolutos, não approx). Se algum ticker rotear para motor ≠ "rim" (BBSE3 é o candidato natural — depende da string de setor da CVM casar `seguradora`), REGISTRAR o fato no `fonte`/comentário do snapshot e no SUMMARY do plano como exceção documentada natural (D-08) — NÃO forçar rota. Ajustar o snapshot só se um campo mínimo estiver faltando/None e impedir a reprodução (ex: preco_atual ausente); nenhum ajuste ao motor.
  </action>
  <verify>
    <automated>python -c "
import yaml
from analista.core.fundamentals import CompanyData
from analista.report import report
cfg = yaml.safe_load(open('config.yaml'))
snap = yaml.safe_load(open('tests/fixtures/snapshot_bancos_2026-07-12.yaml'))
cfg['capm']['rf_local'] = snap['rf_local']          # injeta o rf_local congelado ANTES de analisar
SERIES = ['lucro_liquido','patrimonio_liquido','num_acoes','dividendos','vendas_liquidas','fco']
for tk in ['ITUB4','BBAS3','BBSE3','BBDC4']:
    d = snap[tk]
    c = CompanyData(ticker=tk, nome=d.get('nome',tk), setor=d['setor'], anos=[int(a) for a in d['anos']])
    for s in SERIES:
        for ano, val in (d.get(s) or {}).items():
            getattr(c, s)[int(ano)] = val
    c.preco_atual = d['preco_atual']; c.beta = d['beta']
    a = report.analisar_acao(c, cfg)
    assert a.motor == d['motor_observado'], (tk, a.motor, d['motor_observado'])                       # paridade da rota
    ri, rio = a.intrinseco_motor, d['intrinseco_motor_observado']
    assert (ri is None) == (rio is None), (tk, ri, rio)
    if ri is not None:
        assert abs(ri - rio) < 0.01, (tk, ri, rio)                                                     # reprodutibilidade offline
    if tk == 'ITUB4':
        assert a.motor == 'rim' and a.arquetipo == 'financeira' and 30.0 <= ri <= 40.0, (a.motor, ri)  # gate ITUB4
    print(tk, a.motor, round(ri,2) if ri is not None else None)
print('reproducao offline OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Reconstruir CompanyData do snapshot + injetar rf_local + rodar analisar_acao é offline (sem rede) e não lança exceção
    - ITUB4: a.arquetipo == "financeira", a.motor == "rim", a.intrinseco_motor entre 30.0 e 40.0
    - Para os 4 bancos, a.motor e a.intrinseco_motor observados na reconstrução batem os campos motor_observado/intrinseco_motor_observado gravados na Task 1 (prova de reprodutibilidade offline)
    - Qualquer ticker com motor != "rim" está anotado como exceção documentada (D-08) no snapshot e no SUMMARY, não silenciado nem re-roteado
  </acceptance_criteria>
  <done>Snapshot reproduz offline os números RIM da Fase 4 (ITUB4 ~R$32,87 na faixa 32–40); rota por ticker confirmada/registrada; exceções documentadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rede→fixture (captura one-time) | CVM/Yahoo/BCB (fontes públicas gratuitas) cruzam para dentro do snapshot versionado; ocorre UMA vez, offline daí em diante |
| runtime do teste | Nenhum — o teste roda 100% offline sobre o YAML congelado; sem input de usuário, sem rede, sem segredos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | snapshot YAML (fixture) | mitigate | Data-base carimbada ("2026-07-12") + `fonte`/rota observada por ticker + versionado em git → alteração é rastreável no diff; Task 2 reconcilia números observados vs reconstruídos |
| T-05-02 | Information Disclosure | dados capturados | accept | Só fundamentos públicos da CVM/Yahoo; sem PII, sem segredos, alvo de baixo valor |
| T-05-03 | Tampering | integridade dos dados de origem (CVM/Yahoo) | accept | Fontes públicas confiáveis; captura one-time e conferida contra os números verificados da Fase 4 (SUMMARY 04-01) |
</threat_model>

<verification>
- `python scripts/capturar_snapshot_bancos.py` gera o snapshot YAML sem erro
- Reconstrução offline reproduz ITUB4 na faixa R$32–40 e a rota financeira→rim
- `git diff --name-only` NÃO lista core/motores.py, core/ddm.py, report/selo.py, core/lentes.py, config.yaml
</verification>

<success_criteria>
- `tests/fixtures/snapshot_bancos_2026-07-12.yaml` versionado, data-base carimbada, raw fundamentals + rf_local + rota observada dos 4 bancos
- O snapshot reproduz offline o intrínseco RIM calibrado da Fase 4 (ITUB4 ~R$32,87), sem rede
- Escopo cirúrgico respeitado: nenhum arquivo de motor tocado
</success_criteria>

<output>
After completion, create `.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-01-SUMMARY.md`
</output>
