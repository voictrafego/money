---
phase: 05-backtest-01-valida-o-na-cesta-de-bancos
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - tests/test_backtest_bancos.py
  - tests/fixtures/fair_values_bancos.yaml   # CONDICIONAL — editado só se BBSE3/outro ticker exigir excecao_nota (D-08); owner primário é o Plan 05-02
autonomous: true
requirements: [VAL-01, VAL-02]

must_haves:
  truths:
    - "D-09: Um teste pytest determinístico (a metade 'teste' da entrega pytest+script) trava o gate quórum-3/4-±15% (D-06/D-07/D-08) sobre o snapshot congelado, offline"
    - "O 4º banco pode ficar fora da banda SE E SOMENTE SE tiver excecao_nota — desvio não anotado = FAIL (distingue exceção documentada de FAIL silencioso)"
    - "A suíte completa segue verde (nenhuma regressão; firewall selo↛report, DDM/TAEE11 intactos)"
    - "Se a validação reprovar além da exceção permitida, o achado é registrado e volta à Fase 4 (loop D-12), não silenciado"
  artifacts:
    - path: "tests/test_backtest_bancos.py"
      provides: "Gate determinístico VAL-01/VAL-02; reusa rodar_cesta; constantes nomeadas BANDA_PASS/QUORUM_MIN"
      contains: "QUORUM_MIN"
  key_links:
    - from: "tests/test_backtest_bancos.py"
      to: "src/analista/backtest.py::rodar_cesta"
      via: "importa e reusa o MESMO harness do script (mesmo resultado)"
      pattern: "rodar_cesta"
    - from: "tests/test_backtest_bancos.py"
      to: "tests/fixtures/fair_values_bancos.yaml"
      via: "gate ±15% + regra de anotação da 4ª exceção (D-08)"
      pattern: "excecao_nota"
---

<objective>
Travar a regressão do backtest (VAL-01/VAL-02): um teste pytest determinístico `tests/test_backtest_bancos.py` que, sobre o snapshot congelado e via `rodar_cesta` (mesmo harness do script), crava o gate quórum-3/4-±15% contra a tabela de fair values (D-06/D-07/D-08), com a regra de anotação que distingue "3 passam + 1 anotada" de "3 passam + 1 silenciosa". Confirmar que a suíte completa segue verde e tratar o loop D-12 se a cesta reprovar.

Purpose: Este é o gate automatizado que prova que a calibração da Fase 4 GENERALIZA na cesta — não é overfit no ITUB4. O teste é o guardião da regressão: roda 100% offline, cobra só a âncora manual de FV (D-06), e não pode ser afrouxado para forçar um pass (D-12).
Output: `tests/test_backtest_bancos.py` + confirmação da suíte cheia verde (+ finding registrado se D-12 disparar).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-CONTEXT.md
@.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-RESEARCH.md
@.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-PATTERNS.md
@src/analista/backtest.py
@tests/fixtures/snapshot_bancos_2026-07-12.yaml
@tests/fixtures/fair_values_bancos.yaml

<interfaces>
<!-- Contratos do gate. RESEARCH Q3/Q7 + PATTERNS §test_backtest_bancos + §Gate. -->
De src/analista/backtest.py — rodar_cesta(empresas, fair_values, cfg, rf_local) -> list[dict]; carregar_snapshot(caminho)->(empresas, rf_local); carregar_fair_values(caminho)->dict. Cada dict: ticker, motor, rim, ..., passa(bool), excecao_nota
Molde do teste (test_vulc3_regressao.py:20-36): ROOT=os.path.dirname(os.path.dirname(os.path.abspath(__file__))); _cfg() abre config.yaml via yaml.safe_load. Fixtures em os.path.join(ROOT,"tests","fixtures","<arquivo>.yaml")
Config pytest (pyproject.toml:14-16): pythonpath=["src"], testpaths=["tests"]. Rodar quick: pytest tests/test_backtest_bancos.py -x. Suíte cheia: pytest (440 testes hoje verdes)
Constantes nomeadas (Established Pattern, zero número solto): BANDA_PASS = 0.15 (D-07), QUORUM_MIN = 3 (D-08)
Gate (RESEARCH Q7): passes = [t for t com passa]; falhas = [t sem passa]; assert len(passes) >= QUORUM_MIN; for t in falhas: assert fv[t].get("excecao_nota"), "FAIL silencioso"
Convenção de tolerância do repo: bounds absolutos (> 30.0, 32.0<=v<=40.0), NÃO pytest.approx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever o gate determinístico tests/test_backtest_bancos.py</name>
  <files>tests/test_backtest_bancos.py</files>
  <read_first>
    - tests/test_vulc3_regressao.py:20-36,160-188,274-276 (molde: _cfg, reconstrução, assert bounds, determinismo de reexecução)
    - src/analista/backtest.py (rodar_cesta + loaders — a superfície que o teste importa)
    - tests/fixtures/fair_values_bancos.yaml e tests/fixtures/snapshot_bancos_2026-07-12.yaml (shapes reais)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-RESEARCH.md §Q3 (estrutura do arquivo), §Q7 (mecânica do gate), §Q9 (landmines)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-PATTERNS.md §test_backtest_bancos.py (excerpts lift-ready)
  </read_first>
  <action>
    Criar `tests/test_backtest_bancos.py` espelhando VERBATIM o estilo de `test_vulc3_regressao.py`. Definir `ROOT` e `_cfg()` (copiar do molde). Importar `rodar_cesta`, `carregar_snapshot`, `carregar_fair_values` de `analista.backtest` — o teste reusa o MESMO harness do script (garante mesmo resultado). Definir as constantes nomeadas `BANDA_PASS = 0.15` e `QUORUM_MIN = 3` no topo (zero número solto — Established Pattern). Escrever os testes:
    (1) `test_backtest_cesta_rota_por_ticker`: roda `rodar_cesta` sobre os fixtures; assert que ITUB4 tem `motor == "rim"` e `arquetipo == "financeira"`; para CADA ticker, registra `motor` (o teste NÃO assume rim cegamente — se BBSE3 rotear ≠ rim, o teste tolera desde que a FV do ticker tenha `excecao_nota`, D-08).
    (2) `test_backtest_gate_quorum_e_anotacao`: computa `passes = [r for r in res if r["passa"]]` e `falhas = [r for r in res if not r["passa"]]`; `assert len(passes) >= QUORUM_MIN`; `for r in falhas: assert r["excecao_nota"], f"{r['ticker']} fora da banda sem nota de exceção → FAIL silencioso"`. Isto trava 4/4 PASS (verde trivial), 3 PASS + 1 anotada (verde), 3 PASS + 1 silenciosa (FAIL do assert da nota), ≤2 PASS (FAIL do quórum → loop D-12).
    (3) `test_backtest_determinismo`: roda `rodar_cesta` duas vezes com o mesmo cfg e assert que os `rim` por ticker são idênticos (prova que o snapshot não deriva; espelha test_vulc3_regressao.py:274-276). Usar bounds absolutos, não `pytest.approx`. Não tocar nenhum arquivo de motor.
  </action>
  <verify>
    <automated>pytest tests/test_backtest_bancos.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_backtest_bancos.py -x` sai com código 0 (todos verdes)
    - As constantes `BANDA_PASS` e `QUORUM_MIN` são nomeadas: `grep -v '^#' tests/test_backtest_bancos.py | grep -c QUORUM_MIN` ≥ 1 e nenhum literal `3` ou `0.15` solto no corpo do gate
    - O teste importa `rodar_cesta` de `analista.backtest` (reusa o harness do Plan 03; não reimplementa o cálculo)
    - Existe um assert `len(passes) >= QUORUM_MIN` E um loop `for ... : assert ...excecao_nota` (a mecânica que barra o FAIL silencioso, D-08)
    - Existe uma asserção de determinismo (rim idêntico em duas execuções)
    - ITUB4: motor == "rim", intrínseco na faixa 30–40
    - `git diff --name-only` NÃO lista core/motores.py, core/lentes.py, core/ddm.py, report/selo.py, config.yaml
  </acceptance_criteria>
  <done>Gate determinístico verde: quórum 3/4 ±15% com regra de anotação da exceção e checagem de determinismo, reusando rodar_cesta.</done>
</task>

<task type="auto">
  <name>Task 2: Rodar a suíte completa, confirmar zero regressão e tratar o loop D-12</name>
  <files>tests/test_backtest_bancos.py</files>
  <read_first>
    - .planning/STATE.md §Blockers/Concerns (invariantes: test_ddm, test_selo firewall, test_vulc3_regressao, consistência cross-modo)
    - .planning/phases/04-rim-com-valor-terminal-ke-revisado/04-01-SUMMARY.md §Task 3 (invariantes HARD do SC#4, suíte 440 verde)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-CONTEXT.md §D-12 (loop de falha)
    - out/backtest_bancos.md (a tabela gerada pelo Plan 03 — evidência dos desvios)
  </read_first>
  <action>
    Rodar a suíte completa `pytest` e confirmar que o novo teste SOMA à suíte sem quebrar nada: o total sobe (~440 → ~443+) e os invariantes HARD seguem verdes (`test_ddm.py` DDM Itaú R$37,22, `test_vulc3_regressao.py` capstone, firewall selo↛report, TAEE11 rota DDM idêntica). Confirmar via `git diff --name-only` que nenhum arquivo proibido foi tocado (core/ddm.py, report/selo.py, core/lentes.py, core/motores.py, config.yaml). TRATAR O LOOP D-12: se o gate da Task 1 revelar que a cesta reprova além da exceção permitida (≤2 PASS, ou uma 4ª falha que NÃO é a exceção BBSE3 naturalmente documentável), NÃO afrouxar a banda nem o quórum para forçar verde — em vez disso, registrar o achado no SUMMARY do plano como finding (ticker, RIM observado, faixa FV, magnitude do desvio, hipótese de causa) e sinalizar explicitamente que a Fase 4 precisa reabrir (loop). Se a única falha for BBSE3 por roteamento ≠ rim, garantir que a FV de BBSE3 tem `excecao_nota` preenchida com o motivo (motor observado real) — isto fecha o gate honestamente sem silenciar. NÃO editar core/config para forçar um pass.
  </action>
  <verify>
    <automated>pytest -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest` (suíte cheia) sai com código 0; nenhum teste pré-existente regrediu
    - O contador de testes aumentou em relação ao baseline (~440) pelos testes novos do backtest
    - Invariantes HARD verdes: test_ddm (R$37,22), test_vulc3_regressao capstone, firewall selo↛report, TAEE11 DDM idêntico
    - `git diff --name-only` NÃO lista core/ddm.py, report/selo.py, core/lentes.py, core/motores.py, config.yaml (a ÚNICA edição de fixture permitida aqui é adicionar `excecao_nota` a `tests/fixtures/fair_values_bancos.yaml` para um ticker roteado ≠ rim — D-08)
    - Se a cesta reprova além da exceção permitida: um finding está registrado no SUMMARY (ticker/RIM/FV/desvio/causa) e a necessidade de loop para a Fase 4 está sinalizada — o gate NÃO foi afrouxado
    - Se BBSE3 rotear ≠ rim: fair_values_bancos.yaml de BBSE3 tem excecao_nota com o motor real observado
  </acceptance_criteria>
  <done>Suíte completa verde sem regressão; escopo cirúrgico confirmado; loop D-12 tratado honestamente se disparado (finding registrado, gate não afrouxado).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture→teste | O teste lê apenas YAML congelado e versionado; roda 100% offline, sem input de usuário, sem rede, sem segredos |
| gate→integridade da calibração | O gate é a barreira que impede um FAIL silencioso ou um afrouxamento para forçar verde (D-08/D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | gate afrouxado para forçar um PASS | mitigate | Banda ±15% e quórum 3/4 são constantes nomeadas; D-12 proíbe afrouxá-las — falha real vira finding + loop Fase 4, não patch no gate; acceptance_criteria exige o finding registrado |
| T-05-11 | Repudiation | FAIL silencioso (4ª exceção não anotada) | mitigate | Loop `for t in falhas: assert fv[t].get("excecao_nota")` — desvio sem nota reprova o teste, tornando a exceção rastreável e não-repudiável |
| T-05-12 | Denial of Service | rede acidental quebrando o determinismo | mitigate | rf_local injetado do snapshot; teste proibido de chamar rede → 100% offline e reprodutível |
</threat_model>

<verification>
- `pytest tests/test_backtest_bancos.py -x` verde; gate quórum 3/4 ±15% + anotação + determinismo
- `pytest` (suíte cheia) verde; contador subiu; invariantes HARD intactos
- `grep -v '^#' tests/test_backtest_bancos.py | grep -c QUORUM_MIN` ≥ 1
- `git diff --name-only` NÃO lista arquivos de motor/config
- Loop D-12 tratado se disparado (finding + sinalização, gate não afrouxado)
</verification>

<success_criteria>
- Gate determinístico (VAL-01/VAL-02) prova que a calibração generaliza na cesta: quórum 3/4 dentro de ±15% da FV
- A 4ª exceção só passa se documentada (D-08); FAIL silencioso barrado
- Suíte completa verde, firewall e invariantes intactos; escopo cirúrgico respeitado
- Desvios remanescentes registrados/explicados (D-12), nunca silenciados
</success_criteria>

<output>
After completion, create `.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-04-SUMMARY.md`
</output>
