---
phase: 05-backtest-01-valida-o-na-cesta-de-bancos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/fair_values_bancos.yaml
autonomous: false
requirements: [VAL-02]

must_haves:
  truths:
    - "Uma tabela manual de fair values (faixa mín–máx por ticker) existe versionada como âncora-verdade do gate (D-06)"
    - "Os valores vêm de pesquisa de consenso de casas de análise (target prices públicos), NÃO derivados de Graham/Bazin nem digitados pelo usuário (D-01)"
    - "O usuário aprovou explicitamente as faixas ANTES de versionar (D-01)"
  artifacts:
    - path: "tests/fixtures/fair_values_bancos.yaml"
      provides: "Por ticker: min, max, data, fonte, e excecao_nota opcional (D-02/D-03/D-08)"
      contains: "ITUB4"
  key_links:
    - from: "tests/fixtures/fair_values_bancos.yaml"
      to: "gate do teste (Plan 04)"
      via: "min/max = banda de PASS ±15%; excecao_nota = distingue exceção documentada de FAIL silencioso"
      pattern: "excecao_nota"
---

<objective>
Produzir a 4ª âncora do backtest (VAL-02): a tabela manual de fair values dos 4 bancos, como FAIXA mín–máx por ticker (D-02), a partir de pesquisa de consenso de casas de análise (D-01), apresentada ao usuário para aprovação ANTES de versionar, e então gravada em `tests/fixtures/fair_values_bancos.yaml` (D-03).

Purpose: Esta é a âncora-verdade que o gate automatizado (Plan 04) cobra (D-06). VAL-02 exige uma âncora manual INDEPENDENTE — por isso não pode derivar de Graham/Bazin (que já são âncoras (a)). O consenso deve alinhar à janela temporal do snapshot (~2026-07-12, D-05).
Output: `tests/fixtures/fair_values_bancos.yaml` (aprovado pelo usuário, versionado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-CONTEXT.md
@.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-RESEARCH.md
@.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-PATTERNS.md

<interfaces>
<!-- Shape do fixture que o gate (Plan 04) vai ler. RESEARCH Q7 + D-02/D-03/D-08. -->
Por ticker (chave = ticker string, ex: ITUB4):
  min: float          # borda inferior da faixa de consenso
  max: float          # borda superior
  data: "2026-07-XX"  # janela do consenso, alinhada ao snapshot (D-05)
  fonte: str          # casa/relatório — média de target prices
  excecao_nota: str   # OPCIONAL — presente só se o ticker fica fora da banda (D-08)
Consumido por: o teste lê min/max (gate ±15%) e .get("excecao_nota") (distingue exceção documentada de FAIL silencioso).
Cesta fixa: ITUB4, BBAS3, BBSE3, BBDC4.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pesquisar consenso de fair value e montar a proposta de faixas mín–máx</name>
  <files>tests/fixtures/fair_values_bancos.yaml</files>
  <read_first>
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-CONTEXT.md §decisions (D-01, D-02, D-03, D-05)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-RESEARCH.md §Q7 (shape do YAML), §Open Questions (valores de consenso a definir)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-PATTERNS.md §fair_values fixture (shape por ticker)
  </read_first>
  <action>
    Para cada ticker da cesta (ITUB4, BBAS3, BBSE3, BBDC4), pesquisar preços-alvo / fair values de consenso de casas de análise em relatórios públicos e agregadores de target price (média/faixa de target prices de mercado), alinhados à janela ~2026-07-12 (D-05). Para cada ticker, derivar uma FAIXA `min`–`max` (D-02, não um ponto) que reflita a dispersão do consenso, registrando `data` (data da coleta) e `fonte` (casa/relatório ou agregador, descrevendo que é média de target prices). NÃO derivar os números de Graham/Bazin nem do RIM — VAL-02 pede âncora manual INDEPENDENTE (D-01). Preparar uma tabela-proposta legível (ticker | min | max | fonte) para apresentar ao usuário na Task 2. NÃO gravar ainda o YAML final — a gravação (Task 3) só ocorre após a aprovação (bloqueio D-01). Guardar a proposta como texto para o checkpoint.
  </action>
  <verify>
    <automated>MISSING — task de pesquisa/curadoria; a verificação é a aprovação humana da Task 2. O YAML só é gravado na Task 3.</automated>
  </verify>
  <acceptance_criteria>
    - Existe uma faixa min–max proposta para CADA um dos 4 tickers (ITUB4, BBAS3, BBSE3, BBDC4)
    - Cada faixa tem `fonte` citável (casa de análise/agregador de target price) e `data` alinhada a ~2026-07-12
    - As faixas NÃO são cópia de Graham/Bazin nem do RIM (âncora independente — D-01)
    - Nenhum arquivo foi versionado ainda (a gravação aguarda a aprovação do usuário)
  </acceptance_criteria>
  <done>Proposta de faixas de fair value por consenso pronta para apresentação ao usuário; nada versionado ainda.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Aprovação do usuário das faixas de fair value (bloqueio D-01)</name>
  <decision>Aprovar as faixas mín–máx de fair value por consenso para os 4 bancos, para versioná-las como âncora-verdade do backtest.</decision>
  <context>D-01 exige que o usuário aprove os valores de consenso ANTES de versionar — não é o usuário que digita, mas ele valida a proposta de pesquisa. Estas faixas serão a âncora-verdade que o gate automatizado (Plan 04) cobra (D-06): PASS se o RIM cai dentro da faixa ou até ±15% fora de qualquer borda. Faixas mais largas afrouxam o gate; mais estreitas o endurecem.</context>
  <options>
    <option id="approve-as-is">
      <name>Aprovar as faixas propostas como estão</name>
      <pros>Versiona direto; consenso de mercado preservado sem viés do desenvolvedor</pros>
      <cons>Depende inteiramente da qualidade da pesquisa de consenso</cons>
    </option>
    <option id="adjust">
      <name>Ajustar bordas de tickers específicos antes de versionar</name>
      <pros>Usuário corrige faixas que discorda com base em conhecimento próprio</pros>
      <cons>Introduz julgamento manual sobre o consenso puro</cons>
    </option>
  </options>
  <resume-signal>Responda "aprovado" para versionar as faixas como propostas, ou informe os ajustes de min/max por ticker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Gravar o fixture fair_values_bancos.yaml aprovado</name>
  <files>tests/fixtures/fair_values_bancos.yaml</files>
  <read_first>
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-RESEARCH.md §Q7 (chaves min/max/data/fonte/excecao_nota)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-PATTERNS.md §fair_values fixture (shape verbatim)
  </read_first>
  <action>
    Criar `tests/fixtures/fair_values_bancos.yaml` (o dir `tests/fixtures/` pode já ter sido criado pelo Plan 01; usar o mesmo). Gravar, por ticker aprovado na Task 2, as chaves `min` (float), `max` (float), `data` (str), `fonte` (str). NÃO incluir `excecao_nota` por padrão — é campo OPCIONAL, adicionado só se/quando o Plan 04 constatar que um ticker fica fora da banda e precisar documentar a exceção (D-08). Incorporar quaisquer ajustes de min/max que o usuário informou na Task 2. Este arquivo é a âncora-verdade do gate (D-06); não é knob do motor — por isso vive em fixtures, NÃO em config.yaml (D-03).
  </action>
  <verify>
    <automated>python -c "import yaml; fv=yaml.safe_load(open('tests/fixtures/fair_values_bancos.yaml')); assert set(['ITUB4','BBAS3','BBSE3','BBDC4'])<=set(fv), 'faltam tickers'; assert all('min' in fv[t] and 'max' in fv[t] and 'data' in fv[t] and 'fonte' in fv[t] for t in fv), 'faltam chaves'; print('fair_values OK', list(fv))"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/fair_values_bancos.yaml` existe e carrega via yaml.safe_load
    - Tem entrada para os 4 tickers (ITUB4, BBAS3, BBSE3, BBDC4), cada uma com min, max, data, fonte
    - Os valores gravados são os aprovados/ajustados pelo usuário na Task 2 (não os originais se houve ajuste)
    - Nenhuma entrada tem excecao_nota gravada nesta fase (campo reservado para o Plan 04)
    - config.yaml NÃO foi tocado (`git diff --name-only` não lista config.yaml)
  </acceptance_criteria>
  <done>Fixture de fair values versionado com as faixas aprovadas pelo usuário; âncora-verdade do gate pronta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pesquisa pública→fixture | Target prices de relatórios públicos cruzam para o YAML versionado após aprovação humana explícita |
| aprovação do usuário | Gate humano obrigatório (D-01) antes de qualquer versionamento dos números |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | fair_values YAML (âncora-verdade do gate) | mitigate | Números só entram após aprovação do usuário (Task 2, D-01); `fonte`+`data` por ticker tornam a origem citável; versionado em git → alteração rastreável no diff |
| T-05-05 | Repudiation | proveniência dos fair values | mitigate | Campo `fonte` obrigatório por ticker documenta a origem do consenso; a aprovação é um checkpoint explícito registrado |
| T-05-06 | Information Disclosure | dados de consenso | accept | Target prices públicos; sem PII, sem segredos |
</threat_model>

<verification>
- `tests/fixtures/fair_values_bancos.yaml` carrega e tem os 4 tickers com min/max/data/fonte
- A gravação (Task 3) ocorreu SÓ após a aprovação do usuário (Task 2)
- config.yaml intocado
</verification>

<success_criteria>
- Tabela manual de fair values (faixa mín–máx por ticker) versionada como âncora-verdade do gate (D-06)
- Valores de consenso independentes (não Graham/Bazin), aprovados pelo usuário antes de versionar (D-01)
- Alinhados à janela do snapshot (~2026-07-12)
</success_criteria>

<output>
After completion, create `.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-02-SUMMARY.md`
</output>
