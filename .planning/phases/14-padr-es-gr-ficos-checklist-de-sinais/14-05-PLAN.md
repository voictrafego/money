---
phase: 14-padr-es-gr-ficos-checklist-de-sinais
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - config.yaml
autonomous: false
requirements: [PAT-01]

must_haves:
  truths:
    - "Os detectores rodam sobre >=3 tickers B3 variados via dados reais (prices.coletar_mercado)"
    - "Os limiares geométricos no config.yaml estão calibrados para NÃO gerar enxurrada de falso positivo (pareidolia)"
    - "Os tipos de padrão detectados são plausíveis (poucos padrões de alta confiança, não dezenas sobrepostos)"
  artifacts:
    - path: "config.yaml"
      provides: "Bloco `padroes:` com limiares calibrados multi-ticker (ou confirmados como adequados)"
      contains: "padroes:"
  key_links:
    - from: "indicators.calcular (real OHLC)"
      to: "SinaisTecnicos.padroes / .checklist"
      via: "prices.coletar_mercado(ticker).ohlc"
      pattern: "coletar_mercado"
---

<objective>
Validar e calibrar os limiares geométricos do bloco `padroes:` contra DADOS REAIS de >=3 tickers B3
variados (Success Criterion 3) — provar que os detectores não viram gerador de pareidolia (Pitfall 1/11).
Este plano NÃO altera código: só inspeciona a saída do pipeline e, se necessário, ajusta valores no
`config.yaml` (sem tocar `indicadores:`). Termina num checkpoint humano de aprovação.

Purpose: Os limiares A1–A7 são ASSUMED (LOW-MEDIUM confidence). Sem validação multi-ticker, um limiar
frouxo dispararia 5 padrões por gráfico e destruiria a confiança. A calibração é parte do aceite da fase.
Output: limiares `padroes:` confirmados ou ajustados; goldens da fase seguem verdes após qualquer ajuste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-04-SUMMARY.md

<interfaces>
<!-- Fonte de OHLC real p/ a calibração (nominal 5a, auto_adjust=False). -->

```python
from analista.ingest import prices
dados = prices.coletar_mercado(ticker)   # DadosMercado; dados.ohlc = frame OHLCV nominal 5a
from analista.core import indicators
s = indicators.calcular(dados.ohlc, cfg, ohlc_nominal=dados.ohlc)
s.padroes.lista   # [PadraoGrafico(tipo, estado, ...)]
```
config.yaml bloco `padroes:` = único alvo de ajuste (NÃO tocar `indicadores:`, anti-rebaseline Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rodar os detectores sobre >=3 tickers B3 reais e dumpar a varredura</name>
  <files>(script efêmero no scratchpad — NÃO commitar; só config.yaml pode mudar)</files>
  <read_first>
    - src/analista/ingest/prices.py (`coletar_mercado` 113 — retorna DadosMercado com `.ohlc` nominal 5a)
    - src/analista/core/indicators.py (`calcular` 921–972 — assinatura `calcular(ohlc, cfg, ohlc_nominal)`)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md (§Assumptions Log A1–A7 388–401, Pitfall 1/11 283–289)
    - config.yaml (bloco `padroes:`)
  </read_first>
  <action>
    Escrever um script EFÊMERO no diretório scratchpad (NÃO no repo — CLAUDE.md "não criar arquivos extras")
    que: para cada ticker de uma lista variada de >=3 papéis B3 (ex.: um de alta volatilidade, um lateral,
    um de tendência clara — escolher entre tickers já cobertos em `data/ticker_map.json`, ex.: PETR4, ITUB4,
    VALE3, WEGE3), chama `prices.coletar_mercado(ticker).ohlc`, roda
    `indicators.calcular(ohlc, cfg, ohlc_nominal=ohlc)`, e imprime por ticker: quantidade total de padrões
    em `s.padroes.lista`, os `tipo:estado` de cada um, neckline/alvo, e o resumo do `s.checklist`.
    Agregar uma CONTAGEM por ticker (nº de padrões "em_formacao" vs "confirmado") para avaliar enxurrada.
    Se a rede/yfinance falhar para algum ticker, registrar e seguir (degradação graciosa não deve levantar).
    Apresentar a tabela de contagem ao final (entrada do checkpoint da Task 2). NÃO ajustar config ainda —
    só medir.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -c "from analista.ingest import prices; from analista.core import indicators; import yaml; cfg=yaml.safe_load(open('config.yaml')); [indicators.calcular(d.ohlc,cfg,ohlc_nominal=d.ohlc) for d in (prices.coletar_mercado(t) for t in ['PETR4','ITUB4','VALE3']) if d and d.ohlc is not None]; print('SCAN OK')"</automated>
  </verify>
  <done>Varredura roda sobre >=3 tickers reais sem levantar; tabela de contagem de padrões por ticker apresentada.</done>
  <acceptance_criteria>
    - O script roda `calcular` com `ohlc_nominal` para >=3 tickers e imprime, por ticker, a contagem e os
      `tipo:estado` dos padrões + resumo do checklist, SEM levantar exceção (degradação graciosa em falha de rede).
    - A tabela final permite avaliar enxurrada (muitos padrões sobrepostos) vs. poucos de alta confiança.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — aprovar/calibrar os limiares `padroes:` (anti-pareidolia)</name>
  <what-built>
    A Task 1 rodou os detectores de padrões (duplo topo/fundo + OCO) sobre dados reais de >=3 tickers B3
    e produziu uma tabela: nº de padrões por ticker, tipos, estados (em_formacao/confirmado), neckline/alvo.
  </what-built>
  <how-to-verify>
    1. Revisar a tabela de contagem por ticker apresentada pela Task 1.
    2. Critério de aprovação (Pitfall 1/11 — pareidolia): nenhum ticker deve mostrar uma ENXURRADA de
       padrões sobrepostos (regra prática: poucos padrões de alta confiança por gráfico; "confirmado" é raro
       e exige rompimento+volume). Padrões plausíveis e geometricamente coerentes (neckline entre os pivôs,
       alvo measured-move na direção correta).
    3. Se houver enxurrada / falso positivo evidente → pedir ajuste: subir `price_tolerance_pct`/
       `shoulder_symmetry_pct` para BAIXO (mais rígido), ou `min_pattern_height_pct` para CIMA (anti-ruído),
       ou reduzir `lookback_pivos`. O executor ajusta APENAS o bloco `padroes:` no config.yaml e re-roda a
       varredura (Task 1) + a suíte de testes (`pytest -q`) — os goldens da fase devem seguir verdes
       (limiares calibrados podem exigir reajuste de fixtures dos planos 02/03 se um golden pinava um valor
       de limiar; nesse caso, ajustar a fixture, não rebaselinar contrato).
    4. Iterar até a saída ser plausível em todos os tickers.
  </how-to-verify>
  <read_first>
    - config.yaml (bloco `padroes:`)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md (§Assumptions Log, Pitfall 1/11)
  </read_first>
  <acceptance_criteria>
    - Usuário confirma que a saída multi-ticker é plausível (sem enxurrada de falso positivo).
    - Qualquer ajuste de limiar fica registrado no `config.yaml` (bloco `padroes:` apenas; `indicadores:` intocado).
    - `pytest -q` (suíte inteira) verde após o ajuste final.
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" ou descreva o ajuste de limiar desejado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yfinance (rede) → engine | Dados externos reais; o pipeline já saneia e a engine degrada graciosamente |
| limiares de config → produto | Risco é regulatório/de confiança (pareidolia), não segurança técnica |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Denial of Service | falha de rede yfinance em algum ticker | mitigate | Script segue com try/registro; engine degrada para `Padroes(lista=[])` sem levantar |
| T-14-12 | Information Disclosure (regulatório) | enxurrada de falso positivo = pareidolia mina confiança | mitigate | Checkpoint humano de aprovação; limiares conservadores config-driven; calibração multi-ticker |
</threat_model>

<verification>
- Varredura roda sobre >=3 tickers reais sem exceção.
- Usuário aprova a plausibilidade multi-ticker; limiares confirmados/ajustados em config.yaml.
- `pytest -q` (suíte inteira) verde após qualquer ajuste.
</verification>

<success_criteria>
- Limiares `padroes:` validados multi-ticker (Success Criterion 3).
- Sem enxurrada de falso positivo na saída real.
- Suíte inteira verde.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-05-SUMMARY.md`.
</output>
