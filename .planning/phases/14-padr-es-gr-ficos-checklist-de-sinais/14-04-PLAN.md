---
phase: 14-padr-es-gr-ficos-checklist-de-sinais
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - src/analista/core/indicators.py
  - tests/test_indicators.py
autonomous: true
requirements: [SIG-01, PAT-01]

must_haves:
  truths:
    - "A engine expõe um checklist de sinais (rompimento, cruzamento_mm, rsi, macd, padrao, volume) com status liga/desliga"
    - "O checklist é read-only: agrega rótulos JÁ computados das famílias, sem recalcular indicador"
    - "calcular() popula os campos aditivos padroes e checklist em SinaisTecnicos"
    - "Frame curto/sem padrão degrada para Padroes(lista=[]) e checklist com sinais ativo=False, sem exceção"
  artifacts:
    - path: "src/analista/core/indicators.py"
      provides: "_checklist() + wiring de _padroes/_checklist em calcular()"
      contains: "def _checklist"
    - path: "tests/test_indicators.py"
      provides: "Goldens de checklist + integração via calcular() + degradação graciosa"
      contains: "def test_checklist"
  key_links:
    - from: "src/analista/core/indicators.py::calcular"
      to: "SinaisTecnicos.padroes / SinaisTecnicos.checklist"
      via: "padroes=_padroes(...); checklist=_checklist(...) nos kwargs do return"
      pattern: "checklist=_checklist"
    - from: "_checklist"
      to: "rótulos de tendencia/canais/momentum/volume/padroes"
      via: "leitura de strings já validadas pelos goldens (zero recálculo)"
      pattern: "rompimento_donchian"
---

<objective>
Implementar `_checklist(tend, canais, mom, vol, padroes)` (SIG-01) — agregação read-only de rótulos JÁ
computados em booleanos liga/desliga — e fazer o WIRING aditivo de `_padroes`/`_checklist` em `calcular`,
populando os campos `padroes`/`checklist` de `SinaisTecnicos`. Fecha a integração ponta-a-ponta da Fase 14.

Purpose: O checklist torna explícito *por que* o setup existe (lista de flags ligadas), sem nunca
recomendar (firewall de copy D-01). O wiring conecta os detectores ao contrato consumido pela Fase 15.
Output: `_checklist`, wiring em `calcular`, goldens de checklist + integração + degradação. Suíte verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-PATTERNS.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-03-SUMMARY.md

<interfaces>
<!-- Rótulos JÁ computados que _checklist apenas LÊ (zero recálculo). -->

```python
Canais.rompimento_donchian   # "nova_maxima"|"perda_minima"|"nenhum"|"indisponivel"
Tendencia.cruzamento         # "golden_cross"|"death_cross"|"nenhum"|"indisponivel"
Momentum.nivel_rsi           # "sobrecomprado"|"sobrevendido"|"neutro"|"indisponivel"
Momentum.cruzamento_macd     # "cruz_alta"|"cruz_baixa"|"nenhum"|"indisponivel"
Volume.rompimento_com_volume # bool
Padroes.lista                # [PadraoGrafico(...)], cada um com .estado
```

Forma do checklist (RESEARCH §Pattern 4 230–253): `Sinal(nome, ativo, detalhe)` × Checklist(sinais=[...]).
calcular() (indicators.py 921–972) — bloco de famílias de PREÇO sobre `nominal` (940–961) e return (962–972).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `_checklist` + wiring de _padroes/_checklist em `calcular`</name>
  <files>src/analista/core/indicators.py</files>
  <read_first>
    - src/analista/core/indicators.py (`calcular` 921–972 — extrair `volume = _volume(ohlc, cfg)` p/ variável;
      bloco de PREÇO sobre `nominal` 940–961; return kwargs 962–972; idioma de rótulos discretos de
      Tendencia/Canais/Momentum/Volume)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md (§Pattern 4 230–253)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-PATTERNS.md (§_checklist 142–164, §wiring 168–192)
  </read_first>
  <action>
    Criar `def _checklist(tend, canais, mom, vol, padroes) -> "Checklist":` que apenas LÊ rótulos e compõe
    `Checklist(sinais=[...])` com 6 `Sinal(nome, ativo, detalhe)` (chaves estáveis, NUNCA copy natural):
    - `"rompimento"`: ativo = `canais.rompimento_donchian not in ("nenhum","indisponivel")`; detalhe = o rótulo.
    - `"cruzamento_mm"`: ativo = `tend.cruzamento in ("golden_cross","death_cross")`; detalhe = o rótulo.
    - `"rsi"`: ativo = `mom.nivel_rsi in ("sobrecomprado","sobrevendido")`; detalhe = o rótulo.
    - `"macd"`: ativo = `mom.cruzamento_macd in ("cruz_alta","cruz_baixa")`; detalhe = o rótulo.
    - `"padrao"`: ativo = `any(p.estado == "confirmado" for p in padroes.lista)`; detalhe = um resumo neutro
      (ex.: junção dos "tipo:estado" da lista, ou "nenhum" se vazia). NÃO usar linguagem natural.
    - `"volume"`: ativo = `bool(vol.rompimento_com_volume)`; detalhe = "rompimento_com_volume".
    Guard de degradação: se `padroes is None` tratar como `Padroes(lista=[])`; nunca levantar.
    "ativo" = sinal disparado/relevante — JAMAIS "compre/venda" (firewall de copy é da Fase 16).

    No `calcular`, fazer o wiring ADITIVO (mesmo padrão das famílias de PREÇO sobre `nominal`, D-02):
    - Extrair `volume = _volume(ohlc, cfg)` para uma VARIÁVEL antes do return (hoje roda inline) — mudança
      local não-quebrante; reusar essa variável no return.
    - `padroes = _padroes(pivos, nominal, volume, cfg)` (família de PREÇO → frame nominal, igual a _pivos/_niveis).
    - `checklist = _checklist(tendencia_obj, canais, momentum_obj, volume, padroes)` — extrair
      `tendencia = _tendencia(close, cfg)` e `momentum = _momentum(close, cfg)` para variáveis se ainda inline,
      e reusá-las no return (não recalcular).
    - Adicionar `padroes=padroes, checklist=checklist` aos kwargs do `return SinaisTecnicos(...)` SEM reordenar
      os existentes.
    Chamadas existentes de `calcular` (sem `ohlc_nominal`) permanecem idênticas; os ~252 goldens seguem verdes.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -c "from analista.core import indicators; import pandas as pd, numpy as np, yaml; cfg=yaml.safe_load(open('config.yaml')); df=pd.DataFrame({c:np.linspace(50,70,60) for c in ('Open','High','Low','Close')}|{'Volume':np.full(60,1e3)}, index=pd.date_range('2021-01-01',periods=60,freq='B')); s=indicators.calcular(df,cfg); assert s.padroes is not None and s.checklist is not None and len(s.checklist.sinais)==6; print('OK')"</automated>
  </verify>
  <done>`_checklist` existe; `calcular` popula padroes e checklist; chamadas existentes inalteradas.</done>
  <acceptance_criteria>
    - `calcular(df, cfg)` retorna `SinaisTecnicos` com `padroes` (Padroes) e `checklist` (Checklist de 6 sinais).
    - `_checklist` não chama nenhuma função de cálculo de indicador (só lê rótulos/strings).
    - `volume`, `tendencia`, `momentum` são computados UMA vez e reusados (sem recomputo no return).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Goldens de checklist + integração via `calcular` + degradação graciosa</name>
  <files>tests/test_indicators.py</files>
  <read_first>
    - tests/test_indicators.py (`_cfg_ind` 15–19, `_frame_ohlcv` 861–870, `_frame_ohlc_longo` 299, testes de
      degradação via calcular ~910–932 — molde frame curto → defaults)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md (§Pattern 4 230–253)
  </read_first>
  <action>
    Adicionar goldens em `tests/test_indicators.py`:
    - `test_checklist_liga_desliga`: construir objetos de família com rótulos conhecidos (ou via `calcular`
      num frame controlado) e assert que cada `Sinal.ativo` reflete o rótulo: ex. `rompimento_donchian="nenhum"`
      → sinal "rompimento" ativo=False; `cruzamento="golden_cross"` → "cruzamento_mm" ativo=True;
      `nivel_rsi="sobrecomprado"` → "rsi" ativo=True; etc. Os 6 nomes presentes e estáveis.
    - `test_checklist_padrao_confirmado_ativa`: `Padroes(lista=[PadraoGrafico(estado="confirmado", ...)])` →
      sinal "padrao" ativo=True; `Padroes(lista=[])` → ativo=False, detalhe neutro (não copy natural).
    - `test_calcular_integra_padroes_checklist`: `calcular(frame_longo, cfg)` → `s.padroes` é Padroes e
      `s.checklist.sinais` tem 6 itens com nomes esperados; nenhum golden existente quebra.
    - `test_calcular_frame_curto_degrada`: frame com poucas barras → `s.padroes.lista == []` e todos os
      `s.checklist.sinais` com `ativo is False`, SEM exceção (degradação graciosa).
    - `test_checklist_sem_copy_natural`: assert que nenhum `detalhe`/`nome` contém termos imperativos
      ("compre","venda","compra","compre agora") — firewall de copy D-01 (rótulos = chaves estáveis).
    Limiares/config via `_cfg_ind()`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -m pytest tests/test_indicators.py -k "checklist or calcular_integra or calcular_frame_curto" -q</automated>
  </verify>
  <done>Goldens de checklist/integração/degradação verdes; suíte inteira verde.</done>
  <acceptance_criteria>
    - `pytest tests/test_indicators.py -k "checklist or calcular_integra or calcular_frame_curto"` sai 0.
    - O teste de degradação prova `padroes.lista == []` e todos os sinais `ativo=False` em frame curto, sem exceção.
    - O teste anti-copy prova ausência de linguagem imperativa nos rótulos.
    - `pytest -q` (suíte inteira) verde.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| famílias de SinaisTecnicos → `_checklist` | Rótulos já validados por golden; agregação read-only, sem recálculo |
| `_checklist` → UI (Fase 16) | Rótulos são chaves estáveis/neutras; copy review é gate da Fase 16 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Denial of Service | `_checklist`/`calcular` com padroes None ou frame curto | mitigate | Guard `padroes is None`→lista vazia; degradação via `calcular` já existente; teste de frame curto |
| T-14-10 | Information Disclosure (regulatório) | rótulo virar "recomendação" | accept | Rótulos são chaves estáveis; copy review é gate da Fase 16; teste anti-copy nesta fase reduz risco |
</threat_model>

<verification>
- `pytest tests/test_indicators.py -q` verde.
- `pytest -q` (suíte inteira, ~252 + novos da fase) verde.
- `calcular()` popula padroes e checklist; degradação graciosa provada.
</verification>

<success_criteria>
- `_checklist` agrega 6 sinais liga/desliga read-only; `calcular` popula padroes/checklist.
- Frame curto degrada sem exceção.
- Rótulos neutros (sem copy natural).
- Suíte inteira verde.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-04-SUMMARY.md`.
</output>
