---
phase: 14-padr-es-gr-ficos-checklist-de-sinais
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/analista/core/indicators.py
  - tests/test_indicators.py
autonomous: true
requirements: [PAT-01]

must_haves:
  truths:
    - "A engine detecta duplo topo e duplo fundo sobre pivôs confirmados"
    - "Cada padrão recebe rótulo em_formacao vs confirmado (confirmado exige rompimento da neckline na barra fechada + volume)"
    - "O alvo measured-move é a altura da neckline projetada (para baixo no duplo topo, para cima no duplo fundo)"
    - "O rótulo de um duplo topo/fundo em t é imutável em t+1 para barras fechadas (no-repaint)"
  artifacts:
    - path: "src/analista/core/indicators.py"
      provides: "_padroes() detectando duplo_topo/duplo_fundo, retornando Padroes(lista=[...])"
      contains: "def _padroes"
    - path: "tests/test_indicators.py"
      provides: "Goldens de geometria + measured-move + estado + no-repaint do duplo topo/fundo"
      contains: "def test_padroes_duplo"
  key_links:
    - from: "src/analista/core/indicators.py::_padroes"
      to: "Pivos.pivot_high / Pivos.pivot_low"
      via: ".dropna() sobre pivôs confirmados"
      pattern: "pivot_high.dropna"
    - from: "_padroes (confirmação)"
      to: "barra fechada iloc[-2] + Volume.volume_acima_mm"
      via: "close fechado além da neckline E volume_acima_mm"
      pattern: "iloc\\[-2\\]"
---

<objective>
Implementar o detector de **duplo topo** e **duplo fundo** como função pura `_padroes(pivos, nominal,
volume, cfg)` em `core/indicators.py`, retornando `Padroes(lista=[PadraoGrafico, ...])`. Geometria sobre
pivôs CONFIRMADOS (Fase 13), neckline horizontal, máquina de estado em_formacao/confirmado e alvo
measured-move. Inclui o GATE de no-repaint do duplo (obrigatório).

Purpose: Entregar a metade simples (neckline horizontal) de PAT-01 — o duplo é o caso de 2/3 pivôs;
a OCO (neckline inclinada, 5 pivôs) vem no plano 03 estendendo a mesma `_padroes`.
Output: `_padroes` com detecção de duplo_topo/duplo_fundo + goldens (geometria, measured-move, estado,
no-repaint truncação). Suíte inteira verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-PATTERNS.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-01-SUMMARY.md

<interfaces>
<!-- Contratos do plano 01 que este plano CONSOME. -->

PadraoGrafico/Padroes (criados no plano 01):
```python
@dataclass
class PadraoGrafico:
    tipo: str; estado: str; neckline: float; alvo: float; altura: float; pivos_envolvidos: dict
@dataclass
class Padroes:
    lista: list = field(default_factory=list)
```

Pivos (indicators.py 86–93) — insumo no-repaint:
```python
pivot_high: pd.Series   # preço nas barras de topo CONFIRMADO, NaN nas demais
pivot_low:  pd.Series   # preço nas barras de fundo CONFIRMADO, NaN nas demais
```

Volume.volume_acima_mm (campo bidirecional do plano 01) — confirmação direção-agnóstica.
Helper de teste `_pivos_ts(topos: dict, fundos: dict, n_barras=10)` (test 949–964) injeta pivôs
determinísticos com timestamps; `_frame_ohlcv(close, volume=...)` (861–870) constrói OHLCV sintético.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `_padroes` — detecção de duplo topo / duplo fundo</name>
  <files>src/analista/core/indicators.py</files>
  <read_first>
    - src/analista/core/indicators.py (`_niveis_sr` 634–688 e `_dow` 545–592 — idioma de consumir pivôs
      via .dropna(), ler cfg, degradar graciosamente retornando dataclass; `_niveis_stop_rr` 768–828 e
      `_canais` 248–320 — guard np.errstate em razões)
    - src/analista/core/indicators.py (`_volume` 829–861 — idioma iloc[-2] barra fechada)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md (§Pattern 1 153–187, §Pattern 3 217–228)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-PATTERNS.md (§_padroes 89–138)
    - config.yaml (bloco `padroes:` criado no plano 01)
  </read_first>
  <action>
    Criar `def _padroes(pivos, nominal, volume, cfg) -> "Padroes":` em `indicators.py` (perto de
    `_niveis_sr`). Ler `cfg["padroes"]`. Guard de degradação graciosa no topo: se `pivos is None` ou
    `nominal` curto (`len(nominal) < 2`) → `return Padroes(lista=[])` ANTES de qualquer cálculo (idioma
    `_volume`/`_niveis_sr`). NUNCA levantar.

    **Duplo topo:** `topos = pivos.pivot_high.dropna()`; se `len(topos) >= 2`, pegar os dois mais recentes
    `t1, t2 = topos.iloc[-2], topos.iloc[-1]` com timestamps `ts1, ts2`. Simetria de preço
    `simetria = abs(t1 - t2) / ((t1 + t2) / 2)` sob `np.errstate(divide="ignore", invalid="ignore")`.
    Vale intermediário = `pivos.pivot_low` com índice estritamente ENTRE `ts1` e `ts2`, `.dropna()`;
    neckline = `float(vale.min())`. Casa se `simetria <= cfg_pad["price_tolerance_pct"]` E há vale.
    `altura = (t1 + t2)/2 - neckline`; `altura_pct = altura / neckline` (guard). Casa só se
    `altura_pct >= cfg_pad["min_pattern_height_pct"]` (anti-ruído). Estado na barra FECHADA: `close_f =
    nominal["Close"].iloc[-2]`; `rompeu = close_f < neckline`; `confirmado = rompeu and (volume.volume_acima_mm
    if cfg_pad["exigir_volume_confirma"] else True)`; estado = "confirmado" se confirmado senão "em_formacao".
    `alvo = neckline - altura` (projeção measured-move p/ BAIXO). Anexar `PadraoGrafico(tipo="duplo_topo",
    estado, neckline, alvo, altura, pivos_envolvidos={ts1:t1, ts2:t2, ts_vale: neckline})`.

    **Duplo fundo (espelho):** `pivos.pivot_low.dropna()`, dois fundos mais recentes, pico intermediário
    via `pivot_high` entre eles → neckline = `float(pico.max())`; `altura = neckline - (f1+f2)/2`;
    `rompeu = close_f > neckline`; `alvo = neckline + altura` (p/ CIMA); `tipo="duplo_fundo"`.

    Retornar `Padroes(lista=lista)` (pode conter 0, 1 ou 2 padrões). Todos os rótulos = chaves estáveis (D-01).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -c "from analista.core import indicators; import inspect; assert hasattr(indicators,'_padroes'); print('OK')"</automated>
  </verify>
  <done>`_padroes` existe, detecta duplo_topo e duplo_fundo, retorna Padroes(lista), degrada graciosamente.</done>
  <acceptance_criteria>
    - `indicators._padroes` é chamável e retorna `Padroes` para pivôs válidos, e `Padroes(lista=[])`
      para `pivos=None` ou frame curto, SEM levantar.
    - Simetria e altura usam guard contra divisão por zero (neckline ~0 não explode).
    - Confirmação lê SEMPRE `iloc[-2]` (barra fechada), nunca `iloc[-1]`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Goldens do duplo topo/fundo + GATE no-repaint (obrigatório)</name>
  <files>tests/test_indicators.py</files>
  <read_first>
    - tests/test_indicators.py (`_pivos_ts` 949–964, `_frame_ohlcv` 861–870, `_cfg_ind` 15–19,
      `test_niveis_fib_alta` 967–983 — molde de golden de geometria com pivôs injetados;
      `test_pivos_no_repaint_truncacao` 433–455 — molde EXATO do gate de truncação)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md (§Code Examples 351–377)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-PATTERNS.md (§goldens 221–274)
  </read_first>
  <action>
    Adicionar goldens em `tests/test_indicators.py`:
    - `test_padroes_duplo_topo_geometria`: usar `_pivos_ts(topos={i1:p, i2:p}, fundos={imeio:vale})` com dois
      topos de preço ~igual e um vale entre eles; construir `nominal` via `_frame_ohlcv` cujo Close[-2]
      ainda esteja ACIMA da neckline → assert um `PadraoGrafico(tipo="duplo_topo", estado="em_formacao")`,
      `neckline == pytest.approx(vale)`, `altura == pytest.approx(topo - vale)`,
      `alvo == pytest.approx(neckline - altura)` (measured-move p/ baixo).
    - `test_padroes_duplo_topo_confirmado`: mesmo arranjo, mas Close[-2] ABAIXO da neckline E
      `volume.volume_acima_mm True` (frame `_frame_ohlcv` com volume alto na barra fechada) → estado="confirmado".
    - `test_padroes_duplo_fundo`: espelho (dois fundos + pico; rompe p/ cima; alvo = neckline + altura).
    - `test_padroes_simetria_frouxa_nao_casa`: dois topos com diferença de preço acima de
      `price_tolerance_pct` → lista vazia (sem falso positivo).
    - `test_padroes_no_repaint_truncacao_duplo` (GATE OBRIGATÓRIO — espelha test_pivos_no_repaint_truncacao):
      construir um frame OHLCV sintético com um duplo topo (estilo `_frame_duplo_topo` do RESEARCH 364–375);
      `full = indicators._padroes(indicators._pivos(df, cfg), df, indicators._volume(df, cfg), cfg)`;
      para `k` em barras fechadas, `pk = _padroes(_pivos(df.iloc[:k]), df.iloc[:k], _volume(df.iloc[:k]), cfg)`;
      assert que o ESTADO/neckline/alvo de cada padrão âncora ancorado em barras já fechadas é IDÊNTICO entre
      `pk` e `full` (a transição em_formacao→confirmado ao chegar barra nova NÃO é repaint — comparar apenas
      a barra fechada de referência, não a borda viva).
    Todos os limiares lidos via `_cfg_ind()["padroes"]` — zero constantes hardcoded no teste.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -m pytest tests/test_indicators.py -k "padroes_duplo or padroes_no_repaint or padroes_simetria" -q</automated>
  </verify>
  <done>Goldens de duplo topo/fundo verdes; gate de no-repaint do duplo verde; suíte inteira verde.</done>
  <acceptance_criteria>
    - `pytest tests/test_indicators.py -k "padroes_duplo or padroes_no_repaint or padroes_simetria"` sai 0.
    - O teste de no-repaint prova que o rótulo/alvo de um duplo em barra fechada não muda ao truncar a série.
    - O teste de simetria frouxa prova ausência de falso positivo (lista vazia).
    - `pytest -q` (suíte inteira) verde.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pivôs (Fase 13) → `_padroes` | Pivôs já confirmados/no-repaint; o detector apenas compõe geometria |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Denial of Service | `_padroes` com pivos None / frame curto / NaN | mitigate | Guard de degradação no topo → `Padroes(lista=[])`, nunca levanta |
| T-14-04 | Tampering | divisão por zero em simetria/altura (neckline ~0) | mitigate | `np.errstate(divide,invalid="ignore")` + checagem `np.isfinite`, idioma `_niveis_stop_rr` |
| T-14-05 | Repaint (Integrity) | confirmação na barra viva → padrão "pisca" | mitigate | Confirmação SEMPRE `iloc[-2]`; gate de no-repaint por truncação é teste obrigatório |
</threat_model>

<verification>
- `pytest tests/test_indicators.py -q` verde.
- `pytest -q` (suíte inteira, ~252 + novos) verde.
- Gate de no-repaint do duplo presente e verde.
</verification>

<success_criteria>
- `_padroes` detecta duplo_topo e duplo_fundo sobre pivôs confirmados, com estado e measured-move.
- Confirmação exige rompimento da neckline na barra fechada + volume (config-driven).
- No-repaint do duplo provado por truncação.
- Suíte inteira verde.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-02-SUMMARY.md`.
</output>
