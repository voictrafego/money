---
phase: 05-motor-de-indicadores-puro
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/analista/core/indicators.py
  - tests/test_indicators.py
autonomous: true
requirements: [CHAN-01, CHAN-02, CHAN-03, TEST-04]
must_haves:
  truths:
    - "Donchian 20 and 55 channels are computed with .shift(1) so the channel is defined by PAST bars only (no-repaint breakout)"
    - "rompimento_donchian == nova_maxima / perda_minima / nenhum, using the Donchian-20 channel as primary breakout"
    - "Bollinger bands use SMA20 +/- 2*std(ddof=0) (population std, TradingView convention)"
    - "toque_bollinger labels band touch/break as contexto (banda_superior / banda_inferior / nenhum)"
    - "Bollinger squeeze: squeeze_on when normalized width (sup-inf)/med is <= percentil 20 over a trailing 126-bar window (D-02), else squeeze_off"
    - "squeeze_pct percentile is causal/trailing (uses only the window up to the current bar) and is 'indisponivel' before 126 bars"
    - "Donchian breakout is no-repeat: ind(serie[:k])[-1] == ind(serie)[k-1]"
    - "The 64 existing valuation golden tests stay green (TEST-07)"
  artifacts:
    - path: "src/analista/core/indicators.py"
      provides: "_canais family — Donchian, Bollinger, squeeze percentile + discrete signals"
      contains: "def _canais"
    - path: "tests/test_indicators.py"
      provides: "squeeze causal test, Bollinger touch, Donchian breakout no-repaint, short-history"
      contains: "squeeze"
  key_links:
    - from: "_canais squeeze_pct"
      to: "largura_bb trailing percentile"
      via: "rolling(126).apply(lambda x: (x<=x[-1]).mean()*100, raw=True)"
      pattern: "rolling\\(.*apply"
    - from: "_canais donchian"
      to: "high/low rolling max/min shift(1)"
      via: "causal channel from prior n bars"
      pattern: "shift\\(1\\)"
---

<objective>
Lock the Canais family (CHAN-01..03): Donchian 20/55 channels with labeled breakouts, Bollinger 20/2σ bands with touch context, and the percentile-based Bollinger squeeze (D-02). All channel math is causal (no future leakage) and frozen by golden tests, including a dedicated causality/no-repaint test for the squeeze percentile and Donchian breakout.

Purpose: Channels feed the Phase 6 reverification alert (Donchian "perda_minima" is a trend-loss trigger) and Phase 7 overlays. The squeeze percentile is the subtlest causal pitfall in this family — it must be trailing, never over the whole series.
Output: `_canais` added to `src/analista/core/indicators.py`; Donchian/Bollinger/squeeze golden + no-repaint tests added to `tests/test_indicators.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-motor-de-indicadores-puro/05-CONTEXT.md
@.planning/phases/05-motor-de-indicadores-puro/05-RESEARCH.md
@.planning/phases/05-motor-de-indicadores-puro/05-PATTERNS.md
@src/analista/core/indicators.py
@tests/test_indicators.py

<interfaces>
<!-- From plan 05-01 (already in the module). Use directly. -->
Canais dataclass (already defined in indicators.py):
  donchian_sup, donchian_inf, bb_sup, bb_med, bb_inf, largura_bb, squeeze_pct : pd.Series
  rompimento_donchian, toque_bollinger, squeeze : str (stable/neutral keys)
cfg access: cfg["indicadores"]["donchian"] == [20, 55];
  cfg["indicadores"]["bollinger"] == {"janela": 20, "sigma": 2.0};
  cfg["indicadores"]["squeeze_janela"] == 126; cfg["indicadores"]["squeeze_percentil"] == 20.
Input frame columns: "High", "Low", "Close" (DatetimeIndex). Always ohlc_ajustado (split-adjusted).

Discrete-signal keys (locked, D-01):
  rompimento_donchian: "nova_maxima" | "perda_minima" | "nenhum" | "indisponivel"
  toque_bollinger:     "banda_superior" | "banda_inferior" | "nenhum" | "indisponivel"
  squeeze:             "squeeze_on" | "squeeze_off" | "indisponivel"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Donchian + Bollinger (causal channels + discrete signals)</name>
  <files>src/analista/core/indicators.py, tests/test_indicators.py</files>
  <behavior>
    - donchian_sup = high.rolling(20, min_periods=20).max().shift(1); donchian_inf = low.rolling(20).min().shift(1); plus the 55-window series
    - rompimento_donchian == "nova_maxima" when close.iloc[-1] > donchian_sup.iloc[-1] (Donchian-20 primary), "perda_minima" when close.iloc[-1] < donchian_inf.iloc[-1], else "nenhum", "indisponivel" when NaN
    - bb_med = SMA20; bb_sup/bb_inf = bb_med +/- 2*std(ddof=0); ddof=0 (population) per TradingView convention
    - toque_bollinger == "banda_superior" when close.iloc[-1] >= bb_sup.iloc[-1], "banda_inferior" when <= bb_inf.iloc[-1], else "nenhum", "indisponivel" when NaN
    - Donchian breakout is no-repaint: with .shift(1) the channel never includes the current bar
  </behavior>
  <read_first>
    - src/analista/core/indicators.py (Canais contract + section style from plan 01)
    - .planning/phases/05-motor-de-indicadores-puro/05-RESEARCH.md (Pattern 6 Donchian lines 225-233; Bollinger in Pattern 4 lines 196-204; anti-patterns ddof/shift lines 235-241)
    - .planning/phases/05-motor-de-indicadores-puro/05-PATTERNS.md (series assert np.testing.assert_allclose lines 149/167)
  </read_first>
  <action>
    Implement `_canais(ohlc, cfg)` returning a partial `Canais` (squeeze added in Task 2, or build the full object in Task 2 — keep one function; Task 2 extends it). Compute donchian_sup/donchian_inf for BOTH windows from cfg donchian [20, 55] using `rolling(n, min_periods=n).max().shift(1)` and `.min().shift(1)` — the `.shift(1)` is mandatory (without it the breakout never fires because the channel contains the current close; RESEARCH anti-pattern). Store the 20-window pair as the primary donchian_sup/donchian_inf series (and keep the 55 series available for the Phase 7 plot — store as donchian_sup_55/donchian_inf_55 fields if the contract has them, otherwise expose via the primary fields and note the 55 series). Bollinger: bb_med = close.rolling(20, min_periods=20).mean(); sd = close.rolling(20, min_periods=20).std(ddof=0); bb_sup = bb_med + sigma*sd; bb_inf = bb_med - sigma*sd; ddof=0 is required (TradingView/StockCharts use population std). Discrete signals: rompimento_donchian over the Donchian-20 channel (guard NaN -> "indisponivel"); toque_bollinger by comparing close.iloc[-1] to bb_sup/bb_inf (guard NaN -> "indisponivel").
    Add tests: `test_donchian_breakout_causal` — synthetic high/low/close where the last close exceeds the prior-20 max, assert rompimento_donchian == "nova_maxima"; and a no-repaint sub-assert that recomputing on serie[:k] gives the same channel value at k-1. `test_bollinger_touch` — series where close touches the upper band, assert toque_bollinger == "banda_superior" and that bb uses ddof=0 (cross-check bb_sup against a hand-computed SMA20 + 2*population-std on a fixed slice via np.testing.assert_allclose).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_indicators.py::test_donchian_breakout_causal -x` exits 0
    - `pytest tests/test_indicators.py::test_bollinger_touch -x` exits 0
    - Donchian uses .shift(1): `grep -q 'shift(1)' src/analista/core/indicators.py`
    - Bollinger std uses ddof=0: `grep -q 'ddof=0' src/analista/core/indicators.py`
    - `.venv/bin/python -m pytest -q` exits 0 (TEST-07)
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && .venv/bin/python -m pytest tests/test_indicators.py -k "donchian or bollinger" -x -q</automated>
  </verify>
  <done>Donchian 20/55 causal channels with .shift(1) breakout labeling; Bollinger 20/2σ population-std bands with touch labeling; tests green; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bollinger squeeze (trailing percentile, D-02) + causal/short-history tests</name>
  <files>src/analista/core/indicators.py, tests/test_indicators.py</files>
  <behavior>
    - largura_bb = (bb_sup - bb_inf) / bb_med (normalized width)
    - squeeze_pct = largura_bb.rolling(126, min_periods=126).apply(lambda x: (x <= x[-1]).mean()*100, raw=True) — trailing/causal percentile of current width vs its own prior 126
    - squeeze == "squeeze_on" when squeeze_pct.iloc[-1] <= 20, "squeeze_off" when above, "indisponivel" when NaN (first valid at index 125, 0-based)
    - causal: squeeze_pct computed on serie[:k] equals squeeze_pct(serie)[k-1]
  </behavior>
  <read_first>
    - src/analista/core/indicators.py (_canais from Task 1)
    - .planning/phases/05-motor-de-indicadores-puro/05-RESEARCH.md (Pattern 4 squeeze lines 194-206; Pitfall 3 no-repaint lines 281-285; Pitfall 4 short history lines 287-291; State of the Art raw=True line 398)
  </read_first>
  <action>
    Extend `_canais` to compute largura_bb = (bb_sup - bb_inf) / bb_med (guard bb_med==0 -> NaN via np.errstate so no inf leaks). Compute squeeze_pct = largura_bb.rolling(squeeze_janela, min_periods=squeeze_janela).apply(lambda x: (x <= x[-1]).mean() * 100.0, raw=True) with squeeze_janela from cfg (126); `raw=True` is required (x is an ndarray, x[-1] is the current bar — this is what makes it causal). Discrete squeeze: "squeeze_on" if squeeze_pct.iloc[-1] <= cfg squeeze_percentil (20), "squeeze_off" if above, "indisponivel" if NaN. Populate the Canais object's largura_bb, squeeze_pct, squeeze fields.
    Add tests: `test_squeeze_percentil_causal` — build a width series with a known low-volatility tail, assert squeeze == "squeeze_on", assert squeeze_pct is NaN for the first 125 bars and first valid at index 125, and a causal sub-assert: squeeze_pct(largura[:k]).iloc[-1] == squeeze_pct(largura)[k-1] for several k > 126. `test_canais_historico_curto` — feed <126 bars, assert squeeze_pct all-NaN and squeeze == "indisponivel" (and Donchian/Bollinger with <55/<20 bars degrade to "indisponivel") with no exception.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_indicators.py::test_squeeze_percentil_causal -x` exits 0 (asserts first valid squeeze_pct at index 125 and trailing causality)
    - `pytest tests/test_indicators.py::test_canais_historico_curto -x` exits 0
    - squeeze percentile uses raw=True: `grep -q 'raw=True' src/analista/core/indicators.py`
    - `.venv/bin/python -m pytest -q` exits 0 (TEST-07)
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && .venv/bin/python -m pytest tests/test_indicators.py -k "squeeze or canais_historico_curto" -x -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>Squeeze percentile is trailing/causal (first valid at index 125), labeled squeeze_on/off/indisponivel per D-02; short history degrades gracefully; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ohlc_ajustado -> _canais | In-memory frame; short history or flat price (bb_med near 0) are the failure modes; no network/untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering (display corruption) | largura_bb = (sup-inf)/bb_med when bb_med == 0 (flat price) | mitigate | np.errstate guard so division yields NaN, not inf; squeeze then reads "indisponivel" rather than propagating inf to UI |
| T-05-04 | Denial of Service (local) | rolling percentile / Donchian on <126/<55/<20 bars | mitigate | min_periods=window -> NaN; discrete signals return "indisponivel"; no iloc on empty slice |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_indicators.py -x -q` green for all canais tests
- `.venv/bin/python -m pytest -q` green (TEST-07)
- Donchian uses shift(1); Bollinger uses ddof=0; squeeze uses raw=True (grep)
</verification>

<success_criteria>
- Donchian 20/55 with labeled breakouts, no-repaint via shift(1) (CHAN-01, TEST-04)
- Bollinger 20/2σ population-std bands with touch context (CHAN-02)
- Bollinger squeeze via trailing percentile <= 20 over 126 bars (CHAN-03, D-02)
- Short history -> "indisponivel"; 64 valuation goldens green (TEST-07)
</success_criteria>

<output>
After completion, create `.planning/phases/05-motor-de-indicadores-puro/05-02-SUMMARY.md`
</output>
