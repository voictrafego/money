---
phase: 05-motor-de-indicadores-puro
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/analista/core/indicators.py
  - tests/test_indicators.py
autonomous: false
requirements: [FORCE-01, FORCE-02, TEST-03, TEST-04, TEST-05]
must_haves:
  truths:
    - "adx_wilder uses the full Wilder chain TR/+DM/-DM -> smoothed -> +DI/-DI -> DX -> ADX with DOUBLE Wilder smoothing"
    - "The second ADX smoothing seeds at the first valid DX (index=length), so the first valid ADX appears at index 2*length-1 == 27 for length 14"
    - "forca_adx == 'sem_tendencia' when adx.iloc[-1] < 20, 'forte' when > 25, 'neutro' between, 'indisponivel' when NaN"
    - "Regression slope is annualized %/ano normalized by mean price (slope*252/seg.mean()*100) over a trailing 90-bar window, with R2 = rvalue**2 (D-04)"
    - "indicators.calcular(ohlc, cfg) returns a SinaisTecnicos with .tendencia/.canais/.forca/.momentum populated for all 4 families"
    - "calcular degrades gracefully when ohlc is None/empty/short — returns 'indisponivel' signals, no exception"
    - "ADX is no-repaint: ind(serie[:k])[-1] == ind(serie)[k-1]"
    - "The split-adjusted ITSA4 series (5 splits) produces NO spurious golden/death cross or Donchian breakout on the split dates (TEST-05)"
    - "ADX reference values are cross-checked against TradingView and frozen as literals (TEST-03 human checkpoint)"
    - "The 64 existing valuation golden tests stay green (TEST-07)"
  artifacts:
    - path: "src/analista/core/indicators.py"
      provides: "adx_wilder, _forca, regressao_trailing, and the calcular() entry point assembling all 4 families"
      contains: "def calcular"
    - path: "tests/test_indicators.py"
      provides: "ADX structural invariant + TradingView reference, regression, no-repaint, calcular full, split TEST-05"
      contains: "def test_split_sem_cross_espurio"
  key_links:
    - from: "adx_wilder second smoothing"
      to: "_wilder_rma_from(dx, length, start=length)"
      via: "seed at first valid DX, first ADX at index 27"
      pattern: "_wilder_rma_from\\(.*start"
    - from: "indicators.calcular"
      to: "_tendencia, _canais, _forca, _momentum"
      via: "assemble SinaisTecnicos from the 4 family functions"
      pattern: "SinaisTecnicos\\("
    - from: "test_split_sem_cross_espurio"
      to: "prices._ajustar_por_split(_hist_itsa4_multisplit())"
      via: "feed split-adjusted frame to calcular, assert no cross/breakout on event dates"
      pattern: "_ajustar_por_split|_ITSA4_EVENTOS"
---

<objective>
Lock the Forca family (FORCE-01..02): ADX(14) via the full double-Wilder chain and the linear-regression slope (%/ano + R², D-04). Then assemble the `indicators.calcular(ohlc, cfg)` entry point that returns a complete `SinaisTecnicos` across all 4 families, and freeze the two hardest golden gates: the split-stress test (TEST-05, ITSA4 5 splits, no spurious cross/breakout) and the ADX TradingView cross-check (TEST-03, human checkpoint).

Purpose: ADX double-smoothing seeding is the second-trickiest pitfall (wrong seed -> all-NaN). The TradingView ADX anchor cannot be derived offline — it requires a human cross-check before the golden is frozen. This plan closes the phase: after it, the math is fully locked and ready for Phase 6 integration.
Output: `adx_wilder`, `regressao_trailing`, `_forca`, and `calcular()` in `src/analista/core/indicators.py`; ADX/regression/no-repaint/calcular/split tests in `tests/test_indicators.py`; frozen TradingView ADX literals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-motor-de-indicadores-puro/05-CONTEXT.md
@.planning/phases/05-motor-de-indicadores-puro/05-RESEARCH.md
@.planning/phases/05-motor-de-indicadores-puro/05-PATTERNS.md
@src/analista/core/indicators.py
@tests/test_indicators.py
@src/analista/ingest/prices.py
@tests/test_ingest_ohlc.py

<interfaces>
<!-- From plans 05-01/05-02 (already in the module). Use directly. -->
Forca dataclass: adx, pdi, ndi, regressao_slope_ann, regressao_r2 : pd.Series; forca_adx : str
SinaisTecnicos: tendencia / canais / forca / momentum (all populated by calcular)
Family functions already present: _tendencia(close,cfg) -> Tendencia; _canais(ohlc,cfg) -> Canais; _momentum(close,cfg) -> Momentum
Helper: _wilder_rma_from(arr, length, start=0) -> np.ndarray (use start=length for ADX 2nd smoothing)
cfg: cfg["indicadores"]["adx_janela"] == 14; cfg["indicadores"]["regressao_janela"] == 90

Split fixture (tests/test_ingest_ohlc.py — import or replicate):
  _hist_itsa4_multisplit() -> (hist, A)  # hist = nominal w/ 5 split steps; A = continuous adjusted
  _ITSA4_EVENTOS = [(2021-12-15,1.10),(2022-11-15,1.05),(2023-11-15,1.10),(2024-12-15,1.20),(2025-12-15,1.10)]
  src/analista/ingest/prices.py: _ajustar_por_split(hist) -> pd.DataFrame (split-adjusted frame)

ADX invariants (RESEARCH, verified): seed 2nd smoothing at start=length; first valid ADX at index 2*length-1 = 27 for length 14.
Regression (scipy.stats.linregress): slope_ann = res.slope*252/seg.mean()*100; r2 = res.rvalue**2 (D-04).
forca_adx keys: "sem_tendencia" | "forte" | "neutro" | "indisponivel".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ADX(14) double-Wilder + regression slope/R2 (Forca family)</name>
  <files>src/analista/core/indicators.py, tests/test_indicators.py</files>
  <behavior>
    - +DM/-DM/TR per bar (Wilder DMI); each smoothed via _wilder_rma_from(arr, length) (first valid at index length)
    - +DI = 100*sm(+DM)/ATR; -DI = 100*sm(-DM)/ATR; DX = 100*|+DI - -DI|/(+DI + -DI)
    - ADX = _wilder_rma_from(dx_array, length, start=length) — second smoothing seeded at first valid DX; first valid ADX at index 27 (length 14)
    - With the wrong seed (start=0) ADX is all-NaN — the test asserts the first valid ADX index == 27, catching that bug
    - forca_adx from adx.iloc[-1] vs 20/25 cuts; "indisponivel" when NaN
    - regressao: slope_ann (%/ano) and r2 over trailing 90-bar window via scipy.stats.linregress; causal (trailing only)
    - ADX is no-repaint: adx(s[:k])[-1] == adx(s)[k-1]
  </behavior>
  <read_first>
    - src/analista/core/indicators.py (_wilder_rma_from + Forca contract)
    - .planning/phases/05-motor-de-indicadores-puro/05-RESEARCH.md (Pattern 3 ADX lines 179-192; Pitfall 2 seeding lines 275-279; Pattern 5 regression lines 208-223; code example lines 304-312)
    - tests/test_ddm.py (assert idiom); .planning/phases/05-motor-de-indicadores-puro/05-PATTERNS.md (series assert lines 149/167)
  </read_first>
  <action>
    Implement `adx_wilder(ohlc, length=14)` returning (adx, pdi, ndi) Series. Compute up = high.diff(); dn = -low.diff(); +DM = up where (up>dn and up>0) else 0; -DM = dn where (dn>up and dn>0) else 0; TR = max(high-low, |high-close_prev|, |low-close_prev|). Smooth +DM, -DM, TR each with `_wilder_rma_from(arr, length)` (single smoothing, first valid at index length). +DI = 100*sm_pdm/atr; -DI = 100*sm_ndm/atr (np.errstate guard for atr==0). DX = 100*abs(pdi-ndi)/(pdi+ndi) (guard denom 0). ADX = `_wilder_rma_from(dx.to_numpy(float), length, start=length)` — start=length is mandatory (seeding at 0 makes the SMA-seed average 14 NaNs -> all-NaN ADX, RESEARCH Pitfall 2). Implement `regressao_trailing(close, win=90)` per RESEARCH Pattern 5: for each i from win-1, seg = y[i-win+1:i+1]; res = stats.linregress(arange(win), seg); slope_ann[i] = res.slope*252/seg.mean()*100; r2[i] = res.rvalue**2. Implement `_forca(ohlc, cfg)` -> Forca: adx/pdi/ndi from adx_wilder; regressao_slope_ann/regressao_r2 from regressao_trailing; forca_adx by comparing adx.iloc[-1] to 20 and 25 ("sem_tendencia" < 20, "forte" > 25, "neutro" between, "indisponivel" NaN).
    Add tests: `test_adx_wilder_estrutural` — synthetic deterministic OHLC (np.linspace + seeded noise per RESEARCH line 468), assert the first non-NaN ADX is at index 27 (2*length-1) and that adx.dropna() is non-empty (catches the start=0 bug); plus a no-repaint sub-assert adx(s[:k]).iloc[-1] == pytest.approx(adx(s).iloc[k-1]) for several k. `test_regressao_slope_r2` — a perfectly linear up-series, assert r2 ~ 1.0 and slope_ann > 0; a flat series, assert slope_ann ~ 0. Do NOT freeze TradingView numeric ADX values yet — that is Task 3 (human checkpoint).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_indicators.py::test_adx_wilder_estrutural -x` exits 0 (asserts first valid ADX index == 27 AND adx.dropna() non-empty)
    - `pytest tests/test_indicators.py::test_regressao_slope_r2 -x` exits 0
    - ADX second smoothing seeds at start=length: `grep -Eq '_wilder_rma_from\([^)]*start' src/analista/core/indicators.py`
    - regression uses scipy linregress: `grep -q 'linregress' src/analista/core/indicators.py`
    - `.venv/bin/python -m pytest -q` exits 0 (TEST-07)
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && .venv/bin/python -m pytest tests/test_indicators.py -k "adx_wilder_estrutural or regressao_slope_r2" -x -q</automated>
  </verify>
  <done>ADX double-Wilder chain seeded correctly (first valid at index 27, no-repaint); regression %/ano + R² over trailing 90 bars; forca_adx labeled; tests green; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: calcular() entry point assembling all 4 families + split TEST-05</name>
  <files>src/analista/core/indicators.py, tests/test_indicators.py</files>
  <behavior>
    - calcular(ohlc, cfg) returns SinaisTecnicos(tendencia, canais, forca, momentum) — all 4 families populated from the family functions
    - edge guard: ohlc None / empty / missing OHLC columns -> a SinaisTecnicos with all-NaN series and every discrete signal "indisponivel"; never raises
    - Feeding the split-adjusted ITSA4 frame (5 splits) yields NO golden/death cross and NO Donchian breakout on the 5 split event dates (continuous series, TEST-05)
  </behavior>
  <read_first>
    - src/analista/core/indicators.py (the 4 family functions _tendencia/_canais/_forca/_momentum)
    - src/analista/core/ddm.py (entry-point edge-guard idiom: guard at top, ddm.py lines 78-115)
    - tests/test_ingest_ohlc.py (_hist_itsa4_multisplit lines 214-262, _ITSA4_EVENTOS line 204; consumption idiom lines 255-273)
    - src/analista/ingest/prices.py (_ajustar_por_split signature line 70)
    - .planning/phases/05-motor-de-indicadores-puro/05-PATTERNS.md (TEST-05 reuse lines 161-169; entry-point guard lines 68-81)
  </read_first>
  <action>
    Implement `calcular(ohlc, cfg)` -> SinaisTecnicos. Guard at top (mirror ddm.ddm_dois_estagios guard): if ohlc is None or len(ohlc)==0 or required columns ("Open"/"High"/"Low"/"Close") missing, return a fully-"indisponivel" SinaisTecnicos (build each family with NaN series and "indisponivel" discrete signals — add a small private `_indisponivel_*` builder or pass empty series so the family functions degrade naturally; prefer routing the empty/short frame through the family functions so degradation is single-sourced). Otherwise close = ohlc["Close"]; assemble SinaisTecnicos(tendencia=_tendencia(close,cfg), canais=_canais(ohlc,cfg), forca=_forca(ohlc,cfg), momentum=_momentum(close,cfg)). calcular consumes the split-adjusted frame (CR-01) and is timeframe-agnostic (receives whatever frame it is given; Phase 6 handles weekly resample).
    Add tests: `test_calcular_completo` — a sufficiently long synthetic OHLC frame, assert calcular returns a SinaisTecnicos with .tendencia/.canais/.forca/.momentum and that each family's discrete signals are non-"indisponivel" (full history). `test_calcular_degrada` — ohlc=None and a short (<30 bars) frame, assert calcular returns "indisponivel" across families and does not raise. `test_split_sem_cross_espurio` (TEST-05) — import `_hist_itsa4_multisplit` and `_ITSA4_EVENTOS` from tests.test_ingest_ohlc (or replicate), aj = prices._ajustar_por_split(hist); run calcular on aj and assert that on each of the 5 _ITSA4_EVENTOS dates there is no golden/death cross transition and no Donchian breakout (i.e. compute the per-bar cross/breakout series on the adjusted frame and assert the split dates are clean — the adjusted series is continuous so no step-induced false signal).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_indicators.py::test_calcular_completo -x` exits 0 (SinaisTecnicos has populated tendencia/canais/forca/momentum)
    - `pytest tests/test_indicators.py::test_calcular_degrada -x` exits 0 (ohlc=None and short frame -> "indisponivel", no exception)
    - `pytest tests/test_indicators.py::test_split_sem_cross_espurio -x` exits 0 (no spurious cross/breakout on the 5 ITSA4 split dates)
    - `.venv/bin/python -m pytest -q` exits 0 (TEST-07: 64 valuation goldens green)
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && .venv/bin/python -m pytest tests/test_indicators.py -k "calcular or split_sem_cross" -x -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>calcular() returns a complete SinaisTecnicos across 4 families; degrades to "indisponivel" on None/short frames; split-adjusted ITSA4 produces no spurious cross/breakout (TEST-05); full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cross-check ADX(14) against TradingView and freeze the golden literals (TEST-03)</name>
  <what-built>
    The deterministic ADX(14) implementation (adx_wilder, double-Wilder, first valid at index 27) is locked structurally and for no-repaint (Task 1). What is NOT yet locked is the NUMERIC anchor against TradingView — the only piece that cannot be derived offline (RESEARCH Assumption A1, STATE.md flagged checkpoint). The executor has generated the synthetic reference OHLC series (the same np.linspace+seeded-noise series used in test_adx_wilder_estrutural) and printed the computed ADX(14) values at a handful of bar indices for you to compare.
  </what-built>
  <read_first>
    - tests/test_indicators.py (the synthetic ADX reference series in test_adx_wilder_estrutural)
    - .planning/phases/05-motor-de-indicadores-puro/05-RESEARCH.md (Assumption A1 lines 408; Validation Architecture ADX fixture line 468)
  </read_first>
  <action>
    Before pausing, the executor runs a small script (`.venv/bin/python`) that builds the SAME synthetic OHLC series used in the ADX test and prints the OHLC rows plus the computed ADX(14) at the last bar and at ~3 sampled indices (e.g. 27, 60, last). The executor presents these numbers to the human. After the human confirms (or supplies the TradingView-charted values for an agreed series), the executor adds `test_adx_wilder_referencia` that freezes the confirmed ADX values as literals (np.testing.assert_allclose, atol ~1e-2) — completing the TEST-03 ADX half. If the human reports a mismatch, the executor records the discrepancy in the SUMMARY and does NOT freeze wrong literals (the structural+no-repaint locks from Task 1 remain the floor). This is a numeric VALIDATION step, not a research step — the implementation is already verified in runtime per RESEARCH.
  </action>
  <how-to-verify>
    1. Read the printed synthetic OHLC reference series and the computed ADX(14) values (last bar + sampled indices).
    2. In TradingView (or any charting tool you trust), reproduce the same close/high/low series (or accept the agreed canonical synthetic series) and read its ADX(14) with default Wilder smoothing.
    3. Confirm the app's ADX values match within ~1e-2, OR report the TradingView values so the executor freezes those instead.
    4. Sanity floor: the first non-NaN ADX must sit at bar index 27 (already asserted automatically).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms ADX(14) values match TradingView within tolerance (or supplies the reference values)
    - `test_adx_wilder_referencia` is added freezing the confirmed literals and `pytest tests/test_indicators.py::test_adx_wilder_referencia -x` exits 0
    - The structural invariant (first valid ADX at index 27) and no-repaint remain asserted
    - `.venv/bin/python -m pytest -q` exits 0 (TEST-07)
  </acceptance_criteria>
  <resume-signal>Type "approved" (values match) or paste the TradingView ADX values to freeze, or describe the mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ohlc_ajustado -> calcular | Single public entry point; None/empty/short/missing-columns frames are the untrusted-shape inputs at this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Denial of Service (local) | calcular on None / empty / missing-column frame | mitigate | top-of-function guard returns a fully-"indisponivel" SinaisTecnicos (mirrors ddm guard + GRAF-03/DATA-03), never raises into the Streamlit tab |
| T-05-06 | Tampering (display corruption) | ADX +DI/-DI/DX and regression divide-by-zero (atom ATR==0, +DI+-DI==0, flat seg.mean()) | mitigate | np.errstate guards so divisions yield NaN -> "indisponivel", not inf/NaN poisoning downstream signals |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_indicators.py -x -q` green for ALL Phase 5 tests (tendencia, momentum, canais, forca, calcular, split, ADX reference)
- `.venv/bin/python -m pytest -q` green (TEST-07: 64 valuation goldens)
- Human checkpoint: ADX vs TradingView confirmed, literals frozen
- `grep -q 'def calcular' src/analista/core/indicators.py`
</verification>

<success_criteria>
- ADX(14) double-Wilder, first valid at index 27, cross-checked vs TradingView (FORCE-01, TEST-03)
- Regression slope %/ano + R² over trailing 90 bars (FORCE-02, D-04)
- calcular() returns complete SinaisTecnicos; no-repaint (TEST-04); ITSA4 split clean (TEST-05)
- 64 valuation goldens green (TEST-07); zero new dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/05-motor-de-indicadores-puro/05-03-SUMMARY.md`
</output>
