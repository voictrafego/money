---
phase: 05-motor-de-indicadores-puro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.yaml
  - src/analista/core/indicators.py
  - tests/test_indicators.py
autonomous: true
requirements: [TREND-01, TREND-02, TREND-03, TREND-04, MOM-01, MOM-02, TEST-03, TEST-04]
must_haves:
  truths:
    - "config.yaml has an indicadores: section with canonical params (sma_emas 20/50/200, macd 12/26/9, rsi_janela 14, rsi_faixas 30/70)"
    - "indicators.rsi_wilder on the canonical Wilder dataset returns first RSI == 70.5328 (SMA-seeded, not naive ewm)"
    - "Tendencia signals (golden/death cross, posicao_mm200) are derived ALWAYS from SMA, never EMA (D-03)"
    - "Both SMA and EMA series (20/50/200) are computed on every call (D-03)"
    - "MACD line-vs-signal crossing is labeled cruz_alta / cruz_baixa / nenhum"
    - "Short history (<200 bars for MM200, <length for RSI) degrades to NaN series + 'indisponivel', no exception"
    - "RSI and MACD are no-repaint: ind(serie[:k])[-1] == ind(serie)[k-1]"
    - "The 64 existing valuation golden tests stay green (TEST-07)"
  artifacts:
    - path: "config.yaml"
      provides: "indicadores: canonical params section"
      contains: "indicadores:"
    - path: "src/analista/core/indicators.py"
      provides: "SinaisTecnicos contract, _wilder_rma_from helper, _tendencia, _momentum, rsi_wilder"
      min_lines: 120
    - path: "tests/test_indicators.py"
      provides: "RSI canonical golden, tendencia SMA signals, MACD cross, no-repaint, short-history"
      min_lines: 80
  key_links:
    - from: "indicators.rsi_wilder"
      to: "_wilder_rma_from"
      via: "SMA-seeded Wilder smoothing (alpha=1/length, seed=SMA of first length)"
      pattern: "_wilder_rma"
    - from: "_tendencia discrete signals"
      to: "sma series (not ema)"
      via: "golden/death cross and posicao_mm200 computed over SMA per D-03"
      pattern: "sma200|sma50"
---

<objective>
Establish the pure-module contract and lock the Tendencia (TREND-01..04) and Momentum (MOM-01..02) families with golden tests. This is the foundation plan: it creates `src/analista/core/indicators.py` (mirroring `core/ddm.py`), the `SinaisTecnicos` dataclass family, the genuinely-new `_wilder_rma_from` Wilder helper, the SMA/EMA + RSI + MACD math, and the `indicadores:` config section. The Wilder RSI anchor (70.5328) and no-repaint harness are locked here before any integration.

Purpose: The math must be correct and frozen by golden tests BEFORE the engine consumes it (Phase 6). RSI Wilder seeding is the single highest-risk numeric pitfall in the milestone — locking it first de-risks the rest.
Output: `config.yaml` (+indicadores section), `src/analista/core/indicators.py` (contract + tendencia + momentum), `tests/test_indicators.py` (RSI/tendencia/MACD/no-repaint/short-history golden tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-motor-de-indicadores-puro/05-CONTEXT.md
@.planning/phases/05-motor-de-indicadores-puro/05-RESEARCH.md
@.planning/phases/05-motor-de-indicadores-puro/05-PATTERNS.md
@src/analista/core/ddm.py
@src/analista/core/fundamentals.py
@tests/test_ddm.py
@config.yaml

<interfaces>
<!-- Contracts the executor needs. Do NOT explore the codebase for these. -->

Input frame (from Phase 4 — src/analista/core/fundamentals.py):
  CompanyData.ohlc_ajustado : Optional[pd.DataFrame], split-adjusted (CR-01).
  Columns capitalized: "Open", "High", "Low", "Close", "Volume" (others ignored).
  Index: pd.DatetimeIndex. Access via ohlc["Close"], ohlc["High"], etc.

House style to mirror (src/analista/core/ddm.py):
  from __future__ import annotations
  from dataclasses import dataclass, field
  from typing import List, Optional, Sequence
  Number = Optional[float]
  @dataclass (NOT frozen=True) with derived fields via field(init=False) + __post_init__.
  Module docstring naming the concept, the formula, and a canonical cross-check anchor.

Wilder helper (the only genuinely-new piece — copy verified excerpt from RESEARCH lines 304-312):
  _wilder_rma_from(arr: np.ndarray, length: int, start: int = 0) -> np.ndarray
  seed: out[start+length-1] = arr[start:start+length].mean(); then recursive alpha=1/length.

cfg access pattern (mirror cfg["ddm"]): cfg["indicadores"]["rsi_janela"], etc.
RSI canonical anchor (RESEARCH line 467, 177): first RSI(14)=70.5328,
  next five = [66.3186, 66.5498, 69.4063, 66.3552, 57.9749].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config section + module contract + Wilder helper</name>
  <files>config.yaml, src/analista/core/indicators.py</files>
  <read_first>
    - config.yaml (append after the ddm: section near end of file; mirror ddm:/capm: comment style)
    - src/analista/core/ddm.py (dataclass + __post_init__ + Number alias + module docstring house style)
    - .planning/phases/05-motor-de-indicadores-puro/05-RESEARCH.md (Code Examples lines 299-372 dataclass shapes; lines 304-312 _wilder_rma_from; lines 375-390 config section)
    - .planning/phases/05-motor-de-indicadores-puro/05-PATTERNS.md (config.yaml indicadores section lines 196-211; dataclass mirroring lines 44-66)
  </read_first>
  <action>
    Append an `indicadores:` section to config.yaml after the `ddm:` section, mirroring the `# --- ... ---` header-comment style. Keys (canonical defaults, per CONTEXT D-02/D-04 and RESEARCH): sma_emas [20, 50, 200]; donchian [20, 55]; bollinger nested {janela: 20, sigma: 2.0}; squeeze_janela 126 (inline comment "~6 meses"); squeeze_percentil 20; adx_janela 14 (inline "Wilder"); rsi_janela 14 (inline "Wilder"); rsi_faixas [30, 70]; macd [12, 26, 9]; regressao_janela 90 (inline "~1 trimestre").
    Create src/analista/core/indicators.py with: `from __future__ import annotations`; imports of dataclass/field, typing (List/Optional/Sequence), `import numpy as np`, `import pandas as pd`, `from scipy import stats`; alias `Number = Optional[float]`. Module docstring naming the 4 families, citing the Wilder reference and the RSI anchor 70.5328 (parallel to ddm.py's "Itau Cap.17 Tabela 41" cross-check line).
    Define the four sub-dataclasses grouped by family (D-01, nested) plus the root `SinaisTecnicos`, NOT frozen: `Tendencia` (sma20/sma50/sma200, ema20/ema50/ema200 as pd.Series; posicao_mm200 str, cruzamento str); `Canais` (donchian_sup/donchian_inf, bb_sup/bb_med/bb_inf, largura_bb, squeeze_pct as pd.Series; rompimento_donchian/toque_bollinger/squeeze str); `Forca` (adx/pdi/ndi, regressao_slope_ann/regressao_r2 as pd.Series; forca_adx str); `Momentum` (rsi, macd/macd_sinal/macd_hist as pd.Series; nivel_rsi/cruzamento_macd str); `SinaisTecnicos` aggregating tendencia/canais/forca/momentum. Discrete-signal string literals are the locked stable/neutral keys from D-01: "acima"/"abaixo"/"indisponivel", "golden_cross"/"death_cross"/"nenhum"/"indisponivel", "squeeze_on"/"squeeze_off"/"indisponivel", "sobrecomprado"/"sobrevendido"/"neutro"/"indisponivel", "cruz_alta"/"cruz_baixa"/"nenhum"/"indisponivel", "nova_maxima"/"perda_minima"/"nenhum"/"indisponivel", "sem_tendencia"/"forte"/"neutro"/"indisponivel". NO Portuguese natural-language phrases (those belong to Phase 6).
    Implement the private helper `_wilder_rma_from(arr, length, start=0)` exactly as the RESEARCH verified excerpt: full-NaN out array; guard `if start+length > len(arr): return out`; seed `out[start+length-1] = arr[start:start+length].mean()`; recursive `out[i] = a*arr[i] + (1-a)*out[i-1]` with `a = 1.0/length`. Section the file with regua comments `# --- Tendencia (TREND-01..04) ---` style per PATTERNS lines 93-99. Do NOT add canais/forca implementations yet (stubs not needed — calcular() is assembled in plan 03 after all family functions exist). Do NOT run pip install — numpy/pandas/scipy already in .venv.
  </action>
  <acceptance_criteria>
    - `grep -q '^indicadores:' config.yaml` succeeds and the section contains keys squeeze_janela, rsi_faixas, regressao_janela, macd
    - `.venv/bin/python -c "from analista.core import indicators; indicators.SinaisTecnicos; indicators._wilder_rma_from"` imports without error
    - indicators.py defines dataclasses Tendencia, Canais, Forca, Momentum, SinaisTecnicos (grep confirms `class SinaisTecnicos`)
    - No `frozen=True` anywhere in indicators.py (matches ddm.py house style)
    - No import of yfinance/requests/ta/pandas_ta/talib (pure module): `grep -Eq 'yfinance|requests|pandas_ta|talib|import ta' src/analista/core/indicators.py` returns non-zero
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && .venv/bin/python -c "from analista.core import indicators; assert hasattr(indicators,'SinaisTecnicos') and hasattr(indicators,'_wilder_rma_from'); print('contract ok')"</automated>
  </verify>
  <done>config.yaml has the indicadores: section; indicators.py imports cleanly with the full SinaisTecnicos family contract + _wilder_rma_from helper; no TA deps, no network imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tendencia family (SMA/EMA 20/50/200 + cross + posicao) + golden tests</name>
  <files>src/analista/core/indicators.py, tests/test_indicators.py</files>
  <behavior>
    - SMA20/50/200 via close.rolling(n, min_periods=n).mean(); EMA via close.ewm(span=n, adjust=False).mean(); both always computed (D-03)
    - posicao_mm200 == "acima" when close.iloc[-1] > sma200.iloc[-1], "abaixo" when below, "indisponivel" when sma200.iloc[-1] is NaN (<200 bars)
    - cruzamento == "golden_cross" when sign(sma50-sma200) flips positive on the last bar, "death_cross" when flips negative, "nenhum" otherwise, "indisponivel" when series unavailable — ALWAYS over SMA, never EMA (D-03)
    - With <200 bars: sma200 is all-NaN and signals are "indisponivel" (no IndexError on iloc[-2])
  </behavior>
  <read_first>
    - src/analista/core/indicators.py (the contract + helper from Task 1)
    - tests/test_ddm.py (numeric-assert house style: source-citing comment + assert abs(x-exp)<tol)
    - .planning/phases/05-motor-de-indicadores-puro/05-RESEARCH.md (Pattern 1 lines 139-151; Pitfall 4 short-history lines 287-291)
    - .planning/phases/05-motor-de-indicadores-puro/05-PATTERNS.md (test import idiom lines 126-137; numeric assert lines 140-149)
  </read_first>
  <action>
    Implement `_tendencia(close, cfg)` returning a `Tendencia`. Compute sma20/50/200 with `rolling(n, min_periods=n).mean()` (min_periods=window so short history yields NaN, never partial values — DATA-03 degradation) and ema20/50/200 with `ewm(span=n, adjust=False).mean()`. Windows from `cfg["indicadores"]["sma_emas"]`. Derive discrete signals SOLELY from SMA per D-03: posicao_mm200 by comparing close.iloc[-1] to sma200.iloc[-1] (guard NaN -> "indisponivel"); cruzamento from the sign change of (sma50 - sma200) between the last two bars (guard: if fewer than 2 non-NaN diff values -> "indisponivel"; if sign unchanged -> "nenhum"; if flipped positive -> "golden_cross"; flipped negative -> "death_cross").
    Create tests/test_indicators.py with the house-style one-line docstring, `import numpy as np`, `import pandas as pd`, `import pytest`, `from analista.core import indicators`, and a small helper to load cfg["indicadores"] (read config.yaml via yaml.safe_load, or build the dict literal from canonical defaults — prefer reading config.yaml so tests pin the shipped params). Add `test_sinais_tendencia_sma`: build a synthetic close series that produces a known golden cross over SMA (e.g. a downtrend then sustained uptrend so sma50 crosses above sma200), assert cruzamento == "golden_cross" and posicao_mm200 == "acima". Add `test_historico_curto_tendencia`: feed <200 closes, assert sma200 is all-NaN and posicao_mm200 == cruzamento == "indisponivel" with no exception.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_indicators.py::test_sinais_tendencia_sma -x` exits 0
    - `pytest tests/test_indicators.py::test_historico_curto_tendencia -x` exits 0
    - test asserts cruzamento and posicao_mm200 are derived from sma (not ema) — golden cross test uses an EMA path that would differ and still asserts SMA-based result
    - `.venv/bin/python -m pytest -q` exits 0 (TEST-07: 64 valuation goldens stay green)
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && .venv/bin/python -m pytest tests/test_indicators.py -k "tendencia or historico_curto_tendencia" -x -q</automated>
  </verify>
  <done>_tendencia computes both SMA and EMA series; discrete cross/posicao signals are SMA-based (D-03); short history degrades to "indisponivel"; tests green; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Momentum family (RSI Wilder + MACD) + canonical golden + no-repaint</name>
  <files>src/analista/core/indicators.py, tests/test_indicators.py</files>
  <behavior>
    - rsi_wilder on the canonical Wilder 33-close dataset: first valid RSI == 70.5328 (abs 1e-3), next five == [66.3186, 66.5498, 69.4063, 66.3552, 57.9749]
    - naive ewm (no SMA seed) would give 50.75 — the SMA-seeded helper must give 70.53
    - MACD uses standard EMA (span 12/26/9, adjust=False), NOT Wilder; cruzamento_macd == cruz_alta/cruz_baixa/nenhum on signal-line cross
    - nivel_rsi == "sobrecomprado" when rsi.iloc[-1] >= 70, "sobrevendido" when <= 30, "neutro" between, "indisponivel" when NaN
    - no-repaint: rsi_wilder(s[:k])[-1] == rsi_wilder(s)[k-1] and macd(s[:k])[-1] == macd(s)[k-1] for several k
  </behavior>
  <read_first>
    - src/analista/core/indicators.py (contract + _wilder_rma_from from Task 1)
    - .planning/phases/05-motor-de-indicadores-puro/05-RESEARCH.md (Pattern 2 RSI lines 153-177; MACD example lines 315-327; Pitfall 1 lines 269-273; Pitfall 3 no-repaint lines 281-285; fixture anchor line 467)
    - .planning/phases/05-motor-de-indicadores-puro/05-PATTERNS.md (numeric-assert + anchor literal lines 140-149)
  </read_first>
  <action>
    Implement public `rsi_wilder(close, length=14)`: delta = close.diff(); gain = delta.clip(lower=0); loss = (-delta).clip(lower=0); avg_gain = _wilder_rma_from(gain.iloc[1:].to_numpy(float), length) wrapped back to a Series; avg_loss likewise; rs = avg_gain/avg_loss (guard divide-by-zero with np.errstate so all-gain windows -> RSI 100, not crash); RSI = 100 - 100/(1+rs); reindex to close.index. Implement `_momentum(close, cfg)` returning `Momentum`: rsi via rsi_wilder(close, cfg["indicadores"]["rsi_janela"]); MACD via standard EMA — ema_rapida/ema_lenta = close.ewm(span=fast/slow, adjust=False).mean(), macd = ema_rapida - ema_lenta, macd_sinal = macd.ewm(span=signal, adjust=False).mean(), macd_hist = macd - macd_sinal (spans from cfg["indicadores"]["macd"]). Discrete: nivel_rsi from rsi.iloc[-1] against cfg rsi_faixas [30,70] (guard NaN -> "indisponivel"); cruzamento_macd from sign change of (macd - macd_sinal) on the last two bars -> "cruz_alta"/"cruz_baixa"/"nenhum"/"indisponivel". MACD must use EMA, NOT Wilder (RESEARCH anti-pattern).
    Add tests: `test_rsi_wilder_canonico` — embed the canonical Wilder 33-close list as a literal Series, assert rsi.dropna().iloc[0] == pytest.approx(70.5328, abs=1e-3) and the next five via np.testing.assert_allclose against [66.3186, 66.5498, 69.4063, 66.3552, 57.9749] (atol 1e-3). `test_macd_cross` — synthetic series with a known signal-line crossover, assert cruzamento_macd. `test_no_repaint_momentum` — reusable loop: for k in a set of indices, assert rsi_wilder(s.iloc[:k]).iloc[-1] == pytest.approx(rsi_wilder(s).iloc[k-1]) and same for the macd line.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_indicators.py::test_rsi_wilder_canonico -x` exits 0 (asserts first RSI == pytest.approx(70.5328, abs=1e-3))
    - `pytest tests/test_indicators.py::test_no_repaint_momentum -x` exits 0
    - `pytest tests/test_indicators.py::test_macd_cross -x` exits 0
    - MACD path uses ewm(span=..., adjust=False), not _wilder_rma_from: `grep -A30 'def _momentum' src/analista/core/indicators.py | grep -q 'ewm'`
    - `.venv/bin/python -m pytest -q` exits 0 (TEST-07)
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && .venv/bin/python -m pytest tests/test_indicators.py -k "rsi_wilder_canonico or no_repaint_momentum or macd_cross" -x -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>RSI Wilder matches the published 70.5328 anchor; MACD uses standard EMA with labeled signal cross; RSI/MACD are no-repaint; full suite (incl. 64 valuation goldens) green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 4 ohlc_ajustado -> indicators.calcular | In-memory DataFrame; may be None/empty/short or contain NaN — no network, no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service (local) | _tendencia / _momentum on short or NaN-tail series | mitigate | min_periods=window yields NaN (not partial/exception); discrete-signal guards return "indisponivel" instead of indexing iloc[-2] on <2 valid values |
| T-05-02 | Tampering (display corruption) | rsi_wilder RS divide-by-zero (all-gain or all-loss window) | mitigate | wrap rs = avg_gain/avg_loss in np.errstate; all-gain window -> RSI 100, all-loss -> RSI 0, never inf propagating to UI |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_indicators.py -x -q` green for all tendencia + momentum tests
- `.venv/bin/python -m pytest -q` green (TEST-07: 64 valuation goldens unaffected)
- `grep -q '^indicadores:' config.yaml`
- No new dependency installed (`git diff` shows no change to pyproject/requirements)
</verification>

<success_criteria>
- RSI(14) Wilder == 70.5328 canonical anchor (TEST-03 RSI half); MACD signal cross labeled (MOM-02)
- SMA/EMA 20/50/200 both computed; cross + posicao×MM200 over SMA (TREND-01..04, D-03)
- RSI/MACD no-repaint (TEST-04 partial); short history -> "indisponivel" (DATA-03)
- indicadores: config section present; 64 valuation goldens green (TEST-07)
</success_criteria>

<output>
After completion, create `.planning/phases/05-motor-de-indicadores-puro/05-01-SUMMARY.md`
</output>
