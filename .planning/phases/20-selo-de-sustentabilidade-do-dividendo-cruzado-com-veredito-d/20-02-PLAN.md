---
phase: 20-selo-de-sustentabilidade-do-dividendo-cruzado-com-veredito-d
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/analista/report/presentation.py
  - app.py
  - tests/test_presentation_multiticker.py
autonomous: false
requirements: [SELO-03]
must_haves:
  truths:
    - "O usuário vê na aba Analisar um selo colorido (verde/azul/amarelo/vermelho) em destaque, perto do veredito atual"
    - "O usuário vê na aba Analisar o rótulo do quadrante (JOIA/VALUE TRAP/Boa, no preço/...) cruzando qualidade × preço"
    - "Quando o veredito é VERIFICAR, o usuário vê um alerta 'Verificar dados' em vez de um rótulo de preço"
    - "O usuário vê uma coluna de selo (mesma cor) em Garimpo e em Ranking, por linha"
    - "O selo é visualmente idêntico nos três lugares (mesma função de render)"
    - "Nenhuma fórmula de selo vive em app.py — a view só LÊ campos da engine (a.selo / selo.cor_do_bsd)"
  artifacts:
    - path: "src/analista/report/presentation.py"
      provides: "selo_emoji(cor) e selo_badge(...) — formatação pura do selo (sem streamlit)"
      contains: "def selo_emoji"
    - path: "app.py"
      provides: "render read-only do selo em Analisar (destaque + quadrante) e coluna em Garimpo/Ranking"
      contains: "selo"
  key_links:
    - from: "app.py (aba Analisar)"
      to: "a.selo"
      via: "leitura dos campos do Selo já derivados na engine (sem recálculo)"
      pattern: "a\\.selo"
    - from: "app.py (Garimpo e Ranking)"
      to: "selo.cor_do_bsd / screening.bsd_empresa"
      via: "cor por linha a partir do BSD já em mãos (read-only)"
      pattern: "cor_do_bsd|bsd_empresa"
    - from: "app.py"
      to: "presentation.selo_emoji / selo_badge"
      via: "função única de render do selo nos três lugares"
      pattern: "selo_emoji|selo_badge"
---

<objective>
Camada FINA read-only que DESENHA o selo derivado no Plan 01 (SELO-03), em três lugares: aba Analisar (selo em destaque + rótulo de quadrante perto do veredito), coluna de selo em Garimpo (BSD) e coluna de selo em Ranking. O selo é visualmente idêntico nos três (mesma função de render). `app.py` permanece read-only: só LÊ `a.selo` (Analisar) ou chama funções puras da engine (`selo.cor_do_bsd`/`screening.bsd_empresa`) para as colunas — zero fórmula/recálculo na view.

Purpose: Entregar o diferencial visível (qualidade×preço numa olhada) preservando o gate `app.py` read-only e a fronteira "EXIBE, NUNCA recomenda".
Output: helpers de formatação em `presentation.py`, render nos três lugares em `app.py`, testes de formatação, e um checkpoint humano de verificação visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-selo-de-sustentabilidade-do-dividendo-cruzado-com-veredito-d/20-CONTEXT.md
@.planning/phases/20-selo-de-sustentabilidade-do-dividendo-cruzado-com-veredito-d/20-01-SUMMARY.md

<interfaces>
<!-- Contratos derivados no Plan 01 (Wave 1). Use diretamente. -->

De src/analista/report/selo.py:
- `@dataclass Selo`: bsd, cor (verde|azul|amarelo|vermelho|None), qualidade (Alta|Baixa|None), faixa_preco (Barato|Justo|Caro|None), rotulo (str|None), verificar (bool).
- `cor_do_bsd(bsd, cfg) -> Optional[str]`.

De src/analista/report/report.py:
- `AnaliseAcao.selo: Optional[Selo]` já populado por `analisar_acao`.

De src/analista/core/screening.py:
- `bsd_empresa(c, cfg) -> Optional[float]`.

Sítios de render existentes em app.py (ler antes de editar):
- Analisar / veredito colorido: ~L831-848 (`a.veredito`; st.success/error/warning).
- Garimpo: ~L1197-1256 (monta `rows` com "BSD"/"BSD > 80" e exibe `st.dataframe(df, ...)`; tem `bsd_map`/`b.get("bsd")`).
- Ranking: ~L1261-1345 (monta `rows` por empresa e exibe `st.dataframe`; tem as `empresas` CompanyData em mãos, mas NÃO calcula BSD hoje).

Imports já presentes em app.py:
- `from analista.core import screening as sc` (L25)
- `from analista.report import presentation, report, setup` (L28) — adicionar `selo` aqui.
- helpers: `esc_md(...)`, `fmt_rs(...)`, `h(...)`, `CFG` (config carregado).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helpers puros de formatação do selo em presentation.py</name>
  <files>src/analista/report/presentation.py, tests/test_presentation_multiticker.py</files>
  <read_first>
    - src/analista/report/presentation.py (L1-45 — convenção de helpers puros, sentinela em-dash, header_dy como referência de estilo)
    - src/analista/report/selo.py (dataclass Selo — campos)
    - tests/test_presentation_multiticker.py (padrão dos testes de formatação)
  </read_first>
  <behavior>
    - selo_emoji("verde") == "🟢"; selo_emoji("azul") == "🔵"; selo_emoji("amarelo") == "🟡"; selo_emoji("vermelho") == "🔴"; selo_emoji(None) == "—".
    - selo_badge para cor verde e rotulo "JOIA" retorna uma string curta contendo o emoji e o rótulo (ex.: começa com "🟢" e contém "JOIA"); nunca contém verbo imperativo (compre/venda).
    - selo_badge com verificar=True contém "Verificar" e NÃO contém rótulo de preço.
    - selo_badge com cor None retorna sentinela "—" (degrada).
  </behavior>
  <action>
    Em presentation.py (módulo puro, sem streamlit), adicionar:
    - `selo_emoji(cor: Optional[str]) -> str`: mapa fixo verde→🟢, azul→🔵, amarelo→🟡, vermelho→🔴, None/desconhecido→"—".
    - `selo_badge(cor, rotulo, qualidade, verificar) -> str` (aceitar primitivos OU o objeto Selo — escolher primitivos para não acoplar; o executor lê os campos no app.py): monta a string curta de exibição comum aos três lugares — emoji + (qualidade "Alta"/"Baixa") + rótulo quando houver; quando `verificar`, sufixa/insere "· Verificar dados" e NÃO exibe rótulo de preço; quando cor None, retorna "—". Copy 100% descritiva (D2) — nunca imperativo.
    Manter a convenção do em-dash "—" para ausências (GRAF-03).
    Em tests/test_presentation_multiticker.py (ou um novo bloco), cobrir os casos do `<behavior>` + um assert de que nenhuma saída contém tokens imperativos (compre|venda|recomend).
  </action>
  <verify>
    <automated>./.venv/bin/python -c "from analista.report import presentation as p; assert p.selo_emoji('verde')=='🟢' and p.selo_emoji('vermelho')=='🔴' and p.selo_emoji(None)=='—'; b=p.selo_badge('verde','JOIA','Alta',False); assert '🟢' in b and 'JOIA' in b; v=p.selo_badge('verde',None,'Alta',True); assert 'Verificar' in v; print('ok')" && ./.venv/bin/python -m pytest tests/test_presentation_multiticker.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `selo_emoji` e `selo_badge` existem em presentation.py, são puros (sem `import streamlit`).
    - Todos os casos do `<behavior>` passam.
    - Nenhuma saída de `selo_badge` contém `compre|venda|recomend` (assert no teste).
    - `tests/test_presentation_multiticker.py` verde.
  </acceptance_criteria>
  <done>Formatação do selo centralizada e testada, pronta para uso idêntico nos três sítios da UI.</done>
</task>

<task type="auto">
  <name>Task 2: Render do selo em Analisar, Garimpo e Ranking (app.py read-only)</name>
  <files>app.py</files>
  <read_first>
    - app.py L20-30 (imports — adicionar `selo`) ; L820-870 (Analisar: veredito colorido + métricas) ; L1197-1256 (Garimpo: rows/df/st.dataframe) ; L1261-1345 (Ranking: rows/df/st.dataframe)
    - src/analista/report/selo.py (Selo, cor_do_bsd) e src/analista/report/presentation.py (selo_emoji/selo_badge da Task 1)
    - src/analista/core/screening.py (bsd_empresa)
  </read_first>
  <action>
    Import: em app.py L28 trocar `from analista.report import presentation, report, setup` por `from analista.report import presentation, report, selo, setup`.

    (a) Aba Analisar (perto do bloco do veredito, ~L831-848): DEPOIS do veredito colorido, se `a.selo` e `a.selo.cor`, renderizar o selo em DESTAQUE lendo SÓ campos de `a.selo` (read-only): usar `presentation.selo_badge(a.selo.cor, a.selo.rotulo, a.selo.qualidade, a.selo.verificar)`. Desenhar num container visível (ex.: st.markdown com o badge + um `st.caption` explicando "Selo = qualidade do dividendo (BSD) × preço (DDM)"). Quando `a.selo.verificar`, mostrar um `st.warning("Verificar dados: ...")` (overlay separado, D2) em vez do rótulo de preço. Copy descritiva; nunca "compre/venda". Se `a.selo` None ou cor None, não desenhar nada (degrada).

    (b) Garimpo (~L1225-1245, onde `rows` é montado com o dict `b`): adicionar ao dict de cada linha uma chave "Selo" = `presentation.selo_emoji(selo.cor_do_bsd(b.get("bsd"), CFG))` — a cor sai da função pura da engine (read-only), o emoji da presentation. Posicionar a coluna "Selo" próxima da coluna "BSD" na ordem do DataFrame. Não recomputar BSD (já está em `b`).

    (c) Ranking (~L1300-1325, onde `rows` é montado por empresa): para cada empresa `c` de `empresas`, obter `bsd = sc.bsd_empresa(c, CFG)` (read-only, sem rede — dados já carregados) e adicionar "Selo" = `presentation.selo_emoji(selo.cor_do_bsd(bsd, CFG))` ao dict da linha correspondente (casar pelo ticker `r["empresa"]`). Posicionar a coluna "Selo" no DataFrame.

    Regra dura: app.py NÃO calcula cor/rótulo/faixa — só chama funções da engine e lê `a.selo`. Toda a lógica ficou no Plan 01.
  </action>
  <verify>
    <automated>./.venv/bin/python -c "import ast,sys; src=open('app.py').read(); t=ast.parse(src); assert 'selo' in [n.name for imp in ast.walk(t) if isinstance(imp,ast.ImportFrom) and imp.module=='analista.report' for n in imp.names], 'selo nao importado'; assert 'a.selo' in src and 'cor_do_bsd' in src and 'bsd_empresa' in src and 'selo_emoji' in src, 'sitios de render ausentes'; print('ok')" && ./.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `app.py` importa `selo` de `analista.report` e usa `presentation.selo_emoji`/`selo_badge`.
    - Analisar lê `a.selo.*` (grep `a\.selo`), não recalcula nada; Garimpo usa `selo.cor_do_bsd(b.get("bsd"), CFG)`; Ranking usa `sc.bsd_empresa(c, CFG)` + `selo.cor_do_bsd`.
    - Nenhum threshold numérico de cor (70/55/40) nem rótulo do quadrante (JOIA/VALUE TRAP/...) aparece HARDCODED em app.py (grep confirma que a lógica não vazou): `grep -nE 'JOIA|VALUE TRAP|verde_min|>= *70|>= *55' app.py` retorna vazio.
    - `./.venv/bin/python -m pytest -q` continua verde (>=307 + testes de selo/presentation) — app.py não é golden-testado, mas os módulos importados seguem verdes.
    - `app.py` continua carregando sem erro de import.
  </acceptance_criteria>
  <done>Selo renderizado nos três lugares lendo só a engine; nenhuma fórmula em app.py.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual do selo (Analisar + Garimpo + Ranking)</name>
  <what-built>Selo de Sustentabilidade (4 cores) + rótulo de quadrante na aba Analisar, e coluna de selo em Garimpo e Ranking. Toda a lógica na engine; app.py só desenha.</what-built>
  <how-to-verify>
    1. Rodar o app: `./.venv/bin/streamlit run app.py` (ou o comando de dev do projeto).
    2. Aba **Analisar**: digitar um ticker de boa pagadora e barata (ex.: uma que costume dar SUBAVALIADA) e conferir que aparece o selo colorido em DESTAQUE perto do veredito + o rótulo do quadrante (ex.: "JOIA" ou "Boa, no preço"). Testar também um ticker com veredito **VERIFICAR** (ex.: payout>100% / DY>15%) e confirmar que aparece o alerta "Verificar dados" e NÃO um rótulo de preço.
    3. Aba **Garimpar**: colar a lista default e confirmar a coluna **Selo** (🟢/🔵/🟡/🔴) coerente com o valor da coluna BSD (BSD>=70 verde, 55-70 azul, 40-55 amarelo, <40 vermelho).
    4. Aba **Ranking**: rodar um setor e confirmar a coluna **Selo** por linha.
    5. Conferir que o selo é visualmente IDÊNTICO (mesmo emoji/estilo) nos três lugares e que NENHUM texto diz "compre/venda/recomendo".
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que ajustar (cor, posição, copy).</resume-signal>
</task>

</tasks>

<verification>
- `./.venv/bin/python -m pytest -q` verde (>=307 + selo/presentation).
- `grep -nE 'JOIA|VALUE TRAP|verde_min|>= *70' app.py` vazio (lógica não vazou p/ a view).
- app.py importa `selo`, lê `a.selo`, chama `cor_do_bsd`/`bsd_empresa`/`selo_emoji`.
- Checkpoint humano aprovado (selo idêntico nos 3 lugares, VERIFICAR como alerta, copy não-imperativa).
</verification>

<success_criteria>
- SELO-03: selo exibido em destaque + quadrante na Analisar e como coluna em Garimpo e Ranking, com render único.
- `app.py` read-only (só lê a engine); zero fórmula de selo na view.
- Fronteira "EXIBE, NUNCA recomenda" preservada (copy descritiva).
- Goldens verdes; zero dependência nova; custo-zero.
</success_criteria>

<output>
Após completar, criar `.planning/phases/20-selo-de-sustentabilidade-do-dividendo-cruzado-com-veredito-d/20-02-SUMMARY.md`
</output>
