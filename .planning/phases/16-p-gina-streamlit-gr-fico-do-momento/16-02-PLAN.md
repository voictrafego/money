---
phase: 16-p-gina-streamlit-gr-fico-do-momento
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: [app.py]
autonomous: true
requirements: [CHART-01, SWING-02]
must_haves:
  truths:
    - "O candlestick mostra zonas S/R como bandas (add_hrect), nunca pontos exatos"
    - "Níveis do setup (entrada-zona/stop/alvo) e retrações Fibonacci aparecem como linhas/bandas de referência rotuladas como estudo"
    - "Padrões (duplo topo/fundo, OCO) são anotados quando o toggle padroes_on está ligado, com rótulo neutro em formação/confirmado"
    - "Abaixo do gráfico há um card de veredito read-only: grade+score, decomposição peso-a-peso, checklist ✓/✗ e tabela de níveis 'Referências de estudo (não são ordens)'"
    - "Toda a copy é não-imperativa e há disclaimer condicional inline (exibe sinais, nunca recomenda)"
  artifacts:
    - path: "app.py"
      provides: "add_hrect S/R, add_hline setup/Fib, anotação de padrões, card de veredito (grade/score/decomposicao/checklist/tabela níveis) + disclaimer"
      contains: "Referências de estudo (não são ordens)"
  key_links:
    - from: "app.py figura swing"
      to: "sinais.niveis.suportes / resistencias / fib_retracoes"
      via: "add_hrect (zonas) e add_hline (fib)"
      pattern: "add_hrect"
    - from: "app.py card veredito"
      to: "sw.decomposicao (ContribFamilia)"
      via: "iteração da barra de contribuição por família"
      pattern: "sw\\.decomposicao|\\.decomposicao"
    - from: "app.py card veredito"
      to: "sinais.checklist.sinais (Sinal)"
      via: "lista ✓/✗ por sinal"
      pattern: "checklist"
---

<objective>
Completa a figura candlestick do plano 16-01 com as zonas S/R (bandas), os níveis do setup
(entrada/stop/alvo), as retrações Fibonacci e a anotação opcional de padrões; e adiciona, abaixo
do gráfico, o card de veredito read-only (grade+score, decomposição peso-a-peso, checklist e tabela
de níveis) com disclaimer e copy estritamente não-imperativa.

Purpose: Fecha CHART-01 (S/R + Fibonacci + padrões anotados) e atende SWING-02 (disclaimer "exibe
sinais, nunca recomenda" + linguagem condicional/de estudo para todos os níveis). A copy
não-imperativa é gate de aceite duro.
Output: `app.py` com os overlays de nível na figura swing e o card de veredito completo abaixo dela.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-RESEARCH.md
@.planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-PATTERNS.md
@.planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-01-PLAN.md

<interfaces>
<!-- Contratos read-only — só LER. Construídos no plano 16-01 como `sinais` (SinaisTecnicos) e `sw` (SetupSwing). -->

sinais.niveis (Niveis|None):
  .suportes / .resistencias: list[(low, high)]   # zonas → add_hrect (bandas, NUNCA pontos — LEVEL-01)
  .entrada_zona: (low, high) | .stop: float | .alvo: float
  .fib_retracoes: {"382"/"500"/"618" -> preço} | .risco_retorno: str  ("1 : 2,5" | "indisponivel", já BR)

sw (SetupSwing):
  .score float | .grade str ("Forte"/"Moderado"/"Fraco"/"Sem setup")
  .decomposicao list[ContribFamilia]  (VAZIA quando gate falha / "Sem setup")
  .entrada_zona tuple|None | .stop float|None | .alvo float|None | .rr_valor float|None | .conflito_mtf bool
ContribFamilia: .familia str | .sub_score float | .peso int | .contribuicao float | .detalhe str (rótulo neutro)

sinais.padroes.lista: list[PadraoGrafico]
PadraoGrafico: .tipo ("duplo_topo"/"duplo_fundo"/"oco"/"oco_invertido") | .estado ("em_formacao"/"confirmado")
  .neckline float | .alvo float | .altura float | .pivos_envolvidos {Timestamp: preço}

sinais.checklist.sinais: list[Sinal]
Sinal: .nome str | .ativo bool | .detalhe str (rótulo neutro)

helpers: fmt_rs(x) (R$ BR, None→"—", app.py L85) | esc_md(s) (escapa $, L89)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zonas S/R + níveis do setup + Fibonacci + anotação de padrões na figura</name>
  <files>app.py</files>
  <read_first>
    - app.py bloco swing (figura make_subplots construída no plano 16-01) — onde inserir os add_hrect/add_hline ANTES do update_layout/plotly_chart
    - app.py L275-279 (banda DDM via add_hrect na aba Analisar — molde de fillcolor/opacity/annotation_text/row=1)
    - .planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-RESEARCH.md §"Q2" (anotação de padrões: COR_PAD, add_shape neckline horizontal, add_annotation rótulo, add_hline alvo, marcar pivôs), §"Code Examples" (esqueleto S/R+Fib+setup), Pitfall 5 (copy não-imperativa)
    - .planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-PATTERNS.md §"Zonas S/R + níveis", §"Anotação de padrões", §"Copy não-imperativa"
    - src/analista/core/indicators.py — contratos Niveis e PadraoGrafico (read-only)
  </read_first>
  <action>
    Na figura swing do plano 16-01, ANTES do `update_layout`, adicionar os overlays de nível lendo `sinais.niveis` e `sw`, cada bloco gateado pelo seu toggle em `est`.
    S/R (gate `est["sr_on"]` e `sinais.niveis is not None`): para cada `(lo, hi)` em `sinais.niveis.suportes` → `fig.add_hrect(y0=lo, y1=hi, line_width=0, fillcolor="green", opacity=0.08, row=1, col=1)`; idem `sinais.niveis.resistencias` com `fillcolor="red"`. Sempre BANDAS, nunca pontos (LEVEL-01).
    Níveis do setup (gate `est["niveis_setup_on"]` e `sw.entrada_zona`): `lo, hi = sw.entrada_zona` → `add_hrect(..., fillcolor="blue", opacity=0.10, annotation_text="zona de entrada (estudo)", row=1, col=1)`; se `sw.stop is not None` → `add_hline(y=sw.stop, line_dash="dash", line_color="#d62728", annotation_text="stop (estudo)", row=1, col=1)`; se `sw.alvo is not None` → `add_hline(y=sw.alvo, line_dash="dash", line_color="#2ca02c", annotation_text="alvo (estudo)", row=1, col=1)`.
    Fibonacci (gate `est["fib_on"]` e `sinais.niveis.fib_retracoes`): para cada `nome, preco` → `add_hline(y=preco, line_dash="dot", line_color="#9467bd", annotation_text=f"Fib {nome}", row=1, col=1)`.
    Padrões (gate `est["padroes_on"]`, OFF por padrão): iterar `sinais.padroes.lista`. COR por direção (espelha setup._PADROES_ALTA/_BAIXA): duplo_fundo/oco_invertido = verde `#2ca02c`; duplo_topo/oco = vermelho `#d62728`. Para cada padrão: `ts = sorted(p.pivos_envolvidos)`; `dash = "solid" if p.estado=="confirmado" else "dot"`; neckline HORIZONTAL via `add_shape(type="line", x0=ts[0], x1=ts[-1], y0=p.neckline, y1=p.neckline, line=dict(color=cor, width=1.5, dash=dash), row=1, col=1)`; rótulo via `add_annotation(x=ts[-1], y=p.neckline, text=f"{p.tipo.replace('_',' ')} · {('confirmado' if p.estado=='confirmado' else 'em formação')}", showarrow=False, yshift=12, font=dict(color=cor, size=10), row=1, col=1)`; alvo via `add_hline(y=p.alvo, line_dash="dot", line_color=cor, annotation_text="alvo (projeção de estudo)", row=1, col=1)`; marcar pivôs com `go.Scatter(mode="markers", marker=dict(symbol="circle-open", color=cor, size=9), showlegend=False, hoverinfo="skip")`. Reta inclinada da OCO fica deferida (horizontal é simplificação honesta do MVP).
    Pitfall 5 / SWING-02: toda annotation_text NEUTRA ("estudo"/"projeção de estudo"/"em formação"/"confirmado") — proibido "compre/venda/comprar/vender/entre/recomend/sugiro/indico" e qualquer "alvo de compra". Tratar `sinais.niveis is None` e listas vazias sem quebrar (degradação graciosa).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && grep -q "add_hrect" app.py && grep -q 'sr_on' app.py && grep -q 'niveis_setup_on' app.py && grep -q 'fib_on' app.py && grep -q 'padroes_on' app.py && grep -q '(estudo)' app.py && ! grep -Eiq 'alvo de compra|recomend|sugiro|indico' app.py && python -c "import ast; ast.parse(open('app.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `app.py contains "add_hrect"` lendo `sinais.niveis.suportes`/`.resistencias` como `(low,high)` (bandas, LEVEL-01)
    - Níveis do setup desenhados via `add_hrect` (entrada-zona) + `add_hline` (stop/alvo) gateados por `est["niveis_setup_on"]`, com rótulos "(estudo)"
    - Fibonacci via `add_hline` gateado por `est["fib_on"]`
    - Anotação de padrões gateada por `est["padroes_on"]` (OFF default), neckline horizontal via `add_shape`, rótulo "em formação"/"confirmado" via `add_annotation`
    - Behavior: nenhuma string imperativa — `grep -Ei 'alvo de compra|recomend|sugiro|indico|comprar|vender' app.py` não casa no bloco swing (gate SWING-02 / Pitfall 5)
    - `sinais.niveis is None` e listas vazias não quebram a render (degradação graciosa)
    - `python -c "import ast; ast.parse(open('app.py').read())"` sai 0
  </acceptance_criteria>
  <done>O candlestick mostra S/R como bandas, entrada/stop/alvo e Fibonacci como referências de estudo rotuladas, e os padrões anotados quando o toggle está ligado — toda a copy neutra.</done>
</task>

<task type="auto">
  <name>Task 2: Card de veredito read-only (grade/score/decomposição/checklist/níveis) + disclaimer condicional</name>
  <files>app.py</files>
  <read_first>
    - app.py bloco swing (após st.plotly_chart e o caption de atraso) — onde inserir o card abaixo do gráfico (D-01 leitura top-down)
    - app.py L77-92 (helpers fmt_rs/fmt_pct/fmt_num/esc_md) — formatação BR dos preços do card
    - .planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-RESEARCH.md §"Q5" (campos exatos de SetupSwing/ContribFamilia/Sinal), §"Code Examples" (_render_veredito), Pitfall 3 (decomposicao vazia em "Sem setup"), Pitfall 5 (sem st.metric p/ níveis), Open Question 1 (badge tendência/MTF)
    - .planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-PATTERNS.md §"Card de veredito", §"Copy não-imperativa"
    - .planning/phases/15-montagem-do-setup-setupswing-score/15-CONTEXT.md — D-06 "Pontuação de confluência técnica" (copy do score)
    - src/analista/report/setup.py (SetupSwing/ContribFamilia) e tests/test_setup_report.py L160-178 (lista de termos imperativos proibidos)
  </read_first>
  <action>
    Abaixo do gráfico e do caption de atraso, renderizar o card de veredito read-only lendo `sw` + `sinais`. NUNCA `st.metric` para entrada/stop/alvo (Pitfall 5).
    Cabeçalho: grade em destaque (`sw.grade`) + score rotulado como "Pontuação de confluência técnica" (D-06 da Fase 15), ex.: `st.markdown` com a grade e o score `sw.score`. Linha discreta opcional de contexto (Open Q1): "Tendência: {sinais.contexto.dow_diario} · MTF: {sinais.contexto.alinhamento_mtf}" quando `sinais.contexto is not None` — neutro, sem recomendação.
    Decomposição peso-a-peso (D-04): se `sw.decomposicao` não vazia, iterar `ContribFamilia` mostrando `familia`, `contribuicao` (pontos), `peso` e `detalhe` como barra/linha de contribuição (ex.: `st.progress` por contribuição normalizada ou markdown tabular). Se `sw.decomposicao == []` ou `sw.grade == "Sem setup"` (Pitfall 3): mensagem neutra "Sem confluência suficiente para um setup de estudo." e NÃO assumir decomposição não-vazia.
    Checklist (D-05): iterar `sinais.checklist.sinais` (quando `sinais.checklist is not None`) como lista com "✓" para `ativo=True` e "✗" para `ativo=False`, mostrando `nome` + `detalhe` neutro. Vem independente do gate (sempre que houver checklist).
    Tabela de níveis rotulada exatamente "Referências de estudo (não são ordens)" (D-05): linhas para zona de entrada (`sw.entrada_zona` ou `sinais.niveis.entrada_zona`), stop (`sw.stop`), alvo (`sw.alvo`) e R:R (`sinais.niveis.risco_retorno`, já formatado BR "1 : 2,5"/"indisponivel"). Usar `fmt_rs`/`esc_md` para os preços; `None` → "—". Usar `st.table`/markdown, NUNCA `st.metric`.
    Disclaimer condicional inline junto da tabela: texto curto pt-BR no tom "Esta página exibe sinais técnicos de estudo e não recomenda compra ou venda." — condicional/contextual (ajustar quando "Sem setup").
    Toda a copy adicionada deve passar o mesmo firewall do `test_setup_report.py::test_setup_sem_copy_imperativa` (proibido: compre/venda/comprar/vender/entre/recomend/sugiro/indico). Copy em pt-BR.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && grep -q "Referências de estudo (não são ordens)" app.py && grep -Eq "sw\.decomposicao|\.decomposicao" app.py && grep -q "checklist" app.py && grep -q "Sem confluência suficiente" app.py && grep -q "risco_retorno" app.py && ! grep -nE "st\.metric\(" app.py | sed -n '580,720p' | grep -q . && python -c "import ast; ast.parse(open('app.py').read())" && .venv/bin/python -m pytest -q tests/test_setup_report.py</automated>
  </verify>
  <acceptance_criteria>
    - `app.py contains "Referências de estudo (não são ordens)"` (label exato D-05)
    - Card lê `sw.grade`/`sw.score`/`sw.decomposicao` e `sinais.checklist.sinais` read-only (nenhum recálculo)
    - Decomposição vazia / "Sem setup" tratada com mensagem neutra ("Sem confluência suficiente para um setup de estudo") — Pitfall 3
    - Checklist renderiza ✓ (ativo) / ✗ (inativo) por `Sinal` (D-05)
    - Níveis numa tabela/markdown, NUNCA `st.metric` para entrada/stop/alvo (Pitfall 5); R:R lido de `sinais.niveis.risco_retorno` já formatado
    - Disclaimer condicional inline presente; `pytest tests/test_setup_report.py` sai 0 (firewall de copy intacto)
    - Behavior: a copy do card não contém termos imperativos (compre/venda/comprar/vender/entre/recomend/sugiro/indico)
    - `python -c "import ast; ast.parse(open('app.py').read())"` sai 0
  </acceptance_criteria>
  <done>Abaixo do gráfico aparece o card de veredito: grade+score, decomposição por família, checklist ✓/✗ e a tabela "Referências de estudo (não são ordens)" com disclaimer — tudo read-only e não-imperativo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → app.py (toggles do expander, ticker) | input não confiável; toggles só ligam/desligam overlays, sem efeito de cálculo |
| engine (dataclasses em memória) → render Plotly/markdown | a UI só LÊ campos; risco é exibir valor None/NaN ou copy imperativa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Repudiation/Integrity da fronteira regulatória | copy do card e dos rótulos de nível | mitigate | Copy estritamente não-imperativa (label "Referências de estudo (não são ordens)", sem st.metric); coberta pelo firewall `test_setup_report.py::test_setup_sem_copy_imperativa`. Gate de aceite SWING-02. |
| T-16-05 | Information Disclosure (robustez) | `$` em string R$ em markdown/metric (LaTeX) | mitigate | `esc_md()` ao exibir valores; `fmt_rs` trata None→"—". Baixa severidade. |
| T-16-06 | Tampering | `sw.decomposicao` vazia / `sinais.niveis is None` | accept | Branches de degradação graciosa tratam vazio sem levantar exceção (Pitfall 3). Sem severidade de segurança. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 283 testes verdes (nenhuma edição fora de app.py; firewall de copy intacto).
- `python -c "import ast; ast.parse(open('app.py').read())"` → sintaxe válida.
- Label "Referências de estudo (não são ordens)" e ausência de `st.metric` para níveis via grep (acceptance_criteria).
</verification>

<success_criteria>
- CHART-01 completo: S/R em bandas, Fibonacci, níveis do setup e padrões anotados sobre o candlestick.
- SWING-02 atendido: disclaimer condicional + copy não-imperativa em todos os níveis e no card.
- Card de veredito read-only (grade/score/decomposição/checklist/níveis) abaixo do gráfico; 283 goldens verdes.
</success_criteria>

<output>
After completion, create `.planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-02-SUMMARY.md`
</output>
