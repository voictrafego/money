---
phase: 16-p-gina-streamlit-gr-fico-do-momento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app.py]
autonomous: true
requirements: [SWING-01, CHART-01]
must_haves:
  truths:
    - "O 4º menu monta SinaisTecnicos e SetupSwing read-only a partir do FrameOHLC (zero recálculo de método na UI)"
    - "O gráfico vira candlestick multi-painel: candle no topo + overlays MM ligados + subpainéis RSI/MACD/ADX"
    - "Os toggles de overlay usam um dict de estado próprio da página (tec_estado_swing), nunca o tec_estado da aba Analisar"
    - "A barra viva continua marcada e o selo de atraso continua visível; nenhum nível/marcador deriva de iloc[-1]"
  artifacts:
    - path: "app.py"
      provides: "imports indicators+setup, cadeia calcular→montar_setup, dict tec_estado_swing, expander Overlays, figura make_subplots com candlestick+overlays+subpainéis"
      contains: "from analista.report import setup"
  key_links:
    - from: "app.py bloco swing"
      to: "indicators.calcular"
      via: "indicators.calcular(f.ohlc_ajustado, CFG, ohlc_nominal=f.ohlc)"
      pattern: "indicators\\.calcular\\(f\\.ohlc_ajustado, CFG, ohlc_nominal=f\\.ohlc\\)"
    - from: "app.py bloco swing"
      to: "setup.montar_setup"
      via: "montar_setup(sinais, CFG)"
      pattern: "setup\\.montar_setup\\("
    - from: "app.py figura swing"
      to: "grafico.overlays_preco / subpaineis_ativos / layout_subplots"
      via: "reuso direto das funções puras com tec_estado_swing"
      pattern: "grafico\\.(overlays_preco|subpaineis_ativos|layout_subplots)"
---

<objective>
Estende o bloco swing existente de `app.py` (L581–648) para deixar de ser um candlestick nu e
passar a montar — read-only — a cadeia de engine (`SinaisTecnicos` + `SetupSwing`) e a renderizar
uma figura multi-painel: candlestick no topo com overlays de médias móveis e subpainéis
RSI/MACD/ADX, controlados por um dicionário de estado ISOLADO da página swing.

Purpose: É o coração da fase — sem a cadeia `calcular → montar_setup` wirada e sem a figura
`make_subplots`, não há overlays, níveis nem veredito para as fases seguintes consumirem. Atende
SWING-01 (isolamento read-only) e a base de CHART-01 (candlestick + overlays + subpainéis + barra viva).
Output: `app.py` com 2 imports novos, a cadeia de engine, o dict `tec_estado_swing`, o expander
"⚙️ Overlays" e a figura candlestick multi-painel. Zonas S/R, Fibonacci, padrões e card de veredito
ficam para o plano 16-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-RESEARCH.md
@.planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-PATTERNS.md

<interfaces>
<!-- Contratos read-only. NÃO editar a engine; só LER estes campos. -->

frame_intraday(ticker, timeframe, nonce) -> FrameOHLC  (app.py L53-63, @st.cache_data ttl=300)
  .disponivel: bool | .motivo: str | .ohlc: DataFrame|None (NOMINAL Open/High/Low/Close/Volume)
  .ohlc_ajustado: DataFrame|None (split-adj) | .barra_viva: bool | .ultima_barra_ts: Timestamp|None
  .idx_ultima_fechada: int|None | .atraso_min: float|None | .timeframe: str

indicators.calcular(ohlc_ajustado, cfg, ohlc_nominal=None) -> SinaisTecnicos  (core/indicators.py)
  .tendencia (sma/ema 20/50/200, posicao_mm200) | .canais (donchian*, bb_*) | .forca (adx/pdi/ndi, atr)
  .momentum (rsi/macd/macd_sinal/macd_hist) | .niveis | .padroes | .checklist | .contexto

setup.montar_setup(sinais, cfg) -> SetupSwing  (report/setup.py L161; degrada p/ "Sem setup", nunca levanta)
  .score: float | .grade: str | .decomposicao: list[ContribFamilia] | .gate_rr_ok: bool
  .rr_valor: float|None | .conflito_mtf: bool | .entrada_zona: tuple|None | .stop: float|None | .alvo: float|None

grafico (from analista import grafico)  — funções PURAS golden-pinned, reusar as-is:
  overlays_preco(estado, sinais) -> list[OverlaySpec(nome, serie, estilo)]
  subpaineis_ativos(estado, sinais) -> list[SubpainelSpec(nome, series:[(rotulo,serie)], referencias)]
  layout_subplots(n_subpaineis) -> {"rows": int, "row_heights": [...]}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire da cadeia de engine + estado isolado + expander de overlays</name>
  <files>app.py</files>
  <read_first>
    - app.py L1-92 (imports atuais L16-22; CFG L72; helpers fmt_rs L85/esc_md L89/h) — ver o que JÁ existe
    - app.py L190-236 (aba Analisar: setdefault("tec_estado", grafico.estado_padrao()) L198-199 + expander "⚙️ Indicadores técnicos" com colunas + st.toggle + help=h(...)) — analog EXATO do expander
    - app.py L581-648 (bloco swing MVP a estender: ticker/TF/Atualizar/nonce/frame_intraday/degradação já implementados)
    - .planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-RESEARCH.md §"Pattern 1", §"Q5" (campos de SetupSwing/SinaisTecnicos), Pitfall 6 (ohlc_nominal)
    - .planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-PATTERNS.md §"Imports a adicionar", §"Cadeia de engine a wire", §"Estado isolado", §"Expander de toggles"
    - src/analista/report/setup.py (SetupSwing/ContribFamilia/montar_setup) e src/analista/core/indicators.py docstring de calcular() (L1222-1284) — contratos lidos read-only
  </read_first>
  <action>
    No topo de app.py, junto dos imports da engine (L16-22), adicionar `from analista.core import indicators`
    e `from analista.report import setup`. Não há colisão: `sc` já é screening, `setup` é nome livre.
    Dentro do bloco swing (`elif modo.startswith("📈"):`), no branch `else:` onde `f.disponivel` é True
    (após L624), ANTES de montar a figura, wirar a cadeia read-only: `sinais = indicators.calcular(f.ohlc_ajustado, CFG, ohlc_nominal=f.ohlc)`
    e `sw = setup.montar_setup(sinais, CFG)`. Nomear o resultado `sw` (NUNCA `setup`, que sombrearia o módulo).
    Passar SEMPRE `ohlc_nominal=f.ohlc` (Pitfall 6: sem ele, pivôs/S-R/Fib saem em escala ajustada e desalinham do candle nominal).
    Criar o dict de estado ISOLADO via `st.session_state.setdefault("tec_estado_swing", {...})` com os defaults D-02:
    `tendencia {"on": True, "tipo": "sma", "janelas": [20,50,200]}`, `canais {"donchian_on": False, "donchian_janela": 20, "bollinger_on": False}`,
    `forca {"on": True}`, `momentum {"rsi_on": True, "macd_on": True}`, e as chaves extras `"sr_on": True, "fib_on": True, "niveis_setup_on": True, "padroes_on": False`.
    Ler em `est = st.session_state["tec_estado_swing"]`. NUNCA usar `grafico.estado_padrao()` (tudo OFF) nem a chave `tec_estado` da Analisar (D-03 / SWING-01).
    Reservar o slot do gráfico ANTES do expander com `grafico_box = st.container()` (padrão Analisar L190-193) para o toggle redesenhar no mesmo rerun.
    Adicionar `with st.expander("⚙️ Overlays", expanded=False):` espelhando colunas+st.toggle do expander da Analisar (L200-236), gravando direto em
    `est["tendencia"]["on"]`, `est["tendencia"]["tipo"]` (radio sma/ema), `est["tendencia"]["janelas"]` (multiselect 20/50/200),
    `est["canais"]["donchian_on"]`, `est["canais"]["bollinger_on"]`, `est["forca"]["on"]`, `est["momentum"]["rsi_on"]`, `est["momentum"]["macd_on"]`,
    e os toggles extras `est["sr_on"]`, `est["fib_on"]`, `est["niveis_setup_on"]`, `est["padroes_on"]`. Usar `help=h(...)` onde houver chave de glossário existente.
    Copy em pt-BR. Não recalcular nada na UI — só ler campos das dataclasses (regra app.py read-only travada desde a Phase 2).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && grep -q "from analista.core import indicators" app.py && grep -q "from analista.report import setup" app.py && grep -Eq "indicators\.calcular\(f\.ohlc_ajustado, CFG, ohlc_nominal=f\.ohlc\)" app.py && grep -q "setup.montar_setup(" app.py && grep -q "tec_estado_swing" app.py && python -c "import ast,sys; ast.parse(open('app.py').read()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `app.py contains "from analista.core import indicators"` e `"from analista.report import setup"`
    - `app.py contains "indicators.calcular(f.ohlc_ajustado, CFG, ohlc_nominal=f.ohlc)"` (com ohlc_nominal — Pitfall 6)
    - `app.py contains "setup.montar_setup("` e o resultado é atribuído a `sw` (módulo `setup` não é sombreado)
    - `app.py contains "tec_estado_swing"` com defaults D-02 (MMs/forca/momentum ON; donchian/bollinger/padroes OFF; sr_on/fib_on/niveis_setup_on True)
    - O bloco swing NÃO referencia `tec_estado` nem `grafico.estado_padrao()` (estado isolado D-03 / SWING-01)
    - `python -c "import ast; ast.parse(open('app.py').read())"` sai 0 (sintaxe válida)
    - Nenhuma linha do bloco swing recalcula indicador/score na UI (só lê campos de FrameOHLC/SinaisTecnicos/SetupSwing)
  </acceptance_criteria>
  <done>Imports adicionados, cadeia `calcular → montar_setup` wirada com `ohlc_nominal`, dict `tec_estado_swing` isolado criado e expander "⚙️ Overlays" renderiza os toggles gravando em `est`.</done>
</task>

<task type="auto">
  <name>Task 2: Figura make_subplots — candlestick + overlays MM + subpainéis RSI/MACD/ADX + barra viva</name>
  <files>app.py</files>
  <read_first>
    - app.py L239-344 (aba Analisar: molde make_subplots — layout_subplots L262, make_subplots L263-266, overlays loop L281-286, subpainéis loop L306-327 com MACD-Histograma go.Bar colorido, update_layout+st.plotly_chart L339-344)
    - app.py L624-644 (figura MVP single-panel atual a SUBSTITUIR: go.Figure candlestick L626, xaxis_rangeslider_visible=False L634, add_vline barra viva L637-639, caption atraso L642-644)
    - .planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-RESEARCH.md §"Pattern 2", Pitfall 4 (rangeslider em make_subplots), §"Code Examples" (esqueleto)
    - .planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-PATTERNS.md §"Figura make_subplots candlestick", §"Barra viva (REUSO)"
    - src/analista/grafico.py — assinaturas de overlays_preco/subpaineis_ativos/layout_subplots e dataclasses OverlaySpec/SubpainelSpec (read-only)
  </read_first>
  <action>
    Substituir a figura single-panel `go.Figure(...)` do MVP (L624-640) por uma figura `make_subplots` espelhando o molde da aba Analisar (L262-344), dentro do `grafico_box` reservado no Task 1.
    Calcular `specs = grafico.subpaineis_ativos(est, sinais)` e `layout = grafico.layout_subplots(len(specs))`; criar `fig = make_subplots(rows=layout["rows"], cols=1, shared_xaxes=True, row_heights=layout["row_heights"], vertical_spacing=0.03)`.
    Row 1 = `go.Candlestick(x=f.ohlc.index, open=f.ohlc["Open"], high=f.ohlc["High"], low=f.ohlc["Low"], close=f.ohlc["Close"], name=ticker)` em `row=1, col=1` — usar `f.ohlc` NOMINAL (D-02).
    Desligar o rangeslider com `fig.update_xaxes(rangeslider_visible=False, row=1, col=1)` (Pitfall 4: rangeslider rouba altura das rows; NÃO usar `xaxis_rangeslider_visible` no layout do make_subplots).
    Overlays MM/Donchian/Bollinger: copiar o loop da Analisar L281-286 — `for ov in grafico.overlays_preco(est, sinais): fig.add_trace(go.Scatter(x=ov.serie.index, y=ov.serie.values, mode="lines", name=ov.nome, line=dict(ov.estilo)), row=1, col=1)`.
    Subpainéis RSI/MACD/ADX: copiar verbatim o loop da Analisar L306-327 — `for i, spec in enumerate(specs): r = i + 2`; "Histograma" vira `go.Bar` com marker_color verde≥0/vermelho<0; demais `go.Scatter`; `for ref in spec.referencias: fig.add_hline(..., row=r, col=1)`; `fig.update_yaxes(title_text=spec.nome.upper(), row=r, col=1)`.
    Barra viva: manter o `add_vline(x=f.ultima_barra_ts, ..., row=1, col=1)` quando `f.barra_viva and f.ultima_barra_ts is not None` (mover para a nova fig; é cosmético — iloc[-1], no-repaint preservado, nunca derivar nível dele).
    `fig.update_layout(height=400 + 140*len(specs), margin=dict(l=10,r=10,t=40,b=10), showlegend=True, legend=dict(orientation="h", y=1.02))` e `grafico_box.plotly_chart(fig, width="stretch")` (ou `st.plotly_chart` dentro do container).
    Manter intactos o selo/caption de atraso (L642-644) e o warning de histórico insuficiente (L646-648).
    Pitfall 1: NÃO gatear a figura por `leitura_tecnica_disponivel` — o candlestick e os subpainéis devem renderizar sempre que `f.disponivel`; `overlays_preco`/`subpaineis_ativos` já pulam série toda-NaN sozinhos.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && grep -q "make_subplots" app.py && grep -q "go.Candlestick" app.py && grep -Eq "rangeslider_visible=False" app.py && grep -q "grafico.overlays_preco(est" app.py && grep -q "grafico.subpaineis_ativos(est" app.py && grep -q "grafico.layout_subplots(" app.py && grep -q "add_vline" app.py && python -c "import ast; ast.parse(open('app.py').read())" && grep -cv '^\s*#' app.py >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `app.py contains "make_subplots"` usado dentro do bloco swing (figura multi-painel, não mais `go.Figure` single-panel no swing)
    - `app.py contains "go.Candlestick"` alimentado por `f.ohlc` (nominal) em `row=1, col=1`
    - `app.py contains "fig.update_xaxes(rangeslider_visible=False, row=1, col=1)"` (Pitfall 4)
    - O bloco swing chama `grafico.overlays_preco(est, sinais)`, `grafico.subpaineis_ativos(est, sinais)` e `grafico.layout_subplots(...)` com o `est` isolado (não recria a lógica de overlay na UI)
    - MACD-Histograma renderiza como `go.Bar` colorido verde≥0/vermelho<0 (verbatim do molde Analisar)
    - `add_vline` da barra viva permanece (`f.barra_viva` + `f.ultima_barra_ts`); nenhum nível é derivado de `iloc[-1]`
    - A figura renderiza mesmo quando `posicao_mm200=="indisponivel"` (não gateada por `leitura_tecnica_disponivel` — Pitfall 1)
    - `python -c "import ast; ast.parse(open('app.py').read())"` sai 0
  </acceptance_criteria>
  <done>O 4º menu mostra um candlestick multi-painel com overlays MM ligados por padrão, subpainéis RSI/MACD/ADX em rows próprias e a barra viva marcada; o selo de atraso continua visível.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → app.py (input ticker/timeframe) | input não confiável cruza para a UI; já normalizado/whitelistado na engine |
| app.py → Yahoo (yfinance via frame_intraday) | dependência externa best-effort, instável por natureza |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | timeframe escolhido pelo usuário (selectbox → tf_key) | accept | Whitelist fechada `_TF_MAP`/`_PERIODO_POR_TF`; `coletar_intraday` rejeita inválido com motivo `timeframe_invalido` (já implementado, V5). Baixa severidade. |
| T-16-02 | DoS | botão Atualizar martelando Yahoo | mitigate | Refresh manual + `@st.cache_data(ttl=300)` + nonce targetado (já implementado, Phase 12). Auto-refresh em segundos está fora de escopo. Baixa severidade. |
| T-16-03 | Information Disclosure | `st.session_state` (tec_estado_swing/nonce) | accept | Só guarda toggles/nonce não-sensíveis; sem auth/PII nesta camada (V3 n/a). |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 283 testes verdes (engine fundamentalista e funções puras de grafico.py intactas; nenhuma edição fora de app.py).
- `python -c "import ast; ast.parse(open('app.py').read())"` → sintaxe válida.
- Imports e cadeia de engine presentes via grep (ver acceptance_criteria).
</verification>

<success_criteria>
- 4º menu monta read-only `SinaisTecnicos` + `SetupSwing` com `ohlc_nominal`, sem recálculo na UI (SWING-01).
- Candlestick multi-painel com overlays MM + subpainéis RSI/MACD/ADX + barra viva, estado isolado em `tec_estado_swing` (CHART-01 base).
- 283 goldens verdes; `app.py` é a única edição.
</success_criteria>

<output>
After completion, create `.planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-01-SUMMARY.md`
</output>
