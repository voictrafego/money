---
phase: 09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/core/normalizacao.py
  - tests/test_normalizacao.py
autonomous: true
requirements: [PAY-01]

must_haves:
  truths:
    - "Existe uma primitiva pura que devolve a MEDIANA do payout sobre a série COMPLETA (todos os anos com payout não-None), sem janela de 3a e sem clamp em 1.0"
    - "A mediana pode legitimamente ficar > 100% (caso TAEE11 ≈ 2.16) — a primitiva nunca crava o teto"
    - "Um ano de payout anômalo do próprio histórico é descartado pela mediana sem marcação explícita"
    - "A fronteira de None é preservada: série vazia/só-None → None; 1 ano → o próprio valor"
  artifacts:
    - path: "src/analista/core/normalizacao.py"
      provides: "Primitiva pura mediana_payout (irmã de base_normalizada, reusando _limpar)"
      contains: "def mediana_payout"
    - path: "tests/test_normalizacao.py"
      provides: "Goldens unitários da mediana de payout (no-clamp >1.0, descarte de spike, fronteira None)"
      contains: "mediana_payout"
  key_links:
    - from: "src/analista/core/normalizacao.py::mediana_payout"
      to: "_limpar"
      via: "reuso do convênio de descarte de None"
      pattern: "_limpar\\("
---

<objective>
Criar a primitiva pura `mediana_payout` em `normalizacao.py` — a raiz metodológica do PAY-01.
Ela recebe a série de payouts por ano (com None) e devolve a **mediana sobre a série COMPLETA**,
**sem janela de 3 anos e sem clamp em 1.0** (D-01, D-03, D-04). É irmã de `base_normalizada`,
reusando `_limpar` e a mesma escada de fallback (vazio→None, 1 ponto→o próprio valor), mas com
regra de mediana pura sobre todos os pontos válidos.

Por que primeiro: as duas reescritas de método da Fase 9 (payout_valuation, dy_recorrente — Plan 02)
delegam a este número. O contrato puro precisa existir e estar travado por golden antes de qualquer
consumidor. Primitiva sem ciclo de import (só `statistics.median`) — a invariante de pureza
existente (`test_primitiva_e_pura_sem_import_de_fundamentals`) deve seguir verde.

Output: `normalizacao.mediana_payout` + goldens unitários.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-CONTEXT.md
@.planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-PATTERNS.md

<interfaces>
<!-- Contratos já existentes em src/analista/core/normalizacao.py que a nova primitiva espelha. -->
<!-- O executor usa estes diretamente — não precisa explorar o codebase. -->

Convênio de descarte de None (reusar VERBATIM, normalizacao.py L34-36):
  def _limpar(valores: Sequence[Number]) -> List[float]:
      # descarta os None (não contam como 0) e converte para float

Forma a espelhar — escada de fallback (base_normalizada, normalizacao.py L58-75):
  limpos = _limpar(valores) ; if not limpos: return None ;
  janela = ... ; if n == 1: return janela[0] ; if n < 5: return float(median(janela)) ; ...

Import já presente no módulo: `from statistics import median`. Number = Optional[float].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Goldens unitários da mediana de payout (RED)</name>
  <read_first>
    - tests/test_normalizacao.py (espelhar o estilo: golden de 1 linha com "porquê pelo método" + tolerância 1e-9; ver test_outlier_alto_suavizado_pela_mediana L21-28)
    - src/analista/core/normalizacao.py (assinatura/forma de base_normalizada e _limpar a espelhar)
    - .planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-CONTEXT.md §D-01/D-02/D-03/D-04
  </read_first>
  <files>tests/test_normalizacao.py</files>
  <action>
    Adicionar um bloco novo de goldens unitários para `norm.mediana_payout(serie_de_payouts)` (nome
    da primitiva fixado em D-discretion). Cobrir exatamente estas propriedades, cada uma com 1
    comentário "pelo método":
    (a) NO-CLAMP >1.0 (espírito TAEE11, D-03): uma série de payouts toda > 1.0 (ex.: [2.0, 2.1, 2.2,
        2.3]) devolve a mediana (~2.15) — assert que NÃO é cravada em 1.0 (`mediana_payout(serie) > 1.0`).
    (b) DESCARTE DE SPIKE pela mediana (D-01): série [0.40, 0.45, 0.50, 1.30] (um ano extraordinário
        do próprio histórico) devolve a mediana (0.475), claramente abaixo da média crua contaminada
        pelo 1.30 — assert mediana < média aritmética.
    (c) SÉRIE COMPLETA, não 3a (D-04): com 4 pontos válidos a primitiva usa TODOS (não só os 3
        últimos) — usar uma série onde a mediana-de-4 difere do que seria a média-dos-3-últimos.
    (d) IGNORA None (reuso de _limpar): [0.5, None, 0.7] → mediana([0.5,0.7]) = 0.6.
    (e) FRONTEIRA None/único (D-04): [] → None; [None, None] → None; [0.42] → 0.42.
    Estes testes DEVEM FALHAR agora (a função ainda não existe) — é o passo RED.
    NÃO tocar os goldens de base_normalizada/serie_winsorizada nem o teste de pureza.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" &amp;&amp; python -m pytest tests/test_normalizacao.py -k mediana_payout 2>&amp;1 | grep -E "error|AttributeError|failed" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_normalizacao.py -k mediana_payout` coleta ≥4 testes novos e FALHA (RED) por
      `AttributeError`/ausência de `norm.mediana_payout` — prova que os goldens existem antes da impl.
    - Os goldens existentes (base_normalizada, serie_winsorizada, pureza) seguem coletando sem erro de import.
  </acceptance_criteria>
  <done>Bloco de goldens da mediana de payout adicionado; falham por ausência da primitiva (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar a primitiva pura mediana_payout (GREEN)</name>
  <read_first>
    - src/analista/core/normalizacao.py (L34-36 _limpar; L58-75 base_normalizada — copiar a escada de fallback)
    - tests/test_normalizacao.py (os goldens da Task 1 + L87-92 teste de pureza que deve seguir verde)
    - .planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-PATTERNS.md §normalizacao.py
  </read_first>
  <behavior>
    - Série toda > 1.0 → mediana > 1.0 (SEM clamp, D-03)
    - Série com 1 ano anômalo → mediana descarta o spike (D-01)
    - Série completa usada (sem fatiar últimos 3, D-04)
    - None descartados via _limpar; [] / só-None → None; 1 ponto → o próprio valor
  </behavior>
  <files>src/analista/core/normalizacao.py</files>
  <action>
    Adicionar `def mediana_payout(valores: Sequence[Number]) -> Number:` (irmã de `base_normalizada`).
    Implementação: `limpos = _limpar(valores)`; se vazio → `return None`; se `len(limpos) == 1` →
    `return limpos[0]`; senão `return float(median(limpos))`. SEM parâmetro `anos_media`/janela (D-04:
    série completa), SEM `min(..., 1.0)` (D-03). Docstring de 2-3 linhas explicando o porquê PELO
    MÉTODO: "extraordinário = desvio do próprio histórico, capturado pela mediana sobre a série
    completa; mediana legítima pode ser >100% (transmissora que distribui de caixa regulatório —
    TAEE11)". Manter o módulo puro: só `statistics.median` (já importado) / numpy — NUNCA importar
    fundamentals/report (a invariante de pureza L87-92 vigia isso).
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" &amp;&amp; python -m pytest tests/test_normalizacao.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_normalizacao.py -q` 100% verde (goldens novos da Task 1 passam — GREEN).
    - `test_primitiva_e_pura_sem_import_de_fundamentals` verde (sem "fundamentals"/"report" no source).
    - Assinatura: `mediana_payout(valores)` chamável SEM args extras (sem janela/winsor obrigatórios).
  </acceptance_criteria>
  <done>mediana_payout implementada, pura, todos os goldens de normalizacao.py verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Função pura offline sobre números já validados; sem input de usuário, sem rede, sem deserialização não confiável, sem auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering (numérico) | mediana_payout devolver número errado/clamp acidental que distorce silenciosamente o valuation | mitigate | Goldens unitários travam no-clamp >1.0, descarte de spike e fronteira None (Task 1/2) |
| T-09-02 | (S/R/I/D/E) | superfície de função pura offline | accept | Sem rede/auth/PII/IO; vetor inexistente neste módulo |
</threat_model>

<verification>
- `pytest tests/test_normalizacao.py -q` totalmente verde.
- `grep "def mediana_payout" src/analista/core/normalizacao.py` retorna 1 ocorrência.
- Nenhuma import de engine adicionada ao módulo (pureza preservada).
</verification>

<success_criteria>
- Primitiva `mediana_payout` existe, é pura e está travada por goldens que provam D-01/D-03/D-04.
- Pronta para ser consumida por `payout_valuation` (Plan 02) sem ciclo de import.
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-01-SUMMARY.md`
</output>
