---
phase: 09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/analista/core/fundamentals.py
  - tests/test_fundamentals_consistencia.py
  - tests/test_vulc3_regressao.py
autonomous: true
requirements: [PAY-01, DYR-01]

must_haves:
  truths:
    - "payout_valuation() devolve a MEDIANA do payout sobre a série completa, sem janela 3a e sem clamp em 1.0 — payout sustentável < 100% para VULC3, preservado >100% para TAEE11"
    - "DY recorrente deriva de payout_valuation × lpa_valuation ÷ preço (earnings-based), não mais da mediana crua de 3 anos da série de dividendos"
    - "Com payout sem clamp, g_fundamentos = ROE_norm × (1 − payout) pode ficar ≤ 0 e o piso já existente em report.py (max(0,…)) zera g_alto — sem piso novo"
    - "A fronteira CRU é preservada: payout(ano) cru segue intacto (tabela por ano, detector de armadilha, screening per-ano)"
    - "Os métodos canônicos seguem chamáveis SEM args — consistência Analisar/Ranking app/Ranking cli por construção (FIX-04)"
    - "Os goldens que quebram por design (clamp/3a-média) são rebaselinados DELIBERADAMENTE para a semântica de mediana-sobre-série-completa, com justificativa"
  artifacts:
    - path: "src/analista/core/fundamentals.py"
      provides: "payout_valuation (mediana, sem clamp) + dpa_recorrente/dy_recorrente (earnings-based)"
      contains: "norm.mediana_payout"
    - path: "tests/test_fundamentals_consistencia.py"
      provides: "Goldens de payout rebaselinados para mediana-sobre-série-completa"
      contains: "payout_valuation"
    - path: "tests/test_vulc3_regressao.py"
      provides: "Capstone VULC3 rebaselinado (payout mediano >1.0, g_fund ≤ 0, g_alto 0)"
      contains: "payout_valuation"
  key_links:
    - from: "src/analista/core/fundamentals.py::payout_valuation"
      to: "norm.mediana_payout"
      via: "delegação da série completa de payout(ano)"
      pattern: "norm\\.mediana_payout\\("
    - from: "src/analista/core/fundamentals.py::dpa_recorrente"
      to: "payout_valuation × lpa_valuation"
      via: "composição earnings-based (D-05)"
      pattern: "payout_valuation\\(\\).*lpa_valuation\\(\\)|lpa_valuation\\(\\).*payout_valuation\\(\\)"
---

<objective>
Reescrever os dois métodos-núcleo da Fase 9 em `fundamentals.py`, consumindo a primitiva do Plan 01:

1. **PAY-01 — `payout_valuation()`** passa a delegar a `norm.mediana_payout` sobre a série COMPLETA
   de `payout(ano)`, **removendo a janela de 3 anos E o `min(..., 1.0)`** (D-01, D-03, D-04). Mantém
   a assinatura canônica chamável sem args (FIX-04).
2. **DYR-01 — `dpa_recorrente()/dy_recorrente()`** passam a ser **earnings-based**:
   `payout_valuation() × lpa_valuation() ÷ preço` (D-05), reusando a base de lucro normalizada da
   Fase 8 — não mais a mediana crua de 3 anos da série de dividendos (que, no VULC3, cai inteira na
   era de payout >100% e devolvia 20,4% falso).

Como a remoção do clamp invalida 4 asserts existentes POR DESIGN, este plano também os **rebaselina
deliberadamente com justificativa** para deixar a suíte verde ao final (o invariante TEST-07/TEST-08:
golden muda só por rebaseline justificado). A fronteira CRU (`payout(ano)`) NÃO é tocada (D-06).

Output: `payout_valuation` mediano/sem-clamp + DY recorrente earnings-based + suíte verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-CONTEXT.md
@.planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-PATTERNS.md
@src/analista/core/fundamentals.py
@src/analista/report/report.py

<interfaces>
<!-- Contratos que o executor consome diretamente — sem exploração de codebase. -->

Primitiva criada no Plan 01 (src/analista/core/normalizacao.py):
  def mediana_payout(valores: Sequence[Number]) -> Number   # mediana sobre série completa, sem clamp, fronteira None

Métodos canônicos da Fase 8 a reusar (src/analista/core/fundamentals.py):
  def lpa_valuation(self, anos_media=3, winsor=0.10) -> Optional[float]   # base de lucro normalizada / nº ações do último ano
  def payout(self, ano: int) -> Optional[float]                          # payout CRU por ano (NÃO tocar — D-06)
  def anos_ordenados(self) -> List[int]
  preco_atual: Optional[float]

Helpers de múltiplos (src/analista/core/multiples.py):
  mult.dividend_yield(dpa, preco) -> Optional[float]   # None se algum input None

Call sites de payout_valuation que DEVEM continuar funcionando SEM ARGS (FIX-04 — não adicionar
parâmetro obrigatório): report.py L66/L82/L133; cli.py L149/L159; app.py L318/L466/L472.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reescrever payout_valuation (mediana sobre série completa, sem clamp)</name>
  <read_first>
    - src/analista/core/fundamentals.py §payout_valuation (L77-90, implementação a substituir) e §base_lucro_normalizada (L122-125, a forma "delegar a série à primitiva" a copiar)
    - src/analista/core/normalizacao.py §mediana_payout (primitiva do Plan 01)
    - src/analista/report/report.py L82, L93-98 (piso g_alto max(0,…)) e L133-137 (DDM consome payout_proj) — confirmar que o piso absorve payout >1.0 SEM edição
    - .planning/phases/.../09-PATTERNS.md §fundamentals.py §payout_valuation
  </read_first>
  <behavior>
    - Série de payouts toda >1.0 (TAEE11 spirit) → payout_valuation > 1.0 (sem clamp)
    - Série com 1 ano extraordinário → mediana descarta o spike (payout sustentável < clamp antigo)
    - Série completa usada, não os 3 últimos anos
    - Sem dados / só-None → None (fronteira preservada)
  </behavior>
  <files>src/analista/core/fundamentals.py</files>
  <action>
    Substituir o corpo de `payout_valuation` (L77-90). Nova forma, espelhando `base_lucro_normalizada`
    (delegar a série à primitiva pura): construir a série COMPLETA `[self.payout(a) for a in
    self.anos_ordenados()]` (mantendo os None — a primitiva os descarta via _limpar) e devolver
    `norm.mediana_payout(...)`. REMOVER o slice `[-janela:]` e o `min(..., 1.0)`. Remover o parâmetro
    `janela` da assinatura → `def payout_valuation(self) -> Optional[float]:` (todos os call sites já
    chamam sem args; nenhum passa janela). Atualizar a docstring para a nova semântica PELO MÉTODO:
    "mediana do payout sobre a série histórica completa; expurgo de anos não-recorrentes é intrínseco
    à mediana (D-01); sem clamp — mediana pode ser >100% para quem distribui de caixa regulatório
    (TAEE11, D-03); `payout(ano)` CRU segue alimentando tabela/armadilha/screening (D-06)". NÃO tocar
    `payout(ano)` (L74-75) nem `report.py`.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" &amp;&amp; python -c "from analista.core.fundamentals import CompanyData; c=CompanyData(ticker='T',anos=[2015,2016,2017,2018]); c.lucro_liquido={a:100 for a in c.anos}; c.dividendos={a:200 for a in c.anos}; c.num_acoes={a:1 for a in c.anos}; print('no-clamp>1:', c.payout_valuation()); import inspect; assert 'janela' not in inspect.signature(c.payout_valuation).parameters; assert c.payout_valuation() &gt; 1.0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "norm.mediana_payout" src/analista/core/fundamentals.py` retorna 1 ocorrência (delegação).
    - `grep -c "min(.*1.0)" ` no corpo de payout_valuation = 0 (clamp removido).
    - Série toda payout 2.0 → `payout_valuation()` retorna 2.0 (snippet acima imprime >1.0 e assert passa).
    - `inspect.signature(payout_valuation)` SEM parâmetro `janela` (canônico no-arg, FIX-04).
    - `payout(ano)` cru inalterado (L74-75 intactas).
  </acceptance_criteria>
  <done>payout_valuation delega à mediana sobre série completa, sem clamp, sem janela; assinatura no-arg.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DY recorrente earnings-based (payout sustentável × LPA normalizado)</name>
  <read_first>
    - src/analista/core/fundamentals.py §dpa_recorrente/dy_recorrente (L173-181, a substituir) e §lpa_valuation (L132-135, a reusar)
    - tests/test_guardrails_fix06.py L114-137 (dy_recorrente < trailing; multiplos["DY rec."] != "DY") — devem seguir valendo
    - tests/test_vulc3_regressao.py L115-125 (dy_recorrente() <= dy_atual()) — deve seguir valendo
    - .planning/phases/.../09-CONTEXT.md §D-05 e §<specifics> (alvos VULC3 6,2% / TAEE11 8,3%)
  </read_first>
  <behavior>
    - dpa_recorrente = payout_valuation() × lpa_valuation() (provento sustentável por ação)
    - dy_recorrente = dpa_recorrente ÷ preço (earnings-based)
    - Qualquer input None (payout_valuation, lpa_valuation, preço) → None (fronteira)
    - dy_recorrente ≤ dy_atual num cenário com provento extraordinário no trailing
  </behavior>
  <files>src/analista/core/fundamentals.py</files>
  <action>
    Substituir `dpa_recorrente` (L173-176) e `dy_recorrente` (L178-181) pela composição earnings-based
    de D-05. `dpa_recorrente`: obter `p = self.payout_valuation()` e `l = self.lpa_valuation(anos_media,
    winsor)`; se `p is None or l is None` → `return None`; senão `return p * l`. `dy_recorrente`:
    `return mult.dividend_yield(self.dpa_recorrente(anos_media, winsor), self.preco_atual)`. NÃO mais
    `norm.base_normalizada(self.serie("dividendos"), ...)` (rejeitado por D-05 — cai inteira na era de
    payout >100%). Atualizar docstrings: "renda SUSTENTÁVEL = payout sustentável aplicado ao lucro
    normalizado por ação, consistente com g_fund (mesmo payout_valuation); distinta do dy_atual
    trailing". Manter `anos_media=3`/`winsor=0.10` nos kwargs (repassados a lpa_valuation; payout_valuation
    é no-arg).
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" &amp;&amp; python -m pytest tests/test_guardrails_fix06.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "payout_valuation\\(\\).*lpa_valuation|lpa_valuation.*payout_valuation" src/analista/core/fundamentals.py` confirma a composição earnings-based em dpa_recorrente.
    - `grep "base_normalizada(self.serie(.dividendos" src/analista/core/fundamentals.py` retorna 0 (mediana de dividendos crus removida).
    - `pytest tests/test_guardrails_fix06.py -q` verde (dy_recorrente < trailing; "DY rec." != "DY").
    - Fronteira None: com `preco_atual=None` ou lucro ausente, `dy_recorrente()` retorna None (não exceção).
  </acceptance_criteria>
  <done>dpa_recorrente/dy_recorrente earnings-based; guardrails FIX-06 verdes; fronteira None preservada.</done>
</task>

<task type="auto">
  <name>Task 3: Rebaseline deliberado dos 4 asserts que quebram por design + suíte verde</name>
  <read_first>
    - tests/test_fundamentals_consistencia.py L13-45 (4 testes de payout_valuation; semântica antiga = 3a + clamp)
    - tests/test_vulc3_regressao.py L67-113 (capstone; L83 payout==1.0, L84 g_fundamentos==0.0)
    - src/analista/core/growth.py §crescimento_por_fundamentos (L49-56 — retorna roe×(1−payout) SEM piso; payout>1 ⇒ negativo)
    - src/analista/report/report.py L93-98 (piso g_alto max(0,…) que pina g_alto=0 mesmo com g_fund<0)
    - .planning/phases/.../09-PATTERNS.md §Golden tests — REBASELINE/EXTEND
  </read_first>
  <files>tests/test_fundamentals_consistencia.py, tests/test_vulc3_regressao.py</files>
  <action>
    Rebaselinar APENAS os 4 asserts invalidados pela remoção do clamp/janela (D-01/D-03/D-04), cada um
    com 1 comentário de justificativa PELO MÉTODO (não "verde a qualquer custo"):
    (1) test_fundamentals_consistencia.py L15-21 `test_payout_valuation_clamp_em_1`: payout 1.5 em todo
        ano → mediana = 1.5 (sem clamp). Trocar `== 1.0` por `== 1.5` e renomear/recomentar o teste
        para "sem clamp — mediana legítima >1.0 (D-03)".
    (2) test_fundamentals_consistencia.py L24-31 `..._media_dos_3_ultimos_anos`: agora é mediana sobre a
        série COMPLETA (4 anos: 0.2, 0.6, 0.7, 0.8) ⇒ mediana = 0.65 (o ano 2021=0.2 ENTRA, D-04).
        Trocar o assert para `abs(v - 0.65) < 1e-9`, renomear para "...mediana_da_serie_completa" e
        ajustar o comentário (2021 não é mais ignorado).
    (3) test_vulc3_regressao.py L83 `assert c.payout_valuation() == 1.0`: a VULC3 sintética tem div≥lucro
        todo ano ⇒ mediana do payout ≈ 1.25 (>1.0, sem clamp). Trocar para um assert da nova semântica:
        `c.payout_valuation() > 1.0` (mediana da era de payout >100%, expurgo intrínseco) + comentário.
    (4) test_vulc3_regressao.py L84 `assert a.g_fundamentos == 0.0`: com payout ≈1.25 sem clamp,
        g_fund = ROE×(1−1.25) < 0 (crescimento_por_fundamentos não tem piso). Trocar para
        `assert a.g_fundamentos <= 0.0` com comentário "payout sustentável >100% ⇒ g_fund ≤ 0; o piso
        max(0,…) do report pina g_alto=0 (L85 segue valendo)". NÃO mexer em L85 (`a.g_alto == 0.0`
        segue verde via o piso) nem em L125 (dy_recorrente ≤ dy_atual segue valendo).
    NÃO relaxar nenhum outro assert. Não tocar test_growth_reconciliacao (payout exatamente 1.0 ⇒
    mediana 1.0 ⇒ g_fund 0.0 segue verde) nem test_consistencia_modos (consistência cross-menu por
    construção). Rodar a suíte completa ao final.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" &amp;&amp; python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` (suíte COMPLETA) 100% verde após o rebaseline.
    - Exatamente os 4 asserts acima foram alterados; `grep -n "== 1.0" tests/test_vulc3_regressao.py tests/test_fundamentals_consistencia.py` não retorna mais o assert de clamp de payout.
    - test_vulc3_regressao.py L85 (`a.g_alto == 0.0`) e L125 (`dy_recorrente() <= dy_atual()`) INTACTOS e verdes.
    - test_consistencia_modos.py verde (cross-menu: `multiplos["DP (payout)"] == c.payout_valuation()` ainda casa).
  </acceptance_criteria>
  <done>Os 4 goldens rebaselinados com justificativa; suíte completa verde; consistência cross-menu preservada.</done>
</task>

</tasks>

<verification>
- `pytest -q` suíte completa verde.
- `payout_valuation()` mediano/sem-clamp; `dy_recorrente()` earnings-based; ambos no-arg.
- `payout(ano)` cru e as superfícies de fronteira (tabela por ano, detector de armadilha, screening
  per-ano) inalteradas — D-06.
- Consumidor `report.analisar_acao`: o piso `g_alto = max(0,…)` absorve payout >1.0 (g_fund ≤ 0 ⇒
  g_alto 0) SEM piso novo; o DDM produz intrínseco finito com `payout_proj` possivelmente >1.0
  (verificado via test_vulc3_regressao / test_growth_reconciliacao verdes).
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Reescrita de métodos puros de analytics offline sobre dados CVM/Yahoo/BCB já validados; sem novo input de usuário, sem rede, sem auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering (numérico/regressão) | payout_valuation sem clamp distorcer silenciosamente o DDM (payout_proj >1.0) ou o g_alto | mitigate | test_vulc3_regressao (DDM finito, g_alto 0 via piso) + test_growth_reconciliacao verdes; piso existente max(0,…) cobre payout>1 |
| T-09-04 | Tampering (numérico) | dy_recorrente earnings-based produzir renda superestimada vs trailing | mitigate | test_guardrails_fix06 (dy_recorrente < trailing) + test_vulc3 L125 (≤ dy_atual) travam a relação |
| T-09-05 | (S/R/I/D/E) | superfície de métodos puros offline | accept | Sem rede/auth/PII/IO; vetor inexistente |

## Efeito cruzado registrado para a Fase 10 (NÃO resolver aqui)
`payout_valuation` SEM clamp (TAEE11 ≈ 2.16) passa a fluir para a regressão P/L de `screening.py`
(`preco_alvo_por_regressao(reg, c.payout_valuation(), …)` em cli.py L159 / app.py L472), calibrada
com payout ∈ [0,1]. A decisão de clampar SÓ na entrada da regressão é da Fase 10 (de-poison do
screening). Este plano NÃO toca screening.py; o item fica formalmente registrado no Plan 03.
</threat_model>

<success_criteria>
- PAY-01: payout sustentável = mediana sobre série completa, sem clamp, < 100% para VULC3 e
  preservado >100% para TAEE11; g_fundamentos volta a existir quando o payout cru passou de 100%
  num único ano (não mais zerado por saturação do clamp).
- DYR-01: DY recorrente = payout sustentável × lucro normalizado por ação ÷ preço, robusto à era de
  payout >100%.
- Fronteira CRU preservada (D-06); golden rebaselinados deliberadamente; suíte completa verde.
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-02-SUMMARY.md`
</output>
