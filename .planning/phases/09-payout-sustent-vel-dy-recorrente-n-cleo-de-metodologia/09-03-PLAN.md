---
phase: 09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - tests/test_payout_sustentavel_multiticker.py
  - .planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-CROSS-EFFECT-FASE10.md
autonomous: false
requirements: [PAY-01, DYR-01]

must_haves:
  truths:
    - "O expurgo de não-recorrentes é validado em VULC3 (caso-limite) E em ≥2 tickers normais (TAEE11/EGIE3): a mediana só descarta anos realmente extraordinários e não rebaixa quem distribui muito de forma sustentável"
    - "Existem goldens OFFLINE que travam as PROPRIEDADES do método (no-clamp >100%, descarte de spike, estabilidade de payout normal) — não dependem de rede"
    - "A validação contra os 5 tickers REAIS (VULC3/TAEE11/EGIE3/ITUB4/BBAS3) bate os números-alvo da metodologia"
    - "O efeito cruzado payout-sem-clamp → regressão P/L do screening está REGISTRADO para a Fase 10, não resolvido aqui"
  artifacts:
    - path: "tests/test_payout_sustentavel_multiticker.py"
      provides: "Goldens offline de propriedade multi-ticker (sintéticos calibrados aos 5 perfis)"
      contains: "mediana"
      min_lines: 40
    - path: ".planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-CROSS-EFFECT-FASE10.md"
      provides: "Registro do efeito cruzado payout-sem-clamp → regressão P/L (handoff Fase 10)"
      contains: "Fase 10"
  key_links:
    - from: "tests/test_payout_sustentavel_multiticker.py"
      to: "report.analisar_acao / payout_valuation / dy_recorrente"
      via: "construtores sintéticos por perfil de ticker"
      pattern: "analisar_acao|payout_valuation|dy_recorrente"
---

<objective>
Fechar a Fase 9 com a **trava de validação multi-ticker** (critério de aceite explícito do marco:
o fix vale para QUALQUER ticker, não tuna a um) e **registrar o efeito cruzado** para a Fase 10.

Duas camadas de validação:
1. **Offline (golden de propriedade):** construtores sintéticos calibrados aos 5 perfis — VULC3
   (ano extraordinário → mediana expurga o spike), TAEE11 (payout >100% TODO ano → mediana >1.0,
   NÃO zerada/clampada), EGIE3/ITUB4/BBAS3 (payout normal estável → mediana próxima do regime).
   Travam as PROPRIEDADES do método sem rede, deterministas.
2. **Live (checkpoint human-verify):** rodar o CLI nos 5 tickers REAIS e confirmar os números-alvo
   da discussão (mediana de payout: VULC3 43%, TAEE11 216%, EGIE3 49%, ITUB4 31%, BBAS3 20%; DY
   recorrente: VULC3 6,2% caindo dos 20,4% falsos, TAEE11 8,3% ≈ dividend-based real 8,1%).

E registrar o handoff: o `payout_valuation` sem clamp (TAEE11 ≈ 2.16) passa a alimentar a regressão
P/L do screening — a resolver na Fase 10 (de-poison), não aqui (D-06 / out of scope).

NOTA: a trava multi-ticker FORMAL/completa (TEST-08) é da Fase 11; aqui entregamos a validação
própria da Fase 9 (success criterion 4) sobre payout sustentável + DY recorrente.

Output: golden multi-ticker offline + validação live aprovada + nota de cross-effect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-CONTEXT.md
@.planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-PATTERNS.md
@tests/test_vulc3_regressao.py

<interfaces>
<!-- Padrao de golden engine-level a copiar (tests/test_vulc3_regressao.py): construtor sintetico
     CompanyData OFFLINE + report.analisar_acao(c, _cfg()); _cfg() carrega o config.yaml shipado. -->

  def _cfg() -> dict:  # carrega ROOT/config.yaml (deterministico, offline)
  CompanyData(ticker, nome, setor, anos=[...]) com series {ano: valor}: lucro_liquido,
    patrimonio_liquido, dividendos, num_acoes, vendas_liquidas, fco, preco_atual, beta
  c.payout_valuation() -> mediana sobre serie completa, sem clamp (Plan 02)
  c.dy_recorrente()     -> payout_valuation x lpa_valuation / preco (Plan 02)
  report.analisar_acao(c, cfg).multiplos["DP (payout)"] / ["DY rec."]

CLI live (README L42): ./.venv/bin/python -m analista analyze <TICKER>   # busca dados reais (rede)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Golden offline de propriedade multi-ticker + registro do cross-effect Fase 10</name>
  <read_first>
    - tests/test_vulc3_regressao.py L32-64 (_cfg + construtor sintetico _vulc3_sintetica — copiar a forma)
    - tests/test_normalizacao.py L21-28 (estilo de golden de propriedade com comentario "pelo metodo")
    - .planning/phases/.../09-CONTEXT.md secao specifics (numeros-alvo) e canonical_refs (cross-effect Fase 10)
    - src/analista/core/screening.py (call sites preco_alvo_por_regressao — so para citar no registro; NAO editar)
  </read_first>
  <files>tests/test_payout_sustentavel_multiticker.py, .planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-CROSS-EFFECT-FASE10.md</files>
  <action>
    (A) Criar tests/test_payout_sustentavel_multiticker.py com construtores sinteticos OFFLINE (um
    helper por perfil, copiando a forma de _vulc3_sintetica) que travam as PROPRIEDADES do metodo —
    sem numeros reais (que exigiriam rede); cada assert com 1 comentario "pelo metodo":
      - PERFIL TAEE11 (payout maior que 100% em TODO ano, ex.: div/lucro perto de 2.1 nos 10 anos):
        assert payout_valuation() maior que 1.0 — NAO clampada nem zerada (D-02/D-03: limiar absoluto
        rejeitado; TAEE11 e o contraexemplo que zeraria sob "expurgar maior que 100%").
      - PERFIL VULC3 (recorrente perto de 0.43 mais 1 ano extraordinario maior que 1.0): assert que a
        mediana fica no regime recorrente (menor que 1.0 e perto do payout dos anos normais),
        descartando o spike (D-01).
      - PERFIL NORMAL ESTAVEL (EGIE3/ITUB4/BBAS3 spirit, payout estavel ~0.30-0.50): assert que a
        mediana fica perto do payout do regime (nao rebaixada) E que dy_recorrente() e earnings-based
        e finito (perto de payout x LPA_norm / preco), com dy_recorrente() menor ou igual a dy_atual()
        sob trailing inflado.
    Usar tolerancias justificadas (nao numero magico). Carregar config via _cfg() shipado.
    (B) Criar 09-CROSS-EFFECT-FASE10.md (nota de handoff, ~15-25 linhas) REGISTRANDO o efeito cruzado,
    sem resolve-lo: payout_valuation sem clamp (TAEE11 perto de 2.16) agora flui para a regressao P/L
    de screening.py via preco_alvo_por_regressao(reg, c.payout_valuation(), ...) (cli.py L159, app.py
    L472), calibrada em payout no intervalo [0,1]. Item de verificacao para a Fase 10 (de-poison):
    decidir se clampa SO na entrada da regressao. Citar que o BSD per-ano (media([c.payout(a) ...]),
    screening.py L218/L257) e CRU per-ano e NAO e afetado (D-06). NAO editar screening.py.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" &amp;&amp; python -m pytest tests/test_payout_sustentavel_multiticker.py -q &amp;&amp; test -f ".planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-CROSS-EFFECT-FASE10.md"</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_payout_sustentavel_multiticker.py -q verde, com ≥3 perfis (TAEE11 no-clamp >1.0; VULC3 spike-discard <1.0; normal estável).
    - O perfil TAEE11 assert `payout_valuation() > 1.0` (prova D-02/D-03: limiar absoluto rejeitado, não zera quem paga >100% recorrente).
    - O perfil VULC3 assert mediana no regime recorrente (< 1.0), provando o descarte do ano extraordinário (D-01).
    - `09-CROSS-EFFECT-FASE10.md` existe, contém "Fase 10" e cita `preco_alvo_por_regressao`/`screening.py`; nenhum arquivo de src/ foi editado neste plano.
  </acceptance_criteria>
  <done>Golden multi-ticker offline verde (3+ perfis) + nota de cross-effect Fase 10 registrada; screening.py intacto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validacao live dos 5 tickers reais contra os numeros-alvo</name>
  <what-built>
    payout_valuation (mediana sobre serie completa, sem clamp) e dy_recorrente (earnings-based) na
    engine, mais o golden offline de propriedade multi-ticker. Falta confirmar contra DADOS REAIS
    (CVM/Yahoo/BCB, que exigem rede) que os numeros-alvo da metodologia batem — algo que um golden
    offline deterministico nao pode reproduzir sem fixtures congelados.
  </what-built>
  <read_first>
    - README.md L40-45 (como rodar o CLI analyze)
    - .planning/phases/.../09-CONTEXT.md secao specifics (numeros-alvo)
  </read_first>
  <how-to-verify>
    1. Rodar para cada ticker (rede necessaria):
       ./.venv/bin/python -m analista analyze VULC3
       ./.venv/bin/python -m analista analyze TAEE11
       ./.venv/bin/python -m analista analyze EGIE3
       ./.venv/bin/python -m analista analyze ITUB4
       ./.venv/bin/python -m analista analyze BBAS3
    2. Em cada saida, ler a linha de payout p/ valuation ("DP (payout)") e o "DY rec.".
    3. Conferir contra os alvos da metodologia (tolerancia de alguns p.p.; novos exercicios fiscais
       deslocam levemente a mediana):
       - Mediana de payout: VULC3 ~43%, TAEE11 ~216% (PRESERVADO >100%, nao clampado), EGIE3 ~49%,
         ITUB4 ~31%, BBAS3 ~20%.
       - DY recorrente: VULC3 ~6,2% (caiu dos ~20,4% falsos do metodo antigo), TAEE11 ~8,3%
         (proximo do dividend-based real ~8,1%).
    4. Confirmar o sentido qualitativo: TAEE11 segue com payout >100% (a mediana NAO o rebaixou);
       VULC3 caiu para um payout sustentavel < 100% e um DY recorrente muito abaixo do trailing.
  </how-to-verify>
  <acceptance_criteria>
    - Os 5 tickers rodam sem excecao e exibem payout p/ valuation + DY rec.
    - TAEE11 payout p/ valuation > 100% (preservado); VULC3 < 100% e DY rec. << DY trailing.
    - Os valores estao na vizinhanca dos alvos (algumas casas/p.p. de tolerancia); divergencia
      material e investigada antes de aprovar.
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" se os 5 baterem os alvos, ou descreva a divergencia.</resume-signal>
</task>

</tasks>

<verification>
- `pytest -q` (suite completa, incluindo o novo multi-ticker) verde.
- Validacao live dos 5 tickers reais aprovada contra os numeros-alvo.
- `09-CROSS-EFFECT-FASE10.md` registrado; screening.py NAO editado (D-06 / handoff Fase 10).
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analise live -> rede (Yahoo/CVM/BCB) | A Task 2 toca a rede para buscar dados reais; superficie ja existente do app, sem nova borda de input de usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering (numerico) | metodo passar nos sinteticos mas falhar em dados reais (calibracao enganosa) | mitigate | Checkpoint human-verify confronta os 5 tickers reais contra os alvos da metodologia |
| T-09-07 | Information disclosure | dados de mercado publicos via rede | accept | Sem credenciais/PII; APIs publicas gratuitas ja em uso |
| T-09-08 | (S/R/D/E) | superficie offline de testes | accept | Vetor inexistente |
</threat_model>

<success_criteria>
- Validado em VULC3 (caso-limite) E em >=2 tickers normais (TAEE11/EGIE3 e mais): o expurgo so atua
  sobre anos realmente extraordinarios e nao rebaixa quem distribui muito de forma sustentavel
  (success criterion 4 da Fase 9).
- Golden offline de propriedade verde + validacao live aprovada.
- Efeito cruzado payout-sem-clamp -> regressao P/L do screening registrado para a Fase 10 (nao
  resolvido aqui).
</success_criteria>

<output>
Apos completar, criar `.planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-03-SUMMARY.md`
</output>
