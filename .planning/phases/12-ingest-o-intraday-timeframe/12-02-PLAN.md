---
phase: 12-ingest-o-intraday-timeframe
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - app.py
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "Existe um wrapper de cache frame_intraday(ticker, timeframe, nonce) em app.py com TTL curto (300s)"
    - "A invalidação é targetada por nonce — re-buscar um (ticker, timeframe) cria nova entrada de cache sem tocar nas demais"
    - "app.py NUNCA chama st.cache_data.clear() (o cache da aba Analisar — montar/selic_atual/rf_capm — permanece intacto)"
    - "Os 191 testes golden seguem verdes após a modificação thin em app.py"
  artifacts:
    - path: "app.py"
      provides: "frame_intraday (wrapper @st.cache_data ttl=300) + _nonce_key helper — contrato de cache consumido pelo botão Atualizar da Fase 16"
      contains: "def frame_intraday"
  key_links:
    - from: "app.py frame_intraday"
      to: "intraday.coletar_intraday"
      via: "wrapper @st.cache_data(ttl=300) com nonce só na chave de cache"
      pattern: "intraday\\.coletar_intraday"
    - from: "app.py"
      to: "(firewall) cache da aba Analisar"
      via: "ausência de st.cache_data.clear() global"
      pattern: "cache_data\\.clear"
---

<objective>
Adicionar a `app.py` o contrato de cache intraday targetado: o wrapper
`frame_intraday(ticker, timeframe, nonce)` decorado com `@st.cache_data(show_spinner=False, ttl=300)`
e o helper `_nonce_key(ticker, timeframe)`. O `nonce` entra **apenas na chave de cache** —
incrementá-lo (no botão Atualizar, que será wirado na Fase 16) invalida **só** aquele
`(ticker, timeframe)`, nunca o `.clear()` global que apagaria o cache da aba Analisar.

Purpose: Cobre DATA-03 (Atualizar + cache TTL curto + invalidação targetada). Entrega o
**contrato de cache** (interface-first): a Fase 16 consome `frame_intraday` no botão Atualizar
da página de swing. `app.py` permanece read-only quanto à engine (toda a lógica vive em `intraday.py`).

Output: `app.py` com o wrapper de cache + helper de nonce, sem `.clear()` global.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-ingest-o-intraday-timeframe/12-CONTEXT.md
@.planning/phases/12-ingest-o-intraday-timeframe/12-RESEARCH.md
@.planning/phases/12-ingest-o-intraday-timeframe/12-PATTERNS.md

<interfaces>
<!-- Contratos existentes em app.py a espelhar — verificado 2026-06-29. -->

De app.py (wrappers de cache existentes, o molde a seguir):
- linha 30: `@st.cache_data(show_spinner=False)` / `def carregar_config()`
- linha 36: `@st.cache_data(show_spinner=False, ttl=3600)` / `def montar(ticker, ano_base, n)` → build.montar_empresa
- linha 41: `@st.cache_data(show_spinner=False, ttl=3600)` / `def selic_atual()`
- linha 46: `@st.cache_data(show_spinner=False, ttl=3600)` / `def rf_capm(fallback, anos)`
- idioma de session_state já usado: `st.session_state.setdefault("tec_estado", ...)` (linha 178); `st.button(...)` (linhas 409, 465)

Da engine (criada no Plan 12-01):
- `analista.ingest.intraday.coletar_intraday(ticker, timeframe) -> FrameOHLC`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar o wrapper de cache intraday + helper de nonce a app.py</name>
  <files>app.py</files>
  <read_first>
    - app.py — cabeçalho + wrappers de cache (linhas 1-55), especialmente `montar`/`selic_atual`/`rf_capm` (36-50) como molde exato; idioma de `st.session_state.setdefault` (178) e `st.button` (409, 465).
    - src/analista/ingest/intraday.py — a função `coletar_intraday` criada no Plan 12-01 (assinatura a chamar).
    - .planning/phases/12-ingest-o-intraday-timeframe/12-RESEARCH.md — Pattern 2 (linhas 264-287: wrapper + nonce), §"Anti-Patterns" (289-296: NUNCA `.clear()`).
    - .planning/phases/12-ingest-o-intraday-timeframe/12-PATTERNS.md — seção `app.py` (105-131): molde do wrapper e anti-pattern travado.
  </read_first>
  <action>
    Modificação THIN em `app.py`, junto aos wrappers de cache existentes (perto da linha 36-50):

    1. Adicionar o wrapper `frame_intraday(ticker, timeframe, nonce)` decorado com
       `@st.cache_data(show_spinner=False, ttl=300)` (TTL curto, 300s — não 3600 como os demais).
       Corpo: import tardio `from analista.ingest import intraday` (espelha como `app.py` isola imports
       pesados nas funções) e `return intraday.coletar_intraday(ticker, timeframe)`. O parâmetro `nonce`
       entra **apenas na assinatura/chave de cache** — NÃO é repassado a `coletar_intraday` (a engine não
       o conhece). Comentar inline o "porquê": mudar só o nonce de um par cria nova entrada e a antiga
       expira pelo TTL, sem tocar nas outras entradas nem no cache de `montar()`.

    2. Adicionar o helper `_nonce_key(ticker, timeframe)` retornando `f"nonce_intraday::{ticker}::{timeframe}"`
       — chave de `st.session_state` por par (ticker, timeframe) que o botão Atualizar da Fase 16 vai
       incrementar (`setdefault(k, 0)` + `if st.button("Atualizar"): st.session_state[k] += 1`).

    ANTI-PATTERN TRAVADO (D-08): NUNCA chamar `st.cache_data.clear()` em lugar nenhum de `app.py`
    (apagaria `montar`/`selic_atual`/`rf_capm` da aba Analisar). NÃO adicionar a página/menu de swing
    nem o botão renderizado aqui — isso é Fase 16; este plano entrega só o contrato de cache.
    NÃO alterar nenhum dos wrappers existentes nem a lógica da aba Analisar (`app.py` read-only).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)"; .venv/bin/python -m py_compile app.py && test "$(grep -vc '^[[:space:]]*#' app.py >/dev/null; grep -v '^[[:space:]]*#' app.py | grep -c 'cache_data\.clear')" = "0" && grep -q 'def frame_intraday' app.py && grep -q 'def _nonce_key' app.py && grep -q 'ttl=300' app.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app.py` contém `def frame_intraday(` e `def _nonce_key(`.
    - `app.py` contém `@st.cache_data(show_spinner=False, ttl=300)` no wrapper intraday (TTL 300s, distinto do 3600 dos demais).
    - `app.py` contém `intraday.coletar_intraday(` (wrapper chama a engine do Plan 12-01).
    - `grep -v '^[[:space:]]*#' app.py | grep -c 'cache_data\.clear'` == 0 (sem `.clear()` global, comentários ignorados).
    - `.venv/bin/python -m py_compile app.py` sai com código 0 (sintaxe válida).
    - `.venv/bin/python -m pytest -q` → 191+ testes verdes (a modificação thin em app.py não regride nada).
  </acceptance_criteria>
  <done>app.py expõe o wrapper de cache `frame_intraday(ticker, timeframe, nonce)` com TTL 300s + helper `_nonce_key`, chamando a engine intraday, sem nunca usar `.clear()` global; a aba Analisar e seus caches permanecem intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → cache wrapper (`frame_intraday`) | `ticker`/`timeframe`/`nonce` do usuário cruzam para o wrapper de cache (camada UI thin) |
| app.py (UI) → engine (`intraday.coletar_intraday`) | a UI delega toda a lógica/I-O à engine; não recalcula método |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Denial of Service | refresh/fetch loop martelando a Yahoo via o botão Atualizar | mitigate | refresh **manual** + `@st.cache_data(ttl=300)`; invalidação por **nonce** (não `.clear()`); sem auto-refresh (out of scope explícito no REQUIREMENTS) |
| T-12-05 | Tampering / Availability | `.clear()` global apagaria o cache da aba Analisar | mitigate | proibição travada (D-08): nenhum `st.cache_data.clear()` em `app.py`; gate de aceite verifica `grep -c 'cache_data\.clear' == 0` |
| T-12-08 | Information Disclosure | conteúdo cacheado | accept | apenas dados públicos de mercado em memória; `session_state` guarda só o nonce (inteiro), nunca credencial |
</threat_model>

<verification>
- `.venv/bin/python -m py_compile app.py` → código 0 (sintaxe válida; app.py é script Streamlit, não importável standalone).
- `grep -v '^[[:space:]]*#' app.py | grep -c 'cache_data\.clear'` == 0 (firewall do cache da aba Analisar).
- `.venv/bin/python -m pytest -q` → 191+ verdes (modificação thin não regride a engine fundamentalista).
</verification>

<success_criteria>
- `frame_intraday(ticker, timeframe, nonce)` existe com `@st.cache_data(ttl=300)` e chama `intraday.coletar_intraday`.
- O `nonce` entra só na chave de cache; `_nonce_key` define a chave de session_state por par (ticker, timeframe).
- Nenhum `.clear()` global em `app.py`; cache da aba Analisar intacto.
- 191 testes golden seguem verdes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ingest-o-intraday-timeframe/12-02-SUMMARY.md`
</output>
