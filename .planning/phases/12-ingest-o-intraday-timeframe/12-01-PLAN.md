---
phase: 12-ingest-o-intraday-timeframe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/ingest/intraday.py
  - tests/test_ingest_intraday.py
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "A engine retorna OHLCV de um ticker para diário, 1h, 30m e 5m via coletar_intraday(ticker, timeframe) isolada do pipeline diário"
    - "Os timestamps do índice são normalizados para America/Sao_Paulo independente do TZ do processo (VPS=UTC)"
    - "A última barra (viva) é identificável e idx_ultima_fechada permite calcular sobre a barra fechada (iloc[-2])"
    - "Qualquer falha/vazio retorna FrameOHLC(disponivel=False) com motivo categorizado, nunca None nem exceção"
    - "ohlc_ajustado é derivado de prices._ajustar_por_split sem nova chamada de rede; ohlc nominal é preservado"
    - "Os 191 testes golden seguem verdes e novos testes cobrem barra viva, matriz period×interval, timezone e barras ilíquidas"
  artifacts:
    - path: "src/analista/ingest/intraday.py"
      provides: "coletar_intraday + FrameOHLC + _PERIODO_POR_TF + categorias de motivo + _normaliza_tz"
      contains: "def coletar_intraday"
      min_lines: 70
    - path: "tests/test_ingest_intraday.py"
      provides: "Testes offline (monkeypatch) das edges da ingestão intraday"
      contains: "def test_idx_ultima_fechada_clock_free"
      min_lines: 80
  key_links:
    - from: "src/analista/ingest/intraday.py"
      to: "prices._ajustar_por_split"
      via: "reuso de função pura split-only (sem rede)"
      pattern: "prices\\._ajustar_por_split"
    - from: "src/analista/ingest/intraday.py"
      to: "prices.yahoo_symbol + prices._yf + prices._MAX_TENTATIVAS"
      via: "resolução .SA + retry yfinance reaproveitados"
      pattern: "prices\\.(yahoo_symbol|_yf|_MAX_TENTATIVAS)"
---

<objective>
Criar a camada de ingestão de OHLCV multi-timeframe (`src/analista/ingest/intraday.py`)
como réplica paralela e parametrizada de `ingest/prices.py`, mais a sua suíte de testes
offline (`tests/test_ingest_intraday.py`). A função `coletar_intraday(ticker, timeframe)`
devolve o dataclass rico `FrameOHLC` (nominal + split-ajustado + metadados de barra viva),
é **isolada** de `coletar_mercado`/`montar_empresa` e do cache da aba Analisar, e **nunca**
levanta exceção.

Purpose: Fundação de dados da v1.4. As Fases 13 (indicadores/pivôs sobre `ohlc_ajustado` na
barra fechada) e 16 (gráfico sobre `ohlc` nominal + metadados) consomem este contrato. Sem ele,
nada do produto de swing existe.

Output: `intraday.py` (engine de borda) + `test_ingest_intraday.py` (golden offline).
Cobre DATA-01 (ingestão intraday isolada, `auto_adjust=False` + split-adjust, tz America/Sao_Paulo)
e o lado-engine de DATA-02 (atraso_min injetável, degradação para "indisponível", historico_insuficiente).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ingest-o-intraday-timeframe/12-CONTEXT.md
@.planning/phases/12-ingest-o-intraday-timeframe/12-RESEARCH.md
@.planning/phases/12-ingest-o-intraday-timeframe/12-PATTERNS.md

<interfaces>
<!-- Contratos a REUSAR de prices.py — extrair daqui, não explorar o codebase. -->
<!-- Verificado em src/analista/ingest/prices.py (2026-06-29). -->

De src/analista/ingest/prices.py:
- `_MAX_TENTATIVAS = 3` (linha 23) — nº de tentativas do retry
- `_BACKOFF_SEG = (0.5, 1.0)` (linha 24) — tempos de backoff
- `def _yf()` (linha 27) — import tardio de yfinance, mockável no teste
- `def yahoo_symbol(ticker: str) -> str` (linha 40) — resolução `.SA` (casing/dup)
- `@dataclass class DadosMercado` (linhas 45-60) — molde do dataclass rico (campos `ohlc` nominal + `ohlc_ajustado`)
- `def _ajustar_por_split(hist) -> Optional["pd.DataFrame"]` (linha 70) — split-only puro, golden-testado, SEM rede; REUSAR tal-qual
- padrão do laço de retry em `coletar_mercado` (linhas 113-155): `tk.history(period="5y", auto_adjust=False)` + break ao ter dados + `time.sleep(_BACKOFF_SEG[...])`

Tabela period×interval CRAVADA nos tetos da Yahoo (RESEARCH §"Tabela period×interval", [VERIFIED] contra API):
- diario → ("5y", "1d")
- 1h     → ("730d", "1h")   (teto HARD ≤ 730 dias)
- 30m    → ("60d", "30m")   (teto HARD ≤ 60 dias)
- 5m     → ("60d", "5m")    (teto HARD ≤ 60 dias)
Exceder o teto NÃO clampa: retorna DataFrame vazio. NUNCA derivar period do input do usuário.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar a engine de ingestão intraday (intraday.py)</name>
  <files>src/analista/ingest/intraday.py</files>
  <read_first>
    - src/analista/ingest/prices.py — INTEGRAL: cabeçalho/imports (10-29), `_MAX_TENTATIVAS`/`_BACKOFF_SEG` (23-24), `_yf` (27-29), `yahoo_symbol` (40-42), dataclass `DadosMercado` (45-60), `_ajustar_por_split` (70-110), laço de retry de `coletar_mercado` (113-155). É o analog exact.
    - .planning/phases/12-ingest-o-intraday-timeframe/12-RESEARCH.md — Pattern 1 (linhas 164-262: código-fonte de referência de `coletar_intraday`), §"Normalização de timezone determinística" (349-356), Pitfalls 1-5 (313-341), §"Tabela period×interval" (80-91).
    - .planning/phases/12-ingest-o-intraday-timeframe/12-PATTERNS.md — seção `intraday.py` (24-102): mapeamento campo-a-campo dos analogs.
    - src/analista/core/indicators.py — guard de borda (linhas ~415-416): molde do "nunca exceção, degrada para vazio". NÃO modificar; é o consumidor da Fase 13.
  </read_first>
  <behavior>
    - test_mapa_tf_period_interval: `_PERIODO_POR_TF` == {diario:("5y","1d"), 1h:("730d","1h"), 30m:("60d","30m"), 5m:("60d","5m")}
    - test_tf_invalido: coletar_intraday("PETR4","15m_xyz") → FrameOHLC(disponivel=False, motivo="timeframe_invalido"), sem exceção
    - test_ohlc_ajustado_reusa_split: com fixture de hist, `ohlc_ajustado` == prices._ajustar_por_split(hist) (ponta nominal coincide)
    - test_tz_normaliza_naive: índice naive (assumido UTC) → índice final em America/Sao_Paulo (offset -03:00)
    - test_tz_idempotente_sao_paulo: índice já em America/Sao_Paulo permanece America/Sao_Paulo
    - test_idx_ultima_fechada_clock_free: fixture 6 barras, última Volume=0 → idx_ultima_fechada == len(ohlc)-2, barra_viva True
    - test_atraso_min_injetavel: agora=Timestamp fixo → atraso_min == (agora - ultima_barra_ts) em minutos (determinístico, ~15.0 no exemplo do RESEARCH)
    - test_frame_curto_historico_insuficiente: fixture com 1 barra → idx_ultima_fechada is None, motivo="historico_insuficiente"
    - test_disponivel_true_frame_curto: fixture com 1 barra → disponivel=True (há barra p/ o gráfico)
    - test_vazio_sem_dados: history() vazio → disponivel=False, motivo="sem_dados"
    - test_excecao_fetch_falhou: history() levanta exceção em todas as tentativas → disponivel=False, motivo="fetch_falhou"
  </behavior>
  <action>
    Criar `src/analista/ingest/intraday.py` espelhando o estilo de `prices.py`. Componentes concretos:

    1. Imports: `from __future__ import annotations`, `import time`, `from dataclasses import dataclass`, `from typing import Optional`, `from . import prices`. Reusar `prices.yahoo_symbol`, `prices._ajustar_por_split`, `prices._yf`, `prices._MAX_TENTATIVAS`, `prices._BACKOFF_SEG` — NÃO redefinir essas constantes. Usar `time.sleep` (import local), pois o teste monkeypatcha `intraday.time.sleep`. NÃO importar `streamlit` (a engine permanece testável offline — anti-pattern travado).

    2. Constante `_TZ_B3 = "America/Sao_Paulo"`.

    3. Tabela `_PERIODO_POR_TF` cravada nos tetos da Yahoo (ver `<interfaces>`): diario→("5y","1d"), 1h→("730d","1h"), 30m→("60d","30m"), 5m→("60d","5m"). Estes valores são `[VERIFIED]` no RESEARCH — não inventar variações nem usar `period="max"` (inconsistente p/ intraday).

    4. Categorias de `motivo` como constantes de módulo (D-07, copy de UI fica fora da engine): `MOTIVO_OK=""`, `MOTIVO_TF_INVALIDO="timeframe_invalido"`, `MOTIVO_FETCH_FALHOU="fetch_falhou"`, `MOTIVO_SEM_DADOS="sem_dados"`, `MOTIVO_HIST_INSUF="historico_insuficiente"`.

    5. `@dataclass FrameOHLC` (D-01/D-02), molde de `DadosMercado`, campos exatos: `timeframe: str`, `ohlc: Optional["pd.DataFrame"]=None` (nominal), `ohlc_ajustado: Optional["pd.DataFrame"]=None` (split-only), `ultima_barra_ts: Optional["pd.Timestamp"]=None`, `barra_viva: bool=False`, `idx_ultima_fechada: Optional[int]=None`, `atraso_min: Optional[float]=None`, `disponivel: bool=False`, `motivo: str=MOTIVO_SEM_DADOS`. Anotações pandas em string para evitar import pesado no topo (espelha `prices.py`).

    6. `_normaliza_tz(df)` (código novo, 3 linhas — Pitfall 5): se `getattr(df.index, "tz", None) is None` → `index.tz_localize("UTC").tz_convert(_TZ_B3)`; senão → `index.tz_convert(_TZ_B3)` (idempotente no caso `.SA`). Determinístico e imune ao TZ do processo (VPS=UTC).

    7. `coletar_intraday(ticker, timeframe, agora: Optional["pd.Timestamp"]=None) -> FrameOHLC`:
       - validar `timeframe` contra `_PERIODO_POR_TF` (conjunto fechado); se ausente → retornar `FrameOHLC(timeframe=timeframe, disponivel=False, motivo=MOTIVO_TF_INVALIDO)` (V5 input validation).
       - `period, interval = _PERIODO_POR_TF[timeframe]`; `sym = prices.yahoo_symbol(ticker)`.
       - laço de retry reusando `prices._MAX_TENTATIVAS`/`prices._BACKOFF_SEG`: `prices._yf().Ticker(sym).history(period=period, interval=interval, auto_adjust=False)`. `auto_adjust=False` é obrigatório (preserva Close nominal + Stock Splits). `try/except → hist=None`; break quando `hist is not None and not hist.empty`; `time.sleep(prices._BACKOFF_SEG[min(tentativa, len-1)])` entre tentativas.
       - guard de borda (D-06, nunca exceção): `hist is None` → `motivo=MOTIVO_FETCH_FALHOU, disponivel=False`; `hist.empty` → `motivo=MOTIVO_SEM_DADOS, disponivel=False`.
       - `hist = _normaliza_tz(hist)`; `ohlc_aj = prices._ajustar_por_split(hist)` (reuso direto, sem rede).
       - metadados clock-free (D-04): `n=len(hist)`; `ultima_ts=hist.index[-1]`; `idx_fechada = n-2 if n>=2 else None`; `barra_viva = (n>=1)` (última barra SEMPRE suspeita); `agora` default `pd.Timestamp.now(tz=_TZ_B3)`; `atraso_min = float((agora-ultima_ts).total_seconds()/60.0)`.
       - retornar `FrameOHLC(disponivel=True, ...)` com `motivo = MOTIVO_HIST_INSUF if idx_fechada is None else MOTIVO_OK` (frame com 1 barra fica disponivel=True para o gráfico, mas marcado historico_insuficiente — A3).

    NÃO reimplementar split-adjust, retry, resolução `.SA` nem degradação de indicador (Don't Hand-Roll). NÃO tocar em `build.py`/`coletar_mercado` (firewall).
  </action>
  <verify>
    <automated>.venv/bin/python -c "from analista.ingest import intraday; f=intraday.coletar_intraday('PETR4','tf_invalido'); assert f.disponivel is False and f.motivo=='timeframe_invalido'; assert intraday._PERIODO_POR_TF=={'diario':('5y','1d'),'1h':('730d','1h'),'30m':('60d','30m'),'5m':('60d','5m')}; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/analista/ingest/intraday.py` contém `def coletar_intraday(` e `class FrameOHLC`.
    - `FrameOHLC` tem exatamente os campos: timeframe, ohlc, ohlc_ajustado, ultima_barra_ts, barra_viva, idx_ultima_fechada, atraso_min, disponivel, motivo.
    - `intraday._PERIODO_POR_TF == {'diario':('5y','1d'),'1h':('730d','1h'),'30m':('60d','30m'),'5m':('60d','5m')}` (assert do comando verify passa).
    - `coletar_intraday('PETR4','tf_invalido')` retorna `FrameOHLC(disponivel=False, motivo='timeframe_invalido')` sem exceção.
    - `grep -c 'import streamlit' src/analista/ingest/intraday.py` == 0 (engine não conhece Streamlit).
    - `grep -c 'auto_adjust=False' src/analista/ingest/intraday.py` >= 1.
    - `grep -c 'prices\._ajustar_por_split' src/analista/ingest/intraday.py` >= 1 (reuso, não reimplementação).
  </acceptance_criteria>
  <done>A engine `intraday.py` existe, importa limpo, valida timeframe contra conjunto fechado, faz fetch com auto_adjust=False + retry reaproveitado, normaliza tz e produz metadados clock-free; nunca levanta exceção.</done>
</task>

<task type="auto">
  <name>Task 2: Testes golden offline da ingestão intraday</name>
  <files>tests/test_ingest_intraday.py</files>
  <read_first>
    - tests/test_ingest_ohlc.py — INTEGRAL: docstring "offline/zero rede" (1-12), fixtures `_hist_com_split`/`_hist_sem_split` (19-58), classes-fake `_TkComOHLC`/`_TkVazio` (136-152), `_monkeypatch_yf` (154-162), estilo um-teste-uma-asserção (65-129). É o analog exact.
    - src/analista/ingest/intraday.py — o módulo criado na Task 1 (assinaturas de `coletar_intraday`, `FrameOHLC`, `_PERIODO_POR_TF`, constantes de motivo).
    - .planning/phases/12-ingest-o-intraday-timeframe/12-RESEARCH.md — §"Testes offline" (358-379: fixture `_TkIntraday` com última barra Volume=0 + exemplo `test_idx_ultima_fechada_clock_free`), §"Phase Requirements → Test Map" (444-452), §"Edges explícitas" (454-458).
  </read_first>
  <behavior>
    - Cobertura mínima (≥10 testes, ver <behavior> da Task 1 para a semântica de cada um):
      test_mapa_tf_period_interval, test_tf_invalido, test_ohlc_ajustado_reusa_split,
      test_tz_normaliza_naive, test_tz_idempotente_sao_paulo, test_idx_ultima_fechada_clock_free,
      test_atraso_min_injetavel, test_frame_curto_historico_insuficiente, test_disponivel_true_frame_curto,
      test_vazio_sem_dados, test_excecao_fetch_falhou.
    - Todos 100% offline (sem rede): monkeypatch de `intraday.prices._yf` e `intraday.time.sleep`.
  </behavior>
  <action>
    Criar `tests/test_ingest_intraday.py` espelhando `test_ingest_ohlc.py`. Componentes concretos:

    1. Docstring declarando "offline, zero rede"; imports `numpy`/`pandas`/`pytest` + `from analista.ingest import intraday`.

    2. Helper de monkeypatch espelhando `_monkeypatch_yf` (test_ingest_ohlc.py:154-162), mas patchando `intraday.prices._yf` (lambda → objeto com `Ticker` staticmethod retornando a classe-fake) e `intraday.time.sleep` (lambda no-op). Classe-fake `_TkIntraday` com método `.history(*a, **k)` que devolve um `pd.DataFrame` com colunas Open/High/Low/Close/Adj Close/Volume/Stock Splits/Dividends sobre `pd.date_range(..., freq="5min", tz="America/Sao_Paulo")`, última barra com `Volume=0` (barra viva). Variantes: `_TkVazio` (history → DataFrame vazio), `_TkExcecao` (history → raise), `_TkUmaBarra` (1 barra), `_TkNaiveUTC` (índice tz-naive).

    3. Os testes da lista do <behavior>. Pontos críticos:
       - `test_idx_ultima_fechada_clock_free`: usar `agora` injetável fixo (ex.: `pd.Timestamp("2026-06-29 10:40", tz="America/Sao_Paulo")`) e asserir `f.idx_ultima_fechada == len(f.ohlc)-2` e `f.atraso_min == pytest.approx(15.0)` (Pitfall 3 — `agora` injetável é obrigatório p/ determinismo).
       - `test_tz_normaliza_naive`: fixture com índice naive → asserir `str(f.ohlc.index.tz)` contém "Sao_Paulo" (offset -03:00).
       - `test_ohlc_ajustado_reusa_split`: asserir que a ponta recente de `ohlc_ajustado` coincide com `ohlc` nominal (espelha `test_ponta_recente_coincide_com_nominal` de test_ingest_ohlc.py).
       - `test_vazio_sem_dados` / `test_excecao_fetch_falhou` / `test_tf_invalido`: asserir `disponivel is False` + `motivo` exato, e que NÃO levanta exceção.

    NÃO usar rede em nenhum teste. NÃO depender de `pd.Timestamp.now()` real (sempre injetar `agora`).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_ingest_intraday.py -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_ingest_intraday.py -q` sai com código 0 e coleta >= 10 testes.
    - `.venv/bin/python -m pytest -q` → suíte completa verde com >= 201 testes (191 golden + >=10 novos); nenhum dos 191 golden regrediu.
    - `tests/test_ingest_intraday.py` contém `def test_idx_ultima_fechada_clock_free` e `def test_tz_normaliza_naive`.
    - `grep -c '_yf' tests/test_ingest_intraday.py` >= 1 (monkeypatch offline presente).
    - `grep -c 'Volume.*0\|Volume=0\|"Volume"' tests/test_ingest_intraday.py` >= 1 (fixture de barra viva).
  </acceptance_criteria>
  <done>A suíte offline cobre as edges (barra viva clock-free, matriz period×interval, timezone naive+SP, frame curto, vazio/exceção/tf inválido) e os 191 golden seguem verdes ao lado dos novos testes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → engine (`coletar_intraday`) | `ticker` e `timeframe` são input do usuário (via Fase 16); cruzam para a borda de dados |
| engine → Yahoo Finance | chamada de rede a `query*.finance.yahoo.com` via yfinance (host fixo no yfinance, não controlado pelo input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `coletar_intraday(ticker, timeframe)` input | mitigate | `timeframe` validado contra o conjunto fechado `_PERIODO_POR_TF` → `FrameOHLC(disponivel=False, motivo="timeframe_invalido")` sem exceção; `ticker` normalizado por `prices.yahoo_symbol` (upper/strip/`.SA`). `period` cravado na tabela, NUNCA derivado do input (Pitfall 1) |
| T-12-03 | Denial of Service | retry de fetch contra rate-limit/IP ban da Yahoo | mitigate | retry com backoff **limitado** (`_MAX_TENTATIVAS=3`, `_BACKOFF_SEG`); sem retry infinito; falha → `disponivel=False` (degradação graciosa, D-06) |
| T-12-06 | Information Disclosure | dados retornados pela engine | accept | apenas dados públicos de mercado (custo zero); sem PII/secrets/auth/persistência nesta camada |
| T-12-07 | (SSRF) Spoofing/host | destino do fetch | accept | host `finance.yahoo.com` é fixo dentro do yfinance; o `ticker` não controla o domínio — sem superfície de SSRF |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → suíte completa verde (191 golden + novos), nenhum golden regrediu.
- `intraday.py` não importa `streamlit` (engine offline) e não toca `build.py`/`coletar_mercado` (firewall fundamentalista intacto).
- `_PERIODO_POR_TF` cravado nos tetos `[VERIFIED]`; `coletar_intraday` nunca levanta exceção (todas as edges retornam `FrameOHLC` com `motivo` categorizado).
</verification>

<success_criteria>
- `coletar_intraday(ticker, timeframe)` entrega OHLCV para diário/1h/30m/5m via tabela parametrizada, isolada do pipeline diário.
- Timestamps normalizados para America/Sao_Paulo de forma idempotente e imune ao TZ do processo.
- Barra viva identificável (`barra_viva`, `idx_ultima_fechada=len-2`); cálculos da Fase 13 podem usar `iloc[-2]`.
- `ohlc` nominal + `ohlc_ajustado` (split-only via reuso) ambos presentes.
- Falha/vazio/tf inválido → `disponivel=False` + `motivo` categorizado, nunca exceção.
- 191 golden verdes + >=10 novos testes de edge verdes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ingest-o-intraday-timeframe/12-01-SUMMARY.md`
</output>
