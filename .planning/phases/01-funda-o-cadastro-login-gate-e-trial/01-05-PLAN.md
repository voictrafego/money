---
phase: 01-funda-o-cadastro-login-gate-e-trial
plan: 05
type: execute
wave: 4
depends_on: [4]
files_modified:
  - "app.py"
  - "requirements.txt"
  - "stack.yml"
autonomous: true
requirements: [AUTH-03, AUTH-02]
must_haves:
  truths:
    - "O app.py lê a identidade injetada pelo gate via st.context.headers['X-User-Email'] no boot"
    - "O requirements.txt exige streamlit >= 1.37 (mínimo para st.context.headers)"
    - "Os 338 testes golden do engine continuam verdes após o bump do Streamlit"
    - "O stack.yml declara o middleware Traefik forwardAuth apontando para o gate e para de publicar a porta 8501 diretamente"
  artifacts:
    - path: "app.py"
      provides: "Leitura read-only de X-User-Email (personalização/telemetria; NUNCA autoriza acesso)"
      contains: "st.context.headers"
    - path: "requirements.txt"
      provides: "streamlit>=1.37"
      contains: "streamlit>=1.37"
    - path: "stack.yml"
      provides: "middleware forwardAuth + authResponseHeaders X-User-Email + Streamlit sem porta pública"
      contains: "forwardauth"
  key_links:
    - from: "stack.yml router money middleware"
      to: "GateView do lazari-capital (/gate/)"
      via: "forwardauth.address"
      pattern: "forwardauth.address"
    - from: "app.py boot"
      to: "header X-User-Email injetado pelo Traefik"
      via: "st.context.headers.get"
      pattern: "X-User-Email"
---

<objective>
Fechar a propagação de identidade (AUTH-03) do lado do Streamlit (repo `analista_dividendos`, ESTE
repo): (1) subir o pino `streamlit>=1.37` (mínimo para `st.context.headers`, sem fallback abaixo);
(2) ler `X-User-Email` no boot do `app.py` de forma **read-only** (personalização/telemetria — NUNCA
para autorizar; o gate já garantiu o acesso); (3) escrever no `stack.yml` o **middleware Traefik
forwardAuth** apontando para a `GateView` (`/gate/`) com `authResponseHeaders=X-User-Email`, e tirar
o Streamlit do acesso público direto (sem porta 8501 publicada — só rede interna do Swarm, D-11).

Purpose: AUTH-03 (identidade confiável no app) e o fechamento de AUTH-02 no lado da infra (Streamlit
só alcançável via gate). O deploy/efetivação na VPS + E2E + websockets é escopo da Fase 3 — aqui os
patterns são escritos e o header-read é validado em isolamento (RESEARCH Pattern 2, Code Examples,
Pitfall 1).
Output: app.py lê o header, requirements bumpado, 338 testes verdes, stack.yml com forwardAuth.

REPO ALVO: `analista_dividendos` (ESTE repo — `/Users/giovanelazari/projects/Analista de
Investimentos/analista_dividendos`). NÃO toca o `lazari-capital`. Depende do Plano 04 apenas pelo
CONTRATO da rota `/gate/` (string) e do header `X-User-Email` — nenhum arquivo compartilhado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-funda-o-cadastro-login-gate-e-trial/01-CONTEXT.md
@.planning/phases/01-funda-o-cadastro-login-gate-e-trial/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Contrato vindo do Plano 04 (lazari-capital) — apenas strings, sem arquivo compartilhado. -->
GateView (lazari-capital): endpoint GET `/gate/`; em 200 responde header `X-User-Email: <email>`;
  em bloqueio responde 302. O serviço Django é alcançável como `web:8000` na rede overlay Swarm.
Streamlit st.context (RESEARCH Code Examples): `st.context.headers.get("X-User-Email")` —
  case-insensitive, read-only, reflete os headers da REQUISIÇÃO INICIAL da sessão; requer
  streamlit >= 1.37; fora do gate (dev local) retorna None → tratar como anônimo/dev.
app.py atual (ESTE repo): `st.set_page_config(...)` na linha ~33; usa `st.session_state` por sessão.
requirements.txt atual: linha `streamlit>=1.30` → subir para `streamlit>=1.37`.
stack.yml atual: service `money`, labels router Host(money.voictech.com.br), publica
  loadbalancer.server.port=8501, rede network_swarm_public.
Traefik forwardAuth labels (RESEARCH Pattern 2):
  traefik.http.middlewares.lazari-gate.forwardauth.address=http://web:8000/gate/
  traefik.http.middlewares.lazari-gate.forwardauth.authResponseHeaders=X-User-Email
  traefik.http.middlewares.lazari-gate.forwardauth.trustForwardHeader=true
  traefik.http.routers.money.middlewares=lazari-gate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump streamlit>=1.37 + leitura read-only de X-User-Email no boot do app.py</name>
  <files>app.py; requirements.txt</files>
  <read_first>
    - app.py (linhas 1-40: imports + st.set_page_config na ~33 — o ponto de inserção do boot)
    - requirements.txt (linha `streamlit>=1.30`)
    - .planning/phases/01-funda-o-cadastro-login-gate-e-trial/01-RESEARCH.md § Code Examples ("Ler X-User-Email no boot do Streamlit") e § Environment Availability (bump obrigatório, sem fallback)
    - CLAUDE.md (testes golden em tests/ devem continuar passando)
  </read_first>
  <action>
    Em `requirements.txt`, trocar `streamlit>=1.30` por `streamlit>=1.37`. Em `app.py`, logo após
    `st.set_page_config(...)` (linha ~33), adicionar uma função `_current_user_email() -> str | None`
    que faz `return st.context.headers.get("X-User-Email")` dentro de try/except (retornando None em
    qualquer erro, ex.: dev local sem Traefik) e atribuir `user_email = _current_user_email()`.
    Documentar no docstring/comentário: é read-only, injetado pelo gate (AUTH-03); NUNCA usar para
    autorizar acesso (o gate já garantiu quem chega aqui) — só personalização/telemetria/"logado como".
    NÃO alterar nenhuma lógica de dados/engine. Instalar o streamlit atualizado no `.venv` do projeto
    para poder rodar os testes na Task 2.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && grep -q "streamlit>=1.37" requirements.txt && grep -q "st.context.headers" app.py && python -c "import ast; ast.parse(open('app.py').read())" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contém `streamlit>=1.37` e não contém mais `streamlit>=1.30`
    - `app.py` contém `st.context.headers` e uma função que lê `X-User-Email` com fallback None
    - `app.py` faz `ast.parse` sem SyntaxError
    - Um comentário deixa claro que o header é read-only e NÃO autoriza acesso
  </acceptance_criteria>
  <done>Streamlit bumpado para >=1.37 e app.py lê X-User-Email de forma read-only no boot.</done>
</task>

<task type="auto">
  <name>Task 2: Rodar a suíte golden (338 testes) após o bump e garantir verde</name>
  <files>(nenhum arquivo novo — validação; correções pontuais só se o bump quebrar API)</files>
  <read_first>
    - CLAUDE.md (338 testes golden; compatibilidade obrigatória)
    - .planning/phases/01-funda-o-cadastro-login-gate-e-trial/01-RESEARCH.md § Assumptions A7 (bump de minor pode ter breaking change — validar rodando o app/testes)
    - tests/ (estrutura da suíte pytest do engine)
  </read_first>
  <action>
    Rodar `pytest` na suíte do engine com o Streamlit já atualizado. Se algum teste quebrar por
    causa do bump (mudança de API do Streamlit entre 1.30 e 1.37), aplicar a correção mínima
    necessária no ponto exato do uso (sem tocar as fórmulas do engine — CLAUDE.md). Se nada quebrar,
    apenas registrar o resultado. Fazer um smoke `import`/parse do `app.py` para garantir que a nova
    referência a `st.context` não quebra o boot em versão >=1.37.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest` termina com 0 failures (a suíte golden — ~338 testes — continua verde após o bump)
    - Nenhuma fórmula do engine (src/analista/**) foi alterada (grep/diff confirma que mudanças, se houver, são só de compatibilidade de API Streamlit)
  </acceptance_criteria>
  <done>Suíte golden do engine verde com streamlit>=1.37; compatibilidade preservada.</done>
</task>

<task type="auto">
  <name>Task 3: stack.yml — middleware Traefik forwardAuth + Streamlit sem porta pública</name>
  <files>stack.yml</files>
  <read_first>
    - stack.yml (labels atuais do service money: router Host, loadbalancer.server.port=8501)
    - .planning/phases/01-funda-o-cadastro-login-gate-e-trial/01-RESEARCH.md § Pattern 2 (labels forwardAuth) e § Pitfall 1 (Streamlit acessível fora do Traefik = spoof)
  </read_first>
  <action>
    No `stack.yml`, no bloco `deploy.labels` do service `money`, adicionar as labels do middleware
    forwardAuth (RESEARCH Pattern 2): `traefik.http.middlewares.lazari-gate.forwardauth.address=
    http://web:8000/gate/`, `...forwardauth.authResponseHeaders=X-User-Email`,
    `...forwardauth.trustForwardHeader=true`, e `traefik.http.routers.money.middlewares=lazari-gate`.
    Garantir que o `money` deixa de ser alcançável fora do Traefik: a porta publicada
    `loadbalancer.server.port=8501` continua sendo o alvo interno do router, mas o serviço NÃO deve
    expor porta pública direta — documentar com comentário que o único ingresso é o Traefik + gate
    (D-11, Pitfall 1) e que Django (`web`) e `money` precisam compartilhar a rede overlay para o
    `forwardauth.address` alcançar `web:8000` (RESEARCH A3). NÃO fazer deploy (efetivação é Fase 3);
    este é o pattern versionado. Deixar claro no comentário que o domínio/host real e a validação de
    websockets ficam para a Fase 3 (OPS-01).
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && grep -q "forwardauth.address" stack.yml && grep -q "authResponseHeaders=X-User-Email" stack.yml && grep -q "routers.money.middlewares=lazari-gate" stack.yml && ! grep -qE '^[[:space:]]*ports:' stack.yml && python -c "import yaml; yaml.safe_load(open('stack.yml'))" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `stack.yml` contém as 4 labels: forwardauth.address (=http://web:8000/gate/), authResponseHeaders=X-User-Email, trustForwardHeader=true, routers.money.middlewares=lazari-gate
    - `stack.yml` é YAML válido (yaml.safe_load não levanta)
    - Um comentário documenta D-11 (Streamlit só via Traefik/gate), a necessidade de rede overlay compartilhada, e que deploy/websockets são Fase 3
    - `stack.yml` NÃO (re)introduz um bloco `ports:` de publicação de host: `! grep -qE '^[[:space:]]*ports:' stack.yml` (a label interna `loadbalancer.server.port=8501` permanece intacta como alvo do router Traefik, não como porta pública)
  </acceptance_criteria>
  <done>stack.yml declara o forwardAuth apontando ao gate e injetando X-User-Email; ingresso restrito ao Traefik documentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Traefik → Streamlit | O Streamlit confia em `X-User-Email` SÓ porque é inalcançável fora do Traefik/gate |
| Traefik (authResponseHeaders) → Streamlit | O header injetado pelo gate substitui qualquer X-User-Email de entrada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-23 | Spoofing | atacante seta `X-User-Email` na mão acessando o Streamlit direto | mitigate | stack.yml documenta Streamlit sem ingresso público (só rede overlay/Traefik, D-11); `authResponseHeaders` do gate substitui headers conflitantes de entrada (RESEARCH Pitfall 1) |
| T-01-24 | Elevation of Privilege | app.py usar X-User-Email para autorizar acesso | mitigate | `_current_user_email` é read-only e documentado como NÃO-autorizador; a decisão de acesso é 100% do gate (Plano 04); app é consumidor passivo |
| T-01-25 | Denial of Service | bump do Streamlit quebrar o engine (338 testes) | mitigate | Task 2 roda a suíte golden e exige 0 failures antes de aceitar o bump; correção só de compatibilidade, sem tocar fórmulas |
| T-01-26 | Information Disclosure | header None em dev vazar exceção/stack | mitigate | leitura em try/except retornando None fora do gate; nenhum dado sensível além do e-mail (já em trânsito autenticado) |
</threat_model>

<verification>
- `grep streamlit>=1.37 requirements.txt`; `grep st.context.headers app.py`.
- `pytest` do engine verde (~338 testes).
- `stack.yml` com as 4 labels forwardAuth e YAML válido.
</verification>

<success_criteria>
A identidade do usuário é propagada ao Streamlit via `X-User-Email` confiável (lido em `app.py` com
streamlit>=1.37) e o Streamlit só é alcançável via Traefik+gate (stack.yml) — cobrindo AUTH-03 e o
fechamento de infra de AUTH-02. Os 338 testes golden seguem verdes. Deploy/E2E/websockets = Fase 3.
</success_criteria>

<output>
Após completar, criar `.planning/phases/01-funda-o-cadastro-login-gate-e-trial/01-05-SUMMARY.md`
</output>
