---
phase: 01-funda-o-cadastro-login-gate-e-trial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/projects/lazari-capital/ (novo repo, fork de crm-voic)"
  - "~/projects/lazari-capital/config/urls.py"
  - "~/projects/lazari-capital/config/settings/base.py"
  - "~/projects/lazari-capital/apps/users/models.py"
  - "~/projects/lazari-capital/apps/users/views.py"
  - "~/projects/lazari-capital/apps/users/urls.py"
  - "~/projects/lazari-capital/apps/users/forms.py"
  - "~/projects/lazari-capital/apps/billing/services.py"
  - "~/projects/lazari-capital/apps/core/context_processors.py"
autonomous: true
requirements: [ACCT-01]
must_haves:
  truths:
    - "O projeto Django lazari-capital sobe com `manage.py check` sem erros de import"
    - "`makemigrations` + `migrate` rodam limpo num DB sqlite novo e vazio"
    - "`pytest` passa (com os testes de leads/dashboard/integrations removidos)"
    - "Cada User está ligado 1:1 a uma Conta via FK (isolamento de conta = fundação do multiusuário)"
  artifacts:
    - path: "~/projects/lazari-capital/apps/accounts/models.py"
      provides: "Conta com status/trial_ate/asaas_customer_id/grace_ate/plano (fonte de verdade do gate)"
      contains: "class Conta"
    - path: "~/projects/lazari-capital/apps/users/models.py"
      provides: "User(AbstractUser) com email USERNAME_FIELD, sem username, sem Papel B2B"
      contains: "USERNAME_FIELD = 'email'"
    - path: "~/projects/lazari-capital/config/urls.py"
      provides: "URLConf sem includes de leads/dashboard/integrations"
    - path: "~/projects/lazari-capital/.git"
      provides: "Repo git próprio novo (git init limpo, sem histórico do crm-voic)"
  key_links:
    - from: "apps/users/models.py User"
      to: "apps/accounts/models.py Conta"
      via: "FK conta (related_name=usuarios)"
      pattern: "conta = models.ForeignKey"
---

<objective>
Erguer o esqueleto do projeto Django `~/projects/lazari-capital` (marca Lazari Capital) por
**fork-and-prune** do `crm-voic`: copiar o repo, `git init` limpo, remover o que é B2B/CRM
(apps `leads`/`dashboard`/`integrations` + papéis corretor/gerente do `User`), cortar o
acoplamento que quebra o signup (`provisionar_pipeline_padrao`), limpar `config/urls.py` e
`settings/base.py`, resetar migrations e deixar `manage.py check` + `migrate` + `pytest`
verdes num DB novo e vazio.

Purpose: fundação de TODAS as demais tarefas da fase. Sem um projeto que sobe verde, cadastro,
login, reset e gate não têm onde existir. Estabelece o `User→Conta` 1:1 (ACCT-01).
Output: repo `lazari-capital` que sobe (`runserver`) e passa a suíte, com `accounts`/`users`/
`billing`/`webhooks`/`core` intactos e os 3 apps B2B removidos.

REPO ALVO: `~/projects/lazari-capital` (NOVO — não existe ainda; é criado nesta tarefa por cópia
do `/Users/giovanelazari/projects/crm-voic`). Este plano NÃO toca o repo `analista_dividendos`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-cadastro-login-gate-e-trial/01-CONTEXT.md
@.planning/phases/01-funda-o-cadastro-login-gate-e-trial/01-RESEARCH.md

<interfaces>
<!-- Fonte-de-verdade do fork. O executor lê diretamente estes arquivos do crm-voic. -->
crm-voic Conta (accounts/models.py): Status = {pendente_pagamento, ativo, inadimplente, suspenso,
  cancelado}; campos status (default PENDENTE_PAGAMENTO, db_index), plano FK (nullable),
  grace_ate (DateTimeField), trial_ate (DateField), asaas_customer_id, cupom_primeiro_ciclo_pendente,
  onboarding_concluido.
crm-voic User (users/models.py): AbstractUser; username=None; email unique USERNAME_FIELD;
  conta FK (related_name=usuarios); Papel {admin,gerente,corretor} + helpers is_gerente/is_corretor/
  is_admin_conta/can_manage_users + deve_trocar_senha. >>> Papel e helpers B2B: REMOVER.
crm-voic config/urls.py includes: leads/pipeline/campos/tags/dashboard/integracoes >>> REMOVER;
  MANTER admin, health, billing-signup, apps.users.urls, billing/, conta/(accounts), webhooks/.
crm-voic billing/services.py provisionar_signup: importa `from apps.leads.services import
  provisionar_pipeline_padrao` e o chama >>> CORTAR import + chamada (única dependência de leads
  no billing). O resto de provisionar_signup é reescrito no Plano 02 — aqui só torná-lo importável.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copiar crm-voic → lazari-capital, git init limpo, mapear acoplamentos e remover apps B2B</name>
  <files>~/projects/lazari-capital/ (repo novo); apps/leads/, apps/dashboard/, apps/integrations/ (deletados); apps/*/migrations/0*.py (resetados)</files>
  <read_first>
    - /Users/giovanelazari/projects/crm-voic/.gitignore (o que já é ignorado)
    - .planning/phases/01-funda-o-cadastro-login-gate-e-trial/01-RESEARCH.md § "Fork-and-prune Sequencing (D-04/D-05)" — a ordem segura verificada
    - /Users/giovanelazari/projects/crm-voic/config/urls.py (os 6 includes B2B a remover)
  </read_first>
  <action>
    Copiar `/Users/giovanelazari/projects/crm-voic` inteiro para `~/projects/lazari-capital`
    EXCLUINDO `.git/`, `.venv/`, `db.sqlite3`, `staticfiles/`, `__pycache__/` (usar rsync com
    --exclude ou cp seguido de remoção desses artefatos). Rodar `git init` limpo no novo diretório
    (D-05: repo próprio, sem histórico do crm-voic). Confirmar que o `.gitignore` herdado cobre
    `.env`, `.venv`, `db.sqlite3`, `__pycache__`, `staticfiles`.
    Antes de remover qualquer app, rodar o grep de acoplamentos da seção de sequencing do RESEARCH
    (padrões: `apps.leads`, `apps.dashboard`, `apps.integrations`, `provisionar_pipeline_padrao`,
    `_leads_urlpatterns`, `_dashboard_urlpatterns`, `_integrations_urlpatterns`, `_campos_urlpatterns`,
    `_pipeline_urlpatterns`, `_tags_urlpatterns`, `avisos_limite`, `corretor`, `gerente`, `Papel.`)
    sobre `apps/` e `config/` e anotar todos os pontos que precisarão ser tocados na Task 2.
    Deletar as pastas `apps/leads/`, `apps/dashboard/`, `apps/integrations/` e seus `tests/`.
    Resetar migrations: apagar `apps/*/migrations/0*.py` (MANTER cada `__init__.py`) — DB é novo e
    vazio (D-05), então migração do zero é mais limpa que migration de remoção com FKs órfãs.
  </action>
  <verify>
    <automated>test -d ~/projects/lazari-capital/apps/accounts && test ! -d ~/projects/lazari-capital/apps/leads && test ! -d ~/projects/lazari-capital/apps/dashboard && test ! -d ~/projects/lazari-capital/apps/integrations && test -d ~/projects/lazari-capital/.git && test ! -d ~/projects/lazari-capital/.git/refs/remotes && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `~/projects/lazari-capital/.git` existe e NÃO contém histórico/remotes do crm-voic (git init novo)
    - `apps/leads/`, `apps/dashboard/`, `apps/integrations/` não existem em lazari-capital
    - `apps/accounts/`, `apps/users/`, `apps/billing/`, `apps/webhooks/`, `apps/core/` existem
    - Nenhum arquivo `apps/*/migrations/0*.py` presente; cada `apps/*/migrations/__init__.py` presente
    - Nenhum `db.sqlite3`, `.venv/`, `staticfiles/`, `__pycache__/` copiado
  </acceptance_criteria>
  <done>Repo lazari-capital criado por fork, git limpo, 3 apps B2B removidos, migrations resetadas.</done>
</task>

<task type="auto">
  <name>Task 2: Cortar todos os imports/refs órfãos para o projeto voltar a importar</name>
  <files>~/projects/lazari-capital/config/urls.py; config/settings/base.py; apps/users/models.py; apps/users/views.py; apps/users/urls.py; apps/users/forms.py; apps/billing/services.py; apps/core/context_processors.py</files>
  <read_first>
    - /Users/giovanelazari/projects/crm-voic/config/urls.py
    - /Users/giovanelazari/projects/crm-voic/config/settings/base.py (linhas de INSTALLED_APPS ~50-66 e context_processors ~103)
    - /Users/giovanelazari/projects/crm-voic/apps/users/models.py (Papel + helpers a remover)
    - /Users/giovanelazari/projects/crm-voic/apps/users/views.py (CreateUserView/UserListView/DeactivateUserView/AdminOuGerenteMixin dependem de papel)
    - /Users/giovanelazari/projects/crm-voic/apps/users/urls.py (_gestao_urlpatterns namespace usuarios)
    - /Users/giovanelazari/projects/crm-voic/apps/users/forms.py (ContaUserCreationForm usa papel)
    - /Users/giovanelazari/projects/crm-voic/apps/billing/services.py (import de provisionar_pipeline_padrao linha ~40 e chamada linha ~155)
    - /Users/giovanelazari/projects/crm-voic/apps/core/context_processors.py (avisos_limite)
  </read_first>
  <action>
    `config/urls.py`: remover os includes e imports de `_leads_urlpatterns`, `_pipeline_urlpatterns`,
    `_campos_urlpatterns`, `_tags_urlpatterns`, `_dashboard_urlpatterns`, `_integrations_urlpatterns`
    (6 paths + seus imports no topo). MANTER: admin/, health/, signup/ (SignupView, name="billing-signup"),
    include("apps.users.urls"), billing/, conta/(accounts namespace), webhooks/.
    `settings/base.py`: tirar `apps.leads`, `apps.dashboard`, `apps.integrations` de INSTALLED_APPS;
    remover `apps.core.context_processors.avisos_limite` da lista de context_processors dos TEMPLATES.
    `apps/core/context_processors.py`: remover a função `avisos_limite` (dependia de leads/assentos).
    `apps/users/models.py`: remover a inner class `Papel`, o campo `papel`, e os helpers
    `is_admin_conta`/`is_gerente`/`is_corretor`/`can_manage_users`. MANTER `email` USERNAME_FIELD,
    `username=None`, a FK `conta`, `deve_trocar_senha`, `objects = UserManager()`.
    `apps/users/views.py`: remover `CreateUserView`, `UserListView`, `DeactivateUserView` e o
    import de `AdminOuGerenteMixin` (gestão de equipe não existe no B2C). MANTER `TrocarSenhaView`,
    `DashboardPlaceholderView`, `LandingPageView`.
    `apps/users/urls.py`: remover `_gestao_urlpatterns` e o `path("usuarios/", ...)` namespace
    "usuarios", e os imports de CreateUserView/UserListView/DeactivateUserView. MANTER login/logout/
    password_change/password_change_done, dashboard, home.
    `apps/users/forms.py`: remover `ContaUserCreationForm` (usa papel/creator). MANTER `EmailAuthenticationForm`.
    `apps/billing/services.py`: cortar `from apps.leads.services import provisionar_pipeline_padrao`
    e a chamada `provisionar_pipeline_padrao(conta)` dentro de `provisionar_signup`. NÃO reescrever
    o resto de `provisionar_signup` agora (isso é o Plano 02) — apenas torná-lo importável/sem leads.
    Se remover o import de `AdminOuGerenteMixin` deixar `apps/core/mixins.py` referenciando papel,
    ajustar/remover o mixin conforme o grep da Task 1.
  </action>
  <verify>
    <automated>cd ~/projects/lazari-capital && python -c "import ast,glob,sys; [ast.parse(open(f).read()) for f in glob.glob('apps/**/*.py',recursive=True)+glob.glob('config/**/*.py',recursive=True)]" && ! grep -rn "apps.leads\|apps.dashboard\|apps.integrations\|provisionar_pipeline_padrao\|Papel\.\|avisos_limite" apps/ config/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "apps.leads\|apps.dashboard\|apps.integrations\|provisionar_pipeline_padrao" apps/ config/` retorna 0 linhas
    - `apps/users/models.py`: sem `class Papel`, sem campo `papel`; `USERNAME_FIELD = 'email'` e FK `conta` presentes
    - `apps/users/forms.py`: `EmailAuthenticationForm` presente; `ContaUserCreationForm` ausente
    - `config/urls.py`: nenhum import/include de leads/pipeline/campos/tags/dashboard/integracoes; billing-signup + apps.users.urls + billing + accounts + webhooks presentes
    - Todos os `.py` de `apps/` e `config/` fazem `ast.parse` sem SyntaxError
  </acceptance_criteria>
  <done>Projeto importa sem ModuleNotFoundError; nenhuma referência B2B órfã em apps/ ou config/.</done>
</task>

<task type="auto">
  <name>Task 3: Migrations do zero, check/migrate/pytest verdes, podar testes B2B e rebranding</name>
  <files>~/projects/lazari-capital/apps/*/migrations/ (novas 0001); conftest.py; apps/*/tests/; config/settings/base.py (nome do projeto); README/strings de marca</files>
  <read_first>
    - /Users/giovanelazari/projects/crm-voic/conftest.py e apps/*/conftest.py (factories de Conta/User)
    - /Users/giovanelazari/projects/crm-voic/apps/billing/tests/ e apps/users/tests/ (quais testes dependem de papel/leads/asaas)
    - /Users/giovanelazari/projects/crm-voic/apps/billing/tests/test_signup_form.py, test_signup_service.py, test_signup_view.py (asseram o signup B2B ANTIGO; o Plano 01-02 reescreve o signup — estes três devem ser aposentados aqui para o pytest do 01-01 ficar verde após a Task 2 cortar a chamada de pipeline)
    - .planning/phases/01-funda-o-cadastro-login-gate-e-trial/01-RESEARCH.md § sequencing passos 8-10
  </read_first>
  <action>
    Criar/ativar um `.venv` e instalar `requirements.txt` (idêntico ao crm-voic). Rodar
    `python manage.py makemigrations` (gera 0001 limpos para accounts/users/billing/webhooks/core).
    Rodar `python manage.py migrate` sobre um sqlite dev novo (settings dev). Rodar
    `python manage.py check`. Podar os testes que referenciam papéis B2B/leads que
    não existem mais (factory-boy factories que setam `papel`, testes de UserListView/CreateUserView,
    testes de provisionar_pipeline_padrao); ajustar as factories de `Conta`/`User` para não setar
    `papel`. APOSENTAR os três testes de signup herdados do crm-voic —
    `apps/billing/tests/test_signup_form.py`, `test_signup_service.py`, `test_signup_view.py` — que
    asseram o comportamento B2B antigo (CPF/telefone/plano; Asaas/Assinatura/TrialCpf/trial 30d;
    auto-login) e quebram assim que a Task 2 corta a chamada de pipeline: DELETAR os três agora (o
    fluxo B2C recebe cobertura nova em `test_signup_b2c.py` no Plano 01-02). Rodar `pytest` e deixar verde. Rebranding mínimo de identidade de projeto: strings
    `crm-voic`/`pocketleads`/`voictech` → `lazari-capital`/`Lazari Capital` em `settings/base.py`
    (nome do projeto, DEFAULT_FROM_EMAIL default), e qualquer `MARKETING_HOSTS`/hosts hardcoded.
    NÃO tocar templates de marca (D-13) — isso é dos Planos 02/03/04. Fazer o 1º commit no repo novo.
  </action>
  <verify>
    <automated>cd ~/projects/lazari-capital && python manage.py check && python manage.py makemigrations --check --dry-run && python manage.py migrate --run-syncdb && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py check` sai com código 0, sem erros
    - `makemigrations --check --dry-run` não pede migrations pendentes (0001 já geradas e aplicadas)
    - `python manage.py migrate` completa num DB sqlite novo sem erro de FK órfã
    - `pytest` termina com 0 failures (testes B2B + os 3 testes de signup legados removidos)
    - Os três testes de signup legados foram removidos: `test ! -f apps/billing/tests/test_signup_form.py && test ! -f apps/billing/tests/test_signup_service.py && test ! -f apps/billing/tests/test_signup_view.py`
    - `grep -rni "crm-voic\|pocketleads" config/settings/base.py` retorna 0 linhas (rebranding do projeto feito)
    - Existe pelo menos 1 commit no repo `~/projects/lazari-capital`
  </acceptance_criteria>
  <done>lazari-capital sobe verde: check/migrate/pytest passam num DB novo; identidade rebrandizada; 1º commit feito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crm-voic (fonte) → lazari-capital (fork) | Código B2B com acoplamentos (leads/papéis) que, se sobreviverem ao prune, viram superfície morta ou quebram o boot |
| Fork → repo git novo | Segredos/artefatos do crm-voic não podem vazar para o novo histórico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | fork copiando `.env`/`db.sqlite3`/`.git` do crm-voic | mitigate | Task 1 exclui `.git`, `.venv`, `db.sqlite3`, `staticfiles`, `__pycache__` na cópia; `git init` limpo; `.gitignore` herdado cobre `.env` |
| T-01-02 | Elevation of Privilege | resíduo de papel `corretor/gerente`/`can_manage_users` sobrevivendo ao prune | mitigate | Task 2 remove `Papel`, campo `papel`, helpers e as views de gestão de usuário; grep gate confirma 0 refs |
| T-01-03 | Tampering | migrations legadas com FK a apps removidos deixando o schema inconsistente | mitigate | Task 3 reseta migrations e regenera 0001 num DB novo; `makemigrations --check` prova ausência de drift |
| T-01-04 | Denial of Service | boot quebrado por import órfão (`ModuleNotFoundError: apps.leads`) | mitigate | Task 2 corta todos os imports B2B; `manage.py check` verde é critério de aceite |
</threat_model>

<verification>
- `python manage.py check` sem erros; `migrate` limpo em DB novo; `pytest` verde.
- `grep -rn "apps.leads\|apps.dashboard\|apps.integrations\|provisionar_pipeline_padrao\|Papel\."` em apps/config = 0.
- `runserver` sobe e a rota `/entrar/` (login) e `/signup/` respondem (smoke manual opcional).
</verification>

<success_criteria>
Repo `~/projects/lazari-capital` existe como git próprio, sobe verde (check/migrate/pytest),
mantém `accounts`/`users`/`billing`/`webhooks`/`core`, removeu os 3 apps B2B e os papéis, e
preserva o `User→Conta` 1:1 que sustenta o multiusuário isolado (ACCT-01).
</success_criteria>

<output>
Após completar, criar `.planning/phases/01-funda-o-cadastro-login-gate-e-trial/01-01-SUMMARY.md`
</output>
