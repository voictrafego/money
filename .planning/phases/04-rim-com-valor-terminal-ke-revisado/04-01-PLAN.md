---
phase: 04-rim-com-valor-terminal-ke-revisado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_motores.py
  - tests/test_vulc3_regressao.py
  - src/analista/core/motores.py
  - config.yaml
  - src/analista/report/report.py
autonomous: true
requirements: [CAL-01, CAL-02]

must_haves:
  truths:
    - "ITUB4 (inputs live VPA≈19, ROE≈19,3%, roteado para RIM) produz intrínseco na faixa R$32–40, não os ~R$23 atuais"
    - "O valor terminal do RIM é parametrizado em config.yaml (excesso_sustentavel, g_terminal, ke_g_spread_min) — sem constante mágica no código"
    - "Ke do RIM revisado: ke_teto 0.14→0.13 (CAL-02), documentado, sem intrínseco explosivo (clamps sãos)"
    - "Um banco com ROE < Ke valua ABAIXO do book (guarda anti-bad-bank fade_para = ke + min(roe0−ke, cap))"
    - "Suíte completa verde: test_ddm intacto, TAEE11 idêntica (rota DDM), firewall selo↛report intacto"
  artifacts:
    - path: "src/analista/core/motores.py"
      provides: "rim() com valor terminal (perpetuidade de Gordon sobre RI terminal) + ResultadoRIM.vp_terminal"
      contains: "vp_terminal"
    - path: "config.yaml"
      provides: "knobs motores.rim.excesso_sustentavel / g_terminal / ke_g_spread_min + ke_teto revisado"
      contains: "excesso_sustentavel"
    - path: "src/analista/report/report.py"
      provides: "dispatch _intrinseco_motor passa os novos knobs para motores.rim"
      contains: "excesso_sustentavel"
    - path: "tests/test_motores.py"
      provides: "golden atualizado + gate duro ITUB4 R$32-40 + teste anti-bad-bank"
      contains: "32.0 <= res.valor_intrinseco <= 40.0"
  key_links:
    - from: "src/analista/core/motores.py::rim"
      to: "src/analista/core/ddm.py::valor_gordon"
      via: "perpetuidade do RI terminal"
      pattern: "ddm\\.valor_gordon"
    - from: "src/analista/report/report.py::_intrinseco_motor"
      to: "motores.rim"
      via: "passa excesso_sustentavel/g_terminal lidos de cfg"
      pattern: "excesso_sustentavel"
---

<objective>
Dar ao motor RIM um **valor terminal** (perpetuidade de Gordon sobre o Residual Income terminal),
substituindo a estrutura fade-para-Ke-sem-terminal (D-02) que ancora bancos de qualidade no VPA.
Com o híbrido recomendado — fade parcial a um excesso sustentável limitado + perpetuidade Gordon —
o ITUB4 sai de R$23 para ~R$32,9 (CAL-01). O Ke do RIM é revisado como ajuste fino secundário
(ke_teto 0.14→0.13, ~R$2, CAL-02), aplicado por cima do terminal.

Purpose: O v2.2 falhou porque a verificação só checava "não é Evitar" e nunca cobrou o NÚMERO.
Esta fase crava a faixa R$32–40 como **gate duro** de teste.

Output: rim() com terminal parametrizado, knobs em config.yaml, dispatch atualizado, golden
reescrito e um teste-gate que assere a faixa-alvo. Mudança cirúrgica, motor puro/never-raise.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rim-com-valor-terminal-ke-revisado/04-RESEARCH.md

<interfaces>
<!-- Contratos que o executor precisa. Extraído do código — não explorar. -->

Assinatura ATUAL a modificar — src/analista/core/motores.py:60:
```python
def rim(vpa0, roe0, ke, retencao, n, fade_para=None) -> Optional[ResultadoRIM]:
    # never-raise: None in inputs / n<=0 / ke<=0 / vpa0<=0 -> None
    # janela t=1..n: frac=(t-1)/(n-1); roe_t = roe0 + (fade_para-roe0)*frac
    #                ri=(roe_t-ke)*b_prev; vp += ri/(1+ke)**t; b=b*(1+roe_t*ret)
    # ResultadoRIM(valor_intrinseco=vpa0+vp, vpa_base, vp_residual_income, ri_por_ano)
```

Primitiva a REUSAR (NÃO reimplementar) — src/analista/core/ddm.py:37:
```python
def valor_gordon(dpa1, ke, g) -> Number:  # = dpa1/(ke-g); None se ke-g<=0 ou input None
```

ke_rim (CAL-02 mexe nos KNOBS, não na função) — src/analista/core/motores.py:98:
```python
def ke_rim(beta, cfg) -> Number:
    # rf=cfg.capm.rf_local(0.105); ke = rf + beta*rim_cfg.erp_banco(0.045)
    # clamp: max(ke_piso, min(ke, ke_teto)); depois min(ke_clamp, ke_live)
```

Dispatch a estender — src/analista/report/report.py:202-210 (só o ramo motor=="rim"):
```python
res_rim = motores.rim(vpa0=lentes.vpa(...), roe0=c.roe_valuation(),
    ke=motores.ke_rim(c.beta, cfg), retencao=(1.0-(c.payout_valuation() or 0.0)),
    n=mot_cfg.get("rim", {}).get("n_fade", 10))
```
</interfaces>

<verified_numbers>
<!-- Recipe verificada localmente (excesso_sustentavel=0.045, g_terminal=0.025) — bandas do gate. -->
- LIVE ITUB4 (vpa0=19.0, roe0=0.193, ke=0.13, ret=0.533, n=10) -> R$32,87 (terminal 17,4%). Gate: 32–40.
- GOLDEN fixo (vpa0=22.0, roe0=0.193, ke=0.125, ret=0.53, n=10) -> R$39,23, vp_terminal>0. Banda: 36–42.
- BAD BANK (vpa0=22.0, roe0=0.10 < ke=0.125) -> R$15,54 (P/B≈0,71 < 1) com fade_para = ke + min(roe0−ke, cap) SEM clampar a ≥ ke.
- ROE==Ke (vpa0=22.0, roe0=ke=0.125) -> exatamente 22,0 (RI=0 em toda a janela -> terminal=0). Invariante preservada.
- ke_rim(beta=1.29, cfg com ke_teto=0.13) = 0.13 (clamp), abaixo do ke_live=0.1824.
</verified_numbers>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): reescrever golden + gate duro ITUB4 (unit + integração) + teste anti-bad-bank</name>
  <files>tests/test_motores.py, tests/test_vulc3_regressao.py</files>
  <behavior>
    - test_rim_itub4_honesto_maior_que_ddm (REESCRITO): rim(vpa0=22.0, roe0=0.193, ke=0.125, retencao=0.53, n=10, excesso_sustentavel=0.045, g_terminal=0.025) -> 36.0 <= valor_intrinseco <= 42.0; res.vp_terminal > 0; res.ri_por_ano[-1] > 0 (RI terminal agora POSITIVO — não mais ≈0). Remover a assertiva antiga `abs(res.ri_por_ano[-1]) < 0.05`. Manter res.vpa_base == 22.0.
    - test_rim_itub4_live_alvo_32_40 (NOVO — GATE DURO CAL-01/CAL-02, nível UNIT): lê cfg via _cfg(); ke = motores.ke_rim(1.29, cfg) e assere abs(ke - 0.13) < 1e-9 (prova ke_teto revisado); rc = cfg["motores"]["rim"]; res = motores.rim(vpa0=19.0, roe0=0.193, ke=ke, retencao=0.533, n=rc["n_fade"], excesso_sustentavel=rc["excesso_sustentavel"], g_terminal=rc["g_terminal"]); assere 32.0 <= res.valor_intrinseco <= 40.0 e res.vp_terminal > 0. (Lê os knobs de config -> prova parametrização, zero constante mágica.)
    - test_rim_itub4_dispatch_banda (NOVO — GATE DE INTEGRAÇÃO via report.py, fecha o key_link CAL-01): em tests/test_vulc3_regressao.py, reforçar o capstone existente `test_capstone_itub4_sem_evitar_motor_rim_ddm_como_lente` (ou adicionar teste irmão) que roda o caminho de DISPATCH — report._intrinseco_motor(motor="rim", ...) ou analisar_acao com insumos ITUB4-like — e assere que o intrínseco do motor cai MATERIALMENTE acima do teto D-02 antigo (ex.: > 30.0, não só > 0). Motivo: o gate unit acima chama motores.rim() direto; se o dispatch em report.py ler um knob com key errada, ele degrada silenciosamente para ~R$23 e NENHUM teste hoje pega (o capstone só checa > 0). Este teste prova que os knobs chegam ao funil.
    - test_rim_bad_bank_abaixo_do_book (NOVO — guarda anti-bad-bank): rim(vpa0=22.0, roe0=0.10, ke=0.125, retencao=0.53, n=10, excesso_sustentavel=0.045, g_terminal=0.025) -> valor_intrinseco < 22.0 (P/B < 1); res.vp_terminal <= 0.
    - test_rim_roe_igual_ke_ancora_no_vpa (INALTERADO — reconfirmar): continua verde com os defaults novos (RI=0 -> terminal=0 -> V=VPA).
    - test_rim_never_raise (INALTERADO): reconfirmar.
  </behavior>
  <action>Reescrever a seção RIM de tests/test_motores.py conforme o bloco behavior, usando exatamente os inputs e bandas de verified_numbers. Atualizar o docstring do módulo (linhas 1-8) trocando a narrativa "~R$28, sem prêmio terminal, D-02" pela nova: "RIM híbrido com valor terminal (perpetuidade de Gordon), ITUB4 ~R$32,9, terminal ~17%". Este é o passo RED: rodar agora DEVE falhar (o código ainda não tem terminal nem o arg vp_terminal) — é o esperado.</action>
  <verify>
    <automated>cd "$PWD" && python -m pytest tests/test_motores.py -k "rim" -x 2>&1 | tail -20 ; echo "RED esperado: TypeError (arg novo) ou AssertionError de banda"</automated>
  </verify>
  <done>Testes reescritos/adicionados; a suíte RIM falha por ausência do terminal (RED confirmado). Commit: test(04): golden RIM com valor terminal + gate duro ITUB4 R$32-40.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): valor terminal no motor + knobs em config + dispatch</name>
  <files>config.yaml, src/analista/core/motores.py, src/analista/report/report.py</files>
  <behavior>
    - motores.rim(vpa0=19.0, roe0=0.193, ke=0.13, ret=0.533, n=10, excesso_sustentavel=0.045, g_terminal=0.025) -> valor_intrinseco ≈ 32,87 e vp_terminal ≈ 5,73.
    - Chamada legada rim(vpa0, roe0, ke, retencao, n) SEM os novos args -> comportamento D-02 idêntico (excesso_sustentavel=0.0 e g_terminal=None reproduzem fade-a-Ke sem terminal). vp_terminal == 0.
    - Bad bank roe0<ke -> fade_para < ke, RI terminal negativo, valor < book, vp_terminal <= 0.
  </behavior>
  <action>
1. config.yaml (bloco motores.rim, ~linhas 230-239): trocar ke_teto: 0.14 -> ke_teto: 0.13 e adicionar comentário WHY (CAL-02: banco large-cap/líquido com beta ajustado Blume tem Ke estrutural ~13%, não 14%; Damodaran mature ERP 4,23% + a Selic-ciclo já embute risco-país -> não somar country risk de novo; move ITUB4 ~R$2). Adicionar três knobs NOVOS com comentário do porquê: excesso_sustentavel: 0.045 (cap do excesso de ROE sobre Ke que persiste na perpetuidade — moat durável ~4,5pp; calibrável na cesta em BACKTEST-01/Fase 5), g_terminal: 0.025 (g do RI na perpetuidade; espelha ddm.g_estavel ≤ PIB — declarado localmente por independência do motor, NÃO acopla os blocos), ke_g_spread_min: 0.03 (piso de Ke−g_terminal para liberar o terminal; senão degrada para fade-only). NÃO tocar erp_banco (0.045) nem ke_piso (0.11) nem capm.* . **REESCREVER o comentário estagnado do `n_fade` (linhas ~236-239)**: hoje diz "Golden ITUB4 ... ancora ~R$28 ... NÃO ~R$40 ... Calibração empírica é BACKTEST-01 (deferida)" — factualmente errado após esta fase. Trocar por: o RIM híbrido com valor terminal leva o golden ITUB4 (VPA~22, ke~12,5%) a ~R$39 e o live (VPA~19, ke~13%) a ~R$32,9; a calibração dos knobs contra a cesta é a Fase 5 (BACKTEST-01), não mais "deferida".

2. src/analista/core/motores.py:
   - ResultadoRIM: adicionar campo vp_terminal: float = 0.0 (paridade com ResultadoDDM.vp_residual). peso_residual permanece calculado sobre valor_intrinseco.
   - rim(): nova assinatura backward-safe rim(vpa0, roe0, ke, retencao, n, excesso_sustentavel: float = 0.0, g_terminal: Optional[float] = None, ke_g_spread_min: float = 0.03, fade_para: Optional[float] = None). Guardas never-raise inalteradas. JANELA (t=1..n): quando fade_para não é passado explicitamente, fade_para = ke + min(roe0 - ke, excesso_sustentavel) — SEM clampar o resultado a ≥ ke (é a guarda anti-bad-bank: roe0<ke -> fade_para<ke -> RI terminal negativo -> valor abaixo do book, correto). Restante da janela idêntico (clean surplus). TERMINAL: após a janela, se g_terminal is not None E ke - g_terminal >= ke_g_spread_min, calcular RI_{n+1} = ris[-1]*(1+g_terminal), TV = ddm.valor_gordon(RI_{n+1}, ke, g_terminal) (REUSO — não reimplementar), vp_terminal = TV/(1+ke)**n (0.0 se TV None). Compor valor_intrinseco = vpa0 + vp + vp_terminal. Documentar no docstring o fundamento teórico (RIM multiestágio CFA L2 / Ohlson: janela explícita de residual income + continuing value; terminal cresce a g_terminal, NÃO à taxa do book — evita RIM explosivo) e por que o min(roe0−ke, cap) é a guarda anti-bad-bank. Comentário só onde o porquê não é óbvio (CLAUDE.md).

3. src/analista/report/report.py (só o ramo motor == "rim", ~linhas 202-210): passar os novos knobs lidos de cfg — excesso_sustentavel=mot_cfg.get("rim", {}).get("excesso_sustentavel", 0.0), g_terminal=mot_cfg.get("rim", {}).get("g_terminal"), ke_g_spread_min=mot_cfg.get("rim", {}).get("ke_g_spread_min", 0.03). NÃO alterar mais nada do funil (leitura defensiva, defaults reproduzem D-02 se o config for antigo).
  </action>
  <verify>
    <automated>cd "$PWD" && python -m pytest tests/test_motores.py -k "rim" -x 2>&1 | tail -15</automated>
  </verify>
  <done>Toda a suíte RIM verde (golden 36-42, gate duro 32-40, bad-bank menor que book, roe==ke==VPA, never-raise). Terminal via ddm.valor_gordon (grep confirma reuso). Commit: feat(04): valor terminal no RIM (perpetuidade Gordon) + ke_teto 0.13.</done>
</task>

<task type="auto">
  <name>Task 3 (invariantes): suíte completa verde + firewall/DDM/TAEE11 intactos</name>
  <files>src/analista/core/motores.py, config.yaml, src/analista/report/report.py, tests/test_motores.py</files>
  <action>Rodar a suíte completa e confirmar os invariantes HARD do SC#4 (nada quebrou fora do RIM). Se algo além de test_motores.py falhar, é regressão — corrigir na origem (motores.py/report.py), NÃO afrouxar o teste alheio. Especificamente confirmar: test_ddm (DDM puro, ddm.py intocado), test_vulc3_regressao (TAEE11 roteia para DDM, baseline idêntico ao v2.2), test_selo::test_firewall_selo_nao_importa_report (firewall intacto), test_arquetipo_roteamento. NÃO tocar ddm.py/selo.py/lentes.py.</action>
  <verify>
    <automated>cd "$PWD" && python -m pytest tests/ 2>&1 | tail -8 && echo "--- invariantes chave ---" && python -m pytest tests/test_ddm.py tests/test_vulc3_regressao.py "tests/test_selo.py::test_firewall_selo_nao_importa_report" tests/test_motores.py -q 2>&1 | tail -6</automated>
  </verify>
  <done>Suíte completa verde (0 falhas). test_ddm, TAEE11/vulc3 baseline e firewall selo↛report intactos. Nenhum arquivo proibido (ddm.py/selo.py/lentes.py) modificado. Commit: test(04): confirma invariantes — suíte verde, firewall e DDM/TAEE11 intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CVM/Yahoo → engine | Dados já ingeridos entram como floats no motor; nenhum input de usuário não-confiável cruza aqui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (input degenerado) | motores.rim / ddm.valor_gordon | mitigate | Padrão never-raise: None/n<=0/ke<=0/vpa0<=0 -> None; guarda ke−g_terminal ≥ ke_g_spread_min degrada para fade-only (sem explosão de perpetuidade) |
| T-04-02 | Denial (perpetuidade explosiva) | rim() terminal | mitigate | g_terminal fixo 0.025 + clamps [ke_piso, ke_teto]; terminal cresce a g_terminal (não à taxa do book); ke_g_spread_min protege configs futuros |
| T-04-03 | Elevation/Info | rede/PII | accept | Função numérica pura, sem rede, sem PII — nenhuma superfície nova (ASVS V2/V3/V4/V6 não se aplicam) |
</threat_model>

<verification>
Gates duros desta fase (cobram o NÚMERO — a falha do v2.2):
1. CAL-01 gate: test_rim_itub4_live_alvo_32_40 assere 32.0 <= valor_intrinseco <= 40.0 lendo os knobs de config (prova parametrização) e ke via ke_rim (prova ke_teto=0.13). ITUB4 ≈ R$32,9.
2. CAL-02: ke_rim(1.29, cfg) == 0.13 (teto revisado, clamp são, < ke_live).
3. Anti-bad-bank: banco ROE<Ke valua < book (test_rim_bad_bank_abaixo_do_book).
4. Não quebrou nada: suíte completa verde; test_ddm, TAEE11 (rota DDM), firewall selo↛report intactos; roe==ke ancora no VPA; never-raise.
</verification>

<success_criteria>
- ITUB4 roteado para RIM: R$32–40 (gate de teste), não R$23.
- Valor terminal parametrizado em config.yaml (excesso_sustentavel, g_terminal, ke_g_spread_min), zero magic constant, fundamento teórico documentado no docstring.
- ke_teto 0.14->0.13 documentado, clamps sãos, sem explosão.
- Suíte completa verde; ddm.py/selo.py/lentes.py intocados; firewall intacto.
</success_criteria>

<output>
After completion, create `.planning/phases/04-rim-com-valor-terminal-ke-revisado/04-01-SUMMARY.md`
</output>
