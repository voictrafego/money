---
phase: 04-rim-com-valor-terminal-ke-revisado
plan: 03
type: tdd
wave: 2
depends_on: ["04-02"]
files_modified:
  - src/analista/report/report.py
  - tests/test_motores.py
  - tests/fixtures/fair_values_bancos.yaml
autonomous: true
requirements: [CAL-01]

must_haves:
  truths:
    - "BBSE3 passa: rota Gordon-franquia entrega ~R$39,87, dentro da banda [28,05; 52,90]"
    - "A cesta chega a 4/4 na banda ±15%"
    - "BBSE3 roteia para motor='seguradora' com excecao_nota (rota documentada, não silenciosa)"
    - "Os bancos (ITUB4/BBAS3/BBDC4) ficam inalterados pela rota de seguradora"
    - "Reuso PURO de ddm.valor_gordon + dpa_recorrente — ddm.py INTOCADO, zero knob numérico novo"
    - "D-03: seguradora capital-light recebe rota própria (Gordon-franquia), fora do bank-RIM ancorado em book — mudança minimalista, sem motor novo"
    - "D-04: roteamento parte do setor CVM (token 'seguradora' via _setor_casa_token) — ponto de corte no ramo de report._intrinseco_por_motor"
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "ramo de roteamento de seguradora em _intrinseco_por_motor (antes do ramo rim)"
      contains: "seguradora"
    - path: "tests/test_motores.py"
      provides: "teste da rota seguradora (V_seguradora(BBSE3) ≈ 39,87)"
      contains: "seguradora"
    - path: "tests/fixtures/fair_values_bancos.yaml"
      provides: "excecao_nota do BBSE3 atualizada para descrever a rota Gordon-franquia"
      contains: "excecao_nota"
  key_links:
    - from: "src/analista/report/report.py"
      to: "src/analista/core/ddm.py::valor_gordon"
      via: "valor_gordon(dpa_recorrente*(1+g), a.ke, g_estavel) no ramo seguradora"
      pattern: "valor_gordon"
    - from: "src/analista/report/report.py"
      to: "src/analista/core/arquetipo.py::_setor_casa_token"
      via: "detecção do token 'seguradora' no setor CVM"
      pattern: "_setor_casa_token"
---

<objective>
Levar a cesta de **3/4 para 4/4** roteando a BBSE3 (única não-banco) por uma **rota de seguradora
capital-light** (Alavanca 3, D-03/D-04): reuso PURO de `ddm.valor_gordon` sobre o dividendo
sustentável (`dpa_recorrente`), fora do bank-RIM ancorado em book. Computado:
`valor_gordon(dpa_recorrente×(1+g), ke_live, 2,5%)` = **R$39,87 — em cima do mid de consenso (39,5)**,
dentro da banda [28,05; 52,90]. É a menor mudança reusável (não prolifera knob — D-08), economicamente
correta para franquia de alto payout.

Purpose: superar o piso D-05 (3/4+1) e entregar 4/4, com a BBSE3 como rota de arquétipo documentada
em vez de exceção que falha.
Output: ramo de seguradora em `report._intrinseco_por_motor`, teste unit da rota, e `excecao_nota`
do BBSE3 atualizada para descrever a rota (não mais a falha).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rim-com-valor-terminal-ke-revisado/04-RESEARCH.md
@.planning/phases/04-rim-com-valor-terminal-ke-revisado/04-CONTEXT.md
@.planning/phases/04-rim-com-valor-terminal-ke-revisado/04-02-SUMMARY.md

<interfaces>
<!-- Contratos que o executor consome DIRETAMENTE — sem explorar o codebase. -->

Dispatch (src/analista/report/report.py:198-240): `_intrinseco_por_motor(motor, c, a, cfg)`.
`ult = c.ultimo_ano()`, `g_estavel = cfg["ddm"]["g_estavel"]` (=0.025). O ramo `motor == "rim"`
começa na linha 202. `a.ke` (CAPM ao vivo, beta local) já está setado antes do dispatch (report.py:381).

Detecção de arquétipo por setor (src/analista/core/arquetipo.py:107): `_setor_casa_token(setor, tokens)`
casa por limite de palavra; `setor` deve vir em minúsculas. BBSE3 setor = "Emp. Adm. Part. -
Seguradoras e Corretoras" → casa o token "seguradora". (BBSE3 hard-roteia FINANCEIRA → motor="rim".)

Primitivas de reuso (INTOCADAS, só CHAMADAS):
- src/analista/core/ddm.py:37 `valor_gordon(dpa1, ke, g) -> Optional[float]` (None se ke−g ≤ 0).
- src/analista/core/fundamentals.py:185 `c.dpa_recorrente() -> Optional[float]` (= payout_valuation × lpa_valuation; ≈ R$3,83 no snapshot BBSE3).

Como a.motor/intrinseco chegam ao gate: `rodar_cesta` (backtest.py) lê `a.motor` e `a.intrinseco_motor`.
O gate `test_backtest_cesta_rota_por_ticker` (test_backtest_bancos.py:63) EXIGE `excecao_nota` para
qualquer ticker com motor != "rim". BBSE3 já tem `excecao_nota` (adicionada em 04-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rota de seguradora (Gordon-franquia) em _intrinseco_por_motor</name>
  <files>src/analista/report/report.py, tests/test_motores.py</files>
  <read_first>
    - src/analista/report/report.py (_intrinseco_por_motor, linhas 198-240; e report.py:381 onde a.ke é setado)
    - src/analista/core/ddm.py (valor_gordon, linhas 37-50)
    - src/analista/core/fundamentals.py (dpa_recorrente, linha 185)
    - src/analista/core/arquetipo.py (_setor_casa_token, linhas 107-121)
    - .planning/phases/04-rim-com-valor-terminal-ke-revisado/04-RESEARCH.md (§Alavanca 3 — mecanismo, ponto de corte exato D-04, alternativas rejeitadas; Pitfalls 3/4)
  </read_first>
  <behavior>
    - RED: teste da rota seguradora — a partir dos inputs congelados do BBSE3 (dpa_recorrente ≈ 3,83, ke_live ≈ 12,36%, g=2,5%), a rota devolve `V_seguradora ≈ R$39,87` (dentro de ±R$1). Falha antes da implementação (o ramo não existe → BBSE3 sai R$25,38 pelo RIM).
    - Regressão negativa: um ticker de setor "Bancos" (ITUB4) NÃO entra na rota (não casa o token seguradora) → segue pelo RIM.
  </behavior>
  <action>
    Escreva PRIMEIRO o teste em tests/test_motores.py que exercita a rota Gordon-franquia com os
    inputs do BBSE3: `valor_gordon(dpa_recorrente*(1+g_estavel), ke_live, g_estavel)` ≈ R$39,87
    (assert em banda ±R$1), e um caso de banco (setor "Bancos") que NÃO deve casar o token seguradora.
    Rode e confirme RED.
    Depois adicione, em `_intrinseco_por_motor`, um ramo de seguradora ANTES do `if motor == "rim"`:
    quando `motor == "rim"` E `arquetipo._setor_casa_token((c.setor or "").lower(), ["seguradora"])`,
    curto-circuite o RIM e devolva `ddm.valor_gordon(dpa1=c.dpa_recorrente()*(1+g_estavel), ke=a.ke,
    g=g_estavel)`. Use `a.ke` (CAPM ao vivo, NÃO o ke_rim — a seguradora não é banco de balanço
    large-cap, Pitfall 3) e `g_estavel = cfg["ddm"]["g_estavel"]` (2,5%, zero knob numérico novo).
    Use `dpa_recorrente()` (NÃO dpa_trailing_12m — Pitfall 4). Setar `a.motor = "seguradora"` (rótulo
    honesto) neste ramo. Se `dpa_recorrente()` ou `a.ke` forem None, ou valor_gordon devolver None,
    degrade para o RIM legado (never-raise) — não force a rota sob dado degenerado. NÃO reimplemente
    o casamento de token (reuse `_setor_casa_token`). NÃO toque ddm.py/fundamentals.py/arquetipo.py.
    Cuidado com o ramo fronteiriço (`_veredito_fronteirico`): a mutação de `a.motor` só deve valer no
    dispatch primário — BBSE3 hard-roteia financeira (não é fronteiriço), então na prática só o
    caminho primário aciona a rota; garanta que o ramo não quebra os candidatos.
  </action>
  <verify>
    <automated>pytest tests/test_motores.py -k seguradora -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_motores.py -k seguradora -x` PASSA (V_seguradora(BBSE3) ≈ 39,87 ±1).
    - Via rodar_cesta sobre o snapshot: BBSE3 tem `motor == "seguradora"` e `intrinseco_motor ≈ 39,87` (dentro de [28,05; 52,90]).
    - Bancos inalterados: ITUB4 ≈ 32,88, BBAS3 ≈ 43,89, BBDC4 ≈ 13,37 (não entram na rota).
    - `git diff --name-only` NÃO inclui src/analista/core/ddm.py, fundamentals.py nem arquetipo.py.
  </acceptance_criteria>
  <done>Ramo de seguradora (Gordon-franquia) roteia BBSE3 → ~R$39,87 via reuso puro de ddm.valor_gordon; bancos intocados.</done>
</task>

<task type="auto">
  <name>Task 2: Fechar 4/4 — excecao_nota do BBSE3 como rota documentada + suíte verde</name>
  <files>tests/fixtures/fair_values_bancos.yaml</files>
  <read_first>
    - tests/fixtures/fair_values_bancos.yaml (bloco BBSE3 com a excecao_nota adicionada em 04-02)
    - tests/test_backtest_bancos.py (test_backtest_cesta_rota_por_ticker linhas 63-83; test_backtest_gate_quorum_e_anotacao)
    - .planning/phases/04-rim-com-valor-terminal-ke-revisado/04-RESEARCH.md (§Alavanca 3 — ponto de corte, consequência no gate; §Impacto nos Goldens)
  </read_first>
  <action>
    Atualize o texto da `excecao_nota` do BBSE3 em fair_values_bancos.yaml para descrever a ROTA (não
    mais a falha): "seguradora capital-light → rota DDM-franquia (Gordon sobre o dividendo sustentável),
    fora do bank-RIM ancorado em book — arquétipo documentado (D-03/D-05)". O gate
    `test_backtest_cesta_rota_por_ticker` exige nota para motor != "rim", e BBSE3 agora roteia
    "seguradora" — a nota é o que impede a "rota silenciosa" (Pitfall 5). Com BBSE3 dentro da banda,
    `test_backtest_gate_quorum_e_anotacao` fica 4/4 (verde trivial, sem falhas a anotar).
    Rode a suíte completa e confirme que nada regrediu.
  </action>
  <verify>
    <automated>pytest tests/test_backtest_bancos.py -q && pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_backtest_bancos.py -q` verde: cesta 4/4 na banda; BBSE3 motor="seguradora" com excecao_nota (rota não-silenciosa).
    - `pytest -q` suíte completa verde (goldens intactos: ddm/vulc3/selo/motores; 440+ baseline).
    - `test_backtest_cesta_rota_por_ticker` verde com BBSE3 roteando "seguradora".
  </acceptance_criteria>
  <done>excecao_nota do BBSE3 descreve a rota documentada; cesta 4/4 na banda e suíte completa verde sem regressão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingestão CVM/Yahoo → engine | Inputs já ingeridos a montante; nesta fase vêm do snapshot congelado (fixture determinística offline). Nenhuma nova entrada não-confiável cruza a fronteira. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure / Tampering (I/T) | ramo seguradora em report._intrinseco_por_motor | accept | Roteamento numérico puro reusando primitiva já testada (ddm.valor_gordon); sem rede, sem I/O, sem input não-confiável novo (research §Security). |
| T-04-04 | Denial of Service (D) — input degenerado (dpa_recorrente/ke None, ke−g ≤ 0) | ramo seguradora | mitigate | Never-raise preservado: None → degrada para o RIM legado; valor_gordon já retorna None em ke−g ≤ 0. ASVS V5 satisfeita pelo never-raise. |
</threat_model>

<verification>
- `pytest tests/test_motores.py tests/test_backtest_bancos.py -q` — verde (por-commit).
- `pytest -q` — suíte completa verde (por-wave).
- Cesta final: ITUB4 32,88 · BBAS3 43,89 · BBDC4 13,37 · BBSE3 39,87 (rota seguradora) → 4/4.
- `ddm.py`/`fundamentals.py`/`arquetipo.py`/`selo.py`/`lentes.py` intocados.
</verification>

<success_criteria>
- BBSE3 passa via rota Gordon-franquia (~R$39,87), dentro da banda.
- Cesta 4/4 na banda ±15%.
- BBSE3 roteia motor="seguradora" com excecao_nota (rota documentada, não silenciosa).
- Bancos inalterados; ddm.py e demais primitivas intocadas.
- Suíte completa verde.
</success_criteria>

<output>
Após a conclusão, crie `.planning/phases/04-rim-com-valor-terminal-ke-revisado/04-03-SUMMARY.md`
</output>
