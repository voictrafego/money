---
phase: 06-integra-o-na-engine-composite-alerta-cli
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/analista/report/report.py
  - tests/test_report.py
autonomous: true
requirements: [TIMING-02, TIMING-03, CLI-01]
must_haves:
  truths:
    - "matriz_leitura cruza o veredito DDM (SUBAVALIADA/NO INTERVALO/SOBREAVALIADA) com o estado técnico numa frase curada fundamento-primeiro, lendo a.veredito sem recalcular (TIMING-02)"
    - "alerta_reverificacao dispara no OR de perda-da-MM200 / death cross / perda da mínima do Donchian, com voz 'reverifique os fundamentos' e nunca 'venda' (TIMING-03)"
    - "relatorio_markdown imprime a seção 'Sinais técnicos (consultivos)' espelhando o read da engine, com fallback de histórico curto (CLI-01)"
    - "Os 64 golden tests de valuation existentes continuam verdes (TEST-07)"
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "derivação da matriz fundamento×técnico + alerta consolidado em analisar_acao; seção CLI em relatorio_markdown"
      contains: "Sinais técnicos (consultivos)"
    - path: "tests/test_report.py"
      provides: "golden da matriz (células-âncora D-05/D-06), do alerta OR-of-three e da seção CLI"
      contains: "matriz_leitura"
  key_links:
    - from: "src/analista/report/report.py:analisar_acao"
      to: "a.veredito (token líder) × a.timing_estado"
      via: "seleção de frase curada por célula (D-04), read-only sobre o fundamento"
      pattern: "matriz_leitura"
    - from: "src/analista/report/report.py:analisar_acao"
      to: "a.sinais.tendencia.posicao_mm200 / cruzamento / a.sinais.canais.rompimento_donchian"
      via: "OR dos três gatilhos de baixa → alerta_reverificacao único consolidado"
      pattern: "alerta_reverificacao"
    - from: "src/analista/report/report.py:relatorio_markdown"
      to: "a.timing_resumo / a.matriz_leitura / a.alerta_reverificacao"
      via: "seção 'Sinais técnicos (consultivos)' via L.append"
      pattern: "Sinais técnicos"
---

<objective>
Fechar o read técnico consultivo e espelhá-lo na CLI: `analisar_acao` ganha a matriz fundamento×técnico (frase curada por célula, fundamento sempre líder — D-04/D-05/D-06) e o alerta de reverificação consolidado (OR de perda-da-MM200 / death cross / perda da mínima do Donchian — D-07/D-08/D-09, voz "reverifique os fundamentos", nunca venda). `relatorio_markdown` ganha a seção "Sinais técnicos (consultivos)" (CLI-01/D-13). Trava tudo por golden e confirma o invariante TEST-07 (64 testes de valuation verdes).

Purpose: É a camada que garante por construção que o fundamento manda (a frase começa pelo veredito DDM) e que o rompimento dispara reverificação — não venda. Depende da Plan 01 (lê timing_estado e a.sinais já populados).
Output: `report.py` (matriz+alerta em analisar_acao; seção CLI), `tests/test_report.py` (estendido).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-CONTEXT.md
@.planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-PATTERNS.md
@.planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-01-SUMMARY.md

<interfaces>
<!-- Campos já criados na Plan 01 (report.py / AnaliseAcao) que esta plan preenche e consome. -->
  a.veredito : str  # começa com token "SUBAVALIADA" | "NO INTERVALO" | "SOBREAVALIADA" (report.py 118-124); "" se DDM não calculou
  a.timing_estado : str   # "tendencia_de_alta" | "sem_tendencia" | "atencao" (preenchido na Plan 01)
  a.timing_resumo : str   # frase PT do timing (Plan 01)
  a.matriz_leitura : str  # ESTA plan preenche
  a.alerta_reverificacao : Optional[str]  # ESTA plan preenche (None se nada rompeu)
  a.sinais.tendencia.posicao_mm200 : "acima"|"abaixo"|"indisponivel"
  a.sinais.tendencia.cruzamento    : "golden_cross"|"death_cross"|"nenhum"|"indisponivel"
  a.sinais.canais.rompimento_donchian : "nova_maxima"|"perda_minima"|"nenhum"|"indisponivel"

<!-- Frases-âncora VERBATIM (CONTEXT D-05 / D-06) — copiar exatamente, incluindo travessão "—" e acentos. -->
  BARATO+ATENÇÃO (SUBAVALIADA × atencao):
    "Fundamentalmente descontada, porém o preço perdeu a tendência — confirme que os fundamentos seguem intactos antes de entrar."
  CARO+ALTA (SOBREAVALIADA × tendencia_de_alta):
    "Tecnicamente em alta, porém acima do valor intrínseco — o método não compra caro; aguarde um preço melhor."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: analisar_acao — matriz fundamento×técnico + alerta de reverificação consolidado</name>
  <files>src/analista/report/report.py</files>
  <behavior>
    - matriz_leitura SEMPRE começa pelo fundamento (token do veredito) e subordina a parte técnica (D-04, garante UI-06).
    - Célula SUBAVALIADA × atencao → matriz_leitura == frase-âncora D-05 verbatim.
    - Célula SOBREAVALIADA × tendencia_de_alta → matriz_leitura == frase-âncora D-06 verbatim.
    - Demais células (combinações não-conflitantes) → frase curada PT fundamento-primeiro e consultiva (discrição), nunca um template composicional cego.
    - veredito == "" (DDM não calculou) → matriz_leitura == "" (degradação; sem frase inventada).
    - alerta_reverificacao é None quando NENHUM dos três gatilhos de baixa aciona.
    - alerta_reverificacao dispara se posicao_mm200=="abaixo" OU cruzamento=="death_cross" OU rompimento_donchian=="perda_minima" (OR, D-07), INDEPENDENTE do veredito (D-08).
    - Quando ≥2 gatilhos acionam → UMA única mensagem consolidada listando os gatilhos (D-09).
    - A mensagem usa a voz "Reverifique os fundamentos" / "Não é sinal de venda" — NUNCA "venda".
  </behavior>
  <read_first>
    - src/analista/report/report.py (analisar_acao APÓS a Plan 01 — reler o arquivo; bloco veredito 114-124; o novo bloco técnico antes de `return a`)
    - .planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-PATTERNS.md (Analogs C e D — composição de string estilo veredito + acúmulo de gatilhos consolidado)
    - .planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-CONTEXT.md (D-04, D-05, D-06, D-07, D-08, D-09)
  </read_first>
  <action>
    No bloco técnico já existente (Plan 01) dentro de analisar_acao, logo após a definição de `a.timing_estado`/`a.timing_resumo` e antes de `return a`, adicionar a matriz e o alerta. Manter tudo read-only sobre o fundamento (lê a.veredito/vmin/vmax, nunca recalcula).
    Matriz (D-04, TIMING-02): extrair o token líder do veredito (primeira palavra de `a.veredito` — "SUBAVALIADA" | "NO INTERVALO" | "SOBREAVALIADA"; se `a.veredito == ""`, deixar `a.matriz_leitura = ""` e pular). Selecionar uma frase CURADA por célula (token × a.timing_estado), NÃO um template composicional. As duas células-âncora têm texto VERBATIM (copiar do bloco <interfaces>):
      - SUBAVALIADA × "atencao" → a frase D-05 exata.
      - SOBREAVALIADA × "tendencia_de_alta" → a frase D-06 exata.
    Para as demais combinações (SUBAVALIADA×alta, SUBAVALIADA×sem_tendencia, NO INTERVALO×*, SOBREAVALIADA×atencao, SOBREAVALIADA×sem_tendencia) escrever frases PT curadas curtas, sempre começando pelo fundamento e tratando o técnico como subordinado/consultivo (ex.: NO INTERVALO×tendencia_de_alta → algo como "Dentro do intervalo justo; tecnicamente em alta — acompanhe, sem prêmio de desconto."). Implementar via um mapeamento explícito (dict de tuplas (token, estado) → frase) ou cadeia if/elif clara — travável por golden. Evitar linguagem de ordem de compra/venda.
    Alerta (D-07/D-08/D-09, TIMING-03): montar uma lista `gatilhos` lendo os rótulos discretos de a.sinais: append "preço abaixo da MM200" se posicao_mm200=="abaixo"; append "cruzamento de baixa MM50×MM200" se cruzamento=="death_cross"; append "rompimento da mínima do canal" se rompimento_donchian=="perda_minima". Se `gatilhos` não-vazia, montar UMA mensagem consolidada em `a.alerta_reverificacao` no formato "Reverifique os fundamentos: " + os gatilhos unidos por "; " + ". Não é sinal de venda — confirme se os números seguem intactos." Se vazia, deixar `a.alerta_reverificacao = None`. Dispara independente do veredito (D-08). Quando os sinais forem "indisponivel" nenhum gatilho casa → alerta None (coerente com a degradação).
    Não tocar a.veredito/vmin/vmax nem o bloco de a.alertas (armadilhas de dividendos) existente — alerta_reverificacao é um campo SEPARADO.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && python -m pytest tests/test_report.py -x -q -k "matriz or alerta" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - grep em report.py mostra a frase-âncora D-05 e a frase-âncora D-06 VERBATIM (com o travessão "—").
    - Para SUBAVALIADA×atencao e SOBREAVALIADA×tendencia_de_alta, a.matriz_leitura é exatamente a frase-âncora correspondente (validado por golden na Task 2).
    - a.alerta_reverificacao é None quando nenhum gatilho de baixa; é uma string única "Reverifique os fundamentos: ...Não é sinal de venda..." quando ≥1 gatilho.
    - grep confirma que a string do alerta NÃO contém a palavra "venda" fora da negação "Não é sinal de venda".
    - Nenhuma alteração no bloco veredito (114-124), DDM, múltiplos ou no acúmulo de a.alertas.
  </acceptance_criteria>
  <done>analisar_acao preenche matriz_leitura (fundamento-primeiro, células-âncora verbatim) e alerta_reverificacao (OR-of-three consolidado, voz reverificação) — read-only sobre o fundamento.</done>
</task>

<task type="auto">
  <name>Task 2: Seção CLI "Sinais técnicos (consultivos)" + golden da matriz/alerta/CLI + invariante TEST-07</name>
  <files>src/analista/report/report.py, tests/test_report.py</files>
  <read_first>
    - src/analista/report/report.py (relatorio_markdown 155-242; seção Veredito+Alertas 233-241; fallback degradado DDM 229-231)
    - tests/test_report.py (testes da Plan 01 — estender, não reescrever)
    - .planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-PATTERNS.md (seção "relatorio_markdown" + "Golden Test Patterns" + nota TEST-07)
    - .planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-CONTEXT.md (D-13)
  </read_first>
  <action>
    Parte A — seção CLI (CLI-01/D-13): em relatorio_markdown, APÓS a seção Veredito (após o bloco de a.alertas, linha 241) e ANTES do `return "\n".join(L)` (linha 242), acrescentar via o idiom `L.append(...)` a seção "## Sinais técnicos (consultivos)". Caso degradado (histórico curto): se `a.sinais is None or a.timing_estado == "" or a.sinais.tendencia.posicao_mm200 == "indisponivel"`, imprimir uma linha de fallback em itálico tipo "_Histórico de preços insuficiente para o read técnico._" seguida de `L.append("")` (espelha o fallback DDM 229-231). Caso normal: `L.append(f"**Timing de entrada:** {a.timing_resumo}")`, linha em branco, `L.append(a.matriz_leitura)` (fundamento-primeiro), e SE `a.alerta_reverificacao` então linha em branco + `L.append(f"- ⚠️ {a.alerta_reverificacao}")` (mesmo glifo ⚠️ da seção Alertas, paridade visual). Encerrar com `L.append("")` como toda seção. Formato é discrição (D-13) mas usar o idiom L.append e terminar com linha em branco.
    Parte B — golden (estender tests/test_report.py): adicionar testes que (1) montem um CompanyData/cenário SUBAVALIADA×atencao e afirmem `a.matriz_leitura ==` a frase-âncora D-05 exata; (2) cenário SOBREAVALIADA×tendencia_de_alta → frase-âncora D-06 exata; (3) cenário com perda da mínima do Donchian e/ou perda da MM200 → `a.alerta_reverificacao` não-None contendo "Reverifique os fundamentos" e "Não é sinal de venda", e SEM a palavra "venda" isolada; (4) cenário sem rompimento → `a.alerta_reverificacao is None`; (5) `relatorio_markdown(c, a, cfg)` contém a string "## Sinais técnicos (consultivos)" e, no caso degradado (ohlc_ajustado=None), contém o texto de fallback. Para forçar veredito SUBAVALIADA/SOBREAVALIADA sem montar fundamentos completos, é aceitável construir o AnaliseAcao via analisar_acao e então setar a.veredito/a.timing_estado diretamente no teste antes de checar a matriz, OU construir um CompanyData com fundamentos mínimos que produzam o veredito desejado — documentar a abordagem no comentário do teste (read-only: o teste pode pinar a.veredito porque a matriz só LÊ esse campo).
    Parte C — invariante TEST-07: rodar a suíte COMPLETA e confirmar que os 64 golden tests de valuation seguem verdes (Phase 6 é puramente aditiva).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && python -m pytest -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - relatorio_markdown emite a linha "## Sinais técnicos (consultivos)"; no caso degradado emite a linha de fallback em itálico; ambos terminam com `L.append("")`.
    - Os golden da matriz afirmam as frases-âncora D-05/D-06 VERBATIM e passam.
    - O golden do alerta afirma None sem rompimento e mensagem consolidada com rompimento; o glifo ⚠️ aparece na seção CLI quando há alerta.
    - `python -m pytest -q` (suíte inteira) termina verde — os 64 golden de valuation + os novos testes de report (TEST-07 invariante respeitado).
  </acceptance_criteria>
  <done>A CLI espelha o read técnico (CLI-01) com fallback gracioso, os goldens da matriz/alerta/CLI passam, e a suíte completa fica verde (TEST-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OHLC/sinais já em memória → derivação de texto consultivo → string Markdown | Nenhum novo input externo; a fase só transforma rótulos discretos já calculados em frases curadas e numa seção Markdown. Sem auth, rede, servidor ou escrita persistente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Elevation of Privilege | matriz/alerta sobrescrevendo o veredito fundamentalista | mitigate | Por construção a matriz só LÊ a.veredito (token líder) e o alerta dispara num campo SEPARADO (alerta_reverificacao); nada recalcula nem toca a.veredito/vmin/vmax. Travado por golden + invariante TEST-07 (64 testes verdes). |
| T-06-05 | Spoofing | alerta soando como ordem de venda (induz ação errada do usuário) | mitigate | A mensagem é fixa na voz "Reverifique os fundamentos ... Não é sinal de venda"; golden afirma ausência da palavra "venda" fora da negação (D-09, alinhado à regra travada "técnico é consultivo, nunca ordem"). |
| T-06-06 | Denial of Service | seção CLI quando a.sinais é None / "indisponivel" | mitigate | Guard explícito no início da seção (sinais None / timing_estado "" / posicao_mm200 "indisponivel") imprime fallback em itálico em vez de acessar atributos ausentes; espelha o fallback DDM 229-231. Sem exceção. |
</threat_model>

<verification>
- `python -m pytest -q` (suíte completa) verde — novos goldens de report + 64 goldens de valuation (TEST-07).
- relatorio_markdown contém "## Sinais técnicos (consultivos)" no caso normal e o fallback no degradado.
- grep: frases-âncora D-05/D-06 verbatim em report.py; alerta sem "venda" fora da negação.
- a.veredito/vmin/vmax e o bloco DDM/múltiplos intactos.
</verification>

<success_criteria>
- A matriz cruza veredito DDM × estado técnico, fundamento-primeiro, sem recalcular (TIMING-02).
- O alerta de reverificação dispara no OR dos três sinais de baixa, voz reverificação, nunca venda (TIMING-03).
- relatorio_markdown imprime a seção espelhando o read da engine, com fallback gracioso (CLI-01).
- Os 64 golden tests de valuation continuam verdes (TEST-07).
</success_criteria>

<output>
After completion, create `.planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-02-SUMMARY.md`
</output>
