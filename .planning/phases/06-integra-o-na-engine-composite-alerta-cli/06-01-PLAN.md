---
phase: 06-integra-o-na-engine-composite-alerta-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/report/report.py
  - config.yaml
  - tests/test_report.py
autonomous: true
requirements: [TIMING-01, TIMING-04, TEST-06]
must_haves:
  truths:
    - "analisar_acao popula a.sinais chamando indicators.calcular (ponto único CLI/UI)"
    - "O resumo composite classifica o timing por árvore de decisão explícita MM200-dá-direção / ADX-confirma-força — tendência de alta / sem tendência / atenção em linguagem natural PT, com RSI/MACD como matiz fino que não muda o estado (TIMING-01 / D-01 / D-03)"
    - "A base temporal default 'semanal' faz resample W-FRI do OHLC (canônica em cfg, ponto único CLI/UI) antes de calcular os indicadores e afeta tanto o timing quanto o alerta; 'diario' usa o frame diário (TIMING-04 / D-10 / D-11 / D-12)"
    - "Golden test trava o caso-limite acima-da-MM200-com-ADX<20 → 'sem_tendencia' (TEST-06 / D-02)"
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "campos sinais/timing_estado/timing_resumo na AnaliseAcao + import indicators + árvore de decisão composite e resample em analisar_acao"
      contains: "timing_estado"
    - path: "config.yaml"
      provides: "chave canônica base_temporal (default semanal) no bloco indicadores"
      contains: "base_temporal"
    - path: "tests/test_report.py"
      provides: "golden TEST-06 (composite tiebreak) + golden do resample W-FRI"
      contains: "def test_composite"
  key_links:
    - from: "src/analista/report/report.py:analisar_acao"
      to: "indicators.calcular"
      via: "c.ohlc_ajustado (resampleado quando base=='semanal')"
      pattern: "indicators\\.calcular"
    - from: "src/analista/report/report.py:analisar_acao"
      to: "a.sinais.tendencia.posicao_mm200 + a.sinais.forca.forca_adx"
      via: "leitura dos rótulos discretos já classificados (não relê o float do ADX)"
      pattern: "posicao_mm200"
---

<objective>
Ligar os sinais técnicos da Phase 5 dentro da engine: `analisar_acao` passa a popular `a.sinais` (via `indicators.calcular`) e a derivar um resumo de "timing de entrada" composite consultivo — três estados macro (tendência de alta / sem tendência / atenção) — com a base temporal diário/semanal (default semanal, resample W-FRI) vivendo em `cfg`. Trava o desempate canônico (acima da MM200 mas ADX < 20 → sem tendência) por golden test.

Purpose: É o coração consultivo do marco v1.2 — a árvore de decisão MM200-dá-direção / ADX-confirma-força (D-01/D-02) e a base semanal (D-10/D-11/D-12). A matriz fundamento×técnico, o alerta de reverificação e a paridade CLI são da Plan 02.
Output: `report.py` (dataclass + analisar_acao), `config.yaml` (chave base_temporal), `tests/test_report.py` (novo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-CONTEXT.md
@.planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-PATTERNS.md

<interfaces>
<!-- Contratos já entregues na Phase 5 (NÃO modificar). O executor lê estes rótulos discretos. -->

De src/analista/core/indicators.py — calcular(ohlc, cfg) -> SinaisTecnicos, com:
  SinaisTecnicos.tendencia.posicao_mm200 : "acima" | "abaixo" | "indisponivel"
  SinaisTecnicos.tendencia.cruzamento    : "golden_cross" | "death_cross" | "nenhum" | "indisponivel"
  SinaisTecnicos.forca.forca_adx         : "sem_tendencia" | "forte" | "neutro" | "indisponivel"
  SinaisTecnicos.canais.rompimento_donchian : "nova_maxima" | "perda_minima" | "nenhum" | "indisponivel"
  SinaisTecnicos.momentum.nivel_rsi      : "sobrecomprado" | "sobrevendido" | "neutro" | "indisponivel"
  SinaisTecnicos.momentum.cruzamento_macd: "cruz_alta" | "cruz_baixa" | "nenhum" | "indisponivel"
calcular() já trata None/vazio/curto → frame vazio → tudo "indisponivel" (degradação única, indicators.py 412-413).
_forca já classifica forca_adx com os limiares < 20 / > 25 (indicators.py 337-344) — reusar, não redefinir.

De src/analista/core/fundamentals.py:
  CompanyData.ohlc_ajustado : Optional[pd.DataFrame]  # split-adjusted, input dos indicadores (CR-01, linha 47)
  CompanyData(ticker=...)   # só ticker é obrigatório; demais campos têm default

De tests/test_indicators.py (espelhar):
  _cfg_ind() carrega o config.yaml shipado (linhas 15-19)
  builders de série: np.linspace/np.concatenate + pd.date_range(..., freq="B") (linhas 22-28)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campos da AnaliseAcao + import indicators + chave base_temporal no config</name>
  <files>src/analista/report/report.py, config.yaml</files>
  <read_first>
    - src/analista/report/report.py (dataclass AnaliseAcao linhas 20-40; imports core linhas 15-17)
    - src/analista/core/indicators.py (linhas 33-89 — contrato SinaisTecnicos por família)
    - config.yaml (bloco indicadores linhas 70-84)
    - .planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-PATTERNS.md (seção "AnaliseAcao dataclass" e "config.yaml")
  </read_first>
  <action>
    Em report.py, adicionar `indicators` à linha de import dos módulos core (atual `from ..core import capm, ddm, growth, lifecycle` na linha 15) — fica `from ..core import capm, ddm, growth, indicators, lifecycle`.
    Em AnaliseAcao, APPEND ao FINAL dos campos existentes (após `alertas`, sem reordenar nada — disciplina aditiva idêntica a `Canais.donchian_sup_55`) os campos novos do read técnico consultivo, read-only sobre o fundamento:
      - `sinais: Optional["indicators.SinaisTecnicos"] = None` (populado por indicators.calcular)
      - `timing_estado: str = ""` (valores internos "tendencia_de_alta" | "sem_tendencia" | "atencao")
      - `timing_resumo: str = ""` (frase PT consultiva — TIMING-01)
      - `matriz_leitura: str = ""` (frase curada fundamento×técnico — preenchida na Plan 02)
      - `alerta_reverificacao: Optional[str] = None` (None quando nada rompeu — preenchido na Plan 02)
    Convenção de default: string `""` para campos sempre deriváveis (espelha `veredito: str = ""`), `None` para o alerta que é ausente quando não há rompimento (espelha `vmin`). Inserir os 5 campos agora (mesmo os usados só na Plan 02) para fixar o contrato do dataclass de uma vez.
    Em config.yaml, DENTRO do bloco `indicadores:` (após `regressao_janela: 90`, linha 83), adicionar a chave canônica `base_temporal: "semanal"` com comentário inline justificando: `"semanal" (resample W-FRI) | "diario" — default semanal (TIMING-04 / D-12)`. Default OBRIGATORIAMENTE `"semanal"` (REQ TIMING-04).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && python -c "from analista.report import report; a=report.AnaliseAcao(ticker='X', nome='', setor='', preco_atual=None); assert hasattr(a,'sinais') and a.sinais is None and a.timing_estado=='' and a.alerta_reverificacao is None; import yaml; cfg=yaml.safe_load(open('config.yaml')); assert cfg['indicadores']['base_temporal']=='semanal'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - report.py importa `indicators` do pacote core (grep `from ..core import` mostra `indicators` na lista).
    - AnaliseAcao expõe os 5 campos novos com os defaults exatos (`sinais=None`, `timing_estado=""`, `timing_resumo=""`, `matriz_leitura=""`, `alerta_reverificacao=None`), todos no FINAL da lista de campos (nenhum campo pré-existente foi reordenado).
    - config.yaml: `cfg["indicadores"]["base_temporal"] == "semanal"`.
    - Comando de verify imprime `OK`.
  </acceptance_criteria>
  <done>Os campos novos existem com defaults corretos, `indicators` está importado, e a chave `base_temporal: "semanal"` está no config — sem nenhuma mudança em campos/fórmulas existentes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: analisar_acao — resample semanal + popular sinais + árvore de decisão do composite</name>
  <files>src/analista/report/report.py</files>
  <behavior>
    - Com base_temporal "semanal" e ohlc_ajustado diário não-vazio: o frame é resampleado para W-FRI (Open=first, High=max, Low=min, Close=last, depois dropna) ANTES de chamar indicators.calcular.
    - Com base_temporal "diario": calcular roda direto sobre o frame diário (sem resample).
    - a.sinais nunca fica None quando há ohlc_ajustado válido (calcular sempre devolve SinaisTecnicos).
    - posicao_mm200=="acima" e forca_adx=="forte" → timing_estado=="tendencia_de_alta".
    - posicao_mm200=="acima" e forca_adx∈{"sem_tendencia","neutro"} → timing_estado=="sem_tendencia" (caso-limite D-02/TEST-06).
    - posicao_mm200=="abaixo" → timing_estado=="atencao".
    - posicao_mm200=="indisponivel" OU forca_adx=="indisponivel" (histórico curto / ohlc=None) → timing_estado=="sem_tendencia" e timing_resumo=="" (degradação graciosa DATA-03, sem exceção).
    - timing_resumo é uma frase PT coerente com o estado, refinada (não substituída) por momentum.nivel_rsi/cruzamento_macd (D-03 — RSI/MACD são matiz fino, NUNCA mudam timing_estado).
  </behavior>
  <read_first>
    - src/analista/report/report.py (analisar_acao inteiro, linhas 43-141; bloco veredito 114-124; bloco alertas 126-140; `cfg.get(...)` idiom linha 136)
    - src/analista/core/indicators.py (calcular 403-421 — agnóstico de timeframe e guard de borda; _forca 327-350 — limiares < 20 / > 25)
    - src/analista/core/fundamentals.py (linha 47 — ohlc_ajustado é Optional[pd.DataFrame])
    - .planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-PATTERNS.md (seção "analisar_acao", Analogs A/B/E)
    - .planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-CONTEXT.md (D-01, D-02, D-03, D-10, D-11, D-12)
  </read_first>
  <action>
    Inserir o bloco do read técnico no FINAL de analisar_acao, imediatamente ANTES de `return a` (após o bloco de alertas existente, linha 140) — assim a derivação lê veredito/vmin/vmax já calculados (necessário para a Plan 02).
    Passo 1 (resample, D-10/D-12): ler `base = cfg.get("indicadores", {}).get("base_temporal", "semanal")` e `ohlc = c.ohlc_ajustado` (split-adjusted, CR-01 — NUNCA c.ohlc). Se `base == "semanal" and ohlc is not None and len(ohlc) > 0`: `ohlc = ohlc.resample("W-FRI").agg({"Open":"first","High":"max","Low":"min","Close":"last"}).dropna()`. NÃO adicionar segundo guard de None/curto — `indicators.calcular` já substitui frame vazio e degrada para "indisponivel" (ponto único de degradação, indicators.py 412-413).
    Passo 2: `a.sinais = indicators.calcular(ohlc, cfg)`.
    Passo 3 (árvore de decisão composite, D-01/D-02/D-03): ler os rótulos JÁ classificados `pos = a.sinais.tendencia.posicao_mm200` e `forca = a.sinais.forca.forca_adx` (NÃO reler o float do ADX — reusar a classificação de `indicators._forca`). Ramos exatos:
      - se `pos == "indisponivel" or forca == "indisponivel"` → `a.timing_estado = "sem_tendencia"` e `a.timing_resumo = ""` (degradação; nada mais a fazer neste bloco).
      - elif `pos == "acima" and forca == "forte"` → `a.timing_estado = "tendencia_de_alta"`.
      - elif `pos == "abaixo"` → `a.timing_estado = "atencao"`.
      - else (acima mas ADX fraco/neutro — caso-limite D-02/TEST-06) → `a.timing_estado = "sem_tendencia"`.
    Passo 4 (timing_resumo, TIMING-01 + matiz D-03): quando timing_estado != "" e não-degradado, montar uma frase PT consultiva por estado, refinada por momentum sem mudar o estado:
      - "tendencia_de_alta" → base "Tendência de alta confirmada (preço acima da MM200, ADX forte)"; se `a.sinais.momentum.nivel_rsi == "sobrecomprado"` anexar matiz tipo " — porém sobrecomprado; pode valer esperar um pullback."
      - "sem_tendencia" → base "Sem tendência definida (lateral / força fraca) — não é timing de entrada confirmado."
      - "atencao" → base "Atenção: preço abaixo da MM200 (viés de baixa)."; opcionalmente refinar com cruzamento_macd "cruz_baixa". Frases são discrição do planner desde que PT, consultivas e fundamento-subordinadas; usar exatamente as palavras-chave de estado "tendência de alta" / "sem tendência" / "atenção" no texto.
    Manter o bloco read-only sobre o fundamento — NÃO tocar a.veredito/vmin/vmax. matriz_leitura e alerta_reverificacao ficam como estão (preenchidos na Plan 02).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && python -m pytest tests/test_report.py -x -q -k "composite or resample" 2>&1 | tail -5 || echo "test_report.py ainda criado na Task 3 — rode a Task 3 e re-verifique"</automated>
  </verify>
  <acceptance_criteria>
    - grep em report.py mostra `a.sinais = indicators.calcular(` e `resample("W-FRI")` dentro de analisar_acao.
    - grep mostra leitura de `a.sinais.tendencia.posicao_mm200` e `a.sinais.forca.forca_adx` (rótulos discretos), e NÃO há releitura de `.adx.iloc[-1]` no composite.
    - Os 4 ramos da árvore produzem exatamente "tendencia_de_alta" / "sem_tendencia" / "atencao" conforme o bloco <behavior> (validado pelos golden da Task 3).
    - Nenhuma linha do bloco veredito (114-124) nem das fórmulas DDM/múltiplos foi alterada.
    - `python -c "from analista.report import report"` importa sem erro.
  </acceptance_criteria>
  <done>analisar_acao popula a.sinais (com resample W-FRI quando semanal) e deriva timing_estado/timing_resumo pela árvore MM200-direção/ADX-força, degradando para "sem_tendencia" sem exceção quando os sinais são "indisponivel".</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_report.py — golden TEST-06 (composite tiebreak) + golden do resample W-FRI</name>
  <files>tests/test_report.py</files>
  <read_first>
    - tests/test_indicators.py (linhas 1-30 — _cfg_ind loader + builders de série np.linspace/date_range freq="B")
    - tests/test_ddm.py (linhas 1-48 — estilo "comentário declara o esperado, assert pina")
    - src/analista/core/fundamentals.py (linhas 18-47 — CompanyData; só ticker obrigatório, ohlc_ajustado é campo)
    - .planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-PATTERNS.md (seção "Golden Test Patterns")
    - .planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-CONTEXT.md (D-02, D-10 — specifics)
  </read_first>
  <action>
    Criar o arquivo novo tests/test_report.py. Copiar VERBATIM o loader `_cfg_ind()` de test_indicators.py (carrega o config.yaml shipado, garantindo que o teste usa os MESMOS limiares da engine).
    Teste A — TEST-06 / D-02 (`test_composite_acima_mm200_adx_fraco_eh_sem_tendencia`): construir uma série Close DIÁRIA (`pd.date_range(..., freq="B")`, ≥ ~260 pregões para a MM200/ADX existirem) que esteja ACIMA da própria MM200 mas com ADX < 20 — uma deriva lateral suave logo acima da média longa (ex.: subida lenta seguida de oscilação estreita sem direção forte). Montar um CompanyData mínimo `CompanyData(ticker="TST", ohlc_ajustado=df_ohlc)` onde df_ohlc tem colunas Open/High/Low/Close derivadas dessa série (High/Low com folga pequena para ADX baixo). Rodar `report.analisar_acao(c, _cfg_ind())` e `assert a.timing_estado == "sem_tendencia"`. Comentário no teste declarando que é o desempate canônico D-02 (ADX fraco vence o viés de alta da MM200).
    Como a base default é "semanal", o resample roda dentro de analisar_acao; gerar pregões diários suficientes para que após o resample W-FRI ainda haja barras semanais bastante para a MM200 semanal (ou, se preferir cravar o caso no timeframe diário, passar `cfg["indicadores"]["base_temporal"]="diario"` numa cópia do cfg). Documentar a escolha no comentário do teste.
    Teste B — resample W-FRI (`test_resample_semanal_w_fri`): construir um frame OHLC diário de datas conhecidas (ex.: 3 semanas completas via date_range freq="B"), aplicar a MESMA regra `resample("W-FRI").agg({"Open":"first","High":"max","Low":"min","Close":"last"}).dropna()` e assertar: (1) o índice resultante cai em sextas-feiras (weekday == 4); (2) o número de barras semanais bate com o esperado; (3) numa semana conhecida, Open==primeiro dia, High==máximo da semana, Low==mínimo, Close==último dia. Este é o golden dedicado do resample exigido por D-10.
    Não inventar asserts sobre matriz/alerta/CLI — esses são da Plan 02.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && python -m pytest tests/test_report.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_report.py existe, importa `from analista.report import report` e define `_cfg_ind()` idêntico ao de test_indicators.py.
    - `test_composite_acima_mm200_adx_fraco_eh_sem_tendencia` passa e afirma `a.timing_estado == "sem_tendencia"` num cenário comprovadamente acima-da-MM200-com-ADX<20.
    - `test_resample_semanal_w_fri` passa e afirma índice em sexta-feira + agregação first/max/min/last correta.
    - `python -m pytest tests/test_report.py -q` termina com todos os testes verdes (exit 0).
  </acceptance_criteria>
  <done>O golden TEST-06 trava o desempate D-02 e o golden do resample trava a regra W-FRI; ambos passam contra os limiares reais do config.yaml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticker (str) + OHLC já em memória → engine | Único input externo é uma string de ticker e o DataFrame OHLC já buscado pela ingestão. Sem auth, sem servidor, sem rede, sem escrita persistente nesta fase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | resample W-FRI + composite em analisar_acao com ohlc_ajustado curto/vazio/None | mitigate | Rotear pelo guard único de `indicators.calcular` (substitui frame vazio → "indisponivel"); `.dropna()` no resample; histórico curto degrada para timing_estado="sem_tendencia" sem levantar exceção na aba Streamlit (DATA-03). Sem segundo guard duplicado. |
| T-06-02 | Tampering | árvore de decisão lê o float do ADX em vez do rótulo discreto | accept | Por construção o composite lê apenas os rótulos já classificados (posicao_mm200/forca_adx) reusando os limiares de `indicators._forca`; não recalcula. Travado por golden TEST-06. |
| T-06-03 | Information disclosure | textos do timing_resumo | accept | Strings PT estáticas curadas; nenhum dado sensível ou input externo ecoado. Custo zero, sem PII. |
</threat_model>

<verification>
- `python -m pytest tests/test_report.py -q` verde (TEST-06 + resample).
- `python -c "from analista.report import report"` importa sem erro.
- config.yaml: `base_temporal: "semanal"` presente no bloco indicadores.
- Nenhuma fórmula de valuation tocada (bloco veredito/DDM/múltiplos intacto).
</verification>

<success_criteria>
- a.sinais é populado em analisar_acao via indicators.calcular (TIMING-01, ponto único).
- O composite devolve um dos três estados macro em linguagem natural (TIMING-01).
- A base temporal default semanal resampleia para W-FRI antes dos indicadores (TIMING-04).
- O caso-limite acima-da-MM200-com-ADX<20 → "sem_tendencia" está travado por golden (TEST-06).
</success_criteria>

<output>
After completion, create `.planning/phases/06-integra-o-na-engine-composite-alerta-cli/06-01-SUMMARY.md`
</output>
