---
phase: 04-encanamento-de-dados-s-rie-correta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/ingest/prices.py
  - src/analista/ingest/build.py
  - src/analista/core/fundamentals.py
  - tests/test_ingest_ohlc.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]

must_haves:
  truths:
    - "O frame OHLCV de 5 anos que o Yahoo já baixa fica preservado em DadosMercado.ohlc (nenhuma chamada de rede extra)."
    - "Existe uma série OHLCV ajustada por splits (não por dividendos) em DadosMercado.ohlc_ajustado, derivada da coluna Stock Splits do próprio hist."
    - "Os dois campos novos são propagados até CompanyData (ohlc e ohlc_ajustado)."
    - "Quando hist vem vazio/None, ambos os campos ficam None sem quebrar coletar_mercado (espelha GRAF-03)."
    - "serie_precos (Close nominal p/ o gráfico) permanece inalterado e coexiste com ohlc."
  artifacts:
    - path: "src/analista/ingest/prices.py"
      provides: "Campos ohlc/ohlc_ajustado em DadosMercado + helper puro _ajustar_por_split + atribuição no bloco hist"
      contains: "_ajustar_por_split"
    - path: "src/analista/ingest/build.py"
      provides: "Cópia c.ohlc = dm.ohlc e c.ohlc_ajustado = dm.ohlc_ajustado em montar_empresa"
      contains: "c.ohlc"
    - path: "src/analista/core/fundamentals.py"
      provides: "Campos ohlc/ohlc_ajustado no dataclass CompanyData"
      contains: "ohlc_ajustado"
    - path: "tests/test_ingest_ohlc.py"
      provides: "Testes offline da preservação do frame, da função de split e da degradação graciosa"
      contains: "_ajustar_por_split"
  key_links:
    - from: "src/analista/ingest/prices.py"
      to: "src/analista/ingest/build.py"
      via: "campos dm.ohlc / dm.ohlc_ajustado lidos em montar_empresa"
      pattern: "c\\.ohlc"
    - from: "src/analista/ingest/build.py"
      to: "src/analista/core/fundamentals.py"
      via: "atribuição para CompanyData"
      pattern: "ohlc_ajustado"
---

<objective>
Preservar o frame OHLCV nominal que o Yahoo já baixa em `coletar_mercado` e disponibilizá-lo na engine via um novo campo `ohlc`, além de derivar — por função pura — uma série/frame **ajustado por splits** (`ohlc_ajustado`) a partir da coluna `Stock Splits` que já vem dentro do mesmo `hist`. Espelha 1:1 o fluxo existente de `serie_precos` da v1.1.

Purpose: Os indicadores técnicos da Phase 5 precisam de uma terceira base de preço (split-only-adjusted, sem proventos) que não existe pronta no Yahoo; e o frame OHLCV completo, hoje descartado, precisa chegar à engine sem nenhuma nova chamada de rede (DATA-01) e na base correta (DATA-02, decisão CR-01).

Output: Campos `ohlc` e `ohlc_ajustado` em `DadosMercado` e `CompanyData`, função pura `_ajustar_por_split`, e testes offline cobrindo preservação, ajuste por split e degradação graciosa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-encanamento-de-dados-s-rie-correta/04-CONTEXT.md
@.planning/phases/04-encanamento-de-dados-s-rie-correta/04-PATTERNS.md

<interfaces>
<!-- Contratos existentes que o executor deve espelhar (extraídos do codebase). Não explorar; usar diretamente. -->

DadosMercado (src/analista/ingest/prices.py:46-58) — dataclass de saída de coletar_mercado.
Campo blueprint a replicar (linha 58):
    serie_precos: Optional["pd.Series"] = None  # close diário 5a (índice = datas) p/ o gráfico

Helper puro blueprint (src/analista/ingest/prices.py:61-65) — _retornos_mensais(precos) -> list:
função module-level pura, recebe série/frame, retorna estrutura derivada, sem efeitos colaterais.

Bloco onde o hist já está em memória (src/analista/ingest/prices.py:100-112):
    try:
        hist = tk.history(period="5y", auto_adjust=False)
    except Exception:
        hist = None
    if hist is not None and not hist.empty:
        nominal = hist["Close"].dropna()
        ...
        dm.serie_precos = nominal
        ...
        ult_ano = hist.tail(252)
        dm.volume_financeiro_diario = float((ult_ano["Close"] * ult_ano["Volume"]).mean())

Bloco de cópia de campos de mercado (src/analista/ingest/build.py:35-42):
    c.serie_precos = dm.serie_precos
    c.eh_concessionaria = any(...)

CompanyData (src/analista/core/fundamentals.py:19-45) — campo blueprint (linha 45):
    serie_precos: Optional["pd.Series"] = None  # close diário 5a (índice = datas) p/ o gráfico

Fixture/stub de teste offline (tests/test_ingest_resolucao.py:91-132) — _hist_fake(),
_TkComHist, monkeypatch de prices._yf / prices._fetch_info / prices.time.sleep.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Função pura _ajustar_por_split + testes offline</name>
  <files>src/analista/ingest/prices.py, tests/test_ingest_ohlc.py</files>
  <read_first>
    - src/analista/ingest/prices.py (ler 46-153 — DadosMercado, _retornos_mensais como blueprint de helper puro, e o bloco hist 100-112)
    - tests/test_ingest_resolucao.py (ler 88-132 — _hist_fake, _TkComHist, padrão de monkeypatch offline)
    - .planning/phases/04-encanamento-de-dados-s-rie-correta/04-PATTERNS.md (seção "Função pura de ajuste por split" e "tests/test_ingest_ohlc.py")
  </read_first>
  <behavior>
    - Sem coluna "Stock Splits" ou com 0 eventos: ohlc_ajustado == frame nominal (todos OHLC iguais ao hist de entrada).
    - Com 1 evento de split fator 2.0 numa data intermediária: a ponta recente (após o último split) tem fator cumulativo = 1, logo Open/High/Low/Close da cauda coincidem com o nominal; as datas ANTERIORES ao split ficam divididas pelo fator cumulativo 2.0 (preços pré-split escalados para baixo), Volume multiplicado por 2.0; sem salto/descontinuidade na data do split.
    - Função é pura: não muta o frame de entrada (hist original inalterado após a chamada) e não faz chamadas de rede.
    - Frame vazio: retorna None (ou frame vazio coerente) sem estourar.
  </behavior>
  <action>
    Criar helper module-level puro `_ajustar_por_split(hist: "pd.DataFrame") -> "pd.DataFrame"` em prices.py, à imagem de `_retornos_mensais` (função pura, import tardio `import pandas as pd` local se precisar de API pandas explícita). Regra de negócio (D-05): a partir da coluna "Stock Splits" do hist (valor 0.0 nos dias sem evento, fator > 0 no dia do split), computar o fator de split CUMULATIVO de trás para frente — fator = produto dos splits que ocorrem APÓS cada data — de modo que após o último split o fator seja 1.0 e a ponta recente da série ajustada coincida com a nominal. Multiplicar Open/High/Low/Close por (1/fator_cumulativo) nas datas pré-split (preços antigos escalados para a base recente) e dividir Volume pelo mesmo fator no sentido inverso (Volume pré-split multiplicado pelo fator). Preservar as colunas Adj Close/Dividends/Stock Splits como vieram (não recalculá-las) ou copiá-las inalteradas — o consumidor de indicadores usará O/H/L/C/Volume ajustados. NUNCA usar "Adj Close" como base (mistura proventos — anti-pattern explícito no CONTEXT). Tratar ausência da coluna "Stock Splits" e frame vazio sem estourar (retornar nominal inalterado e None respectivamente). Não mutar o frame de entrada (operar sobre cópia).

    Criar tests/test_ingest_ohlc.py com testes offline (zero rede), reaproveitando o padrão de _hist_fake/_TkComHist e monkeypatch de test_ingest_resolucao.py. Construir uma fixture de hist com colunas Open/High/Low/Close/Adj Close/Volume/Stock Splits e um evento de split intermediário. Cobrir: (1) ponta recente do ohlc_ajustado == nominal (fator 1 pós-último split); (2) datas pré-split escaladas pelo fator cumulativo correto, sem salto na data do split; (3) 0 eventos / sem coluna Stock Splits → ajustado == nominal; (4) função não muta o frame de entrada.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_ingest_ohlc.py -q</automated>
  </verify>
  <acceptance_criteria>
    - prices.py contém `def _ajustar_por_split(`
    - tests/test_ingest_ohlc.py existe e contém `_ajustar_por_split`
    - `.venv/bin/pytest tests/test_ingest_ohlc.py -q` exits 0 (todos os testes do arquivo passam)
    - Nenhum teste do arquivo usa rede real (todos via monkeypatch / fixtures locais)
    - `grep -c "Adj Close" src/analista/ingest/prices.py` não cresce dentro de _ajustar_por_split como base de ajuste (a função usa Stock Splits, não Adj Close)
  </acceptance_criteria>
  <done>_ajustar_por_split é pura, deriva o ajuste da coluna Stock Splits, mantém a ponta recente == nominal, escala corretamente as datas pré-split e os testes offline do arquivo passam.</done>
</task>

<task type="auto">
  <name>Task 2: Encanamento dos campos ohlc/ohlc_ajustado (ingest → build → CompanyData) + teste de fluxo</name>
  <files>src/analista/ingest/prices.py, src/analista/ingest/build.py, src/analista/core/fundamentals.py, tests/test_ingest_ohlc.py</files>
  <read_first>
    - src/analista/ingest/prices.py (linhas 46-58 DadosMercado, 100-112 bloco hist)
    - src/analista/ingest/build.py (linhas 35-42 cópia de campos de mercado)
    - src/analista/core/fundamentals.py (linhas 19-45 CompanyData)
    - tests/test_ingest_resolucao.py (88-132 — padrão de teste de coletar_mercado e degradação None)
    - .planning/phases/04-encanamento-de-dados-s-rie-correta/04-PATTERNS.md (seções de prices.py, build.py, fundamentals.py)
  </read_first>
  <action>
    Em src/analista/ingest/prices.py, dataclass DadosMercado, logo após o campo `serie_precos` (linha 58), declarar dois campos novos no mesmo estilo (tipo entre aspas para forward-ref de pandas, comentário inline em PT): `ohlc: Optional["pd.DataFrame"] = None` (comentário: frame OHLCV nominal 5a, Yahoo cru, auto_adjust=False) e `ohlc_ajustado: Optional["pd.DataFrame"] = None` (comentário: OHLCV split-only-adjusted p/ indicadores, Phase 5). Dentro do bloco `if hist is not None and not hist.empty:` (linhas 105-112), logo após `dm.serie_precos = nominal`, atribuir `dm.ohlc = hist` (preservar o frame cru completo — D-01, nada descartado) e `dm.ohlc_ajustado = _ajustar_por_split(hist)` (D-03/D-05). Não tocar em serie_precos, preco_atual, volume, beta ou qualquer outro campo. A degradação graciosa já é coberta pelo guard existente: quando hist é None/vazio, o bloco não executa e ambos os campos permanecem None (D-06).

    Em src/analista/ingest/build.py, montar_empresa, logo após `c.serie_precos = dm.serie_precos` (linha 41), adicionar `c.ohlc = dm.ohlc` e `c.ohlc_ajustado = dm.ohlc_ajustado` — cópia direta, sem lógica condicional (campos já são None na origem quando ausentes — D-02).

    Em src/analista/core/fundamentals.py, dataclass CompanyData, logo após o campo `serie_precos` (linha 45), declarar `ohlc: Optional["pd.DataFrame"] = None` e `ohlc_ajustado: Optional["pd.DataFrame"] = None` no mesmo estilo de comentário/typing. Nenhum método de CompanyData muda.

    Estender tests/test_ingest_ohlc.py com testes de fluxo de coletar_mercado (offline, via monkeypatch do padrão existente): (1) quando há hist, dm.ohlc é o frame cru completo (mesmas colunas do hist) e dm.ohlc_ajustado não é None; (2) quando hist vem vazio, dm.ohlc is None e dm.ohlc_ajustado is None (D-06); (3) serie_precos permanece igual ao comportamento anterior (Close nominal) — não regrediu.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_ingest_ohlc.py tests/test_ingest_resolucao.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ohlc_ajustado" src/analista/ingest/prices.py` >= 2 (declaração + atribuição)
    - `grep -q "c.ohlc = dm.ohlc" src/analista/ingest/build.py` (cópia presente)
    - `grep -q "c.ohlc_ajustado = dm.ohlc_ajustado" src/analista/ingest/build.py`
    - `grep -c "ohlc" src/analista/core/fundamentals.py` >= 2 (ambos os campos declarados)
    - serie_precos continua presente e inalterado em prices.py, build.py e fundamentals.py (linhas originais intactas)
    - `.venv/bin/pytest tests/test_ingest_ohlc.py tests/test_ingest_resolucao.py -q` exits 0
  </acceptance_criteria>
  <done>O frame OHLCV nominal chega a CompanyData.ohlc e o split-adjusted a CompanyData.ohlc_ajustado, ambos None quando o Yahoo falha, sem nenhuma alteração em serie_precos ou em qualquer fórmula de valuation; testes de fluxo passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo Finance → ingest (`coletar_mercado`) | Fronteira já existente; o `hist` é buscado pela v1.1. Esta fase NÃO adiciona nenhuma chamada de rede nova (D-04): reusa o `hist` já em memória. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Denial of Service | `_ajustar_por_split` sobre `hist` da Yahoo (frame possivelmente malformado/vazio) | mitigate | Função pura tolera coluna "Stock Splits" ausente e frame vazio sem estourar (retorna nominal/None); guard `if hist is not None and not hist.empty` já existente cobre fetch falho (degradação para None — DATA-03). |
| T-04-02 | Information Disclosure / Elevation | (N/A) | accept | Fase é transformação puramente em memória de dados já buscados; sem novo input de usuário, sem nova superfície de rede, sem persistência, sem secrets. Sem ameaça incremental. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_ingest_ohlc.py tests/test_ingest_resolucao.py -q` passa (testes da fase + regressão de ingest).
- A suíte completa de golden tests fica verde — verificada explicitamente no plano 04-02 (TEST-07).
- Nenhuma nova chamada de rede: `_ajustar_por_split` opera sobre o `hist` já recebido; nenhum `tk.splits` / refetch adicionado.
</verification>

<success_criteria>
- DATA-01: `DadosMercado.ohlc` e `CompanyData.ohlc` carregam o frame OHLCV nominal de 5 anos sem chamada de rede extra.
- DATA-02: `ohlc_ajustado` é split-only-adjusted (derivado de Stock Splits), não dividend-adjusted; `serie_precos` (Close nominal, eixo do gráfico) intacto (CR-01).
- DATA-03: hist vazio/None → ambos os campos None, sem quebrar coletar_mercado.
- Nenhuma fórmula de valuation alterada (campos puramente aditivos).
</success_criteria>

<output>
After completion, create `.planning/phases/04-encanamento-de-dados-s-rie-correta/04-01-SUMMARY.md`
</output>
