---
phase: 04-encanamento-de-dados-s-rie-correta
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/test_ingest_ohlc.py
autonomous: false
requirements: [DATA-02, TEST-07]

must_haves:
  truths:
    - "A série split-adjusted, validada no ITSA4 (5 eventos de split em 5 anos), não gera saltos/cruzamentos espúrios nas datas de split."
    - "A ponta recente da série ajustada do ITSA4 coincide com a série nominal (fator cumulativo = 1 após o último split)."
    - "Os 64 golden tests de valuation existentes continuam verdes após o encanamento (TEST-07)."
  artifacts:
    - path: "tests/test_ingest_ohlc.py"
      provides: "Asserção da validação ITSA4 (offline via fixture com os 5 splits reais) — sem salto na data do split"
      contains: "ITSA4"
  key_links:
    - from: "tests/test_ingest_ohlc.py"
      to: "src/analista/ingest/prices.py"
      via: "_ajustar_por_split aplicada à fixture multi-split estilo ITSA4"
      pattern: "_ajustar_por_split"
---

<objective>
Provar que o ajuste por split implementado em 04-01 está correto sob estresse de múltiplos eventos (ITSA4, 5 splits/bonificações em 5 anos) e confirmar o invariante do marco: os 64 golden tests de valuation continuam verdes após o encanamento.

Purpose: O critério de aceite #2 da fase exige validação num ticker com split conhecido antes de fechar — ITSA4 é o ticker escolhido (D-08) por ter 5 eventos recorrentes que estressam o fator cumulativo. E TEST-07 é o invariante de fidelidade do marco: nenhuma fórmula do livro pode mudar.

Output: Teste de validação multi-split (estilo ITSA4) anexado a tests/test_ingest_ohlc.py, suíte golden verde (64 passed), e um checkpoint de validação real de rede confirmando o comportamento no ticker de produção.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-encanamento-de-dados-s-rie-correta/04-CONTEXT.md
@.planning/phases/04-encanamento-de-dados-s-rie-correta/04-PATTERNS.md
@.planning/phases/04-encanamento-de-dados-s-rie-correta/04-01-SUMMARY.md

<interfaces>
<!-- Da entrega 04-01 (já implementada): -->
src/analista/ingest/prices.py :: _ajustar_por_split(hist: "pd.DataFrame") -> "pd.DataFrame"
src/analista/ingest/prices.py :: DadosMercado.ohlc, DadosMercado.ohlc_ajustado
src/analista/core/fundamentals.py :: CompanyData.ohlc, CompanyData.ohlc_ajustado

Eventos de split reais do ITSA4 verificados em runtime (CONTEXT 04 <specifics>):
2021-12, 2022-11, 2023-11, 2024-12, 2025-12 (5 eventos). TAEE4 = 0 eventos (controle).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teste de validação multi-split estilo ITSA4 (offline) + suíte golden verde</name>
  <files>tests/test_ingest_ohlc.py</files>
  <read_first>
    - src/analista/ingest/prices.py (a função _ajustar_por_split entregue em 04-01)
    - tests/test_ingest_ohlc.py (os testes de 04-01 — reusar fixtures/padrão)
    - tests/test_ingest_resolucao.py (88-132 — padrão de fixture e monkeypatch offline)
    - .planning/phases/04-encanamento-de-dados-s-rie-correta/04-CONTEXT.md (D-08 — ITSA4, 5 eventos; critério de aceite #2)
  </read_first>
  <action>
    Anexar a tests/test_ingest_ohlc.py um teste de validação que constrói uma fixture de hist com 5 eventos de split em datas distintas (estilo ITSA4: aprox. dez/2021, nov/2022, nov/2023, dez/2024, dez/2025), índice diário cobrindo ~5 anos, com Open/High/Low/Close/Volume coerentes e fatores de split plausíveis na coluna "Stock Splits". Aplicar _ajustar_por_split à fixture e asseverar: (1) a ponta recente (após o último evento) do frame ajustado coincide com o nominal — fator cumulativo = 1; (2) nas datas exatas de cada split não há salto/descontinuidade espúria: a razão Close_ajustado[dia_split] / Close_ajustado[dia_split - 1] permanece próxima da razão "orgânica" do nominal corrigida pelo fator (sem o degrau que apareceria na série nominal crua), ou seja, a série ajustada é contínua através do evento dentro de uma tolerância pequena; (3) o fator cumulativo aplicado nas datas anteriores ao primeiro split é o produto dos 5 fatores. Nomear o teste e os comentários referenciando ITSA4/D-08. Manter o teste 100% offline (fixture sintética com os 5 eventos), sem rede — a validação de rede real é o checkpoint da Task 2.

    Após escrever o teste, rodar a suíte COMPLETA e confirmar TEST-07: 64 golden tests de valuation continuam verdes (mais os novos testes de ingest da fase). Se algum golden test de valuation quebrar, é regressão — investigar e corrigir (a fase é aditiva; nenhuma fórmula do livro deve ter mudado).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_ingest_ohlc.py contém um teste com `ITSA4` (ou referência D-08) cobrindo 5 eventos de split
    - `.venv/bin/pytest tests/ -q` exits 0
    - A contagem de testes de valuation golden permanece >= 64 passed (nenhum removido/quebrado): `.venv/bin/pytest tests/test_ddm.py tests/test_multiples.py tests/test_screening.py tests/test_comparables.py tests/test_fundamentals_consistencia.py tests/test_consistencia_modos.py -q` exits 0
    - O novo teste é offline (não usa rede; usa fixture sintética com os 5 eventos)
  </acceptance_criteria>
  <done>O ajuste por split está validado sob 5 eventos sem cruzamentos/saltos espúrios e a ponta recente coincide com o nominal; a suíte completa (64 golden + testes de ingest da fase) passa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validação de rede real no ITSA4 (critério de aceite #2)</name>
  <what-built>
    Encanamento de `ohlc`/`ohlc_ajustado` (04-01) + função `_ajustar_por_split`, validados offline por fixtures. Resta confirmar o comportamento contra o Yahoo real com o ticker de produção ITSA4 (5 splits), conforme D-08 — algo que os testes offline deliberadamente não fazem (zero rede).
  </what-built>
  <how-to-verify>
    1. Com a `.venv` ativa, rodar no REPL/script de uma linha:
       `.venv/bin/python -c "from src.analista.ingest import prices; dm = prices.coletar_mercado('ITSA4'); n = dm.ohlc; a = dm.ohlc_ajustado; print('nominal cols:', list(n.columns)); print('ajustado None?', a is None); print('ponta recente igual?', float(n['Close'].iloc[-1]) == float(a['Close'].iloc[-1])); print('Close pre-split (head):', float(n['Close'].iloc[0]), '->', float(a['Close'].iloc[0]))"`
       (ajustar o import à forma usada no projeto se necessário — ver como cli.py importa prices).
    2. Confirmar: `dm.ohlc` traz as colunas OHLCV completas (Open/High/Low/Close/Adj Close/Volume/Stock Splits); `dm.ohlc_ajustado` NÃO é None.
    3. Confirmar: a ponta recente (`Close` mais recente) do ajustado coincide com a nominal (fator cumulativo = 1 após o último split de dez/2025).
    4. Confirmar: o `Close` ajustado mais antigo (anterior ao primeiro split de dez/2021) é MENOR que o nominal correspondente (escalado pelo produto dos 5 fatores) — sem salto abrupto nas datas dos 5 splits.
    5. (Opcional) Repetir com TAEE4 (0 splits) e confirmar que ajustado == nominal.
  </how-to-verify>
  <resume-signal>Digite "aprovado" se a série split-adjusted do ITSA4 estiver coerente (ponta == nominal, sem saltos espúrios), ou descreva a divergência observada.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo Finance → ingest (validação real ITSA4) | A Task 2 faz UMA leitura de rede de validação manual contra o ticker real; é leitura read-only de dados públicos, sem novo input de usuário nem persistência. A fase em si não adiciona chamada de rede ao fluxo de produção (reusa o hist em memória). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | (N/A) | Validação/testes — sem código de produção novo neste plano | accept | Plano só adiciona um teste offline e um checkpoint de validação manual; nenhuma nova superfície, input ou persistência. A robustez da transformação contra dados malformados já é mitigada por T-04-01 (plano 04-01). |
</threat_model>

<verification>
- `.venv/bin/pytest tests/ -q` exits 0 — suíte completa verde (64 golden + ingest da fase) — TEST-07.
- Checkpoint humano confirma a série split-adjusted do ITSA4 real (ponta == nominal, sem saltos nas 5 datas de split) — DATA-02 / critério de aceite #2.
</verification>

<success_criteria>
- DATA-02 (validação): ajuste por split correto sob 5 eventos (ITSA4), sem cruzamentos/saltos espúrios; ponta recente == nominal.
- TEST-07: os 64 golden tests de valuation permanecem verdes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-encanamento-de-dados-s-rie-correta/04-02-SUMMARY.md`
</output>
