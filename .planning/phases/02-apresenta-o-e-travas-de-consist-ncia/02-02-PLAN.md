---
phase: 02-apresenta-o-e-travas-de-consist-ncia
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/test_consistencia_modos.py
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "Existe um teste automatizado que monta UMA CompanyData à mão (sem rede) e afirma que ROE/payout/veredito são coerentes entre os 3 modos"
    - "O teste afirma igualdade exata de ROE (c.roe(ult)) entre o caminho Ranking e o caminho Analisar"
    - "O teste afirma igualdade exata de payout_valuation entre o DDM do Analisar e o vetor DP do Ranking"
    - "O teste afirma a DIREÇÃO (barato/caro) coerente do veredito entre DDM e regressão (não igualdade numérica)"
    - "pytest tests/ -q termina verde (44 golden + os novos), sem regressão"
  artifacts:
    - path: "tests/test_consistencia_modos.py"
      provides: "Trava de consistência cross-modo (TEST-01)"
      contains: "def test_"
  key_links:
    - from: "tests/test_consistencia_modos.py"
      to: "analista.report.report.analisar_acao(c, cfg)"
      via: "caminho Analisar"
      pattern: "analisar_acao"
    - from: "tests/test_consistencia_modos.py"
      to: "c.roe(c.ultimo_ano()) e c.payout_valuation()"
      via: "caminho Ranking (mesmas funções da engine)"
      pattern: "payout_valuation|ultimo_ano"
    - from: "tests/test_consistencia_modos.py"
      to: "config.yaml"
      via: "loader espelhando cli.py (yaml.safe_load), sem @st.cache_data"
      pattern: "yaml.safe_load|config.yaml"
---

<objective>
Travar com pytest a consistência cross-modo prometida pela Fase 1 (TEST-01) e garantir
explicitamente que os golden existentes continuam verdes (TEST-02).

Purpose: A mesma empresa (mesmo dado de entrada) deve produzir ROE/payout coerentes nos 3 modos
e veredito de direção coerente entre DDM (Analisar) e regressão (Ranking). É a rede de segurança
que impede uma futura divergência de reintroduzir os bugs CR-02/CR-03/WR-03 fechados na Fase 1.

Output: tests/test_consistencia_modos.py (novo), montando CompanyData à mão sem rede, espelhando
a fixture _empresa_solida de tests/test_screening.py; e a suíte completa verde (44 golden + novos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-apresenta-o-e-travas-de-consist-ncia/02-PATTERNS.md

<interfaces>
<!-- Pontos de engine que os 3 modos consomem (já unificados na Fase 1). -->

Caminho Analisar:
- report.analisar_acao(c, cfg) -> AnaliseAcao
  - .multiplos["ROE"]          # = c.roe(ult)
  - .multiplos["DP (payout)"]  # = c.payout(ult), último ano cru
  - .veredito                  # texto: começa com "SUBAVALIADA"/"SOBREAVALIADA"/"NO INTERVALO"
  - .vmin / .vmax              # intervalo intrínseco único
  - exige cfg (dict) carregado de config.yaml

Caminho Ranking (funções diretas da engine):
- c.roe(c.ultimo_ano())        # mesmo c.roe(ult) usado pelo Analisar (report.py:53)
- c.payout_valuation()         # mesmo payout canônico usado pelo DDM (report.py:97)
- c.payout(c.ultimo_ano())     # último ano cru = multiplos["DP (payout)"]
- comparables.ajustar_regressao_pl(PL, DP, ROE) -> reg ou None se n<4 (comparables.py:94)
- comparables.preco_alvo_por_regressao(reg, dp, roe, lpa, preco) -> PrecoAlvo ou None
  - PrecoAlvo.subavaliada: bool  (direção barato/caro do Ranking)

Fixture-modelo (tests/test_screening.py:7-26): _empresa_solida — CompanyData de 10 anos, todos os
campos, sem rede. É a mais completa do harness; reusar/inspirar.

Loader de config (espelhar cli.py, SEM @st.cache_data): ler config.yaml da raiz do projeto via
yaml.safe_load. No teste em tests/, a raiz é os.path.dirname(os.path.dirname(__file__)).
Harness: pytest 9.0.3, pyproject.toml com pythonpath=["src"], testpaths=["tests"]. SEM conftest.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever tests/test_consistencia_modos.py (TEST-01)</name>
  <files>tests/test_consistencia_modos.py</files>
  <read_first>
    - tests/test_screening.py (fixture _empresa_solida linhas 7-26; estilo de asserção 29-49; imports 1-4)
    - .planning/phases/02-apresenta-o-e-travas-de-consist-ncia/02-PATTERNS.md (seção "tests/test_consistencia_modos.py (NOVO) — TEST-01 cross-modo": imports, fixture, loader de CFG, asserções)
    - .planning/phases/02-apresenta-o-e-travas-de-consist-ncia/02-RESEARCH.md (seção "Como TEST-01 (coerência cross-modo) deve plugar" + Open Question #2 sobre ≥4 fixtures)
    - src/analista/cli.py (loader de config: yaml.safe_load de config.yaml na raiz — espelhar SEM cache)
  </read_first>
  <action>
    Criar tests/test_consistencia_modos.py. Imports: `from analista.core.fundamentals import CompanyData`,
    `from analista.report import report`, `from analista.core import comparables as cmp` (e
    `multiples as mult` se necessário). Sem prefixo `src.` (pythonpath=["src"]).

    Fixture: copiar/adaptar _empresa_solida de test_screening.py (CompanyData de 10 anos, à mão, sem
    rede). NÃO chamar montar()/rede.

    Loader de cfg: definir um helper que lê config.yaml da raiz do projeto via yaml.safe_load,
    espelhando cli.py (SEM @st.cache_data). Caminho:
    os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "config.yaml").
    analisar_acao(c, cfg) exige cfg dict.

    Testes de coerência (mínimo obrigatório — sem regressão, não precisa de ≥4 fixtures):
    - test_roe_coerente_analisar_vs_ranking: ult = c.ultimo_ano(); a = report.analisar_acao(c, cfg);
      assert a.multiplos["ROE"] == c.roe(ult)  (ambos c.roe(ult); igualdade exata)
    - test_payout_valuation_coerente: assert valor de payout_valuation usado pelo Analisar (DDM)
      == c.payout_valuation() do caminho Ranking. Como ambos chamam a mesma função canônica, afirmar
      c.payout_valuation() == c.payout_valuation() é trivial; em vez disso afirmar a SEMÂNTICA:
      a.multiplos["DP (payout)"] == c.payout(ult) (último ano cru, report.py:56) E que
      c.payout_valuation() é distinto/igual de forma documentada (apenas afirmar tipo float ou
      igualdade com c.payout_valuation()). Foco: garantir que o Analisar expõe o último ano cru
      enquanto o canônico de valuation é payout_valuation() — esses são os dois números do PAYOUT-02.
    - test_veredito_direcao_coerente (ESTENDIDO, requer ≥4 fixtures para a regressão rodar):
      montar ≥4 CompanyData (ex.: variações de _empresa_solida com tickers distintos). Montar os
      vetores PL/DP/ROE como em app.py:256-266, chamar cmp.ajustar_regressao_pl(PL, DP, ROE); se
      reg não-None, para uma empresa: pa = cmp.preco_alvo_por_regressao(reg, c.payout_valuation(),
      c.roe(ult), c.lpa(ult), c.preco_atual). Afirmar DIREÇÃO coerente: se a.veredito começa com
      "SUBAVALIADA" então (em geral) pa.subavaliada é True — afirmar SINAL/direção, NÃO igualdade
      numérica (DDM ≠ regressão por construção; Assumption A2 do RESEARCH). Se a regressão não puder
      ser forçada a um sinal estável, restringir a asserção a coerência fraca (ex.: ambos definidos,
      ou pular com pytest.skip documentado) — mas preferir ≥4 fixtures que produzam um sinal claro.

    Cobrir no mínimo os dois primeiros testes (não exigem regressão). O terceiro (direção do veredito)
    é desejável; se não estabilizar, deixar a asserção mínima documentada — NUNCA usar "v1"/placeholder
    como desculpa para não cobrir ROE+payout cross-modo, que são obrigatórios (TEST-01).
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && .venv/bin/pytest tests/test_consistencia_modos.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_consistencia_modos.py -q` sai 0
    - O arquivo monta CompanyData à mão (sem chamar montar()/rede): `grep -c "montar(" tests/test_consistencia_modos.py` == 0
    - O arquivo carrega config.yaml via yaml.safe_load (não via streamlit): contém "yaml.safe_load" e "config.yaml"; NÃO contém "import streamlit" nem "@st.cache_data"
    - Há pelo menos 2 funções `def test_` cobrindo ROE cross-modo e payout (último ano cru vs payout_valuation)
    - Asserção de ROE usa igualdade exata entre a.multiplos["ROE"] e c.roe(ult)
    - Asserção de payout distingue c.payout(ult) (= multiplos["DP (payout)"]) de c.payout_valuation()
  </acceptance_criteria>
  <done>
    tests/test_consistencia_modos.py existe, monta fixtures à mão sem rede, carrega config.yaml como
    o cli.py, e passa afirmando coerência cross-modo de ROE/payout (e direção do veredito quando
    a regressão roda com ≥4 fixtures).
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirmar suíte golden verde (TEST-02)</name>
  <files>tests/test_consistencia_modos.py</files>
  <read_first>
    - .planning/phases/02-apresenta-o-e-travas-de-consist-ncia/02-RESEARCH.md (seção "Estrutura de testes" — baseline 44 passed; Pitfall 4 sobre não quebrar golden)
  </read_first>
  <action>
    Rodar a suíte completa e confirmar que os 44 golden existentes
    (test_ddm/test_multiples/test_comparables/test_screening/test_fundamentals_consistencia)
    continuam passando junto com os novos testes de consistência. Nenhum arquivo de produção é
    alterado neste plano (só o teste novo); se algum golden falhar, é sinal de que algo na cadeia
    de plano 02-01 quebrou um contrato — investigar e reportar (NÃO ajustar golden para "passar").
    Esta task é a trava explícita TEST-02.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && .venv/bin/pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/ -q` sai 0
    - O total de passed é >= 44 + (nº de testes novos do plano), sem failed/errored
    - Nenhum golden existente foi editado para forçar passagem (apenas tests/test_consistencia_modos.py é novo)
  </acceptance_criteria>
  <done>pytest tests/ -q verde com os 44 golden + os testes de consistência novos; zero failed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Testes pytest offline montam CompanyData à mão; sem rede, sem entrada de usuário, sem persistência. Lê config.yaml (arquivo local versionado). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | tests/test_consistencia_modos.py | accept | Código de teste offline; fixtures à mão; sem leitura de input externo além de config.yaml (arquivo do repo). Risco nulo. |
| T-02-05 | Information Disclosure | leitura de config.yaml no teste | accept | config.yaml é configuração versionada sem segredos (parâmetros do método). Sem exposição. |

Nota: ASVS L1 sem categoria nova aplicável — não há authn/authz, rede, nem persistência nesta fase.
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_consistencia_modos.py -q` sai 0 (TEST-01)
- `.venv/bin/pytest tests/ -q` sai 0, >= 44 + novos passed, zero failed (TEST-02)
- tests/test_consistencia_modos.py não usa rede (grep "montar(" == 0) e carrega config.yaml via yaml.safe_load
</verification>

<success_criteria>
- Um teste automatizado garante ROE/payout/veredito coerentes entre os 3 modos para a mesma empresa (TEST-01)
- A suíte golden existente continua verde após todas as correções (TEST-02)
</success_criteria>

<output>
After completion, create `.planning/phases/02-apresenta-o-e-travas-de-consist-ncia/02-02-SUMMARY.md`
</output>
