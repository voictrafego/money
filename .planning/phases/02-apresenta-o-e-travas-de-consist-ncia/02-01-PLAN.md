---
phase: 02-apresenta-o-e-travas-de-consist-ncia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/glossario.py
  - app.py
autonomous: false
requirements: [ANO-01, PAYOUT-02, RANK-01]

must_haves:
  truths:
    - "No Garimpo, cada empresa exibe uma coluna 'Ano-base' com o ano-base efetivo (ultimo_ano)"
    - "No Ranking, cada empresa exibe uma coluna 'Ano-base' com o ano-base efetivo (ultimo_ano)"
    - "Na aba Múltiplos do Analisar, aparecem DUAS linhas: 'Payout (último ano)' e 'Payout p/ valuation (média 3a)'"
    - "No Ranking, quando a empresa é descartada da regressão (pa is None), as células mostram 'indisponível', não '—'"
    - "Os três tooltips novos (ano_base, payout_dual, indisponivel) existem no dict G de glossario.py e são acessíveis por h()"
    - "pytest tests/ -q continua verde (44+ passed) após as mudanças de apresentação"
  artifacts:
    - path: "src/analista/glossario.py"
      provides: "Tooltips ano_base, payout_dual, indisponivel no dict G"
      contains: "ano_base"
    - path: "app.py"
      provides: "Coluna Ano-base (Garimpo+Ranking), dual-payout (Analisar), 'indisponível' (Ranking)"
      contains: "Ano-base"
  key_links:
    - from: "app.py (dict rows do Garimpo, ~213)"
      to: "c.ultimo_ano()"
      via: "leitura de campo da engine"
      pattern: "ultimo_ano"
    - from: "app.py (dict rows do Ranking, ~282)"
      to: "next(c.ultimo_ano() for c in empresas if c.ticker == r['empresa'])"
      via: "idiom de lookup já usado em 'Preço atual'"
      pattern: "ultimo_ano"
    - from: "app.py (aba Múltiplos, ~125-132)"
      to: "c.payout_valuation()"
      via: "leitura do payout canônico (não recálculo)"
      pattern: "payout_valuation"
    - from: "app.py (branch pa is None, ~276-288)"
      to: "literal 'indisponível'"
      via: "substituição local do '—' ambíguo"
      pattern: "indisponível"
    - from: "app.py"
      to: "h('ano_base'), h('payout_dual')"
      via: "help=h(...)"
      pattern: "h\\(\"(ano_base|payout_dual|indisponivel)\"\\)"
---

<objective>
Conectar à UI (app.py) os campos que a engine canônica (Fase 1) já expõe, em três mudanças
aditivas de apresentação, e adicionar os três tooltips correspondentes ao glossário.

Purpose: O usuário precisa enxergar (a) qual ano-base cada empresa usa na comparação (ANO-01),
(b) os dois payouts quando divergem — exibido vs. usado no DDM (PAYOUT-02), e (c) "indisponível"
em vez de "—" quando uma empresa é descartada da regressão de preço-alvo (RANK-01). NENHUM cálculo
de método é feito em app.py — é leitura de campo + formatação, regra LOCKED da Fase 1.

Output: glossario.py com 3 chaves novas; app.py com coluna "Ano-base" no Garimpo e no Ranking,
duas linhas de payout rotuladas na aba Múltiplos do Analisar, e "indisponível" no branch pa is None
do Ranking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-apresenta-o-e-travas-de-consist-ncia/02-UI-SPEC.md
@.planning/phases/02-apresenta-o-e-travas-de-consist-ncia/02-PATTERNS.md

<interfaces>
<!-- Campos da engine que a UI deve LER (já expostos pela Fase 1). Não recomputar. -->

CompanyData (src/analista/core/fundamentals.py):
- ultimo_ano() -> Optional[int]            # maior ano com lucro coletado (ANO-01)
- payout(ano) -> Optional[float]           # payout CRU do ano, sem clamp (PAYOUT-02 lado "último ano")
- payout_valuation(janela=3) -> Optional[float]  # média 3a + clamp 1.0, canônico (PAYOUT-02 lado DDM)
- ano_dpa: Optional[int]                   # nice-to-have, pode ir no tooltip

AnaliseAcao (src/analista/report/report.py):
- multiplos["DP (payout)"] -> Optional[float]   # = c.payout(ult), último ano cru

PrecoAlvo (src/analista/core/comparables.py):
- preco_alvo_por_regressao(...) retorna None quando ROE/payout/lpa/preço faltam (comparables.py:129)
  → ESSE None é o gatilho do RANK-01.

Helpers de formatação (app.py:48-57) — devolvem "—" para None. NÃO alterar:
- fmt_pct(x, casas=1) / fmt_num(x, casas=2) / fmt_rs(x, casas=2)

Glossário (src/analista/glossario.py):
- dict module-level G: dict[str, str] (linha 11)
- h(chave) -> str | None (linha 102-104) — já importado em app.py:17
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar 3 tooltips ao glossário</name>
  <files>src/analista/glossario.py</files>
  <read_first>
    - src/analista/glossario.py (dict G integral, ver padrão das chaves "roe" e "ranking"; fechamento do dict ~linha 99; função h() ~linha 102-104 — NÃO tocar)
    - .planning/phases/02-apresenta-o-e-travas-de-consist-ncia/02-UI-SPEC.md (seção "Copywriting Contract" e "Tooltips a adicionar em glossario.py" — textos VERBATIM)
    - .planning/phases/02-apresenta-o-e-travas-de-consist-ncia/02-PATTERNS.md (seção "src/analista/glossario.py — 3 tooltips novos" — padrão de string)
  </read_first>
  <action>
    Inserir TRÊS chaves novas no dict module-level G de src/analista/glossario.py, antes do
    fechamento `}` (~linha 99), seguindo a organização por seções comentadas; criar um bloco
    `# ---- Fase 2: ano-base, dual-payout, indisponível ----`.

    Chaves e textos (copiar VERBATIM do UI-SPEC "Copywriting Contract"):
    - `ano_base` — texto do UI-SPEC ANO-01 (começa "**Ano-base** — último exercício (ano)...")
    - `payout_dual` — texto do UI-SPEC PAYOUT-02 (começa "**Por que dois payouts?** ...")
    - `indisponivel` — texto do UI-SPEC RANK-01 (começa "**indisponível** — esta empresa foi...")

    Usar string entre parênteses com concatenação implícita de literais (estilo das entradas
    existentes). NÃO alterar a função h(). NÃO mexer em nenhuma chave existente.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && .venv/bin/python -c "from analista.glossario import h; assert h('ano_base') and h('payout_dual') and h('indisponivel'), 'faltam chaves'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "from analista.glossario import G; assert all(k in G for k in ['ano_base','payout_dual','indisponivel'])"` sai 0 (pacote editable-installed; sem `sys.path.insert`)
    - h("ano_base"), h("payout_dual"), h("indisponivel") retornam string não-vazia
    - O texto de cada chave bate VERBATIM com o UI-SPEC (negrito **...** no início, pt-BR)
    - Nenhuma chave existente foi removida ou alterada (h() intacta)
  </acceptance_criteria>
  <done>As 3 chaves existem em G, acessíveis por h(), com texto verbatim do UI-SPEC; h() não foi tocada.</done>
</task>

<task type="auto">
  <name>Task 2: ANO-01 + PAYOUT-02 + RANK-01 em app.py</name>
  <files>app.py</files>
  <read_first>
    - app.py (integral; sítios-alvo: aba Múltiplos ~121-132; dict rows do Garimpo ~209-226; bloco Ranking ~274-290)
    - .planning/phases/02-apresenta-o-e-travas-de-consist-ncia/02-PATTERNS.md (seções "ANO-01 Garimpo", "ANO-01 Ranking", "RANK-01 indisponível", "PAYOUT-02 dois payouts rotulados" — código analogon com file:linha)
    - .planning/phases/02-apresenta-o-e-travas-de-consist-ncia/02-UI-SPEC.md (Copywriting Contract: rótulos e literais exatos)
  </read_first>
  <action>
    Três mudanças aditivas, lendo campos da engine — NUNCA recalcular método em app.py.

    ANO-01 (Garimpo, dict rows ~213-221): inserir a chave `"Ano-base": c.ultimo_ano()` no dict
    `rows.append({...})`. `c` é o CompanyData já em escopo no loop `for c in empresas:`. Quando
    ultimo_ano() é None, deixar o helper genérico mostrar "—" (não "indisponível"). Opcional:
    tooltip via st.column_config.Column("Ano-base", help=h("ano_base")) SEM alterar larguras/ordem
    das demais colunas; se preferir, manter st.dataframe simples e citar h("ano_base") num caption.

    ANO-01 (Ranking, dict rows ~282-289): inserir `"Ano-base": next(c.ultimo_ano() for c in empresas if c.ticker == r["empresa"])`, espelhando EXATAMENTE o idiom já usado em "Preço atual"
    (next(c.preco_atual for c in empresas if c.ticker == r["empresa"]), app.py:285). Aqui o loop é
    `for r in ranking` (dicts), por isso o lookup por ticker.

    RANK-01 (Ranking, branch ~276-288): refatorar o trecho que monta veredito/preço-alvo/upside
    para distinguir `pa is None`. Quando `pa is None`, usar literais (UI-SPEC, VERBATIM):
    Preço-alvo = "indisponível", Upside = "indisponível", Veredito = "indisponível (ROE/payout ausente)".
    Caso contrário, manter o comportamento atual (fmt_rs(pa.preco_alvo), fmt_pct(pa.upside) ou "—",
    "Subavaliada ✅"/"Cara 🔺" + " ⚠️ payout ajustado" se pa.payout_fora_faixa). Renderizar
    "indisponível" como texto simples na célula — SEM cor de erro (estado neutro, UI-SPEC Color).
    NÃO alterar fmt_rs/fmt_pct (app.py:48-57); a substituição é LOCAL no branch. NÃO confundir com
    o caso n<4 (regressão inteira não roda), já tratado por st.info em app.py:295.

    PAYOUT-02 (Analisar, aba Múltiplos ~125-132): desdobrar a linha única "DP (payout)" em DUAS
    linhas rotuladas. Ler ambos os campos: payout_ult = a.multiplos.get("DP (payout)") (= c.payout(ult))
    e payout_proj = c.payout_valuation() (média 3a + clamp; `c` está em escopo no bloco Analisar,
    app.py:87). No loop sobre a.multiplos, ao encontrar k == "DP (payout)", emitir
    ("Payout (último ano)", fmt_pct(payout_ult)) e logo após ("Payout p/ valuation (média 3a)",
    fmt_pct(payout_proj)) em vez da linha genérica; manter o resto do loop intacto. Wire o tooltip
    via help=h("payout_dual") no st.markdown("**Múltiplos (Cap. 10)**", ...) (app.py:124) — adicionar
    a referência sem remover h("tab_multiplos") (pode-se manter o existente; o payout_dual pode ir num
    st.caption próximo ou substituir conforme clareza, SEM perder o tooltip de múltiplos).
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && .venv/bin/python -c "import ast; ast.parse(open('app.py').read()); print('parse OK')" && grep -q "Ano-base" app.py && grep -q "Payout p/ valuation (média 3a)" app.py && grep -q "indisponível" app.py && grep -q "payout_valuation" app.py && echo "TODOS LITERAIS PRESENTES"</automated>
  </verify>
  <acceptance_criteria>
    - app.py compila: `.venv/bin/python -c "import ast; ast.parse(open('app.py').read())"` sai 0
    - `grep -c "Ano-base" app.py` >= 2 (Garimpo + Ranking; ou >=1 se a coluna for definida uma vez e reusada — mínimo 1 ocorrência por sítio)
    - app.py contém literal "Payout (último ano)" e "Payout p/ valuation (média 3a)"
    - app.py contém o literal "indisponível" e "indisponível (ROE/payout ausente)"
    - app.py contém "payout_valuation()" no bloco Analisar (leitura do canônico, não recálculo)
    - app.py contém o idiom `next(c.ultimo_ano() for c in empresas if c.ticker == r["empresa"])` no Ranking
    - fmt_pct/fmt_num/fmt_rs (app.py:48-57) NÃO foram alterados (continuam retornando "—" para None)
    - Nenhuma aritmética de payout/ROE/min-max nova em app.py (zero recálculo de método)
  </acceptance_criteria>
  <done>
    Garimpo e Ranking exibem coluna "Ano-base"; aba Múltiplos exibe os dois payouts rotulados;
    Ranking mostra "indisponível" quando pa is None; tooltips wired via h(); helpers fmt_* intactos;
    app.py compila.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Coluna "Ano-base" no Garimpo e no Ranking; duas linhas de payout rotuladas na aba "📈 Múltiplos
    & Crescimento" do Analisar; "indisponível" no Ranking quando uma empresa é descartada da regressão.
    Tooltips ano_base/payout_dual wired via ícone de ajuda.
  </what-built>
  <how-to-verify>
    1. Rodar o app: `.venv/bin/streamlit run app.py` (a partir da raiz do projeto).
    2. Modo "🔎 Analisar uma ação": ticker TAEE11 → Analisar. Na aba "📈 Múltiplos & Crescimento",
       confirmar DUAS linhas: "Payout (último ano)" e "Payout p/ valuation (média 3a)". Passar o
       mouse no ícone de ajuda dos múltiplos e ver o tooltip payout_dual.
    3. Modo "⛏️ Garimpar carteira (BSD)": Garimpar com os tickers default. Confirmar a coluna
       "Ano-base" com anos (ex.: 2024) e tooltip ano_base.
    4. Modo "📊 Ranking por múltiplos": Rankear com os tickers default. Confirmar a coluna "Ano-base".
       Se alguma empresa tiver ROE/payout faltante, confirmar "indisponível" (texto neutro, NÃO
       vermelho) em Preço-alvo/Upside/Veredito. Se todas têm dados, isso é esperado não aparecer —
       confirmar apenas que nenhuma célula faltante virou "—" ambíguo.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Esta fase é exibição read-only de dados já computados pela engine, num app Streamlit local sem auth. Não há nova entrada de usuário nem chamada de rede introduzida. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | app.py (leitura de campos da engine) | accept | Sem nova superfície: a UI só formata Optional[int]/Optional[float] já calculados pela Fase 1; nenhum input novo é parseado. Risco nulo de injeção. |
| T-02-02 | Information Disclosure | tooltips do glossário | accept | Tooltips contêm apenas texto educativo do método (livro), sem dado sensível. App local sem rede/segredo. |
| T-02-03 | Denial of Service | st.dataframe com coluna extra | accept | Coluna adicional num dataframe já renderizado; custo computacional desprezível, sem loop novo sobre rede. |

Nota: ASVS L1 não introduz categoria nova aplicável — sem authn/authz, sem entrada de rede nova,
sem persistência. Tickers já são normalizados (.strip().upper()) na borda (pré-existente).
</threat_model>

<verification>
- `.venv/bin/python -c "import ast; ast.parse(open('app.py').read())"` sai 0 (app.py compila)
- `.venv/bin/python -c "from analista.glossario import h; assert h('ano_base') and h('payout_dual') and h('indisponivel')"` sai 0
- app.py contém os literais: "Ano-base", "Payout (último ano)", "Payout p/ valuation (média 3a)", "indisponível", "payout_valuation"
- `.venv/bin/pytest tests/ -q` continua verde (44+ passed) — nenhuma mudança de apresentação quebra golden
- Checkpoint humano confirma os três comportamentos no navegador
</verification>

<success_criteria>
- Ranking e Garimpo exibem o ano-base efetivo (ultimo_ano) por empresa (ANO-01)
- Aba Múltiplos do Analisar mostra os dois payouts rotulados sem ambiguidade (PAYOUT-02)
- Ranking mostra "indisponível" (neutro, não "—") quando pa is None (RANK-01)
- 3 tooltips novos no glossário, wired via h()
- pytest tests/ -q verde após as mudanças
</success_criteria>

<output>
After completion, create `.planning/phases/02-apresenta-o-e-travas-de-consist-ncia/02-01-SUMMARY.md`
</output>
