---
phase: 03-go-live-e2e-pago
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/projects/lazari-capital/templates/marketing/landing.html
  - ~/projects/lazari-capital/templates/marketing/base_marketing.html
  - ~/projects/lazari-capital/apps/users/tests/test_landing.py
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "Um visitante anônimo em www.lazaricapital.com.br vê a landing da marca Lazari Capital (o que é, preço R$19,90/mês, disclaimer educacional, CTA cadastrar/entrar)"
    - "A landing NÃO menciona mais Pocket Leads / CRM / Kanban / leads (conteúdo herdado do crm-voic)"
    - "Os CTAs apontam para /signup/ (cadastro) e /entrar/ (login) do Django"
  artifacts:
    - path: "~/projects/lazari-capital/templates/marketing/landing.html"
      provides: "Landing mínima de vendas Lazari Capital (D-07/D-08)"
      contains: "R$ 19,90"
    - path: "~/projects/lazari-capital/apps/users/tests/test_landing.py"
      provides: "Regressão da landing re-brandada"
  key_links:
    - from: "templates/marketing/landing.html"
      to: "/signup/ e /entrar/"
      via: "href {{ app_base_url }}/signup/ e /entrar/"
      pattern: "/(signup|entrar)/"
---

<objective>
Re-brandar a landing pública em `/` (hoje ainda é a vitrine Pocket Leads/CRM herdada do crm-voic)
para a **landing mínima de vendas da Lazari Capital** (D-07/D-08): o que é (análise de dividendos
pelo método do livro), preço **R$ 19,90/mês**, disclaimer **software educacional — sem recomendação
(CVM)**, e CTA **Cadastrar** (`/signup/`) + **Entrar** (`/entrar/`). Plano único PRO — sem os 3
planos/tiers herdados.

Purpose: sem uma landing apresentável na estreia do domínio, `www.lazaricapital.com.br` mostraria
conteúdo de CRM de leads — quebra a marca. É decisão travada (D-07) e entra na imagem
`lazari-web:latest` que o deploy (03-04) builda.
Output: `landing.html` re-brandada + título/base + teste de regressão atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-go-live-e2e-pago/03-CONTEXT.md
@.planning/phases/03-go-live-e2e-pago/03-RESEARCH.md
@.planning/phases/03-go-live-e2e-pago/03-PATTERNS.md

<interfaces>
Rotas do Django (config/urls.py + apps/users/urls.py) que a landing referencia:
- `/signup/` → name `billing-signup` (SignupView) — cadastro self-serve
- `/entrar/` → name `login` (LoginView, EmailAuthenticationForm)
- `/` → name `home` (LandingPageView) — pública; autenticado redireciona a `/painel/`

Contexto de template (apps/users/views.py LandingPageView.get_context_data):
- `{{ app_base_url }}` = settings.APP_BASE_URL (será `https://www.lazaricapital.com.br`, setado no .env — plano 03-02)
- A view serve a landing só em hosts de settings.MARKETING_HOSTS (default `['*']`)

Plano PRO real (billing.migrations.0002_seed_plano_pro): nome "PRO", preco_mensal 19.90, MONTHLY, trial 7d.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-brandar landing.html para Lazari Capital (D-07/D-08)</name>
  <read_first>
    - ~/projects/lazari-capital/templates/marketing/landing.html (261 linhas — arquivo a reescrever; hoje é vitrine Pocket Leads: header "Pocket Leads", hero de leads, seção de 6 recursos de CRM, 3 planos)
    - ~/projects/lazari-capital/templates/marketing/base_marketing.html (20 linhas — layout base Tailwind/Preline; herda daqui)
    - ~/projects/lazari-capital/CLAUDE.md (marca/tom; software educacional CVM)
    - .planning/phases/01-funda-o-cadastro-login-gate-e-trial/01-CONTEXT.md (tom Lazari Capital das telas da Phase 1)
  </read_first>
  <files>~/projects/lazari-capital/templates/marketing/landing.html, ~/projects/lazari-capital/templates/marketing/base_marketing.html</files>
  <action>
    Substituir todo o conteúdo de leads/CRM/Pocket Leads pela proposta da Lazari Capital, mantendo a
    estrutura Tailwind/Preline e o Alpine (`x-data`) existentes (não trocar de framework CSS).
    Trocar a marca no header de "Pocket Leads" para "Lazari Capital". Hero: headline sobre analisar
    ações de dividendos da B3 pelo método do livro (consistência/valuation), subtítulo curto. Seção de
    recursos: descrever o que o produto faz (garimpo por múltiplos + DDM + indicadores de tendência),
    SEM termos de CRM (Kanban, leads, pipeline). Seção de preço: **plano único PRO — R$ 19,90/mês**,
    com **trial de 7 dias sem cartão** (não 3 planos). Incluir explicitamente o disclaimer educacional:
    "software educacional — não constitui recomendação de investimento (CVM)". CTAs: botão primário
    "Criar conta grátis" → `{{ app_base_url }}/signup/`; link secundário "Entrar" → `{{ app_base_url }}/entrar/`.
    Manter `{{ app_base_url }}` (não hard-codar domínio). Em base_marketing.html, ajustar `<title>`/meta
    para Lazari Capital. É intencionalmente enxuta (D-08) — sem copywriting/SEO de conversão nesta fase.
  </action>
  <acceptance_criteria>
    - `grep -c "Pocket Leads\|Kanban\|pipeline\|leads" ~/projects/lazari-capital/templates/marketing/landing.html` retorna 0 (sem termos herdados)
    - `grep -c "R\$ 19,90\|Lazari Capital" ~/projects/lazari-capital/templates/marketing/landing.html` >= 2
    - landing.html contém a string do disclaimer educacional (ex.: "sem recomendação" ou "software educacional")
    - landing.html contém `{{ app_base_url }}/signup/` e `{{ app_base_url }}/entrar/`
  </acceptance_criteria>
  <verify>
    <automated>grep -Eqi "recomenda|educacional" ~/projects/lazari-capital/templates/marketing/landing.html && grep -q "19,90" ~/projects/lazari-capital/templates/marketing/landing.html && ! grep -qi "pocket leads\|kanban" ~/projects/lazari-capital/templates/marketing/landing.html && echo OK</automated>
  </verify>
  <done>Landing serve conteúdo Lazari Capital com preço, disclaimer e CTAs corretos; zero conteúdo de CRM.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atualizar teste de regressão da landing</name>
  <read_first>
    - ~/projects/lazari-capital/apps/users/tests/test_landing.py (arquivo a atualizar — asserts em "Organize seus leads...", "Kanban de leads", 3 planos, hosts pocketleads)
    - ~/projects/lazari-capital/apps/billing/models.py (modelo Plano — nome/preco_mensal, para asserts de preço)
  </read_first>
  <files>~/projects/lazari-capital/apps/users/tests/test_landing.py</files>
  <behavior>
    - GET `/` anônimo → 200 e o corpo contém "Lazari Capital" e "19,90"
    - GET `/` anônimo → o corpo contém o disclaimer educacional (ex.: "recomenda" case-insensitive)
    - GET `/` anônimo → o corpo NÃO contém "Pocket Leads" nem "Kanban"
    - GET `/` autenticado → 302 para `/painel/` (regressão de rota preservada)
  </behavior>
  <action>
    Reescrever os asserts de conteúdo para a landing Lazari Capital (headline/recursos/preço da B3 de
    dividendos), removendo as asserções de leads/Kanban/3-planos. Manter a asserção de redirect
    autenticado→`/painel/` e a estrutura do teste (client.get + asserts em content.decode()). Não
    introduzir fixtures novas além das já usadas (ContaFactory/UserFactory de apps.conftest).
  </action>
  <acceptance_criteria>
    - `cd ~/projects/lazari-capital && python -m pytest apps/users/tests/test_landing.py -q` passa (0 falhas)
    - O teste asserta presença de "19,90" e ausência de "Pocket Leads" no corpo da landing
  </acceptance_criteria>
  <verify>
    <automated>cd ~/projects/lazari-capital && python -m pytest apps/users/tests/test_landing.py -q</automated>
  </verify>
  <done>Suite de landing verde refletindo o conteúdo re-brandado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → www (Django) | Visitante anônimo carrega a landing pública em `/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | landing.html | mitigate | Landing é pública e estática de marketing — não expõe dados de conta nem endpoints internos; nenhum contexto de usuário renderizado para anônimo (LandingPageView redireciona autenticado a /painel/) |
| T-03-02 | Repudiation/Legal | copy da landing | mitigate | Disclaimer educacional "sem recomendação (CVM)" presente na landing (LEGAL-01/D-07) — evita posicionamento como aconselhamento de investimento |
</threat_model>

<verification>
- `python -m pytest apps/users/tests/test_landing.py -q` verde
- Inspeção manual: `GET /` (host de marketing) mostra marca, preço R$19,90, disclaimer, CTAs corretos
</verification>

<success_criteria>
Landing Lazari Capital mínima pronta e testada, entrando na imagem `lazari-web:latest` do deploy (03-04). Atende D-07/D-08 (parte da criterio #1 — "estreia sob o domínio Lazari Capital de forma apresentável").
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-go-live-e2e-pago/03-01-SUMMARY.md`.
</output>
