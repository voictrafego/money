# Continue Here — Phase 3 (go-live-e2e-pago)

**Última sessão:** 2026-07-08. **Status: GO-LIVE NO AR.** Faltou só o **03-05 (E2E pago)**.

## O que já está feito (não refazer)
- **03-01..03-04 completos** e commitados (repos `analista_dividendos` = `.planning/` + `lazari-capital` = código).
- **Lazari Capital no ar** na VPS `31.97.130.40` (Swarm, stack `lazari`):
  - `www.lazaricapital.com.br` → Django (landing/auth/billing/gate), TLS Let's Encrypt, imagem `lazari-web:latest`, deploy em `/opt/lazari-capital`.
  - `app.lazaricapital.com.br` → Streamlit (Analista) atrás do gate forwardAuth, `money:latest`, source `/root/money`. WS 101 validado.
  - `money.voictech.com.br` → 301 → app (cutover feito; **n8n/crm intactos**).
  - `.env` prod em `/opt/lazari-capital/.env` (chmod 600). Cron backup `0 2 * * *` do `lazari_db`.
- **7 fixes de deploy já aplicados** (ver 03-04-SUMMARY): Asaas `$` único no env_file, `USE_X_FORWARDED_HOST`, redirects cross-host absolutos (gate/e-mail/login), `backup.sh` no Swarm, Resend domain verificado.

## O que falta: 03-05 — E2E pago (3 tasks)
Ler `03-05-PLAN.md`. Precisa do **usuário** (pagamento real). Passos:
1. **Task 1 (auto)** — rodar a suíte ao vivo:
   `cd ~/projects/lazari-capital && python -m pytest apps/billing apps/gate apps/accounts -q` (já passou 270+; reconfirmar).
   E exercitar o **webhook público idempotente**: POST assinado (header `asaas-access-token` = `ASAAS_WEBHOOK_TOKEN` do `.env`) em `https://www.lazaricapital.com.br/billing/webhook/` simulando `PAYMENT_CONFIRMED`, repetir o mesmo `event_id` → 2ª vez não duplica; token inválido → 4xx.
2. **Task 2 (checkpoint, navegador, Asaas SANDBOX)** — percorrer transições de `Conta.status` e conferir o acesso ao `app.`: trial→abre; anon→302 login; `PAYMENT_CONFIRMED`→pago abre; cancelar→libera até paid-through, depois bloqueia; `PAYMENT_OVERDUE`+grace vencido→bloqueia.
3. **Task 3 (checkpoint:human-action, Asaas PROD)** — 1 assinatura real do Plano PRO **R$ 19,90** no cartão próprio via checkout hospedado; confirmar webhook prod ativa a conta + acesso libera; **estornar** logo em seguida.

## Gotchas / infra para retomar
- SSH: `ssh -i ~/.ssh/id_ed25519_vps root@31.97.130.40`. Chave do webhook = `ASAAS_WEBHOOK_TOKEN` (no `.env`).
- Webhook Asaas prod deve apontar p/ `https://www.lazaricapital.com.br/billing/webhook/`.
- Redeploy após mudança de código: rsync `~/projects/lazari-capital` → `/opt/lazari-capital` (excluir `.env`), `docker build -t lazari-web:latest .`, `docker service update --force --image lazari-web:latest lazari_web`. **Nunca `--remove-orphans`.**
- Criar usuário de teste (trial, sem e-mail) via `docker exec lazari_web python manage.py shell` (Conta status=ATIVO, trial_ate=hoje+7) — é escrita em prod, o usuário roda/aprova.
- CRM dormente do fork ainda existe (poda = pós-v2.0). `verificar/<uid>/<token>/` arma o trial.

## Após 03-05
Fechar a Fase 3 (verificação/`gsd`), e a Fase 3 conclui o milestone v2.0 (OPS-01). Ver `03-05-PLAN.md` e a memória `lazari-capital-golive-deploy`.
