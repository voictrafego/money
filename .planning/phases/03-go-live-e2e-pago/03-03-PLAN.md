---
phase: 03-go-live-e2e-pago
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/projects/lazari-capital/docker-stack.yml
  - ~/projects/lazari-capital/scripts/backup.sh
  - ~/projects/Analista de Investimentos/analista_dividendos/stack.yml
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "Existe um docker-stack.yml unificado (stack `lazari`) com web + worker + db + money — sem referência a pocketleads/crm.voictech/processar_fila_capi"
    - "O money referencia o gate pelo FQDN do Swarm http://lazari_web:8000/gate/ (não http://web:8000)"
    - "O money roteia em Host(app.lazaricapital.com.br) e existe um router 301 de money.voictech.com.br → app.lazaricapital.com.br"
    - "O worker roda apenas processar_billing (o comando morto processar_fila_capi foi removido)"
  artifacts:
    - path: "~/projects/lazari-capital/docker-stack.yml"
      provides: "Stack unificado lazari (web+worker+db+money) re-brandado"
      contains: "lazari_web:8000/gate/"
    - path: "~/projects/lazari-capital/scripts/backup.sh"
      provides: "Backup do Postgres lazari_db (nome/prefixo re-brandado)"
      contains: "lazari"
  key_links:
    - from: "money (deploy.labels)"
      to: "lazari_web (Django gate)"
      via: "forwardauth.address FQDN Swarm"
      pattern: "forwardauth.address=http://lazari_web:8000/gate/"
    - from: "router money_old (money.voictech.com.br)"
      to: "app.lazaricapital.com.br"
      via: "redirectregex 301 permanente"
      pattern: "redirectregex.replacement=https://app.lazaricapital.com.br"
---

<objective>
Produzir o **`docker-stack.yml` unificado** (stack `lazari`) que integra os 4 serviços num só stack —
`web` (Django), `worker` (só `processar_billing`), `db` (Postgres 17 isolado) e `money` (Streamlit
gated, dobrado a partir do `stack.yml` do engine) — resolvendo o **risco #1** (resolução de nome de
serviço no Swarm) ao apontar o gate pelo FQDN `lazari_web`, re-brandando hosts para `www`/`app`
Lazari Capital, adicionando o **301** de `money.voictech.com.br` → `app.`, e **corrigindo o worker
quebrado** (chama `processar_fila_capi`, comando podado na Phase 1 → crash-loop). Também re-brandar
o `backup.sh` para o `lazari_db`.

Purpose: o stack herdado é cópia byte-a-byte do crm-voic (aponta pocketleads, worker morto, volumes
`crm_*`). Sem essa correção, o deploy sobe apontando para o CRM e o worker entra em crash-loop.
Output: `docker-stack.yml` unificado pronto para deploy + `backup.sh` re-brandado + `stack.yml` do
engine corrigido/anotado como não-canônico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-go-live-e2e-pago/03-CONTEXT.md
@.planning/phases/03-go-live-e2e-pago/03-RESEARCH.md
@.planning/phases/03-go-live-e2e-pago/03-PATTERNS.md

<interfaces>
FQDN de serviço no Swarm = `<stack>_<service>`. Stack `lazari` → `lazari_web`, `lazari_db`, `lazari_money`.
- Gate: Traefik chama `http://lazari_web:8000/gate/` (web e Traefik ambos em network_swarm_public).
- Streamlit escuta 8501 (loadbalancer.server.port); Django escuta 8000; Postgres 5432 (só rede interna).
- Health Django: `GET /health/` → "ok" (config/urls.py) — usado no healthcheck e no smoke do gate.
- money NÃO publica porta (anti-spoof de X-User-Email) — manter sem bloco `ports:`.
- worker: só `processar_billing` existe (apps/billing/management/commands/processar_billing.py); `processar_fila_capi` NÃO existe (podado na Phase 1).

Escapes `$$` (docker stack interpola `$`): no 301 (`$${1}`) e no shell do worker (`$$(date +%H%M)`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever docker-stack.yml como stack unificado `lazari`</name>
  <read_first>
    - ~/projects/lazari-capital/docker-stack.yml (arquivo a reescrever — hoje idêntico ao crm-voic: pocketleads/crm.voictech, worker processar_fila_capi, volume crm_postgres_data, rede crm_internal)
    - ~/projects/crm-voic/docker-stack.yml (analog completo — web/worker/db/networks/301 pl_apex)
    - ~/projects/Analista de Investimentos/analista_dividendos/stack.yml (bloco `money` a dobrar; invariante sem-porta)
    - .planning/phases/03-go-live-e2e-pago/03-PATTERNS.md (seção CENTRAL FILE — skeletons de web/worker/db/money/301/networks já com re-brand)
    - ~/projects/lazari-capital/entrypoint.sh (o boot roda migrate → seed idempotente do Plano PRO; não duplicar)
  </read_first>
  <files>~/projects/lazari-capital/docker-stack.yml</files>
  <action>
    Reescrever o arquivo como um único stack (deploy `docker stack deploy -c docker-stack.yml lazari`)
    com os 4 serviços, seguindo os skeletons de 03-PATTERNS.md (seção CENTRAL FILE) e o analog crm-voic:
    - **web**: `image: lazari-web:latest`, `command: ["/app/entrypoint.sh"]`, `env_file: [.env]`,
      `DJANGO_SETTINGS_MODULE: config.settings.prod`, redes `network_swarm_public` + `lazari_internal`,
      healthcheck em `localhost:8000/health/` com `start_period: 180s` (Pitfall 5). Labels Traefik NO
      NÍVEL DO SERVIÇO (provider Swarm ignora labels de container): router `lazari_web` em
      `Host(\`www.lazaricapital.com.br\`)`, entrypoints websecure, tls + certresolver letsencryptresolver,
      `services.lazari_web.loadbalancer.server.port=8000` + `passHostHeader=true`.
    - **301 do domínio antigo** (nas labels do web, espelhando pl_apex): router `money_old` em
      `Host(\`money.voictech.com.br\`)` websecure/tls/certresolver, middleware `money_301` com
      `redirectregex.regex=^https?://[^/]+/(.*)`,
      `redirectregex.replacement=https://app.lazaricapital.com.br/$${1}` (ESCAPAR `$${1}`), `permanent=true`.
    - **worker**: `image: lazari-web:latest`, loop shell que roda **apenas** `python manage.py processar_billing`
      no gate horário (`if [ "$$(date +%H%M)" = "0300" ]; then ...; fi`), `sleep 60`. **REMOVER** a linha
      `processar_fila_capi` (comando morto). Só na rede `lazari_internal`. (Manter o worker: o
      `processar_billing` faz o fechamento diário que consolida overdue→graça→bloqueio, D-04.)
    - **db**: `image: postgres:17-alpine`, `env_file: [.env]`, volume `lazari_postgres_data`, só rede
      `lazari_internal` (NUNCA em network_swarm_public), healthcheck pg_isready.
    - **money**: `image: money:latest`, rede `network_swarm_public`, **sem `ports:`** (anti-spoof),
      `replicas: 1`, labels Traefik: router `money` em `Host(\`app.lazaricapital.com.br\`)`
      websecure/tls/certresolver, `services.money.loadbalancer.server.port=8501`, middleware
      `lazari-gate.forwardauth.address=http://lazari_web:8000/gate/` (FQDN — Pitfall 1),
      `authResponseHeaders=X-User-Email`, `trustForwardHeader=true`, `routers.money.middlewares=lazari-gate`.
      NÃO setar `authRequestHeaders` (removeria o Cookie → loop de auth).
    - **footer**: `volumes: lazari_postgres_data:`; `networks: network_swarm_public: {external: true}` +
      `lazari_internal: {driver: overlay}`.
    Zero ocorrências de pocketleads/crm.voictech/crm_internal/crm_postgres_data/processar_fila_capi.
  </action>
  <acceptance_criteria>
    - `grep -Ec "pocketleads|crm\\.voictech|processar_fila_capi|crm_internal|crm_postgres_data" ~/projects/lazari-capital/docker-stack.yml` == 0
    - `grep -c "lazari_web:8000/gate/" ~/projects/lazari-capital/docker-stack.yml` == 1
    - `grep -c "Host(\`app.lazaricapital.com.br\`)" ~/projects/lazari-capital/docker-stack.yml` >= 1
    - `grep -c "Host(\`money.voictech.com.br\`)" ~/projects/lazari-capital/docker-stack.yml` == 1 (router 301)
    - `grep -c "app.lazaricapital.com.br/\$\${1}" ~/projects/lazari-capital/docker-stack.yml` == 1 (escape correto)
    - `grep -c "processar_billing" ~/projects/lazari-capital/docker-stack.yml` == 1 (worker) e a linha do `money` NÃO tem bloco `ports:`
    - `python -c "import yaml,sys; yaml.safe_load(open('$HOME/projects/lazari-capital/docker-stack.yml'))"` sem erro (YAML válido)
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('$HOME/projects/lazari-capital/docker-stack.yml')); s=set(d['services']); assert {'web','worker','db','money'}<=s, s; print('services ok')" && ! grep -Eq "pocketleads|processar_fila_capi|crm_internal" ~/projects/lazari-capital/docker-stack.yml && grep -q "lazari_web:8000/gate/" ~/projects/lazari-capital/docker-stack.yml && echo OK</automated>
  </verify>
  <done>Stack unificado `lazari` válido (YAML), re-brandado, worker corrigido, gate por FQDN e 301 no lugar.</done>
</task>

<task type="auto">
  <name>Task 2: Re-brandar backup.sh para lazari_db</name>
  <read_first>
    - ~/projects/lazari-capital/scripts/backup.sh (arquivo a editar — hoje CRM_DB_CONTAINER default crm-voic-db-1, prefixo crm-)
    - ~/projects/crm-voic/scripts/backup.sh (analog)
    - .planning/phases/03-go-live-e2e-pago/03-PATTERNS.md (seção backup.sh — DB_CONTAINER via .env, prefixo lazari-, retenção 7d)
  </read_first>
  <files>~/projects/lazari-capital/scripts/backup.sh</files>
  <action>
    Ajustar o default de `DB_CONTAINER` para `${CRM_DB_CONTAINER:-lazari_db}` (o nome real do container
    Swarm será confirmado no deploy via `docker ps --format '{{.Names}}' | grep db`), o padrão do
    `BACKUP_FILE` para prefixo `lazari-$(date +%F).sql.gz`, e o `find ... -name "lazari-*.sql.gz"` na
    retenção de 7 dias. Manter a mecânica pg_dump | gzip e o `set -e`. Ajustar o comentário de cabeçalho
    para Lazari Capital (remover "CRM VoicTech / D-12").
  </action>
  <acceptance_criteria>
    - `grep -c "lazari" ~/projects/lazari-capital/scripts/backup.sh` >= 2 (container default + prefixo/retenção)
    - `grep -c "crm-\\*.sql.gz\|crm-voic-db-1" ~/projects/lazari-capital/scripts/backup.sh` == 0
    - `bash -n ~/projects/lazari-capital/scripts/backup.sh` sem erro de sintaxe
  </acceptance_criteria>
  <verify>
    <automated>bash -n ~/projects/lazari-capital/scripts/backup.sh && ! grep -q "crm-voic-db-1" ~/projects/lazari-capital/scripts/backup.sh && grep -q "lazari-" ~/projects/lazari-capital/scripts/backup.sh && echo OK</automated>
  </verify>
  <done>backup.sh aponta para lazari_db com prefixo/retenção lazari-*.</done>
</task>

<task type="auto">
  <name>Task 3: Corrigir/anotar o stack.yml standalone do engine</name>
  <read_first>
    - ~/projects/Analista de Investimentos/analista_dividendos/stack.yml (43 linhas — host money.voictech, gate http://web:8000)
    - .planning/phases/03-go-live-e2e-pago/03-PATTERNS.md (seção analista_dividendos/stack.yml — dois edits obrigatórios + invariante sem-porta)
  </read_first>
  <files>~/projects/Analista de Investimentos/analista_dividendos/stack.yml</files>
  <action>
    O deploy canônico da Phase 3 é o **stack unificado `lazari`** (Task 1) — o `money` mora lá. Para
    não deixar este arquivo stale/enganoso: (1) adicionar um comentário de cabeçalho declarando que o
    deploy de produção do `money` acontece pelo `docker-stack.yml` unificado do lazari-capital (stack
    `lazari`) e que este arquivo é um fallback standalone; (2) corrigir as duas linhas defasadas mesmo
    assim — o router para `Host(\`app.lazaricapital.com.br\`)` (era money.voictech.com.br) e o
    `forwardauth.address` para `http://lazari_web:8000/gate/` (era http://web:8000, que não resolve no
    Swarm — Pitfall 1). Preservar a invariante de **não publicar porta** (sem bloco `ports:`).
  </action>
  <acceptance_criteria>
    - `grep -c "money.voictech.com.br" ~/projects/Analista de Investimentos/analista_dividendos/stack.yml` == 0
    - `grep -c "lazari_web:8000/gate/" ~/projects/Analista de Investimentos/analista_dividendos/stack.yml` == 1
    - `grep -c "^\\s*ports:" ~/projects/Analista de Investimentos/analista_dividendos/stack.yml` == 0 (invariante mantida)
    - O cabeçalho cita o stack `lazari` como deploy canônico
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "money.voictech.com.br" "$HOME/projects/Analista de Investimentos/analista_dividendos/stack.yml" && grep -q "lazari_web:8000/gate/" "$HOME/projects/Analista de Investimentos/analista_dividendos/stack.yml" && ! grep -qE "^\s*ports:" "$HOME/projects/Analista de Investimentos/analista_dividendos/stack.yml" && echo OK</automated>
  </verify>
  <done>stack.yml do engine consistente com o domínio/gate novos e anotado como fallback do stack unificado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Traefik (ingress) → money (Streamlit) | Toda requisição ao app passa pelo gate forwardAuth antes de rotear |
| network_swarm_public ↔ lazari_internal | Postgres só na rede interna; web faz a ponte |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Spoofing | X-User-Email (acesso direto ao Streamlit) | mitigate | money SEM bloco `ports:` — só alcançável via Traefik+gate; ninguém seta X-User-Email na mão (invariante verificada no acceptance_criteria) |
| T-03-09 | Elevation of Privilege / DoS | gate inalcançável | mitigate | forwardauth.address por FQDN `lazari_web` (não `web`) — evita gate inatingível que ora bloqueia tudo, ora falha aberto |
| T-03-10 | DoS | porta do Postgres exposta | mitigate | `db` apenas em `lazari_internal` (overlay interna), nunca em network_swarm_public |
| T-03-11 | Info Disclosure | Cookie removido no gate | accept→mitigate | NÃO setar `authRequestHeaders` (default encaminha Cookie); documentado no action p/ evitar loop de auth |
</threat_model>

<verification>
- `docker-stack.yml` é YAML válido, tem os 4 serviços, gate por FQDN, 301, worker só processar_billing
- zero referências a pocketleads/crm_*/processar_fila_capi
- backup.sh e stack.yml do engine re-brandados; invariante sem-porta preservada
</verification>

<success_criteria>
Artefatos de stack prontos e consistentes para o deploy (03-04): stack unificado `lazari` re-brandado
com o risco #1 (DNS de serviço) resolvido por FQDN, worker corrigido, 301 do domínio antigo e backup
apontando ao Postgres novo. Atende à parte "Django + gate + Streamlit integrados na VPS" da criterio #1.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-go-live-e2e-pago/03-03-SUMMARY.md`.
</output>
