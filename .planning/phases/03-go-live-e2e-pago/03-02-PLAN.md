---
phase: 03-go-live-e2e-pago
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/projects/lazari-capital/config/settings/prod.py
  - /opt/lazari-capital/.env  (na VPS, fora do git, chmod 600)
autonomous: false
requirements: [OPS-01]
user_setup:
  - service: cloudflare
    why: "Registros DNS A dos hosts Lazari Capital (nuvem cinza) para TLS via Let's Encrypt"
    dashboard_config:
      - task: "Confirmar/criar A records www + app (+ money.voictech mantido) → 31.97.130.40 em NUVEM CINZA (DNS-only)"
        location: "Cloudflare → DNS → lazaricapital.com.br"
  - service: asaas
    why: "Chaves de PRODUÇÃO (API key + webhook token) para a virada sandbox→prod"
    env_vars:
      - name: ASAAS_API_KEY
        source: "Asaas (conta prod) → Configurações → Integrações → API Key (começa com $aact_prod_)"
      - name: ASAAS_WEBHOOK_TOKEN
        source: "Asaas (conta prod) → Notificações/Webhooks → token de autenticação"
  - service: resend
    why: "E-mail transacional de produção (verificação/reset — caminho crítico do trial)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend → API Keys (re_...)"
must_haves:
  truths:
    - "www.lazaricapital.com.br e app.lazaricapital.com.br resolvem para 31.97.130.40 em nuvem cinza (DNS-only)"
    - "Existe /opt/lazari-capital/.env (chmod 600, fora do git) com segredos de PRODUÇÃO: Asaas prod, Resend prod, DATABASE_URL prod, SECRET_KEY novo"
    - "prod.py e o .env concordam no domínio do cookie: SESSION_COOKIE_DOMAIN=.lazaricapital.com.br (default stale .lazaritechcapital.com.br corrigido)"
  artifacts:
    - path: "/opt/lazari-capital/.env"
      provides: "Segredos de produção via env_file (D-09/D-10)"
      contains: "ASAAS_BASE_URL=https://api.asaas.com/v3"
    - path: "~/projects/lazari-capital/config/settings/prod.py"
      provides: "Default de cookie corrigido para o domínio real"
      contains: ".lazaricapital.com.br"
  key_links:
    - from: "/opt/lazari-capital/.env"
      to: "container web (env_file)"
      via: "docker stack deploy injeta env_file"
      pattern: "SESSION_COOKIE_DOMAIN=.lazaricapital.com.br"
---

<objective>
Preparar os pré-requisitos de produção que NÃO são arquivos de stack: (1) confirmar/criar os
registros **DNS (nuvem cinza)** de `www`/`app` → VPS (senão o Let's Encrypt falha no deploy);
(2) criar o **`.env` de produção** em `/opt/lazari-capital` (chmod 600, fora do git) com as chaves
**Asaas de PRODUÇÃO** (virada sandbox→prod), Resend prod, `DATABASE_URL` do Postgres prod,
`SECRET_KEY` novo, hosts/origins e o cookie de domínio-pai; (3) corrigir o **default stale**
`.lazaritechcapital.com.br` em `prod.py` para `.lazaricapital.com.br` (código e env concordando).

Purpose: estes três itens são as pré-condições do deploy (03-04). DNS errado → cert inválido; `.env`
faltando/errado → 401 Asaas, e-mail sem enviar, loop de auth por cookie no subdomínio errado.
Output: DNS verificado + `.env` prod completo na VPS + `prod.py` com domínio correto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-go-live-e2e-pago/03-CONTEXT.md
@.planning/phases/03-go-live-e2e-pago/03-RESEARCH.md
@.planning/phases/03-go-live-e2e-pago/03-PATTERNS.md

<interfaces>
Domínio RESOLVIDO (evidência dig + memória + D-03/05/06): **lazaricapital.com.br**.
- www.lazaricapital.com.br → Django (web); app.lazaricapital.com.br → Streamlit (money gated).
- O default de código `.lazaritechcapital.com.br` em prod.py é STALE — corrigir e setar no .env explicitamente.

Vars consumidas (config/settings/base.py + prod.py):
- SECRET_KEY (obrigatório, sem default); ALLOWED_HOSTS (env.list); CSRF_TRUSTED_ORIGINS (deriva ou env)
- SESSION_COOKIE_DOMAIN / CSRF_COOKIE_DOMAIN (default stale .lazaritechcapital.com.br)
- DATABASE_URL; POSTGRES_DB/USER/PASSWORD; GUNICORN_WORKERS
- RESEND_API_KEY; DEFAULT_FROM_EMAIL
- ASAAS_API_KEY (lido via os.environ.get p/ tolerar "$"); ASAAS_BASE_URL (default = SANDBOX!); ASAAS_WEBHOOK_TOKEN
- MARKETING_HOSTS (default ['*']); APP_BASE_URL (default '')
- CRM_DB_CONTAINER / CRM_BACKUP_DIR (backup)

Nome do serviço Postgres no Swarm = `lazari_db` (stack `lazari` + serviço `db`) — usar no DATABASE_URL host.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirmar DNS (nuvem cinza) dos hosts Lazari Capital</name>
  <read_first>
    - .planning/phases/03-go-live-e2e-pago/03-RESEARCH.md (Pitfall 3 + D-06 — nuvem cinza antes do deploy)
  </read_first>
  <what-built>
    DNS é infra externa (Cloudflare) — não há artefato de código. Este checkpoint confirma o
    pré-requisito de TLS antes do deploy: A records de www/app apontando à VPS em NUVEM CINZA
    (DNS-only, não proxy laranja), senão o Let's Encrypt/Traefik falha a emissão do certificado (SAN)
    e o app fica com cert inválido.
  </what-built>
  <how-to-verify>
    1. Rodar `dig +short www.lazaricapital.com.br` → deve retornar `31.97.130.40`.
    2. Rodar `dig +short app.lazaricapital.com.br` → deve retornar `31.97.130.40`.
    3. No Cloudflare → DNS de lazaricapital.com.br: confirmar que os registros A de `www` e `app`
       estão com o ícone de **nuvem CINZA** (DNS only), NÃO laranja (proxied).
    4. Confirmar que `money.voictech.com.br` continua resolvendo à VPS (será mantido + 301 no cutover).
  </how-to-verify>
  <acceptance_criteria>
    - `dig +short www.lazaricapital.com.br` == 31.97.130.40
    - `dig +short app.lazaricapital.com.br` == 31.97.130.40
    - www e app em nuvem cinza (DNS-only) no Cloudflare
  </acceptance_criteria>
  <resume-signal>Digite "dns ok" após confirmar os 4 itens, ou descreva o que falta (criar A record / trocar laranja→cinza).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Corrigir o default de domínio do cookie em prod.py</name>
  <read_first>
    - ~/projects/lazari-capital/config/settings/prod.py (linhas ~40-49 — SESSION_COOKIE_DOMAIN/CSRF_COOKIE_DOMAIN com default `.lazaritechcapital.com.br`)
    - .planning/phases/03-go-live-e2e-pago/03-RESEARCH.md (Assumption A1 / Open Q#1 resolvida — domínio é lazaricapital.com.br)
  </read_first>
  <files>~/projects/lazari-capital/config/settings/prod.py</files>
  <action>
    Trocar o default stale das duas linhas de env() de `.lazaritechcapital.com.br` para
    `.lazaricapital.com.br` em SESSION_COOKIE_DOMAIN e CSRF_COOKIE_DOMAIN, para que código e `.env`
    concordem (defesa em profundidade — o `.env` também seta explicitamente, mas o default não pode
    contradizer). Atualizar o comentário adjacente que ainda cita `lazaritechcapital.com.br [ASSUMED]`
    para refletir o domínio confirmado. NÃO alterar SECURE_PROXY_SSL_HEADER, SameSite, HSTS ou o bloco
    Resend — já estão corretos.
  </action>
  <acceptance_criteria>
    - `grep -c "lazaritechcapital" ~/projects/lazari-capital/config/settings/prod.py` == 0
    - `grep -c ".lazaricapital.com.br" ~/projects/lazari-capital/config/settings/prod.py` >= 2
    - `cd ~/projects/lazari-capital && python -m pytest apps/core/tests/test_deploy_check.py -q` passa (settings de prod válidas)
  </acceptance_criteria>
  <verify>
    <automated>! grep -q lazaritechcapital ~/projects/lazari-capital/config/settings/prod.py && grep -c "\.lazaricapital\.com\.br" ~/projects/lazari-capital/config/settings/prod.py</automated>
  </verify>
  <done>prod.py sem nenhuma referência a lazaritechcapital; default de cookie = .lazaricapital.com.br.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Criar /opt/lazari-capital/.env de produção na VPS</name>
  <read_first>
    - ~/projects/crm-voic/.env.prod.example (estrutura de referência, 80 linhas)
    - ~/projects/lazari-capital/.env.prod.example (já re-brandado pela Phase 1/2 — usar como base)
    - ~/projects/lazari-capital/config/settings/base.py (linhas 12/14/31-39/45 — vars e como ASAAS_API_KEY é lido)
    - ~/projects/lazari-capital/config/settings/prod.py (vars de cookie/CSRF/Resend consumidas)
    - .planning/phases/03-go-live-e2e-pago/03-PATTERNS.md (seção `/opt/lazari-capital/.env` — esqueleto com as 3 pegadinhas)
  </read_first>
  <files>/opt/lazari-capital/.env (na VPS — criar via ssh, key id_ed25519_vps; NUNCA commitar)</files>
  <action>
    Via SSH na VPS (`ssh -i ~/.ssh/id_ed25519_vps root@31.97.130.40`), criar o diretório
    `/opt/lazari-capital` e o arquivo `.env` (chmod 600) com TODAS as vars de produção, seguindo o
    esqueleto de 03-PATTERNS.md. Valores obrigatórios e pegadinhas:
    - `SECRET_KEY`= novo, gerado (`python -c "import secrets;print(secrets.token_urlsafe(64))"`) — NÃO reusar o de dev.
    - `DEBUG=False`.
    - `ALLOWED_HOSTS=www.lazaricapital.com.br,app.lazaricapital.com.br,localhost,127.0.0.1` (localhost/127.0.0.1 OBRIGATÓRIOS — healthcheck bate em localhost:8000/health/, Pitfall 5).
    - `CSRF_TRUSTED_ORIGINS=https://www.lazaricapital.com.br,https://app.lazaricapital.com.br`.
    - `SESSION_COOKIE_DOMAIN=.lazaricapital.com.br` e `CSRF_COOKIE_DOMAIN=.lazaricapital.com.br` (D-05).
    - `MARKETING_HOSTS=www.lazaricapital.com.br` e `APP_BASE_URL=https://www.lazaricapital.com.br` (a landing usa app_base_url nos CTAs).
    - `DATABASE_URL=postgres://lazari_user:<senha-forte>@lazari_db:5432/lazari` — host = FQDN do serviço Swarm `lazari_db` (curto `db` pode não resolver, Pitfall 1); `POSTGRES_DB=lazari`, `POSTGRES_USER=lazari_user`, `POSTGRES_PASSWORD=<mesma senha do DATABASE_URL>`, `GUNICORN_WORKERS=5`.
    - `RESEND_API_KEY=re_...` (prod) e `DEFAULT_FROM_EMAIL=no-reply@lazaricapital.com.br`.
    - `ASAAS_API_KEY=$$aact_prod_...` — **ESCAPAR o `$` inicial como `$$`** (docker stack interpola `$`, Pitfall 4). A chave é de PRODUÇÃO.
    - `ASAAS_BASE_URL=https://api.asaas.com/v3` — **PROD** (o default de base.py é sandbox; a virada acontece aqui, D-10).
    - `ASAAS_WEBHOOK_TOKEN=<token do painel Asaas prod>`.
    - `CRM_DB_CONTAINER=lazari_db` e `CRM_BACKUP_DIR=/backups` (consumidos pelo backup em 03-04).
    `chmod 600 /opt/lazari-capital/.env`. Este arquivo fica FORA do git (D-09) — nunca copiá-lo para o repo.
  </action>
  <acceptance_criteria>
    - `ssh ... 'stat -c %a /opt/lazari-capital/.env'` == 600
    - `ssh ... 'grep -c "ASAAS_BASE_URL=https://api.asaas.com/v3" /opt/lazari-capital/.env'` == 1 (prod, não sandbox)
    - `ssh ... 'grep -c "SESSION_COOKIE_DOMAIN=.lazaricapital.com.br" /opt/lazari-capital/.env'` == 1
    - `ssh ... 'grep -c "localhost,127.0.0.1" /opt/lazari-capital/.env'` == 1 (healthcheck não derruba o container)
    - `ssh ... 'grep -E "^ASAAS_API_KEY=\\\$\\\$aact" /opt/lazari-capital/.env'` casa (o `$` inicial escapado como `$$`)
  </acceptance_criteria>
  <resume-signal>Digite "env criado" após criar o .env (chmod 600) com todas as vars, ou reporte quais chaves ainda faltam.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host VPS → containers | `.env` no host é injetado nos containers via env_file |
| Internet → ACME (Let's Encrypt) | DNS precisa resolver à VPS para o desafio HTTP-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | segredos (Asaas/DB/Resend) | mitigate | `.env` em /opt/lazari-capital fora do git, `chmod 600`, injetado por env_file (D-09) — nunca em imagem/repo |
| T-03-04 | Tampering/Spoofing | webhook Asaas | mitigate | `ASAAS_WEBHOOK_TOKEN` de prod no .env; validação do token no webhook (Phase 2) |
| T-03-05 | Information Disclosure | TLS/cert | mitigate | DNS nuvem cinza (DNS-only) → ACME HTTP-01 funciona; `SECURE_SSL_REDIRECT`+HSTS em prod.py |
| T-03-06 | Spoofing (sessão cross-host) | cookie de sessão | mitigate | `SESSION_COOKIE_DOMAIN=.lazaricapital.com.br` (parent, não None) + `SameSite=Lax` + `Secure` — cookie válido em www e app sem vazar cross-site |
| T-03-07 | Tampering | ASAAS_API_KEY truncada | mitigate | `$$` escape no .env (Pitfall 4) — validado no acceptance_criteria e no smoke do E2E (03-05) |
</threat_model>

<verification>
- `dig +short www/app.lazaricapital.com.br` == 31.97.130.40 (nuvem cinza)
- `/opt/lazari-capital/.env` chmod 600, com ASAAS_BASE_URL prod, cookie domain correto, `$$` escapado
- `grep -c lazaritechcapital prod.py` == 0
</verification>

<success_criteria>
Pré-requisitos de deploy prontos: DNS resolvendo em nuvem cinza, `.env` prod completo e seguro fora do
git, e prod.py com o domínio correto. Habilita o deploy (03-04) sem falha de cert/cookie/Asaas.
Atende à parte "segredos fora do git" da criterio #1.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-go-live-e2e-pago/03-02-SUMMARY.md`.
</output>
