---
phase: 11-apresenta-o-hierarquia-e-trava-multi-ticker
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - app.py
  - src/analista/glossario.py
autonomous: true
requirements: [DYR-02, PAY-02, HIER-01]
must_haves:
  truths:
    - "O header m3 do Analisar exibe o DY recorrente como valor principal e o trailing como delta cinza (delta_color='off')"
    - "Quando dy_recorrente é None, o header cai p/ trailing como principal (label 'Dividend Yield (trailing)')"
    - "'DY rec.' aparece formatado como % na tabela de Múltiplos (não decimal cru)"
    - "'Payout (último ano)' lê c.payout(ult) CRU, distinto de 'Payout p/ valuation (sustentável)'"
    - "Rótulos/comentários obsoletos ('média 3a', 'CAGR lucro') foram removidos no app e no glossário"
    - "app.py segue read-only: só lê campos da engine e delega a montagem aos helpers"
  artifacts:
    - path: "app.py"
      provides: "Chamador fino de presentation.header_dy / linhas_multiplos; rótulos atualizados"
      contains: "presentation.header_dy"
    - path: "src/analista/glossario.py"
      provides: "Copy de payout_dual/tab_crescimento sem 'média 3a'/'CAGR'"
      contains: "sustentável"
  key_links:
    - from: "app.py header m3"
      to: "presentation.header_dy"
      via: "st.metric(label, value, delta=..., delta_color='off')"
      pattern: "presentation\\.header_dy"
    - from: "app.py tab Múltiplos"
      to: "c.payout(c.ultimo_ano())"
      via: "payout_ult CRU (não a.multiplos['DP (payout)'])"
      pattern: "c\\.payout\\(c\\.ultimo_ano\\(\\)\\)"
---

<objective>
Religar o `app.py` para consumir os helpers puros do Plan 01 — header m3 com DY recorrente em
destaque + trailing como delta cinza (HIER-01), tabela de Múltiplos com "DY rec." formatado como %
(DYR-02) e payout cru do último ano distinto do sustentável (PAY-02) — e varrer rótulos/comentários
obsoletos no `app.py` e no `glossario.py` (D-07).

Purpose: tornar VISÍVEL a renda sustentável e a fronteira cru-vs-sustentável que a engine já expõe.
Output: `app.py` vira chamador fino dos helpers (read-only, sem recálculo de método) e o glossário
fica fiel às Fases 9-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-CONTEXT.md
@.planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-UI-SPEC.md
@.planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-01-SUMMARY.md

<interfaces>
<!-- Helpers criados no Plan 01 (src/analista/report/presentation.py) -->
header_dy(dy_recorrente, dy_atual) -> dict  # {label, value, delta, delta_color, help, fallback}
linhas_multiplos(multiplos, payout_ult, payout_proj) -> list[tuple[str, str]]
fmt_pct(x, casas=1) -> str   # "—" se None senão f"{x*100:.1f}%"
fmt_num(x, casas=2) -> str

<!-- app.py hoje importa report; o módulo presentation está no mesmo pacote report -->
<!-- Métodos da engine (read-only): c.ultimo_ano(), c.payout(ano) CRU, c.payout_valuation() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: app.py — religar header m3 + tabela de Múltiplos + relabel de Crescimento aos helpers</name>
  <files>app.py</files>
  <read_first>
    - app.py L51-56 (fmt_pct/fmt_num locais — após mover p/ presentation no Plan 01, decidir entre importar de presentation ou manter alias fino; NÃO duplicar lógica divergente)
    - app.py L131-147 (header m1..m5; m3 L134 é o alvo do header_dy; alertas L145-147 ficam intactos — D-04 sem badge novo)
    - app.py L309-339 (tab Múltiplos & Crescimento: L315 caption, L317-318 comentários errados, L320-327 montagem buggada, L333 "CAGR lucro")
    - src/analista/report/presentation.py (helpers do Plan 01)
    - src/analista/report/report.py L155-158 (payout_ult = c.payout(ult) CRU — a fonte que o app deve replicar) e L396-401 (paridade % do CLI)
    - .planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-UI-SPEC.md §Component Inventory L121-133 e §Copywriting Contract L143-156
  </read_first>
  <action>
    Importar os helpers: `from analista.report import presentation` (app.py já importa `report` do mesmo pacote). Garantir que `fmt_pct`/`fmt_num` usados no app referenciam UMA fonte (importar de `presentation` ou manter os locais idênticos; não criar divergência de separador — em-dash, ponto).

    Header m3 (HIER-01 / C-1,C-2,D-01,D-02,D-03): substituir `m3.metric("Dividend Yield", fmt_pct(a.multiplos.get("DY")), help=h("dy"))` por uma chamada a `presentation.header_dy(a.multiplos.get("DY rec."), a.multiplos.get("DY"))` e alimentar o dict em `m3.metric(label, value, delta=<delta ou ausente>, delta_color="off", help=<help>)`. Quando `delta` for None, chamar `st.metric` sem o argumento `delta` (ou delta=None). `delta_color="off"` é trava firme (cinza neutro — D-02). Manter o estilo `esc_md(...)` se entrar `R$`/`$` (strings de DY são %, então não precisa, mas seguir o padrão vizinho).

    Tab Múltiplos (DYR-02 + PAY-02 / C-3,C-4,C-5): substituir o loop L320-327 por `presentation.linhas_multiplos(a.multiplos, payout_ult, payout_proj)`. `payout_ult` MUST ler `c.payout(c.ultimo_ano())` CRU (o valor que a engine já usa em report.py L156), NÃO `a.multiplos.get("DP (payout)")`. `payout_proj = c.payout_valuation()`. Atualizar a caption L315 para `"Dois payouts: o cru do último ano e o sustentável usado no valuation (DDM)."`. Remover/atualizar os comentários errados de L317-318 (o antigo dizia "= c.payout(ult), último ano cru" quando lia payout_valuation; e "média 3a + clamp 1.0").

    Tab Crescimento (D-07 / C-6): trocar o rótulo `"g histórico (CAGR lucro)"` (L333) por `"g histórico (tendência log-linear)"` (Fase 10 tornou isto regressão log-linear sobre série normalizada).

    Manter app.py read-only: nenhuma fórmula de método recalculada; só leitura de campos já expostos + formatação/rotulagem. Alertas de armadilha (L145-147) ficam como estão (D-04, sem badge novo).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast; ast.parse(open('app.py').read()); print('parse ok')" && grep -c "c.payout(c.ultimo_ano())" app.py && grep -c "presentation.header_dy" app.py && test $(grep -c "CAGR lucro\|média 3a" app.py) -eq 0 && echo "labels swept"</automated>
  </verify>
  <acceptance_criteria>
    - `app.py` parseia (`python -c "import ast; ast.parse(...)"` sai 0)
    - `grep -c "presentation.header_dy" app.py` >= 1 e `grep -c 'delta_color="off"' app.py` >= 1
    - `grep -c "c.payout(c.ultimo_ano())" app.py` >= 1 (payout cru); `grep -c 'presentation.linhas_multiplos' app.py` >= 1
    - `grep -c "CAGR lucro" app.py` == 0 e `grep -c "média 3a" app.py` == 0
    - `grep -c 'a.multiplos.get("DP (payout)")' app.py` == 0 (a linha de payout cru não lê mais o campo de valuation)
    - app.py não recalcula método: nenhuma chamada nova a normalizacao/ddm/growth fora dos campos já expostos
  </acceptance_criteria>
  <done>Header m3 destaca recorrente com delta cinza e fallback; tabela formata "DY rec." como % e mostra payout cru distinto do sustentável; rótulos obsoletos varridos no app.</done>
</task>

<task type="auto">
  <name>Task 2: glossário — varrer copy obsoleta de payout_dual e tab_crescimento (D-07)</name>
  <files>src/analista/glossario.py</files>
  <read_first>
    - src/analista/glossario.py L54-62 (`tab_crescimento` — bullet "g histórico (CAGR)") e L105-110 (`payout_dual` — "Payout p/ valuation (média 3a)… média projetada dos últimos 3 anos (com teto de 100%)")
    - tests/test_glossario.py (confirmar que NÃO asserta as strings obsoletas — já verificado: 0 ocorrências de payout_dual/tab_crescimento/"média 3a"/"CAGR")
    - .planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-UI-SPEC.md §Copywriting Contract L158-166
  </read_first>
  <action>
    Edição de copy pura (sem lógica). `payout_dual` (L105-110): substituir "*Payout p/ valuation (média 3a)* é a média projetada dos últimos 3 anos (com teto de 100%)" pelo enquadramento **sustentável** — é a **mediana da série histórica completa** (sem clamp/teto), usada pelo DDM (Fase 9); manter a explicação de "por que dois payouts" (cru do último ano vs sustentável p/ valuation). `tab_crescimento` (L54-62): trocar o bullet "**g histórico (CAGR)**: crescimento médio do lucro nos últimos anos" por uma redação de **tendência log-linear** sobre a série de lucro normalizada (Fase 10). Tom em pt-BR, consistente com as demais entradas do dict; não introduzir "média 3a" nem "CAGR".
  </action>
  <verify>
    <automated>test $(grep -c "média 3a\|CAGR\|teto de 100" src/analista/glossario.py) -eq 0 && .venv/bin/pytest tests/test_glossario.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "média 3a" src/analista/glossario.py` == 0 e `grep -c "CAGR" src/analista/glossario.py` == 0
    - `payout_dual` contém "sustentável" e referência à mediana/série completa (sem "teto de 100%")
    - `tab_crescimento` descreve tendência log-linear (sem "CAGR")
    - `.venv/bin/pytest tests/test_glossario.py -q` sai 0
  </acceptance_criteria>
  <done>Glossário fiel às Fases 9-10: payout sustentável = mediana sem clamp; g histórico = tendência log-linear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | `app.py`/glossário local-only, read-only sobre campos já calculados. Sem input externo novo, sem rede, sem auth, sem escrita. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Information Disclosure | app.py (render de % / payout) | accept | Dados públicos de mercado já exibidos; a fase só rerrotula/formata. Sem PII, sem novo dado sensível. Nenhuma mitigação requerida. |
</threat_model>

<verification>
- `app.py` parseia; header chama `presentation.header_dy` com `delta_color="off"`; payout cru via `c.payout(c.ultimo_ano())`
- Rótulos "CAGR lucro"/"média 3a" ausentes em app.py e glossario.py
- `.venv/bin/pytest -q` segue verde (suíte inteira, sem rebaseline)
</verification>

<success_criteria>
- HIER-01: header destaca DY recorrente, trailing como delta cinza, fallback gracioso quando recorrente None
- DYR-02: "DY rec." formatado como % na tabela de Múltiplos
- PAY-02: "Payout (último ano)" cru distinto de "Payout p/ valuation (sustentável)"
- app.py read-only; glossário fiel às Fases 9-10
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-02-SUMMARY.md`
</output>
