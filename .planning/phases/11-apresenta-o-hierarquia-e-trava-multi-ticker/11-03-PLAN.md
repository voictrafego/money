---
phase: 11-apresenta-o-hierarquia-e-trava-multi-ticker
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified: []
autonomous: false
requirements: [TEST-08]
must_haves:
  truths:
    - "Os 5 tickers (VULC3 + ITUB4/EGIE3/TAEE11/BBAS3) renderizam a apresentação correta no Streamlit real"
    - "O delta do trailing aparece em cinza (delta_color='off'), nunca verde/vermelho"
    - "VULC3 mostra DY rec. ~6% (não ~20% trailing) e Payout (último ano) ~124,7% distinto do sustentável ~43%"
    - "Tickers normais não regridem na apresentação"
  artifacts: []
  key_links:
    - from: "Streamlit (app.py) ao vivo"
      to: "presentation.header_dy / linhas_multiplos"
      via: "render real dos 5 tickers (camada b da trava TEST-08)"
      pattern: "n/a (verificação humana)"
---

<objective>
Camada (b) da trava TEST-08 (D-08b): checkpoint manual ao vivo dos 5 tickers no Streamlit real,
confirmando visualmente a apresentação que o golden offline (Plan 01) travou em propriedade.

Purpose: provar que o render real (delta cinza, layout, % e payout duplo) bate com os números-alvo
reais — fechando o marco v1.3. Encerra a fase.
Output: aprovação humana do operador (ou lista de ajustes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-CONTEXT.md
@.planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-UI-SPEC.md
@.planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Checkpoint live dos 5 tickers no Streamlit (VULC3 + ITUB4/EGIE3/TAEE11/BBAS3)</name>
  <files>app.py (sem edição — verificação visual do render)</files>
  <action>
    Verificação humana (sem edição de código). Subir o Streamlit e conferir visualmente a apresentação
    dos 5 tickers — não há automação que valide cor de delta / layout do `st.metric`, por isso é
    checkpoint. Detalhes operacionais em <how-to-verify>.
  </action>
  <what-built>
    O `app.py` foi religado aos helpers puros: header m3 com DY recorrente em destaque + DY trailing
    como delta cinza (delta_color="off"), tabela de Múltiplos com "DY rec." formatado como % e duas
    linhas de payout distintas (cru do último ano vs sustentável p/ valuation), e rótulos atualizados
    ("g histórico (tendência log-linear)"). O golden offline (Plan 01) já travou estas propriedades.
  </what-built>
  <how-to-verify>
    1. Subir o app: `.venv/bin/streamlit run app.py` (abre em http://localhost:8501).
    2. No modo "🔎 Analisar uma ação", analisar cada ticker e conferir:
       - **VULC3**: header DY recorrente ~6% como valor PRINCIPAL (não ~20% trailing); o trailing
         aparece como delta em CINZA (não verde/vermelho). Na aba "📈 Múltiplos & Crescimento":
         linha "DY rec." formatada como % (ex. 6.x%, não "0.06"); "Payout (último ano)" ~124.7% CRU
         DISTINTO de "Payout p/ valuation (sustentável)" ~43%; "g histórico (tendência log-linear)".
       - **TAEE11**: DY recorrente em destaque; payout p/ valuation sustentável >100% preservado (não
         rebaixado); delta trailing cinza.
       - **EGIE3 / ITUB4 / BBAS3**: apresentação não regride — header coerente, "DY rec." como %,
         payout duplo plausível, sem layout quebrado.
    3. Confirmar que NÃO há badge/chip novo (só os alertas amarelos de armadilha já existentes).
    4. Conferir um caso de fallback se aparecer (ticker com ano de prejuízo na janela): header cai
       para "Dividend Yield (trailing)" sem delta — opcional, se algum dos 5 disparar.
  </how-to-verify>
  <verify>Confirmação visual humana dos 5 tickers no Streamlit (delta cinza, %, payout duplo distinto).</verify>
  <acceptance_criteria>
    - Delta do trailing renderiza em CINZA nos 5 tickers (delta_color="off" confirmado visualmente)
    - VULC3: DY rec. ~6% como principal; Payout (último ano) ~124,7% distinto do sustentável ~43%
    - "DY rec." aparece como % (não decimal cru) na tabela dos 5 tickers
    - Nenhum badge/chip novo; apenas os alertas amarelos pré-existentes
    - Tickers normais (ITUB4/EGIE3/TAEE11/BBAS3) sem regressão visual
  </acceptance_criteria>
  <done>Operador aprovou o render real dos 5 tickers; marco v1.3 fechado (ou lista de ajustes registrada).</done>
  <resume-signal>Digite "approved" para fechar a Phase 11, ou descreva os ajustes necessários.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Checkpoint local de verificação visual; nenhuma nova superfície de ataque. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | (n/a) | checkpoint humano | accept | Verificação visual local-only, sem entrada/rede/escrita. Nenhuma mitigação requerida. |
</threat_model>

<verification>
- Aprovação humana do render real dos 5 tickers (delta cinza, % , payout duplo distinto)
</verification>

<success_criteria>
- TEST-08 camada (b) cumprida: 5 tickers confirmados ao vivo, marco v1.3 fechado
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-03-SUMMARY.md`
</output>
