---
phase: 06-redeploy-do-app-v2-3-na-vps
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified: []
autonomous: false   # D-07: smoke visual exige o operador logar com a conta pessoal
requirements: [OPS-01]
user_setup: []

must_haves:
  truths:
    - "O service lazari_money está 1/1 healthy; o gate forwardAuth libera 2xx e promove X-User-Email; o WS abre 101 atrás do gate (D-09)"
    - "docker exec da CLI no container money imprime, para ITUB4, o arquétipo financeira→RIM e intrínseco calibrado ~R$32-40 (código servido é v2.3) (D-08)"
    - "No app ao vivo (navegador logado), ITUB4 mostra arquétipo→RIM, intrínseco ~R$32-40 e veredito 'ver motor primário' — NÃO mais 'Evitar' com faixa DDM R$12,93–19,32 (D-07)"
  artifacts:
    - path: "produção: app.lazaricapital.com.br (ITUB4)"
      provides: "Prova visual do comportamento v2.3 ao vivo (smoke visual do roadmap)"
      contains: "arquétipo RIM + intrínseco ~R$32-40 + veredito 'ver motor primário'"
  key_links:
    - from: "browser logado (conta pessoal do operador)"
      to: "money (Streamlit) atrás do gate"
      via: "gate forwardAuth 2xx → promove X-User-Email → WS 101"
      pattern: "X-User-Email"
    - from: "container money (código servido)"
      to: "engine v2.3 (RIM calibrado)"
      via: "docker exec python -m analista analyze ITUB4 (PYTHONPATH=/app/src já embutido na imagem)"
      pattern: "ITUB4.*RIM"
---

<objective>
Smoke pós-deploy em 3 camadas (defesa em profundidade) provando que o redeploy v2.3 chegou a prod
sem quebrar a infra: (D-09) infra/gate/WS, (D-08) código servido via CLI dentro do container, e
(D-07) validação visual no navegador logado. O número-âncora: ITUB4 ~R$32-40 (RIM calibrado),
veredito "ver motor primário" — o oposto do sintoma antigo "Evitar" DDM R$12,93–19,32 (OPS-01).

Purpose: Fechar o loop até o usuário real. Confirmar que o comportamento da cesta 4/4 da Fase 4
(ITUB4 32,88 · BBAS3 43,89 · BBDC4 13,37 · BBSE3 39,87) alcançou produção.
Output: Os três smokes aprovados; OPS-01 atendido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-redeploy-do-app-v2-3-na-vps/06-CONTEXT.md
@.planning/milestones/v2.0-phases/03-go-live-e2e-pago/.continue-here.md
@.planning/milestones/v2.0-phases/03-go-live-e2e-pago/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke infra (D-09) — service 1/1 healthy, gate forwardAuth 2xx + X-User-Email, WS 101</name>
  <files></files>
  <read_first>
    - .planning/phases/06-redeploy-do-app-v2-3-na-vps/06-CONTEXT.md (§decisions D-09; §code_context — gate lazari_web:8000/gate/)
    - .planning/milestones/v2.0-phases/03-go-live-e2e-pago/03-04-SUMMARY.md (gate ao vivo, WS 101, anônimo→302 login)
    - stack.yml (labels do gate forwardAuth: authResponseHeaders=X-User-Email, money sem ports:)
  </read_first>
  <action>
    Na VPS (`ssh -i ~/.ssh/id_ed25519_vps root@31.97.130.40`), confirmar que o redeploy não quebrou
    a infra: (a) `docker service ls`/`docker service ps lazari_money` mostra `1/1` healthy e task
    Running; (b) o gate forwardAuth em `http://lazari_web:8000/gate/` responde — anônimo em
    `app.lazaricapital.com.br` → 302 para o login no `www.`; sessão válida → 2xx e o gate promove o
    header `X-User-Email` upstream; (c) o websocket do Streamlit abre com upgrade `101` atrás do gate
    (mesma prova do go-live v2.0). Verificação de DNS/TLS já herdada do v2.0 (best-effort).
    Fallback D-02: se o SSH pedir auth interativa, entregar o comando exato ao operador via `! ...`.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/id_ed25519_vps root@31.97.130.40 'docker service ls --filter name=lazari_money --format "{{.Name}} {{.Replicas}}"'</automated>
  </verify>
  <acceptance_criteria>
    - `docker service ls --filter name=lazari_money` → REPLICAS `1/1`; `docker service ps` → task Running (sem Failed/Rejected).
    - Requisição anônima a `https://app.lazaricapital.com.br` → 302 para o login em `www.lazaricapital.com.br`.
    - Com sessão válida, o gate retorna 2xx e o request upstream carrega `X-User-Email` (gate promove o header).
    - O handshake do websocket do Streamlit retorna `101 Switching Protocols` atrás do gate (sem loop de auth).
  </acceptance_criteria>
  <done>Infra intacta pós-redeploy: service 1/1, gate 2xx + X-User-Email, WS 101.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke código servido (D-08) — docker exec CLI analyze ITUB4 no container money</name>
  <files></files>
  <read_first>
    - .planning/phases/06-redeploy-do-app-v2-3-na-vps/06-CONTEXT.md (§decisions D-08; §code_context — cli.py espelha a engine da UI)
    - .planning/STATE.md (§Accumulated Context — comportamento-alvo: ITUB4 RIM 32,88, cesta 4/4 Fase 4)
  </read_first>
  <action>
    Na VPS, descobrir o container id da task de `lazari_money` e rodar a CLI DENTRO do container:
    `docker exec <cid> python -m analista analyze ITUB4` (o `PYTHONPATH=/app/src` já vem embutido
    na imagem, então nenhum env extra é necessário no exec; o comando é o módulo `analista` — não
    existe `cli.py` na raiz — e o subcomando é `analyze`, não `analisar`). Isso prova que o
    código SERVIDO é v2.3, independente do render Streamlit e do gate (D-08). A saída deve mostrar
    o ITUB4 roteado ao arquétipo financeira→RIM, com intrínseco calibrado na faixa ~R$32-40
    (âncora 32,88 da cesta 4/4 da Fase 4) e o veredito remetendo ao motor primário — não o sintoma
    antigo DDM ~R$12-19 / "Evitar". Cold start pode bater CVM/Yahoo ao vivo (best-effort na latência;
    Claude's Discretion do CONTEXT.md).
    Fallback D-02: se o SSH pedir auth interativa, entregar o comando exato ao operador via `! ...`.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/id_ed25519_vps root@31.97.130.40 'docker exec $(docker ps -q -f name=lazari_money) python -m analista analyze ITUB4'</automated>
  </verify>
  <acceptance_criteria>
    - A saída da CLI para ITUB4 indica o arquétipo financeira→RIM (rótulo do motor = RIM, não DDM).
    - O intrínseco impresso cai na faixa ~R$32-40 (âncora 32,88), NÃO ~R$12,93–19,32.
    - O veredito remete ao motor primário ("ver motor primário" / equivalente), não "Evitar" via DDM.
  </acceptance_criteria>
  <done>Código servido comprovadamente v2.3 (RIM calibrado) via CLI dentro do container.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke visual (D-07) — ITUB4 no app ao vivo, navegador logado</name>
  <files></files>
  <read_first>
    - .planning/phases/06-redeploy-do-app-v2-3-na-vps/06-CONTEXT.md (§decisions D-07; §specifics — número-âncora do smoke)
    - .planning/ROADMAP.md (§Phase 6 Success Criteria #2 — o veredito/faixa exatos a confirmar)
  </read_first>
  <what-built>
    Redeploy v2.3 no ar (06-02) + infra e código servido validados (Tasks 1–2 deste plano). Falta a
    confirmação visual no app ao vivo, atrás do gate, com sessão real.
  </what-built>
  <how-to-verify>
    1. O operador acessa `https://app.lazaricapital.com.br` e loga com sua conta pessoal ATIVA
       (sem criar usuário de teste em prod).
    2. Claude conduz a análise de ITUB4 via Claude-in-Chrome no app logado.
    3. Confirmar na tela: (a) arquétipo exibido = financeira → RIM; (b) intrínseco calibrado na
       faixa ~R$32-40; (c) veredito "ver motor primário".
    4. Confirmar a AUSÊNCIA do sintoma antigo: NÃO deve aparecer "Evitar" com faixa DDM R$12,93–19,32.
  </how-to-verify>
  <acceptance_criteria>
    - Na tela do app logado, ITUB4 mostra arquétipo→RIM, intrínseco ~R$32-40 e veredito "ver motor primário".
    - O sintoma antigo ("Evitar" / faixa DDM R$12,93–19,32) NÃO aparece.
    - Se qualquer item divergir: reportar e considerar rollback (money:pre-v2.3, D-06) antes de aprovar.
  </acceptance_criteria>
  <resume-signal>Digite "approved" se ITUB4 mostra RIM ~R$32-40 / "ver motor primário", ou descreva o que apareceu de errado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → money (Streamlit) | único ingresso legítimo é Traefik + gate forwardAuth (money sem ports:) |
| browser do operador → app gated | sessão real promove X-User-Email; o app confia nesse header vindo do gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Spoofing | header X-User-Email forjado se money fosse alcançável fora do gate | mitigate | invariante mantida (money sem ports:); Task 1 confirma que só o gate promove X-User-Email |
| T-06-10 | Information Disclosure | uso de conta de teste/genérica no smoke em prod | accept | D-07 usa a conta pessoal ATIVA do operador; sem criação de usuário de teste em prod |
| T-06-11 | Denial of Service | comportamento v2.3 errado ao vivo (regressão do valuation) | mitigate | smoke em 3 camadas; divergência dispara rollback money:pre-v2.3 (D-06) antes de aprovar |
</threat_model>

<verification>
- D-09: `docker service ls` → lazari_money `1/1`; anônimo→302; gate 2xx + X-User-Email; WS 101.
- D-08: `docker exec ... python -m analista analyze ITUB4` → RIM + ~R$32-40, não DDM ~R$12-19.
- D-07: app logado mostra ITUB4 arquétipo→RIM, ~R$32-40, "ver motor primário"; sem "Evitar" DDM.
</verification>

<success_criteria>
OPS-01 atendido: app na VPS roda v2.3 (service 1/1, gate/WS intactos), ITUB4 ao vivo mostra
arquétipo→RIM calibrado (~R$32-40) e veredito "ver motor primário" — não mais "Evitar" DDM
R$12,93–19,32. Smoke visual aprovado pelo operador.
</success_criteria>

<output>
Após completar, criar `.planning/phases/06-redeploy-do-app-v2-3-na-vps/06-03-SUMMARY.md`.
</output>
</output>
