---
phase: 06-redeploy-do-app-v2-3-na-vps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false   # D-02: fallback de auth interativa (gh/host-key) pode exigir o operador
requirements: [OPS-01]
user_setup: []

must_haves:
  truths:
    - "A suíte completa (447 testes) passa verde localmente ANTES de qualquer push"
    - "O firewall selo↛report está intacto (selo.py não importa report.py)"
    - "A tag git v2.3 existe no remote voictrafego/money"
    - "main local está sincronizado com origin (voictrafego/money)"
  artifacts:
    - path: "git tag v2.3 (remote voictrafego/money)"
      provides: "Versão nomeada reproduzível que a VPS dará checkout (D-04)"
      contains: "tag v2.3 apontando para o HEAD de main com a engine v2.3"
  key_links:
    - from: "conta gh local"
      to: "remote voictrafego/money"
      via: "gh auth switch -u voictrafego (única conta com permissão de push; voicproducoes dá 403)"
      pattern: "voictrafego"
---

<objective>
Gate pré-deploy (D-10) e entrega do código v2.3 ao GitHub (D-03, D-04). Rodar a suíte completa
verde + confirmar o firewall selo↛report intacto como PRÉ-CONDIÇÃO DURA do redeploy (critério de
aceite OPS-01) e, só com a suíte verde, criar/publicar a tag `v2.3` e sincronizar `main` no remote
`voictrafego/money`.

Purpose: GitHub é a fonte de verdade do deploy — a VPS dará checkout da tag `v2.3` (D-04). Nenhum
push/deploy pode acontecer com a suíte vermelha (D-10).
Output: Suíte verde comprovada + tag `v2.3` e `main` presentes no remote `voictrafego/money`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-redeploy-do-app-v2-3-na-vps/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate pré-deploy — suíte verde 447 + firewall selo↛report (HARD BLOCK, D-10)</name>
  <files></files>
  <read_first>
    - .planning/phases/06-redeploy-do-app-v2-3-na-vps/06-CONTEXT.md (§decisions D-10)
    - .planning/STATE.md (§Blockers/Concerns — lista dos goldens que não podem quebrar)
    - tests/test_selo.py (o teste `test_firewall_selo_nao_importa_report` é a prova do firewall)
  </read_first>
  <action>
    Rodar a suíte completa do repo com `python -m pytest -q` a partir da raiz do projeto
    (pytest usa `pythonpath=["src"]` / `testpaths=["tests"]` do pyproject.toml). O resultado
    esperado é a suíte inteira verde na ordem de ~447 testes, incluindo os goldens sensíveis
    (test_ddm.py, test_selo.py, test_vulc3_regressao.py, test_consistencia_modos.py,
    test_backtest_bancos.py, test_motores.py). Confirmar explicitamente que
    `test_selo.py::test_firewall_selo_nao_importa_report` PASSA (firewall selo↛report intacto).
    Este é um HARD BLOCK: se QUALQUER teste falhar, PARAR aqui — não prosseguir para a Task 2,
    não fazer push, não fazer deploy. Reportar a falha ao operador.
  </action>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q` termina com status 0 e resumo `passed` (ordem ~447), zero `failed`/`error`.
    - `test_selo.py::test_firewall_selo_nao_importa_report` consta entre os passados.
    - Se houver qualquer falha: a task PARA e NÃO libera a Task 2 (nenhum push/deploy com suíte vermelha).
  </acceptance_criteria>
  <done>Suíte 447 verde + firewall intacto comprovados; gate de deploy liberado.</done>
</task>

<task type="auto">
  <name>Task 2: Entrega v2.3 — gh auth switch voictrafego, tag v2.3, push main + tag (D-03, D-04)</name>
  <files></files>
  <read_first>
    - .planning/phases/06-redeploy-do-app-v2-3-na-vps/06-CONTEXT.md (§decisions D-03, D-04; §canonical_refs — memória v2-2-shipped-not-deployed)
    - As memórias do projeto: rim-terminal-value-root-cause / v2.2-shipped-not-deployed
      (a auth do push exige a conta gh `voictrafego`; remote é `voictrafego/money`).
  </read_first>
  <action>
    Somente após a Task 1 verde. Trocar a conta ativa do GitHub CLI para `voictrafego`
    (`gh auth switch -u voictrafego`) — é a única conta com permissão no remote `voictrafego/money`
    (a conta `voicproducoes` retorna 403). Criar a tag git anotada `v2.3` no HEAD de `main`
    (deploy reproduzível amarrado à versão nomeada, D-04), depois `git push origin main` e
    `git push origin v2.3`. O remote `origin` já aponta para `https://github.com/voictrafego/money.git`.
    Fallback D-02: se `gh`/`git` pedir auth interativa nesta sessão (login por browser, passphrase,
    host-key prompt), NÃO tentar automatizar às cegas — entregar o comando exato ao operador para
    rodar via `! ...` no prompt e aguardar a confirmação antes de seguir.
  </action>
  <verify>
    <automated>git ls-remote --tags origin v2.3</automated>
  </verify>
  <acceptance_criteria>
    - `gh auth status` mostra a conta ativa `voictrafego`.
    - `git ls-remote --tags origin refs/tags/v2.3` retorna uma linha (tag presente no remote).
    - `git ls-remote origin refs/heads/main` bate com o SHA do HEAD local de `main` (main sincronizado).
    - Nenhum 403 no push (se ocorreu, a conta gh estava errada — corrigir para voictrafego e repetir).
  </acceptance_criteria>
  <done>Tag `v2.3` e `main` publicados em voictrafego/money; a VPS pode dar checkout da tag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → GitHub (voictrafego/money) | credencial gh com permissão de push cruza aqui; conta errada = 403 |
| suíte de testes → decisão de deploy | o gate verde é o que autoriza publicar código à fonte de verdade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | push para voictrafego/money com conta indevida | mitigate | `gh auth switch -u voictrafego` explícito antes do push; 403 aborta (D-03) |
| T-06-02 | Tampering | deploy de código não testado | mitigate | HARD BLOCK D-10: push só após suíte 447 verde + firewall intacto (Task 1 bloqueia Task 2) |
| T-06-03 | Repudiation | deploy sem versão rastreável | mitigate | tag anotada `v2.3` amarra o artefato deployado a um commit nomeado (D-04) |
| T-06-04 | Information Disclosure | credencial gh/token exposta em log | accept | tokens vivem no keyring do gh CLI; comandos não imprimem segredos; baixo risco em sessão local |
</threat_model>

<verification>
- `python -m pytest -q` verde (~447 passed, 0 failed) com firewall selo↛report passando.
- `gh auth status` → voictrafego ativo.
- `git ls-remote --tags origin refs/tags/v2.3` retorna a tag.
</verification>

<success_criteria>
Suíte 447 verde + firewall intacto comprovados ANTES de qualquer push; tag `v2.3` e `main`
presentes no remote `voictrafego/money`. Nenhum deploy foi feito ainda (isso é o 06-02).
</success_criteria>

<output>
Após completar, criar `.planning/phases/06-redeploy-do-app-v2-3-na-vps/06-01-SUMMARY.md`.
</output>
