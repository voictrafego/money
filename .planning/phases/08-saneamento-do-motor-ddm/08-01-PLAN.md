---
phase: 08-saneamento-do-motor-ddm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.yaml
  - src/analista/core/fundamentals.py
  - src/analista/core/normalizacao.py
  - src/analista/report/report.py
  - tests/test_normalizacao.py
  - tests/test_consistencia_modos.py
autonomous: true
requirements: [DDM-FIX-04]

must_haves:
  truths:
    - "O lucro que alimenta o valuation (ROE, LPA, CAGR, payout) é uma base normalizada — média winsorizada/mediana de N anos — e não mais o lucro CVM cru de um único exercício."
    - "Um ano atípico de lucro (recuperação fiscal, distribuição extraordinária) deixa de inflar ROE/CAGR/g do valuation."
    - "A tabela 'Fundamentos (por ano)' continua mostrando o lucro/ROE/payout CRUS por ano (exibição inalterada)."
    - "O ROE/payout de valuation é o MESMO número no Analisar e no Ranking — a consistência cross-modo (TEST-01) é preservada sobre a base corrigida."
  artifacts:
    - path: "src/analista/core/normalizacao.py"
      provides: "Primitiva de normalização (média winsorizada / mediana de série de lucro)"
      contains: "def "
    - path: "src/analista/core/fundamentals.py"
      provides: "Método CompanyData que devolve a base de lucro normalizada + série normalizada p/ CAGR"
    - path: "config.yaml"
      provides: "Bloco normalizacao (anos_media, winsor) — janela N canônica"
      contains: "normalizacao"
    - path: "tests/test_normalizacao.py"
      provides: "Golden unitário da primitiva (winsor 10%, mediana, ignora None, série curta)"
  key_links:
    - from: "src/analista/report/report.py"
      to: "CompanyData.lucro base normalizada"
      via: "analisar_acao consome a base normalizada p/ ROE de g_fund, LPA do seed DDM, payout e a série do CAGR"
      pattern: "normaliz"
    - from: "src/analista/core/fundamentals.py"
      to: "src/analista/core/normalizacao.py"
      via: "método de base normalizada chama a primitiva winsorizada"
      pattern: "import.*normaliz"
---

<objective>
FIX-04 (raiz da cascata VULC3): introduzir a camada de **normalização de lucro** e roteá-la para os
consumidores de valuation. Hoje `lucro_liquido` cru de 1 ano contamina ROE, CAGR, LPA, payout e DY —
um ano atípico explode todo o modelo (VULC3: ROE 51,4%, CAGR 47,3%, payout 124,7%). A decisão TRAVADA
(CONTEXT FIX-04) é usar a **mediana/média winsorizada de N anos** (espírito BSD: `anos_media=3`,
`winsor=0.10`) como base de lucro do valuation, preservando a exibição crua por ano.

Purpose: cortar a raiz a montante — sem isso, FIX-02/03/06 corrigem sintomas sobre números contaminados.
Output: módulo de normalização, knob de config, roteamento no `analisar_acao`, golden rebaselinado com justificativa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md
@.planning/phases/08-saneamento-do-motor-ddm/FINDINGS.md
@./CLAUDE.md

<interfaces>
<!-- Contratos que o executor consome direto — sem exploração de codebase. -->

src/analista/core/fundamentals.py — CompanyData (séries {ano: valor}):
  lucro_liquido: Dict[int, float]   # CVM cru
  def serie(self, attr: str) -> List[float]          # série na ordem dos anos (usada p/ CAGR)
  def roe(self, ano) -> Optional[float]              # roe_medio(LL(ano), PL_ini, PL_fim)
  def lpa(self, ano) -> Optional[float]              # mult.lpa(LL(ano), num_acoes)
  def payout(self, ano) -> Optional[float]           # CRU por ano (mantém p/ exibição)
  def payout_valuation(self, janela=3) -> Optional[float]  # média 3a + clamp 1.0 (canônico DDM/Ranking)

src/analista/core/growth.py:
  def cagr(valor_inicial, valor_final, n_periodos) -> Optional[float]
  def crescimento_por_fundamentos(roe, payout) -> Optional[float]  # ROE*(1-payout)

src/analista/report/report.py — analisar_acao (pontos de consumo do lucro):
  L55:  lpa = c.lpa(ult)                                    # seed do DDM (L113)
  L68:  lucros = c.serie("lucro_liquido"); cagr(lucros[0], lucros[-1], n-1)   # g_historico
  L71:  g_fundamentos = crescimento_por_fundamentos(c.roe(ult), c.payout(ult))
  L58-65: a.multiplos["ROE"]=c.roe(ult), "DP (payout)"=c.payout(ult)  # exibidos

config.yaml — bloco bsd (espírito a reaproveitar): bsd.anos_media: 3, bsd.winsor: 0.10
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Primitiva de normalização + knob de config + golden unitário</name>
  <files>src/analista/core/normalizacao.py, config.yaml, tests/test_normalizacao.py</files>
  <read_first>
    - .planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md (decisão FIX-04, bloco "Implementation Decisions")
    - .planning/phases/08-saneamento-do-motor-ddm/FINDINGS.md (itens C, D, cascata)
    - config.yaml (bloco bsd: anos_media/winsor — o espírito a espelhar)
  </read_first>
  <behavior>
    - Mediana de [100, 105, 300] (1 outlier alto) ≈ 105 (não 168 da média) — robusto a 1 ano atípico.
    - Média winsorizada a 10% de uma série de 5+ pontos: extremos clampados aos percentis, depois média.
    - Série com None: ignora os None antes de normalizar (não conta como 0).
    - Série curta (< 2 pontos válidos): degrada para o único valor disponível (ou None se vazia).
  </behavior>
  <action>
    Criar `src/analista/core/normalizacao.py` com uma primitiva pura que recebe uma sequência de lucros
    (floats, podendo conter None) e a janela/winsor de config e devolve a base normalizada. Discrição
    (CONTEXT permite): usar mediana OU média winsorizada — proponha e DOCUMENTE em docstring qual e por quê
    (recomendação: média winsorizada a `winsor` nos extremos sobre os últimos `anos_media` válidos, espelhando
    o BSD Cap. 8.4; mediana como fallback p/ N<3). Adicionar ao config.yaml um bloco novo `normalizacao:` com
    `anos_media: 3` e `winsor: 0.10` (knob canônico do valuation, separado do bloco `bsd` de screening para não
    acoplar valuation a screening — documentar a escolha em comentário no yaml). Ignorar None antes de calcular;
    degradar graciosamente p/ série curta. NÃO importar nada de fundamentals (primitiva pura, sem ciclo).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_normalizacao.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_normalizacao.py` cobre: outlier alto suavizado, winsor nos extremos, None ignorado, série curta → fallback; sai 0.
    - config.yaml tem bloco `normalizacao` com `anos_media` e `winsor`, com comentário explicando por que é separado de `bsd`.
    - A primitiva é pura (sem import de fundamentals/report): `grep -L "fundamentals\|report" src/analista/core/normalizacao.py` lista o arquivo.
  </acceptance_criteria>
  <done>Primitiva de normalização existe, é pura, configurável e travada por golden unitário verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Método de base de lucro normalizada em CompanyData + roteamento no analisar_acao</name>
  <files>src/analista/core/fundamentals.py, src/analista/report/report.py</files>
  <read_first>
    - src/analista/core/fundamentals.py (roe, lpa, payout, payout_valuation, serie, lucro_liquido)
    - src/analista/report/report.py (analisar_acao L54-79: múltiplos, CAGR, g_fundamentos)
    - .planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md (FIX-04: "lucro_liquido.get(ult) que alimenta ROE/payout/DY e a série do CAGR passam pela camada normalizada")
  </read_first>
  <behavior>
    - Empresa com lucro estável (todos os anos iguais): base normalizada == lucro cru ⇒ ROE/LPA/payout de valuation inalterados.
    - Empresa com 1 ano de lucro 3x os demais: base normalizada << lucro cru do último ano ⇒ ROE/CAGR/LPA de valuation caem; g do valuation cai.
    - A tabela por ano (relatorio_markdown "Fundamentos (por ano)") segue usando `c.lucro_liquido.get(ano)`/`c.payout(ano)` CRUS — exibição não muda.
  </behavior>
  <action>
    Em `fundamentals.py`, adicionar um método em CompanyData (ex.: `lucro_base()` / `serie_lucro_normalizada()`)
    que aplica a primitiva de `normalizacao` sobre a série de `lucro_liquido` (janela/winsor lidos do cfg passado
    OU de defaults espelhando config — discrição, mas a janela canônica vem de config). Em `report.analisar_acao`,
    rotear os consumidores de VALUATION para a base normalizada (per FIX-04): (a) a série do CAGR (L68) usa a série
    de lucro normalizada; (b) o ROE/LPA/payout que alimentam `g_fundamentos` (L71), o seed do DDM `dpa_inicial`
    (L113) e os múltiplos EXIBIDOS de valuation (ROE/payout em `a.multiplos`) usam a base normalizada — tornando o
    ROE/payout de valuation o número canônico ÚNICO (mesma base no Analisar e no Ranking, preservando TEST-01).
    NÃO alterar `c.roe(ano)`/`c.payout(ano)` crus nem a tabela "Fundamentos (por ano)" — eles continuam crus para
    exibição por ano (CONTEXT: "payout(ano) cru continua existindo para a exibição"). Documentar no código a fronteira
    "valuation = normalizado / exibição por ano = cru".
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_normalizacao.py tests/test_fundamentals_consistencia.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - CompanyData expõe um método de base de lucro normalizada que chama `normalizacao` (grep confirma o import: `grep -c "normaliz" src/analista/core/fundamentals.py` ≥ 1).
    - `analisar_acao` alimenta CAGR (L68), g_fundamentos (L71) e o seed do DDM com a base normalizada (grep do identificador da base normalizada em report.py ≥ 2 ocorrências, filtrando comentários: `grep -v '^\s*#' src/analista/report/report.py | grep -c "lucro_base\|normaliz"`).
    - A tabela "Fundamentos (por ano)" em `relatorio_markdown` ainda referencia `c.lucro_liquido.get(ano)` e `c.payout(ano)` crus.
    - test_fundamentals_consistencia segue verde (métodos crus `roe`/`payout_valuation` intactos).
  </acceptance_criteria>
  <done>Valuation consome lucro normalizado; exibição por ano permanece crua; fronteira documentada.</done>
</task>

<task type="auto">
  <name>Task 3: Rebaseline deliberado dos golden de cascata + suíte verde</name>
  <files>tests/test_consistencia_modos.py</files>
  <read_first>
    - tests/test_consistencia_modos.py (test_roe_coerente_analisar_vs_ranking, test_payout_coerente, test_veredito_direcao_coerente, fixtures _empresa_solida/_empresa_param)
    - src/analista/report/report.py (analisar_acao, após Tasks 1-2)
    - .planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md (decisão "Rebaseline dos golden tests — LOCKED": cada novo esperado com 1 linha de justificativa)
  </read_first>
  <action>
    Rodar a suíte e identificar os golden que mudaram por causa da normalização. Para cada asserção alterada,
    recalcular o esperado COM o método novo e documentar numa linha (comentário no teste) por que o novo número é o
    correto pelo método — NÃO relaxar assert nem "manter verde a qualquer custo". Foco esperado:
    `test_roe_coerente_analisar_vs_ranking` deve passar a comparar o ROE de valuation contra a MESMA base
    normalizada canônica (prova de consistência cross-modo sobre a base corrigida, não sobre o ano cru);
    `test_veredito_direcao_coerente` deve manter a DIREÇÃO (alvo barata) — se a normalização mexer no limiar,
    recalibrar as fixtures (preços/lucros/PL), nunca afrouxar o assert (a própria docstring do teste manda isso).
    `_empresa_solida` (lucro linear 1000→1450): documentar que a base normalizada da janela 3a vira ~1400 e o impacto.
    Empresas com séries constantes (test_payout_*) não devem mudar (normalizado == cru). Ao final, a suíte INTEIRA
    (103 testes pré-fase) volta a ficar verde com os novos números justificados.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` sai 0 (suíte inteira verde).
    - Cada asserção rebaselinada em test_consistencia_modos tem comentário de 1 linha justificando o novo valor pelo método.
    - Nenhum assert de direção/igualdade foi removido ou afrouxado (diff só recalibra valores/fixtures, não deleta asserts): `grep -c "assert" tests/test_consistencia_modos.py` ≥ valor pré-fase.
    - test_ddm.py e test_multiples.py (matemática pura com literais) permanecem inalterados.
  </acceptance_criteria>
  <done>Golden de cascata rebaselinados com justificativa; suíte 100% verde sobre a base normalizada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| série CVM → normalização | Lucro com buracos/None e outliers entra na primitiva; não é input de usuário, mas dado externo ruidoso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering/DoS | normalizacao primitiva c/ série vazia ou só-None | mitigate | degradação graciosa (fallback p/ valor único ou None), coberta por golden unitário |
| T-08-02 | I (info) | divergência exibição×valuation confunde usuário | accept | fronteira documentada (cru por ano / normalizado p/ valuation); Core Value preservado pela base canônica única |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 0 falhas (103+ verdes, incluindo o novo test_normalizacao).
- Sanity manual opcional: rodar `analisar_acao` numa CompanyData sintética com 1 ano de lucro 3x e confirmar que g_historico/g_fundamentos caem vs base crua.
</verification>

<success_criteria>
- Camada de normalização existe, é pura e configurável (knob `normalizacao` no yaml).
- ROE/CAGR/LPA/payout de valuation consomem a base normalizada; exibição por ano permanece crua.
- Consistência cross-modo (TEST-01) preservada sobre a base corrigida.
- Golden de cascata rebaselinados com justificativa documentada; suíte inteira verde.
</success_criteria>

<output>
After completion, create `.planning/phases/08-saneamento-do-motor-ddm/08-01-SUMMARY.md`
</output>
