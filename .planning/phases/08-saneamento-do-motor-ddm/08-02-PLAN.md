---
phase: 08-saneamento-do-motor-ddm
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - src/analista/report/report.py
  - tests/test_growth_reconciliacao.py
  - tests/test_consistencia_modos.py
autonomous: true
requirements: [DDM-FIX-02]

must_haves:
  truths:
    - "O teto do g_alto adotado é o g sustentável por fundamentos calculado com o MESMO payout do valuation (g_fund = ROE_normalizado × (1 − payout_valuation))."
    - "Payout ≥ 100% ⇒ g_fund ≤ 0 ⇒ g_alto cai para 0 — sem o piso artificial g_estavel na fase explícita quando os fundamentos não sustentam crescimento."
    - "O teto absoluto e a trava g_alto ≤ Ke (FIX-01) continuam valendo."
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "Nova regra de seleção de g_alto subordinada ao reinvestimento real (g_fund c/ payout_valuation)"
    - path: "tests/test_growth_reconciliacao.py"
      provides: "Golden da reconciliação: payout≥100%→g_alto=0; teto g_fund; trava ≤Ke"
  key_links:
    - from: "src/analista/report/report.py"
      to: "g_fund = ROE_norm × (1 − payout_valuation)"
      via: "seleção de g_alto limita ao g sustentável, não mais haircut fixo do CAGR"
      pattern: "payout_valuation"
---

<objective>
FIX-02 (reconciliação g × fundamentos): hoje `report.py:76-78` faz `g_alto = CAGR clampado [g_estavel, 0.25]`
e IGNORA o `g_fundamentos` (VULC3: g_fund = −12,7% exibido e descartado, g_alto adotado = 25%). A decisão
TRAVADA (CONTEXT FIX-02) subordina o g ao reinvestimento real: o teto do `g_alto` passa a ser
`g_fund = ROE_normalizado × (1 − payout_valuation)`. Payout ≥ 100% ⇒ g_fund ≤ 0 ⇒ g_alto = 0, **sem** o piso
`g_estavel` na fase explícita. Mantém o teto absoluto e a trava `≤ Ke` (FIX-01, já no código).

Purpose: o crescimento projetado deixa de ser um haircut arbitrário do CAGR e passa a refletir o que a empresa
consegue reinvestir — mata a fonte do valuation explosivo quando o payout consome todo o lucro.
Output: nova regra de seleção de g em `analisar_acao`, golden de reconciliação, rebaseline justificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md
@.planning/phases/08-saneamento-do-motor-ddm/FINDINGS.md
@.planning/phases/08-saneamento-do-motor-ddm/08-01-SUMMARY.md

<interfaces>
<!-- Estado de report.py após FIX-04 (Plan 01) — base de lucro já normalizada. -->

src/analista/report/report.py — analisar_acao (região de g, hoje):
  L70:  a.g_historico = growth.cagr(lucros[0], lucros[-1], len-1)   # sobre série normalizada (FIX-04)
  L71:  a.g_fundamentos = growth.crescimento_por_fundamentos(ROE_norm, payout(ult))  # ajustar p/ payout_valuation
  L72:  g_estavel = cfg["ddm"]["g_estavel"]
  L76:  g_alto = a.g_historico if not None else a.g_fundamentos    # <-- REGRA A SUBSTITUIR
  L77-78: g_alto = max(g_estavel, min(g_alto, 0.25))               # piso g_estavel + teto 25%
  L105-106: g_alto = min(g_alto, ke)                               # FIX-01 (MANTER)

src/analista/core/growth.py:
  def crescimento_por_fundamentos(roe, payout) -> Optional[float]   # ROE*(1-payout)
src/analista/core/fundamentals.py:
  def payout_valuation(self, janela=3) -> Optional[float]           # média 3a + clamp 1.0 (o MESMO do DDM)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconciliação de g_alto com g_fund (payout do valuation) + trava payout≥100%→0</name>
  <files>src/analista/report/report.py, tests/test_growth_reconciliacao.py</files>
  <read_first>
    - src/analista/report/report.py (L67-79 seleção de g; L102-106 trava FIX-01)
    - src/analista/core/growth.py (crescimento_por_fundamentos)
    - src/analista/core/fundamentals.py (payout_valuation)
    - .planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md (decisão FIX-02, verbatim)
  </read_first>
  <behavior>
    - payout_valuation == 1.0 (≥100%) ⇒ g_fund = ROE_norm × 0 = 0 ⇒ g_alto adotado == 0 (sem piso g_estavel).
    - g_fund > 0 e < CAGR ⇒ g_alto adotado == g_fund (teto pelo sustentável, não pelo CAGR cru).
    - g_fund > teto absoluto (0.25) ⇒ g_alto == 0.25 (teto absoluto mantido).
    - g_fund > Ke ⇒ após a trava FIX-01, g_alto == Ke (trava ≤ Ke mantida).
  </behavior>
  <action>
    Em `analisar_acao`, calcular `g_fund` com o MESMO payout do valuation: `crescimento_por_fundamentos(ROE_norm, c.payout_valuation())`
    (NÃO `c.payout(ult)` cru — usar payout_valuation, alinhado ao DDM). Substituir a regra L76-78: o `g_alto` adotado passa a ser
    limitado por `g_fund` (teto = g sustentável), mantendo o teto absoluto `0.25`. Quando `g_fund <= 0` (payout ≥ 100% ou ROE não
    positivo), `g_alto = 0` — REMOVER o piso `max(g_estavel, ...)` na fase explícita (CONTEXT: "sem piso artificial g_estavel quando
    fundamentos não sustentam"). Atenção: `g_estavel` continua sendo a taxa da PERPETUIDADE no DDM (não mexer nisso); o que muda é só
    deixar de usá-lo como PISO do g_alto da fase explícita. Manter intactas as linhas L105-106 (trava `g_alto = min(g_alto, ke)`, FIX-01).
    Quando `g_alto == 0`, validar que o DDM ainda roda (Gordon/dois-estágios com g_alto 0 é válido se Ke > g_estavel) — degradar sem exceção.
    Documentar a precedência: g_fund (sustentável) → teto absoluto 0.25 → trava ≤ Ke.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_growth_reconciliacao.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_growth_reconciliacao cobre: payout≥100%→g_alto=0 (sem piso), g_fund<CAGR→g_alto=g_fund, teto absoluto 0.25, trava ≤Ke; sai 0.
    - report.py usa `payout_valuation()` no cálculo do g_fund da reconciliação: `grep -v '^\s*#' src/analista/report/report.py | grep -c "payout_valuation"` ≥ 1.
    - O piso `max(g_estavel, ...)` foi removido da seleção do g_alto da fase explícita (g_estavel ainda usado na perpetuidade do DDM).
    - As linhas da trava FIX-01 (`min(a.g_alto, a.ke)`) permanecem.
  </acceptance_criteria>
  <done>g_alto adotado subordinado ao g sustentável; payout≥100%⇒g=0; teto absoluto e trava ≤Ke preservados.</done>
</task>

<task type="auto">
  <name>Task 2: Rebaseline dos golden de veredito/DDM afetados pelo novo g + suíte verde</name>
  <files>tests/test_consistencia_modos.py</files>
  <read_first>
    - tests/test_consistencia_modos.py (test_veredito_direcao_coerente, fixtures)
    - src/analista/report/report.py (após Task 1)
    - .planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md (Rebaseline LOCKED: 1 linha de justificativa por valor)
  </read_first>
  <action>
    Rodar a suíte e identificar vereditos/valores que mudaram porque o g_alto agora vem do g sustentável. Para a fixture-alvo
    de `test_veredito_direcao_coerente` (ROE ~25%, payout ~50%): g_fund ≈ 0,25×0,5 = 0,125 — documentar o novo g_alto e confirmar
    que a DIREÇÃO (alvo barata → SUBAVALIADA) se mantém; se o intrínseco mudar a ponto de flipar o sinal, recalibrar as fixtures
    (preço/lucro/PL), NUNCA afrouxar o assert (a docstring do teste é explícita). Cada esperado alterado recebe 1 linha de
    justificativa pelo método. Suíte inteira verde ao final.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` sai 0.
    - Cada valor/veredito rebaselinado tem comentário de 1 linha justificando-o pelo g sustentável.
    - Nenhum assert removido/afrouxado: `grep -c "assert" tests/test_consistencia_modos.py` ≥ valor pré-task.
  </acceptance_criteria>
  <done>Golden afetados pelo novo g rebaselinados com justificativa; suíte 100% verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | FIX-02 é lógica de seleção interna sobre números já normalizados (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | DoS (div/zero) | DDM com g_alto=0 | mitigate | DDM já guarda `ke - g_estavel <= 0 → None`; g_alto=0 é entrada válida; coberto por golden |
| T-08-04 | T (lógica) | remoção do piso g_estavel altera valuation | accept | mudança deliberada e documentada (CONTEXT FIX-02); travada por golden de reconciliação |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 0 falhas.
- Sanity: CompanyData sintética com payout_valuation=1.0 ⇒ g_alto adotado == 0 e DDM ainda calcula um intrínseco finito.
</verification>

<success_criteria>
- g_alto adotado = g sustentável (ROE_norm × (1 − payout_valuation)), com teto absoluto e trava ≤ Ke.
- Payout ≥ 100% ⇒ g_alto = 0, sem piso g_estavel na fase explícita.
- Golden afetados rebaselinados com justificativa; suíte verde.
</success_criteria>

<output>
After completion, create `.planning/phases/08-saneamento-do-motor-ddm/08-02-SUMMARY.md`
</output>
