---
phase: 08-saneamento-do-motor-ddm
plan: 03
type: execute
wave: 3
depends_on: [2]
files_modified:
  - config.yaml
  - src/analista/ingest/macro.py
  - src/analista/report/report.py
  - src/analista/cli.py
  - app.py
  - tests/test_capm_local.py
  - tests/test_consistencia_modos.py
autonomous: true
requirements: [DDM-FIX-03]

must_haves:
  truths:
    - "O Ke é calculado pela abordagem 'local' com rf = Selic ao vivo do BCB (macro.selic_meta()) + ERP Brasil fixo documentado + beta atual — não mais os literais de 2019 (Ke 9,4%)."
    - "Se o BCB estiver indisponível (selic_meta() → None), o Ke degrada graciosamente para um rf de fallback documentado em config, sem quebrar."
    - "A engine (analisar_acao) permanece offline/pura: lê o rf já resolvido de cfg['capm']; a chamada de rede vive nos pontos de entrada (cli/app), espelhando o padrão já usado p/ o corte de DY."
    - "O Ke resultante para VULC3 sobe para a faixa de small cap BR (materialmente acima dos 9,4% antigos)."
  artifacts:
    - path: "config.yaml"
      provides: "Bloco capm com abordagem 'local', erp_local e rf de fallback (selic_fallback)"
      contains: "erp_local"
    - path: "src/analista/ingest/macro.py"
      provides: "Resolvedor de rf p/ o CAPM (Selic ao vivo com fallback)"
    - path: "tests/test_capm_local.py"
      provides: "Golden: caminho ao vivo (Ke na faixa) + caminho fallback (rf de config), offline/determinístico"
  key_links:
    - from: "src/analista/cli.py / app.py"
      to: "cfg['capm'] rf resolvido"
      via: "injeta Selic ao vivo (ou fallback) em cfg antes de analisar_acao"
      pattern: "selic"
    - from: "src/analista/report/report.py"
      to: "capm.ke_local(beta, rf_local, erp_local)"
      via: "branch 'local' consome rf/erp do cfg já resolvido"
      pattern: "ke_local"
---

<objective>
FIX-03 (CAPM ao vivo): hoje `config.yaml` trava o CAPM em literais de fim de 2019 (rf_us 1,92%, embi 2,14%,
erp_us 6,20%) → Ke VULC3 = 9,43%, baixo demais para uma small cap BR e combustível do valuation explosivo
(Ke baixo ⇒ desconto fraco ⇒ intrínseco alto). A decisão TRAVADA (CONTEXT FIX-03) troca para a abordagem
**`local`**: `rf = macro.selic_meta()` ao vivo (BCB) + **ERP Brasil fixo** documentado + beta atual, com
**fallback gracioso** aos literais quando o BCB cai (`selic_meta() → None`). `capm.ke_local(beta, rf, erp)` já existe.

Purpose: Ke coerente com o custo de capital real de hoje (~small cap BR), não com 2019 — fecha o terceiro vetor da cascata.
Output: rewire do branch 'local', resolvedor de Selic com fallback nos entry points, config, golden offline, rebaseline.

**ERP Brasil proposto:** `erp_local = 0.06` (6%) — ancorado em Damodaran (ERP de mercado maduro ~4,5%) + prêmio de
small-cap/iliquidez ~1,5%, aplicado SOBRE a Selic (que já precifica país + inflação). Documentar; com Selic ao vivo (~2-dígitos
em 2026) e beta < 1, o Ke resultante deve aterrissar na faixa de small cap BR. O executor DEVE registrar no SUMMARY o Ke real
derivado da Selic ao vivo no momento da implementação (decisão delegada por CONTEXT: "valor exato do ERP proposto no plano").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md
@.planning/phases/08-saneamento-do-motor-ddm/FINDINGS.md

<interfaces>
src/analista/core/capm.py:
  def ke_local(beta_acao, rf_local, erp_local) -> float    # rf + beta*erp  (JÁ EXISTE)
  def ke_eua_ajustada(beta_acao, params) -> float          # literais 2019 (fallback/legado)

src/analista/ingest/macro.py:
  def selic_meta() -> Optional[float]   # Selic em fração ao vivo; None se BCB indisponível (JÁ EXISTE)

src/analista/report/report.py — analisar_acao L88-100 (branch CAPM atual):
  cap = cfg["capm"]
  if cap.get("abordagem") == "local":
      a.ke = capm.ke_local(beta, cap["rf_us"]+cap["embi_brasil"], cap["erp_us"]+cap["embi_brasil"])  # <-- REWIRE
  else: ke_eua_ajustada(...)

Padrão já existente de Selic-ao-vivo-com-fallback nos entry points (REUSAR o mesmo padrão):
  src/analista/cli.py:85  → selic = macro.selic_meta() or 0.105
  app.py:41               → return macro.selic_meta() or 0.105
  cli.py:65 / app.py:99   → report.analisar_acao(c, cfg)   # ponto onde injetar o rf resolvido
config.yaml bloco capm: abordagem "eua_ajustada", rf_us/embi_brasil/erp_us/inflacao_*  (literais livro)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branch 'local' do CAPM com Selic ao vivo + fallback + config</name>
  <files>config.yaml, src/analista/ingest/macro.py, src/analista/report/report.py</files>
  <read_first>
    - src/analista/report/report.py (L88-106 branch CAPM + trava FIX-01)
    - src/analista/core/capm.py (ke_local)
    - src/analista/ingest/macro.py (selic_meta)
    - config.yaml (bloco capm)
    - .planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md (decisão FIX-03, fallback gracioso)
  </read_first>
  <behavior>
    - cfg['capm'] com rf_local resolvido (ex.: 0.15) e erp_local 0.06, beta 0.88 ⇒ Ke = 0.15 + 0.88*0.06 ≈ 0.2028 (faixa small cap BR).
    - Resolvedor de rf: selic_meta() devolve valor ⇒ usa-o; selic_meta() → None ⇒ usa selic_fallback do config (sem exceção).
    - analisar_acao com cfg cujo rf_local é o fallback (offline, sem rede) ⇒ Ke determinístico (testável offline).
  </behavior>
  <action>
    (a) config.yaml: mudar `capm.abordagem` para `"local"`; adicionar `erp_local: 0.06` (com comentário citando a justificativa
    Damodaran + small-cap) e `selic_fallback: 0.105` (rf de degradação, documentado como "Selic de referência quando BCB indisponível");
    manter os literais legados (rf_us/embi/erp_us/inflacao_*) para o fallback `eua_ajustada` continuar funcionando. Adicionar `rf_local`
    no bloco (default == selic_fallback) — é o slot que os entry points sobrescrevem com a Selic ao vivo.
    (b) macro.py: adicionar um resolvedor puro (ex.: `rf_capm(cfg)` ou `selic_para_capm(fallback)`) que devolve `selic_meta()` quando
    não-None, senão o fallback do config — espelhando exatamente `selic_meta() or 0.105` já usado. Sem efeito colateral, sem exceção.
    (c) report.py: reescrever o branch `if cap.get("abordagem") == "local"` para `capm.ke_local(c.beta, cap["rf_local"], cap["erp_local"])`.
    NÃO chamar rede dentro de analisar_acao — a engine lê apenas `cfg["capm"]` (mantém a engine pura/offline; tests passam config.yaml direto
    e usam o `rf_local` de fallback). Manter o branch `else` (eua_ajustada) intacto como caminho legado. Preservar a trava FIX-01 (L105-106).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_capm_local.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_capm_local cobre: Ke pela `ke_local` com rf_local+erp_local na faixa esperada; resolvedor de rf devolve fallback quando Selic é None (monkeypatch de selic_meta→None); sai 0.
    - config.yaml tem `abordagem: "local"`, `erp_local`, `rf_local`, `selic_fallback`, com comentários de justificativa.
    - report.py branch local chama `ke_local(c.beta, cap["rf_local"], cap["erp_local"])`: `grep -c "ke_local" src/analista/report/report.py` ≥ 1.
    - analisar_acao NÃO chama `selic_meta` (engine pura): `grep -c "selic_meta" src/analista/report/report.py` == 0.
  </acceptance_criteria>
  <done>Branch 'local' usa rf/erp do cfg; resolvedor de Selic com fallback existe; engine permanece offline; golden verde.</done>
</task>

<task type="auto">
  <name>Task 2: Injeção da Selic ao vivo nos entry points + rebaseline dos golden de Ke</name>
  <files>src/analista/cli.py, app.py, tests/test_consistencia_modos.py</files>
  <read_first>
    - src/analista/cli.py (L85 selic_meta()or0.105; L65 analisar_acao)
    - app.py (L41 selic; L99 analisar_acao)
    - tests/test_consistencia_modos.py (fixtures beta 0.8 → DDM)
    - .planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md (Rebaseline LOCKED)
  </read_first>
  <action>
    (a) Em cli.py e app.py, antes de chamar `report.analisar_acao(c, cfg)`, injetar o rf resolvido pelo resolvedor da Task 1 em
    `cfg["capm"]["rf_local"]` (Selic ao vivo do BCB, ou o fallback do config). Reaproveitar/centralizar a chamada de Selic que já existe
    nesses arquivos (evitar duas chamadas de rede por execução — resolver uma vez e reusar). app.py permanece read-only sobre a engine
    (só injeta input de config, não recalcula método).
    (b) Rebaseline: os golden que rodam DDM com beta (test_consistencia_modos) agora têm Ke maior ⇒ intrínseco menor. Recalcular os
    esperados afetados com 1 linha de justificativa cada; manter a DIREÇÃO do veredito (recalibrar fixtures se um caso-limite flipar,
    nunca afrouxar assert). Suíte inteira verde. test_ddm.test_ke_itau_capm (literais do livro) NÃO muda — é o caso canônico do livro
    pela abordagem eua_ajustada e deve continuar verde como regressão da fórmula.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - cli.py e app.py injetam `rf_local` resolvido em cfg antes de analisar_acao: `grep -c "rf_local" src/analista/cli.py app.py` ≥ 2 (≥1 cada).
    - Nenhum arquivo faz duas chamadas separadas a selic_meta por execução (resolve uma vez): inspeção confirma reuso.
    - `.venv/bin/python -m pytest -q` sai 0; cada Ke/intrínseco rebaselinado tem justificativa de 1 linha.
    - test_ddm.py::test_ke_itau_capm permanece inalterado e verde.
  </acceptance_criteria>
  <done>Selic ao vivo injetada nos entry points com fallback; golden de Ke rebaselinados; suíte verde; app.py read-only preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BCB SGS (rede) → CAPM | rf vem de uma API externa ao vivo; resposta ausente/malformada não pode quebrar a análise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | DoS/Avail | BCB indisponível/lento durante analisar_acao | mitigate | rede só nos entry points (não na engine); resolvedor faz fallback p/ selic_fallback de config; engine offline/determinística |
| T-08-06 | Tampering | valor de Selic absurdo retornado pela API | accept | macro.selic_meta já valida parse/exceções → None; baixo risco, dado público do BCB |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 0 falhas (inclui test_capm_local offline).
- Sanity ao vivo (opcional, exige rede): `rf_capm(cfg)` retorna a Selic atual do BCB; com BCB mockado p/ None retorna o fallback.
- Registrar no SUMMARY o Ke real do VULC3 derivado da Selic ao vivo (faixa small cap BR, materialmente > 9,4%).
</verification>

<success_criteria>
- CAPM 'local' com Selic ao vivo + ERP Brasil documentado + beta; fallback gracioso a config quando BCB cai.
- Engine pura/offline (lê rf de cfg); rede isolada nos entry points; app.py read-only preservado.
- Ke VULC3 na faixa de small cap BR; golden de Ke rebaselinados com justificativa; suíte verde.
</success_criteria>

<output>
After completion, create `.planning/phases/08-saneamento-do-motor-ddm/08-03-SUMMARY.md`
</output>
