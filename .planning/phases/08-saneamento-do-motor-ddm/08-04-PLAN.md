---
phase: 08-saneamento-do-motor-ddm
plan: 04
type: execute
wave: 4
depends_on: [3]
files_modified:
  - config.yaml
  - src/analista/core/fundamentals.py
  - src/analista/report/report.py
  - src/analista/ingest/universe.py
  - data/ticker_map.json
  - tests/test_vulc3_regressao.py
  - tests/test_consistencia_modos.py
autonomous: true
requirements: [DDM-FIX-06]

must_haves:
  truths:
    - "A banda intrínseca vmin/vmax reflete a sensibilidade REAL (matriz Ke×g de ddm.matriz_sensibilidade), não só o toggle binário de 2 cenários."
    - "Existe uma noção de DY recorrente (sobre dividendo normalizado) distinta do DY trailing sobre extraordinários."
    - "O setor exibido para VULC3 é Calçados/Consumo Cíclico, não Têxtil."
    - "VULC3 é um caso de regressão golden: intrínseco deixa de ser 11–23× o preço; veredito não volta a 'SUBAVALIADA' verde; Ke na faixa small cap; g coerente com payout≥100% (~0)."
    - "No caso âncora VULC3, o ROE/payout exibido no Analisar é o MESMO que o Ranking consome (consistência entre menos travada também no golden de regressão)."
  artifacts:
    - path: "tests/test_vulc3_regressao.py"
      provides: "Golden de regressão end-to-end do caso VULC3 (cascata FIX-04→02→03→06 domada)"
      contains: "def test_"
    - path: "src/analista/report/report.py"
      provides: "Banda vmin/vmax derivada da matriz de sensibilidade real + DY recorrente exibido"
    - path: "src/analista/ingest/universe.py"
      provides: "Override/correção de setor (VULC3 = Calçados)"
  key_links:
    - from: "src/analista/report/report.py"
      to: "ddm.matriz_sensibilidade"
      via: "vmin/vmax = min/max da matriz Ke×g real"
      pattern: "sensibilidade"
    - from: "src/analista/ingest/universe.py"
      to: "setor corrigido"
      via: "override de setor por ticker (VULC3 → Calçados)"
      pattern: "setor"
---

<objective>
FIX-06 (guardrails + regressão, capstone): fecha a cascata VULC3 com os ajustes de apresentação e o caso de
regressão golden. Decisões TRAVADAS (CONTEXT FIX-06): (1) **banda intrínseca** = sensibilidade REAL — usar a
matriz Ke×g (`ddm.matriz_sensibilidade`, já existente) para vmin/vmax em vez do toggle binário de 2 cenários
(item H da FINDINGS); (2) **DY recorrente vs trailing** — expor um DY sobre dividendo normalizado, não só o
trailing sobre extraordinários (item J); (3) **setor** — corrigir o mapeamento (VULC3 = Calçados/Consumo Cíclico,
não Têxtil — item K); (4) **VULC3 vira caso de regressão golden** — intrínseco deixa de ser 11–23× o preço.

Purpose: travar o resultado da cascata num teste que falha se qualquer FIX regredir, e sanear os tells de apresentação.
Output: banda real, DY recorrente, setor correto, golden de regressão VULC3, rebaseline final, suíte verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md
@.planning/phases/08-saneamento-do-motor-ddm/FINDINGS.md
@.planning/phases/08-saneamento-do-motor-ddm/08-01-SUMMARY.md
@.planning/phases/08-saneamento-do-motor-ddm/08-03-SUMMARY.md

<interfaces>
src/analista/core/ddm.py:
  def matriz_sensibilidade(dpa_inicial, g_alto, n, g_estavel, ke, deltas_ke, deltas_g, ...) -> List[List[Optional[float]]]
src/analista/report/report.py — analisar_acao:
  L120-124: a.sensibilidade = ddm.matriz_sensibilidade(...)   # JÁ calculada, hoje só vira tabela
  L133-135: valores=[r.valor_intrinseco for r in (ddm_h, ddm_constante)]; vmin,vmax = min/max(valores)  # <-- banda binária a trocar
  L127-130: dy = a.multiplos["DY"]; flag_dy = dy>0.15; flag_payout = payout_ult>1.0   # veredito FIX-05
config.yaml: ddm.sensibilidade.delta_ke / delta_g (define a amplitude da matriz)
src/analista/core/fundamentals.py:
  def dy_atual() -> Optional[float]   # trailing-12m com fallback (existe)
  dividendos: Dict[int, float]        # série p/ DY recorrente normalizado (via primitiva do Plan 01)
src/analista/ingest/universe.py: resolver(ticker, nome) -> (CD_CVM, setor); setor vem de SETOR_ATIV da CVM
data/ticker_map.json: override ticker→CD_CVM (precedência determinística)

VULC3 (FINDINGS, 26/06/2026 — referência p/ a fixture de regressão):
  preço 14,40 · payout último 124,7% · payout valuation 100% · ROE 51,4% (cru) · beta 0,88 · DY 37% trailing
  Pré-fix: intrínseco 167–334 (11–23×). Alvo pós-cascata: intrínseco << 11× o preço; veredito não-verde; Ke faixa small cap.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Banda vmin/vmax pela sensibilidade real + DY recorrente + setor correto</name>
  <files>src/analista/report/report.py, src/analista/core/fundamentals.py, src/analista/ingest/universe.py, data/ticker_map.json, config.yaml</files>
  <read_first>
    - src/analista/report/report.py (L120-135 sensibilidade + banda binária + veredito)
    - src/analista/core/ddm.py (matriz_sensibilidade)
    - src/analista/core/fundamentals.py (dy_atual, dividendos, método de base normalizada do Plan 01)
    - src/analista/ingest/universe.py (resolver: de onde vem o setor)
    - .planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md (FIX-06, itens H/J/K)
  </read_first>
  <behavior>
    - vmin/vmax derivam do min/max dos valores não-None da matriz Ke×g (sensibilidade real), não do par (ddm_h, ddm_constante).
    - DY recorrente (sobre dividendo normalizado) < DY trailing quando há ano de provento extraordinário; ambos exibidos/distintos.
    - resolver('VULC3', ...) devolve setor "Calçados" (ou "Consumo Cíclico"), não "Têxtil e Vestuário".
  </behavior>
  <action>
    (a) Banda real: trocar a derivação de `a.vmin/a.vmax` (L133-135) para o min/max dos valores não-None de `a.sensibilidade`
    (a matriz Ke×g já calculada em L120-124). A matriz cobre o grid `delta_ke × delta_g` do config — isso É a sensibilidade real
    (CONTEXT item H). O veredito (preço vs vmin/vmax) passa a ler a banda real; manter a lógica FIX-05 (flags payout/DY rebaixam
    "SUBAVALIADA"→"VERIFICAR") intacta. Garantir que, quando a matriz tem só None (DDM não rodou), o fallback degrada como hoje.
    (b) DY recorrente: adicionar em CompanyData um DY recorrente sobre o dividendo NORMALIZADO (reusar a primitiva `normalizacao`
    do Plan 01 sobre a série `dividendos`/DPA), distinto do `dy_atual()` trailing. Expor ambos no relatório (DY recorrente vs DY trailing)
    — o recorrente é a leitura sustentável; o trailing fica como contexto. NÃO remover o trailing.
    (c) Setor: corrigir VULC3 = Calçados. Discrição (CONTEXT): implementar um override de setor por ticker — recomendação: estender
    `data/ticker_map.json` para aceitar setor opcional por ticker (ou um pequeno mapa de setor em universe.py) e fazer `resolver` preferir
    o override de setor quando presente. Adicionar VULC3 (Calçados/Consumo Cíclico). Documentar que é display-only (baixo impacto, item K).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_consistencia_modos.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - report.py deriva vmin/vmax da matriz de sensibilidade: `grep -v '^\s*#' src/analista/report/report.py | grep -c "sensibilidade"` ≥ 2 (cálculo + uso na banda).
    - CompanyData expõe um DY recorrente normalizado distinto de dy_atual(); o relatório imprime DY recorrente e DY trailing.
    - resolver('VULC3', '...') retorna setor contendo "Calçados" ou "Consumo Cíclico" (não "Têxtil"): coberto no test de regressão (Task 2) ou unitário aqui.
    - A lógica FIX-05 do veredito (flags payout/DY) permanece.
  </acceptance_criteria>
  <done>Banda = sensibilidade real; DY recorrente exposto ao lado do trailing; setor VULC3 corrigido.</done>
</task>

<task type="auto">
  <name>Task 2: Golden de regressão VULC3 (cascata domada) + rebaseline final + suíte verde</name>
  <files>tests/test_vulc3_regressao.py, tests/test_consistencia_modos.py</files>
  <read_first>
    - .planning/phases/08-saneamento-do-motor-ddm/FINDINGS.md (Sintoma + números VULC3)
    - tests/test_consistencia_modos.py (padrão de fixture CompanyData offline, _empresa_param)
    - src/analista/report/report.py (analisar_acao, estado final pós FIX-04/02/03 + Task 1)
    - .planning/phases/08-saneamento-do-motor-ddm/08-CONTEXT.md (VULC3 caso de regressão; Rebaseline LOCKED)
  </read_first>
  <action>
    Criar `tests/test_vulc3_regressao.py`: montar uma CompanyData OFFLINE que reproduz a patologia VULC3 — série de lucro com 1 ano
    extraordinário muito acima dos demais (puxa ROE/CAGR crus p/ cima), dividendos > lucro em ≥1 ano (payout > 100%), beta ~0,88,
    preço ~14,40, num_acoes/PL consistentes p/ o DDM rodar. Usar o config.yaml shipado (já com `capm.abordagem: local` + `rf_local`
    de fallback do Plan 03, offline/determinístico). Asserções de regressão (a cascata DEVE domar o caso):
      - intrínseco (vmax) NÃO é mais ~11–23× o preço — afirmar `a.vmax < K * a.preco_atual` com K um múltiplo modesto (ex.: ≤ 3-4×),
        documentando o limiar escolhido pelo método;
      - g_alto adotado ≈ 0 dado payout_valuation ≥ 100% (FIX-02);
      - Ke na faixa small cap BR (materialmente > 0,094 — ex.: ≥ 0,15) pelo rf_local de fallback (FIX-03);
      - veredito NÃO é "SUBAVALIADA" verde — é "VERIFICAR ..." (flags payout/DY, FIX-05) OU dentro/acima do intervalo;
      - base de lucro de valuation < lucro cru do ano extraordinário (FIX-04);
      - **consistência entre menus (Warning do plan-checker):** o ROE/payout exibido pelo Analisar do VULC3
        (`a.multiplos["ROE"]`, `a.multiplos["DP (payout)"]`) é IGUAL à base canônica que o Ranking consome
        (`c.roe_valuation()` / `c.payout_valuation()`) — trava que a mesma ação não aparece barata num menu e diferente no outro.
    Cada limiar com 1 linha de justificativa pelo método (não números mágicos). Em seguida, rodar a suíte inteira; rebaselinar qualquer
    golden remanescente afetado pela banda real/DY recorrente com justificativa; nunca afrouxar assert. Suíte 100% verde.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_vulc3_regressao.py` existe, é offline (sem rede) e cobre os 5 invariantes acima; `grep -c "def test_" tests/test_vulc3_regressao.py` ≥ 1.
    - A asserção do intrínseco prova que NÃO é mais 11–23× o preço (limiar documentado).
    - Há asserção cross-menu no caso âncora: `a.multiplos["ROE"] == c.roe_valuation()` e o payout do Analisar == `c.payout_valuation()`.
    - `.venv/bin/python -m pytest -q` sai 0 (suíte inteira verde, incluindo o novo golden).
    - Cada valor rebaselinado tem comentário de 1 linha de justificativa; nenhum assert removido/afrouxado.
  </acceptance_criteria>
  <done>VULC3 travado como regressão: cascata domada e provada; rebaseline final concluído; suíte 100% verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| matriz de sensibilidade → banda | células None (cenários inválidos) não podem virar vmin/vmax espúrio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | T (lógica) | matriz só-None vira banda inválida | mitigate | filtrar None antes de min/max; fallback p/ "DDM não calculado" como hoje; coberto por golden |
| T-08-08 | I (info) | setor override desatualiza vs CVM | accept | override é display-only por ticker, curado manualmente (baixo impacto, item K) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 0 falhas (inclui test_vulc3_regressao offline).
- Critério visível (CONTEXT specifics): no relatório VULC3, intrínseco deixa de ser 11–23× o preço; selo não-verde; Ke faixa small cap; g≈0.
- Invariante de marco: a suíte completa (golden de valuation rebaselinados + indicadores v1.2 intactos) fica verde.
</verification>

<success_criteria>
- Banda intrínseca = sensibilidade real (matriz Ke×g); DY recorrente exposto vs trailing; setor VULC3 correto.
- VULC3 travado como caso de regressão golden, provando a cascata FIX-04→02→03→06 domada.
- Rebaseline final deliberado e justificado; suíte 100% verde.
</success_criteria>

<output>
After completion, create `.planning/phases/08-saneamento-do-motor-ddm/08-04-SUMMARY.md`
</output>
