---
phase: 11-crescimento-g-grow
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - tests/test_growth_reconciliacao.py
  - tests/test_motores.py
  - tests/test_invariantes_v24.py
  - tests/classificacao.yaml
autonomous: true
requirements: [GROW-04, GROW-05]
must_haves:
  truths:
    - "os goldens de nível do g antigo (subordinação ao histórico + 39,87 da seguradora) foram DELETADOS, não atualizados"
    - "os invariantes estruturais presos neles (adoção de g_fundamentos, teto 0.25, trava Ke, roteamento seguradora) SOBREVIVEM extraídos ANTES da deleção"
    - "excesso_sustentavel e ke_g_spread_min são load-bearing por COBERTURA de teste (spread Ke−g ~5,5pp), não por recalibração (GROW-05/D-07)"
    - "a suíte termina verde com BLIND-02 AINDA xfail (XPASS = bug) e sem regressão em TAEE11/BBSE3/VULC3"
  artifacts:
    - path: "tests/test_invariantes_v24.py"
      provides: "teste de cobertura D-07 (terminal não explode + degrada para fade-only) + medição g_cap/adoção"
      contains: "ke_g_spread_min"
    - path: "tests/classificacao.yaml"
      provides: "registro dos novos invariantes + remoção das linhas dos goldens deletados (zero órfão)"
  key_links:
    - from: "tests/test_invariantes_v24.py (D-07)"
      to: "motores.rim terminal release (motores.py:128) + ddm.valor_gordon (ddm.py:44)"
      via: "exercita spread Ke−g apertado e assere não-explosão + degradação never-raise"
      pattern: "ke_g_spread_min|fade"
    - from: "tests/classificacao.yaml"
      to: "funções deletadas em test_growth_reconciliacao.py / test_motores.py"
      via: "remoção da linha no MESMO diff da deleção (completude da coleta)"
      pattern: "golden_nivel"
---

<objective>
Fechar a fase: (1) **split-before-delete** dos goldens de nível que codificavam o método antigo do `g` (o Plano 02
os quebrou como consequência da cura) — extrair os invariantes estruturais presos ANTES de deletar as bandas de
nível (padrão WR-04); (2) tornar `excesso_sustentavel` e `ke_g_spread_min` **load-bearing por cobertura de teste**
sob o spread `Ke − g` ~5,5pp que o `g` novo produz (GROW-05/D-07 — "prever, não descobrir", NÃO recalibrar);
(3) **verificação da fase**: a suíte termina verde, BLIND-02 AINDA xfail, e o mapa de 104 tickers não regride em
TAEE11/BBSE3/VULC3.

Purpose: a Armadilha 5 (o peso do valor terminal quase dobra quando o spread cai) é endereçada aqui, não descoberta
depois; e o método antigo do `g` some do repo sem manter reflexos vivos (regra dura do CLAUDE.md).
Output: goldens deletados + invariantes sobreviventes + teste de cobertura D-07 + veredito da fase.

**NÃO recalibrar** `excesso_sustentavel` (0.045) nem `ke_g_spread_min` (0.03) — mexer é knob move (exige lock no
mesmo diff) e abre a porta para calibrar contra resultado (Armadilha 4). Este plano só ADICIONA cobertura.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-crescimento-g-grow/11-CONTEXT.md
@.planning/phases/11-crescimento-g-grow/11-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Os goldens a deletar e os invariantes a extrair. -->

Goldens de nível que o método novo do g invalida (classificacao.yaml):
  test_growth_reconciliacao.py::test_g_fund_menor_que_cagr_vira_teto_do_g_alto      (golden_nivel, l.161) — doutrina "subordinação ao histórico" (revogada por D-01)
  test_growth_reconciliacao.py::test_teto_absoluto_025_quando_g_fund_e_cagr_explodem (golden_nivel, l.162) — invariante 0.25 SOBREVIVE
  test_growth_reconciliacao.py::test_trava_ke_quando_g_fund_supera_ke               (golden_nivel, l.163) — invariante trava Ke (FIX-01) SOBREVIVE
  test_growth_reconciliacao.py::test_payout_acima_de_100_zera_g_alto_sem_piso       (contrato, l.160) — SOBREVIVE (payout≥100% ⇒ g_alto=0, sem piso)
  test_motores.py::test_rota_seguradora_bbse3_gordon_franquia                       (golden_nivel, l.359) — lê cfg["ddm"]["g_estavel"] (removido) + assere nível 39,87; DELETAR
    (o roteamento-negativo já sobrevive em test_setor_de_banco_nao_casa_o_token_seguradora, l.360; a ROTA positiva motor=="seguradora" pode ser extraída como contrato sem nível)

Alvo do teste D-07 (onde mora, PATTERNS.md §D-07): tests/test_invariantes_v24.py, ao lado dos @pytest.mark.invariante
que usam h.cfg_e_empresas_do_snapshot(). Ramo exato a exercitar:
  motores.py:128  if g_terminal is not None and ke - g_terminal >= ke_g_spread_min:   # libera terminal
  ddm.py:44       if ke - g <= 0: return None                                          # never-raise (não explode)

Números-âncora: g_cap ≈ 0.0728; pi_ciclo ≈ 0.0518; PIB_real = 0.02; ITUB4 g_fundamentos ≈ 0.1029 (adotado);
spread Ke−g após a cura ~5,5pp (era ~10,5pp).

Regra de coleta (CLAUDE.md): teste sem entrada em classificacao.yaml QUEBRA a coleta; entrada órfã idem.
`pytest tests/arquivo.py` NÃO funciona neste repo — use `-k`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: split-before-delete dos goldens do g antigo</name>
  <read_first>
    - tests/test_growth_reconciliacao.py (as 4 funções + docstrings da doutrina antiga)
    - tests/test_motores.py:185-213 (test_rota_seguradora_bbse3_gordon_franquia — lê cfg g_estavel + nível 39,87)
    - tests/test_motores.py:214-240 (test_setor_de_banco_nao_casa_o_token_seguradora — o roteamento-negativo que JÁ sobrevive)
    - tests/classificacao.yaml:160-168 (entradas de growth) e :359-360 (entradas de seguradora)
    - .planning/STATE.md §"DÍVIDA WR-04" (o padrão split-before-delete: extrair o invariante ANTES de apagar, no MESMO diff)
    - CLAUDE.md §"suíte verde" (golden que quebra é DELETADO, nunca atualizado)
  </read_first>
  <action>
    ANTES de deletar qualquer golden, EXTRAIA os invariantes estruturais que sobrevivem à cura, como
    `@pytest.mark.invariante` (sem nível em reais, sem ticker onde possível — BLIND-04a-safe):
    (a) ADOÇÃO (novo invariante GROW-04): com g_fundamentos em (0, 0.25) e < Ke, `a.g_alto == a.g_fundamentos`
    (não mais o min com o histórico) — reescreva `test_g_fund_menor_que_cagr...` para asserir a ADOÇÃO, com
    docstring da nova doutrina (o histórico não é mais teto).
    (b) TETO ABSOLUTO 0.25: quando g_fund e CAGR explodem, `a.g_alto == 0.25` — sobrevive como invariante estrutural.
    (c) TRAVA Ke (FIX-01): quando g_fund > Ke, `a.g_alto == a.ke` — sobrevive como invariante estrutural (D-02).
    (d) `test_payout_acima_de_100_zera_g_alto_sem_piso` (já contrato) — mantém; atualize só a docstring se citar a
    doutrina de subordinação.
    (e) ROTA SEGURADORA (positiva): extraia um contrato `motor == "seguradora"` + intrínseco finito e > 0 para o
    BBSE3 do snapshot, SEM asserir o nível 39,87 e SEM ler `cfg["ddm"]["g_estavel"]` (usa g_cap agora). O
    roteamento-negativo (l.360) já existe — não duplique.
    DEPOIS, DELETE os goldens de nível invalidados — a FUNÇÃO e a LINHA em classificacao.yaml no MESMO diff (zero
    órfão): remova `test_g_fund_menor_que_cagr_vira_teto_do_g_alto`/`test_teto_absoluto_025...`/`test_trava_ke...`
    (substituídos pelos invariantes acima) e `test_rota_seguradora_bbse3_gordon_franquia` (nível 39,87, lê chave
    removida). Registre cada novo invariante/contrato em classificacao.yaml. NÃO atualize nenhum número para o
    valor novo — extrair o invariante e deletar a banda, nunca reajustar o nível.
    Este commit toca só arquivos de teste (sem config.yaml) → o hook BLIND-05 não dispara; nenhum trailer necessário.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -m golden_nivel -q && .venv/bin/python -m pytest -k "adoc or seguradora or teto_absoluto or trava_ke or payout_acima" -m "" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -m golden_nivel` → 0 failed e **0 CLASSIFICACAO ORFA** (goldens do g antigo removidos, linhas do classificacao removidas juntas).
    - Nenhuma leitura de `cfg["ddm"]["g_estavel"]` sobra em tests/ (grep vazio; a chave não existe mais).
    - Existem invariantes vivos para: adoção de g_fundamentos, teto 0.25, trava Ke, e rota seguradora (motor=="seguradora", valor finito>0) — todos registrados em classificacao.yaml.
    - Nenhum número de nível ITUB4/BBSE3 novo foi ASSERIDO (deletados, não atualizados).
  </acceptance_criteria>
  <done>Os invariantes estruturais foram extraídos e os goldens de nível do g antigo deletados no mesmo diff, sem órfão e sem atualizar níveis.</done>
</task>

<task type="auto">
  <name>Task 2: cobertura D-07 — terminal load-bearing sob spread apertado (GROW-05)</name>
  <read_first>
    - tests/test_invariantes_v24.py:103-152 (o padrão de teste de engine com h.cfg_e_empresas_do_snapshot — o análogo mais próximo)
    - src/analista/core/motores.py:113-145 (rim: guarda ke−g_terminal ≥ ke_g_spread_min; excesso_t = min(roe_t−ke, excesso_sustentavel))
    - src/analista/core/ddm.py:37-46 (valor_gordon never-raise)
    - .planning/phases/11-crescimento-g-grow/11-CONTEXT.md §decisions D-07 e §specifics (Armadilha 5)
    - .planning/phases/11-crescimento-g-grow/11-PATTERNS.md §"D-07 Coverage Test"
    - config.yaml:256/264 (excesso_sustentavel 0.045, ke_g_spread_min 0.03 — CONGELADOS, não tocar)
  </read_first>
  <action>
    Adicione a tests/test_invariantes_v24.py um teste `@pytest.mark.invariante` (registrado em classificacao.yaml
    como invariante/contrato — NÃO golden_nivel) que, sobre o snapshot + config de produção (já com g_cap ≈ 0.0728):
    (a) verifica que a derivação bate: `g_cap ≈ 0.0728` a partir de pi_ciclo≈0.0518 e PIB_real=0.02, e que para o
    ITUB4 `a.g_alto == min(a.g_fundamentos, a.ke)` com `a.g_fundamentos ≈ 0.1029` (o número do livro adotado, GROW-04);
    (b) EXERCITA o RI terminal sob o spread apertado `Ke − g_terminal` ~5,5pp (os dois knobs binding): assere que o
    intrínseco do RIM é FINITO e não explode (não None por explosão, valor positivo e limitado);
    (c) assere a DEGRADAÇÃO HONESTA: construindo um caso em que `Ke − g_terminal < ke_g_spread_min` (spread abaixo
    do piso), o terminal NÃO é liberado (`vp_terminal == 0`, cai para fade-only) e a chamada NÃO levanta
    (never-raise) — exercita o ramo motores.py:128 e ddm.valor_gordon→None. Use os knobs LIDOS de config
    (excesso_sustentavel/ke_g_spread_min), NUNCA hardcode: é cobertura, não recalibração. Sem ticker literal no
    corpo onde possível (os tickers vêm do snapshot).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "terminal_load_bearing or spread_apertado or armadilha_5 or g_cap_deriv" -m "" -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Existe um teste que assere `g_cap ≈ 0.0728` (derivado) e a adoção do ITUB4 `g_fundamentos ≈ 0.1029`.
    - O teste exercita o ramo de liberação do terminal (motores.py:128) e o never-raise de `ddm.valor_gordon`: terminal finito quando spread ≥ piso; `vp_terminal == 0` (fade-only) quando spread < `ke_g_spread_min`.
    - `excesso_sustentavel` e `ke_g_spread_min` são LIDOS de config no teste (grep), não hardcodados.
    - `git diff config.yaml calibracao.lock.yaml` VAZIO neste plano (nenhum knob movido; só cobertura adicionada).
    - Novo teste registrado em classificacao.yaml (coleta não quebra).
  </acceptance_criteria>
  <done>Os dois knobs decorativos viraram load-bearing por cobertura: o terminal não explode e degrada honestamente sob o spread apertado.</done>
</task>

<task type="auto">
  <name>Task 3: verificação da fase — suíte verde, BLIND-02 ainda xfail, não-regressão TAEE11/BBSE3/VULC3</name>
  <read_first>
    - tests/test_invariantes_v24.py:103-152 (BLIND-02b — deve terminar xfail, não XPASS)
    - tests/test_blindagem_orcamento.py (orçamento de 3 knobs)
    - tests/test_vulc3_regressao.py, tests/test_payout_sustentavel_multiticker.py (TAEE11), tests/test_guardrails_ddm.py (as regressões dos 3 tickers sensíveis)
    - .planning/STATE.md §"A ordem é a decisão" (BLIND-02 vira verde na Fase 12, não aqui) e a memória ddm-nao-consertar (g quebrou TAEE11/BBSE3/VULC3)
    - <phase_specific_hard_constraints> #2 e #6 do contexto de planejamento
  </read_first>
  <action>
    Rode a verificação final e registre os números no SUMMARY. Confirme:
    (1) `pytest -q` → 0 failed (default verde, goldens do g antigo já deletados no plano — `-m golden_nivel` também verde).
    (2) BLIND-02 AINDA xfail: `pytest -k invariancia_inflacao_engine_itub4 -rX` mostra `xfailed`, NUNCA `XPASS`.
    Como `xfail_strict`, um XPASS deixaria o default vermelho — se acontecer, é BUG (investigar; provável derivação
    do g não propagando a perna do choque, ou ke_teto removido por engano). NÃO afrouxar, NÃO trocar por skip.
    (3) Orçamento: `pytest -k "orcamento_de_knobs or knobs_batem_com_o_lock or justificativa_de_knob"` verde (3 graus).
    (4) NÃO-REGRESSÃO dos 3 tickers sensíveis: `pytest -k "vulc3 or taee11 or payout_sustentavel or guardrails_ddm or guardrails_fix06"` verde — o `g` novo não pode quebrá-los (produzir None/absurdo/exceção); os asserts existentes de finitude/sanidade seguem valendo.
    Se algum destes ficar vermelho, PARE: o que mudou foi o sistema, não o teste — diagnostique a raiz (nunca
    afrouxe o assert nem atualize um golden). Registre no SUMMARY: g_cap medido, ITUB4 g_fundamentos adotado, e a
    contagem final da suíte (passed/xfailed/deselected).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q && .venv/bin/python -m pytest -k "orcamento_de_knobs or knobs_batem_com_o_lock or vulc3 or taee11 or payout_sustentavel or guardrails_ddm" -m "" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` (default) e `pytest -m golden_nivel` → 0 failed, 0 CLASSIFICACAO ORFA.
    - `pytest -k invariancia_inflacao_engine_itub4 -rX` → mostra `xfailed`, NUNCA `XPASS` (BLIND-02 permanece a Doenca 1 ate a Fase 12).
    - `pytest -k "orcamento_de_knobs or knobs_batem_com_o_lock or justificativa_de_knob"` → verde (3 graus: ERP, n_fade, PIB_real).
    - `pytest -k "vulc3 or taee11 or payout_sustentavel or guardrails_ddm or guardrails_fix06"` → verde (nao-regressao dos 3 tickers sensiveis; nenhum None/absurdo/excecao).
    - SUMMARY registra: g_cap medido (~0.0728), ITUB4 g_fundamentos adotado (~0.1029), e a contagem final da suite.
  </acceptance_criteria>
  <done>A suite termina verde com BLIND-02 ainda xfail e sem regressao em TAEE11/BBSE3/VULC3; os numeros da fase estao registrados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste ↔ engine (snapshot) | Os testes exercitam a engine sobre o snapshot congelado; sem rede, sem input de usuario. |

Sistema offline/deterministico. A superficie real e a integridade da blindagem de teste (a suite ainda constrange o modelo) e a nao-regressao.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | golden atualizado em vez de deletado (reflexo do metodo antigo sobrevive) | mitigate | CLAUDE.md + este plano: golden de nivel e DELETADO, invariante estrutural extraido ANTES (split-before-delete); `-m golden_nivel` e a completude da coleta pegam orfaos. |
| T-11-07 | Elevation of Privilege | BLIND-02 "consertado" na fase errada (XPASS mascarado) | mitigate | `xfail_strict`: XPASS deixa a suite default vermelha; a Task 3 verifica xfailed explicitamente. A cura real e a Fase 12. |
| T-11-08 | Denial of Service | perpetuidade explosiva sob spread apertado nao coberta | mitigate | Teste D-07 (Task 2) exercita o ramo de liberacao do terminal e o never-raise de valor_gordon — Armadilha 5 prevista, nao descoberta. |

Nenhuma ameaca de web-app se aplica.
</threat_model>

<verification>
- `pytest -q` default verde; `-m golden_nivel` verde (goldens do g antigo deletados, 0 orfao).
- BLIND-02 termina xfail (nao XPASS) — a metade Ke da Doenca 1 e a Fase 12.
- Orcamento em 3 graus; `git diff config.yaml calibracao.lock.yaml` VAZIO neste plano.
- TAEE11/BBSE3/VULC3 sem regressao (finitos/sensatos).
</verification>

<success_criteria>
- Goldens de nivel do g antigo DELETADOS; invariantes estruturais (adocao, teto 0.25, trava Ke, rota seguradora) sobrevivem.
- `excesso_sustentavel`/`ke_g_spread_min` load-bearing por cobertura (GROW-05/D-07), sem recalibracao.
- Suite verde, BLIND-02 xfail, nao-regressao dos 3 tickers sensiveis — a metade `g` da Doenca 1 curada e medivel.
</success_criteria>

<output>
Apos completar, crie `.planning/phases/11-crescimento-g-grow/11-03-SUMMARY.md`.
</output>
