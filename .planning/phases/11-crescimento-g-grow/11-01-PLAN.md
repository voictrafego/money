---
phase: 11-crescimento-g-grow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/ingest/macro.py
  - src/analista/cli.py
  - app.py
  - config.yaml
autonomous: true
requirements: [GROW-01, GROW-02]
must_haves:
  truths:
    - "π_ciclo é a média aritmética do IPCA anual (SGS 13522) na MESMA janela do rf (rf_ciclo_anos=10) via helper ipca_ciclo_para_g, irmão de selic_ciclo_para_capm (D-06)"
    - "π_ciclo é resolvido UMA vez nos entry points (cli/app) e carimbado em cfg['macro']['pi_ciclo']"
    - "config.yaml tem um default macro.pi_ciclo (~0.0518) para determinismo offline/testes"
    - "a engine (analisar_acao) nunca chama a rede para o π_ciclo — lê o valor carimbado de cfg"
  artifacts:
    - path: "src/analista/ingest/macro.py"
      provides: "helper ipca_ciclo_para_g(fallback, anos=10), irmão de selic_ciclo_para_capm"
      contains: "def ipca_ciclo_para_g"
    - path: "config.yaml"
      provides: "default macro.pi_ciclo ~0.0518 (mirror do selic_fallback)"
      contains: "pi_ciclo"
  key_links:
    - from: "src/analista/cli.py::_carimbar_macro"
      to: "macro.ipca_ciclo_para_g"
      via: "stamp cfg['macro']['pi_ciclo'] na MESMA janela rf_ciclo_anos"
      pattern: "pi_ciclo.*ipca_ciclo_para_g"
    - from: "app.py (analyze stamp block)"
      to: "macro.ipca_ciclo_para_g"
      via: "cached wrapper pi_ciclo_capm + stamp CFG['macro']['pi_ciclo']"
      pattern: "pi_ciclo"
---

<objective>
Entregar o **insumo carimbado de inflação de ciclo** (`π_ciclo`) que o `g_cap` do Plano 02 vai consumir.
`π_ciclo` = média aritmética do IPCA anual (BCB SGS 13522) medida na MESMA janela do `rf` (`rf_ciclo_anos = 10`)
— é essa simetria de janela, e nada mais, que torna o valuation invariante à inflação (GROW-02). O helper é um
**espelho exato** de `selic_ciclo_para_capm`; o carimbo é um espelho exato do carimbo de `ipca_deflatores`.

Purpose: sem este insumo carimbado, o `g_cap` derivado do Plano 02 não tem de onde ler `π_ciclo` mantendo a
pureza da engine (rede só nos entry points). Este plano NÃO deriva `g_cap` nem toca em nenhum knob de valuation.
Output: `macro.ipca_ciclo_para_g`, o carimbo `cfg['macro']['pi_ciclo']` em cli/app, e o default offline no config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-crescimento-g-grow/11-CONTEXT.md
@.planning/phases/11-crescimento-g-grow/11-PATTERNS.md

<interfaces>
<!-- Copie a forma exata do irmão. NÃO invente estrutura. -->

Irmão a espelhar — src/analista/ingest/macro.py:160-173:
  def selic_ciclo_para_capm(fallback: float, anos: int = 10) -> float:
      hist = _selic_historico(anos)
      if hist:
          return sum(hist) / len(hist)
      return selic_para_capm(fallback)

Série que o novo helper consome (JÁ EXISTE, macro.py:109-140) — retorna {ano: ipca_fração}:
  def _ipca_anual_dezembro(anos: int = 10) -> Dict[int, float]:  # SGS 13522 (IPCA_12M), dez de cada ano

Carimbo-modelo — cli.py:80-86 (_carimbar_macro) e app.py:874-882 (bloco analyze):
  cfg["macro"] = {**cfg.get("macro", {}), "ipca_deflatores": macro.ipca_deflatores_anuais(cfg["capm"].get("rf_ciclo_anos", 10))}
  wrapper cacheado app.py:249-254: ipca_deflatores_capm(anos) -> macro.ipca_deflatores_anuais(anos)  (@st.cache_data)

Config macro block atual — config.yaml:64-65:
  macro:
    ipca_deflatores: {}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: helper ipca_ciclo_para_g + default macro.pi_ciclo no config</name>
  <read_first>
    - src/analista/ingest/macro.py:160-173 (selic_ciclo_para_capm — o irmão a espelhar EXATAMENTE)
    - src/analista/ingest/macro.py:109-157 (_ipca_anual_dezembro e ipca_deflatores_anuais — a série reusada e o ethos de pureza da docstring)
    - config.yaml:64-84 (bloco macro + capm.selic_fallback/rf_ciclo_anos, o padrão de default offline a espelhar)
    - .planning/phases/11-crescimento-g-grow/11-CONTEXT.md §decisions D-06 e D-06a
  </read_first>
  <action>
    Em src/analista/ingest/macro.py, adicione `ipca_ciclo_para_g(fallback: float, anos: int = 10) -> float`,
    irmão de `selic_ciclo_para_capm`: chama `_ipca_anual_dezembro(anos)`; se o dict tiver itens, retorna
    `sum(por_ano.values()) / len(por_ano)` (média ARITMÉTICA — a simetria exata com o rf `sum(hist)/len(hist)`,
    D-06); senão retorna `fallback`. Docstring espelhando o ethos de pureza de `selic_ciclo_para_capm`/
    `ipca_deflatores_anuais`: "Chamado SÓ nos entry points; a engine lê cfg e permanece determinística;
    degradação graciosa: rede falha → fallback." O helper NÃO pode ser chamado de `analisar_acao`. Reusa a
    MESMA série SGS 13522 — zero fonte de rede nova, zero grau de liberdade novo.
    Em config.yaml, no bloco `macro:` (config.yaml:64), adicione `pi_ciclo: 0.0518` com comentário de que é o
    default OFFLINE/determinístico medido (IPCA médio 10a), espelhando `capm.selic_fallback` — os entry points
    o SOBRESCREVEM com o valor ao vivo do BCB (D-06a). NÃO é knob de valuation: o bloco `macro` está FORA do
    escopo do lock (motores/capm/ddm/normalizacao), como `ipca_deflatores`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from analista.ingest import macro; print(macro.ipca_ciclo_para_g(0.0518))" && .venv/bin/python -c "import yaml; c=yaml.safe_load(open('config.yaml')); assert abs(c['macro']['pi_ciclo']-0.0518)<1e-9, c['macro']['pi_ciclo']; print('pi_ciclo default OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `macro.ipca_ciclo_para_g` existe e é chamável; offline (sem rede) retorna o `fallback` (a série vazia → fallback).
    - A média é ARITMÉTICA (`sum(values)/len(values)`), idêntica em forma a `selic_ciclo_para_capm`.
    - `config.yaml` tem `macro.pi_ciclo` ≈ 0.0518.
    - Nenhuma chamada nova a `ipca_ciclo_para_g` dentro de `report.py`/`analisar_acao` (grep vazio).
    - `pytest -k orcamento_de_knobs` continua verde (macro fora do escopo — a partição não muda).
  </acceptance_criteria>
  <done>O helper existe espelhando o irmão, o default offline está no config, e o teste de orçamento segue verde.</done>
</task>

<task type="auto">
  <name>Task 2: carimbar pi_ciclo nos entry points (cli + app)</name>
  <read_first>
    - src/analista/cli.py:66-86 (_carimbar_macro — fonte única do carimbo para analyze E rank, WR-03)
    - app.py:243-254 (rf_capm e ipca_deflatores_capm — os wrappers @st.cache_data a espelhar)
    - app.py:868-882 (bloco de carimbo do fluxo analyze — "app.py segue read-only")
    - .planning/phases/11-crescimento-g-grow/11-PATTERNS.md §cli.py e §app.py (sítios copy-from)
  </read_first>
  <action>
    Em cli.py `_carimbar_macro` (cli.py:66-86), adicione ao dict `cfg["macro"]` uma chave
    `"pi_ciclo": macro.ipca_ciclo_para_g(cfg["macro"].get("pi_ciclo", cfg["capm"]["selic_fallback"]), cfg["capm"].get("rf_ciclo_anos", 10))`
    — MESMA janela `rf_ciclo_anos` do rf/deflatores (a simetria do GROW-02). Use `cfg["macro"].get("pi_ciclo", ...)`
    como fallback offline (o default do config). Mantenha a fonte única: analyze E rank passam por esta função.
    Em app.py, adicione um wrapper cacheado `pi_ciclo_capm(fallback, anos)` (@st.cache_data, ttl=3600) espelhando
    `ipca_deflatores_capm` (app.py:249-254) que delega a `macro.ipca_ciclo_para_g`; e no bloco de carimbo do
    analyze (app.py:879-882) adicione `"pi_ciclo": pi_ciclo_capm(CFG["macro"].get("pi_ciclo", CFG["capm"]["selic_fallback"]), CFG["capm"].get("rf_ciclo_anos", 10))`.
    Respeite o comentário "app.py segue read-only": nenhum recálculo de método na UI, só o carimbo de rede uma vez.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import yaml; from analista import cli; from analista.ingest import macro; cfg=yaml.safe_load(open('config.yaml')); cli._carimbar_macro(cfg); assert 'pi_ciclo' in cfg['macro'] and cfg['macro']['pi_ciclo']>0, cfg['macro']; print('cli stamp pi_ciclo OK', cfg['macro']['pi_ciclo'])" && .venv/bin/python -c "import ast; ast.parse(open('app.py').read()); print('app.py parses')" && grep -q "pi_ciclo" app.py && echo "app stamp present"</automated>
  </verify>
  <acceptance_criteria>
    - `cli._carimbar_macro(cfg)` deixa `cfg['macro']['pi_ciclo']` presente e > 0 (offline usa o default do config como fallback).
    - O carimbo usa `cfg['capm'].get('rf_ciclo_anos', 10)` — a MESMA janela do rf e dos deflatores.
    - app.py tem um wrapper `pi_ciclo_capm` @st.cache_data e o carimbo `CFG['macro']['pi_ciclo']` no bloco analyze.
    - `pytest -q` → 0 failed (nenhum consumidor da engine mudou ainda; só o carimbo foi adicionado).
  </acceptance_criteria>
  <done>analyze e rank (cli) e o fluxo analyze (app) carimbam pi_ciclo na janela do rf; a suíte segue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BCB SGS 13522 → entry point | Fonte de dados externa (rede) atravessa aqui, SÓ nos entry points (cli/app). |
| entry point → engine (cfg) | O valor resolvido é carimbado em `cfg`; a engine é offline/determinística a partir daí. |

Natureza do sistema: engine de valuation **offline e determinística**, sem input de usuário em runtime, sem auth,
sem rede na engine. A superfície real de ameaça é apenas integridade da fonte de dados e determinismo do valor carimbado.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Denial of Service | `_ipca_anual_dezembro` (rede BCB indisponível) | mitigate | Degradação graciosa já é design: rede falha → `fallback` (default `macro.pi_ciclo` do config). Nunca levanta; a engine ainda deriva um `g_cap` determinístico. |
| T-11-02 | Tampering | determinismo do valor carimbado | mitigate | Média aritmética pura sobre série datada (SGS 13522, dez de cada ano) — sem escolha livre de ano-base; testes carimbam `pi_ciclo` diretamente, resultado reproduzível. |

Nenhuma ameaça de web-app (injeção, auth, sessão) se aplica: não há input de usuário nem rede na engine.
</threat_model>

<verification>
- `pytest -q` → 0 failed (o carimbo é aditivo; nenhum consumidor de valuation mudou neste plano).
- `pytest -k orcamento_de_knobs` verde: `macro.pi_ciclo` está FORA do escopo do lock, a partição de 30 folhas não muda.
- `grep -rn "ipca_ciclo_para_g" src/analista/report/` vazio: a engine não chama o helper (pureza preservada).
</verification>

<success_criteria>
- `macro.ipca_ciclo_para_g` existe, espelha `selic_ciclo_para_capm` (média aritmética, degradação graciosa).
- cli e app carimbam `cfg['macro']['pi_ciclo']` na janela `rf_ciclo_anos` (simetria rf↔π_ciclo, GROW-02).
- `config.yaml` tem o default offline `macro.pi_ciclo ≈ 0.0518` (GROW-01 insumo).
- Suíte default verde; orçamento de knobs intacto (3 graus).
</success_criteria>

<output>
Após completar, crie `.planning/phases/11-crescimento-g-grow/11-01-SUMMARY.md`.
</output>
