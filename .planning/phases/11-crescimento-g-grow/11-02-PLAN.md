---
phase: 11-crescimento-g-grow
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/analista/report/report.py
  - config.yaml
  - calibracao.lock.yaml
  - tests/helpers_blindagem.py
autonomous: true
requirements: [GROW-01, GROW-03, GROW-04]
must_haves:
  truths:
    - "g_cap é DERIVADO na engine: (1 + cfg['macro']['pi_ciclo'])(1 + PIB_real) − 1 ≈ 0.0728 — nunca digitado"
    - "um único g_cap substitui ddm.g_estavel E motores.rim.g_terminal em TODOS os call sites (fonte única, D-04)"
    - "o g terminal do RIM é a identidade fechada por empresa g_T = min(roe_terminal × retenção, g_cap) (GROW-03)"
    - "a fase explícita ADOTA g_fundamentos (ITUB4 ~10.29%); g_historico vira sanidade/fallback (GROW-04, D-01)"
    - "o teto FIX-01 (g_alto ≤ Ke) e o teto absoluto 0.25 na fase explícita ficam INTOCADOS (D-02)"
    - "o orçamento permanece 3 graus: PIB_real migra de home (caminho no lock atualizado no MESMO diff, D-05)"
    - "BLIND-02 (choque_nominal) choca o NOVO home do g (macro.pi_ciclo) e permanece xfail"
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "derivação g_cap + adoção g_fundamentos + g_T fechado por empresa"
      contains: "pi_ciclo"
    - path: "calibracao.lock.yaml"
      provides: "PIB_real caminho migrado + g_terminal removido de congelados (partição 29 folhas)"
      contains: "PIB_real"
  key_links:
    - from: "report.py (derivação g_cap)"
      to: "cfg['macro']['pi_ciclo'] + cfg[...]['pib_real']"
      via: "g_cap = (1+pi_ciclo)(1+PIB_real)-1"
      pattern: "1 \\+ .*pi_ciclo"
    - from: "report.py (RIM terminal)"
      to: "motores.rim(g_terminal=...)"
      via: "g_terminal = min(roe_terminal*retencao, g_cap)"
      pattern: "min\\(.*g_cap\\)"
    - from: "tests/helpers_blindagem.py::choque_nominal"
      to: "cfg2['macro']['pi_ciclo']"
      via: "perna do g do choque migra de ddm.g_estavel/motores.rim.g_terminal para o novo home"
      pattern: "pi_ciclo"
---

<objective>
A **mudança atômica de valuation** desta fase: derivar `g_cap` na engine, torná-lo a FONTE ÚNICA do crescimento
terminal (substituindo as duas constantes gêmeas de 2,5% `ddm.g_estavel` e `motores.rim.g_terminal`), fechar a
identidade `g_T = min(roe_terminal × retenção, g_cap)` por empresa (GROW-03), e reconciliar a fase explícita com o
livro **adotando** `g_fundamentos` em vez do histórico (GROW-04, D-01) — tudo mantendo o orçamento em 3 graus de
liberdade (D-05).

**CRÍTICO (D-05, MESMO DIFF):** a remoção de `ddm.g_estavel`/`motores.rim.g_terminal` do config, a migração do
`caminho` do grau `PIB_real` no lock, a remoção de `motores.rim.g_terminal` dos `congelados`, e a atualização do
`choque_nominal` (que hoje choca as duas chaves removidas) DEVEM cair no MESMO commit — senão a partição de knobs
quebra (`test_orcamento_de_knobs`) ou a engine levanta KeyError. Como esse commit toca `config.yaml` + arquivos de
teste (`helpers_blindagem.py`), o hook BLIND-05 exige o trailer `Knob-Change-Justification:` (razão ECONÔMICA, SEM
citar ticker) — é o caso legítimo "primitiva/derivação nova + knob + teste nascendo juntos".

**Escopo negativo (regra dura A):** NÃO tocar `Ke`, `ke_teto`, `ke_piso`, `ERP`, `beta`. NÃO mexer em
`excesso_sustentavel`/`ke_g_spread_min` (Plano 03 cobre por teste, não por recalibração). BLIND-02 permanece xfail.

Purpose: curar a metade `g` da Doença 1 — o único parâmetro que os 4 motores compartilham deixa de ser um `g` real
de 2,5% descontado por um Ke nominal.
Output: engine com `g_cap` derivado + adoção do g por fundamentos; orçamento intacto; BLIND-02 ainda xfail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-crescimento-g-grow/11-CONTEXT.md
@.planning/phases/11-crescimento-g-grow/11-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Sítios exatos. Copie a forma; NÃO reescreva a estrutura circundante. -->

report.py — os DOIS sítios que leem a constante de perpetuidade hoje (viram derivação de g_cap):
  report.py:217  g_estavel = cfg["ddm"]["g_estavel"]     # dentro de _intrinseco_por_motor
  report.py:416  g_estavel = cfg["ddm"]["g_estavel"]; a.g_estavel = g_estavel   # dentro de analisar_acao

report.py — os 6 call sites de perpetuidade/terminal a repontar para g_cap (D-04):
  :234  ddm.valor_gordon(dpa_sust*(1+g_estavel), a.ke, g_estavel)      # rota seguradora (Gordon) → usa g_cap
  :248  motores.rim(..., g_terminal=rim_cfg.get("g_terminal"), ...)    # RIM terminal → usa g_T (GROW-03, ver abaixo)
  :293  motores.lucro_normalizado(lpa_mid, a.ke, g_estavel)            # cíclica (Gordon) → usa g_cap
  :296  motores.dcf_crescimento(c.lpa_valuation(), a.g_alto, g_estavel, a.ke, ...)  # dcf perpetuidade → g_cap
  :512  if ... and a.ke > g_estavel:  ... ddm.ddm_dois_estagios(..., g_estavel, a.ke, ...)  # DDM lens → g_cap (×3 chamadas 515/518/522)
  :699  if a.ke is not None and a.ke <= g_estavel:  faltou.append("Ke ≤ g estável...")       # guarda → g_cap

report.py — seleção g_alto a REVERTER (D-01), report.py:426-431 (comentário DDM-FIX-02 em :418 é revertido):
  hoje: g_alto = min(g_historico, g_fundamentos) capado [0,0.25]
  alvo: g_alto = a.g_fundamentos if a.g_fundamentos is not None else a.g_historico; g_alto = max(0.0, min(g_alto, 0.25))
  a.g_fundamentos JÁ é calculado em report.py:415 = growth.crescimento_por_fundamentos(c.roe_valuation(), c.payout_valuation())

report.py — FIX-01, NÃO TOCAR (D-02), report.py:462-463:
  if a.g_alto is not None and a.ke is not None: a.g_alto = min(a.g_alto, a.ke)   # g_cap trava SÓ o terminal, nunca o explícito

RIM terminal — a identidade GROW-03. motores.rim já recebe g_terminal como a taxa de crescimento do RI terminal
(motores.py:128 libera terminal só se g_terminal is not None E ke - g_terminal >= ke_g_spread_min; motores.py:136
ri_terminal = base*(1+g_terminal)). Os insumos por empresa já existem no call site :245/:250:
  retencao=(1.0 - (c.payout_valuation() or 0.0)); roe_terminal=_roe_through_cycle(c, rim_cfg)

lock — grau PIB_real (calibracao.lock.yaml:84-101): caminho=ddm.g_estavel, valor=0.025 → migrar caminho para o novo
home + valor 0.02, NO MESMO COMMIT (o próprio bloco instrui isso).
lock — congelados (calibracao.lock.yaml:149): `motores.rim.g_terminal: 0.025` SAI de congelados (chave removida do config).

choque_nominal — tests/helpers_blindagem.py:755-758 (perna da TAXA do choque de inflação):
  cfg2["capm"]["rf_local"] += delta
  cfg2["ddm"]["g_estavel"] += delta          # <- chave removida; migrar para cfg2["macro"]["pi_ciclo"] += delta
  cfg2["motores"]["rim"]["g_terminal"] += delta   # <- chave removida; some (o g agora deriva de pi_ciclo)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: derivar g_cap na engine, adotar g_fundamentos, fechar g_T por empresa (report.py)</name>
  <behavior>
    - Com pi_ciclo=0.0518 e PIB_real=0.02 carimbados/lidos de cfg, `g_cap` derivado ≈ 0.0728 (não digitado em lugar nenhum).
    - Para o ITUB4 (snapshot): `a.g_alto == a.g_fundamentos` (adoção) e `a.g_fundamentos ≈ 0.1029` (antes do teto Ke).
    - Quando g_fundamentos is None: `a.g_alto == a.g_historico` (fallback de sanidade).
    - Teto absoluto 0.25 e teto FIX-01 (g_alto ≤ Ke) permanecem: um g_fund > Ke ainda resulta g_alto == Ke.
    - RIM terminal: g_T passado a motores.rim == min(roe_terminal × retenção, g_cap); satura em g_cap quando roe_T×ret é alto.
    - Todo consumidor de perpetuidade (Gordon seguradora/cíclica, dcf, DDM lens) usa g_cap — never-raise preservado (valor_gordon devolve None quando ke−g≤0).
  </behavior>
  <read_first>
    - src/analista/report/report.py:200-300 (bloco _intrinseco_por_motor: sítios :217, :234, :248, :293, :296)
    - src/analista/report/report.py:405-463 (bloco g_historico/g_fundamentos/g_alto/g_estavel + FIX-01 Ke)
    - src/analista/report/report.py:505-525 e :695-700 (DDM lens + guarda de convergência)
    - src/analista/report/report.py:955-1015 (rótulos markdown "g estável" + header de sensibilidade)
    - src/analista/core/motores.py:65-145 (rim: como g_terminal libera/cresce o RI terminal)
    - src/analista/core/ddm.py:37-46 (valor_gordon: None quando ke−g ≤ 0, never-raise)
    - .planning/phases/11-crescimento-g-grow/11-CONTEXT.md §decisions D-01, D-02, D-03, D-04
  </read_first>
  <action>
    Introduza uma derivação local `g_cap = (1.0 + cfg["macro"]["pi_ciclo"]) * (1.0 + <PIB_real de cfg>) - 1.0`
    nos DOIS sítios que hoje leem `g_estavel = cfg["ddm"]["g_estavel"]` (report.py:217 e :416). Onde ler `PIB_real`:
    do novo home de config escolhido na Task 2 (ex.: `cfg["ddm"]["pib_real"]`) — leia da MESMA fonte que a Task 2
    grava. Em report.py:417 mantenha `a.g_estavel = g_cap` (o campo do dataclass agora carrega o g_cap derivado,
    ~0.0728) para os rótulos. Substitua TODOS os usos de `g_estavel` pelos usos de `g_cap` nos 6 call sites
    (:234, :293, :296, :512/515/518/522, :699) — FONTE ÚNICA (D-04).
    Para o RIM (report.py:248): calcule por empresa `g_T = min(roe_terminal * retencao, g_cap)` usando
    `retencao = 1.0 - (c.payout_valuation() or 0.0)` e `roe_terminal = _roe_through_cycle(c, rim_cfg)`; se
    `roe_terminal` for None, use `g_cap` (o cap puro). Passe `g_terminal=g_T` a `motores.rim` (GROW-03 — identidade
    fechada por empresa, não constante). NÃO altere a assinatura de `motores.rim`.
    Reverta a seleção `g_alto` (report.py:426-431, D-01): `g_alto = a.g_fundamentos if a.g_fundamentos is not None
    else a.g_historico; g_alto = max(0.0, min(g_alto, 0.25))`. Reescreva o comentário DDM-FIX-02 (report.py:418-425)
    para a nova doutrina: a fase explícita ADOTA o g por fundamentos (método do livro, Cap. 14.3); `g_historico`
    fica como número de sanidade e fallback. NÃO toque no bloco FIX-01 (report.py:462-463) — o teto Ke e o teto
    absoluto 0.25 na fase explícita ficam INTOCADOS (D-02: `g_cap` trava SÓ o terminal, nunca o `g` explícito;
    travar o explícito derrubaria o caso soberano ITUB4=R$37,22 da Fase 14).
    Atualize os rótulos markdown (report.py:960-962 e o header de sensibilidade em :1013, discrição/apresentação):
    "g estável (perpetuidade)" 2,5% → "g_cap (perpetuidade, derivado)" ~7,28%; deixe explícito que `g_historico`
    agora é sanidade. Mudança de apresentação, não de método.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from analista.report import report; import tests.helpers_blindagem as h" 2>/dev/null; cd tests && ../.venv/bin/python -c "import helpers_blindagem as h; from analista.report import report; emp,cfg=h.cfg_e_empresas_do_snapshot(); cfg.setdefault('macro',{})['pi_ciclo']=0.0518; import yaml; gcap=(1+cfg['macro']['pi_ciclo'])*(1+0.02)-1; print('g_cap', round(gcap,4)); assert abs(gcap-0.0728)<0.0005; it={c.ticker:c for c in emp}['ITUB4']; a=report.analisar_acao(it,cfg); print('g_alto',a.g_alto,'g_fund',a.g_fundamentos); assert a.g_fundamentos is not None; assert abs(a.g_alto-min(a.g_fundamentos,a.ke))<1e-9"</automated>
  </verify>
  <acceptance_criteria>
    - `g_cap` é derivado em report.py de `cfg['macro']['pi_ciclo']` e do PIB_real de config; ≈ 0.0728. Nenhum `0.0728` literal no código.
    - ITUB4: `a.g_alto == min(a.g_fundamentos, a.ke)` (adoção + teto FIX-01) e `a.g_fundamentos ≈ 0.1029`.
    - `grep -n "cfg\[.ddm.\]\[.g_estavel.\]\|g_terminal=rim_cfg" src/analista/report/report.py` → 0 (nenhuma leitura das chaves antigas sobra).
    - O RIM recebe `g_terminal = min(roe_terminal*retencao, g_cap)` (grep por `min(` + `g_cap` no call site do RIM).
    - FIX-01 (report.py:462-463) byte-idêntico; assinatura de `motores.rim`/`ddm.valor_gordon` inalterada.
  </acceptance_criteria>
  <done>A engine deriva g_cap, adota g_fundamentos na fase explícita, e fecha g_T por empresa no RIM — com FIX-01 intocado.</done>
</task>

<task type="auto">
  <name>Task 2: migração de knob no MESMO diff (config + lock + choque_nominal) — commit com trailer</name>
  <read_first>
    - config.yaml:56-98 (blocos normalizacao/macro/capm/ddm — onde g_estavel vive) e config.yaml:256-264 (motores.rim.g_terminal/excesso_sustentavel/ke_g_spread_min)
    - calibracao.lock.yaml:30-101 (escopo + graus_de_liberdade, bloco PIB_real com a instrução literal de migração)
    - calibracao.lock.yaml:123-161 (congelados: a linha motores.rim.g_terminal:0.025 a remover; a partição das folhas)
    - tests/test_blindagem_orcamento.py:43-164 (o que test_orcamento_de_knobs e test_knobs_batem_com_o_lock exigem)
    - tests/helpers_blindagem.py:727-758 (choque_nominal — a perna da taxa que choca as chaves removidas)
    - .githooks/commit-msg (BLIND-05: config.yaml + tests/* exige Knob-Change-Justification sem ticker)
    - .planning/phases/11-crescimento-g-grow/11-CONTEXT.md §decisions D-05
  </read_first>
  <action>
    Confirme o hook instalado: `git config core.hooksPath` deve retornar `.githooks` (se não, `git config
    core.hooksPath .githooks`).
    Em config.yaml: (a) REMOVA a folha `ddm.g_estavel` (config.yaml:97); (b) REMOVA a folha `motores.rim.g_terminal`
    (config.yaml:261, incluindo o comentário CAL-01); (c) ADICIONE o novo home do PIB_real DENTRO de um bloco do
    escopo (motores/capm/ddm/normalizacao) — recomendado `ddm.pib_real: 0.02` com comentário econômico (PIB real
    estrutural ~2,0%, Focus/BCB; entra na derivação g_cap = (1+pi_ciclo)(1+PIB_real)−1). NÃO ponha o PIB_real no
    bloco `macro` (está fora do escopo → viraria fantasma na partição). Comentário sem ticker.
    Em calibracao.lock.yaml (MESMO commit): (a) grau `PIB_real` (lock:84-86) → `caminho: ddm.pib_real`, `valor: 0.02`
    (mesmo home escolhido no config); (b) REMOVA de `congelados` a linha `motores.rim.g_terminal: 0.025` (lock:149-150);
    (c) atualize os comentários de contagem ("30 folhas"/"motores 11"/"ddm 5") para refletir a nova partição de 29
    folhas (motores 10 + capm 12 + ddm 5 + normalizacao 2), 3 graus + 26 congelados. `excesso_sustentavel` e
    `ke_g_spread_min` PERMANECEM em congelados intocados (D-07/Plano 03 os cobre por teste, não por recalibração).
    Em tests/helpers_blindagem.py `choque_nominal` (linhas 755-758): a perna do `g` do choque migra — troque
    `cfg2["ddm"]["g_estavel"] += delta` e `cfg2["motores"]["rim"]["g_terminal"] += delta` por
    `cfg2["macro"]["pi_ciclo"] += delta` (o novo home do `g` da perpetuidade; a derivação de g_cap propaga a perna).
    Atualize a docstring de `choque_nominal` (linha 733) para citar `macro.pi_ciclo` em vez das chaves removidas.
    Commit ÚNICO e atômico contendo report.py (Task 1) + config.yaml + calibracao.lock.yaml + helpers_blindagem.py,
    com o trailer no corpo da mensagem: `Knob-Change-Justification: PIB real migra para o home derivado do g_cap;
    o grau de liberdade continua sendo um (crescimento estrutural na perpetuidade), muda apenas onde mora e como e'
    consumido` (razão ECONÔMICA, SEM ticker). O hook valida contra data/ticker_map.json.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -k "orcamento_de_knobs or knobs_batem_com_o_lock" -q && cd tests && ../.venv/bin/python -c "import yaml; c=yaml.safe_load(open('../config.yaml')); assert 'g_estavel' not in c.get('ddm',{}), 'g_estavel ainda existe'; assert 'g_terminal' not in c.get('motores',{}).get('rim',{}), 'g_terminal ainda existe'; assert abs(c['ddm']['pib_real']-0.02)<1e-9; l=yaml.safe_load(open('../calibracao.lock.yaml')); assert l['graus_de_liberdade']['PIB_real']['caminho']=='ddm.pib_real'; assert 'motores.rim.g_terminal' not in l['congelados']; print('migracao OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `config.yaml` NÃO tem mais `ddm.g_estavel` nem `motores.rim.g_terminal`; TEM `ddm.pib_real: 0.02`.
    - `calibracao.lock.yaml`: grau `PIB_real` com `caminho: ddm.pib_real` e `valor: 0.02`; `motores.rim.g_terminal` fora de `congelados`.
    - `pytest -k "orcamento_de_knobs or knobs_batem_com_o_lock"` → verde (partição 29 folhas = 3 graus | 26 congelados; lock bate com config).
    - `choque_nominal` choca `cfg2['macro']['pi_ciclo']` (grep); não referencia mais `g_estavel`/`g_terminal`.
    - O commit tem o trailer `Knob-Change-Justification:` sem ticker (o hook BLIND-05 deixou passar; `git log -1` mostra o trailer).
  </acceptance_criteria>
  <done>PIB_real migrou de home no MESMO diff (config+lock), g_terminal saiu de congelados, choque_nominal choca o novo home, e o orçamento segue em 3 graus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cfg (carimbado) → engine | A engine lê `pi_ciclo`/`pib_real` de `cfg`; permanece offline/determinística. |
| commit (knob) → git hook | O co-change config+teste atravessa o hook BLIND-05, que exige justificativa auditável sem ticker. |

Sistema offline/determinístico: sem input de usuário em runtime, sem auth, sem rede na engine. A superfície real é
integridade da migração de knob (partição de orçamento) e reprodutibilidade da derivação.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | migração de knob silenciosa (4º grau de liberdade escondido) | mitigate | `test_orcamento_de_knobs` (partição) + `test_knobs_batem_com_o_lock` (lock↔config) reprovam qualquer knob novo/órfão; a mudança fica visível no diff config+lock no MESMO commit (D-05). |
| T-11-04 | Repudiation | knob calibrado contra um ticker | mitigate | Hook BLIND-05 + `test_nenhuma_justificativa_de_knob_menciona_ticker`: o trailer `Knob-Change-Justification:` (sem ticker) fica no git log para sempre. |
| T-11-05 | Denial of Service | perpetuidade explosiva sob spread Ke−g apertado | mitigate | `ddm.valor_gordon` devolve None quando ke−g ≤ 0; `motores.rim` só libera terminal se ke−g_terminal ≥ ke_g_spread_min (never-raise). Cobertura formal no Plano 03 (D-07). |

Nenhuma ameaça de web-app se aplica.
</threat_model>

<verification>
- `pytest -q` → 0 failed (default). Goldens de nível que codificam o método antigo do `g` estão em quarentena
  (deselecionados); sua limpeza split-before-delete é o Plano 03. O `-m golden_nivel` pode ficar vermelho AQUI —
  esperado e temporário até o Plano 03.
- `pytest -k invariancia_inflacao_engine_itub4` → **1 xfailed** (NÃO xpassed). Como `xfail_strict`, um XPASS deixaria
  o default VERMELHO — então o "default verde" já prova que BLIND-02 continua falhando (a Doença 1 só cai na Fase 12).
- `pytest -k "orcamento_de_knobs or knobs_batem_com_o_lock"` → verde.
- `git log -1 --format=%B` mostra o trailer `Knob-Change-Justification:` sem ticker.
</verification>

<success_criteria>
- `g_cap` derivado (~7,28%) é a fonte única do crescimento terminal (GROW-01/D-04).
- `g_T = min(roe_terminal × retenção, g_cap)` por empresa no RIM (GROW-03).
- A fase explícita adota `g_fundamentos` (ITUB4 ~10,29%); histórico vira sanidade/fallback (GROW-04/D-01).
- Orçamento em 3 graus; PIB_real migrado no MESMO diff (D-05); BLIND-02 ainda xfail.
</success_criteria>

<output>
Após completar, crie `.planning/phases/11-crescimento-g-grow/11-02-SUMMARY.md`.
</output>
