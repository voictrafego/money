---
phase: 17-modo-trading-candlestick-tradingview-lightweight-charts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app.py, .planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/.phase-base-sha]
autonomous: true
requirements: [LWC-01, LWC-03]
must_haves:
  truths:
    - "Usuário liga um toggle 'Modo Trading' na aba de swing; o Plotly continua a vista default"
    - "Modo Trading renderiza um candlestick via Lightweight Charts v5 com scroll-zoom, pan, crosshair com rótulos nos eixos, Y-autoscale e linha de último preço"
    - "Alternar Modo Trading não dispara novo fetch nem recálculo da engine (usa f.ohlc / sw / sinais já montados)"
    - "O range visível persiste entre reruns do Streamlit (togglar um overlay não reseta o zoom)"
  artifacts:
    - path: "app.py"
      provides: "Função _render_lwc(...) + toggle de vista 'Modo Trading' dentro do bloco de swing"
      contains: "def _render_lwc"
    - path: "app.py"
      provides: "CDN Lightweight Charts v5.2.0 pinado com Subresource Integrity"
      contains: "lightweight-charts@5.2.0"
  key_links:
    - from: "app.py:_render_lwc"
      to: "f.ohlc"
      via: "serialização OHLC → JSON candles/vols (sem recálculo)"
      pattern: "f\\.ohlc"
    - from: "toggle de vista"
      to: "_render_lwc vs bloco Plotly"
      via: "ramo condicional que não refaz fetch/frame_intraday"
      pattern: "_render_lwc\\("
---

<objective>
Adicionar uma vista "Modo Trading" (toggle) DENTRO do bloco de swing (`_render_swing` em `app.py`)
que renderiza o candlestick puro do ticker/timeframe via TradingView Lightweight Charts v5, carregada
por `st.components.v1.html` + CDN unpkg pinado — ZERO dependência Python nova. Entrega a UX que o
Plotly não dá (scroll-zoom, pan, crosshair com rótulos nos eixos, Y-autoscale, linha de último preço)
e persiste o range visível entre reruns do Streamlit.

Purpose: fundação da Phase 17 (LWC-01 + persistência de range LWC-03). Os overlays da engine (LWC-02)
entram no plano 02; a verificação final no plano 03.
Output: `app.py` ganha `_render_lwc(...)` e um toggle de vista; o Plotly permanece a vista default e
intacto; `grafico.py` não é tocado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-CONTEXT.md
@.planning/spikes/001-tv-feel-candlestick/spike_app.py
@.planning/spikes/CONVENTIONS.md

<interfaces>
<!-- Contratos read-only já existentes que _render_lwc consome. NÃO recalcular. -->

FrameOHLC (retorno de frame_intraday em app.py):
  f.ohlc            -> DataFrame nominal, colunas Open/High/Low/Close, index datetime (America/Sao_Paulo)
  f.ohlc_ajustado  -> DataFrame ajustado por split (usado só p/ indicadores, NÃO p/ o candle)
  f.barra_viva      -> bool (última barra em formação)
  f.ultima_barra_ts -> Timestamp | None
  f.idx_ultima_fechada -> int | None
  f.disponivel / f.motivo

Bloco de swing existente (app.py:583-907): `_render_swing()` é um `@st.fragment`; já monta
`f`, `sinais = indicators.calcular(...)`, `sw = setup.montar_setup(...)`, `est = st.session_state["tec_estado_swing"]`
e reserva `grafico_box = st.container()` ANTES do expander de Overlays. O candlestick Plotly é
construído em grafico_box (app.py:722-828).

Spike de referência (produção-alvo): `.planning/spikes/001-tv-feel-candlestick/spike_app.py`
— template `components.html` funcional: createChart(tema dark) → addSeries(CandlestickSeries,…),
HistogramSeries p/ volume (priceScaleId próprio + scaleMargins), crosshair CrosshairMode.Normal,
rightPriceScale.autoScale, priceLineVisible/lastValueVisible, ResizeObserver p/ largura.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toggle de vista "Modo Trading" + _render_lwc (candlestick base)</name>
  <files>app.py</files>
  <read_first>
    - app.py (bloco de swing 583-907 — ler INTEIRO: onde `est`/`grafico_box`/`f`/`sinais`/`sw` são montados e onde o Plotly é renderizado)
    - .planning/spikes/001-tv-feel-candlestick/spike_app.py (template components.html funcional a portar)
    - .planning/spikes/CONVENTIONS.md (API v5: addSeries(CandlestickSeries,…) NÃO addCandlestickSeries; gotchas)
    - .planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-CONTEXT.md (decisões travadas LWC-01)
  </read_first>
  <action>
    ANTES de editar `app.py`, capturar a BASE FIXA da fase para os gates de invariância dos planos 02/03:
    `git rev-parse HEAD` e gravar o SHA em
    `.planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/.phase-base-sha`
    (uma linha, só o SHA) e citá-lo no 17-01-SUMMARY. Esse é o ponto de comparação p/ provar que
    `grafico.py` ficou intacto (o plano 03 diffa contra ele — evita `HEAD~N` com contagem imprevisível).

    Em `app.py`, dentro de `_render_swing` (ramo `else:` onde `f.disponivel` é True), adicionar um
    controle de vista ANTES de `grafico_box` — um `st.segmented_control` ou `st.radio` horizontal
    (rótulo "Vista", opções: "Plotly" default e "Modo Trading"), com estado próprio isolado em
    `st.session_state` (chave própria, ex.: "swing_vista"; NUNCA a chave da aba Analisar). Default = "Plotly".
    O Plotly permanece a vista default (LWC-01): quando "Plotly" selecionado, o bloco Plotly atual
    (app.py:722-828) roda inalterado; quando "Modo Trading" selecionado, no lugar do bloco Plotly
    (dentro de `grafico_box`) chamar `_render_lwc(f, sw, sinais, est, ticker, tf_key)`. O selo de atraso,
    o card de veredito e o expander de Overlays continuam iguais nas duas vistas.

    Criar a função de módulo `def _render_lwc(f, sw, sinais, est, ticker, tf_key):` (nível de app.py, fora
    do fragment — decisão de organização é sua, per Claude's Discretion no CONTEXT). IMPORTANTE: a
    assinatura JÁ inclui `tf_key` (a chave de timeframe pt-BR→engine que o bloco de swing tem em `tf_key`)
    porque `_render_lwc` é module-level (sem closure) e os planos posteriores dependem dela: a Task 2
    deste plano usa `tf_key` p/ a chave do `localStorage` por (ticker, tf) e a Task 2 do plano 02 usa
    `tf_key` p/ converter `ts` de pivô em epoch. Fixar `tf_key` no contrato AGORA evita refactor do call
    site na wave 2. Ela:
    1. Serializa `f.ohlc` em duas listas JSON (padrão do spike_app.py): `candles` = [{time, open, high,
       low, close}] e `vols` = [{time, value, color}] (verde `rgba(38,166,154,0.5)` se close>=open,
       vermelho `rgba(239,83,80,0.5)`). IMPORTANTE (o spike só cobriu diário): para `f.ohlc` com index
       DATETIME intraday, o campo `time` do LWC precisa ser UNIX epoch SEGUNDOS (UTCTimestamp), não a
       string "%Y-%m-%d". Detectar o timeframe: diário → string "%Y-%m-%d"; intraday (1h/30m/5m) →
       inteiro epoch segundos (usar o timestamp já em America/Sao_Paulo convertido p/ epoch UTC). Isso
       mantém o crosshair e o eixo de tempo corretos.
    2. Monta o template HTML espelhando o spike 001: `createChart` tema dark (#0e1117), grid #1c2030,
       `crosshair: CrosshairMode.Normal`, `rightPriceScale.autoScale: true`, `timeScale` com
       `rightOffset: 6` e `timeVisible: true` p/ intraday, `handleScroll`/`handleScale` true;
       `addSeries(CandlestickSeries, …)` com upColor #26a69a / downColor #ef5350, `priceLineVisible:
       true`, `lastValueVisible: true`; volume via `addSeries(HistogramSeries, …)` priceScaleId próprio
       + `scaleMargins {top:0.82, bottom:0}`; `chart.timeScale().fitContent()`; ResizeObserver p/ largura.
    3. Renderiza via `st.components.v1.html(html, height=580)` (o import de components já existe no spike;
       garantir o import em app.py).
    SUPPLY-CHAIN: o `<script src>` do CDN DEVE usar a URL pinada
    `https://unpkg.com/lightweight-charts@5.2.0/dist/lightweight-charts.standalone.production.js`
    COM atributos `integrity="sha384-…"` (SRI hash) e `crossorigin="anonymous"`. O executor DEVE computar
    o hash real do arquivo `@5.2.0` (rede ao unpkg + `openssl` estão disponíveis): baixar via
    `curl -sL https://unpkg.com/lightweight-charts@5.2.0/dist/lightweight-charts.standalone.production.js`
    e gerar `openssl dgst -sha384 -binary | openssl base64 -A`, colando o resultado em `integrity=`. A
    disposição "mitigate" da ameaça T-17-01 DEPENDE disso — não é opcional. Apenas se a rede falhar em
    RUNTIME (não por conveniência) documentar a falha e cair para URL-pinada-sem-SRI como último recurso.
    Não tocar `grafico.py` nem o bloco Plotly.
  </action>
  <verify>
    <automated>cd "$PWD" && .venv/bin/python -m pytest -q 2>&1 | tail -2 && .venv/bin/python -c "import ast,sys; ast.parse(open('app.py').read()); print('app.py OK')" && grep -q "def _render_lwc" app.py && grep -q "lightweight-charts@5.2.0" app.py && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `app.py` contém `def _render_lwc(` e a chamada condicional gateada pelo toggle de vista.
    - `grep -c "lightweight-charts@5.2.0" app.py` >= 1 e o `<script>` usa a URL `standalone.production.js` pinada.
    - O `<script>` do CDN contém `integrity="sha384-` (hash SRI REAL computado) + `crossorigin="anonymous"` — `grep -q 'integrity="sha384-' app.py` PASSA.
    - `_render_lwc` tem a assinatura `def _render_lwc(f, sw, sinais, est, ticker, tf_key)` (com `tf_key`) e o call site passa `tf_key`.
    - `grep "addCandlestickSeries" app.py` retorna VAZIO (API v5 usa `addSeries(CandlestickSeries,…)`).
    - `.venv/bin/python -m pytest -q` → 283 passed (zero regressão; goldens intactos).
    - `git diff --name-only` NÃO inclui `src/analista/grafico.py` (grafico.py intacto).
    - No navegador (verificado no plano 03): selecionar "Modo Trading" mostra candlestick TV-like
      (scroll=zoom, pan, crosshair com rótulos, Y-autoscale, linha de último preço); "Plotly" continua default.
  </acceptance_criteria>
  <done>Toggle de vista funciona; `_render_lwc` renderiza o candlestick base via LWC v5 + CDN pinado/SRI; Plotly default e intacto; 283 goldens verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Persistência do range visível entre reruns (LWC-03)</name>
  <files>app.py</files>
  <read_first>
    - app.py (a função `_render_lwc` recém-criada — já recebe `tf_key` na assinatura, ver Task 1 — + o toggle de vista)
    - .planning/spikes/002-overlays-da-engine/spike_app.py (padrão try/catch das overlays a espelhar no acesso ao localStorage)
    - .planning/spikes/CONVENTIONS.md (gotcha: rerun re-renderiza o iframe → persistir range e reaplicar via timeScale().setVisibleRange())
    - .planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-CONTEXT.md (Robustez Streamlit LWC-03: components.html é sandbox unidirecional Python→JS)
  </read_first>
  <action>
    Fazer o range visível do chart PERSISTIR entre reruns do Streamlit (togglar um overlay, trocar de
    aba de expander ou o auto-refresh do fragment NÃO pode resetar o zoom para `fitContent`). Como
    `components.html` é unidirecional (Python→JS, sem callback de volta), a leitura do range de volta
    para Python não está disponível — a ponte é CLIENT-SIDE dentro do iframe: no JS do template,
    assinar `chart.timeScale().subscribeVisibleLogicalRangeChange(...)` (ou
    `subscribeVisibleTimeRangeChange`) e GRAVAR o range corrente em `window.localStorage` sob uma chave
    estável por (ticker, timeframe) — ex.: `lwc_range_<ticker>_<tf_key>`, compondo a chave a partir de
    `ticker` e `tf_key` (ambos já passados do Python p/ o template; `tf_key` está na assinatura de
    `_render_lwc` desde a Task 1). Ao criar o chart, LER essa chave do localStorage e, se existir, aplicar
    via `chart.timeScale().setVisibleLogicalRange(...)` / `setVisibleRange(...)`; caso contrário
    `fitContent()`. A chave por par evita "vazar" zoom de um ticker para outro.

    OBRIGATÓRIO (robustez — o iframe de `components.html` pode ter origem opaca/sandbox e QUALQUER acesso
    a `localStorage` pode lançar `SecurityError`, que abortaria o script inteiro e IMPEDIRIA o candlestick
    de renderizar): envolver TODO acesso a `localStorage` — tanto o `getItem` (leitura na criação) quanto
    o `setItem` (gravação no callback de range) — em `try/catch` INDEPENDENTES, espelhando o padrão
    try/catch das overlays do spike 002. No `catch` da leitura, cair para `chart.timeScale().fitContent()`
    (o chart SEMPRE renderiza, mesmo sem persistência); no `catch` da gravação, apenas `console.log('[lwc]
    localStorage indisponível')` e seguir. A persistência é best-effort; a renderização do candle NUNCA
    pode depender dela. Manter o comportamento de estudo: nada disso recalcula a engine.
    (Nota de fidelidade ao CONTEXT: a decisão travada cita "session_state + setVisibleRange"; como o
    round-trip JS→Python não existe em components.html, o localStorage por-iframe é o mecanismo que
    entrega o mesmo comportamento observável de persistência — documentar isso no SUMMARY.)
  </action>
  <verify>
    <automated>cd "$PWD" && .venv/bin/python -m pytest -q 2>&1 | tail -2 && grep -Eq "setVisibleLogicalRange|setVisibleRange" app.py && grep -q "localStorage" app.py && grep -q "fitContent" app.py && echo RANGE_GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `app.py` contém no template LWC uma assinatura de mudança de range e uma reaplicação via
      `setVisibleLogicalRange`/`setVisibleRange` (grep encontra ambos os lados: gravar + reaplicar).
    - A chave de persistência é por (ticker, timeframe) — grep mostra a chave composta com `ticker`/`tf_key`.
    - Todo acesso a `localStorage` (`getItem` E `setItem`) está dentro de `try`/`catch` e o `catch` da
      leitura chama `fitContent()` como fallback — checável: o template contém `try` em volta de
      `localStorage.getItem`/`setItem` e `fitContent()` no `catch` (`grep -q "fitContent" app.py` PASSA
      e o bloco de localStorage está sob try/catch). Assim um `SecurityError` NÃO impede o candle de renderizar.
    - `.venv/bin/python -m pytest -q` → 283 passed.
    - No navegador (plano 03): dar zoom/pan no Modo Trading, então togglar um overlay (força um rerun);
      o range visível é PRESERVADO (não volta para fitContent).
  </acceptance_criteria>
  <done>O zoom/pan do Modo Trading sobrevive a reruns do Streamlit via persistência client-side por par ticker/timeframe; goldens verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN unpkg → iframe do usuário | JS de terceiros (Lightweight Charts) carregado em runtime no browser via `<script src>` |
| Python → JS (components.html) | Dados OHLC serializados em JSON, injetados no template (sandbox unidirecional) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `<script>` do CDN unpkg (`lightweight-charts@5.2.0`) | mitigate | CDN pinado por VERSÃO exata (`@5.2.0`, feito) + `integrity="sha384-…"` (SRI) + `crossorigin="anonymous"` no `<script>`; o browser rejeita o bundle se o hash não bater |
| T-17-02 | Info Disclosure | Dados OHLC serializados no template HTML | accept | Dados públicos de mercado (Yahoo), sem PII/segredo; app Streamlit local sem auth; superfície mínima |

Severidade máxima: baixa. Bloquear apenas se o SRI não puder ser aplicado E a URL não estiver pinada (não é o caso — a URL já é pinada).
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 283 passed (goldens intactos; `grafico.py` e aba Analisar não tocados).
- `python -c "import ast; ast.parse(open('app.py').read())"` sem erro de sintaxe.
- `grep "def _render_lwc" app.py` e `grep "lightweight-charts@5.2.0" app.py` presentes.
- Smoke no navegador coberto pelo plano 03 (checkpoint humano).
</verification>

<success_criteria>
- Toggle "Modo Trading" existe na aba de swing; Plotly é a vista default e permanece inalterado.
- `_render_lwc` renderiza candlestick + volume + linha de último preço via LWC v5 (CDN pinado + SRI).
- Range visível persiste entre reruns.
- 283 testes golden verdes; zero dependência Python nova; `grafico.py` intacto; `app.py` só render.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-01-SUMMARY.md`
(incluir: SHA base da fase gravado em `.phase-base-sha`, mecanismo de persistência de range escolhido, hash SRI aplicado, formato de time diário vs intraday).
</output>
