---
phase: 17-modo-trading-candlestick-tradingview-lightweight-charts
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [app.py]
autonomous: true
requirements: [LWC-02]
must_haves:
  truths:
    - "No Modo Trading, a zona de entrada e as zonas de S/R aparecem como BANDAS preenchidas (BandPrimitive)"
    - "Stop, alvo e níveis de Fibonacci aparecem como LINHAS rotuladas no eixo (createPriceLine)"
    - "Pivôs e padrões aparecem como MARKERS (createSeriesMarkers); neckline opcional como LineSeries de 2 pontos"
    - "Todas as sobreposições LEEM campos de SetupSwing/sinais sem recalcular o método; copy neutra de estudo mantida"
    - "Cada bloco de overlay é gateado pelo mesmo toggle est[...] do Plotly e degrada sem quebrar quando os campos são None/vazios"
  artifacts:
    - path: "app.py"
      provides: "Helper BandPrimitive (series primitive v5) no template de _render_lwc"
      contains: "BandPrimitive"
    - path: "app.py"
      provides: "createPriceLine para stop/alvo/Fibonacci + createSeriesMarkers para pivôs/padrões"
      contains: "createPriceLine"
  key_links:
    - from: "_render_lwc"
      to: "sw.entrada_zona / sw.stop / sw.alvo"
      via: "serialização read-only dos níveis do setup (sem recálculo)"
      pattern: "sw\\.(entrada_zona|stop|alvo)"
    - from: "_render_lwc"
      to: "sinais.niveis (suportes/resistencias/fib_retracoes) e sinais.padroes.lista"
      via: "serialização read-only para bandas/linhas/markers"
      pattern: "sinais\\.niveis|sinais\\.padroes"
    - from: "cada overlay JS"
      to: "est['sr_on'|'niveis_setup_on'|'fib_on'|'padroes_on']"
      via: "flags Python passadas ao template (paridade com app.py:751-799)"
      pattern: "sr_on|niveis_setup_on|fib_on|padroes_on"
---

<objective>
Portar as sobreposições da engine para o chart LWC de `_render_lwc`, espelhando 1:1 o que o Plotly já
faz em `app.py:751-799` — mas lendo os MESMOS campos de `SetupSwing`/`sinais`, SEM recalcular o método:
zona de entrada e zonas de S/R como BANDAS (`BandPrimitive`), stop/alvo/Fibonacci como LINHAS rotuladas
(`createPriceLine`), pivôs/padrões como MARKERS (`createSeriesMarkers`). Cada bloco gateado pelo mesmo
toggle `est[...]` e degradando sem quebrar quando os campos são None/vazios. Copy neutra de estudo mantida.

Purpose: entrega LWC-02 — paridade de overlays entre o Modo Trading e o Plotly.
Output: `_render_lwc` ganha o helper `BandPrimitive` + priceLines + markers; `grafico.py` intacto; `app.py` só render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-CONTEXT.md
@.planning/spikes/002-overlays-da-engine/spike_app.py
@.planning/spikes/CONVENTIONS.md
@.planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-01-SUMMARY.md

<interfaces>
<!-- Campos read-only lidos pelos overlays. NÃO recalcular. Espelham app.py:751-799. -->

SetupSwing (sw) — de src/analista/report/setup.py:
  sw.entrada_zona -> tuple (lo, hi) | None   (zona de entrada → BANDA azul)
  sw.stop         -> float | None            (→ priceLine vermelho tracejado, título "stop (estudo)")
  sw.alvo         -> float | None            (→ priceLine verde tracejado, título "alvo (estudo)")
  sw.grade / sw.score / sw.decomposicao      (usados só pelo card de veredito, não no chart)

SinaisTecnicos (sinais):
  sinais.niveis.suportes      -> list[(lo,hi)]  (→ BANDAS verdes, S/R)
  sinais.niveis.resistencias  -> list[(lo,hi)]  (→ BANDAS vermelhas, S/R)
  sinais.niveis.fib_retracoes -> dict{nome: preco}  (→ priceLines pontilhados roxos "Fib {nome}")
  sinais.niveis.risco_retorno -> str
  sinais.padroes.lista        -> list[PadraoGrafico]:
       p.pivos_envolvidos -> dict{ts: price}   (→ markers)
       p.tipo   -> "duplo_topo"|"duplo_fundo"|"oco"|"oco_invertido"  (cor: alta=#2ca02c, baixa=#d62728)
       p.estado -> "confirmado"|"em_formacao"
       p.neckline -> float   (neckline horizontal → LineSeries opcional de 2 pontos)
       p.alvo     -> float

est (st.session_state["tec_estado_swing"]) — flags de overlay já existentes:
  est["sr_on"], est["niveis_setup_on"], est["fib_on"], est["padroes_on"]

Spike de referência (produção-alvo): `.planning/spikes/002-overlays-da-engine/spike_app.py`
— BandPrimitive (class BandPrimitive + BandPaneView: zOrder 'bottom', priceToCoordinate + fillRect em
useBitmapCoordinateSpace), createPriceLine({price,color,lineWidth,lineStyle,axisLabelVisible,title}),
createSeriesMarkers(candle, [{time,position,color,shape,text}]), tudo em try/catch + console.log.

Overlay Plotly a espelhar (comportamento canônico): app.py:751-799.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BandPrimitive + priceLines (zona/S-R como bandas; stop/alvo/Fib como linhas)</name>
  <files>app.py</files>
  <read_first>
    - app.py (a função `_render_lwc` do plano 01 + o bloco de overlays Plotly app.py:751-799 a espelhar)
    - .planning/spikes/002-overlays-da-engine/spike_app.py (BandPrimitive + createPriceLine funcionais)
    - .planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-CONTEXT.md (Sobreposições da engine LWC-02: cores entrada azul / suporte verde / resistência vermelho)
  </read_first>
  <action>
    Estender o payload Python de `_render_lwc` e o template JS. No Python, serializar (read-only) os
    níveis já presentes em `sw`/`sinais`, cada grupo condicionado ao respectivo flag `est[...]` (paridade
    exata com app.py:751-771), degradando sem quebrar quando None/vazio:
    - `est["sr_on"] and sinais.niveis is not None` → listas `suportes` e `resistencias` (cada item [lo,hi]).
    - `est["niveis_setup_on"] and sw.entrada_zona` → `entrada` [lo,hi], `stop` (sw.stop), `alvo` (sw.alvo).
    - `est["fib_on"] and sinais.niveis.fib_retracoes` → dict/lista de {nome, preco}.
    Passar tudo no objeto CFG JSON do template (como no spike 002).

    No JS do template, portar o helper `BandPrimitive` (class BandPrimitive + BandPaneView) exatamente
    como no spike 002 (zOrder 'bottom', `priceToCoordinate` + `fillRect` em `useBitmapCoordinateSpace`)
    — helper ÚNICO e reutilizável. Desenhar, dentro de um `try/catch` + `console.log('[lwc] overlays …')`:
    - Zona de entrada → `candle.attachPrimitive(new BandPrimitive(candle, lo, hi, 'rgba(41,98,255,0.18)'))`
      + bordas via createPriceLine pontilhado (azul #2962ff, lineStyle 3, título "entrada").
    - Cada suporte → BandPrimitive verde `rgba(38,166,154,0.10)`; cada resistência → BandPrimitive
      vermelho `rgba(239,83,80,0.10)` (espelha o add_hrect verde/vermelho do Plotly).
    - Stop → createPriceLine vermelho #ef5350 tracejado (lineStyle 2, axisLabelVisible true, title "stop (estudo)").
    - Alvo → createPriceLine verde #26a69a tracejado (title "alvo (estudo)").
    - Cada Fibonacci → createPriceLine roxo #9467bd pontilhado (lineStyle 3, title "Fib {nome}").
    Copy dos títulos NEUTRA/de estudo (gate SWING-02): "stop (estudo)", "alvo (estudo)", nunca imperativa.
    Cada grupo só é emitido se o flag correspondente veio true e a lista não é vazia (o Python já filtra;
    o JS itera o que chegou). Não tocar `grafico.py` nem o bloco Plotly.
  </action>
  <verify>
    <automated>cd "$PWD" && .venv/bin/python -m pytest -q 2>&1 | tail -2 && grep -q "BandPrimitive" app.py && grep -q "createPriceLine" app.py && grep -Eq "sw\.entrada_zona|entrada_zona" app.py && echo OVERLAY_GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `app.py` contém a class `BandPrimitive` no template e chamadas `createPriceLine` para stop/alvo/Fib.
    - As bandas usam cores: entrada azul, suporte verde, resistência vermelho (espelha app.py:751-761).
    - Os grupos de overlay são gateados por `est["sr_on"|"niveis_setup_on"|"fib_on"]` no Python.
    - Títulos de estudo presentes (`grep -q "(estudo)" app.py` para stop/alvo); zero linguagem imperativa.
    - Quando `sw.entrada_zona`/`sinais.niveis` são None/vazios, o template não quebra (try/catch + Python filtra).
    - `.venv/bin/python -m pytest -q` → 283 passed; `grafico.py` intacto (`git diff --name-only` não o lista).
  </acceptance_criteria>
  <done>Zona de entrada e S/R como bandas; stop/alvo/Fibonacci como linhas rotuladas de estudo; tudo read-only de sw/sinais, gateado por est[...]; goldens verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Markers de pivôs/padrões + neckline opcional (createSeriesMarkers)</name>
  <files>app.py</files>
  <read_first>
    - app.py (`_render_lwc` com bandas/priceLines da Task 1 + o bloco Plotly de padrões app.py:772-799)
    - .planning/spikes/002-overlays-da-engine/spike_app.py (createSeriesMarkers funcional)
    - .planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-CONTEXT.md (pivôs/padrões como markers; neckline → LineSeries de 2 pontos; copy "projeção de estudo")
  </read_first>
  <action>
    Portar a anotação de padrões (gate `est["padroes_on"]`, espelhando app.py:775-799) para markers LWC.
    No Python, quando `est["padroes_on"] and sinais.padroes is not None`, serializar por padrão da
    `sinais.padroes.lista`: os pontos de `p.pivos_envolvidos` (dict ts→price) como markers, `p.tipo`,
    `p.estado`, `p.neckline`, `p.alvo`. Converter cada `ts` para o MESMO formato de time do candle
    (string "%Y-%m-%d" diário / epoch segundos intraday — reusar o helper de conversão do plano 01).
    Cor por direção: alta (duplo_fundo/oco_invertido) #2ca02c, baixa (duplo_topo/oco) #d62728.

    No JS, dentro do try/catch, usar `createSeriesMarkers(candle, [...])` com um marker por pivô
    (`{time, position:'belowBar'|'aboveBar' conforme direção, color, shape:'circle', text:'pivô'}`).
    Para cada padrão, desenhar a neckline horizontal como um `LineSeries` de 2 pontos (time inicial/final
    do padrão no mesmo `p.neckline`) — simplificação honesta do MVP (reta inclinada da OCO fica deferida,
    per CONTEXT). Opcional: priceLine do alvo com título "alvo (projeção de estudo)". Rótulo do estado
    "confirmado"/"em formação" no `text` do marker ou no título — copy NEUTRA (gate SWING-02).
    Degradar sem quebrar quando `sinais.padroes` é None ou `.lista` vazia. Não tocar `grafico.py`.
  </action>
  <verify>
    <automated>cd "$PWD" && .venv/bin/python -m pytest -q 2>&1 | tail -2 && grep -q "createSeriesMarkers" app.py && grep -Eq "padroes|padroes_on" app.py && echo MARKERS_GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `app.py` contém `createSeriesMarkers` no template LWC e serialização read-only de `sinais.padroes.lista`.
    - Markers de pivô usam cor por direção (alta verde / baixa vermelho), espelhando `_COR_PAD` do Plotly.
    - Neckline desenhada como LineSeries de 2 pontos no mesmo `p.neckline`.
    - Bloco gateado por `est["padroes_on"]`; degrada sem quebrar quando `sinais.padroes` é None/vazio.
    - Copy de estudo mantida ("projeção de estudo"/"em formação"); zero linguagem imperativa.
    - `.venv/bin/python -m pytest -q` → 283 passed; `grafico.py` intacto.
  </acceptance_criteria>
  <done>Pivôs e padrões aparecem como markers no Modo Trading com neckline opcional, read-only de sinais.padroes, gateado por est["padroes_on"]; copy de estudo; goldens verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN unpkg → iframe | JS de terceiros (Lightweight Charts) já carregado; sem nova origem introduzida por este plano |
| Python → JS (components.html) | Níveis da engine (números) serializados no template; nenhuma entrada de usuário livre atravessa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | mesmo `<script>` CDN do plano 01 | mitigate | Herda o CDN pinado `@5.2.0` + SRI do plano 01; este plano não adiciona nova dependência nem origem |
| T-17-04 | Injection | rótulos/valores serializados no template JS | accept | Valores são numéricos da engine + rótulos de estudo estáticos (chaves controladas); sem input livre do usuário no template; risco baixo |

Severidade máxima: baixa. Sem novos vetores além do CDN já mitigado no plano 01.
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 283 passed.
- `grep -E "BandPrimitive|createPriceLine|createSeriesMarkers" app.py` — os três presentes.
- `git diff --name-only` não inclui `src/analista/grafico.py`.
- Smoke visual no navegador coberto pelo plano 03.
</verification>

<success_criteria>
- Zona de entrada e S/R como bandas; stop/alvo/Fibonacci como linhas rotuladas; pivôs/padrões como markers.
- Tudo read-only de `sw`/`sinais`, gateado por `est[...]`, degradando sem quebrar; copy neutra de estudo.
- 283 goldens verdes; `grafico.py` intacto; `app.py` só render.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-02-SUMMARY.md`.
</output>
