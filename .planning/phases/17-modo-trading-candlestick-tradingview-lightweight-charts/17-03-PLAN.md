---
phase: 17-modo-trading-candlestick-tradingview-lightweight-charts
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified: []
autonomous: false
requirements: [LWC-01, LWC-02, LWC-03]
must_haves:
  truths:
    - "Os 283+ testes golden seguem verdes ao final da fase"
    - "grafico.py permanece intacto (não tocado por nenhum plano da fase)"
    - "app.py permanece thin renderer (só lê a engine; nada de recálculo de método na UI)"
    - "Verificação humana no navegador aprova o Modo Trading sem regressão nas abas existentes"
  artifacts:
    - path: ".planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-03-SUMMARY.md"
      provides: "Registro da verificação (goldens + smoke no navegador)"
  key_links:
    - from: "verificação"
      to: "Modo Trading + Plotly + aba Analisar + Ranking + Garimpo"
      via: "smoke no navegador via Claude-in-Chrome (sem regressão)"
      pattern: "Modo Trading"
---

<objective>
Fechar a Phase 17 com a verificação de aceite: (1) automatizada — os 283+ testes golden verdes,
`grafico.py` intacto e `app.py` ainda thin renderer; (2) humana — smoke no navegador (via Claude-in-Chrome)
do Modo Trading (candlestick TV-like + overlays da engine + persistência de range) SEM regressão nas
abas/vistas existentes (Plotly default, aba Analisar, Ranking, Garimpo).

Purpose: gate de aceite consolidado de LWC-01/02/03 (goldens + navegador), invariante herdado da v1.4.
Output: `17-03-SUMMARY.md` com o resultado da verificação; fase pronta para fechar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-CONTEXT.md
@.planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-01-SUMMARY.md
@.planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificação automatizada — goldens verdes + invariantes intactos</name>
  <files>(nenhum arquivo modificado — verificação read-only: pytest + git diff + grep em app.py)</files>
  <read_first>
    - app.py (revisar que _render_lwc só LÊ f/sw/sinais e não chama indicators.calcular/montar_setup fora da cadeia existente)
    - .planning/ROADMAP.md (Phase 17 success criteria 5 + gates inegociáveis da v1.4)
  </read_first>
  <action>
    Rodar a suíte golden completa e provar os invariantes herdados da v1.4 de forma automatizada:
    1. `.venv/bin/python -m pytest -q` deve reportar 283 passed (ou mais, se novos testes foram
       adicionados) — ZERO falhas/regressão.
    2. Provar que `src/analista/grafico.py` NÃO foi tocado por esta fase: `git diff --stat` desde o
       início da fase (ou contra o commit base do milestone) não deve listar `grafico.py`.
    3. Provar que `app.py` continua thin renderer no caminho LWC: o `_render_lwc` NÃO deve chamar
       `indicators.calcular(` nem `setup.montar_setup(` (esses só existem na cadeia read-only já
       existente do fragment); confirmar por grep que essas chamadas não aparecem dentro de `_render_lwc`.
    Se qualquer item falhar, corrigir a regressão antes de prosseguir ao checkpoint humano.
  </action>
  <verify>
    <automated>cd "$PWD" && .venv/bin/python -m pytest -q 2>&1 | tail -2 && ! git diff --name-only HEAD~6..HEAD -- src/analista/grafico.py 2>/dev/null | grep -q grafico.py && echo GRAFICO_INTACTO && awk '/def _render_lwc/{f=1} f&&/^def [a-zA-Z]/&&!/def _render_lwc/{f=0} f' app.py | grep -Eq "indicators\.calcular|montar_setup" && echo "FAIL_RECALC" || echo "THIN_RENDERER_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` → 283 passed (ou mais); zero falhas.
    - `git diff` da fase NÃO lista `src/analista/grafico.py`.
    - `_render_lwc` não contém chamadas a `indicators.calcular(` nem `setup.montar_setup(` (thin renderer).
  </acceptance_criteria>
  <done>Goldens verdes, grafico.py intacto e _render_lwc comprovadamente read-only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke no navegador do Modo Trading (via Claude-in-Chrome)</name>
  <read_first>
    - app.py (bloco de swing + _render_lwc, para saber exatamente o que verificar)
    - .planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-CONTEXT.md (specifics: dado canônico BBSE3 diário — entrada 38,50–39,12 / stop 37,50 / alvo 41,74 / RR 2,2)
  </read_first>
  <what-built>
    Modo Trading (Lightweight Charts v5) na aba de swing: candlestick TV-like com scroll-zoom, pan,
    crosshair com rótulos nos eixos, Y-autoscale e linha de último preço; overlays da engine portados
    (zona de entrada e S/R como bandas, stop/alvo/Fibonacci como linhas rotuladas, pivôs/padrões como
    markers); persistência do range visível entre reruns; Plotly permanece a vista default.
  </what-built>
  <how-to-verify>
    1. Subir o app: `.venv/bin/streamlit run app.py --server.port 8501 --server.headless true` e abrir
       http://localhost:8501 no navegador (Claude-in-Chrome).
    2. Ir ao 4º menu (Swing trade). Confirmar que a vista DEFAULT é o Plotly (candlestick + subpainéis).
    3. Digitar ticker **BBSE3**, timeframe **Diário**. Ligar a vista **"Modo Trading"**.
    4. Confirmar UX TV-like: rolar o mouse dá zoom no tempo; arrastar dá pan; o cursor mostra crosshair
       com preço/data nos eixos; o eixo Y REENQUADRA sozinho ao dar zoom; há linha tracejada de último preço.
    5. Confirmar overlays (com os toggles de Overlays ligados): banda azul de zona de entrada
       (~38,50–39,12), linha "stop (estudo)" (~37,50), linha "alvo (estudo)" (~41,74), bandas verdes/
       vermelhas de S/R, linhas roxas de Fibonacci; ligar "Anotar padrões" e ver markers/neckline se houver.
    6. Confirmar persistência: dar zoom/pan, então togglar um overlay (força rerun) — o zoom é PRESERVADO.
    7. Confirmar copy de estudo: rótulos "(estudo)"/"projeção de estudo"; disclaimer "não recomenda" presente.
    8. Regressão: voltar para "Plotly" (idêntico ao anterior) e visitar as abas Analisar, Ranking e
       Garimpo — nenhuma quebra; checar console do iframe (`read_console_messages`) sem erros de overlay.
    9. Tirar screenshot do Modo Trading com overlays para o SUMMARY.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN unpkg → iframe | Verificação confirma que o bundle pinado + SRI carrega sem erro de integridade no navegador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Tampering | carregamento do `<script>` CDN no navegador | mitigate | O smoke de navegador confirma que o chart renderiza (SRI não bloqueou → hash bate) e o console não acusa erro de integridade |

Severidade máxima: baixa. Este plano é verificação; não introduz superfície nova.
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 283 passed.
- `grafico.py` fora do diff da fase.
- Checkpoint humano aprovado (Modo Trading + overlays + range persistente + sem regressão nas abas).
</verification>

<success_criteria>
- 283+ goldens verdes; `grafico.py` intacto; `app.py` thin renderer comprovado.
- Verificação humana no navegador aprova o Modo Trading sem regressão nas vistas/abas existentes.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/17-modo-trading-candlestick-tradingview-lightweight-charts/17-03-SUMMARY.md`
(incluir screenshot do Modo Trading + resultado dos goldens + confirmação de não-regressão).
</output>
