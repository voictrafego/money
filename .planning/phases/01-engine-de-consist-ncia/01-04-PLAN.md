---
phase: 01-engine-de-consist-ncia
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/analista/report/report.py
autonomous: true
requirements: [PAYOUT-01, VAL-01]
must_haves:
  truths:
    - "O DDM do Analisar usa a função canônica de payout-para-valuation (a mesma que o Ranking usará), em vez de uma média 3a+clamp própria."
    - "O intervalo de valor intrínseco (vmin/vmax) é calculado UMA vez em report.py e exposto no AnaliseAcao; a UI deixa de recomputar min/max."
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "analisar_acao consumindo CompanyData.payout_valuation; AnaliseAcao com campos vmin/vmax expostos do cálculo único do veredito"
      contains: "payout_valuation"
  key_links:
    - from: "src/analista/report/report.py::analisar_acao"
      to: "CompanyData.payout_valuation (Plano 01)"
      via: "função canônica única de payout"
      pattern: "payout_valuation"
    - from: "src/analista/report/report.py::AnaliseAcao"
      to: "app.py métrica de valor intrínseco"
      via: "vmin/vmax expostos, reusados pela UI"
      pattern: "vmin"
---

<objective>
Alinhar o Analisar à fonte canônica e remover a duplicação do intervalo intrínseco: (1) o DDM passa a usar `CompanyData.payout_valuation()` (Plano 01) em vez do `_media_payout_3a`+clamp local; (2) o cálculo de vmin/vmax do veredito é exposto em `AnaliseAcao` para a UI reusar (em vez de recomputar min/max).

Purpose: Fechar a parte engine de PAYOUT-01 (CR-02/WR-03) e VAL-01 (WR-07). Hoje o Analisar tem sua própria média de payout (que o Ranking não compartilha) e o intervalo intrínseco é calculado em report.py E de novo em app.py.
Output: `analisar_acao` usando `payout_valuation`; `AnaliseAcao` com `vmin`/`vmax` (Optional[float]) preenchidos no bloco do veredito.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CONSISTENCY-REVIEW.md
@.planning/phases/01-engine-de-consist-ncia/01-01-SUMMARY.md

<interfaces>
<!-- Plano 01 já entregou CompanyData.payout_valuation(janela=3) (média 3a + clamp 1.0, canônica). -->

De src/analista/report/report.py:
- _media_payout_3a(c): média payout 3a (linha 41-45) — a SER substituída pela canônica e REMOVIDA
- analisar_acao(c, cfg) -> AnaliseAcao
    payout_proj = _media_payout_3a(c); if payout_proj is not None: payout_proj=min(payout_proj,1.0)  (102-104)
    veredito (122-130): valores=[r.valor_intrinseco for r in (ddm_h, ddm_constante) if r]; vmin,vmax=min/max(valores)
- AnaliseAcao: dataclass (campos atuais incluem ddm_constante, ddm_h, veredito, alertas...) — SEM vmin/vmax hoje

De app.py (Analisar, linhas 107-108) — será wired no Plano 05:
    valores=[r.valor_intrinseco for r in (a.ddm_h, a.ddm_constante) if r]; intervalo=min/max(valores)  # duplicação (VAL-01)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DDM do Analisar usa a função canônica de payout (e remove o caminho antigo)</name>
  <files>src/analista/report/report.py</files>
  <read_first>
    - src/analista/report/report.py linhas 41-45 (_media_payout_3a) e 102-104 (uso no DDM com clamp)
    - .planning/phases/01-engine-de-consist-ncia/01-01-SUMMARY.md (assinatura de payout_valuation)
    - CONSISTENCY-REVIEW.md CR-02/WR-03 (payout do DDM = média 3a clamp; precisa ser a mesma função do Ranking)
  </read_first>
  <action>
    Substituir o uso de `_media_payout_3a(c)` + clamp manual por `c.payout_valuation()` (que já faz média 3a + clamp 1.0, definição canônica do Plano 01): a linha `payout_proj = _media_payout_3a(c)` deixa de existir; `payout_proj` passa a vir de `c.payout_valuation()` (o clamp 1.0 já está dentro da canônica, então remover também o `if payout_proj is not None: payout_proj=min(payout_proj,1.0)` redundante). REMOVER a definição de `_media_payout_3a` do módulo (grep antes para confirmar que não é usada em nenhum outro lugar). Manter o restante do fluxo do DDM intacto (g_alto, ke, n, tributação). O alerta de payout>100% (linhas 136-137) continua baseado no payout cru do último ano `c.payout(ult)` — não alterar essa semântica de exibição. Não inserir blocos de código no plano.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && .venv/bin/python -c "
import re
src=open('src/analista/report/report.py').read()
# (1) passou a usar a canônica
assert 'payout_valuation' in src, 'analisar_acao deve chamar c.payout_valuation()'
# (2) o caminho antigo foi de fato REMOVIDO (não basta presença textual da nova)
assert '_media_payout_3a' not in src, 'def/uso de _media_payout_3a ainda presente em report.py'
assert not re.search(r'payout_proj\s*=\s*_media_payout_3a', src), 'atribuicao antiga payout_proj=_media_payout_3a ainda existe'
print('ok: canonica em uso e caminho antigo removido')
" && .venv/bin/pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `analisar_acao` chama `c.payout_valuation()` para o payout projetado do DDM.
    - O identificador `_media_payout_3a` NÃO existe mais em report.py (def e usos removidos) — checado automaticamente, não só pela presença de 'payout_valuation'.
    - `pytest tests/ -q` passa.
  </acceptance_criteria>
  <done>O payout do DDM do Analisar vem da função canônica compartilhável com o Ranking e o cálculo antigo foi removido.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expor vmin/vmax do intervalo intrínseco no AnaliseAcao</name>
  <files>src/analista/report/report.py</files>
  <read_first>
    - src/analista/report/report.py dataclass AnaliseAcao (linhas 20-38) e bloco do veredito (122-130)
    - CONSISTENCY-REVIEW.md WR-07 (min/max recomputado em app.py e report.py independentemente)
  </read_first>
  <behavior>
    - AnaliseAcao tem campos vmin/vmax (Optional[float]).
    - Quando há DDM, vmin/vmax são o min/max de (ddm_h, ddm_constante) — exatamente os usados no texto do veredito.
    - Quando não há DDM, vmin/vmax ficam None.
  </behavior>
  <action>
    Adicionar a `AnaliseAcao` os campos `vmin: Optional[float] = None` e `vmax: Optional[float] = None`. No bloco do veredito, atribuir `a.vmin`/`a.vmax` a partir do mesmo `min(valores)`/`max(valores)` já calculado (cálculo único) e usar `a.vmin`/`a.vmax` na formatação do texto do veredito — para que o texto e os campos venham da MESMA fonte. A UI (Plano 05) passará a ler `a.vmin`/`a.vmax` em vez de recomputar. Não inserir blocos de código no plano.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && .venv/bin/python -c "from analista.report.report import AnaliseAcao; a=AnaliseAcao(ticker='X', nome='X', setor='', preco_atual=10.0); assert hasattr(a,'vmin') and hasattr(a,'vmax'); print('ok')" && .venv/bin/pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `AnaliseAcao` tem `vmin` e `vmax` (default None).
    - Com DDM presente, `vmin`/`vmax` são preenchidos e iguais ao intervalo do texto do veredito.
    - `pytest tests/ -q` passa.
  </acceptance_criteria>
  <done>O intervalo intrínseco é calculado uma única vez e exposto no AnaliseAcao.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine → relatório | Entrada é CompanyData já montado; sem I/O nem entrada direta de usuário neste módulo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | payout/valor intrínseco | accept | Cálculo determinístico sobre dados já validados na borda; ferramenta local sem persistência. |
</threat_model>

<verification>
- `pytest tests/ -q` verde.
- Smoke tests inline confirmam uso de payout_valuation, REMOÇÃO de _media_payout_3a e presença de vmin/vmax.
</verification>

<success_criteria>
- DDM do Analisar usa a função canônica de payout e o caminho antigo foi removido.
- vmin/vmax exposto no AnaliseAcao a partir de cálculo único.
- Testes golden passam.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-de-consist-ncia/01-04-SUMMARY.md`
</output>
</content>
