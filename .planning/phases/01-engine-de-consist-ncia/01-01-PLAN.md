---
phase: 01-engine-de-consist-ncia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/core/fundamentals.py
  - src/analista/ingest/prices.py
  - src/analista/ingest/build.py
autonomous: true
requirements: [PAYOUT-01, ROE-01, DY-01]
must_haves:
  truths:
    - "A mesma empresa produz o mesmo número de payout-para-valuation independentemente de quem o chama (Analisar ou Ranking)."
    - "O ROE de todos os anos da série usa a mesma base de PL (não mistura PL inicial num ano e PL final noutro silenciosamente)."
    - "O DY corrente reflete dividendos dos últimos 12 meses reais, não dividendos de um ano-base antigo sobre o preço de hoje."
  artifacts:
    - path: "src/analista/core/fundamentals.py"
      provides: "Função canônica de payout-para-valuation (CompanyData.payout_valuation) + ROE com base de PL consistente + dy_atual trailing-12m com ano-base sinalizado"
      contains: "def payout_valuation"
    - path: "src/analista/ingest/prices.py"
      provides: "dividendos por DATA real (não só agregado por ano-calendário) para o DY trailing-12m"
    - path: "src/analista/ingest/build.py"
      provides: "Propagação dos proventos por data para o CompanyData"
  key_links:
    - from: "src/analista/core/fundamentals.py::payout_valuation"
      to: "report.analisar_acao e app.py Ranking"
      via: "função única reusada nos dois modos"
      pattern: "payout_valuation"
    - from: "src/analista/core/fundamentals.py::dy_atual"
      to: "proventos trailing-12m do yfinance"
      via: "soma das datas reais dos últimos 12 meses"
      pattern: "dy_atual"
---

<objective>
Unificar na engine, na ORIGEM, três cálculos que hoje divergem entre modos: (1) a janela+clamp do payout usado para valuation, numa função canônica única; (2) a base de PL do ROE ao longo da série; (3) o DY corrente, que deve usar dividendos dos últimos 12 meses reais e sinalizar o ano-base do DPA.

Purpose: Eliminar na fonte as inconsistências CR-02/WR-03 (payout), WR-01 (ROE) e WR-04 (DY) — os modos consomem a mesma engine, então corrigir aqui propaga para Analisar, Garimpo e Ranking.
Output: `CompanyData.payout_valuation()` canônica, `CompanyData.roe()` com base consistente, `CompanyData.dy_atual()` trailing-12m com ano-base exposto; proventos por data propagados de prices → build → CompanyData.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CONSISTENCY-REVIEW.md

<interfaces>
<!-- Contratos atuais que o executor precisa conhecer. NÃO reescrever as fórmulas de multiples.py. -->

De src/analista/core/fundamentals.py (CompanyData):
- payout(ano) -> dividend_payout(dpa(ano), lpa(ano))  # já existe, payout cru de 1 ano
- roe(ano): pl_ini = patrimonio_liquido.get(ano-1, patrimonio_liquido.get(ano))  # fallback p/ PL final no 1º ano (WR-01)
- dy_atual(): mult.dividend_yield(dpa(ultimo_ano), preco_atual)  # usa DPA do ultimo_ano (WR-04)
- ultimo_ano(): maior ano com lucro coletado

De src/analista/report/report.py:
- _media_payout_3a(c): média de c.payout(a) nos 3 últimos anos; depois clamp min(.,1.0) (report.py:102-104)
  Esta é a definição que o Analisar já usa para o DDM.

De src/analista/core/multiples.py (NÃO ALTERAR — fórmulas corretas IN-01..05):
- roe(ll, pl_inicial), roe_medio(ll, pl_ini, pl_fim), dividend_payout(dpa, lpa), dividend_yield(dpa, preco)

De src/analista/ingest/prices.py (DadosMercado):
- dividendos_por_ano: Dict[int, float]  # DPA agregado por ano-calendário (de tk.dividends)
- a série tk.dividends tem as DATAS reais de cada provento (linha ~99: `for data, valor in divs.items()`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Função canônica de payout-para-valuation</name>
  <files>src/analista/core/fundamentals.py</files>
  <read_first>
    - src/analista/core/fundamentals.py (método payout existente)
    - src/analista/report/report.py linhas 41-45 e 102-104 (_media_payout_3a + clamp 1.0 — definição que o Analisar já usa)
    - CONSISTENCY-REVIEW.md CR-02 e WR-03 (a divergência: Analisar usa média 3a+clamp; Ranking usa 1 ano sem clamp)
  </read_first>
  <action>
    Criar em CompanyData o método canônico `payout_valuation(self, janela: int = 3) -> Optional[float]`: média dos `self.payout(a)` nos `janela` últimos anos de `anos_ordenados()` (ignorando None), depois clamp em 1.0 (min(media, 1.0)). Retorna None se não houver nenhum payout calculável. Esta é a definição ÚNICA de "payout que decide valuation/regressão" (decisão canônica: média 3a + clamp 1.0, igual ao que o Analisar já fazia em _media_payout_3a). NÃO remover o método payout(ano) cru — ele continua servindo a exibição por ano. Documentar no docstring que é a janela+clamp única usada por DDM (Analisar) e regressão (Ranking). Não inserir blocos de código no plano — implementar direto no arquivo.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && .venv/bin/python -c "from analista.core.fundamentals import CompanyData; c=CompanyData(ticker='X', anos=[2022,2023,2024]); c.lucro_liquido={2022:100,2023:100,2024:100}; c.dividendos={2022:150,2023:50,2024:100}; c.num_acoes={2022:1,2023:1,2024:1}; v=c.payout_valuation(); assert v is not None and v<=1.0, v; print('ok', v)"</automated>
  </verify>
  <acceptance_criteria>
    - `CompanyData.payout_valuation()` existe e retorna a média 3a com clamp 1.0.
    - Para uma empresa cujo payout médio 3a > 1.0, o retorno é exatamente 1.0.
    - `pytest tests/ -q` continua verde.
  </acceptance_criteria>
  <done>Função canônica de payout existe e é a única definição de payout-para-valuation; testes golden passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ROE com base de PL consistente na série</name>
  <files>src/analista/core/fundamentals.py</files>
  <read_first>
    - src/analista/core/fundamentals.py método roe(ano) (linha ~69-72)
    - src/analista/core/multiples.py funções roe e roe_medio (não alterar)
    - src/analista/glossario.py chave "roe" (linhas 34-38: promete "PL inicial ou médio (não o final)")
    - CONSISTENCY-REVIEW.md WR-01 (o 1º ano cai p/ PL final silenciosamente, misturando bases)
  </read_first>
  <action>
    Decisão canônica (LOCKED): alinhar a base do ROE para que TODOS os anos da série usem a mesma definição. Implementar: `roe(ano)` usa `roe_medio` (PL médio = (PL_ano-1 + PL_ano)/2) quando AMBOS PL inicial e final existem; quando o PL inicial (ano-1) não foi coletado (1º ano da janela), retornar None para esse ano em vez de cair silenciosamente para PL final — assim a série nunca mistura bases. Atualizar o docstring de `roe` para descrever a base efetiva ("PL médio; None quando falta o PL inicial"). Ajustar a chave "roe" do glossário (glossario.py) para "PL médio (inicial+final)/2; indisponível no 1º ano sem histórico" — alinhar o tooltip à base real. NÃO mexer em multiples.roe/roe_medio (já corretos). Não inserir blocos de código no plano.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && .venv/bin/python -c "from analista.core.fundamentals import CompanyData; c=CompanyData(ticker='X', anos=[2023,2024]); c.lucro_liquido={2023:100,2024:120}; c.patrimonio_liquido={2023:1000,2024:1200}; assert c.roe(2023) is None, 'sem PL inicial -> None'; r=c.roe(2024); assert r is not None and abs(r-120/1100)<1e-9, r; print('ok', r)"</automated>
  </verify>
  <acceptance_criteria>
    - No 1º ano da janela (sem PL ano-1) `roe` retorna None, não o PL final.
    - Nos demais anos `roe` usa PL médio ((PL_ini+PL_fim)/2) de forma consistente.
    - O glossário descreve a base real usada.
    - `pytest tests/ -q` passa (ajustar apenas asserções de teste que dependiam do antigo fallback, se houver — sem afetar fórmulas).
  </acceptance_criteria>
  <done>ROE usa uma base de PL única em toda a série e o glossário reflete a base real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DY corrente trailing-12m com ano-base sinalizado</name>
  <files>src/analista/ingest/prices.py, src/analista/ingest/build.py, src/analista/core/fundamentals.py</files>
  <read_first>
    - src/analista/ingest/prices.py linhas 94-103 (coleta de dividendos: `for data, valor in divs.items()` tem as datas reais)
    - src/analista/ingest/build.py linhas 60-66 (dividendos agregados por ano → CompanyData.dividendos)
    - src/analista/core/fundamentals.py método dy_atual (linha ~74-78)
    - CONSISTENCY-REVIEW.md WR-04 (dpa(ultimo_ano)/preco_atual pode usar dividendo de ano antigo sobre preço de hoje)
  </read_first>
  <action>
    Em prices.py: além de `dividendos_por_ano`, expor a soma dos proventos por ação dos últimos 12 meses reais (campo novo em DadosMercado, ex.: `dpa_trailing_12m: Optional[float]`), calculado a partir das datas de `tk.dividends` (filtrar `data >= hoje - 365 dias`, somar valores). Sinalizar também o ano-base do DPA usado (ex.: ano da última data de provento, campo `ano_dpa: Optional[int]`). Em build.py: propagar esses campos para o CompanyData (adicionar campos `dpa_trailing_12m` e `ano_dpa` ao CompanyData ou passá-los no construtor). Em fundamentals.py: `dy_atual()` passa a usar `dpa_trailing_12m / preco_atual` quando disponível; fallback para o comportamento atual (dpa(ultimo_ano)/preco_atual) quando não houver série de datas. Expor o ano-base do DPA via atributo já preenchido (`ano_dpa`) para a Fase 2 consumir. Validar dados só na borda (datas ausentes → fallback). Não inserir blocos de código no plano.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && .venv/bin/python -c "from analista.core.fundamentals import CompanyData; c=CompanyData(ticker='X', anos=[2024]); c.preco_atual=10.0; c.dpa_trailing_12m=0.8; assert abs(c.dy_atual()-0.08)<1e-9, c.dy_atual(); c2=CompanyData(ticker='Y', anos=[2024]); c2.preco_atual=10.0; c2.dividendos={2024:100}; c2.num_acoes={2024:100}; assert c2.dy_atual() is not None; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `CompanyData.dy_atual()` usa o DPA trailing-12m quando disponível.
    - O atributo de ano-base do DPA (`ano_dpa`) existe no CompanyData (pronto para a Fase 2 exibir).
    - Sem datas de proventos, `dy_atual` ainda funciona via fallback do ano-base.
    - `pytest tests/ -q` passa.
  </acceptance_criteria>
  <done>DY corrente reflete os últimos 12 meses reais e o ano-base do DPA fica exposto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yfinance/CVM/BCB → engine | Dados externos read-only; podem vir ausentes/parciais (tratado como None/fallback, não como falha de segurança) |
| usuário → app | Única entrada é string de ticker, validada/normalizada na borda (upper/strip, mapeamento) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | dados externos (dividendos/PL) | accept | Ferramenta local, dados read-only de fontes públicas; sem persistência nem privilégios. Lacunas tratadas como None/fallback, não como ameaça. |
| T-01-02 | Denial of Service | yfinance timeout/vazio | accept | Coleta já encapsulada em try/except; ausência vira fallback. Sem superfície de rede exposta. |
</threat_model>

<verification>
- `pytest tests/ -q` verde após cada tarefa (testes golden de multiples/comparables/ddm/screening intactos).
- `payout_valuation`, `roe` e `dy_atual` retornam os valores esperados nos smoke tests inline acima.
</verification>

<success_criteria>
- Existe uma função canônica de payout-para-valuation reusável por Analisar e Ranking.
- ROE usa base de PL única em toda a série; glossário alinhado.
- DY corrente usa trailing-12m com ano-base exposto.
- Testes golden continuam passando.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-de-consist-ncia/01-01-SUMMARY.md`
</output>
