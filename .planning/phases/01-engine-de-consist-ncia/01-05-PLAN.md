---
phase: 01-engine-de-consist-ncia
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - app.py
autonomous: false
requirements: [GARIMPO-01, PAYOUT-01, RANK-02, VAL-01]
must_haves:
  truths:
    - "No Garimpo, uma ação com DY abaixo da Selic não aparece como recomendada no topo: a tabela ordena/filtra por 'Passa filtros' (que contém o corte Selic) antes do BSD."
    - "No Ranking, o payout que alimenta a regressão é o canônico (mesmo do Analisar), com clamp em [0,1] — não o payout cru de 1 ano."
    - "No Analisar, o intervalo de valor intrínseco exibido na métrica vem de a.vmin/a.vmax (report.py), sem recomputar min/max na UI."
  artifacts:
    - path: "app.py"
      provides: "Garimpo ordenado por filtros; Ranking usando payout_valuation+clamp; Analisar reusando vmin/vmax"
      contains: "payout_valuation"
  key_links:
    - from: "app.py modo Garimpar"
      to: "filtros_customizados (corte Selic)"
      via: "ordenação/realce da tabela por 'Passa filtros' antes do BSD"
      pattern: "Passa filtros"
    - from: "app.py modo Ranking"
      to: "CompanyData.payout_valuation + preco_alvo_por_regressao com clamp"
      via: "vetor DP montado pela função canônica"
      pattern: "payout_valuation"
    - from: "app.py modo Analisar"
      to: "AnaliseAcao.vmin/vmax"
      via: "métrica de valor intrínseco lê os campos expostos"
      pattern: "a.vmin"
---

<objective>
Conectar (wire) os três modos do app à engine já corrigida pelos Planos 01–04, na borda: (1) Garimpo deixa de recomendar quem não passa no corte Selic, ordenando/realçando por "Passa filtros" antes do BSD; (2) Ranking monta o vetor de payout via `payout_valuation` e usa o `preco_alvo_por_regressao` com clamp/flag; (3) Analisar lê `a.vmin`/`a.vmax` em vez de recomputar o intervalo.

Purpose: Fechar GARIMPO-01 (CR-01, comportamento na borda), a parte de borda de PAYOUT-01 e RANK-02 (Ranking consumindo a engine canônica) e VAL-01 (UI reusa o cálculo único). É a camada que faz a UI cumprir o que a engine agora promete.
Output: `app.py` com os três modos consistentes com a engine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CONSISTENCY-REVIEW.md
@.planning/phases/01-engine-de-consist-ncia/01-01-SUMMARY.md
@.planning/phases/01-engine-de-consist-ncia/01-02-SUMMARY.md
@.planning/phases/01-engine-de-consist-ncia/01-03-SUMMARY.md
@.planning/phases/01-engine-de-consist-ncia/01-04-SUMMARY.md

<interfaces>
<!-- A engine já entregue pelos planos anteriores: -->
<!-- Plano 01: CompanyData.payout_valuation(janela=3); dy_atual trailing-12m; roe base consistente. -->
<!-- Plano 02: preco_alvo_por_regressao com clamp + PrecoAlvo.payout_fora_faixa. -->
<!-- Plano 03: bsd_ranking absoluto + fatores_faltantes/n_fatores_faltantes por linha. -->
<!-- Plano 04: AnaliseAcao.vmin/vmax expostos. -->

Pontos atuais em app.py a alterar:
- Garimpo (linhas 203-223): monta `rows` com BSD/Passa filtros e `df.sort_values("BSD")`. CR-01: ordenar por "Passa filtros" antes do BSD; e deixar claro que BSD>80 sem "Passa filtros" não é recomendação.
- Ranking (linhas 249-266): `DP.append(c.payout(u))` (cru, 1 ano) e `preco_alvo_por_regressao(reg, c.payout(u), ...)`. Trocar para `c.payout_valuation()`.
- Analisar (linhas 107-108): recomputa `min/max([ddm_h, ddm_constante])`. Trocar para `a.vmin`/`a.vmax`.
- fmt_pct/fmt_rs já existem para formatação na borda.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Garimpo ordena/realça por "Passa filtros" (corte Selic) antes do BSD</name>
  <files>app.py</files>
  <read_first>
    - app.py modo Garimpar (linhas 185-224), em especial a montagem de `rows` e `df.sort_values("BSD")`
    - src/analista/core/screening.py filtros_customizados (corte dy_acima_corte = DY>Selic)
    - CONSISTENCY-REVIEW.md CR-01 (ranking exibido ignora o corte Selic; ordenado só por BSD)
  </read_first>
  <action>
    No modo Garimpar: ordenar a tabela primeiro por "Passa filtros" (True antes de False) e só então por BSD desc — usar `df.sort_values(["Passa filtros", "BSD"], ascending=[False, False])` ou equivalente (ajustando a coluna "Passa filtros" para um booleano ordenável e formatando o ✅ na exibição). Adicionar uma indicação visual/caption deixando explícito que "BSD>80 sem 'Passa filtros' (DY>Selic) NÃO é recomendação" — alinhando ao rótulo "Selic (corte do DY)" da sidebar. Aproveitar os campos novos do Plano 03 (`n_fatores_faltantes`) para, se natural, exibir quantos fatores faltaram por empresa (coluna opcional "Fatores faltando"). Não inventar features além disso. Não inserir blocos de código no plano.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && .venv/bin/python -c "import ast; ast.parse(open('app.py').read()); src=open('app.py').read(); assert 'Passa filtros' in src and 'sort_values' in src; print('ok app.py parseia e ordena por filtros')"</automated>
  </verify>
  <acceptance_criteria>
    - A tabela do Garimpo ordena por "Passa filtros" antes de BSD (quem reprova no corte Selic não fica no topo).
    - Há indicação explícita de que BSD>80 sem "Passa filtros" não é recomendação.
    - `app.py` continua parseável (sem erro de sintaxe).
  </acceptance_criteria>
  <done>O Garimpo respeita de fato o corte por Selic na ordenação/realce.</done>
</task>

<task type="auto">
  <name>Task 2: Ranking usa payout canônico (com clamp/flag) e Analisar reusa vmin/vmax</name>
  <files>app.py</files>
  <read_first>
    - app.py modo Ranking (linhas 249-283) e modo Analisar (linhas 107-111)
    - .planning/phases/01-engine-de-consist-ncia/01-01-SUMMARY.md (payout_valuation), 01-02-SUMMARY.md (payout_fora_faixa), 01-04-SUMMARY.md (vmin/vmax)
    - CONSISTENCY-REVIEW.md CR-02/CR-03/WR-07
  </read_first>
  <action>
    No modo Ranking: montar o vetor `DP` com `c.payout_valuation()` (canônico) em vez de `c.payout(u)`; ao chamar `preco_alvo_por_regressao`, passar `c.payout_valuation()` como dp. Quando `pa.payout_fora_faixa` for True, sinalizar na tabela (ex.: sufixo/aviso na coluna do veredito ou nota "payout>100% ajustado"), espelhando o alerta do Analisar. NÃO alterar o ajuste da regressão em si (os pares do setor seguem como estão na engine). No modo Analisar: substituir o recálculo `valores=[...]; min/max(valores)` (linhas 107-108) pela leitura de `a.vmin`/`a.vmax`; manter o fallback de exibição "—" quando `a.vmin`/`a.vmax` forem None. Não inserir blocos de código no plano.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && .venv/bin/python -c "import ast; ast.parse(open('app.py').read()); src=open('app.py').read(); assert 'payout_valuation' in src, 'Ranking usa canonica'; assert 'a.vmin' in src and 'a.vmax' in src, 'Analisar reusa vmin/vmax'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - O vetor DP e o dp do preço-alvo no Ranking vêm de `c.payout_valuation()`.
    - `payout_fora_faixa` é sinalizado na tabela do Ranking.
    - A métrica de valor intrínseco no Analisar lê `a.vmin`/`a.vmax` (não recomputa min/max).
    - `app.py` parseia sem erro.
  </acceptance_criteria>
  <done>Ranking e Analisar consomem a engine canônica (mesmo payout; intervalo único).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana dos três modos no navegador</name>
  <action>Rodar o app localmente e validar manualmente os três modos contra os critérios de "how-to-verify"; não há código a escrever nesta tarefa — apenas verificação visual/funcional após a automação das Tasks 1-2.</action>
  <what-built>
    Os três modos do app agora consomem a engine corrigida: Garimpo ordena por "Passa filtros" (corte Selic) antes do BSD e mostra BSD absoluto/reprodutível; Ranking usa o payout canônico com clamp/flag; Analisar exibe o intervalo intrínseco a partir de a.vmin/a.vmax. (`pytest tests/ -q` já validado verde na engine.)
  </what-built>
  <how-to-verify>
    1. Rode: `./.venv/bin/streamlit run app.py`
    2. Modo "⛏️ Garimpar carteira (BSD)": cole `TAEE11, EGIE3, CMIG4, ALUP11, CPFE3, EQTL3, ITUB4, BBAS3` e clique Garimpar.
       - Confirme que empresas SEM "Passa filtros" (DY < Selic) NÃO aparecem no topo da lista.
       - Confirme que existe o aviso de que BSD>80 sem "Passa filtros" não é recomendação.
       - Repita com um subconjunto (ex.: só `TAEE11, EGIE3`) e confirme que o BSD de TAEE11 é o MESMO valor de antes (reprodutível entre lotes).
    3. Modo "📊 Ranking por múltiplos": rode com os tickers de energia. Confirme que aparece sinalização quando algum payout foi ajustado (>100%).
    4. Modo "🔎 Analisar uma ação": analise `TAEE11`. Confirme que o "Valor intrínseco (DDM)" exibido bate com o intervalo do veredito (mesmo número).
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva divergências encontradas.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → app | Única entrada são strings de ticker, normalizadas na borda (upper/strip/split) antes de chegar à engine |
| engine → UI | Valores já calculados; UI apenas formata (×100 na borda) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering/Injection | textarea de tickers | mitigate | Tickers já são normalizados (upper/strip) e usados só como chave de busca/símbolo Yahoo; sem eval/SQL/shell. |
| T-01-08 | Denial of Service | lote grande de tickers | accept | Ferramenta local single-user; coleta sequencial com progress; sem multiusuário. |
</threat_model>

<verification>
- `app.py` parseia (`python -c "import ast; ast.parse(...)"`) após cada tarefa.
- `pytest tests/ -q` permanece verde (engine intacta).
- Checkpoint humano confirma os três modos coerentes no navegador.
</verification>

<success_criteria>
- Garimpo não recomenda quem reprova no corte Selic.
- Ranking usa payout canônico com clamp/flag.
- Analisar exibe o intervalo intrínseco do cálculo único (a.vmin/a.vmax).
- Suíte de testes verde; verificação humana aprovada.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-de-consist-ncia/01-05-SUMMARY.md`
</output>
