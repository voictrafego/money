---
phase: 02-cobran-a-asaas-webhooks-conta
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - /Users/giovanelazari/projects/lazari-capital/apps/gate/views.py
  - /Users/giovanelazari/projects/lazari-capital/apps/core/middleware/billing_gate.py
  - /Users/giovanelazari/projects/lazari-capital/apps/gate/tests/test_gate.py
  - /Users/giovanelazari/projects/lazari-capital/apps/billing/tests/test_billing_gate.py
  - /Users/giovanelazari/projects/lazari-capital/apps/billing/tests/test_webhook_ciclo.py
autonomous: true
requirements: [BILL-03, BILL-04]

must_haves:
  truths:
    - "O gate libera enquanto trial vigente OU assinatura paga ativa (status=ATIVO, trial_ate=None)"
    - "O gate libera durante o grace de inadimplência (now <= grace_ate) e bloqueia depois (D-04)"
    - "O gate libera após cancelamento até o paid-through (now <= grace_ate) e bloqueia depois (D-05)"
    - "Um pagamento confirmado (webhook) ativa a assinatura; webhook repetido não duplica efeito (idempotente)"
    - "As rotas de conta/checkout permanecem alcançáveis para contas CANCELADO/SUSPENSO (isenção do middleware)"
  artifacts:
    - path: "/Users/giovanelazari/projects/lazari-capital/apps/gate/views.py"
      provides: "GateView com a regra de 3 ramos (paid/trial, grace, cancel paid-through)"
      contains: "grace_ate"
    - path: "/Users/giovanelazari/projects/lazari-capital/apps/core/middleware/billing_gate.py"
      provides: "EXEMPT_URL_NAMES com as rotas de conta/checkout"
      contains: "billing-assinar"
    - path: "/Users/giovanelazari/projects/lazari-capital/apps/billing/tests/test_webhook_ciclo.py"
      provides: "Regressão do ciclo assinar→webhook→ativação idempotente + gate"
      contains: "PAYMENT_CONFIRMED"
  key_links:
    - from: "apps/gate/views.py::GateView"
      to: "Conta.status/trial_ate/grace_ate"
      via: "decisão fail-closed lendo só Conta"
      pattern: "grace_ate|trial_ate"
    - from: "apps/core/middleware/billing_gate.py::EXEMPT_URL_NAMES"
      to: "billing-assinar / conta / cancelar-assinatura"
      via: "isenção por url_name"
      pattern: "billing-assinar"
    - from: "AsaasWebhookView (existente)"
      to: "_ativar_conta/_marcar_inadimplente"
      via: "PAYMENT_CONFIRMED/RECEIVED/OVERDUE correlacionado por asaas_sub_id"
      pattern: "PAYMENT_CONFIRMED"
---

<objective>
Fazer o acesso honrar o status de assinatura (BILL-04) e fechar o ciclo de webhooks (BILL-03).
Estender `GateView` para a regra precisa de 3 ramos (lendo SÓ `Conta`, RESEARCH § Gate Status
Machine): libera se **ATIVO & (trial_ate is None OR trial_ate>=hoje)** (pago OU trial), **OU
INADIMPLENTE & now<=grace_ate** (graça, D-04), **OU CANCELADO & now<=grace_ate** (paid-through,
D-05); senão 302 `/trial-acabou/`. Isentar as rotas de conta/checkout do `BillingGateMiddleware`
(Pitfall 2) para que contas CANCELADO/SUSPENSO possam ver a conta e re-assinar. E travar por
regressão que o ciclo `assinar → webhook PAYMENT_CONFIRMED → _ativar_conta` ativa a assinatura de
forma **idempotente** (webhook repetido não duplica efeito), correlacionado por `asaas_sub_id`.

Purpose: garantir que quem paga (ou está em trial/graça/paid-through) entra e o resto é bloqueado —
o coração regulatório do produto.
Output: GateView de 3 ramos, isenção de middleware, e teste de ciclo webhook idempotente ponta-a-ponta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cobran-a-asaas-webhooks-conta/02-CONTEXT.md
@.planning/phases/02-cobran-a-asaas-webhooks-conta/02-RESEARCH.md

<interfaces>
<!-- Contratos existentes que o executor DEVE reusar. Lidos do código. -->

GateView (apps/gate/views.py) — HOJE libera só: status==ATIVO AND trial_ate is not None AND trial_ate>=hoje.
  ⚠ BUG que este plano corrige: um usuário PAGO tem trial_ate=None (setado por _ativar_conta) e seria
     BLOQUEADO pela regra atual. É read-only, fail-closed, lê SÓ Conta.

Conta (apps/accounts/models.py): Status.{ATIVO,INADIMPLENTE,SUSPENSO,CANCELADO,PENDENTE_PAGAMENTO},
  trial_ate (DateField → comparar com timezone.localdate()),
  grace_ate (DateTimeField → comparar com timezone.now()).  (Pitfall 3 — não misturar os tipos.)
  Transições já implementadas em services._ativar_conta (ATIVO, trial_ate=None, grace_ate=None) e
  _marcar_inadimplente (INADIMPLENTE, grace_ate=now+grace_period_dias). Plan 02 seta CANCELADO+grace_ate=paid-through.

Regra alvo (RESEARCH § Gate Status Machine — allow se QUALQUER):
  (status==ATIVO and (trial_ate is None or trial_ate>=today))
  or (status==INADIMPLENTE and grace_ate is not None and now<=grace_ate)
  or (status==CANCELADO   and grace_ate is not None and now<=grace_ate)

BillingGateMiddleware (apps/core/middleware/billing_gate.py):
  EXEMPT_URL_NAMES (frozenset) — isenta por url_name resolvido (SEM namespace). Já contém
  "billing-signup","billing-webhook","pagamento-pendente","conta-suspensa","login","logout", etc.
  BLOCKING_STATUSES = {PENDENTE_PAGAMENTO, SUSPENSO, CANCELADO}. resolve(path).url_name p/ rotas namespaced
  devolve só o nome ("conta","cancelar-assinatura"), sem "accounts:".

AsaasWebhookView (apps/billing/views.py) — JÁ implementa: auth constant-time asaas-access-token → 401;
  parse → 400; idempotência insert-and-catch em AsaasWebhookLog.event_id → 200 "duplicado";
  correlação Assinatura.objects.unscoped() por asaas_sub_id; dispatch PAYMENT_CONFIRMED/RECEIVED→_ativar_conta,
  PAYMENT_OVERDUE→_marcar_inadimplente; sempre 200 (Asaas pausa a fila após não-2xx). NÃO reescrever — travar por teste.
  Fixtures/token de teste: apps/billing/tests/test_asaas_webhook_view.py (TOKEN_VALIDO, _read_fixture, fixtures/).

Rotas criadas nesta fase: name="billing-assinar" (Plan 01), name="conta" e name="cancelar-assinatura" (Plan 02, namespace accounts).
Fábricas: apps.conftest.ContaFactory/UserFactory/PlanoFactory. pytest usa config.settings.test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GateView — regra de 3 ramos (BILL-04)</name>
  <files>/Users/giovanelazari/projects/lazari-capital/apps/gate/views.py, /Users/giovanelazari/projects/lazari-capital/apps/gate/tests/test_gate.py</files>
  <read_first>
    - /Users/giovanelazari/projects/lazari-capital/apps/gate/views.py (GateView atual — regra a estender; comentário Fase 2)
    - /Users/giovanelazari/projects/lazari-capital/apps/accounts/models.py (Conta.Status, trial_ate DateField, grace_ate DateTimeField)
    - /Users/giovanelazari/projects/lazari-capital/apps/gate/tests/test_gate.py (padrão RequestFactory; casos allow/anon/method/conta-None já cobertos)
    - 02-RESEARCH.md § "Gate Status Machine (BILL-04)" (booleano preciso) e Pitfall 3
  </read_first>
  <behavior>
    - ATIVO + trial_ate futuro → 200 + X-User-Email (trial vigente).
    - ATIVO + trial_ate=None → 200 (pago; hoje isso é BLOQUEADO — o teste deve provar a correção).
    - ATIVO + trial_ate passado → 302 /trial-acabou/ (trial expirado sem pagamento).
    - INADIMPLENTE + now<=grace_ate → 200; INADIMPLENTE + grace_ate passado → 302 (D-04).
    - CANCELADO + now<=grace_ate → 200 (paid-through, D-05); CANCELADO + grace_ate passado → 302.
    - SUSPENSO / PENDENTE_PAGAMENTO → 302.
    - grace_ate None em INADIMPLENTE/CANCELADO → 302 (fail-closed).
    - anônimo / conta None → 302 login (preservar comportamento atual).
    - GET-only, read-only (nunca muta Conta).
  </behavior>
  <action>Substituir a expressão `ativo_ou_trial` da `GateView.get` pela regra de 3 ramos do RESEARCH: `now = timezone.now()`, `today = timezone.localdate()`, `S = Conta.Status`; `allow = (conta.status==S.ATIVO and (conta.trial_ate is None or conta.trial_ate>=today)) or (conta.status==S.INADIMPLENTE and conta.grace_ate is not None and now<=conta.grace_ate) or (conta.status==S.CANCELADO and conta.grace_ate is not None and now<=conta.grace_ate)`. Manter o resto igual: anônimo/conta None → `redirect("login")`; `not allow` → `redirect("trial-acabou")`; allow → `HttpResponse(200)` com header `X-User-Email = user.email`. Comparar `trial_ate` (DateField) com `today` e `grace_ate` (DateTimeField) com `now` — NÃO misturar (Pitfall 3). Atualizar o docstring da Fase 2. Estender test_gate.py com os casos de pago(trial_ate=None), grace vigente/expirado e cancelado vigente/expirado.</action>
  <acceptance_criteria>
    - `apps/gate/views.py` referencia `grace_ate` e implementa os 3 ramos (grep confirma `INADIMPLENTE` e `CANCELADO` na decisão do gate).
    - Teste: `ContaFactory(status=ATIVO, trial_ate=None)` → GateView responde 200 com `X-User-Email` (correção do bug do pago).
    - Teste: `INADIMPLENTE` com `grace_ate=now+1d` → 200; com `grace_ate=now-1d` → 302 `trial-acabou`.
    - Teste: `CANCELADO` com `grace_ate=now+1d` → 200; com `grace_ate=now-1d` → 302.
    - Teste: `ATIVO, trial_ate=ontem` → 302; `SUSPENSO` → 302; `INADIMPLENTE, grace_ate=None` → 302.
    - Casos existentes (anon/method/conta-None) continuam verdes.
    - `pytest apps/gate/tests/test_gate.py` sai 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/giovanelazari/projects/lazari-capital && pytest apps/gate/tests/test_gate.py -x</automated>
  </verify>
  <done>Gate libera pago/trial/graça/paid-through e bloqueia o resto, lendo só Conta, em tempo real.</done>
</task>

<task type="auto">
  <name>Task 2: Isentar rotas de conta/checkout do BillingGateMiddleware (Pitfall 2)</name>
  <files>/Users/giovanelazari/projects/lazari-capital/apps/core/middleware/billing_gate.py, /Users/giovanelazari/projects/lazari-capital/apps/billing/tests/test_billing_gate.py</files>
  <read_first>
    - /Users/giovanelazari/projects/lazari-capital/apps/core/middleware/billing_gate.py (EXEMPT_URL_NAMES, _is_exempt, BLOCKING_STATUSES)
    - /Users/giovanelazari/projects/lazari-capital/apps/billing/urls.py e apps/accounts/urls.py (nomes de rota; namespace accounts → url_name sem prefixo)
    - /Users/giovanelazari/projects/lazari-capital/apps/billing/tests/test_billing_gate.py (padrão RequestFactory + resolve; testes de isenção existentes)
    - 02-RESEARCH.md Pitfall 2 (dois gates devem concordar; contas CANCELADO/SUSPENSO precisam re-assinar)
  </read_first>
  <action>Adicionar ao `EXEMPT_URL_NAMES` os url_names das novas rotas: `"billing-assinar"` (Plan 01), `"conta"` e `"cancelar-assinatura"` (Plan 02, resolvem sem o prefixo de namespace). Racional: uma conta CANCELADO/SUSPENSO precisa alcançar a página de conta e o checkout para re-assinar (Pitfall 2, Open Q4). NÃO remover as reservas existentes. Manter a isenção por url_name (nunca por prefixo de path). Estender test_billing_gate.py provando que uma conta CANCELADO NÃO é redirecionada em `/conta/`, `/conta/cancelar/` e `/billing/assinar/`, e que rotas não-isentas (ex.: `/dashboard/`) continuam bloqueadas para CANCELADO/SUSPENSO.</action>
  <acceptance_criteria>
    - `billing_gate.py` `EXEMPT_URL_NAMES` contém `"billing-assinar"`, `"conta"` e `"cancelar-assinatura"`.
    - Teste: conta `CANCELADO` acessando `/conta/` → passa (não redireciona); acessando `/billing/assinar/` → passa.
    - Teste: conta `SUSPENSO` acessando `/dashboard/` (rota não-isenta) → ainda redireciona para `conta-suspensa` (regressão de bloqueio preservada).
    - Teste: conta `ATIVO` inalterada (passa em tudo).
    - `pytest apps/billing/tests/test_billing_gate.py` sai 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/giovanelazari/projects/lazari-capital && pytest apps/billing/tests/test_billing_gate.py -x</automated>
  </verify>
  <done>Os dois gates concordam: contas bloqueadas ainda alcançam conta/checkout para re-assinar; demais rotas seguem bloqueadas.</done>
</task>

<task type="auto">
  <name>Task 3: Travar o ciclo de webhook idempotente assinar→pagamento→ativação (BILL-03)</name>
  <files>/Users/giovanelazari/projects/lazari-capital/apps/billing/tests/test_webhook_ciclo.py</files>
  <read_first>
    - /Users/giovanelazari/projects/lazari-capital/apps/billing/views.py (AsaasWebhookView — ordem AUTH→PARSE→IDEMPOTÊNCIA→CORRELAÇÃO→DISPATCH)
    - /Users/giovanelazari/projects/lazari-capital/apps/billing/services.py (_ativar_conta: ATIVO, trial_ate=None, convergência por dueDate; _marcar_inadimplente)
    - /Users/giovanelazari/projects/lazari-capital/apps/billing/tests/test_asaas_webhook_view.py (TOKEN_VALIDO, _read_fixture, uso de fixtures/, set_current_conta_id nas fixtures)
    - /Users/giovanelazari/projects/lazari-capital/apps/billing/tests/fixtures (payloads existentes de PAYMENT_*; reusar/derivar)
    - 02-RESEARCH.md § "Webhook Events", "Idempotency & Delivery", A3 (3 eventos bastam; cancel é app-initiated)
  </read_first>
  <action>Escrever um teste de regressão de ciclo em `test_webhook_ciclo.py` (SEM reescrever a view/serviço — só travar o comportamento) que: (1) prepara uma `Assinatura` com `asaas_sub_id` conhecido (via `services.assinar` mockado OU criação direta com set_current_conta_id, no padrão de test_asaas_webhook_view), conta em trial (ATIVO, trial_ate futuro); (2) POST `/billing/webhook/` com header `asaas-access-token=TOKEN_VALIDO` e payload `PAYMENT_CONFIRMED` correlacionado por `payment.subscription = asaas_sub_id` → 200 e a conta vira ATIVO com `trial_ate=None` (pago); (3) reenvia o MESMO evento (mesmo `id`) → 200 "duplicado" e NENHUMA segunda transição/efeito (idempotência por event_id); (4) reenvia `PAYMENT_RECEIVED` do mesmo pagamento (event_id distinto) → converge ao mesmo estado sem avançar em dobro o `proximo_vencimento` (convergência por `dueDate` absoluto); (5) `PAYMENT_OVERDUE` → INADIMPLENTE com `grace_ate` armado. Confirmar que 3 eventos PAYMENT_* bastam (A3) — sem SUBSCRIPTION_* nesta fase. NÃO alterar a view nem os serviços; se algum comportamento não bater, reportar como blocker (não regredir a infra hardened).</action>
  <acceptance_criteria>
    - `test_webhook_ciclo.py` contém asserts para `PAYMENT_CONFIRMED` (ativa: status ATIVO, trial_ate None), reenvio duplicado (200 "duplicado", sem 2ª transição) e `PAYMENT_OVERDUE` (INADIMPLENTE + grace_ate não-nulo).
    - Teste prova idempotência: após 2 POSTs do mesmo `event_id`, existe exatamente 1 `AsaasWebhookLog` para aquele id e o estado da conta é idêntico ao de 1 POST.
    - Teste prova convergência CONFIRMED+RECEIVED: `proximo_vencimento` não avança duas vezes para o mesmo pagamento.
    - `pytest apps/billing/tests/test_webhook_ciclo.py` sai 0.
    - `pytest apps/billing/tests/test_asaas_webhook_view.py` continua verde (sem regressão da infra existente).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/giovanelazari/projects/lazari-capital && pytest apps/billing/tests/test_webhook_ciclo.py apps/billing/tests/test_asaas_webhook_view.py -x</automated>
  </verify>
  <done>Ciclo assinar→pagamento→ativação travado por regressão; webhook repetido comprovadamente não duplica efeito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Traefik → GateView | forward-auth decide acesso ao Streamlit lendo só Conta (fail-closed) |
| Asaas → AsaasWebhookView | M2M público; credencial = header asaas-access-token; sem cookie/sessão |
| usuário bloqueado → rotas Django | BillingGateMiddleware guarda rotas; deve concordar com o GateView |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Elevation | GateView | mitigate (HIGH) | Regra de 3 ramos fail-closed: cancelado/overdue além do grace e trial expirado → 302; nunca libera estado bloqueado |
| T-02-14 | Elevation | GateView vs BillingGateMiddleware | mitigate | Ambos leem `Conta`; EXEMPT_URL_NAMES alinhado para conta/checkout — sem desacordo que vaze ou trave indevidamente |
| T-02-15 | Spoofing | AsaasWebhookView | mitigate (HIGH) | Constant-time `asaas-access-token` → 401 antes de qualquer DB write (existente; travado por teste) |
| T-02-16 | Tampering/Replay | AsaasWebhookView | mitigate (HIGH) | insert-and-catch em `AsaasWebhookLog.event_id` antes de efeito; teste de reenvio prova zero efeito duplicado |
| T-02-17 | Info-disclosure | AsaasWebhookView | accept | sub_id desconhecido → 200 idêntico a "processado e ignorado" (existente, T-17-10); risco de enumeração residual aceito |
| T-02-18 | DoS | AsaasWebhookView | mitigate | Sempre 200 após log; erro interno não propaga (Asaas pausa fila após não-2xx) — comportamento existente preservado |
</threat_model>

<verification>
- `pytest apps/gate/tests/test_gate.py apps/billing/tests/test_billing_gate.py apps/billing/tests/test_webhook_ciclo.py apps/billing/tests/test_asaas_webhook_view.py` verdes.
- Suíte completa de billing/gate/accounts sem regressão (`pytest apps/billing apps/gate apps/accounts`).
</verification>

<success_criteria>
Cobre SC2 (webhook idempotente ativa/inadimplente) e SC3 (gate libera enquanto trial OU ativa; bloqueia
após graça/cancelamento passado o período devido), fechando com a garantia de que os dois gates concordam.
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-cobran-a-asaas-webhooks-conta/02-03-SUMMARY.md`.
</output>
