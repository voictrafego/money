---
phase: 03-go-live-e2e-pago
plan: 05
type: execute
wave: 3
depends_on: ["03-04"]
files_modified: []
autonomous: false
requirements: [OPS-01]
user_setup:
  - service: asaas
    why: "Sandbox p/ o fluxo automatizável + 1 cobrança real R$19,90 no cartão próprio (smoke, com estorno)"
    dashboard_config:
      - task: "Confirmar webhook prod apontando para https://www.lazaricapital.com.br/billing/webhook/ com o ASAAS_WEBHOOK_TOKEN"
        location: "Asaas (conta prod) → Notificações/Webhooks"
must_haves:
  truths:
    - "A suíte de billing/gate/webhook da Phase 2 roda verde (fluxo cadastro→trial→PAYMENT_CONFIRMED→ativação→overdue→graça→cancelamento→bloqueio, idempotente por event_id)"
    - "Um POST assinado (ASAAS_WEBHOOK_TOKEN) ao endpoint público /billing/webhook/ ao vivo é aceito e é idempotente (repetição não duplica efeito)"
    - "Cada transição de Conta.status reflete no acesso ao app.host ao vivo: trial libera; confirmado libera; cancelado libera até paid-through e depois bloqueia; overdue+grace vencido bloqueia"
    - "1 cobrança real de R$19,90 no cartão próprio confirma chaves prod + checkout hospedado + webhook prod + gate/WS reais, seguida de estorno"
  artifacts:
    - path: "checklist E2E (RESEARCH § E2E pago)"
      provides: "Aceite ponta-a-ponta verificado ao vivo — E2E sandbox + smoke real R$19,90 estornado + reuso da suíte Phase 2 (D-01/D-02)"
  key_links:
    - from: "Asaas (webhook prod)"
      to: "www.lazaricapital.com.br/billing/webhook/"
      via: "POST assinado + idempotência por event_id"
      pattern: "status da Conta converge sem duplicar"
    - from: "Conta.status"
      to: "acesso ao app (gate)"
      via: "GateView regra de 3 ramos"
      pattern: "transição de status → libera/bloqueia"
---

<objective>
Executar o **teste E2E pago** (OPS-01, criterio #2) em três camadas (D-01/D-02): (1) **automatizável**
— rodar a suíte de billing/gate/webhook travada na Phase 2 e exercitar o endpoint público
`/billing/webhook/` ao vivo com POST assinado (idempotência); (2) **ao vivo** — percorrer cada
transição de `Conta.status` no host real `app.lazaricapital.com.br` e confirmar que o acesso reflete
(trial → confirmado → cancelado → overdue → bloqueio); (3) **smoke real** — 1 assinatura real de
**R$ 19,90** no cartão próprio contra o Asaas **prod**, confirmando chaves prod + checkout hospedado +
webhook prod + gate/WS reais, **com estorno** logo em seguida.

Purpose: é a prova final de que o produto cobra e o acesso reflete fielmente o status — o coração do
core value do milestone. Nada novo de lógica (reusa a Phase 2); a novidade é exercitar ao vivo.
Output: checklist de aceite E2E completo, incluindo o smoke real estornado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-go-live-e2e-pago/03-CONTEXT.md
@.planning/phases/03-go-live-e2e-pago/03-RESEARCH.md
@.planning/phases/02-cobran-a-asaas-webhooks-conta/02-CONTEXT.md

<interfaces>
Suíte automatizável (Phase 2, lazari-capital/apps/billing/tests/ e apps/gate/tests/):
- test_webhook_ciclo.py — ciclo assinar→PAYMENT_CONFIRMED→ativação; convergência por dueDate; idempotência por event_id
- test_billing_gate.py / apps/gate/tests/test_gate.py — GateView 3 ramos (pago/trial/graça/paid-through)
- test_asaas_webhook_view.py — token + idempotência do endpoint público
- test_cancelar.py — cancel-at-period-end (DELETE Asaas + grace_ate=paid-through)
Rodar: `cd ~/projects/lazari-capital && python -m pytest apps/billing apps/gate apps/accounts -q`.

Endpoint público ao vivo: POST https://www.lazaricapital.com.br/billing/webhook/ com o header/token
do ASAAS_WEBHOOK_TOKEN (mesma mecânica dos testes de idempotência).

Gate ao vivo: acesso a app.lazaricapital.com.br reflete Conta.status (GateView, apps/gate/views.py):
- ATIVO com trial_ate>=hoje OU trial_ate is None (pago) → libera
- INADIMPLENTE/CANCELADO com now<=grace_ate → libera (graça / paid-through)
- senão → 302 /trial-acabou/
Plano PRO real: R$ 19,90 mensal (seed 0002).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Camada automatizável — suíte de billing/gate + webhook público ao vivo</name>
  <read_first>
    - ~/projects/lazari-capital/apps/billing/tests/test_webhook_ciclo.py (ciclo + idempotência)
    - ~/projects/lazari-capital/apps/billing/tests/test_asaas_webhook_view.py (token + repetição)
    - ~/projects/lazari-capital/apps/gate/tests/test_gate.py (3 ramos do gate)
    - .planning/phases/03-go-live-e2e-pago/03-RESEARCH.md (§ E2E pago — camadas do teste + checklist)
  </read_first>
  <files>(nenhum arquivo novo — executa a suíte existente e exercita o endpoint ao vivo)</files>
  <action>
    1. Rodar a suíte de billing/gate/accounts do lazari-capital
       (`cd ~/projects/lazari-capital && python -m pytest apps/billing apps/gate apps/accounts -q`) —
       confirma que a lógica travada na Phase 2 (ciclo, idempotência, 3 ramos do gate, cancelamento)
       segue verde no código deployado. NÃO reescrever testes.
    2. Exercitar o endpoint público **ao vivo**: enviar um POST assinado (com o ASAAS_WEBHOOK_TOKEN de
       prod) para `https://www.lazaricapital.com.br/billing/webhook/` simulando `PAYMENT_CONFIRMED`
       para uma conta de teste, e **repetir o mesmo evento** (mesmo event_id) — confirmar que o segundo
       POST é idempotente (status não duplica, resposta 2xx/já-processado). Usar a mesma forma dos
       testes de idempotência da Phase 2 como referência do payload/assinatura.
  </action>
  <acceptance_criteria>
    - `cd ~/projects/lazari-capital && python -m pytest apps/billing apps/gate apps/accounts -q` → 0 falhas
    - POST assinado a /billing/webhook/ ao vivo retorna 2xx; o POST repetido (mesmo event_id) NÃO altera o status novamente (idempotente)
    - POST com token inválido/ausente é rejeitado (4xx)
  </acceptance_criteria>
  <verify>
    <automated>cd ~/projects/lazari-capital && python -m pytest apps/billing apps/gate apps/accounts -q</automated>
  </verify>
  <done>Camada automatizável verde e endpoint público comprovadamente idempotente ao vivo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Camada ao vivo — transições de status refletem no acesso</name>
  <read_first>
    - ~/projects/lazari-capital/apps/gate/views.py (regra de 3 ramos do GateView)
    - .planning/phases/02-cobran-a-asaas-webhooks-conta/02-CONTEXT.md (política de graça/cancelamento; D-04/D-05)
    - .planning/phases/03-go-live-e2e-pago/03-RESEARCH.md (§ E2E pago — camada ao vivo, itens do checklist)
  </read_first>
  <files>(nenhum — exercício ao vivo no host real)</files>
  <what-built>
    Stack no ar (03-04). Este checkpoint percorre, no host real, cada transição de `Conta.status` e
    confirma que o gate reflete no acesso — o coração do criterio #2.
  </what-built>
  <how-to-verify>
    Usando uma conta de teste no ambiente ao vivo (Asaas **sandbox** para as transições sem dinheiro):
    1. **Trial ativo** → `app.lazaricapital.com.br` abre logado e o WS conecta (interação sem loop).
    2. **Acesso anônimo** a `app.` → 302 login (não vaza o app).
    3. **Assinar (sandbox)** → webhook `PAYMENT_CONFIRMED` → conta ATIVA; o app segue liberado como
       pago (trial zerado). Reenviar o webhook → sem efeito duplicado.
    4. **Cancelar** pela página de conta (`/conta/`) → acesso liberado até paid-through (`grace_ate`);
       após esse prazo, `app.` redireciona para `/trial-acabou/`.
    5. **Inadimplência (sandbox `PAYMENT_OVERDUE`)** + passar de `grace_ate` → `app.` bloqueia.
    Confirmar em cada passo que o comportamento do gate bate com o status no admin/página de conta.
  </how-to-verify>
  <acceptance_criteria>
    - trial ativo → app abre e WS conecta; anônimo → 302 login
    - PAYMENT_CONFIRMED (sandbox) → app liberado como pago; webhook repetido não duplica
    - cancelamento → libera até paid-through, depois bloqueia (/trial-acabou/)
    - overdue + grace vencido → app bloqueia
  </acceptance_criteria>
  <resume-signal>Digite "transições ok" quando todas as transições de status refletirem corretamente no acesso, ou reporte qual ramo divergiu.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Smoke real — R$19,90 no cartão próprio, com estorno</name>
  <read_first>
    - .planning/phases/03-go-live-e2e-pago/03-CONTEXT.md (D-01 — smoke real com estorno)
    - .planning/phases/02-cobran-a-asaas-webhooks-conta/02-CONTEXT.md (checkout hospedado; produto não toca cartão)
    - .planning/phases/03-go-live-e2e-pago/03-RESEARCH.md (§ E2E pago — smoke real 1×; Pitfall 4 valida chaves prod)
  </read_first>
  <files>(nenhum — transação real no Asaas prod)</files>
  <action>
    Contra o Asaas **prod** (chaves reais do `.env`): criar uma conta real, assinar o Plano PRO
    (R$ 19,90) via checkout hospedado com o **cartão próprio**. Confirmar que: o checkout hospedado
    abre (chaves prod válidas — valida Pitfall 4 do `$$`), o pagamento é confirmado, o **webhook prod**
    chega em `/billing/webhook/` e ativa a conta, e o acesso ao `app.` (gate + WS reais) libera como
    pago. Logo em seguida, **estornar/cancelar** a cobrança no painel do Asaas para não reter o valor.
    Registrar o resultado no checklist de aceite E2E do SUMMARY.
  </action>
  <acceptance_criteria>
    - Checkout hospedado abre com chaves prod (sem 401 do Asaas — prova o `$$` correto)
    - Pagamento R$19,90 confirmado → webhook prod ativa a conta → `app.` libera como pago
    - Cobrança estornada/cancelada no Asaas logo após o smoke
  </acceptance_criteria>
  <resume-signal>Digite "smoke real ok" após confirmar o acesso pago real e estornar a cobrança; se o checkout deu 401/chave inválida, reporte para revisar o `$$` no .env (03-02).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asaas → /billing/webhook/ | Webhook externo cruza para a fonte de verdade (Postgres) |
| Usuário → checkout hospedado Asaas | Dados de cartão nunca tocam o produto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Tampering | webhook forjado | mitigate | Exercitado ao vivo: token do ASAAS_WEBHOOK_TOKEN exigido; POST sem token → 4xx (Task 1) |
| T-03-18 | Tampering | replay de webhook | mitigate | Idempotência por event_id validada ao vivo (POST repetido não duplica efeito) |
| T-03-19 | Info Disclosure | dados de cartão | mitigate | Checkout hospedado pelo Asaas — produto nunca manuseia cartão (validado no smoke real) |
| T-03-20 | Elevation of Privilege | acesso sem pagar | mitigate | Gate lê só Conta (fonte de verdade); transições exercitadas ao vivo confirmam bloqueio pós-cancelamento/overdue |
</threat_model>

<verification>
- pytest apps/billing apps/gate apps/accounts verde
- webhook público ao vivo idempotente; token inválido rejeitado
- todas as transições de status refletem no acesso ao app real
- smoke real R$19,90 confirma pago e é estornado
</verification>

<success_criteria>
E2E pago completo ao vivo (cadastro → trial → pagamento → acesso → cancelamento → bloqueio) com cada
transição refletindo no acesso, mais o smoke real estornado — atende integralmente à criterio #2 e
fecha OPS-01.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-go-live-e2e-pago/03-05-SUMMARY.md` com o checklist de aceite E2E preenchido.
</output>
