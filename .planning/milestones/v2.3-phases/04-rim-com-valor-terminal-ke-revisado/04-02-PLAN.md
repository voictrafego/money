---
phase: 04-rim-com-valor-terminal-ke-revisado
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/analista/core/motores.py
  - config.yaml
  - src/analista/report/report.py
  - tests/test_motores.py
  - tests/fixtures/fair_values_bancos.yaml
  - tests/test_backtest_bancos.py
autonomous: true
requirements: [CAL-01, CAL-02]

must_haves:
  truths:
    - "A cesta cruza o quórum: ≥3/4 dentro da banda ±15% (ITUB4, BBAS3, BBDC4 passam)"
    - "ITUB4 não regride: RIM permanece R$32,88 (bit-idêntico ao legado — o cap satura)"
    - "BBAS3 cai de 45,60 para ~43,89 (dentro da banda 17,00–44,85, alto na banda)"
    - "BBDC4 sobe de 10,47 para ~13,37 (dentro da banda 12,75–27,60)"
    - "BBSE3 fica documentado como exceção de arquétipo (excecao_nota presente na fixture)"
    - "O tripwire cai: xfail removido de test_backtest_gate_quorum_e_anotacao e o gate fica verde"
    - "Goldens intactos: test_motores/test_ddm/test_vulc3/selo verdes (roe_terminal default None = legado)"
    - "D-01: normalização through-cycle do ROE aplicada SÓ no valor terminal (janela explícita e roe0 intactos → ITUB4 não regride)"
    - "D-02: turnaround do BBDC4 resolvido pela normalização D-01, sem knob próprio de 'banco em turnaround'"
    - "D-06: piso 3/4 respeitado (2/4 inaceitável) — a cesta cruza o quórum"
    - "D-07: gate não afrouxa (banda ±15% e QUORUM_MIN intocados); xfail(strict) removido ao cruzar 3/4"
    - "D-08: um knob global de banco; divergência de arquétipo por normalização/roteamento, não por caps por sub-tipo"
    - "D-09: BBAS3 não é bug de dado (num_acoes correto) — resolve pela normalização D-01, sem task de conserto de dado"
  artifacts:
    - path: "src/analista/core/motores.py"
      provides: "motores.rim com parâmetro opcional roe_terminal (backward-safe)"
      contains: "roe_terminal"
    - path: "config.yaml"
      provides: "knob roe_terminal_stat no bloco motores.rim"
      contains: "roe_terminal_stat"
    - path: "src/analista/report/report.py"
      provides: "cômputo do ROE through-cycle e injeção via roe_terminal no ramo RIM"
      contains: "roe_terminal"
    - path: "tests/test_motores.py"
      provides: "test_rim_terminal_normalizado (abaixo do cap muda; acima do cap = legado)"
      contains: "test_rim_terminal_normalizado"
    - path: "tests/fixtures/fair_values_bancos.yaml"
      provides: "excecao_nota no BBSE3 (exceção de arquétipo D-05)"
      contains: "excecao_nota"
  key_links:
    - from: "src/analista/report/report.py"
      to: "src/analista/core/motores.py::rim"
      via: "roe_terminal=roe_ciclo (mediana de c.roe(ano)) no ramo motor=='rim'"
      pattern: "roe_terminal"
    - from: "tests/test_backtest_bancos.py"
      to: "src/analista/backtest.py::rodar_cesta"
      via: "gate de quórum (mesma função do script)"
      pattern: "rodar_cesta"
---

<objective>
Fechar o loop D-12 dando ao RIM uma **normalização through-cycle do ROE no valor terminal**
(Alavanca 2, D-01): ancorar o ROE do RI terminal na **mediana histórica do próprio ticker**,
aplicada SÓ na perpetuidade de Gordon e ainda capada por `excesso_sustentavel`. Isso cobre BBAS3
(ROE caindo) e BBDC4 (ROE recuperando) — o mesmo problema em direções opostas — e **deixa o ITUB4
bit-idêntico por construção** (o excesso through-cycle dele satura o cap). Resultado computado:
cesta **3/4 na banda ±15% + BBSE3 como exceção documentada**, cruzando o quórum e destravando a
remoção do `xfail(strict)` — o fechamento explícito do loop D-12 (D-07).

Purpose: a calibração da Fase 4 precisa **generalizar** na cesta de bancos (não ser overfit no
ITUB4); este plano entrega o piso honesto D-05 (3/4+1).
Output: `motores.rim` com param `roe_terminal`, knob `roe_terminal_stat`, wiring no report, teste
unit novo, `excecao_nota` do BBSE3, e o gate `test_backtest_bancos.py` verde sem xfail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rim-com-valor-terminal-ke-revisado/04-RESEARCH.md
@.planning/phases/04-rim-com-valor-terminal-ke-revisado/04-CONTEXT.md
@.planning/phases/04-rim-com-valor-terminal-ke-revisado/04-01-SUMMARY.md

<interfaces>
<!-- Contratos que o executor consome DIRETAMENTE — sem explorar o codebase. -->

Assinatura atual de motores.rim (src/analista/core/motores.py:64) — adicionar UM param opcional no fim:
```
def rim(vpa0, roe0, ke, retencao, n, excesso_sustentavel=0.0, g_terminal=None,
        ke_g_spread_min=0.03, fade_para=None) -> Optional[ResultadoRIM]
```
Bloco do terminal legado (linhas 112-116): usa `ris[-1]` como base do RI terminal.

ROE por ano (src/analista/core/fundamentals.py:92): `c.roe(ano) -> Optional[float]` (PL médio;
None no 1º ano por falta de PL−1). Série de anos: `c.anos_ordenados()`.

Dispatch RIM (src/analista/report/report.py:202-214): já lê rim_cfg e chama motores.rim com
excesso_sustentavel/g_terminal/ke_g_spread_min. É AQUI que roe_terminal deve ser computado e passado.

Config bloco alvo (config.yaml:229-252): `motores.rim` com excesso_sustentavel=0.045,
g_terminal=0.025, ke_teto=0.13. **NÃO alterar esses valores** (mudam o ITUB4).

Gate (tests/test_backtest_bancos.py:86-118): `test_backtest_gate_quorum_e_anotacao` está com
`@pytest.mark.xfail(strict=True, raises=AssertionError)`; QUORUM_MIN=3. `test_backtest_cesta_rota_por_ticker`
exige `excecao_nota` só para motor != "rim". Harness: `rodar_cesta` usa `a.intrinseco_motor` e `a.motor`.

Fixture (tests/fixtures/fair_values_bancos.yaml): BBSE3 tem min:33.00 max:46.00 — banda ±15% =
[28,05; 52,90]. Adicionar chave `excecao_nota` ao bloco BBSE3.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parâmetro roe_terminal (normalização through-cycle) em motores.rim</name>
  <files>src/analista/core/motores.py, tests/test_motores.py</files>
  <read_first>
    - src/analista/core/motores.py (assinatura e corpo de rim, linhas 64-123 — o bloco do terminal 112-116)
    - tests/test_motores.py (padrão dos testes existentes: test_rim_itub4_live_alvo_32_40, test_rim_bad_bank_abaixo_do_book)
    - .planning/phases/04-rim-com-valor-terminal-ke-revisado/04-RESEARCH.md (§Alavanca 2 — fórmula; §Assinatura de motores.rim; §Por que o ITUB4 NÃO regride)
  </read_first>
  <behavior>
    - RED: test_rim_terminal_normalizado — com roe_terminal ABAIXO do cap (excesso terminal < excesso_sustentavel), o RIM muda vs. a chamada legada (roe_terminal=None); com roe_terminal ACIMA do cap, o resultado é IDÊNTICO ao legado (o min satura no cap). Falha antes da implementação (TypeError / valor idêntico onde deveria mudar).
    - Backward-safe: qualquer chamada existente sem roe_terminal reproduz o valor legado bit-a-bit.
  </behavior>
  <action>
    Escreva PRIMEIRO o teste `test_rim_terminal_normalizado` em tests/test_motores.py cobrindo dois
    casos: (a) `roe_terminal` cujo excesso sobre o Ke é MENOR que `excesso_sustentavel` → o
    `valor_intrinseco` difere da chamada legada (prova que a alavanca move); (b) `roe_terminal` cujo
    excesso é MAIOR que o cap → `valor_intrinseco` idêntico à chamada legada (prova a proteção do
    ITUB4 por saturação do cap). Rode e confirme RED.
    Depois adicione o parâmetro opcional `roe_terminal: Optional[float] = None` no FIM da assinatura
    de `rim`. No bloco do terminal (onde hoje `ri_terminal = ris[-1] * (1 + g_terminal)`): quando
    `roe_terminal is not None`, recompute a base do RI terminal como
    `excesso_t = min(roe_terminal - ke, excesso_sustentavel)` e `ri_terminal_base = excesso_t * b_{n-1}`
    (a base de book do último ano da janela, a mesma sobre a qual o RI legado é medido), e então
    `ri_terminal = ri_terminal_base * (1 + g_terminal)`. Quando `roe_terminal is None`, mantenha o
    comportamento legado (`ris[-1]`). A janela explícita (roe0/fade_para) fica INTOCADA — a
    normalização entra SÓ no RI da perpetuidade (Pitfall 1). Não use fenced code no corpo; siga o
    padrão zero-magic-constant do motor. Atualize o docstring de `rim` para descrever roe_terminal.
  </action>
  <verify>
    <automated>pytest tests/test_motores.py::test_rim_terminal_normalizado -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_motores.py::test_rim_terminal_normalizado -x` PASSA.
    - `pytest tests/test_motores.py -q` verde (todos os goldens existentes reproduzem: roe_terminal
      default None = legado — test_rim_itub4_live_alvo_32_40 permanece em [32,40]).
    - A assinatura tem `roe_terminal` como ÚLTIMO parâmetro, default None.
  </acceptance_criteria>
  <done>Param opcional roe_terminal (backward-safe) normaliza o RI terminal abaixo do cap e satura o ITUB4 acima dele; goldens intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Computar o ROE through-cycle no report e injetar via roe_terminal + knob de config</name>
  <files>src/analista/report/report.py, config.yaml, tests/test_backtest_bancos.py</files>
  <read_first>
    - src/analista/report/report.py (função _intrinseco_por_motor, ramo motor=='rim', linhas 202-214)
    - src/analista/core/fundamentals.py (c.roe(ano) linha 92; c.anos_ordenados)
    - config.yaml (bloco motores.rim, linhas 229-252)
    - tests/test_backtest_bancos.py (molde do gate: ROOT/_cfg, import de rodar_cesta/carregar_snapshot/carregar_fair_values, bounds absolutos sobre o snapshot congelado)
    - .planning/phases/04-rim-com-valor-terminal-ke-revisado/04-RESEARCH.md (§Alavanca 2 — ROE through-cycle computado; §Knobs de Config; Assumptions A3)
  </read_first>
  <action>
    No ramo `motor == "rim"` de `_intrinseco_por_motor`, compute o ROE through-cycle do ticker:
    a série `[c.roe(a) for a in c.anos_ordenados()]` filtrando os None, e tome a **mediana** (ou média,
    conforme o knob) — use `statistics.median`/`statistics.mean` (adicione `import statistics` no topo
    se ausente). Leia a estatística do novo knob `roe_terminal_stat` (default `"mediana"`) em
    `rim_cfg`. Passe `roe_terminal=roe_ciclo` na chamada de `motores.rim` (só neste ramo). Se a série
    filtrada tiver menos de ~3 pontos válidos ou roe_ciclo for None, passe `roe_terminal=None`
    (degrada para o legado, never-raise). NÃO toque nos outros ramos (normalizado/dcf/nav/ddm) nem no
    ramo fronteiriço.
    Em config.yaml, no bloco `motores.rim`, adicione a chave `roe_terminal_stat: "mediana"` com o WHY
    documentado (estatística do ROE through-cycle no terminal; mediana é robusta ao colapso de ROE de
    1 ano — agro 2025 do BBAS3; único knob novo desta alavanca, o anchor sai da série, não é
    constante — D-08). NÃO altere excesso_sustentavel, g_terminal, ke_teto ou qualquer outro knob
    existente (mudariam o ITUB4).
    Por fim, converta os alvos por-ticker desta alavanca numa asserção AUTOMATIZADA (não inspeção
    manual): escreva o teste determinístico `test_backtest_alvos_recalibrados` em
    tests/test_backtest_bancos.py que rode `rodar_cesta` sobre o snapshot congelado (mesmo molde de
    ROOT/_cfg/carregar_snapshot/carregar_fair_values já usado no arquivo) e ASSERTE, com bounds
    absolutos (tolerância ±R$0,20; NÃO pytest.approx), os alvos intermediários: ITUB4 ≈ 32,88 (não
    regride), BBAS3 ≈ 43,89, BBDC4 ≈ 13,37. Reuse o MESMO harness do gate (não reimplemente o cálculo);
    não toque em arquivo de motor. Mantenha os mesmos números-alvo já no plano.
  </action>
  <verify>
    <automated>pytest tests/test_backtest_bancos.py::test_backtest_alvos_recalibrados -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_backtest_bancos.py::test_backtest_alvos_recalibrados -x` PASSA: sobre o snapshot congelado, `rodar_cesta` crava ITUB4 ≈ R$32,88 (INALTERADO, não regride), BBAS3 ≈ R$43,89 (dentro de 17,00–44,85), BBDC4 ≈ R$13,37 (dentro de 12,75–27,60) — com bounds absolutos ±R$0,20.
    - `pytest tests/test_backtest_bancos.py::test_backtest_determinismo -x` verde (o snapshot não deriva; BBSE3 ≈ R$25,38 inalterado por esta alavanca — o cap satura).
    - config.yaml contém `roe_terminal_stat: "mediana"` no bloco motores.rim; nenhum outro knob mudou (`git diff config.yaml` mostra só a linha nova + comentário).
    - `pytest tests/test_motores.py::test_rim_itub4_live_alvo_32_40 -x` continua verde (não-regressão do ITUB4).
  </acceptance_criteria>
  <done>ROE through-cycle computado no report e injetado via roe_terminal (knob roe_terminal_stat); asserção automatizada crava ITUB4 32,88 / BBAS3 43,89 / BBDC4 13,37 sobre o snapshot.</done>
</task>

<task type="auto">
  <name>Task 3: Fechar o loop — excecao_nota do BBSE3 e remoção do xfail(strict)</name>
  <files>tests/fixtures/fair_values_bancos.yaml, tests/test_backtest_bancos.py</files>
  <read_first>
    - tests/test_backtest_bancos.py (test_backtest_gate_quorum_e_anotacao com o marcador xfail, linhas 86-118; test_backtest_cesta_rota_por_ticker linhas 63-83)
    - tests/fixtures/fair_values_bancos.yaml (bloco BBSE3)
    - .planning/phases/04-rim-com-valor-terminal-ke-revisado/04-RESEARCH.md (§Impacto nos Goldens; Pitfall 5 e Pitfall 6)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-04-SUMMARY.md (o achado 1/4 que o loop fecha)
  </read_first>
  <action>
    Adicione a chave `excecao_nota` ao bloco BBSE3 em tests/fixtures/fair_values_bancos.yaml,
    documentando que a BBSE3 é a exceção de arquétipo D-05: seguradora capital-light cujo valor está
    na franquia/fluxo de dividendo, não no book minúsculo — o RIM ancorado em book a subvaloriza; fica
    fora do quórum como exceção documentada nesta iteração (a rota própria de seguradora vem no plano
    04-03). Isso satisfaz o loop de anotação do quórum-test (cada falha DEVE ter nota — D-08).
    Em seguida, com a cesta agora em 3/4 (quórum atingido), o `test_backtest_gate_quorum_e_anotacao`
    vira XPASS — e `strict=True` transforma XPASS em FAIL. **Remova o decorator
    `@pytest.mark.xfail(...)`** desse teste (é o fechamento explícito do loop D-12, D-07/Pitfall 6).
    Atualize o docstring do módulo do gate (linhas 8-20) para refletir o novo estado (3/4 + BBSE3
    exceção documentada; loop D-12 fechado pela Alavanca 2). NÃO afrouxe a banda ±15% nem QUORUM_MIN.
  </action>
  <verify>
    <automated>pytest tests/test_backtest_bancos.py -q && test $(grep -v '^#' tests/test_backtest_bancos.py | grep -c "pytest.mark.xfail") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_backtest_bancos.py -q` verde: `test_backtest_gate_quorum_e_anotacao` PASSA (quórum ≥3/4, BBSE3 com excecao_nota), sem marcador xfail no arquivo.
    - `grep -c "pytest.mark.xfail" tests/test_backtest_bancos.py` retorna 0.
    - `test_backtest_cesta_rota_por_ticker` verde (ITUB4 motor rim em [30,40]; BBSE3 ainda roteia rim, sem exigência de nota por aquele teste).
    - Suíte completa verde: `pytest -q` sem falhas (goldens intactos: ddm/vulc3/selo/motores).
  </acceptance_criteria>
  <done>BBSE3 anotado como exceção de arquétipo e xfail(strict) removido: o gate quórum-3/4 fica verde, fechando o loop D-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingestão CVM/Yahoo → engine | Inputs (lucro/PL/ações/dividendos) já ingeridos a montante; nesta fase vêm do snapshot congelado (fixture determinística offline). Nenhuma nova entrada não-confiável cruza a fronteira. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure / Tampering (I/T) | motores.rim(roe_terminal), report._intrinseco_por_motor | accept | Mudança em função numérica pura; sem rede, sem I/O, sem input não-confiável novo. Sem superfície de ataque (research §Security). |
| T-04-02 | Denial of Service (D) — input degenerado (série vazia/None, ke≤0) | motores.rim, cômputo do roe_ciclo | mitigate | Never-raise preservado: roe_ciclo=None ou <3 pontos → roe_terminal=None (degrada para legado); rim já retorna None sob input degenerado. ASVS V5 satisfeita pelo never-raise. |
</threat_model>

<verification>
- `pytest tests/test_motores.py tests/test_backtest_bancos.py -q` — verde (por-commit, research §Sampling).
- `pytest -q` — suíte completa verde (por-wave, 440+ baseline).
- Cesta pós-recalibração: ITUB4 32,88 (não regride) · BBAS3 43,89 · BBDC4 13,37 · BBSE3 25,38 (exceção) → 3/4 + 1 anotada.
- `git diff config.yaml` mostra só o knob novo; `ddm.py`/`selo.py`/`lentes.py` intocados.
</verification>

<success_criteria>
- Cesta cruza o quórum: ≥3/4 na banda ±15% (ITUB4, BBAS3, BBDC4).
- ITUB4 não regride (R$32,88 bit-idêntico — cap satura).
- BBSE3 documentado como exceção de arquétipo (excecao_nota presente).
- xfail(strict) removido de test_backtest_gate_quorum_e_anotacao e o gate verde (loop D-12 fechado).
- Suíte completa verde (goldens intactos).
</success_criteria>

<output>
Após a conclusão, crie `.planning/phases/04-rim-com-valor-terminal-ke-revisado/04-02-SUMMARY.md`
</output>
