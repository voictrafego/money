---
phase: 06-redeploy-do-app-v2-3-na-vps
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: []
autonomous: true   # D-01: Claude dirige o deploy via SSH, sem checkpoint humano por comando
requirements: [OPS-01]
user_setup: []

must_haves:
  truths:
    - "A imagem money:pre-v2.3 existe na VPS ANTES do rebuild (rede de rollback pronta)"
    - "/root/money na VPS está no checkout da tag v2.3 (D-05)"
    - "money:latest foi reconstruída a partir da tag v2.3 (D-05)"
    - "O service lazari_money foi atualizado (--force --image money:latest) e converge para 1/1 — deploy dirigido por Claude via SSH, sem checkpoint por comando (D-01, D-05)"
  artifacts:
    - path: "VPS: imagem docker money:pre-v2.3"
      provides: "Rollback em 1 comando se o novo container quebrar (D-06)"
      contains: "tag docker apontando para a imagem de prod que rodava ANTES do rebuild"
    - path: "VPS: imagem docker money:latest (rebuild da tag v2.3)"
      provides: "Código v2.3 servido pelo service lazari_money"
      contains: "build de /root/money no checkout da tag v2.3"
  key_links:
    - from: "service lazari_money"
      to: "imagem money:pre-v2.3"
      via: "docker service update --rollback lazari_money (ou --image money:pre-v2.3)"
      pattern: "money:pre-v2.3"
---

<objective>
Rede de rollback (D-06) e redeploy do código v2.3 na VPS (D-05). Antes de reconstruir, taguear a
imagem de prod atual como `money:pre-v2.3` para rollback instantâneo; depois dar checkout da tag
`v2.3` em `/root/money`, reconstruir `money:latest` e atualizar o service `lazari_money` no stack
unificado `lazari`.

Purpose: Salto multi-versão (pré-v2.2 → v2.3) em réplica única exige rede de segurança barata e
instantânea (D-06). O procedimento é o runbook canônico do go-live v2.0, com a fonte atualizada
para a tag v2.3.
Output: Service `lazari_money` rodando `money:latest` reconstruída da tag `v2.3`, convergindo 1/1;
imagem `money:pre-v2.3` disponível para rollback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-redeploy-do-app-v2-3-na-vps/06-CONTEXT.md
@.planning/milestones/v2.0-phases/03-go-live-e2e-pago/.continue-here.md
@.planning/milestones/v2.0-phases/03-go-live-e2e-pago/03-04-SUMMARY.md
@.planning/milestones/v2.0-phases/03-go-live-e2e-pago/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rollback safety — taguear a imagem de prod atual como money:pre-v2.3 (D-06)</name>
  <files></files>
  <read_first>
    - .planning/phases/06-redeploy-do-app-v2-3-na-vps/06-CONTEXT.md (§decisions D-06)
    - .planning/milestones/v2.0-phases/03-go-live-e2e-pago/.continue-here.md (SSH, chave id_ed25519_vps, redeploy pattern)
    - .planning/milestones/v2.0-phases/03-go-live-e2e-pago/03-03-SUMMARY.md (stack `lazari`, money sem ports:, NUNCA --remove-orphans)
  </read_first>
  <action>
    Conectar na VPS via `ssh -i ~/.ssh/id_ed25519_vps root@31.97.130.40` (D-01). Descobrir a imagem
    que o service `lazari_money` roda AGORA (a imagem/digest efetivamente servido em prod, antes de
    qualquer rebuild) e criar a tag docker `money:pre-v2.3` apontando para ela (`docker tag`). Este
    passo acontece ANTES do rebuild da Task 2 — se `money:latest` for reconstruída antes de taguear,
    a rede de rollback aponta para o código novo (inútil). Não tocar no stack, não fazer service
    update aqui.
    Fallback D-02: se o SSH pedir auth interativa (passphrase, host-key prompt), entregar o comando
    exato ao operador via `! ...` e aguardar.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/id_ed25519_vps root@31.97.130.40 'docker image inspect money:pre-v2.3 --format "{{.Id}}"'</automated>
  </verify>
  <acceptance_criteria>
    - `docker image inspect money:pre-v2.3` na VPS retorna um Id (a tag existe).
    - O Id de `money:pre-v2.3` corresponde à imagem que `lazari_money` rodava antes do rebuild
      (i.e., NÃO é o build novo — foi tagueado antes da Task 2).
    - Rollback disponível: `docker service update --rollback lazari_money` (ou `--image money:pre-v2.3`).
  </acceptance_criteria>
  <done>Imagem de prod anterior preservada como money:pre-v2.3; rollback em 1 comando garantido.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild + redeploy — checkout tag v2.3, build money:latest, service update lazari_money (D-05)</name>
  <files></files>
  <read_first>
    - .planning/phases/06-redeploy-do-app-v2-3-na-vps/06-CONTEXT.md (§decisions D-05; §code_context — Dockerfile empacota código+data via COPY . .)
    - .planning/milestones/v2.0-phases/03-go-live-e2e-pago/03-04-SUMMARY.md (build money:latest, cutover, NUNCA --remove-orphans)
    - .planning/milestones/v2.0-phases/03-go-live-e2e-pago/03-03-SUMMARY.md (deploy canônico = stack `lazari`; money sem ports:; gate FQDN lazari_web)
    - Dockerfile (raiz do repo: python:3.13-slim, PYTHONPATH=/app/src, streamlit run app.py :8501)
  </read_first>
  <action>
    Somente após a Task 1 (money:pre-v2.3 já existe). Na VPS, em `/root/money`: fazer `git fetch`
    e dar checkout da tag `v2.3` (deploy reproduzível amarrado à versão, D-04) — o remote é
    `voictrafego/money`, a tag foi publicada no 06-01. Reconstruir a imagem com
    `docker build -t money:latest .` a partir de `/root/money` (o `COPY . .` do Dockerfile empacota
    código + data). Atualizar o service com `docker service update --force --image money:latest
    lazari_money` no stack unificado `lazari`. PROIBIDO `--remove-orphans` (n8n/crm são orphans
    intencionais). NÃO adicionar bloco `ports:` ao `money` (invariante anti-spoof: único ingresso é
    Traefik + gate forwardAuth). Aguardar o service convergir para 1/1 antes de considerar concluído
    (o smoke completo é o 06-03).
    Fallback D-02: se o SSH pedir auth interativa, entregar o comando exato ao operador via `! ...`.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/id_ed25519_vps root@31.97.130.40 'docker service ls --filter name=lazari_money --format "{{.Name}} {{.Replicas}} {{.Image}}"'</automated>
  </verify>
  <acceptance_criteria>
    - Em `/root/money`, `git describe --tags` (ou HEAD) confirma o checkout da tag `v2.3`.
    - `docker service ls --filter name=lazari_money` mostra `lazari_money` com REPLICAS `1/1` e imagem `money:latest`.
    - `docker service ps lazari_money` mostra a task nova `Running` (sem `Rejected`/`Failed`/crash-loop).
    - Nenhum `--remove-orphans` foi usado; nenhum bloco `ports:` foi adicionado ao `money`.
  </acceptance_criteria>
  <done>lazari_money rodando money:latest reconstruída da tag v2.3, 1/1 convergido; pronto para o smoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → VPS root (SSH) | acesso root com chave privada id_ed25519_vps; comandos de prod cruzam aqui |
| internet → money (Streamlit) | único ingresso legítimo é Traefik + gate forwardAuth (money sem ports:) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Denial of Service | rebuild v2.3 quebra o container (import/dep) em réplica única | mitigate | rollback em 1 comando via money:pre-v2.3 (Task 1 precede o rebuild, D-06) |
| T-06-06 | Spoofing | money exposto fora do Traefik permitiria setar X-User-Email na mão | mitigate | invariante mantida: money SEM bloco `ports:`; único ingresso é o gate forwardAuth (D-05) |
| T-06-07 | Tampering | `--remove-orphans` derrubaria n8n/crm (orphans intencionais) | mitigate | proibição explícita de `--remove-orphans` no `<action>` (padrão herdado v2.0) |
| T-06-08 | Elevation of Privilege | chave SSH root exposta/reusada indevidamente | accept | chave dedicada id_ed25519_vps sob controle do operador; sessão pontual de deploy |
</threat_model>

<verification>
- `docker image inspect money:pre-v2.3` retorna Id (rollback pronto) — tagueado ANTES do rebuild.
- `/root/money` no checkout da tag `v2.3`; `docker build -t money:latest .` concluído.
- `docker service ls --filter name=lazari_money` → `1/1`, imagem `money:latest`; task nova Running.
- Nenhum `--remove-orphans`; money sem `ports:`.
</verification>

<success_criteria>
Service `lazari_money` rodando `money:latest` reconstruída da tag `v2.3`, convergido 1/1, com a
imagem `money:pre-v2.3` disponível para rollback. Infra e comportamento ao vivo são validados no 06-03.
</success_criteria>

<output>
Após completar, criar `.planning/phases/06-redeploy-do-app-v2-3-na-vps/06-02-SUMMARY.md`.
</output>
