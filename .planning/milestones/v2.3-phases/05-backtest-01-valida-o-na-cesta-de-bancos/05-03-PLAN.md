---
phase: 05-backtest-01-valida-o-na-cesta-de-bancos
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/analista/backtest.py
  - scripts/backtest_bancos.py
autonomous: true
requirements: [VAL-01, VAL-02]

must_haves:
  truths:
    - "Uma função pura rodar_cesta(snapshot, fair_values, cfg) roda o RIM calibrado nos 4 bancos e devolve, por ticker, o intrínseco lado a lado com as 4 âncoras"
    - "As 4 âncoras são computadas: (a) Graham+Bazin, (b) preço, (c) faixa FV, (d) múltiplos de pares (P/VP, P/L medianas da própria cesta, D-11)"
    - "D-09: O script standalone (a metade 'script' da entrega pytest+script) gera out/backtest_bancos.md com uma linha por ticker (RIM × 4 âncoras + desvio + PASS/FAIL + nota)"
    - "rodar_cesta é reusada pelo teste (Plan 04) e pelo script — provam o mesmo resultado"
  artifacts:
    - path: "src/analista/backtest.py"
      provides: "rodar_cesta() pura + loaders de snapshot e fair values; consome analisar_acao, nunca reimplementa RIM"
      exports: ["rodar_cesta"]
    - path: "scripts/backtest_bancos.py"
      provides: "Wrapper standalone → out/backtest_bancos.md via tabulate (D-10)"
  key_links:
    - from: "src/analista/backtest.py::rodar_cesta"
      to: "report.analisar_acao"
      via: "extrai a.intrinseco_motor / a.motor por ticker (nunca reimplementa a fórmula)"
      pattern: "analisar_acao"
    - from: "src/analista/backtest.py::rodar_cesta"
      to: "lentes.metricas_par + statistics.median"
      via: "múltiplos de pares medianas da cesta (D-11), zero fonte externa"
      pattern: "metricas_par"
    - from: "scripts/backtest_bancos.py"
      to: "out/backtest_bancos.md"
      via: "tabulate github + write, espelha cli.cmd_analyze"
      pattern: "backtest_bancos.md"
---

<objective>
Construir o harness reprodutível (VAL-01) que roda o RIM calibrado na cesta e triangula as 4 âncoras (VAL-02): uma função pura `rodar_cesta(snapshot, fair_values, cfg)` em `src/analista/backtest.py` (reusada pelo teste e pelo script — garante mesmo resultado, RESEARCH Open Q3), e um wrapper `scripts/backtest_bancos.py` que gera `out/backtest_bancos.md` (D-10).

Purpose: Consolidar num único lugar puro (sem I/O, sem rede) a extração do intrínseco RIM via `analisar_acao` + o cálculo das 4 âncoras + o veredito PASS/FAIL, para que o gate (Plan 04) e o relatório legível provem o MESMO número. O harness CONSOME o motor; nunca reimplementa a fórmula RIM (se a Fase 4 mudar via loop D-12, re-roda o snapshot).
Output: `src/analista/backtest.py` + `scripts/backtest_bancos.py` → `out/backtest_bancos.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-CONTEXT.md
@.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-RESEARCH.md
@.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-PATTERNS.md
@tests/fixtures/snapshot_bancos_2026-07-12.yaml
@tests/fixtures/fair_values_bancos.yaml

<interfaces>
<!-- Contratos consumidos pelo harness. RESEARCH Q1/Q4/Q5/Q6/Q7 + PATTERNS §Shared Patterns. -->
De src/analista/report/report.py:312 — analisar_acao(c, cfg) -> AnaliseAcao; ler a.arquetipo, a.motor, a.intrinseco_motor (report.py:59, pode ser None never-raise), a.preco_atual
De src/analista/core/lentes.py:37 — preco_justo_graham(lpa, vpa) -> Optional[float]  (√(22,5×LPA×VPA); None se LPA/VPA≤0)
De src/analista/core/lentes.py:75 — preco_teto_bazin(dpa_med, dy_minimo=0.06) -> Optional[float]
De src/analista/core/lentes.py:59 — dpa_medio(dpas, n=5); lentes.vpa(pl, num_acoes); c.dpa(ano); c.anos_ordenados(); c.lpa_valuation()
De src/analista/core/lentes.py:151 — metricas_par(c) -> ParComparavel com .pvp, .pl (never-raise, campos None quando insumo falta)
Receita canônica Graham/Bazin (app.py:1053-1069): _ult=c.ultimo_ano(); _vpa=lentes.vpa(c.patrimonio_liquido.get(_ult), c.num_acoes.get(_ult)); graham=lentes.preco_justo_graham(c.lpa_valuation(), _vpa)
Padrão de saída out/ (cli.py:66-87): os.makedirs(OUT_DIR, exist_ok=True); open(destino,"w",encoding="utf-8").write(md). OUT_DIR=os.path.join(ROOT,"out") (cli.py:28). out/ está no .gitignore linha 8.
Markdown (report.py:14 já importa tabulate): tabulate(linhas, headers=[...], tablefmt="github")
Colunas D-10: Ticker | Motor | RIM | Graham | Bazin | Preço | FV faixa | P/VP med | P/L med | Desvio RIM×FV | PASS/FAIL | Nota exceção
LANDMINE rf_local: injetar o rf_local congelado do snapshot em cfg["capm"]["rf_local"] ANTES de analisar_acao; NUNCA chamar a rede. analisar_acao não muta cfg → seguro rodar os 4 com o mesmo dict.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar src/analista/backtest.py com loaders + rodar_cesta() pura</name>
  <files>src/analista/backtest.py</files>
  <read_first>
    - tests/test_vulc3_regressao.py:20-36,160-188 (reconstrução offline de CompanyData + _cfg + analisar_acao)
    - src/analista/cli.py:150-183 (loop cmd_rank — analog de iterar empresas, analisar_acao, coletar métricas)
    - src/analista/core/lentes.py:37-196 (preco_justo_graham, preco_teto_bazin, dpa_medio, vpa, metricas_par)
    - src/analista/report/report.py:312-432 (analisar_acao → a.intrinseco_motor; never-raise; zera ≤0)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-RESEARCH.md §Q1,Q4,Q5,Q7,Q9 (extração, âncoras, landmines)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-PATTERNS.md §Shared Patterns + §rodar_cesta
    - tests/fixtures/snapshot_bancos_2026-07-12.yaml e tests/fixtures/fair_values_bancos.yaml (shapes reais)
  </read_first>
  <action>
    Criar módulo novo `src/analista/backtest.py` (importável como `analista.backtest`; NÃO tocar core/motores, core/lentes, core/ddm, report/selo, config.yaml — só CONSOME). Definir:
    (1) `carregar_snapshot(caminho) -> tuple[list[CompanyData], float]` — lê o YAML, reconstrói cada CompanyData preenchendo os dicts por ano (padrão `_itub4_live_like`), devolve as 4 empresas + o `rf_local` congelado.
    (2) `carregar_fair_values(caminho) -> dict` — lê o YAML de fair values.
    (3) `rodar_cesta(empresas, fair_values, cfg, rf_local) -> list[dict]` — função PURA (sem I/O, sem rede). Injeta `rf_local` em `cfg["capm"]["rf_local"]` antes do loop (analisar_acao não muta cfg). Para cada empresa: roda `report.analisar_acao(c, cfg)`, lê `a.motor`, `a.arquetipo`, `a.intrinseco_motor` (tratar None como fora-da-banda, RESEARCH Q9-3); calcula âncora (a) Graham via `lentes.preco_justo_graham(c.lpa_valuation(), lentes.vpa(...))` e Bazin via `lentes.preco_teto_bazin(lentes.dpa_medio([c.dpa(ano) for ano in c.anos_ordenados()], n=5))`; âncora (b) preço = `c.preco_atual`; âncora (d) coletar `lentes.metricas_par(c).pvp`/`.pl` de TODAS as 4 e tomar `statistics.median` sobre os não-None (D-11, mediana da própria cesta, zero fonte externa). Calcular a faixa FV (min/max do fair_values do ticker), o desvio RIM×FV, e o veredito PASS/FAIL usando as constantes nomeadas `BANDA_PASS = 0.15` e o predicado `_passa(rim, fv_min, fv_max)` = `rim is not None and fv_min*(1-BANDA_PASS) <= rim <= fv_max*(1+BANDA_PASS)` (D-07). Retornar uma lista de dicts por ticker com: ticker, motor, arquetipo, rim(intrinseco_motor), graham, bazin, preco, fv_min, fv_max, pvp_med, pl_med, desvio, passa(bool), excecao_nota(do fair_values). NÃO reimplementar a fórmula RIM. Manter a mediana de pares como agregação do harness (não do core).
  </action>
  <verify>
    <automated>python -c "import yaml; from analista import backtest; cfg=yaml.safe_load(open('config.yaml')); emp,rf=backtest.carregar_snapshot('tests/fixtures/snapshot_bancos_2026-07-12.yaml'); fv=backtest.carregar_fair_values('tests/fixtures/fair_values_bancos.yaml'); res=backtest.rodar_cesta(emp,fv,cfg,rf); assert len(res)==4; assert all('rim' in r and 'passa' in r and 'pvp_med' in r for r in res); print('rodar_cesta OK', [(r['ticker'],r['motor'],round(r['rim'],2) if r['rim'] else None,r['passa']) for r in res])"</automated>
  </verify>
  <acceptance_criteria>
    - `src/analista/backtest.py` exporta `rodar_cesta`, `carregar_snapshot`, `carregar_fair_values`
    - `rodar_cesta` é offline (não chama a rede) e devolve 1 dict por ticker (4 total) com as chaves: ticker, motor, rim, graham, bazin, preco, fv_min, fv_max, pvp_med, pl_med, desvio, passa, excecao_nota
    - As constantes `BANDA_PASS = 0.15` são nomeadas (grep confirma; nenhum `0.15` solto no corpo): `grep -v '^#' src/analista/backtest.py | grep -c 'BANDA_PASS'` retorna ≥ 1
    - A âncora de múltiplos usa `lentes.metricas_par` + `statistics.median` (D-11), não divisão manual
    - O intrínseco RIM vem de `report.analisar_acao(...).intrinseco_motor` — a fórmula RIM NÃO é reimplementada (grep: nenhuma chamada a `motores.rim(` em backtest.py)
    - ITUB4 no resultado: motor == "rim", rim entre 30.0 e 40.0
    - core/motores.py, core/lentes.py, core/ddm.py, report/selo.py, config.yaml intocados (`git diff --name-only`)
  </acceptance_criteria>
  <done>rodar_cesta pura roda os 4 bancos offline, extrai o RIM via analisar_acao e computa as 4 âncoras + PASS/FAIL com constantes nomeadas.</done>
</task>

<task type="auto">
  <name>Task 2: Criar scripts/backtest_bancos.py que gera out/backtest_bancos.md</name>
  <files>scripts/backtest_bancos.py</files>
  <read_first>
    - src/analista/backtest.py (a rodar_cesta + loaders criados na Task 1)
    - src/analista/cli.py:28,66-87 (OUT_DIR + padrão os.makedirs + write markdown em out/)
    - src/analista/report/report.py:14 (import tabulate já disponível)
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-RESEARCH.md §Q6 (formato de saída), §D-10 colunas
    - .planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-PATTERNS.md §scripts/backtest_bancos.py (tabulate github)
  </read_first>
  <action>
    Criar `scripts/backtest_bancos.py` — wrapper fino e invocável por `python scripts/backtest_bancos.py`. Carregar `config.yaml` (yaml.safe_load), chamar os loaders + `backtest.rodar_cesta(...)` (a MESMA função do teste — garante mesmo resultado). Montar uma tabela única (uma linha por ticker) com as colunas do D-10: `Ticker | Motor | RIM | Graham | Bazin | Preço | FV faixa | P/VP med | P/L med | Desvio RIM×FV | PASS/FAIL | Nota exceção`, formatando a faixa FV como "min–max" e PASS/FAIL a partir de `r['passa']`. Renderizar com `tabulate(linhas, headers=[...], tablefmt="github")`. Criar `OUT_DIR` com `os.makedirs(..., exist_ok=True)` (espelha cli.py) e gravar em `out/backtest_bancos.md` (out/ é gitignored linha 8 — saída gerada, esperado). NÃO reusar `report.relatorio_markdown` (é por-ação; o backtest é tabela-resumo da cesta). Se algum ticker tiver motor ≠ "rim" ou rim None, a linha ainda aparece com o veredito e a Nota exceção da FV — o desvio é reportado, não escondido (D-12).
  </action>
  <verify>
    <automated>python scripts/backtest_bancos.py && test -f out/backtest_bancos.md && python -c "t=open('out/backtest_bancos.md').read(); assert 'ITUB4' in t and 'PASS' in t.upper() and 'RIM' in t; print('markdown OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/backtest_bancos.py` roda sem exceção (exit 0) e grava `out/backtest_bancos.md`
    - O markdown tem uma linha por ticker (4 linhas) com as 12 colunas do D-10, incluindo uma coluna PASS/FAIL e uma coluna Nota exceção
    - A faixa FV aparece como "min–max"; o RIM por ticker bate o valor de rodar_cesta (script e função compartilham o mesmo cálculo)
    - out/ permanece gitignored (o .md não entra no diff versionado)
    - Nenhum arquivo de motor/config tocado (`git diff --name-only`)
  </acceptance_criteria>
  <done>Script standalone gera out/backtest_bancos.md com a tabela-resumo RIM × 4 âncoras + PASS/FAIL da cesta, reusando rodar_cesta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture→harness | O harness lê apenas YAML congelado e versionado; nenhuma entrada de usuário em runtime, nenhuma rede |
| harness→out/ | Escreve um markdown gerado em out/ (gitignored); saída de leitura, sem efeito colateral em dados versionados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | rodar_cesta reimplementando a fórmula RIM | mitigate | rodar_cesta CONSOME `report.analisar_acao` (fonte única calibrada); acceptance_criteria proíbe chamada a `motores.rim(` no harness → o número validado não pode divergir do motor shipado |
| T-05-08 | Denial of Service | chamada de rede acidental no harness | mitigate | rf_local injetado do snapshot; harness proibido de chamar `macro.selic_ciclo_para_capm` → roda 100% offline e determinístico |
| T-05-09 | Elevation of Privilege | runtime | accept | Sem input não confiável, sem segredos, sem auth surface — script lê fixtures locais e escreve um .md; superfície de ataque efetivamente nula |
</threat_model>

<verification>
- `rodar_cesta` devolve 4 dicts com RIM + 4 âncoras + PASS/FAIL; ITUB4 motor rim, RIM 30–40
- `python scripts/backtest_bancos.py` gera out/backtest_bancos.md com a tabela D-10
- `grep -v '^#' src/analista/backtest.py | grep -c 'BANDA_PASS'` ≥ 1 (constante nomeada, zero número solto)
- `git diff --name-only` NÃO lista core/motores.py, core/lentes.py, core/ddm.py, report/selo.py, config.yaml
</verification>

<success_criteria>
- Harness reprodutível (VAL-01): rodar_cesta roda o RIM calibrado na cesta offline, uma linha por ticker
- Triangula as 4 âncoras (VAL-02): Graham+Bazin, preço, faixa FV, múltiplos de pares medianas da cesta
- Script e teste compartilham rodar_cesta (mesmo resultado provável)
- out/backtest_bancos.md legível com desvios reportados, não escondidos (D-12)
</success_criteria>

<output>
After completion, create `.planning/phases/05-backtest-01-valida-o-na-cesta-de-bancos/05-03-SUMMARY.md`
</output>
