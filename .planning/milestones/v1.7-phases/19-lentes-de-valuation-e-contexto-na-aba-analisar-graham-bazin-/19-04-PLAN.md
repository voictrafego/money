---
phase: 19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-
plan: 04
type: execute
wave: 3
depends_on: [19-03]
files_modified: []
autonomous: false
requirements: [VAL-01, VAL-02, RET-01, PEER-01]

must_haves:
  truths:
    - "A suíte completa (296 goldens + novos de test_lentes.py) fica verde ao final da fase"
    - "As engines de método (DDM/múltiplos/comparables/report) e as abas Garimpar/Ranking/Swing/Início ficam sem regressão"
    - "No navegador, a aba Analisar mostra Graham, Bazin, retorno 1a/5a e comparador de pares com o alvo destacado, degradando quando faltam dados"
  artifacts:
    - path: ".planning/phases/19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-/19-04-SUMMARY.md"
      provides: "Registro de verificação (goldens + smoke) da fase"
      contains: "296"
  key_links:
    - from: "verificação automatizada"
      to: "engine de método intocada"
      via: "git diff --quiet dos módulos de método"
      pattern: "git diff"
---

<objective>
Fechar a Fase 19 com verificação dupla (padrão do projeto: espelha 16-03/17-03/18-04):
(1) automatizada — 296 goldens + novos de test_lentes.py verdes e módulos de método intocados;
(2) humana — smoke no navegador confirmando as 4 lentes na aba Analisar sem regressão nas demais.

Purpose: garantir os gates locked da fase (296 goldens verdes, app.py read-only, zero dep nova,
método intacto, "exibe nunca recomenda") antes de marcar a fase completa.
Output: 19-04-SUMMARY.md com o resultado da verificação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate automatizado — goldens verdes + método intocado + zero dep nova</name>
  <read_first>
    - tests/ (padrão da suíte)
    - requirements.txt (confirmar que nenhuma dependência foi adicionada)
  </read_first>
  <action>
    Rodar a suíte completa e as travas de invariante:
    - `.venv/bin/python -m pytest -q` deve sair exit 0 (296 existentes + novos de test_lentes.py).
    - Confirmar que os módulos de MÉTODO não mudaram de comportamento:
      `git diff --stat` não deve listar mudança de lógica em ddm.py, multiples.py, comparables.py,
      screening.py, report/report.py, report/presentation.py (fundamentals.py/prices.py/build.py só
      ganharam o campo aditivo serie_precos_ajustada do Plano 02).
    - Confirmar zero dependência nova: `git diff requirements.txt` vazio.
    - Confirmar app.py read-only: as lentes chamam a engine, sem fórmula na view
      (`grep -n "sqrt\|22.5\|/ 0.06" app.py` não deve haver aritmética de Graham/Bazin nova).
    Registrar a contagem exata de testes e o resultado dos diffs.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q && git diff --quiet requirements.txt && echo "GATES OK"</automated>
  </verify>
  <acceptance_criteria>
    - pytest exit 0; contagem >= 296 (existentes) + novos de test_lentes.py
    - `git diff requirements.txt` vazio (zero dep nova)
    - ddm.py/multiples.py/comparables.py/screening.py/report.py/presentation.py sem mudança de lógica
    - Nenhuma fórmula de Graham/Bazin em app.py (só leitura de lentes.*)
  </acceptance_criteria>
  <done>Todos os gates automatizados da fase passam; resultado registrado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke no navegador — 4 lentes na aba Analisar sem regressão</name>
  <what-built>
    A aba Analisar ganhou 4 lentes read-only: cards Graham e Bazin (com upside), bloco "quanto
    teria rendido" (R$1.000 em 1a/5a) e comparador de pares (P/L, P/VP, ROE, DY, Valor de Mercado)
    com o ticker analisado destacado. Tudo degrada sem quebrar e não recomenda.
  </what-built>
  <action>Verificação manual no navegador (não automatizável): subir o app e conferir as 4 lentes na aba Analisar + ausência de regressão nas demais abas, conforme os passos abaixo.</action>
  <how-to-verify>
    1. Subir o app: `.venv/bin/streamlit run app.py` (ou o comando de dev do projeto).
    2. Ir em "Analisar uma ação", analisar TAEE11 (boa pagadora, deve popular tudo):
       - Confirmar card Preço-Justo (Graham) com valor e upside vs preço.
       - Confirmar card Preço-Teto (Bazin) com valor e upside vs preço.
       - Confirmar "R$ 1.000 há 1 ano / 5 anos valeriam R$ X" (com dividendos reinvestidos).
       - Confirmar tabela de comparador de pares com a linha TAEE11 destacada.
       - Confirmar copy neutra (nenhum "compre/venda").
    3. Analisar um caso de degradação (ex.: empresa sem lucro/PL positivo ou sem histórico de
       dividendos): Graham/Bazin devem mostrar "indisponível" com disclaimer, sem quebrar a aba.
    4. Conferir SEM regressão: o veredito fundamentalista, o gráfico 5a e as tabs continuam iguais;
       abrir Início/Garimpar/Ranking/Swing e confirmar que nada mudou de comportamento.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- pytest verde (296 + novos); requirements.txt inalterado; método intocado.
- Smoke humano aprova as 4 lentes e a ausência de regressão nas demais abas.
</verification>

<success_criteria>
- Gates locked confirmados: 296 goldens verdes, zero dep nova, app.py read-only, método intacto, "exibe nunca recomenda".
- Verificação humana no navegador aprovada.
</success_criteria>

<output>
Após completar, criar `.planning/phases/19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-/19-04-SUMMARY.md`
</output>
