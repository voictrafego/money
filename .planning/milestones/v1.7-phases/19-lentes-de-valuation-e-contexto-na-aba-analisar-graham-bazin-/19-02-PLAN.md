---
phase: 19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/ingest/prices.py
  - src/analista/core/fundamentals.py
  - src/analista/ingest/build.py
autonomous: true
requirements: [RET-01]

must_haves:
  truths:
    - "Após montar_empresa(), o CompanyData carrega a série 5a de Adj Close (retorno total, dividendos reinvestidos) num campo próprio, sem NENHUMA chamada de rede nova"
    - "serie_precos (nominal) e o gráfico/veredito da aba Analisar seguem inalterados em comportamento"
    - "O campo novo default None (never-raise) quando o Yahoo falha; os 296 goldens seguem verdes"
  artifacts:
    - path: "src/analista/core/fundamentals.py"
      provides: "Campo serie_precos_ajustada (Adj Close 5a) no dataclass CompanyData"
      contains: "serie_precos_ajustada"
    - path: "src/analista/ingest/prices.py"
      provides: "Persistência do Adj Close já baixado (variável `ajustado`) em DadosMercado"
      contains: "serie_precos_ajustada"
  key_links:
    - from: "src/analista/ingest/build.py"
      to: "src/analista/core/fundamentals.py"
      via: "c.serie_precos_ajustada = dm.serie_precos_ajustada"
      pattern: "serie_precos_ajustada"
---

<objective>
Expor a série 5a de **Adj Close** que `prices.coletar_mercado()` **já baixa** (variável local
`ajustado`, hoje usada só para beta/desempenho e descartada) num campo próprio de `CompanyData`,
para o RET-01 ("quanto teria rendido") calcular retorno total (dividendos reinvestidos) **sem
nova chamada de rede**. Mudança 100% aditiva.

Purpose: o RET-01 precisa de Adj Close (embute reinvestimento); a série pública atual
(`serie_precos`) é NOMINAL (Close cru) — usá-la subestimaria o retorno. O Adj Close já vem no
mesmo `tk.history(period="5y", auto_adjust=False)`; só falta persisti-lo.
Output: campo `serie_precos_ajustada` propagado prices.py -> DadosMercado -> CompanyData.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-/19-CONTEXT.md

<interfaces>
<!-- Pontos exatos de mudança (aditiva). -->

prices.py (dataclass DadosMercado, ~linha 48-60): já tem serie_precos, ohlc, ohlc_ajustado.
prices.py coletar_mercado (~linha 140-153):
  hist = tk.history(period="5y", auto_adjust=False)
  nominal = hist["Close"].dropna()
  ajustado = hist["Adj Close"] if "Adj Close" in hist else hist["Close"]   # <- JÁ EXISTE
  dm.serie_precos = nominal                                                 # nominal (não mexer)
  # `ajustado` hoje só alimenta beta/desempenho e é descartado

fundamentals.py (dataclass CompanyData, ~linha 46-47): já tem serie_precos, ohlc, ohlc_ajustado.
build.py montar_empresa (~linha 64-66): c.serie_precos = dm.serie_precos ; c.ohlc = dm.ohlc ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persistir Adj Close em DadosMercado (prices.py)</name>
  <read_first>
    - src/analista/ingest/prices.py (dataclass DadosMercado + coletar_mercado: já computa `ajustado`)
  </read_first>
  <action>
    Em src/analista/ingest/prices.py: adicionar ao dataclass DadosMercado o campo
    `serie_precos_ajustada: Optional["pd.Series"] = None` (comentário: "Adj Close diário 5a —
    retorno total c/ dividendos reinvestidos; p/ RET-01, mesma origem do beta, sem rede nova").
    Em coletar_mercado, no MESMO bloco onde `ajustado` já é derivado do `hist` (dentro do try que
    faz o `tk.history`), atribuir `dm.serie_precos_ajustada = ajustado.dropna()`. NÃO alterar
    `dm.serie_precos = nominal` nem o uso de `ajustado` no beta/desempenho. Se o Yahoo falhar, o
    campo permanece None (bloco except já existente cobre — never-raise).
  </action>
  <verify>
    <automated>.venv/bin/python -c "from analista.ingest.prices import DadosMercado; print('serie_precos_ajustada' in DadosMercado.__dataclass_fields__)"</automated>
  </verify>
  <acceptance_criteria>
    - DadosMercado tem o campo serie_precos_ajustada (default None)
    - `grep -c "dm.serie_precos_ajustada = ajustado" src/analista/ingest/prices.py` == 1
    - `dm.serie_precos = nominal` intacto (`grep -q "dm.serie_precos = nominal" src/analista/ingest/prices.py`)
  </acceptance_criteria>
  <done>O Adj Close 5a já baixado é persistido em DadosMercado sem chamada de rede nova.</done>
</task>

<task type="auto">
  <name>Task 2: Campo em CompanyData + wiring no build (fundamentals.py, build.py)</name>
  <read_first>
    - src/analista/core/fundamentals.py (dataclass CompanyData, campos serie_precos/ohlc ~linha 46-47)
    - src/analista/ingest/build.py (montar_empresa, bloco de atribuição dm->c ~linha 58-67)
  </read_first>
  <action>
    Em src/analista/core/fundamentals.py: adicionar ao dataclass CompanyData o campo
    `serie_precos_ajustada: Optional["pd.Series"] = None` (comentário: "Adj Close 5a p/ RET-01 —
    retorno total, dividendos reinvestidos"). Colocar junto de serie_precos/ohlc para coesão.
    Em src/analista/ingest/build.py, no bloco onde `c.serie_precos = dm.serie_precos` é atribuído,
    adicionar `c.serie_precos_ajustada = dm.serie_precos_ajustada`. Nenhuma outra mudança —
    montar_empresa continua com a mesma assinatura e o mesmo nº de chamadas de rede.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from analista.core.fundamentals import CompanyData; print('serie_precos_ajustada' in CompanyData.__dataclass_fields__)"</automated>
  </verify>
  <acceptance_criteria>
    - CompanyData tem o campo serie_precos_ajustada (default None)
    - `grep -c "c.serie_precos_ajustada = dm.serie_precos_ajustada" src/analista/ingest/build.py` == 1
    - montar_empresa não ganhou nova chamada de rede (nenhum novo tk.history/yf.download/requests)
  </acceptance_criteria>
  <done>O Adj Close 5a flui prices -> DadosMercado -> CompanyData; RET-01 pode consumi-lo via c.serie_precos_ajustada.</done>
</task>

<task type="auto">
  <name>Task 3: 296 goldens verdes com o campo aditivo</name>
  <read_first>
    - tests/test_ingest_ohlc.py (padrão dos testes de ingestão; conferir que o campo novo não quebra fixtures)
  </read_first>
  <action>
    Rodar a suíte completa. O campo novo tem default None, então nenhum golden/fixture existente
    deve mudar. Se algum teste de ingestão construir DadosMercado/CompanyData por posição (args
    posicionais), garantir que o campo foi adicionado ao FINAL da lista de fields (após os campos
    Optional já existentes) para não deslocar nada. Não criar teste novo aqui (o retorno_periodo
    puro já é coberto no Plan 01; validação end-to-end com rede fica fora do custo-zero offline).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` exit 0, contagem inalterada (296) — este plano não adiciona testes
    - Nenhum teste de ingestão quebrado pelo campo novo
  </acceptance_criteria>
  <done>A suíte segue com 296 goldens verdes; o campo aditivo não perturbou nada.</done>
</task>

</tasks>

<verification>
- Campo serie_precos_ajustada presente em DadosMercado e CompanyData (default None).
- Zero chamada de rede nova em coletar_mercado/montar_empresa (só persiste o `ajustado` já baixado).
- 296 goldens verdes; serie_precos nominal e o gráfico/veredito da aba Analisar inalterados.
</verification>

<success_criteria>
- RET-01 tem sua fonte de dados (Adj Close 5a) disponível na engine sem custo de rede.
- Mudança aditiva pura; comportamento do método intacto; 296 goldens verdes.
</success_criteria>

<output>
Após completar, criar `.planning/phases/19-lentes-de-valuation-e-contexto-na-aba-analisar-graham-bazin-/19-02-SUMMARY.md`
</output>
