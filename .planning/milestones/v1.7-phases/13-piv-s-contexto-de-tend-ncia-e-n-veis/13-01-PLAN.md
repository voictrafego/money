---
phase: 13-piv-s-contexto-de-tend-ncia-e-n-veis
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/analista/core/indicators.py
  - config.yaml
  - tests/test_indicators.py
autonomous: true
requirements: [PIVOT-01]
must_haves:
  truths:
    - "A engine detecta pivôs (swing highs/lows) de forma determinística pelo fractal de Williams (D-01)"
    - "O rótulo de um pivô confirmado em t é imutável em t+1 (no-repaint para barras fechadas) — gate de truncação obrigatório (D-03)"
    - "A engine expõe o ATR (a partir do TR já calculado na cadeia do ADX) sem recalcular nem alterar adx_wilder (D-08)"
    - "Frame curto/None degrada graciosamente (pivôs vazios / ATR NaN) sem levantar exceção"
  artifacts:
    - path: "src/analista/core/indicators.py"
      provides: "dataclass Pivos + função _pivos (fractal de Williams) + helper atr_wilder + campos aditivos em SinaisTecnicos/Forca"
      contains: "def atr_wilder"
    - path: "config.yaml"
      provides: "params pivo_n e stop_atr_m no bloco indicadores"
      contains: "pivo_n"
    - path: "tests/test_indicators.py"
      provides: "golden no-repaint de truncação para pivôs + golden de ATR"
      contains: "pivo"
  key_links:
    - from: "indicators.calcular"
      to: "_pivos / atr_wilder"
      via: "montagem aditiva no construtor de SinaisTecnicos"
      pattern: "_pivos\\(|atr_wilder\\("
---

<objective>
Construir os DOIS primitivos que destravam toda a Fase 13: (1) detecção de pivôs (swing highs/lows) pelo **fractal de Williams** (PIVOT-01), determinística e no-repaint, e (2) exposição do **ATR** a partir do True Range já calculado na cadeia do ADX (insumo do stop ATR×m e do clustering de S/R). Ambos são puramente aditivos ao contrato `SinaisTecnicos`.

Purpose: Pivôs são o primitivo central que desbloqueia Dow, S/R, Fibonacci e padrões; o ATR é o insumo de volatilidade que alimenta stop (LEVEL-03) e zonas S/R (LEVEL-01). Sem estes dois, nenhum plano subsequente pode rodar.
Output: `Pivos` dataclass + `_pivos`, helper `atr_wilder`, novos params em `config.yaml`, novos goldens (incluindo o GATE de no-repaint por truncação — D-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-CONTEXT.md
@.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-PATTERNS.md

<interfaces>
<!-- Contratos reais extraídos de src/analista/core/indicators.py. Use-os direto; não explore o codebase. -->

Convenção de aditividade (NÃO reordenar/remover campos travados; novo campo entra opcional com default):
- `SinaisTecnicos` hoje: tendencia, canais, forca, momentum, close=None.
- `Canais` ganhou donchian_sup_55/donchian_inf_55 como `pd.Series = None` (precedente exato).

True Range já existe DENTRO de adx_wilder (NÃO recalcular, NÃO mudar a assinatura `(adx, pdi, ndi)`):
- prev_close = close.shift(1)
- tr = max( high-low, |high-prev_close|, |low-prev_close| ) por barra
- atr atual = `_wilder_rma_from(tr.to_numpy(float), length, start=1)`  (start=1: barra 0 é diff indefinido)

Helper de suavização reutilizável: `_wilder_rma_from(arr, length, start=0) -> np.ndarray` (RMA de Wilder SMA-seeded).

Entry-point: `calcular(ohlc, cfg) -> SinaisTecnicos`. Guard de borda já troca None/vazio/sem-colunas por frame vazio e ROTEIA (nunca exceção).

Idioma config: `ind = cfg["indicadores"]`; janelas vêm de subchaves (ex.: `ind["donchian"]`, `ind["adx_janela"]`).

Bloco indicadores existente em config.yaml (linhas ~97-114): sma_emas [20,50,200], donchian [20,55], bollinger{janela,sigma}, squeeze_janela 126, adx_janela 14, rsi_janela 14, rsi_faixas [30,70], macd [12,26,9], regressao_janela 90, base_temporal "diario".

Golden helpers em tests/test_indicators.py: `_cfg_ind()` carrega o config.yaml shipado; `_frame_ohlc(close, high, low, start)` monta DataFrame com colunas capitalizadas High/Low/Close + índice `pd.date_range(freq="B")`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pivôs fractal de Williams (Pivos dataclass + _pivos) — PIVOT-01</name>
  <files>src/analista/core/indicators.py, config.yaml, tests/test_indicators.py</files>
  <read_first>
    - src/analista/core/indicators.py (LER INTEIRO: `_canais` lines 185-252 para o idioma causal `.shift(1)` e degradação; `SinaisTecnicos`/`Canais` lines 44-91 para o padrão de campo aditivo opcional; `calcular` lines 406-425 para a montagem)
    - tests/test_indicators.py (LER INTEIRO: helpers `_cfg_ind`/`_frame_ohlc`; o padrão GATE de no-repaint por truncação `f(serie[:k]).iloc[-1] == f(serie)[k-1]` e o padrão "teeth" anti-falso-positivo)
    - config.yaml (linhas 96-114, bloco `indicadores:`)
    - .planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <behavior>
    - Topo (swing high) confirmado em t: `High[t]` estritamente maior que os `N` Highs de cada lado (t-N..t-1 e t+1..t+N). Idem fundo: `Low[t]` estritamente menor que os `N` Lows de cada lado.
    - GATE no-repaint (D-03): para uma série fixa, `_pivos(df[:k], cfg)` restrito às barras fechadas == `_pivos(df, cfg)` nas mesmas barras. Ou seja, um pivô confirmado nunca muda nem aparece/desaparece quando chegam barras à direita. Testar com k em pontos onde já há ≥N barras à direita.
    - Lag de confirmação: as `N` barras mais recentes NÃO podem ser marcadas como pivô confirmado (faltam barras fechadas à direita). Provar que pivot_high/pivot_low das últimas N barras é NaN.
    - "Teeth" anti-falso-positivo: numa série monotonicamente crescente não há topo interno confirmado; numa série em V há exatamente um fundo confirmado na ponta do V (com N barras de cada lado).
    - Degradação: frame com < 2N+1 barras → séries de pivô todas-NaN e `ultimo_topo`/`ultimo_fundo` = None, sem exceção. `n` no dataclass reflete o N usado.
  </behavior>
  <action>
    Adicionar dataclass `Pivos` em indicators.py (perto das outras famílias) com campos: `pivot_high: pd.Series` (preço do High nas barras de topo CONFIRMADO, NaN nas demais), `pivot_low: pd.Series` (preço do Low nas barras de fundo confirmado, NaN nas demais), `ultimo_topo: Number` (preço do topo confirmado mais recente ou None), `ultimo_fundo: Number` (idem fundo), `n: int`. Implementar `_pivos(ohlc, cfg) -> Pivos` lendo `ind = cfg["indicadores"]; N = ind["pivo_n"]`. Detecção fractal de Williams: para cada barra i com N vizinhos de cada lado já fechados, marcar topo se `High[i] > max(High[i-N..i-1])` e `High[i] > max(High[i+1..i+N])` (estritamente); fundo análogo com Low e min. As últimas N barras ficam SEMPRE NaN (não há N barras à direita) — esse é o no-repaint de D-03. NÃO usar `scipy.signal.find_peaks`/prominence (repaint na borda — proibido por D-01). Reusar o nível de docstring causal do Donchian (lines 188-193) explicando POR QUE `t+N` confirma e nunca muda. Montar `_pivos` dentro de `calcular` como campo aditivo OPCIONAL `pivos: Pivos = None` no fim de `SinaisTecnicos` (espelhando `close=None` line 91 e `donchian_sup_55=None` line 59). Computar pivôs sobre as colunas High/Low do frame recebido (preço estrutural). Em config.yaml acrescentar ao bloco `indicadores:` a chave `pivo_n: 2` com comentário "fractal de Williams: N barras fechadas de cada lado (swing diário, D-02; calibração empírica deferida)". Degradação graciosa idêntica ao resto do módulo (frame curto/vazio → NaN/None, nunca exceção).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_indicators.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_indicators.py` contém um teste de GATE no-repaint por truncação para `_pivos` no idioma `_pivos(df[:k], cfg) == _pivos(df, cfg)` nas barras fechadas (k em ≥2 pontos), e ele passa.
    - Existe teste provando que as últimas N barras nunca são pivô confirmado (NaN) e teste "teeth" (série monotônica sem topo interno; V com 1 fundo).
    - `import config.yaml` via `_cfg_ind()` expõe `indicadores.pivo_n == 2`.
    - `.venv/bin/python -m pytest tests/ -q` continua 100% verde (baseline atual: 202 passed) — nenhum golden existente quebra (mudança 100% aditiva).
    - `grep -n "find_peaks" src/analista/core/indicators.py` retorna vazio (D-01: scipy.find_peaks proibido para pivôs).
  </acceptance_criteria>
  <done>`Pivos` populado em `SinaisTecnicos.pivos`; gate no-repaint verde; suíte completa verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expor ATR a partir do TR do ADX (atr_wilder helper) — insumo de LEVEL-01/03</name>
  <files>src/analista/core/indicators.py, config.yaml, tests/test_indicators.py</files>
  <read_first>
    - src/analista/core/indicators.py (`adx_wilder` lines 258-304 — onde `tr` e `atr` já são calculados; `_wilder_rma_from` lines 97-111; `_forca` lines 330-353 e dataclass `Forca` lines 63-71 para o ponto de exposição aditiva)
    - tests/test_indicators.py (goldens de ADX existentes para reusar fixtures/idioma)
    - .planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-PATTERNS.md (§ "Níveis: stop ATR×m (D-08)")
  </read_first>
  <behavior>
    - `atr_wilder(ohlc, length)` devolve uma `pd.Series` indexada como `ohlc`, igual à variável `atr` interna de `adx_wilder` (1ª suavização de Wilder do TR, `start=1`); 1º valor válido no índice `length`.
    - Consistência: para o mesmo `length`, o ATR exposto bate (approx 1e-9) o `atr` que `adx_wilder` usa internamente (provar reconstruindo o TR no teste).
    - Degradação: frame com < length+1 barras → série todo-NaN; frame None/vazio via `calcular` → `Forca.atr` é série vazia/None sem exceção.
    - `adx_wilder` mantém EXATAMENTE a assinatura `(adx, pdi, ndi)` — goldens de ADX inalterados.
  </behavior>
  <action>
    Extrair o cálculo do True Range/ATR num helper público `atr_wilder(ohlc: pd.DataFrame, length: int) -> pd.Series` que reaproveita `_wilder_rma_from(tr.to_numpy(float), length, start=1)` exatamente como em `adx_wilder` (NÃO recalcular com outra fórmula — D-08 manda reusar o TR já existente). Refatorar `adx_wilder` para chamar esse helper internamente, preservando a assinatura de retorno `(adx, pdi, ndi)` intacta (não tocar `_forca` nem os goldens de ADX). Expor o ATR de forma aditiva: acrescentar campo opcional `atr: pd.Series = None` ao final de `Forca` e preenchê-lo em `_forca` chamando `atr_wilder(ohlc, ind["adx_janela"])`. Em config.yaml acrescentar `stop_atr_m: 1.5` ao bloco `indicadores:` com comentário "multiplicador do ATR no stop técnico (mais conservador entre swing e ATR×m — D-08; consumido na Fase 13 plano 04)". Proteger divisões com `np.errstate` onde aplicável (já é o idioma do módulo).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_indicators.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def atr_wilder" src/analista/core/indicators.py` retorna 1 ocorrência.
    - Teste prova `atr_wilder(df, 14)` ≈ o ATR interno de `adx_wilder` (reconstruído via TR) com `abs=1e-9` e que o 1º valor válido cai no índice 14.
    - Teste de degradação: frame curto → `atr_wilder` todo-NaN; `calcular(None, cfg).forca.atr` não levanta exceção.
    - `adx_wilder` continua retornando `(adx, pdi, ndi)` — `_cfg_ind()` e goldens de ADX inalterados.
    - `_cfg_ind()` expõe `indicadores.stop_atr_m == 1.5`.
    - `.venv/bin/python -m pytest tests/ -q` 100% verde (baseline 202 passed).
  </acceptance_criteria>
  <done>`atr_wilder` exposto e consistente com a cadeia do ADX; `Forca.atr` preenchido; suíte verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FrameOHLC (Fase 12) → indicators.calcular | Único insumo. Já validado/normalizado na Fase 12; sem rede, sem input direto de usuário nesta camada. Esta fase é engine matemática pura. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering/Denial | `_pivos` / `atr_wilder` com NaN ou frame < 2N+1 / < length+1 barras | mitigate | Degradação graciosa pelo guard de `calcular` (None/vazio/sem-colunas → frame vazio) + `min_periods` / checagem de comprimento → séries NaN / None, NUNCA exceção. |
| T-13-02 | Tampering | Repaint de borda mudaria o rótulo de um pivô já confirmado (corromperia níveis a jusante) | mitigate | Fractal de Williams com confirmação só em `t+N` (D-01/D-03); proibição de `scipy.find_peaks`; GATE de truncação no golden. |
| T-13-03 | Denial | Divisão por zero / inf na cadeia do ATR (ATR==0) | accept | `np.errstate` já cobre a cadeia do ADX; ATR==0 vira NaN, propagado como degradação. Sem PII, alvo de baixo valor. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -q` → 100% verde (baseline 202; nenhum golden existente alterado).
- GATE no-repaint de pivôs verde (D-03 — gate obrigatório da fase).
- `grep -n "find_peaks" src/analista/core/indicators.py` vazio.
</verification>

<success_criteria>
- `SinaisTecnicos.pivos` populado com `Pivos` (fractal de Williams, no-repaint, lag de N barras).
- `atr_wilder` exposto reusando o TR do ADX; `Forca.atr` preenchido aditivamente.
- `config.yaml` ganha `pivo_n` (2) e `stop_atr_m` (1.5); contrato 100% aditivo; suíte verde.
</success_criteria>

<output>
Após completar, criar `.planning/phases/13-piv-s-contexto-de-tend-ncia-e-n-veis/13-01-SUMMARY.md`.
</output>
