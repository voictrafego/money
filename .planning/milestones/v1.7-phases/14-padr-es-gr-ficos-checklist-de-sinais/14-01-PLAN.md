---
phase: 14-padr-es-gr-ficos-checklist-de-sinais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.yaml
  - src/analista/core/indicators.py
  - tests/test_indicators.py
autonomous: true
requirements: [PAT-01, SIG-01]

must_haves:
  truths:
    - "Os limiares geométricos dos padrões vivem em config.yaml no bloco novo `padroes:` (não hardcoded)"
    - "Existe uma flag de volume bidirecional (barra fechada > MM, agnóstica de direção) para confirmar rompimentos de BAIXA"
    - "O contrato SinaisTecnicos ganha campos aditivos padroes/checklist sem quebrar nenhum golden existente"
  artifacts:
    - path: "config.yaml"
      provides: "Bloco `padroes:` com os limiares A1–A7"
      contains: "padroes:"
    - path: "src/analista/core/indicators.py"
      provides: "Dataclasses PadraoGrafico/Padroes/Sinal/Checklist + campo volume_acima_mm + campos padroes/checklist em SinaisTecnicos"
      contains: "class PadraoGrafico"
  key_links:
    - from: "src/analista/core/indicators.py::_volume"
      to: "Volume.volume_acima_mm"
      via: "avaliação na barra fechada iloc[-2]"
      pattern: "volume_acima_mm"
---

<objective>
Estabelecer o CONTRATO aditivo da Fase 14 (interface-first): bloco de config `padroes:`, dataclasses
`PadraoGrafico`/`Padroes`/`Sinal`/`Checklist`, campos aditivos `padroes`/`checklist` em `SinaisTecnicos`,
e a flag de volume bidirecional `volume_acima_mm` em `Volume` (resolve Open Question 2 do RESEARCH).

Decisões resolvidas neste plano (Open Questions do RESEARCH):
- **OQ1 — hospedagem dos detectores:** ficam DENTRO de `core/indicators.py` (consistência com a Fase 13:
  pivôs/níveis/volume vivem ali; single-assembly-point em `calcular`). NÃO criar `core/padroes.py`.
- **OQ2 — flag de volume bidirecional:** adicionar campo aditivo `volume_acima_mm: bool` em `Volume`,
  avaliado na barra fechada `iloc[-2]`, AGNÓSTICO de direção. O detector decide a direção pela neckline.
  NÃO criar uma segunda MM de volume.
- **OQ3 — múltiplos padrões:** `Padroes.lista` é uma LISTA (ranqueamento fica para a Fase 15).

Purpose: Sem os contratos prontos, os detectores (planos 02/03) e o checklist (plano 04) fariam
"scavenger hunt" no código. Este plano entrega os contratos e a fonte de limiares de uma vez.
Output: config.yaml com bloco `padroes:`, 4 dataclasses novas, 2 campos novos em SinaisTecnicos,
1 campo novo em Volume, golden da flag bidirecional. ~252 testes existentes seguem verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-PATTERNS.md

<interfaces>
<!-- Contratos existentes que este plano ESTENDE de forma aditiva. Extraídos de indicators.py. -->

Volume (indicators.py 118–123) — adicionar campo aditivo:
```python
@dataclass
class Volume:
    volume_mm: pd.Series = None
    rompimento_com_volume: bool = False
    # NOVO: volume_acima_mm: bool = False   # barra fechada > MM, agnóstico de direção
```

SinaisTecnicos (indicators.py 134–154) — molde dos campos aditivos pivos/niveis/volume:
```python
    pivos: "Pivos" = None
    contexto: "ContextoTendencia" = None
    niveis: "Niveis" = None
    volume: "Volume" = None
    # NOVO: padroes: "Padroes" = None ; checklist: "Checklist" = None
```

Niveis.pivos_ancora (indicators.py 111) = molde de auditabilidade para PadraoGrafico.pivos_envolvidos.
config.yaml bloco `indicadores:` (96–118) = molde de densidade de comentários do bloco `padroes:`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloco `padroes:` no config.yaml + dataclasses do contrato</name>
  <files>config.yaml, src/analista/core/indicators.py</files>
  <read_first>
    - config.yaml (bloco `indicadores:` linhas 96–118 — molde de comentário denso e densidade)
    - src/analista/core/indicators.py (dataclasses Pivos 86–93, Niveis 95–115, Volume 118–123,
      ContextoTendencia 126–131, SinaisTecnicos 134–154 — molde aditivo)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md (§Code Examples 322–349)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-PATTERNS.md (§dataclasses aditivas 34–85)
  </read_first>
  <action>
    No `config.yaml`, ADICIONAR um bloco de topo NOVO `padroes:` (irmão de `indicadores:`, NUNCA tocar
    nenhuma linha do bloco `indicadores:` — Pitfall 5, anti-rebaseline) com as chaves e valores iniciais
    A1–A7 do RESEARCH: `lookback_pivos: 8`, `price_tolerance_pct: 0.03`, `shoulder_symmetry_pct: 0.05`,
    `head_min_prominence_pct: 0.02`, `min_pattern_height_pct: 0.03`, `exigir_volume_confirma: true`.
    Comentar o "porquê" de cada limiar em PT-BR (estes são ASSUMED — calibração multi-ticker é o plano 05,
    Success Criterion 3). O param temporal `max_largura_barras` fica DEFERIDO para a calibração (Open Q4) —
    não adicionar agora.

    No `core/indicators.py`, ADICIONAR (aditivo, sem reordenar/remover nada existente) os dataclasses,
    junto dos demais no topo do módulo, espelhando Niveis/ContextoTendencia:
    - `PadraoGrafico` com campos: `tipo: str` (chaves estáveis D-01: "duplo_topo"|"duplo_fundo"|"oco"|
      "oco_invertido"), `estado: str` ("em_formacao"|"confirmado"), `neckline: float`, `alvo: float`,
      `altura: float`, `pivos_envolvidos: dict` (espelha Niveis.pivos_ancora — {ts: preco} dos pivôs âncora).
    - `Padroes` com `lista: list = field(default_factory=list)` (`[]` = nenhum padrão, degradação graciosa).
    - `Sinal` com `nome: str`, `ativo: bool`, `detalhe: str` (rótulo neutro já existente — NUNCA copy natural).
    - `Checklist` com `sinais: list = field(default_factory=list)`.
    Em `SinaisTecnicos`, ADICIONAR ao FINAL os campos `padroes: "Padroes" = None` e
    `checklist: "Checklist" = None`, copiando o idioma/comentário de `pivos`/`niveis`/`volume` (143–154).
    Todos os rótulos são chaves estáveis/neutras (firewall de copy D-01; a renderização é da Fase 16).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -c "import yaml,ast; cfg=yaml.safe_load(open('config.yaml')); assert 'padroes' in cfg and set(cfg['padroes'])>= {'lookback_pivos','price_tolerance_pct','shoulder_symmetry_pct','head_min_prominence_pct','min_pattern_height_pct','exigir_volume_confirma'}; src=open('src/analista/core/indicators.py').read(); [m:=__import__('re')]; assert all(s in src for s in ('class PadraoGrafico','class Padroes','class Sinal','class Checklist','padroes:','checklist:')); print('OK')"</automated>
  </verify>
  <done>config.yaml tem bloco `padroes:` com as 6 chaves; indicators.py tem os 4 dataclasses + 2 campos em SinaisTecnicos; bloco `indicadores:` intocado.</done>
  <acceptance_criteria>
    - `yaml.safe_load(config.yaml)["padroes"]` contém as 6 chaves A1–A7 com os valores do RESEARCH.
    - `from analista.core import indicators` importa sem erro e expõe `indicators.PadraoGrafico`,
      `indicators.Padroes`, `indicators.Sinal`, `indicators.Checklist`.
    - `dataclasses.fields(indicators.SinaisTecnicos)` inclui `padroes` e `checklist`, ambos default None,
      e a ordem/defaults dos campos pré-existentes é idêntica (nenhum golden de contrato quebra).
    - `git diff config.yaml` não toca nenhuma linha sob `indicadores:`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flag de volume bidirecional `volume_acima_mm` em `_volume`</name>
  <files>src/analista/core/indicators.py, tests/test_indicators.py</files>
  <behavior>
    - Frame OHLCV com volume da barra fechada (iloc[-2]) ACIMA da volume_mm da barra fechada → `volume_acima_mm == True`, independentemente de direção do preço.
    - Frame com volume da barra fechada ABAIXO da MM → `volume_acima_mm == False`.
    - Frame curto (volume_mm todo-NaN na barra fechada) ou sem coluna Volume → `volume_acima_mm == False` (degradação graciosa, sem exceção).
    - `rompimento_com_volume` (campo existente, só-alta) permanece com o MESMO comportamento dos goldens atuais.
  </behavior>
  <read_first>
    - src/analista/core/indicators.py (`_volume` 829–861 — idioma iloc[-2], volume_mm, guard pd.isna)
    - src/analista/core/indicators.py (dataclass Volume 118–123)
    - tests/test_indicators.py (`_frame_ohlcv` 861–870, `_cfg_ind` 15–19, `test_volume_*` próximos a 900–917)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-PATTERNS.md (§campo aditivo Volume 69–76)
  </read_first>
  <action>
    Em `_volume`, calcular um booleano adicional `volume_acima_mm`: na barra FECHADA (`iloc[-2]`),
    `vol_f > vmm_f` (volume fechado acima da MM de volume fechada), protegido pelo MESMO guard
    `pd.isna(...)` já usado para `rompimento_com_volume` — é a parte AGNÓSTICA de direção da confirmação
    (Open Q2). Reusar `vmm_f`/`vol_f` já computados; NÃO criar segunda MM. Retornar
    `Volume(volume_mm=..., rompimento_com_volume=flag, volume_acima_mm=flag_vol)`. Manter `rompimento_com_volume`
    (só-alta, Donchian superior) EXATAMENTE como está — é o que os goldens existentes pinam.
    Em `tests/test_indicators.py`, ADICIONAR um golden `test_volume_acima_mm_bidirecional` reusando
    `_frame_ohlcv`: um frame onde a barra fechada tem volume alto (acima da MM) e o preço CAI (quebra p/
    baixo) → `volume_acima_mm is True` mas `rompimento_com_volume is False` (prova que a flag nova é
    direção-agnóstica e serve ao lado de BAIXA dos padrões); e um caso de volume baixo → ambas False.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -m pytest tests/test_indicators.py -k "volume" -q</automated>
  </verify>
  <done>`_volume` popula `volume_acima_mm` na barra fechada; novo golden verde; goldens de volume existentes verdes.</done>
  <acceptance_criteria>
    - `pytest tests/test_indicators.py -k volume` sai 0.
    - O novo teste prova `volume_acima_mm is True` num rompimento de BAIXA com volume alto, com
      `rompimento_com_volume is False` no mesmo frame.
    - Frame curto/sem Volume → `volume_acima_mm is False` sem exceção.
    - `pytest tests/test_indicators.py -q` (arquivo inteiro) continua verde.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline OHLC (Fase 12) → engine | Série já saneada; a engine apenas degrada graciosamente, não confia em forma |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Denial of Service | `_volume` com frame curto/NaN na barra fechada | mitigate | Guard `pd.isna(...)` já existente cobre `volume_acima_mm`; retorna False sem levantar |
| T-14-02 | Tampering | rebaseline acidental de campo/contrato existente | mitigate | Campos 100% aditivos com default; bloco `padroes:` novo; rodar suíte inteira (~252 testes) |
</threat_model>

<verification>
- `pytest tests/test_indicators.py -q` verde (arquivo inteiro).
- `pytest -q` (suíte inteira, ~252 testes) verde — nenhum golden fundamentalista/técnico quebrado.
- `import analista.core.indicators` expõe os 4 dataclasses novos.
</verification>

<success_criteria>
- Bloco `padroes:` no config.yaml com os 6 limiares (A1–A7); `indicadores:` intocado.
- Dataclasses PadraoGrafico/Padroes/Sinal/Checklist criados; SinaisTecnicos com padroes/checklist=None.
- Volume.volume_acima_mm computado na barra fechada, direção-agnóstico; golden verde.
- Suíte inteira verde.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-01-SUMMARY.md`.
</output>
