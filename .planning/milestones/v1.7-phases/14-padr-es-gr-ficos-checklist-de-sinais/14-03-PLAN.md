---
phase: 14-padr-es-gr-ficos-checklist-de-sinais
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/analista/core/indicators.py
  - tests/test_indicators.py
autonomous: true
requirements: [PAT-01]

must_haves:
  truths:
    - "A engine detecta OCO (ombro-cabeça-ombro) e OCO invertido sobre 5 pivôs intercalados"
    - "A cabeça excede ambos os ombros por head_min_prominence_pct e os ombros são ~simétricos (shoulder_symmetry_pct)"
    - "A neckline da OCO é uma reta pelos dois fundos, computada por POSIÇÃO inteira da barra (não timestamp em ns)"
    - "O rótulo de uma OCO em t é imutável em t+1 para barras fechadas (no-repaint)"
  artifacts:
    - path: "src/analista/core/indicators.py"
      provides: "_padroes estendida com oco/oco_invertido (neckline inclinada por regressão de 2 pivôs)"
      contains: "oco"
    - path: "tests/test_indicators.py"
      provides: "Goldens de OCO/OCO invertido + no-repaint da OCO"
      contains: "def test_padroes_oco"
  key_links:
    - from: "_padroes (OCO neckline)"
      to: "posição inteira da barra (0..n-1)"
      via: "regressão linear dos 2 fundos por índice posicional, não timestamp"
      pattern: "oco"
---

<objective>
Estender `_padroes` (do plano 02) com **OCO (ombro-cabeça-ombro)** e **OCO invertido**: sequência de 5
pivôs intercalados, cabeça proeminente, ombros simétricos, e **neckline INCLINADA** por regressão dos
dois fundos usando **posição inteira da barra** como eixo-x (Pitfall 3 — timestamp em ns explode a reta).
Inclui o GATE de no-repaint da OCO. É a parte genuinamente nova (sem analog exato no repo).

Purpose: Completa PAT-01 (duplo topo/fundo no plano 02 + OCO aqui). OCO é o padrão mais sensível à
simetria — limiares conservadores evitam pareidolia.
Output: `_padroes` cobre os 4 tipos; goldens de OCO/invertido + no-repaint da OCO; suíte inteira verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-PATTERNS.md
@.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-02-SUMMARY.md

<interfaces>
<!-- _padroes e dataclasses já existem (planos 01/02); este plano ESTENDE _padroes. -->

cfg["padroes"] relevantes p/ OCO: `shoulder_symmetry_pct` (0.05), `head_min_prominence_pct` (0.02),
`min_pattern_height_pct` (0.03), `lookback_pivos` (8), `exigir_volume_confirma` (true).

Neckline inclinada (RESEARCH §Pattern 2 195–215) — eixo-x = posição inteira (0..n-1), NÃO timestamp:
```python
m = (f2 - f1) / (pos_f2 - pos_f1)                      # inclinação por índice posicional
neckline_no_rompimento = f1 + m * (pos_barra_fechada - pos_f1)
```
PadraoGrafico.pivos_envolvidos guarda os 5 pivôs ({ts: preco}) p/ auditabilidade (espelha pivos_ancora).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender `_padroes` com OCO e OCO invertido (neckline inclinada)</name>
  <files>src/analista/core/indicators.py</files>
  <read_first>
    - src/analista/core/indicators.py (`_padroes` — função criada no plano 02; `_niveis_fib` 699–765 —
      molde de uso de pivôs âncora múltiplos + pivos_ancora; `_canais`/`_niveis_stop_rr` — guards np.errstate)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md (§Pattern 2 189–215, Pitfall 3 299–304)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-PATTERNS.md (§No Analog Found 319–322, §neckline inclinada 138)
    - config.yaml (bloco `padroes:`)
  </read_first>
  <action>
    Estender `_padroes` (não criar função paralela; adicionar a detecção e ANEXAR à mesma `lista`).
    Reusar `topos = pivos.pivot_high.dropna()` e `fundos = pivos.pivot_low.dropna()`.

    **OCO:** exige `len(topos) >= 3` e `len(fundos) >= 2`. Considerar os 3 topos mais recentes em ordem
    temporal (LS, cabeça, RS) e os 2 fundos ENTRE eles (intercalados). A cabeça é o do MEIO e deve exceder
    AMBOS os ombros: `prom_e = (cabeca - ls)/cabeca`, `prom_d = (cabeca - rs)/cabeca`, ambos
    `>= head_min_prominence_pct` (guard np.errstate). Simetria de ombros
    `sim_ombros = abs(ls - rs)/((ls+rs)/2) <= shoulder_symmetry_pct`. **Neckline inclinada** pelos 2 fundos
    `(f1 em pos_f1, f2 em pos_f2)` usando POSIÇÃO INTEIRA da barra como eixo-x (índice 0..n-1 do `nominal`,
    via `nominal.index.get_loc(ts)`), NUNCA timestamp em ns: `m = (f2-f1)/(pos_f2-pos_f1)` (guard se
    `pos_f2==pos_f1`); extrapolar `neckline_rompimento = f1 + m*(pos_fechada - pos_f1)` na barra fechada
    `iloc[-2]`. `altura = cabeca - (neckline na posição da cabeça)`; casar só se
    `altura/cabeca >= min_pattern_height_pct`. Estado: `confirmado` se `close_f < neckline_rompimento` E
    (volume.volume_acima_mm se `exigir_volume_confirma`), senão `em_formacao`. `alvo = neckline_rompimento -
    altura` (p/ BAIXO). Anexar `PadraoGrafico(tipo="oco", estado, neckline=neckline_rompimento, alvo, altura,
    pivos_envolvidos={5 pivôs ts:preco})`.

    **OCO invertido (espelho):** trocar topos↔fundos; cabeça = fundo mais BAIXO entre dois ombros-fundo;
    neckline pelos 2 topos intermediários; `close_f > neckline_rompimento` confirma; `alvo = neckline +
    altura` (p/ CIMA); `tipo="oco_invertido"`. Degradação graciosa preservada (poucos pivôs → não anexa nada).
    Manter chaves estáveis (D-01). NÃO recalcular volume/MM — consome `volume.volume_acima_mm`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -c "from analista.core import indicators; src=open(indicators.__file__).read(); assert 'oco' in src and 'oco_invertido' in src; print('OK')"</automated>
  </verify>
  <done>`_padroes` detecta oco e oco_invertido com neckline inclinada por posição inteira; degrada graciosamente.</done>
  <acceptance_criteria>
    - `_padroes` retorna padrões `tipo="oco"`/`"oco_invertido"` quando a geometria casa, e nada quando
      faltam pivôs ou a simetria/proeminência falham — sem levantar.
    - A neckline usa posição inteira da barra (`get_loc`), nunca timestamp em ns.
    - Os 5 pivôs âncora ficam em `pivos_envolvidos`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Goldens de OCO/OCO invertido + GATE no-repaint da OCO (obrigatório)</name>
  <files>tests/test_indicators.py</files>
  <read_first>
    - tests/test_indicators.py (`_pivos_ts` 949–964 — injeta 5 pivôs em posições/preços conhecidos;
      `_frame_ohlcv` 861–870; `test_pivos_no_repaint_truncacao` 433–455 — molde do gate)
    - .planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-RESEARCH.md (§Pattern 2, §Code Examples 351–377)
  </read_first>
  <action>
    Adicionar goldens em `tests/test_indicators.py`:
    - `test_padroes_oco_geometria`: `_pivos_ts(topos={i_ls:p, i_cabeca:P_maior, i_rs:p}, fundos={i_f1:n,
      i_f2:n})` com cabeça proeminente e ombros simétricos; `nominal` com Close[-2] ainda acima da
      neckline → um `PadraoGrafico(tipo="oco", estado="em_formacao")`, com `neckline` ≈ extrapolação dos
      fundos na barra fechada e `alvo == pytest.approx(neckline - altura)`.
    - `test_padroes_oco_confirmado`: Close[-2] abaixo da neckline + volume alto → estado="confirmado".
    - `test_padroes_oco_invertido`: espelho (cabeça-fundo proeminente; rompe p/ cima; alvo = neckline + altura).
    - `test_padroes_oco_ombros_assimetricos_nao_casa`: ombros com diferença acima de `shoulder_symmetry_pct`
      → lista sem OCO (anti-falso-positivo).
    - `test_padroes_oco_neckline_inclinada_usa_posicao`: dois fundos em níveis DIFERENTES (neckline com
      inclinação não-nula) → assert que a neckline extrapolada na barra fechada bate com a reta por posição
      inteira (prova Pitfall 3 — não usa timestamp em ns; alvo finito e plausível, não explosivo).
    - `test_padroes_no_repaint_truncacao_oco` (GATE OBRIGATÓRIO): frame OHLCV sintético com OCO; comparar
      estado/neckline/alvo do padrão âncora em barras já fechadas entre `_padroes(_pivos(df[:k]), ...)` e
      `_padroes(_pivos(df), ...)` — idêntico para barras fechadas.
    Limiares via `_cfg_ind()["padroes"]`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -m pytest tests/test_indicators.py -k "padroes_oco or no_repaint_truncacao_oco" -q</automated>
  </verify>
  <done>Goldens de OCO/invertido verdes; neckline inclinada por posição provada; gate no-repaint da OCO verde; suíte inteira verde.</done>
  <acceptance_criteria>
    - `pytest tests/test_indicators.py -k "padroes_oco or no_repaint_truncacao_oco"` sai 0.
    - O teste de neckline inclinada prova alvo finito/plausível (não explosivo — Pitfall 3 mitigado).
    - O teste de ombros assimétricos prova ausência de falso positivo.
    - `pytest -q` (suíte inteira) verde.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pivôs (Fase 13) → `_padroes` OCO | 5 pivôs já confirmados; o detector só compõe a reta da neckline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Tampering (Integrity) | neckline inclinada via timestamp em ns → coeficiente absurdo | mitigate | Eixo-x = posição inteira da barra (`get_loc`); guard `pos_f2==pos_f1`; teste de neckline inclinada |
| T-14-07 | Denial of Service | sequência de pivôs insuficiente / NaN | mitigate | Checagens de `len(...)` e guards; não anexa, retorna lista parcial sem levantar |
| T-14-08 | Repaint (Integrity) | OCO confirmando na barra viva | mitigate | Confirmação `iloc[-2]`; gate de no-repaint da OCO por truncação |
</threat_model>

<verification>
- `pytest tests/test_indicators.py -q` verde.
- `pytest -q` (suíte inteira) verde.
- Gate de no-repaint da OCO presente e verde.
</verification>

<success_criteria>
- `_padroes` detecta os 4 tipos (duplo_topo/fundo + oco/oco_invertido).
- Neckline da OCO por posição inteira da barra; alvo measured-move finito.
- No-repaint da OCO provado.
- Suíte inteira verde.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/14-padr-es-gr-ficos-checklist-de-sinais/14-03-SUMMARY.md`.
</output>
