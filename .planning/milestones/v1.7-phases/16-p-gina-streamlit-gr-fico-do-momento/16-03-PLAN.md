---
phase: 16-p-gina-streamlit-gr-fico-do-momento
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified: []
autonomous: false
requirements: [SWING-01, SWING-02, CHART-01]
must_haves:
  truths:
    - "Os 283 testes golden seguem verdes (engine fundamentalista, aba Analisar e funções puras de grafico.py intactas)"
    - "Um humano abre o app no navegador, navega o 4º menu e confirma candlestick + overlays + subpainéis + níveis + veredito sem recomendação"
    - "As 3 abas existentes (Analisar, Garimpar, Ranking) seguem funcionando sem regressão"
  artifacts:
    - path: ".planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-03-SUMMARY.md"
      provides: "registro da verificação humana e do resultado dos goldens"
  key_links:
    - from: "verificação humana"
      to: "4º menu Swing trade"
      via: "navegador local (streamlit run)"
      pattern: "n/a"
---

<objective>
Fecha a fase com a verificação de aceite: rodar a suíte completa de goldens (283 verdes) e uma
verificação humana no navegador do 4º menu, confirmando que a página swing renderiza tudo
(candlestick + overlays + subpainéis + níveis + veredito), que a copy nunca recomenda, e que as
3 abas existentes seguem sem regressão.

Purpose: É o critério final do ROADMAP (Success Criterion 5) — goldens verdes + verificação humana
no navegador aprovando o 4º menu sem regressão. Atende o gate de aceite de SWING-01, SWING-02 e CHART-01.
Output: goldens executados + `16-03-SUMMARY.md` com o registro da aprovação humana.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-01-PLAN.md
@.planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rodar goldens completos + smoke de import e subir o app local</name>
  <files>n/a (verificação — nenhum arquivo modificado)</files>
  <read_first>
    - app.py bloco swing completo (resultado dos planos 16-01 e 16-02) — confirmar que a cadeia está coesa
    - .planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-RESEARCH.md §"Environment Availability" (streamlit/plotly/pandas instalados no .venv)
  </read_first>
  <action>
    Executar a suíte completa `.venv/bin/python -m pytest -q` e confirmar 283 testes verdes (nenhuma
    regressão na engine fundamentalista nem nas funções puras de grafico.py — esta fase só editou app.py).
    Rodar o smoke de sintaxe `python -c "import ast; ast.parse(open('app.py').read())"`.
    Subir o app localmente com `.venv/bin/python -m streamlit run app.py` (em background) para a verificação
    humana do Task 2, e reportar a URL local (http://localhost:8501). Se a porta 8501 estiver ocupada,
    usar `--server.port` alternativo e reportar a URL real.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && .venv/bin/python -m pytest -q && python -c "import ast; ast.parse(open('app.py').read()); print('syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` sai 0 com 283 testes verdes (sem regressão nos goldens)
    - `python -c "import ast; ast.parse(open('app.py').read())"` sai 0
    - App Streamlit sobe sem traceback e a URL local é reportada para o Task 2
  </acceptance_criteria>
  <done>283 goldens verdes, sintaxe válida e app no ar localmente para a verificação humana.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    O 4º menu "📈 Swing trade (análise técnica)" agora renderiza, read-only: candlestick "do momento"
    com overlays liga/desliga (MMs/S-R/Fibonacci/níveis do setup ON por padrão; Bollinger/Donchian/padrões OFF),
    subpainéis RSI/MACD/ADX, barra viva marcada + selo de atraso, e abaixo o card de veredito
    (grade+score, decomposição por família, checklist ✓/✗, tabela "Referências de estudo (não são ordens)") com disclaimer.
  </what-built>
  <how-to-verify>
    Abra a URL local reportada no Task 1 (ex.: http://localhost:8501) e verifique:
    1. Selecione "📈 Swing trade (análise técnica)" na sidebar. Digite um ticker (ex.: TAEE11) e mantenha "Diário".
    2. O gráfico aparece como CANDLESTICK (não linha), com médias móveis sobre o preço e subpainéis RSI/MACD/ADX abaixo. As zonas de suporte/resistência aparecem como FAIXAS (não linhas pontuais).
    3. Abra o expander "⚙️ Overlays": ligue/desligue Bollinger, Donchian e "Padrões". Confirme que o gráfico atualiza no mesmo clique e que ligar padrões anota neckline/rótulo "em formação/confirmado".
    4. Abaixo do gráfico: confira grade + "Pontuação de confluência técnica", a decomposição por família, o checklist com ✓/✗, e a tabela "Referências de estudo (não são ordens)" com entrada/stop/alvo/R:R. NÃO deve haver nenhuma palavra como "compre", "venda", "recomendo" — só linguagem de estudo. Confirme o disclaimer.
    5. Troque o timeframe para 5m e clique "Atualizar": o selo "~15min atraso / última barra HH:MM" deve aparecer; um timeframe sem histórico deve degradar com aviso, sem quebrar a página.
    6. REGRESSÃO: volte para "🔎 Analisar uma ação", "⛏️ Garimpar carteira (BSD)" e "📊 Ranking por múltiplos" e confirme que as 3 abas seguem funcionando normalmente (gráfico/veredito da Analisar intactos; toggles da Analisar independentes dos da swing).
  </how-to-verify>
  <acceptance_criteria>
    - 4º menu mostra candlestick + overlays + subpainéis + barra viva + selo de atraso (CHART-01)
    - Níveis/veredito sem nenhuma copy imperativa; label "Referências de estudo (não são ordens)" + disclaimer presentes (SWING-02)
    - As 3 abas existentes seguem sem regressão; os toggles da swing não afetam os da Analisar (SWING-01 / isolamento `tec_estado_swing`)
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário (humano verificador) → app local | sessão local de verificação; sem exposição externa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Repudiation | aceite da fronteira "exibe, nunca recomenda" | mitigate | Verificação humana explícita da ausência de copy imperativa no 4º menu (gate de aceite SWING-02), complementando o firewall de testes. Sem severidade técnica. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 283 verdes.
- Verificação humana no navegador aprovada (resume-signal "aprovado").
</verification>

<success_criteria>
- 283 goldens verdes (sem regressão na engine fundamentalista, aba Analisar e grafico.py).
- 4º menu aprovado no navegador: candlestick + overlays + subpainéis + níveis + veredito sem recomendação.
- 3 abas existentes sem regressão; estado dos toggles da swing isolado.
</success_criteria>

<output>
After completion, create `.planning/phases/16-p-gina-streamlit-gr-fico-do-momento/16-03-SUMMARY.md`
</output>
