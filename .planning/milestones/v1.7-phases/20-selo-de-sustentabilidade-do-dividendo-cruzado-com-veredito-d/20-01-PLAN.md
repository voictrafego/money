---
phase: 20-selo-de-sustentabilidade-do-dividendo-cruzado-com-veredito-d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.yaml
  - src/analista/core/screening.py
  - src/analista/report/selo.py
  - src/analista/report/report.py
  - tests/test_selo.py
autonomous: true
requirements: [SELO-01, SELO-02]
must_haves:
  truths:
    - "A engine deriva uma cor de selo (verde/azul/amarelo/vermelho) a partir do score BSD (0-100)"
    - "A engine cruza a cor do selo (qualidade Alta/Baixa) com o veredito de preço do DDM num rótulo de quadrante (JOIA/VALUE TRAP/etc.)"
    - "Quando o veredito é VERIFICAR, o selo marca alerta 'verificar dados' e NÃO atribui faixa de preço"
    - "analisar_acao popula a.selo para 1 ticker sem tocar a rede e sem quebrar os 307 goldens"
    - "Os cortes de cor vivem em config.yaml (tunáveis), não hardcoded espalhados"
  artifacts:
    - path: "src/analista/report/selo.py"
      provides: "Selo dataclass + cor_do_bsd + faixa_do_veredito + montar_selo (lógica pura, firewall vs report.py)"
      contains: "def montar_selo"
      min_lines: 60
    - path: "src/analista/core/screening.py"
      provides: "bsd_empresa(c, cfg) — BSD absoluto de 1 empresa reusando bsd_ranking"
      contains: "def bsd_empresa"
    - path: "config.yaml"
      provides: "bloco selo: cortes de cor do BSD"
      contains: "selo:"
    - path: "src/analista/report/report.py"
      provides: "campo selo em AnaliseAcao + população em analisar_acao"
      contains: "selo"
    - path: "tests/test_selo.py"
      provides: "goldens dos cortes de cor, matriz de quadrante, overlay VERIFICAR e degradação"
      contains: "def test_"
  key_links:
    - from: "src/analista/report/report.py:analisar_acao"
      to: "src/analista/core/screening.py:bsd_empresa"
      via: "chamada para obter o BSD de 1 empresa"
      pattern: "bsd_empresa"
    - from: "src/analista/report/report.py:analisar_acao"
      to: "src/analista/report/selo.py:montar_selo"
      via: "cruza BSD + veredito e atribui a a.selo"
      pattern: "montar_selo"
---

<objective>
Camada de DERIVAÇÃO na engine que produz o Selo de Sustentabilidade (SELO-01) e o cruza com o veredito de preço do DDM num rótulo de quadrante (SELO-02). Reusa números que a engine JÁ calcula e testa: o `bsd` (0-100) de `core/screening.py` e as strings de veredito (SUBAVALIADA / NO INTERVALO / SOBREAVALIADA / VERIFICAR) de `report/report.py`. Nenhum método novo, nenhum recálculo fundamentalista.

Purpose: Concentrar TODA a lógica (score→cor, veredito→faixa, matriz→rótulo) na engine, de forma golden-testável e ANTES de tocar `app.py`. A Fase 20 só desenha (Plan 02) o que esta camada derivar. Espelha o firewall do `SetupSwing`.
Output: `report/selo.py` (novo), `bsd_empresa` em `screening.py`, bloco `selo:` em `config.yaml`, campo `selo` populado em `AnaliseAcao`, `tests/test_selo.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-selo-de-sustentabilidade-do-dividendo-cruzado-com-veredito-d/20-CONTEXT.md

<interfaces>
<!-- Contratos que o executor consome. Extraídos do código. Use diretamente — sem exploração. -->

De src/analista/core/screening.py:
- `bsd_ranking(empresas: List[CompanyData], pesos=None, anos_media=3, winsor=0.10) -> List[Dict]`
  Cada dict: {"ticker", "nome", "setor", "bsd" (float 0-100), "acima_de_80", "indicadores", "fatores_faltantes", "n_fatores_faltantes"}.
  IMPORTANTE (reprodutibilidade): a padronização é ABSOLUTA contra `REFERENCIA_BSD` (bandas fixas), NÃO min-max do lote — logo `bsd_ranking([c])[0]["bsd"]` é IDÊNTICO ao BSD que `c` teria dentro de uma lista maior. É correto e estável chamar com uma lista de 1.
- `PESOS_BSD_PADRAO: Dict[str,int]` (payout 30, ...).

De src/analista/report/report.py:
- `@dataclass AnaliseAcao` — já tem: ticker, nome, setor, preco_atual, veredito: str, vmin, vmax, ... (ver linhas 21-47).
- `def analisar_acao(c: CompanyData, cfg: dict) -> AnaliseAcao` — orquestrador; a string `a.veredito` é montada ~linhas 195-204 e começa com "SUBAVALIADA"/"NO INTERVALO"/"SOBREAVALIADA"/"VERIFICAR" (ou vazia quando preço/DDM indisponível).

De config.yaml (bloco screening.bsd):
- `screening.bsd.pesos` (dict), `screening.bsd.anos_media` (3), `screening.bsd.winsor` (0.10).

Padrão de config nos testes (test_report.py L24-27):
```
raiz = Path(__file__).resolve().parents[1]
with open(raiz / "config.yaml", encoding="utf-8") as f:
    cfg = yaml.safe_load(f)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloco selo: no config.yaml + helper bsd_empresa em screening.py</name>
  <files>config.yaml, src/analista/core/screening.py</files>
  <read_first>
    - config.yaml (ler o bloco `screening:` inteiro, ~onde ficam custom/graham/bsd, para posicionar o novo bloco `selo:` no mesmo nível de indentação de topo)
    - src/analista/core/screening.py (funções bsd_ranking L335-381 e PESOS_BSD_PADRAO L175-186)
  </read_first>
  <action>
    Em config.yaml, adicionar um bloco de topo `selo:` (irmão de `screening:`) com os cortes de cor do BSD, tunáveis (D1). Chaves: `cor:` com `verde_min: 70`, `azul_min: 55`, `amarelo_min: 40` (abaixo de amarelo_min = vermelho). Comentar que são os valores INICIAIS e o único ponto de ajuste dos cortes; a matriz de rótulos do quadrante fica no código (copy estável, não tunável).

    Em screening.py, adicionar `def bsd_empresa(c: CompanyData, cfg: Optional[dict] = None) -> Optional[float]` que retorna o BSD (0-100) de UMA empresa reusando `bsd_ranking`. Ler pesos/anos_media/winsor de `cfg["screening"]["bsd"]` quando cfg vier (mesmos parâmetros que o Garimpo usa — consistência entre menus), com defaults `PESOS_BSD_PADRAO`/3/0.10 quando cfg None. Implementar como `bsd_ranking([c], pesos=..., anos_media=..., winsor=...)[0]["bsd"]`. Degradar para None se a lista vier vazia ou o bsd for None (never-raise para a UI). Docstring: cita que o BSD é reproduzível (padronização absoluta) — o número de 1 empresa == o dela num lote maior.
  </action>
  <verify>
    <automated>./.venv/bin/python -c "import yaml; from analista.core import screening as s; from analista.core.fundamentals import CompanyData; cfg=yaml.safe_load(open('config.yaml')); assert cfg['selo']['cor']['verde_min']==70 and cfg['selo']['cor']['azul_min']==55 and cfg['selo']['cor']['amarelo_min']==40; assert callable(s.bsd_empresa); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `config.yaml` carrega com `yaml.safe_load` e expõe `selo.cor.{verde_min,azul_min,amarelo_min}` == 70/55/40.
    - `screening.bsd_empresa` existe, aceita `(c, cfg=None)`, e para uma empresa com dados válidos retorna um float em [0,100] igual a `bsd_ranking([c])[0]["bsd"]`.
    - `bsd_empresa` retorna None (não levanta) quando os dados não permitem BSD.
    - Nenhuma assinatura/comportamento existente de `bsd_ranking` foi alterado.
  </acceptance_criteria>
  <done>Cortes de cor configuráveis e BSD de 1 empresa disponível como função reusável e reprodutível.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: report/selo.py (Selo + cor + faixa + quadrante) e wiring em report.py</name>
  <files>src/analista/report/selo.py, src/analista/report/report.py</files>
  <read_first>
    - src/analista/report/setup.py (L1-60 — espelhar o cabeçalho de FIREWALL/COPY NEUTRA e o padrão de dataclass aditiva com defaults)
    - src/analista/report/report.py (L21-53 dataclass AnaliseAcao + assinatura de analisar_acao; L195-204 onde a.veredito é montada)
    - src/analista/core/screening.py (a função bsd_empresa criada na Task 1)
    - .planning/phases/20-selo-de-sustentabilidade-do-dividendo-cruzado-com-veredito-d/20-CONTEXT.md (D2 — matriz de rótulos e regra do VERIFICAR)
  </read_first>
  <behavior>
    - cor_do_bsd(90) == "verde"; cor_do_bsd(70) == "verde" (>=70); cor_do_bsd(60) == "azul" (55-70); cor_do_bsd(55) == "azul"; cor_do_bsd(45) == "amarelo" (40-55); cor_do_bsd(30) == "vermelho" (<40); cor_do_bsd(None) == None.
    - qualidade: verde/azul => "Alta"; amarelo/vermelho => "Baixa".
    - faixa_do_veredito("SUBAVALIADA ...") == "Barato"; "NO INTERVALO ..." == "Justo"; "SOBREAVALIADA ..." == "Caro"; "VERIFICAR ..." == None; "" / "Indeterminado" == None.
    - montar_selo(bsd=85, veredito="SUBAVALIADA ...", cfg): cor="verde", qualidade="Alta", faixa_preco="Barato", rotulo="JOIA", verificar=False.
    - montar_selo(bsd=30, veredito="SUBAVALIADA ...", cfg): qualidade="Baixa", rotulo="VALUE TRAP".
    - montar_selo(bsd=85, veredito="VERIFICAR ...", cfg): faixa_preco=None, rotulo=None, verificar=True (só o alerta se sobrepõe; sem rótulo de preço).
    - montar_selo(bsd=None, veredito="NO INTERVALO ...", cfg): cor=None, qualidade=None, rotulo=None (degrada sem quebrar).
  </behavior>
  <action>
    Criar `src/analista/report/selo.py` (módulo puro, espelhando o firewall do setup.py: NUNCA importar report.py; recebe primitivos). Conteúdo:
    - `@dataclass Selo` com campos: `bsd: Optional[float]`, `cor: Optional[str]` (verde|azul|amarelo|vermelho), `qualidade: Optional[str]` (Alta|Baixa), `faixa_preco: Optional[str]` (Barato|Justo|Caro), `rotulo: Optional[str]`, `verificar: bool = False`. Defaults None/aditivos.
    - `cor_do_bsd(bsd, cfg) -> Optional[str]`: lê `cfg["selo"]["cor"]` (verde_min/azul_min/amarelo_min); clamp por faixas; None quando bsd None.
    - `_qualidade(cor) -> Optional[str]`: verde/azul→"Alta", amarelo/vermelho→"Baixa", None→None.
    - `faixa_do_veredito(veredito) -> Optional[str]`: mapeia pelo PREFIXO da string (startswith) — SUBAVALIADA→"Barato", "NO INTERVALO"→"Justo", SOBREAVALIADA→"Caro", VERIFICAR/vazio/outros→None.
    - `_MATRIZ`: dict fixo (qualidade, faixa)→rótulo com os labels de D2 (copy estável, NÃO tunável): ("Alta","Barato")="JOIA"; ("Alta","Justo")="Boa, no preço"; ("Alta","Caro")="Boa, mas cara"; ("Baixa","Barato")="VALUE TRAP"; ("Baixa","Justo")="Fraca"; ("Baixa","Caro")="Evitar".
    - `montar_selo(bsd, veredito, cfg) -> Selo`: deriva cor→qualidade; se veredito startswith "VERIFICAR" → verificar=True, faixa_preco=None, rotulo=None (o alerta se sobrepõe ao rótulo, D2); senão faixa=faixa_do_veredito; rotulo = `_MATRIZ.get((qualidade, faixa))` só quando ambos existem, senão None. Never-raise.
    Copy: docstring reforça "EXIBE, NUNCA recomenda" — JOIA/VALUE TRAP descrevem qualidade×preço, não são ordem de compra/venda.

    Wiring em report.py: (a) importar `from ..core import screening` e `from . import selo as selo_mod` (ou `from .selo import montar_selo, Selo`); (b) adicionar campo `selo: Optional["selo_mod.Selo"] = None` em AnaliseAcao (aditivo, default None — não altera os campos existentes); (c) em analisar_acao, DEPOIS que `a.veredito` estiver montado, computar `bsd = screening.bsd_empresa(c, cfg)` e `a.selo = selo_mod.montar_selo(bsd, a.veredito, cfg)`, envolto para never-raise (se falhar, a.selo fica None). Não tocar a rede (bsd_empresa é puro sobre CompanyData já carregado).
  </action>
  <verify>
    <automated>./.venv/bin/python -c "import yaml; from analista.report import selo; cfg=yaml.safe_load(open('config.yaml')); assert selo.cor_do_bsd(85,cfg)=='verde' and selo.cor_do_bsd(60,cfg)=='azul' and selo.cor_do_bsd(45,cfg)=='amarelo' and selo.cor_do_bsd(30,cfg)=='vermelho' and selo.cor_do_bsd(None,cfg) is None; s=selo.montar_selo(85,'SUBAVALIADA — x',cfg); assert s.rotulo=='JOIA' and s.qualidade=='Alta' and s.faixa_preco=='Barato'; t=selo.montar_selo(30,'SUBAVALIADA — x',cfg); assert t.rotulo=='VALUE TRAP'; v=selo.montar_selo(85,'VERIFICAR — x',cfg); assert v.verificar and v.rotulo is None and v.faixa_preco is None; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/analista/report/selo.py` NÃO importa `report` (grep: `grep -n 'import' src/analista/report/selo.py | grep -v 'from \.\.core\|from __future__\|dataclass\|typing'` não retorna `report`).
    - Todos os casos do bloco `<behavior>` passam.
    - `AnaliseAcao` tem o campo `selo` com default None; `analisar_acao` popula `a.selo` como instância de `Selo` (ou None em degradação) sem tocar a rede.
    - Firewall preservado: `selo.py` recebe só primitivos (bsd float, veredito str, cfg dict).
  </acceptance_criteria>
  <done>Selo e quadrante derivados na engine; a.selo populado em 1 chamada de analisar_acao, sem rede, com degradação graciosa.</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_selo.py + suíte golden completa verde</name>
  <files>tests/test_selo.py</files>
  <read_first>
    - tests/test_report.py (L1-40 — padrão de carregar config.yaml shipado e montar CompanyData/fixtures)
    - tests/test_screening.py (padrão de fixtures de CompanyData para o BSD)
    - src/analista/report/selo.py (a API criada na Task 2)
  </read_first>
  <action>
    Criar `tests/test_selo.py` carregando o config.yaml shipado (mesmo padrão do test_report.py). Cobrir, como goldens determinísticos:
    - Cortes de cor: verde>=70, azul 55-70, amarelo 40-55, vermelho<40, incluindo as BORDAS exatas (70→verde, 55→azul, 40→amarelo) e bsd None→cor None.
    - qualidade: Alta para verde/azul, Baixa para amarelo/vermelho.
    - faixa_do_veredito para as 4 strings reais (usar prefixos SUBAVALIADA/NO INTERVALO/SOBREAVALIADA/VERIFICAR) + string vazia/"Indeterminado" → None.
    - Matriz de quadrante: os 6 rótulos de D2 (JOIA, "Boa, no preço", "Boa, mas cara", VALUE TRAP, Fraca, Evitar).
    - Overlay VERIFICAR: verificar=True, faixa_preco=None, rotulo=None.
    - Degradação: bsd None (cor/rotulo None), veredito vazio (rotulo None mas cor definida).
    - Firewall: assert por introspecção que `analista.report.selo` não tem `report` nos seus imports de módulo OU um teste que importa selo isoladamente sem puxar report (import-time livre).
    Opcional (integração barata): montar um CompanyData de fixture, rodar report.analisar_acao e asserir que `a.selo` é um Selo coerente com o `a.veredito` e o `screening.bsd_empresa(c,cfg)`.
  </action>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_selo.py -q && ./.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_selo.py` passa isolado.
    - A suíte completa `./.venv/bin/python -m pytest -q` fica VERDE com contagem >= 307 + os novos testes de selo (nenhum golden pré-existente quebrado — selo é aditivo).
    - Existe pelo menos 1 teste para cada: borda de cor, cada um dos 6 rótulos do quadrante, overlay VERIFICAR e degradação (bsd None / veredito vazio).
  </acceptance_criteria>
  <done>Selo coberto por goldens; invariante dos goldens pré-existentes preservada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ticker/tickers digitados pelo usuário → engine | Entrada de texto já normalizada e resolvida pela camada de ingest existente; o selo não recebe input novo do usuário, só números já calculados (bsd, veredito). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | `montar_selo` recebendo veredito/bsd inesperado (None, string vazia, formato novo) | mitigate | `faixa_do_veredito` casa por prefixo conhecido e cai para None; `montar_selo` é never-raise; testes cobrem None/vazio. |
| T-20-02 | Denial of Service | `bsd_empresa` levantando e derrubando analisar_acao | mitigate | wrapping never-raise em analisar_acao; a.selo degrada para None sem quebrar o veredito fundamentalista. |
| T-20-03 | Information Disclosure | Rótulo do quadrante ser lido como recomendação (risco regulatório, não técnico) | accept | Copy descritiva fixa (D2) + disclaimer de exibição na UI (Plan 02); fronteira "EXIBE, NUNCA recomenda" é gate do marco. |
</threat_model>

<verification>
- `./.venv/bin/python -m pytest -q` verde (>=307 + testes de selo).
- `grep -n 'import' src/analista/report/selo.py` não referencia `report` (firewall).
- `a.selo` populado em analisar_acao sem chamada de rede (bsd_empresa é puro).
- `config.yaml` carrega e expõe `selo.cor`.
</verification>

<success_criteria>
- SELO-01: engine deriva cor do selo a partir do BSD (config-driven), disponível para 1 empresa (bsd_empresa) e reprodutível.
- SELO-02: engine cruza qualidade×preço no rótulo de quadrante (montar_selo), com VERIFICAR como overlay separado.
- app.py INTOCADO nesta wave (firewall verificável antes da UI).
- Goldens verdes; zero dependência nova; custo-zero.
</success_criteria>

<output>
Após completar, criar `.planning/phases/20-selo-de-sustentabilidade-do-dividendo-cruzado-com-veredito-d/20-01-SUMMARY.md`
</output>
