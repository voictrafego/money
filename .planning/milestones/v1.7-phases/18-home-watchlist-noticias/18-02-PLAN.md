---
phase: 18-home-watchlist-noticias
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/analista/core/home_feed.py, app.py]
autonomous: true
requirements: [WATCH-01, WATCH-02]
user_setup: []
must_haves:
  truths:
    - "A watchlist parte de ~5 tickers default de dividendos, e editavel (add/remove, teto 5) e persiste entre sessoes via localStorage"
    - "Cada item mostra preco + variacao do dia colorida (verde/vermelho) e um selo de atraso ~15min sempre visivel"
    - "As cotacoes atualizam sozinhas (~30-60s) via fragment e o fetch usa cache compartilhado no servidor (1 chamada por conjunto de tickers por intervalo)"
    - "Ticker invalido ou Yahoo em falha degrada por item (mostra travessao), nunca derruba a pagina"
  artifacts:
    - path: "src/analista/core/home_feed.py"
      provides: "cotacoes(tickers) real: yf.download em lote, variacao do dia close[-1]/close[-2]-1, degradacao por item"
      contains: "yf.download"
    - path: "app.py"
      provides: "wrapper @st.cache_data(ttl=45) _cotacoes + fragment @st.fragment(run_every=45) _render_watchlist + editor de tickers com localStorage + st.metric colorido + selo de atraso"
      contains: "run_every"
  key_links:
    - from: "app.py _render_watchlist (fragment)"
      to: "home_feed.cotacoes"
      via: "wrapper _cotacoes(tuple(sorted(tickers))) @st.cache_data(ttl=45)"
      pattern: "cotacoes"
    - from: "app.py editor de tickers"
      to: "localStorage watchlist_v18"
      via: "streamlit-local-storage bidirecional OU fallback session_state semeado pelos defaults"
      pattern: "watchlist_v18"
---

<objective>
Preencher a watchlist da Home (WATCH-01 + WATCH-02): implementar o corpo de home_feed.cotacoes()
(uma chamada yf.download em lote, variacao do dia, degradacao por item) e, no app.py, o wrapper cacheado
process-global _cotacoes (ttl=45), o fragment _render_watchlist (run_every=45), o editor de tickers com
persistencia localStorage (streamlit-local-storage bidirecional, fallback session_state), o st.metric
colorido de preco+variacao e o selo de atraso ~15min. Substitui o placeholder de watchlist do render_home.

Purpose: cotacao auto-atualizavel custo-zero com cache compartilhado (D-05) e persistencia sem backend (D-02).
Output: home_feed.cotacoes real + bloco de watchlist funcional no render_home.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-home-watchlist-noticias/18-CONTEXT.md
@.planning/phases/18-home-watchlist-noticias/18-RESEARCH.md
@.planning/phases/18-home-watchlist-noticias/18-PATTERNS.md
@.planning/phases/18-home-watchlist-noticias/18-01-SUMMARY.md

<interfaces>
home_feed (do plano 01) — DEFAULT_WATCHLIST, MAX_WATCHLIST=5, validar_ticker(t), cotacoes(tickers) a implementar, render_home() (placeholder de watchlist a substituir)
prices.py:27-42 — _yf() (import tardio), yahoo_symbol(ticker) (adiciona .SA)
app.py:58-68 — modelo de wrapper @st.cache_data(ttl=) com import tardio (chave DEVE ser hashavel: tuple, nunca list)
app.py:82-97 — fmt_rs, fmt_pct, esc_md (esc_md em qualquer R$ dentro de markdown/metric — Pitfall 8)
app.py:334-358 — ponte localStorage best-effort (LWC-03): CADA acesso em try/catch independente; render nunca quebra
app.py:912-915 — modelo de @st.fragment(run_every=): re-roda so o bloco; controles que disparam rerun ficam FORA do fragment
app.py:1126-1128 — selo de atraso sempre visivel (st.caption com ~15min)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: home_feed.cotacoes() real — batch yf.download + variacao do dia + degradacao por item</name>
  <files>src/analista/core/home_feed.py</files>
  <read_first>
    - src/analista/ingest/prices.py:27-42, 113-148 (yahoo_symbol, _yf, padrao try/except silencioso)
    - src/analista/ingest/intraday.py:8-14 (contrato never-raise a espelhar)
    - 18-RESEARCH.md (Code Examples: Cotacoes em lote + variacao do dia; Pitfall 2 mercado fechado; A1)
    - 18-PATTERNS.md (home_feed analog primario prices.py; anti-pattern "1 chamada por ticker num loop")
  </read_first>
  <behavior>
    - cotacoes(("BBSE3","TAEE11")) retorna list com 1 dict por ticker, cada um com chaves ticker/preco/pct/ok.
    - Ticker que resolve: ok=True, preco float positivo, pct float (variacao do dia = close[-1]/close[-2]-1 sobre barras diarias).
    - Ticker invalido/sem dado ou Yahoo em falha: ok=False, preco=None, pct=None — a lista mantem o item, nao levanta.
    - Uma falha por ticker nao contamina os demais. Ordem de saida igual a ordem de entrada.
  </behavior>
  <action>
    Implementar cotacoes(tickers) em home_feed.py reusando prices (import tardio): syms = [prices.yahoo_symbol(t) for t in tickers].
    Fazer UMA chamada em lote yf.download(" ".join(syms), period="5d", interval="1d", group_by="ticker", auto_adjust=False,
    progress=False, threads=True) dentro de try/except (df=None em falha). Para cada par (ticker, sym): num try, obter a serie
    Close do sym via df, dropna, preco=float da ultima barra, prev=float da penultima, pct=preco/prev-1, e dar append de
    dict ok=True; no except dar append de dict ok=False com preco=None e pct=None. period FIXO "5d" (nunca derivar do input;
    exceder teto retorna vazio). Variacao do dia sobre barras diarias e robusta a fim de semana (Pitfall 2). NUNCA levantar.
    Nota A1: registrar no SUMMARY a validacao de 1 ticker (ultima/penultima Close) vs o previousClose do site — se divergir,
    ajustar para fast_info.previous_close (custa 1 call/ticker).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q &amp;&amp; .venv/bin/python -c "from analista.core import home_feed as h; r=h.cotacoes(('BBSE3','BADXX')); assert isinstance(r,list) and len(r)==2 and all(all(k in x for k in ('ticker','preco','pct','ok')) for x in r); print('cotacoes contrato OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` sai 0 (283 passed).
    - cotacoes(("BBSE3","BADXX")) retorna list de tamanho 2; cada item tem as chaves ticker/preco/pct/ok; nao levanta (mesmo offline).
    - home_feed.py faz UMA chamada yf.download em lote (nao um loop de download por ticker); period fixo "5d".
    - Firewall D-06 intacto: nenhum import de report/build/indicators/multiples/screening/comparables/grafico.
  </acceptance_criteria>
  <done>cotacoes() implementada: lote unico, variacao do dia, degradacao por item, never-raise.</done>
</task>

<task type="auto">
  <name>Task 2: Bloco de watchlist no app.py — cache wrapper + fragment + editor + metric colorido + selo</name>
  <files>app.py</files>
  <read_first>
    - app.py:58-68 (modelo de wrapper @st.cache_data), app.py:82-97 (fmt_rs/fmt_pct/esc_md), app.py:334-358 (ponte localStorage),
      app.py:884-915 e app.py:1126-1128 (fragment run_every + selo de atraso), e o render_home do plano 01
    - 18-RESEARCH.md (Pattern 2/3/4/5; Q5 persistencia; Pitfalls 1,4,5,7,8)
    - 18-PATTERNS.md (app.py excertos 2,3,4,5,6)
  </read_first>
  <action>
    No app.py, substituir o placeholder de watchlist do render_home por um bloco real:
    - Wrapper cacheado process-global: def _cotacoes(tickers) decorado com @st.cache_data(show_spinner=False, ttl=45) que
      importa home_feed (import tardio) e retorna home_feed.cotacoes(tickers). Chave DEVE ser hashavel: sempre chamar com
      tuple(sorted(...)), NUNCA list. Nunca usar st.cache_data.clear() global (D-05 / apagaria montar/selic_atual/rf_capm).
    - Fonte de verdade em runtime: st.session_state["watchlist"] semeada por home_feed.DEFAULT_WATCHLIST no 1o load.
      Persistencia: se streamlit-local-storage disponivel (decisao A2 do plano 01), ler a chave "watchlist_v18" no load
      inicial e escrever a cada mudanca; CADA acesso em try/except independente (SecurityError em iframe/anonima NAO quebra a
      pagina — Pitfall 4/LWC-03). Se indisponivel, usar so session_state (persistencia best-effort, semeada pelos defaults).
      Namespace proprio "watchlist_v18" (Pitfall 7 — nao colidir com a chave do grafico do LWC-03).
    - Editor de tickers: add (valida com home_feed.validar_ticker antes de aceitar; rejeita invalido sem quebrar; teto
      MAX_WATCHLIST=5) e remove. Os controles do editor ficam FORA do fragment (mexer neles dispara rerun full — Pitfall 5).
    - Fragment @st.fragment(run_every=45) def _render_watchlist(): chama _cotacoes(tuple(sorted(st.session_state["watchlist"])))
      e, para cada item: se ok, st.metric(label=ticker, value=esc_md(fmt_rs(preco)), delta=fmt_pct colorido do dia) — delta_color
      default colore + verde / - vermelho; se nao ok, st.metric(ticker, "—"). Distribuir em colunas.
    - Selo de atraso SEMPRE visivel no bloco: st.caption com aviso de ~15min de atraso (best-effort). Nao passar sensacao de tempo real.
    - "Flash" na mudanca de preco: deferido (a cor do delta ja entrega o essencial — RESEARCH Q4).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q &amp;&amp; grep -q "run_every=45" app.py &amp;&amp; grep -q "ttl=45" app.py &amp;&amp; grep -q "watchlist_v18" app.py &amp;&amp; grep -q "st.metric" app.py</automated>
  </verify>
  <acceptance_criteria>
    - app.py define _cotacoes com @st.cache_data(ttl=45) e chama com tuple (nao list); nao ha st.cache_data.clear() novo.
    - app.py define _render_watchlist com @st.fragment(run_every=45) que renderiza st.metric por ticker (delta colorido) e um selo de atraso ~15min.
    - Existe editor de tickers (add com validar_ticker + teto 5, remove) e a persistencia usa a chave "watchlist_v18" (streamlit-local-storage) OU fallback session_state semeado pelos defaults.
    - `.venv/bin/python -m pytest -q` sai 0 (283 passed); os 4 branches existentes seguem inalterados.
  </acceptance_criteria>
  <done>Watchlist funcional: default editavel persistido, metric colorido auto-atualizavel via cache compartilhado, selo de atraso, degradacao por item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuario -> home_feed -> Yahoo | Ticker digitado pelo usuario vira simbolo Yahoo e dispara fetch externo |
| browser localStorage -> Python | Watchlist persistida e relida (bidirecional via streamlit-local-storage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | ticker do usuario -> yahoo_symbol / fetch | mitigate | validar_ticker (regex 4-6 chars A-Z0-9) ANTES de aceitar na watchlist; SSRF minimo (host fixo query.finance.yahoo.com) |
| T-18-04 | DoS | polling de cotacoes x N usuarios | mitigate | @st.cache_data(ttl=45) process-global (1 chamada por conjunto por TTL) + yf.download em lote (nao loop) + retry calibrado de prices.py |
| T-18-05 | Robustez | localStorage SecurityError em iframe sandbox/anonima | mitigate | cada acesso em try/except independente + fallback session_state semeado pelos defaults (pagina sempre renderiza) |

Severidade residual: baixa. Sem segredos; input restrito a ticker validado; host de fetch fixo.
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` sai 0 (283 passed) — engines/goldens intactos.
- home_feed.cotacoes never-raise e degrada por item (offline inclusive).
- Bloco de watchlist com cache ttl=45, fragment run_every=45, editor validado (teto 5), metric colorido, selo de atraso, persistencia watchlist_v18.
</verification>

<success_criteria>
- WATCH-01: watchlist default editavel, teto 5, persistida via localStorage (ou fallback session_state); ticker invalido degrada sem quebrar.
- WATCH-02: preco + variacao do dia colorida, auto-refresh ~30-60s, cache compartilhado (1 chamada por intervalo), aviso de atraso ~15min, degradacao por item.
</success_criteria>

<output>
Apos concluir, criar .planning/phases/18-home-watchlist-noticias/18-02-SUMMARY.md (registrar a validacao A1 de 1 ticker,
o caminho de persistencia usado (dep ou fallback) e a confirmacao dos 283 goldens verdes).
</output>
