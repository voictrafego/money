---
phase: 18-home-watchlist-noticias
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified: []
autonomous: false
requirements: [HOME-01, WATCH-01, WATCH-02, NEWS-01, NEWS-02]
must_haves:
  truths:
    - "Os 283 testes golden seguem verdes ao final da fase"
    - "As engines fundamentalista/tecnica ficam intactas (report/indicators/multiples/screening/grafico fora do diff da fase)"
    - "app.py continua thin renderer da Home (render_home so le home_feed; nao recalcula metodo)"
    - "Verificacao humana no navegador aprova a Home como landing default (watchlist + noticias) sem regressao nos 4 menus"
  artifacts:
    - path: ".planning/phases/18-home-watchlist-noticias/18-04-SUMMARY.md"
      provides: "Registro da verificacao (goldens + engines intactas + smoke no navegador)"
  key_links:
    - from: "verificacao"
      to: "Home + Analisar + Garimpo + Ranking + Swing"
      via: "smoke no navegador via Claude-in-Chrome (sem regressao)"
      pattern: "Inicio"
---

<objective>
Fechar a Phase 18 com a verificacao de aceite: (1) automatizada — os 283 testes golden verdes, as engines
intactas (diff contra o SHA-base fixo da fase, nunca HEAD~N) e app.py ainda thin renderer da Home; (2)
humana — smoke no navegador (via Claude-in-Chrome) da Home como landing default: watchlist com preco+variacao
colorida, auto-refresh e selo de atraso; feed de noticias com manchete/fonte/hora e link abrindo em nova aba;
SEM regressao nos 4 menus existentes (Analisar/Garimpar/Ranking/Swing).

Purpose: gate de aceite consolidado de HOME-01/WATCH-01/WATCH-02/NEWS-01/NEWS-02 (goldens + navegador).
Output: 18-04-SUMMARY.md com o resultado da verificacao; fase pronta para fechar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-home-watchlist-noticias/18-CONTEXT.md
@.planning/phases/18-home-watchlist-noticias/18-01-SUMMARY.md
@.planning/phases/18-home-watchlist-noticias/18-02-SUMMARY.md
@.planning/phases/18-home-watchlist-noticias/18-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificacao automatizada — goldens verdes + engines intactas + thin renderer</name>
  <files>(nenhum arquivo modificado — verificacao read-only: pytest + git diff + grep)</files>
  <read_first>
    - app.py (render_home + wrappers _cotacoes/_noticias + fragments — revisar que so LEEM home_feed, sem indicators.calcular/montar_setup/build.montar_empresa)
    - src/analista/core/home_feed.py (revisar firewall D-06 e never-raise)
    - .planning/ROADMAP.md (Phase 18 success criteria + gate 283 goldens/engines intactas)
  </read_first>
  <action>
    Rodar a suite golden completa e provar os invariantes de forma automatizada:
    1. `.venv/bin/python -m pytest -q` deve reportar 283 passed — ZERO falhas/regressao.
    2. Provar que as engines NAO foram tocadas: ler o SHA-base fixo da fase em
       `.planning/phases/18-home-watchlist-noticias/.phase-base-sha` (gravado no plano 01, variavel BASE) e exigir que
       `git diff --name-only BASE..HEAD -- src/analista/report src/analista/core/indicators.py src/analista/core/multiples.py src/analista/core/screening.py src/analista/grafico.py`
       esteja VAZIO. Usar a BASE FIXA, nunca HEAD~N.
    3. Provar que app.py continua thin renderer no caminho Home: dentro do bloco render_home (e dos fragments
       _render_watchlist/_render_noticias) NAO deve haver chamada a indicators.calcular( nem setup.montar_setup( nem
       build.montar_empresa(. Confirmar por grep/leitura que essas chamadas nao aparecem no caminho da Home.
    4. Confirmar firewall D-06: home_feed.py sem import de report/build/indicators/multiples/screening/comparables/grafico
       (checar as linhas de import do arquivo).
    Se qualquer item falhar, corrigir a regressao antes do checkpoint humano.
  </action>
  <verify>
    <automated>cd "$PWD" && .venv/bin/python -m pytest -q && BASE=$(cat .planning/phases/18-home-watchlist-noticias/.phase-base-sha) && [ -z "$(git diff --name-only "$BASE"..HEAD -- src/analista/report src/analista/core/indicators.py src/analista/core/multiples.py src/analista/core/screening.py src/analista/grafico.py)" ] && echo ENGINES_INTACTAS && if grep -nE "^[[:space:]]*(from|import).*(report|indicators|multiples|screening|comparables|grafico)" src/analista/core/home_feed.py; then echo FAIL_FIREWALL_HOMEFEED; exit 1; fi && echo FIREWALL_OK && if awk '/^def /{f=(tolower($0)~/home|watchlist|notic|cotac/)?1:0} f' app.py | grep -Eq "indicators\.calcular\(|montar_setup\(|montar_empresa\("; then echo FAIL_RECALC_HOME; exit 1; fi && echo HOME_THIN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` sai 0 (283 passed); zero falhas.
    - `git diff --name-only BASE..HEAD` (BASE = SHA de `.phase-base-sha`) restrito a report/indicators/multiples/screening/grafico esta VAZIO.
    - Nenhuma chamada a indicators.calcular(, setup.montar_setup( ou build.montar_empresa( no caminho da Home (render_home + fragments).
    - home_feed.py nao importa report/build/indicators/multiples/screening/comparables/grafico.
  </acceptance_criteria>
  <done>Goldens verdes, engines intactas (diff vazio) e caminho da Home comprovadamente read-only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke no navegador da Home (via Claude-in-Chrome)</name>
  <files>(nenhum — verificacao manual no navegador)</files>
  <read_first>
    - app.py (render_home + bloco de watchlist + bloco de noticias — para saber o que verificar)
    - .planning/phases/18-home-watchlist-noticias/18-CONTEXT.md (D-01..D-06)
  </read_first>
  <action>
    Subir o app localmente e conduzir o smoke no navegador (Claude-in-Chrome) seguindo o roteiro de how-to-verify;
    registrar o resultado (aprovado / problemas) e um screenshot no 18-04-SUMMARY.md. Nao alterar codigo neste passo
    (so verificacao); se achar bug, abrir gap para o plano de correcao.
  </action>
  <what-built>
    Home como landing default: radio com "Inicio" como 1o item; watchlist de ~5 tickers de dividendos editavel e
    persistida, com preco + variacao do dia colorida (verde/vermelho), auto-refresh ~45s e selo de atraso ~15min;
    feed de noticias (InfoMoney + Google News) com manchete + submanchete + fonte + horario e link que abre o site
    original em nova aba; auto-refresh do feed ~10min. Os 4 menus existentes seguem inalterados.
  </what-built>
  <how-to-verify>
    1. Subir o app: `.venv/bin/streamlit run app.py --server.port 8501 --server.headless true` e abrir
       http://localhost:8501 no navegador (Claude-in-Chrome).
    2. Confirmar que a tela inicial (default, sem clicar em nada) e a Home; no radio lateral, "Inicio" e o 1o item selecionado.
    3. Watchlist: confirmar ~5 tickers default com preco e variacao do dia COLORIDA (verde/vermelho) e o selo "~15min de atraso".
       Adicionar um ticker valido e remover um; recarregar a pagina e confirmar que a escolha PERSISTE (localStorage).
       Adicionar um ticker invalido (ex.: "123") e confirmar que degrada sem quebrar a pagina.
    4. Auto-refresh: aguardar ~45s e confirmar que o bloco da watchlist re-renderiza sozinho (sem recarregar a pagina toda).
    5. Noticias: confirmar a lista com manchete + fonte + horario (hora em fuso BR) e submanchete quando houver; clicar em
       "Abrir no site" e confirmar que abre o site original da fonte em NOVA ABA; confirmar que o texto completo NAO e reproduzido.
    6. Regressao: visitar os 4 menus (Analisar/Garimpar/Ranking/Swing) e confirmar que nenhum mudou de comportamento;
       checar o console do navegador sem erros novos.
    7. Tirar screenshot da Home (watchlist + noticias) para o SUMMARY.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Home ao vivo -> fontes externas (Yahoo/RSS) | Verificacao confirma que a pagina degrada sem quebrar quando uma fonte falha |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Info/Tampering | render ao vivo de RSS/cotacao no navegador | mitigate | O smoke confirma render de texto (sem HTML cru), link abrindo em nova aba, e console sem erros; ticker invalido e feed caido degradam sem quebrar |

Severidade maxima: baixa. Este plano e verificacao; nao introduz superficie nova.
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` sai 0 (283 passed).
- Engines fora do diff da fase (BASE fixa); caminho da Home thin (sem recalculo).
- Checkpoint humano aprovado (Home default + watchlist + noticias + sem regressao nos 4 menus).
</verification>

<success_criteria>
- 283 goldens verdes; engines intactas; app.py thin renderer da Home comprovado.
- Verificacao humana no navegador aprova a Home (landing default, watchlist, noticias) sem regressao nas abas existentes.
</success_criteria>

<output>
Apos concluir, criar .planning/phases/18-home-watchlist-noticias/18-04-SUMMARY.md (incluir screenshot da Home +
resultado dos goldens + confirmacao de nao-regressao).
</output>
