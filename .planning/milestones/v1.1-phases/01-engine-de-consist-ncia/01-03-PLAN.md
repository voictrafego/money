---
phase: 01-engine-de-consist-ncia
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/analista/core/screening.py
  - tests/test_screening.py
  - src/analista/glossario.py
autonomous: true
requirements: [GARIMPO-02, GARIMPO-03, GARIMPO-04]
must_haves:
  truths:
    - "A mesma ação tem o mesmo BSD independentemente de quais outros tickers foram colados no lote (padronização contra referência fixa/bandas, não min-max do lote)."
    - "'BSD > 80' volta a ser um corte absoluto válido (não um rank relativo ao conjunto submetido): uma empresa forte passa de 80 e uma fraca fica abaixo de 80."
    - "Fatores do BSD com dado ausente entram como neutro (não como pior valor 0), e o resultado expõe quantos/quais fatores faltaram por empresa."
    - "O proxy de crescimento do BSD usa a mesma janela (anos_media) dos demais fatores e o tooltip documenta que é ROE×(1−payout) por fundamentos."
  artifacts:
    - path: "src/analista/core/screening.py"
      provides: "Padronização BSD absoluta (REFERENCIA_BSD com 10 bandas fixas), fatores ausentes neutros + contagem de faltantes, proxy de crescimento com janela anos_media"
      contains: "def bsd_ranking"
    - path: "src/analista/glossario.py"
      provides: "Tooltip do BSD documentando referência fixa, fatores faltantes e o proxy de crescimento"
      contains: "\"bsd\""
    - path: "tests/test_screening.py"
      provides: "Teste de BSD atualizado para o novo comportamento absoluto + reprodutibilidade entre lotes + smoke do corte 80"
  key_links:
    - from: "src/analista/core/screening.py::bsd_ranking"
      to: "REFERENCIA_BSD (bandas fixas por indicador)"
      via: "padronização absoluta (não _padronizar_0_100 do lote no passo final)"
      pattern: "REFERENCIA_BSD"
    - from: "src/analista/core/screening.py::indicadores_bsd proxy"
      to: "crescimento_por_fundamentos com média anos_media de roe/payout"
      via: "mesma janela dos outros fatores"
      pattern: "crescimento_por_fundamentos"
---

<objective>
Tornar o BSD do Garimpo reproduzível e absoluto: (1) padronizar contra referência fixa/bandas (não min-max do lote), de modo que a mesma ação tenha o mesmo BSD em lotes diferentes e "BSD>80" seja corte absoluto; (2) tratar fatores ausentes como neutro e expor quantos/quais faltaram; (3) padronizar a janela do proxy de crescimento e documentá-lo no tooltip.

Purpose: Fechar WR-06 (GARIMPO-02), WR-05 (GARIMPO-03) e WR-02 (GARIMPO-04). Hoje o BSD é relativo ao lote, ausentes viram 0 (pior valor) e o proxy de crescimento usa janela de 1 ano enquanto os demais usam anos_media.
Output: `screening.bsd_ranking`/`indicadores_bsd`/padronização absolutos e neutros-para-ausentes; tooltip do BSD atualizado; teste de screening ajustado para o novo comportamento.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CONSISTENCY-REVIEW.md
@.planning/phases/01-engine-de-consist-ncia/01-01-SUMMARY.md

<interfaces>
<!-- Contratos atuais em screening.py. Plano 01 já entregou payout_valuation/roe/dy_atual na engine. -->

De src/analista/core/screening.py:
- PESOS_BSD_PADRAO: dict com 10 fatores (payout 30, cobertura_juros 10, fc_sobre_lucro 5, dividend_yield 5,
  desempenho_relativo_preco 10, variacao_tangivel_vp 10, crescimento_lucro_lp 10, crescimento_fc_3a 5,
  crescimento_dividendos_3a 10, crescimento_lucro_3a 5)
- indicadores_bsd(c, anos_media=3) -> Dict[str, Optional[float]]  # 10 indicadores brutos (médias de anos_media)
    proxy de crescimento (linha 234-237): cresc_lucro_lp cai p/ crescimento_por_fundamentos(c.roe(ult), c.payout(ult)) — ano único (WR-02)
- _winsorize(valores, p) -> List
- _padronizar_0_100(valores) -> List  # min-max [0,100]; AUSENTES recebem 0.0 (WR-05); passo final re-padroniza a média ponderada (WR-06)
- bsd_ranking(empresas, pesos, anos_media=3, winsor=0.10) -> List[Dict]  # itens: ticker, nome, setor, bsd, acima_de_80, indicadores

De src/analista/core/growth.py:
- crescimento_por_fundamentos(roe, payout) = roe*(1-payout)  # NÃO alterar a fórmula

ATENÇÃO ao teste golden existente (tests/test_screening.py::test_bsd_ranking_ordena_e_marca_acima_80):
  hoje afirma ranking[0]["bsd"]==100.0 e ranking[-1]["bsd"]==0.0 — isso É o comportamento min-max do lote
  que GARIMPO-02 elimina. Este teste TEM de ser atualizado para o novo comportamento absoluto (não é golden de
  fórmula de valuation; é golden do comportamento que estamos corrigindo). Os demais testes de screening
  (filtros_customizados, graham) NÃO mudam e devem continuar passando.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Padronização absoluta do BSD (REFERENCIA_BSD com bandas fixas)</name>
  <files>src/analista/core/screening.py</files>
  <read_first>
    - src/analista/core/screening.py linhas 173-184 (PESOS_BSD_PADRAO: nomes EXATOS dos 10 fatores e seus pesos)
    - src/analista/core/screening.py linhas 187-256 (indicadores_bsd: o que cada um dos 10 fatores MEDE e em que unidade — payout em fração, cobertura_juros em "vezes", fc_sobre_lucro em razão, dy em fração, desempenho relativo em fração ±, variacao_tangivel/crescimentos em CAGR a.a.)
    - src/analista/core/screening.py linhas 259-328 (_winsorize, _padronizar_0_100, bsd_ranking)
    - CONSISTENCY-REVIEW.md WR-06 (BSD relativo ao lote; "só 19 de 297 passaram" é corte absoluto de Carlson — payout peso 30%)
    - src/analista/glossario.py chave "bsd" (corte absoluto >80 prometido)
  </read_first>
  <behavior>
    - Mesma empresa em lotes diferentes (sozinha vs junto de outras) → mesmo BSD (tolerância numérica).
    - Empresa FORTE (todos os 10 fatores no TERÇO SUPERIOR das bandas) → BSD > 80.
    - Empresa FRACA (todos os 10 fatores no TERÇO INFERIOR das bandas) → BSD < 80.
    - O passo final NÃO re-padroniza a média ponderada por min-max do lote.
  </behavior>
  <action>
    Substituir a padronização relativa-ao-lote por padronização ABSOLUTA contra referência fixa por indicador. Definir no TOPO do módulo (logo após `PESOS_BSD_PADRAO`) a constante `REFERENCIA_BSD: Dict[str, tuple]` com um par `(lo, hi)` por nome de fator — `lo` mapeia para nota 0 e `hi` para nota 100 (mais é melhor nos 10). Marcar com um comentário "# CALIBRÁVEL — ajustar as bandas aqui muda o corte 80; não tocar na lógica de padronização" deixando explícito que esta constante é o ÚNICO ponto de ajuste do corte. Usar EXATAMENTE estas 10 bandas (uma por fator do PESOS_BSD_PADRAO, nomes idênticos), cada uma com o comentário curto indicado, ancoradas no sentido do fator e no espírito de Carlson (dividendo grande, crescente e SEGURO):
      - payout: (0.0, 0.80)            # fração do lucro distribuída; payout sustentável até ~80% pontua máximo, acima disso não pontua mais (clamp). Carlson penaliza payout esticado/insustentável.
      - cobertura_juros: (1.0, 8.0)    # (LL+juros)/juros, "vezes"; 1x mal cobre a dívida (0), >=8x folga confortável (100).
      - fc_sobre_lucro: (0.5, 1.2)     # FCO/LL; caixa < metade do lucro = baixa qualidade (0), caixa >= 1.2x lucro = lucro "de verdade" (100).
      - dividend_yield: (0.0, 0.10)    # DY corrente em fração; 0% = 0, 10% a.a. = 100. Não estende acima de 10% para não premiar armadilha de dividendos (Cap. 6).
      - desempenho_relativo_preco: (-0.20, 0.20)  # excesso 6m vs Ibov; -20% = 0, +20% = 100; em torno de 0 dá nota ~50 (neutro).
      - variacao_tangivel_vp: (0.0, 0.15)         # CAGR do PL tangível; 0% não cria valor contábil (0), 15% a.a. forte criação de valor (100).
      - crescimento_lucro_lp: (0.0, 0.15)         # g esperado / proxy ROE×(1−payout); reinvestir a 0% = 0, 15% a.a. = 100.
      - crescimento_fc_3a: (-0.05, 0.15)          # CAGR do FCO 3a; queda de caixa (-5%) = 0, +15% a.a. = 100; lo levemente negativo tolera ruído.
      - crescimento_dividendos_3a: (0.0, 0.12)    # CAGR dos proventos 3a; dividendo estagnado (0%) = 0, +12% a.a. = 100 (dividendo CRESCENTE é núcleo do método).
      - crescimento_lucro_3a: (-0.05, 0.15)       # CAGR do lucro 3a; -5% = 0, +15% a.a. = 100; lo levemente negativo tolera anos atípicos.
    Criar `_padronizar_absoluto(valores, lo, hi, maior_melhor=True)` que mapeia cada valor para [0,100] por clamp linear contra (lo,hi), INDEPENDENTE do lote: `nota = clamp((v-lo)/(hi-lo), 0, 1) * 100`. Em `bsd_ranking`: padronizar cada coluna usando a banda fixa de `REFERENCIA_BSD[nome]`; REMOVER o `bsd_final = _padronizar_0_100(medias_ponderadas)` do passo final (a média ponderada das notas absolutas JÁ está em [0,100] e É o BSD). `acima_de_80` continua bsd>80, agora absoluto. Aposentar `_winsorize` no caminho do BSD (com bandas fixas o clamp já limita extremos; documentar no docstring de `bsd_ranking` que a winsorização foi substituída pelo clamp das bandas e que o parâmetro `winsor` deixa de ter efeito, mantido só por compatibilidade de assinatura). Não inserir blocos de código no plano.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && .venv/bin/python -c "
from analista.core.screening import bsd_ranking, REFERENCIA_BSD, PESOS_BSD_PADRAO, indicadores_bsd
from analista.core.fundamentals import CompanyData
# (a) REFERENCIA_BSD cobre exatamente os 10 fatores do PESOS_BSD_PADRAO
assert set(REFERENCIA_BSD) == set(PESOS_BSD_PADRAO), set(PESOS_BSD_PADRAO) ^ set(REFERENCIA_BSD)
def emp(t):
    anos=list(range(2015,2025)); c=CompanyData(ticker=t, nome=t, setor='Energia', anos=anos)
    for a in anos:
        c.lucro_liquido[a]=1000+(a-2015)*50; c.patrimonio_liquido[a]=4000+(a-2015)*100
        c.dividendos[a]=600+(a-2015)*30; c.num_acoes[a]=1000; c.vendas_liquidas[a]=1800
        c.fco[a]=1200; c.despesa_juros[a]=100; c.ativo_intangivel[a]=200
    c.preco_atual=30.0; c.desempenho_relativo_6m=0.10; c.beta=0.8; return c
def emp_fraca(t):
    c=emp(t)
    for a in c.anos:
        c.dividendos[a]=50; c.lucro_liquido[a]=1000; c.fco[a]=200
    c.desempenho_relativo_6m=-0.20; return c
# (b) reprodutibilidade entre lotes
boa=emp('BOA3')
bsd_sozinha=bsd_ranking([boa])[0]['bsd']
bsd_no_lote=[r for r in bsd_ranking([boa, emp_fraca('F1'), emp_fraca('F2')]) if r['ticker']=='BOA3'][0]['bsd']
assert abs(bsd_sozinha-bsd_no_lote)<1e-6, (bsd_sozinha, bsd_no_lote)
print('ok reprodutivel', bsd_sozinha)
"</automated>
  </verify>
  <acceptance_criteria>
    - `REFERENCIA_BSD` existe, é CALIBRÁVEL (constante no topo do módulo) e cobre EXATAMENTE os 10 nomes de `PESOS_BSD_PADRAO`.
    - O BSD de uma mesma empresa é igual (tolerância 1e-6) sozinha ou dentro de um lote com outras (reprodutibilidade).
    - O passo final de re-padronização min-max (`_padronizar_0_100(medias_ponderadas)`) foi removido.
    - `acima_de_80` é corte absoluto (bsd>80 com bandas fixas).
  </acceptance_criteria>
  <done>O BSD é reproduzível entre lotes e "BSD>80" é absoluto contra bandas calibráveis.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fatores ausentes neutros + contagem de faltantes; proxy de crescimento com janela anos_media</name>
  <files>src/analista/core/screening.py</files>
  <read_first>
    - src/analista/core/screening.py linhas 187-256 (indicadores_bsd) e a padronização absoluta criada na Task 1
    - CONSISTENCY-REVIEW.md WR-05 (ausente vira 0/pior valor) e WR-02 (proxy de crescimento usa ano único)
    - NOTA sobre roe(ano)=None: após o Plano 01, `c.roe(ano)` retorna None no 1º ano da janela quando não há PL do ano anterior (decisão LOCKED de WR-01). Portanto a média do proxy de crescimento sobre `anos_media` pode somar apenas N-1 amostras (o 1º ano contribui None e é ignorado). Esse comportamento é ACEITO — não é bug; documentar e seguir.
  </read_first>
  <behavior>
    - Indicador ausente recebe nota NEUTRA (50) na padronização, não 0.
    - O resultado de bsd_ranking expõe por empresa quantos/quais fatores faltaram (ex.: fatores_faltantes: List[str]).
    - O proxy crescimento_lucro_lp usa média de roe/payout na janela anos_media (não c.roe(ult)/c.payout(ult)); roe(ano)=None no 1º ano é ignorado na média.
  </behavior>
  <action>
    Na padronização absoluta (`_padronizar_absoluto` da Task 1), tratar valor None como NEUTRO: atribuir 50.0 (centro da faixa de nota) em vez de 0, distinguindo "ausente" de "pior valor". Em `indicadores_bsd`, computar o proxy de crescimento (`crescimento_lucro_lp`, quando `g_lucro_esperado` é None) usando a MÉDIA de `c.roe(a)` e a MÉDIA de `c.payout(a)` na janela `anos = anos_ordenados()[-anos_media:]` (mesma janela dos outros fatores), IGNORANDO valores None em cada média (incl. o roe(ano)=None do 1º ano), e passar essas médias a `growth.crescimento_por_fundamentos` — não mais `c.roe(ult)/c.payout(ult)`. Se ambas as médias resultarem None (sem amostra), o proxy fica None (vira neutro na padronização). Em `bsd_ranking`, para cada empresa coletar a lista de nomes de fatores cujo indicador BRUTO é None e incluir no dict de saída `fatores_faltantes: List[str]` e `n_fatores_faltantes: int`. Não inserir blocos de código no plano.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && .venv/bin/python -c "
from analista.core.screening import bsd_ranking
from analista.core.fundamentals import CompanyData
anos=list(range(2015,2025)); c=CompanyData(ticker='X', nome='X', setor='Energia', anos=anos)
for a in anos:
    c.lucro_liquido[a]=1000; c.patrimonio_liquido[a]=4000; c.dividendos[a]=600
    c.num_acoes[a]=1000; c.vendas_liquidas[a]=1800; c.fco[a]=1200
# sem despesa_juros (cobertura_juros ausente) e sem desempenho_relativo_6m
c.preco_atual=30.0
r=bsd_ranking([c])[0]
assert 'fatores_faltantes' in r and 'n_fatores_faltantes' in r, r.keys()
assert r['n_fatores_faltantes']>=1, r['n_fatores_faltantes']
assert r['bsd'] is not None
print('ok', r['fatores_faltantes'])
"</automated>
  </verify>
  <acceptance_criteria>
    - Fator ausente recebe 50 (neutro), comprovável por uma empresa sem cobertura_juros não ir a 0 nesse fator.
    - `bsd_ranking` retorna `fatores_faltantes` (lista) e `n_fatores_faltantes` (int) por empresa.
    - O proxy de crescimento usa a média na janela anos_media e ignora roe(ano)=None do 1º ano.
  </acceptance_criteria>
  <done>Ausentes são neutros e contados; o proxy de crescimento usa a janela padrão.</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar tooltip do BSD, o teste golden e o smoke do corte 80</name>
  <files>src/analista/glossario.py, tests/test_screening.py</files>
  <read_first>
    - src/analista/glossario.py chave "bsd" (linhas 79-84)
    - tests/test_screening.py::test_bsd_ranking_ordena_e_marca_acima_80 (linhas 70-83)
    - CONSISTENCY-REVIEW.md WR-06 e WR-02 (o que o tooltip deve explicar)
  </read_first>
  <action>
    No glossário, reescrever a chave "bsd" para: (a) deixar explícito que o BSD é absoluto contra referência fixa (corte 80 de Carlson é válido) e reprodutível entre lotes; (b) documentar que `crescimento_lucro_lp`, na ausência de estimativa de analistas, é o proxy ROE×(1−payout) por fundamentos (média da janela), não previsão de analistas; (c) mencionar que fatores ausentes entram como neutros e que o app indica quantos faltaram. Em tests/test_screening.py: atualizar `test_bsd_ranking_ordena_e_marca_acima_80` para o NOVO comportamento (parar de exigir bsd==100.0/0.0 do min-max do lote) e ADICIONAR um smoke test do CORTE 80 com dois perfis sintéticos — uma "empresa forte" (todos os 10 fatores no TERÇO SUPERIOR das bandas de REFERENCIA_BSD) deve sair com `bsd > 80` e `acima_de_80 is True`; uma "empresa fraca" (fatores no TERÇO INFERIOR das bandas) deve sair com `bsd < 80` e `acima_de_80 is False`. O teste deve construir os perfis a partir das bandas (forte ≈ lo + 2/3·(hi-lo) por fator; fraca ≈ lo + 1/3·(hi-lo)) montando um CompanyData cujos indicadores brutos caiam nessas faixas, OU validando direto via `_padronizar_absoluto`/`bsd_ranking` que o corte separa os dois perfis. Cobrir também: (i) ORDENAÇÃO (forte antes da fraca), (ii) REPRODUTIBILIDADE entre lotes (mesma empresa, mesmo BSD em lotes diferentes), (iii) presença de `fatores_faltantes`/`n_fatores_faltantes`. Não inserir blocos de código no plano.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && .venv/bin/pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - O tooltip "bsd" descreve: corte absoluto, proxy de crescimento e fatores ausentes neutros.
    - Existe um smoke test que TRAVA a ordem de grandeza do corte: empresa forte → bsd>80, empresa fraca → bsd<80 (garante que "BSD>80" tem significado absoluto e não passa todo mundo).
    - `test_bsd_ranking_*` valida ordenação + reprodutibilidade entre lotes + presença das chaves de faltantes (não mais bsd==100/0).
    - `pytest tests/ -q` passa inteiro (golden de multiples/comparables/ddm intactos; demais testes de screening intactos).
  </acceptance_criteria>
  <done>Tooltip, teste golden e smoke do corte 80 refletem o BSD absoluto, ausentes neutros e o proxy documentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yfinance/CVM → indicadores BSD | Fatores podem faltar; tratados como neutro/ausente, não como falha |
| usuário → lista de tickers | Strings normalizadas na borda do app (fora deste plano) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | fatores_faltantes exposto | accept | Apenas nomes de indicadores ausentes; sem dado sensível. Ferramenta local. |
| T-01-05 | Tampering | indicador anômalo no BSD | mitigate | Padronização absoluta com clamp em bandas fixas (REFERENCIA_BSD) limita o impacto de valores extremos. |
</threat_model>

<verification>
- `pytest tests/ -q` verde (suíte completa).
- Smoke tests inline comprovam reprodutibilidade entre lotes, corte 80 (forte>80, fraca<80) e fatores faltantes.
</verification>

<success_criteria>
- BSD reproduzível e absoluto; "BSD>80" volta a ser corte válido (forte passa, fraca não).
- Fatores ausentes neutros e contados; proxy de crescimento com janela padrão e documentado.
- Suíte de testes verde.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-de-consist-ncia/01-03-SUMMARY.md`
</output>
</content>
</invoke>
