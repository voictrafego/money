---
phase: 01-engine-de-consist-ncia
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/core/comparables.py
  - tests/test_comparables.py
autonomous: true
requirements: [RANK-02]
must_haves:
  truths:
    - "Um payout fora de [0,1] (>100% ou negativo) não entra cru na regressão de preço-alvo do Ranking; recebe o mesmo clamp que o Analisar aplica antes do DDM."
    - "Quando o payout de entrada estava fora de faixa, isso é sinalizado (não some silenciosamente)."
  artifacts:
    - path: "src/analista/core/comparables.py"
      provides: "Clamp/sinalização de payout fora de [0,1] em preco_alvo_por_regressao (e/ou no ponto de entrada da regressão)"
      contains: "preco_alvo_por_regressao"
  key_links:
    - from: "src/analista/core/comparables.py::preco_alvo_por_regressao"
      to: "regressão P/L = b0 + b1*DP + b2*ROE"
      via: "DP clampado em [0,1] antes de prever"
      pattern: "preco_alvo_por_regressao"
---

<objective>
Aplicar no Ranking, na engine de comparáveis, o MESMO clamp/alerta de payout fora de [0,1] que o Analisar já aplica antes do DDM — para que payout >100% ou negativo não puxe o P/L esperado e o preço-alvo para valores sem sentido.

Purpose: Fechar a parte (b) de CR-03 (RANK-02). Hoje `preco_alvo_por_regressao` usa `dp` cru em `b1*DP`; o Analisar limita payout a 1.0 e alerta. Mesma empresa, mesmo dado → tratamentos divergentes.
Output: `comparables.preco_alvo_por_regressao` (e o ponto que monta a previsão) aplicam clamp em [0,1] no payout e sinalizam quando o valor original estava fora da faixa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CONSISTENCY-REVIEW.md

<interfaces>
<!-- Contratos atuais. NÃO reescrever a fórmula da regressão (correta). -->

De src/analista/core/comparables.py:
- RegressaoPL.prever(dp, roe) -> float = b0 + b1*dp + b2*roe
- ajustar_regressao_pl(pl, dp, roe) -> Optional[RegressaoPL]  # descarta None e p<=0; requer >=4 linhas
- preco_alvo_por_regressao(reg, dp, roe, lpa, preco_corrente) -> Optional[PrecoAlvo]
    guard: `if reg is None or None in (dp,roe,lpa,preco_corrente) or lpa<=0: return None`
    usa dp CRU em reg.prever(dp, roe)  (comparables.py:130) — sem clamp (RANK-02)
- PrecoAlvo: dataclass com pl_corrente, pl_esperado, lpa, preco_corrente, preco_alvo, upside, subavaliada

Referência do clamp no Analisar (report.py:102-104): payout_proj = min(media_3a, 1.0).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clamp e sinalização de payout fora de faixa na regressão</name>
  <files>src/analista/core/comparables.py</files>
  <read_first>
    - src/analista/core/comparables.py (preco_alvo_por_regressao linhas 117-142; RegressaoPL.prever 76-78)
    - src/analista/report/report.py linhas 102-104 e 136-137 (clamp 1.0 + alerta ">100%" do Analisar)
    - CONSISTENCY-REVIEW.md CR-03 parte final (DP cru entra em b1*DP sem limite nem alerta no Ranking)
  </read_first>
  <action>
    Em `preco_alvo_por_regressao`: antes de chamar `reg.prever(dp, roe)`, aplicar clamp do payout `dp` em [0,1] (mesma regra do Analisar para o teto 1.0; e piso 0.0 para payout negativo de LPA<0). Usar o `dp` clampado na previsão do P/L esperado. Adicionar à dataclass `PrecoAlvo` um campo booleano `payout_fora_faixa: bool = False` (sinaliza que o dp original estava fora de [0,1]) para a UI/Fase 2 poder exibir o alerta equivalente ao do Analisar. Manter o guard de None/lpa<=0 existente. NÃO alterar `ajustar_regressao_pl` (o ajuste da regressão usa os pares do setor como observações; o clamp é na PREVISÃO do alvo, ponto onde o Analisar também clampa). Não inserir blocos de código no plano.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && .venv/bin/python -c "
import numpy as np
from analista.core.comparables import RegressaoPL, preco_alvo_por_regressao
reg=RegressaoPL(coeficientes=np.array([5.0,3.0,2.0]), r2=0.9, n=5)
pa_alto=preco_alvo_por_regressao(reg, dp=1.5, roe=0.2, lpa=2.0, preco_corrente=20.0)
pa_clamp=preco_alvo_por_regressao(reg, dp=1.0, roe=0.2, lpa=2.0, preco_corrente=20.0)
assert abs(pa_alto.preco_alvo - pa_clamp.preco_alvo)<1e-9, (pa_alto.preco_alvo, pa_clamp.preco_alvo)
assert pa_alto.payout_fora_faixa is True and pa_clamp.payout_fora_faixa is False
print('ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - payout 1.5 produz o mesmo preço-alvo que payout 1.0 (clampado).
    - `PrecoAlvo.payout_fora_faixa` é True quando o dp original saiu de [0,1], False caso contrário.
    - payout negativo é tratado (clamp para 0.0) sem exceção.
    - `pytest tests/ -q` passa (atualizar test_comparables apenas se houver caso que dependia de dp cru fora de faixa).
  </acceptance_criteria>
  <done>O Ranking aplica o mesmo clamp/sinalização de payout do Analisar antes da regressão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine → cálculo de preço-alvo | Entrada são números já calculados pela engine; sem I/O nem entrada de usuário direta aqui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | payout anômalo (dado de fonte externa) | mitigate | Clamp em [0,1] antes da previsão evita que dado anômalo distorça o preço-alvo; sinalizado via flag. |
</threat_model>

<verification>
- `pytest tests/ -q` verde.
- Smoke test inline confirma clamp e flag.
</verification>

<success_criteria>
- payout fora de [0,1] é clampado e sinalizado no preço-alvo do Ranking, igualando o tratamento do Analisar.
- Testes golden passam.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-de-consist-ncia/01-02-SUMMARY.md`
</output>
