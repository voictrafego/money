---
phase: 03-gr-fico-de-pre-o-na-aba-analisar
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app.py
autonomous: false

requirements: [GRAF-01, GRAF-02, GRAF-03]

must_haves:
  truths:
    - "Na aba Analisar, o usuário vê a linha do preço de close 5a com zoom e hover (Plotly) — D-03: gráfico no topo da aba, abaixo das métricas/veredito, antes de st.tabs"
    - "D-01: o intervalo intrínseco do DDM (a.vmin–a.vmax) vira uma banda horizontal sombreada (não linha única, não ponto médio); D-02: a banda é horizontal/plana ao longo dos 5 anos via add_hrect, sem recálculo de intrínseco histórico"
    - "D-04: estilo limpo — linha de preço + banda sutil, sem sombrear a área desconto/prêmio e sem marcar o ponto do preço atual"
    - "D-05: quando a série está indisponível, a aba mostra aviso claro espelhando o aviso 'preço atual indisponível' e NÃO quebra (segue para st.tabs)"
    - "D-06: quando o DDM não foi calculado (a.vmin/a.vmax None), o gráfico mostra só a linha de preço, sem banda"
  artifacts:
    - path: "app.py"
      provides: "Bloco de render Plotly na Tela 1, entre o loop de alertas e st.tabs (D-03)"
      contains: "st.plotly_chart"
  key_links:
    - from: "app.py Tela 1"
      to: "c.serie_precos"
      via: "leitura da série preservada no Plano 01 para go.Scatter"
      pattern: "c\\.serie_precos"
    - from: "app.py Tela 1"
      to: "a.vmin / a.vmax"
      via: "fig.add_hrect guardado por if a.vmin is not None and a.vmax is not None (D-01/D-02/D-06)"
      pattern: "add_hrect"
    - from: "app.py import"
      to: "plotly.graph_objects"
      via: "import plotly.graph_objects as go (topo de app.py)"
      pattern: "import plotly"
---

<objective>
Renderizar, na aba "Analisar" (Tela 1 do app.py), um gráfico Plotly da evolução do preço de
close dos últimos 5 anos (`c.serie_precos`, preservado no Plano 01), com a banda horizontal do
valor intrínseco do DDM (`a.vmin`–`a.vmax`) sobreposta — evidenciando a margem de segurança pela
posição relativa preço-vs-banda. Posição: no TOPO da aba, abaixo das métricas/veredito/alertas,
ANTES de `st.tabs` (D-03). Com os dois fallbacks que espelham padrões já existentes: série
indisponível → aviso sem quebrar (D-05/GRAF-03), DDM não calculado → só a linha sem banda (D-06).

Purpose: Entregar a UI de GRAF-01 (linha interativa 5a, zoom+hover nativos do Plotly) e GRAF-02
(banda intrínseca sobreposta), com degradação graciosa GRAF-03.
Output: Bloco de render no app.py + `import plotly.graph_objects as go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-CONTEXT.md
@.planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-RESEARCH.md
@.planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-PATTERNS.md
@.planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-01-SUMMARY.md

<interfaces>
<!-- Estruturas em escopo na Tela 1 do app.py (já existem; o Plano 01 adicionou c.serie_precos). -->

No bloco `if rodar and ticker:` → `else:` (app.py ~87-92), já em escopo:
  c = montar(ticker, ANO_BASE, N_ANOS)   # CompanyData; agora tem c.serie_precos (Plano 01)
  a = report.analisar_acao(c, CFG)       # AnaliseAcao; expõe a.vmin, a.vmax, a.preco_atual

Ponto de inserção EXATO (app.py 122-126) — entre o loop de alertas e st.tabs:
  if a.alertas:
      for al in a.alertas:
          st.warning(f"⚠️ {al}")
  # ◄── GRÁFICO ENTRA AQUI
  tab1, tab2, tab3 = st.tabs([...])   # NÃO MOVER esta linha

Padrões a espelhar (mesmo bloco):
  - Aviso de indisponibilidade (app.py 115-120): st.warning com tom "indisponível agora (fonte Yahoo instável)... seguem válidos".
  - Sentinela DDM (app.py 107): `if a.vmin is not None and a.vmax is not None`.
  - Título de seção (app.py 131,148,160,170): st.markdown("**...**") opcionalmente com help=h(...).

Imports de topo do app.py (11-19): import pandas as pd; import streamlit as st; libs da engine.
Plotly: go.Figure / go.Scatter / fig.add_hrect / st.plotly_chart — sem análogo no repo;
usar RESEARCH.md Pattern 2 + Code Examples (linhas 227-252) como forma de referência.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render Plotly da série 5a + banda DDM na Tela 1, com os dois fallbacks</name>
  <files>app.py</files>
  <read_first>
    - app.py (Tela 1 completa, bloco `if modo.startswith("🔎")` linhas 78-192; ponto de inserção 122-126; aviso de indisponibilidade 115-120; sentinela vmin/vmax 107; imports de topo 11-19)
    - .planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-RESEARCH.md (Pattern 2; Code Examples 227-252; Pitfall 2 width="stretch"; Pitfall 3 dropna/DatetimeIndex)
    - .planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-PATTERNS.md (seção app.py — 5 padrões: ponto de inserção, fallback série, fallback DDM, render de chart, título)
    - .planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-CONTEXT.md (D-01..D-06)
  </read_first>
  <action>
    Adicionar `import plotly.graph_objects as go` ao topo de app.py (junto dos demais imports
    de topo, ~11-19; plotly NUNCA entra em módulos da engine — Pitfall 4).
    D-03: inserir o bloco de gráfico ENTRE o loop de alertas (termina ~linha 124) e
    `tab1, tab2, tab3 = st.tabs([...])` (linha 126) — NÃO mover o `st.tabs` (gráfico no topo da
    aba, abaixo das métricas/veredito, antes dos sub-tabs).
    Acima do gráfico, um título de seção via `st.markdown("**Evolução do preço (5 anos) vs. valor
    intrínseco**")` no mesmo estilo dos cabeçalhos existentes (opcionalmente com `help=h(...)`).
    Ler a série com `serie = c.serie_precos` (já em escopo). D-05/GRAF-03 (fallback série
    indisponível): se `serie is None or len(serie) == 0`, mostrar `st.info`/`st.warning` espelhando
    o tom do aviso "preço atual indisponível" (app.py:115-120) — ex.: "Gráfico de preço
    indisponível agora (fonte Yahoo instável); os fundamentos e o valor intrínseco abaixo seguem
    válidos." — e NÃO desenhar o gráfico (a aba segue para `st.tabs`, não pode quebrar). Caso
    contrário, construir `fig = go.Figure()` com um `go.Scatter(x=serie.index, y=serie.values,
    mode="lines")` (linha de preço; hovertemplate com data `%{x|%d/%m/%Y}` e `R$ %{y:.2f}` —
    zoom/hover são nativos do Plotly, atendem GRAF-01). Banda intrínseca (GRAF-02): D-06 — guardada
    pela MESMA sentinela que a UI já usa (`if a.vmin is not None and a.vmax is not None`, espelhando
    app.py:107; DDM None → sem banda, só a linha). D-01/D-02: chamar `fig.add_hrect(y0=a.vmin,
    y1=a.vmax, line_width=0, fillcolor cor sutil, opacity baixa)` — banda HORIZONTAL PLANA entre
    vmin e vmax (não linha única, não ponto médio, sem recálculo de intrínseco histórico),
    opcionalmente com annotation discreta "Valor intrínseco (DDM)". D-04 (estilo limpo): NÃO
    sombrear a área desconto/prêmio nem marcar o ponto do preço atual (ambos deferidos). NÃO
    desenhar série de vmin/vmax por data (proibido recalcular intrínseco histórico — D-02).
    `fig.update_layout` com altura ~380 e margens enxutas; renderizar com
    `st.plotly_chart(fig, width="stretch")` — NÃO usar `use_container_width` (deprecado — Pitfall 2).
    Cor, opacidade, altura e annotation ficam à discrição do executor (D-04), respeitando "estilo
    limpo". Não tocar em mais nada do bloco (métricas, veredito, sub-tabs).
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && grep -n 'import plotly\|st.plotly_chart\|add_hrect\|c.serie_precos\|width="stretch"' app.py && ! grep -n 'use_container_width' app.py | grep -i plotly && ./.venv/bin/python -c "import ast,sys; ast.parse(open('app.py').read()); print('app.py parses OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `import plotly.graph_objects as go` está no topo de app.py.
    - Existe um bloco que lê `c.serie_precos` e, quando a série é válida, monta `go.Figure()` com
      `go.Scatter` e renderiza com `st.plotly_chart(fig, width="stretch")`.
    - A banda `fig.add_hrect(y0=a.vmin, y1=a.vmax, ...)` está guardada por
      `if a.vmin is not None and a.vmax is not None` (D-06).
    - Há fallback `if serie is None or len(serie) == 0` que mostra aviso (st.info/st.warning) e NÃO
      desenha o gráfico (D-05/GRAF-03) — a linha `st.tabs(...)` continua sendo executada depois.
    - O `st.plotly_chart` novo NÃO usa `use_container_width` (usa `width="stretch"`).
    - A linha `tab1, tab2, tab3 = st.tabs([...])` não foi movida e o gráfico está ANTES dela.
    - `./.venv/bin/python -c "import ast; ast.parse(open('app.py').read())"` não lança (sintaxe válida).
  </acceptance_criteria>
  <done>O gráfico Plotly (linha 5a + banda DDM) renderiza no topo da aba Analisar antes do st.tabs, com fallbacks para série e DDM ausentes; app.py tem sintaxe válida.</done>
</task>

<task type="auto">
  <name>Task 2: Confirmar que pytest segue verde e o app importa com plotly</name>
  <files>tests/</files>
  <read_first>
    - .planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-RESEARCH.md (Pitfall 1 pickle dataclass+Series; Pitfall 4 testes offline não importam app.py)
    - .planning/ROADMAP.md (Phase 3 SC #4: pytest verde)
  </read_first>
  <action>
    Rodar a suíte completa para confirmar que a mudança de UI (app.py) NÃO afetou os golden tests
    (os testes não importam app.py; esta é a trava de regressão final da fase — SC #4). NÃO editar
    testes para passar. Confirmar também que app.py importa sem erro agora que usa plotly
    (`./.venv/bin/python -c "import ast"` valida sintaxe; a importação real de plotly foi instalada
    no Plano 01). Se algum golden quebrar, a causa está numa mudança indevida — corrigir na origem.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && ./.venv/bin/python -m pytest -q && git diff --name-only -- tests/ | grep -c . | grep -qx 0 && echo "tests untouched + green"</automated>
  </verify>
  <acceptance_criteria>
    - `./.venv/bin/python -m pytest -q` sai com código 0.
    - Nenhum arquivo em `tests/` foi modificado (`git diff --name-only -- tests/` vazio).
    - `./.venv/bin/python -c "import plotly.graph_objects as go; print('ok')"` executa sem erro.
  </acceptance_criteria>
  <done>pytest verde com tests intocados; plotly importável.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Gráfico Plotly na aba "Analisar": linha do preço de close 5a (zoom + hover) com a banda
    horizontal do valor intrínseco do DDM (a.vmin–a.vmax) sobreposta, no topo da aba antes dos
    sub-tabs. Fallbacks: série indisponível → aviso sem quebrar; DDM não calculado → só a linha.
  </what-built>
  <how-to-verify>
    1. Rodar: `cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && ./.venv/bin/streamlit run app.py`
    2. No navegador, com modo "🔎 Analisar uma ação", analisar **TAEE11** (ou EGIE3/ITUB4).
    3. Confirmar que ABAIXO do veredito/métricas e ANTES dos sub-tabs (Múltiplos/Valuation/Fundamentos)
       aparece o gráfico de linha do preço dos últimos 5 anos.
    4. Verificar zoom (arrastar/scroll) e hover (passar o mouse → mostra data + R$). [GRAF-01]
    5. Verificar a banda horizontal sombreada do valor intrínseco (DDM) sobre a série — preço abaixo
       da banda = desconto; acima = prêmio. [GRAF-02]
    6. Confirmar que NÃO há warning de deprecação de `use_container_width` no terminal do Streamlit.
    7. Confirmar que NÃO há `UnserializableReturnValueError`/aviso de cache no terminal (Pitfall 1 —
       pickle do CompanyData com pd.Series).
    8. (Opcional) Analisar um ticker cujo DDM não calcula (vmin/vmax None) → o gráfico mostra só a
       linha, sem banda, e a aba não quebra. [GRAF-03/D-06]
  </how-to-verify>
  <resume-signal>Digite "approved" se o gráfico aparece com linha+banda e zoom/hover funcionam sem warnings; senão descreva o problema.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| c.serie_precos → render Plotly | A série vem do Yahoo (borda externa já tratada no Plano 01). O render é display read-only; nenhum input novo do usuário. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | DoS | app.py render com série None/vazia | mitigate | Fallback `if serie is None or len(serie)==0` mostra aviso e pula o gráfico — a aba segue para st.tabs (D-05/GRAF-03), não quebra. |
| T-03-04 | Tampering | a.vmin/a.vmax None na banda | mitigate | Banda guardada por `if a.vmin is not None and a.vmax is not None` (D-06) — sem KeyError/TypeError quando o DDM não calcula. |

Nota honesta: render de dado público read-only, sem input do usuário, rede nova, auth ou
segredos. Superfície de segurança efetivamente nula; os "threats" são robustez de UI (não quebrar),
já cobertos pelos fallbacks de D-05/D-06.
</threat_model>

<verification>
- `grep` em app.py mostra `import plotly`, `st.plotly_chart`, `add_hrect`, `c.serie_precos`, `width="stretch"`.
- O gráfico novo não usa `use_container_width`.
- `./.venv/bin/python -m pytest -q` sai 0; `tests/` intocado.
- Checkpoint humano confirma linha+banda, zoom/hover, fallbacks e ausência de warnings.
</verification>

<success_criteria>
- GRAF-01: linha interativa do preço 5a com zoom/hover (Plotly). ✔ checkpoint.
- GRAF-02: banda intrínseca DDM sobreposta evidenciando margem de segurança. ✔ checkpoint.
- GRAF-03/D-05/D-06: série indisponível → aviso sem quebrar; DDM None → só a linha. ✔
- SC #4: pytest verde, golden intocados.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/03-gr-fico-de-pre-o-na-aba-analisar/03-02-SUMMARY.md`.
</output>
