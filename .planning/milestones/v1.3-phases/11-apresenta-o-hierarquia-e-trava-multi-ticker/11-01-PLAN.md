---
phase: 11-apresenta-o-hierarquia-e-trava-multi-ticker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/report/presentation.py
  - tests/test_presentation_multiticker.py
autonomous: true
requirements: [TEST-08]
must_haves:
  truths:
    - "Os helpers de apresentação importam sem subir o Streamlit (sem efeito de import)"
    - "header_dy escolhe o DY recorrente como valor principal e cai p/ trailing quando recorrente é None"
    - "linhas_multiplos formata 'DY rec.' como % e emite duas linhas de payout distintas (cru x sustentável)"
    - "O golden de apresentação trava as propriedades nos 5 tickers e fica verde"
    - "Os golden de valuation seguem verdes SEM rebaseline"
  artifacts:
    - path: "src/analista/report/presentation.py"
      provides: "Helpers puros fmt_pct/fmt_num + header_dy + linhas_multiplos (sem Streamlit)"
      contains: "def header_dy"
      min_lines: 40
    - path: "tests/test_presentation_multiticker.py"
      provides: "Golden de propriedade multi-ticker da camada de apresentação (layer a)"
      contains: "from analista.report import presentation"
      min_lines: 60
  key_links:
    - from: "tests/test_presentation_multiticker.py"
      to: "src/analista/report/presentation.py"
      via: "import direto (NUNCA from app import ...)"
      pattern: "from analista.report import presentation"
    - from: "src/analista/report/presentation.py"
      to: "a.multiplos['DY rec.']"
      via: "ramo % inclui 'DY rec.' (paridade com report.py L397)"
      pattern: "DY rec\\."
---

<objective>
Criar o módulo puro `src/analista/report/presentation.py` com os helpers de apresentação que o
`app.py` (Plan 02) passará a chamar, e travá-los com um golden de propriedade multi-ticker. É a
**fundação** da fase (D-09): o helper precisa existir antes do rewiring do `app.py` e antes do
checkpoint live.

Purpose: extrair a lógica de formatação/montagem hoje inline no Streamlit (não-testável) para
helpers puros, importáveis SEM subir o Streamlit, e fechar a camada (a) da trava TEST-08.
Output: módulo puro + golden de apresentação verde nos 5 tickers (VULC3 + ITUB4/EGIE3/TAEE11/BBAS3),
sem tocar a engine de valuation (D-10 — nenhum golden de valuation rebaselina).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-CONTEXT.md
@.planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-UI-SPEC.md
@.planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-PATTERNS.md

<interfaces>
<!-- Campos da engine JÁ expostos (read-only) que os helpers consomem. NÃO recalcular. -->

src/analista/report/report.py — analisar_acao monta a.multiplos (L61-70):
  a.multiplos = {
    "ML", "ROE", "P/L", "EY",
    "DP (payout)": c.payout_valuation(),   # sustentável (mediana série completa, sem clamp)
    "CDC",
    "DY":      c.dy_atual(),                # trailing (inclui extraordinários — pode inflar)
    "DY rec.": c.dy_recorrente(),           # sustentável (earnings-based)
  }

src/analista/report/report.py — paridade CLI JÁ correta (L396-401):
  for k, v in a.multiplos.items():
    if k in ("ML","ROE","DP (payout)","DY","DY rec.","EY"):  # <- inclui "DY rec."
        mlin.append([k, _pct(v)])
    else:
        mlin.append([k, _num(v)])

src/analista/core/fundamentals.py — métodos (assinaturas):
  ultimo_ano() -> Optional[int]
  payout(ano: int) -> Optional[float]        # CRU, sem clamp (VULC3: 1.247)
  payout_valuation() -> Optional[float]       # sustentável (mediana, sem clamp)
  dy_atual() -> Optional[float]               # trailing
  dy_recorrente(...) -> Optional[float]       # sustentável; None em ano de prejuízo

app.py L51-56 — sentinelas de formatação a MOVER (em-dash "—", NÃO o hífen do report._pct):
  def fmt_pct(x, casas=1): return "—" if x is None else f"{x*100:.{casas}f}%"
  def fmt_num(x, casas=2): return "—" if x is None else f"{x:.{casas}f}"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar src/analista/report/presentation.py (helpers puros, sem Streamlit)</name>
  <files>src/analista/report/presentation.py</files>
  <read_first>
    - src/analista/report/report.py L1-18 (header de módulo: docstring amarrando ao método, `from __future__ import annotations`, typing Optional, SEM import streamlit) e L355-401 (`_pct`/`_num` com hífen — NÃO unificar; o ramo % do CLI L396-401 é a paridade-alvo que inclui "DY rec.")
    - app.py L51-56 (`fmt_pct`/`fmt_num` em-dash "—" — esta é a variante a MOVER, não a do report)
    - app.py L131-136 (header m3) e L317-327 (montagem buggada das linhas de Múltiplos — o que os helpers substituem)
    - .planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-UI-SPEC.md §Formatting Rules L95-117 e §Copywriting Contract L143-153 (strings pt-BR exatas)
  </read_first>
  <behavior>
    - fmt_pct(None) → "—"; fmt_pct(0.063) → segue f"{x*100:.1f}%" (ponto, NÃO vírgula — UI-SPEC trava o separador como está)
    - fmt_num(None) → "—"; fmt_num(0.9) → "0.90"
    - header_dy(dy_recorrente=0.063, dy_atual=0.203): retorna dict com value=fmt_pct(0.063), label="Dividend Yield (recorrente)", delta="trailing "+fmt_pct(0.203), delta_color="off", fallback=False
    - header_dy(dy_recorrente=None, dy_atual=0.203): fallback → value=fmt_pct(0.203), label="Dividend Yield (trailing)", delta=None, delta_color="off", fallback=True, help explica recorrente indisponível
    - header_dy(dy_recorrente=0.063, dy_atual=None): value=fmt_pct(0.063), delta=None (trailing ausente), fallback=False
    - linhas_multiplos(a.multiplos, payout_ult=1.247, payout_proj=0.43): linha ("Payout (último ano)", fmt_pct(1.247)) DISTINTA de ("Payout p/ valuation (sustentável)", fmt_pct(0.43)); a linha "DY rec." sai como fmt_pct(val) (termina em "%"), nunca fmt_num
  </behavior>
  <action>
    Criar módulo puro espelhando o header de `report.py` (docstring tie-in ao método, `from __future__ import annotations`, `from typing import Optional`). PROIBIDO `import streamlit` e qualquer efeito colateral de import (D-09).

    Mover (não importar do report) os sentinelas em-dash de app.py L51-56: `fmt_pct(x, casas=1)` = `"—" if x is None else f"{x*100:.{casas}f}%"` e `fmt_num(x, casas=2)` = `"—" if x is None else f"{x:.{casas}f}"`. NÃO unificar com `report._pct`/`_num` (hífen, superfície CLI — UI-SPEC trava em-dash p/ a superfície do app).

    `header_dy(dy_recorrente: Optional[float], dy_atual: Optional[float]) -> dict` (HIER-01 / D-01,D-02,D-03): se `dy_recorrente` não-None → valor principal = `fmt_pct(dy_recorrente)`, label `"Dividend Yield (recorrente)"`, delta = `"trailing " + fmt_pct(dy_atual)` quando `dy_atual` não-None senão None, `delta_color="off"` (cinza neutro — trava firme), `fallback=False`. Se `dy_recorrente` é None → valor principal = `fmt_pct(dy_atual)`, label `"Dividend Yield (trailing)"`, delta=None, `delta_color="off"`, `fallback=True`, help = `"DY recorrente indisponível (ano de prejuízo na janela) — exibindo o DY trailing."`. Retornar dict com chaves `label`, `value`, `delta`, `delta_color`, `help`, `fallback`.

    `linhas_multiplos(multiplos: dict, payout_ult: Optional[float], payout_proj: Optional[float]) -> list[tuple[str, str]]` (DYR-02 / PAY-02): iterar `multiplos.items()`; quando `k == "DP (payout)"` emitir DUAS linhas — `("Payout (último ano)", fmt_pct(payout_ult))` (cru) e `("Payout p/ valuation (sustentável)", fmt_pct(payout_proj))` (drop "(média 3a)", D-06); quando `k in ("ML","ROE","DY","DY rec.","EY")` → `(k, fmt_pct(val))` (incluir `"DY rec."` é o fix DYR-02 — paridade com report.py L397); senão → `(k, fmt_num(val))`.

    Texto exato do help do header (normal) fica a critério, seguindo o tom de `h("dy")`: nota curta de que o recorrente usa lucro normalizado × payout sustentável e o trailing (cinza) pode estar inflado por proventos extraordinários (Cap. 6).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import analista.report.presentation as p; assert p.fmt_pct(None)=='—'; assert p.fmt_pct(0.063).endswith('%'); assert p.header_dy(0.063,0.203)['delta_color']=='off'; assert p.header_dy(None,0.203)['fallback'] is True; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import analista.report.presentation"` sai 0 sem importar streamlit (módulo puro, D-09)
    - `header_dy(0.063, 0.203)` retorna `delta_color="off"`, `fallback=False`, `label="Dividend Yield (recorrente)"`, `value` termina em "%"
    - `header_dy(None, 0.203)` retorna `fallback=True`, `label="Dividend Yield (trailing)"`, `delta=None`
    - `linhas_multiplos({"DY rec.": 0.063, "DP (payout)": 0.43}, 1.247, 0.43)` produz a linha "DY rec." com valor terminando em "%" (não "0.06"), e duas linhas de payout com valores distintos
    - `grep -c "import streamlit" src/analista/report/presentation.py` == 0
  </acceptance_criteria>
  <done>Módulo puro criado, importável sem Streamlit; header_dy/linhas_multiplos/fmt_pct/fmt_num com o comportamento do bloco behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Golden de propriedade multi-ticker da apresentação (tests/test_presentation_multiticker.py)</name>
  <files>tests/test_presentation_multiticker.py</files>
  <read_first>
    - tests/test_payout_sustentavel_multiticker.py (ler inteiro — scaffold `_cfg()`/`_mk(...)`, docstring de framing, 1 comentário "pelo método" por assert, perfis VULC3 spike 124,7% / TAEE11 >100% / EGIE3 trailing inflado)
    - tests/test_growth_robusto_multiticker.py L1-60 (mesmo scaffold, confirmação cruzada)
    - src/analista/report/presentation.py (o módulo do Task 1 — alvo dos asserts)
    - src/analista/report/report.py L61-70 (montagem de a.multiplos) e L155-158 (payout_ult = c.payout(ult) CRU — a fronteira que o teste replica)
  </read_first>
  <action>
    Criar o golden offline de PROPRIEDADE (layer a da trava D-08). Reusar verbatim o `_cfg()` (carrega config.yaml shipado) e o construtor sintético `_mk(...)` do analog (séries por ano consistentes; usar `dpa_trailing` p/ casos de trailing inflado tipo EGIE3). Importar `from analista.report import presentation` e `from analista.report import report` — NUNCA `from app import ...` (D-09 rejeita).

    Travar as propriedades nos 5 perfis (1 comentário "pelo método" por assert):
    - VULC3 (recorrente 0.43 em 9 anos + 1 spike): `c.payout(c.ultimo_ano())` ≈ 1.247 cru e `c.payout_valuation()` ≈ 0.43 sustentável → as linhas de `presentation.linhas_multiplos(...)` "Payout (último ano)" e "Payout p/ valuation (sustentável)" são strings DISTINTAS; a linha "DY rec." termina em "%" (== `presentation.fmt_pct(a.multiplos["DY rec."])`), nunca a forma decimal de `fmt_num`.
    - Perfil normal estável (EGIE3/ITUB4/BBAS3 spirit): `header_dy(a.multiplos["DY rec."], a.multiplos["DY"])` escolhe o recorrente como principal (`fallback=False`), `delta_color="off"`; tickers normais não regridem (DY rec. <= trailing sob trailing inflado).
    - TAEE11 spirit (payout >100% recorrente): payout sustentável preservado >1.0 na linha "p/ valuation" (não rebaixado), e a linha continua formatada como %.
    - Fallback: construir um perfil de prejuízo (lucro normalizado indefinido → `dy_recorrente()` None) e assertar `header_dy(None, trailing)['fallback'] is True` e label "Dividend Yield (trailing)".

    Montar `a = report.analisar_acao(c, _cfg())` para obter `a.multiplos` real da engine (read-only) e passá-lo aos helpers, espelhando o consumo do app.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_presentation_multiticker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_presentation_multiticker.py -x -q` sai 0
    - O teste importa `from analista.report import presentation`; `grep -c "from app import" tests/test_presentation_multiticker.py` == 0
    - Assert VULC3: linha "Payout (último ano)" != linha "Payout p/ valuation (sustentável)" (valores distintos)
    - Assert "DY rec." da tabela termina em "%" e == `presentation.fmt_pct(a.multiplos["DY rec."])`
    - Assert fallback: `header_dy(None, <trailing>)['fallback'] is True`
    - Suíte completa segue verde SEM rebaseline: `.venv/bin/pytest -q` sai 0 (nenhum golden de valuation alterado — D-10)
  </acceptance_criteria>
  <done>Golden de apresentação verde nos 5 perfis; suíte inteira verde sem rebaseline de valuation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Fase de apresentação local-only sobre dados de mercado já sanitizados (CVM/Yahoo/BCB). Sem novo input, sem endpoint de rede, sem auth, sem escrita em DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | presentation.py (helpers puros) | accept | Superfície read-only, sem input externo novo; helpers só formatam campos já calculados pela engine. Nenhuma mitigação requerida (ASVS L1 não aplica — sem entrada não confiável). |
</threat_model>

<verification>
- `.venv/bin/python -c "import analista.report.presentation"` sai 0 (puro, sem Streamlit)
- `.venv/bin/pytest tests/test_presentation_multiticker.py -x -q` verde
- `.venv/bin/pytest -q` (suíte inteira) verde SEM rebaseline de golden de valuation
</verification>

<success_criteria>
- Módulo `presentation.py` puro e importável sem Streamlit, com `header_dy`/`linhas_multiplos`/`fmt_pct`/`fmt_num`
- "DY rec." formatável como % e payout cru distinto do sustentável travados por golden nos 5 tickers
- Engine de valuation intocada → golden de valuation verdes sem rebaseline (D-10)
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/11-apresenta-o-hierarquia-e-trava-multi-ticker/11-01-SUMMARY.md`
</output>
