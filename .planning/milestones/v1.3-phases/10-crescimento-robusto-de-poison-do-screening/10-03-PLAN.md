---
phase: 10-crescimento-robusto-de-poison-do-screening
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - tests/test_growth_robusto_multiticker.py
  - tests/test_growth_reconciliacao.py
  - tests/test_screening.py
  - tests/test_report.py
  - tests/test_comparables.py
  - tests/test_consistencia_modos.py
  - tests/test_ddm.py
autonomous: false
requirements: [GROW-01, GROW-02]
must_haves:
  truths:
    - "Em VULC3 (recorrente + 1 ano extraordinario) o ano atipico NAO infla o g exibido nem os fatores do BSD"
    - "Em tickers normais (ITUB4/EGIE3/TAEE11/BBAS3) o g exibido e o ranqueamento NAO regridem materialmente vs. o estado atual"
    - "TAEE11 (payout ~= 2.16) NAO distorce o preco-alvo/Ranking apos o clamp de entrada do fit"
    - "Toda a suite golden volta a ficar verde — verde de verdade OU rebaselinada DELIBERADAMENTE com o delta por ticker documentado (nunca silenciosamente)"
    - "Qualquer movimento de bucket dos fatores crescimento_*_3a (troca de janela 3a -> serie completa winsorizada contra as bandas REFERENCIA_BSD, que NAO mudam) e inspecionado nos tickers normais e anotado no SUMMARY"
  artifacts:
    - path: "tests/test_growth_robusto_multiticker.py"
      provides: "Golden OFFLINE de propriedade multi-ticker do estimador robusto + de-poison (espelha test_payout_sustentavel_multiticker.py)"
      contains: "def test_"
      min_lines: 60
  key_links:
    - from: "tests/test_growth_robusto_multiticker.py"
      to: "report.analisar_acao + screening.indicadores_bsd + comparables.ajustar_regressao_pl"
      via: "perfis sinteticos VULC3/TAEE11/normais com asserts de propriedade"
      pattern: "analisar_acao|indicadores_bsd|ajustar_regressao_pl"
---

<objective>
Trava de aceite do marco: validacao multi-ticker EXPLICITA (criterio de aceite, nao opcional) +
rebaseline DELIBERADO dos golden afetados pela troca CAGR -> log-linear + confirmacao live nos 5
tickers reais.

Purpose: garantir que o de-poison vale para QUALQUER ticker por regra geral — VULC3 e so o
diagnostico. Provar que (a) VULC3 nao infla g/BSD, (b) tickers normais nao regridem, (c) TAEE11
nao distorce o preco-alvo apos o clamp do fit. O rebaseline e a parte que o CLAUDE.md exige ser
deliberada: golden mudam de valor com justificativa por ticker, nunca "verde a qualquer custo".

Output: novo golden offline de propriedade multi-ticker + goldens existentes verdes ou
rebaselinados com delta documentado + checkpoint human-verify dos 5 tickers reais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-crescimento-robusto-de-poison-do-screening/10-CONTEXT.md
@.planning/phases/10-crescimento-robusto-de-poison-do-screening/10-PATTERNS.md

<interfaces>
<!-- Template a espelhar e contratos. Use direto. -->

Template do golden offline de propriedade multi-ticker (espelhar forma e espirito):
- tests/test_payout_sustentavel_multiticker.py — helper `_mk(ticker, lucros, divs, ...)` que monta
  CompanyData sintetica de N anos; cada teste trava uma PROPRIEDADE do metodo (nao um numero de
  mercado); 1 comentario "pelo metodo (D-xx)" por assert; usa config.yaml shipado para
  report.analisar_acao; offline (sem rede).

Pontos de calculo desta fase (ja entregues nos Plans 01/02):
- growth.crescimento_log_linear(serie) -> Number   (Plan 01)
- report.analisar_acao(c, cfg).g_historico         (Plan 01: log-linear sobre serie_lucro_normalizada)
- screening.indicadores_bsd(c)["crescimento_lucro_3a"/"_dividendos_3a"/"_fc_3a"]   (Plan 02: log-linear sobre serie winsorizada)
- comparables.ajustar_regressao_pl(pl, dp, roe)    (Plan 02: dp clampado em [0,1] no fit)

Goldens com risco de mudar de valor (10-PATTERNS.md L197-203):
- tests/test_growth_reconciliacao.py — asserts de DESIGUALDADE sobre a.g_historico (L65,82,100,121);
  series monotonas tendem a manter a desigualdade, mas CONFIRMAR rodando.
- tests/test_screening.py — fatores crescimento_*_3a (varios perfis usam series planas -> log-linear=0=CAGR=0).
- tests/test_consistencia_modos.py, tests/test_comparables.py, tests/test_ddm.py — impacto indireto a verificar.

CLI (para o checkpoint live): subcomandos `analisar <ticker>`, `rank --tickers A,B,C`, `screen --tickers ...`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Golden OFFLINE de propriedade multi-ticker (robustez + de-poison)</name>
  <files>tests/test_growth_robusto_multiticker.py</files>
  <read_first>
    - tests/test_payout_sustentavel_multiticker.py (TODO — espelhar helper _mk, estilo dos asserts de propriedade, docstring, uso de config.yaml shipado)
    - .planning/phases/10-crescimento-robusto-de-poison-do-screening/10-CONTEXT.md (<specifics>: os 3 asserts de aceite a,b,c)
    - src/analista/core/growth.py, src/analista/core/screening.py, src/analista/core/comparables.py (as funcoes a exercitar)
  </read_first>
  <action>
    Criar tests/test_growth_robusto_multiticker.py espelhando a forma de
    test_payout_sustentavel_multiticker.py (helper _mk sintetico, offline, config.yaml shipado, 1
    comentario "pelo metodo" por assert). Cobrir as TRES propriedades de aceite da fase:
    (a) PERFIL VULC3 — serie de lucro recorrente (ex.: ~1000/ano em 9 anos) + 1 ano extraordinario
        (ex.: 4000): assert que report.analisar_acao(...).g_historico (log-linear) NAO e inflado pelo
        spike — comparar contra o CAGR endpoint-a-endpoint do mesmo par (growth.cagr(serie[0],serie[-1],
        n)) e assert g_historico < esse CAGR quando o spike esta na ponta; e que
        screening.indicadores_bsd(...)["crescimento_lucro_3a"] == g_historico (consistencia por
        construcao D-04) e tambem nao inflado.
    (b) PERFIS NORMAIS (espirito ITUB4/EGIE3/TAEE11/BBAS3) — series de crescimento plausivel e estavel:
        assert g_historico finito, sinal coerente com a trajetoria (crescente->positivo), e que o
        ranqueamento/BSD nao colapsa (fatores crescimento_*_3a finitos e na ordem esperada).
    (c) PERFIL TAEE11 (payout >100% recorrente) — montar vetores PL/DP/ROE com uma linha de payout
        ~2.16 e assert que comparables.ajustar_regressao_pl produz os MESMOS coeficientes que com essa
        linha clampada em 1.0 (o fit nao "ve" >1.0 — de-poison D-06), e que o preco-alvo resultante
        e finito/sensato.
    Cada assert deve travar uma PROPRIEDADE (desigualdade/igualdade estrutural), nao um numero de
    mercado real — a confirmacao contra os numeros reais e o checkpoint da Task 3.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -m pytest tests/test_growth_robusto_multiticker.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo tests/test_growth_robusto_multiticker.py existe com >=4 funcoes test_ (VULC3, normais, TAEE11 fit, consistencia screening<->analisar).
    - Ha um assert explicito comparando g_historico log-linear contra o CAGR endpoint (`growth.cagr`) no perfil VULC3, provando que o spike nao infla (criterio a).
    - Ha um assert de igualdade de coeficientes de ajustar_regressao_pl com payout 2.16 vs 1.0 (criterio c).
    - Ha um assert `indicadores_bsd(c)["crescimento_lucro_3a"] == report.analisar_acao(c,cfg).g_historico` (consistencia D-04).
    - pytest tests/test_growth_robusto_multiticker.py sai 0.
  </acceptance_criteria>
  <done>Golden offline de propriedade multi-ticker verde, travando os 3 criterios de aceite (a/b/c) por regra geral e a consistencia Analisar<->Screening.</done>
</task>

<task type="auto">
  <name>Task 2: Rodar a suite completa e rebaselinar goldens afetados DELIBERADAMENTE</name>
  <files>tests/test_growth_reconciliacao.py, tests/test_screening.py, tests/test_report.py, tests/test_comparables.py, tests/test_consistencia_modos.py, tests/test_ddm.py</files>
  <read_first>
    - tests/test_growth_reconciliacao.py (asserts de desigualdade sobre a.g_historico — ver se sobrevivem ao log-linear)
    - tests/test_screening.py (fatores crescimento_*_3a + classificacao de banda REFERENCIA_BSD)
    - tests/test_report.py, tests/test_comparables.py, tests/test_consistencia_modos.py, tests/test_ddm.py (golden de VALOR EXATO movidos dos Plans 01/02 para CA — este e o unico ponto onde rodam como gate)
    - 10-PATTERNS.md L197-203 (lista de goldens com risco + convencao de rebaseline deliberado)
    - CLAUDE.md (golden devem passar OU ser rebaselinados deliberadamente documentando o delta por ticker)
  </read_first>
  <action>
    Rodar a suite COMPLETA (`pytest -q`). Para CADA teste que falhar por mudanca de VALOR causada pela
    troca CAGR -> log-linear (Plans 01/02): confirmar que o novo numero esta CORRETO pelo metodo
    (estimativa robusta sobre serie normalizada), entao atualizar o valor esperado E adicionar/ajustar
    um comentario no teste documentando o delta (valor antigo -> novo) e o porque (log-linear sobre
    serie normalizada robustece o g; um ano de ponta deixou de mandar). NUNCA afrouxar um assert de
    propriedade so para passar — se um assert de propriedade quebrar (ex.: g_alto == 0 deixou de
    valer), isso e um BUG de implementacao, nao caso de rebaseline: parar e investigar. Goldens de
    desigualdade em test_growth_reconciliacao.py (series monotonas) provavelmente sobrevivem — so
    rebaselinar os que mudarem de valor de fato. ESTE e o ponto unico onde os golden de VALOR EXATO
    pre-existentes rodam como gate (test_report.py, test_screening.py, test_comparables.py,
    test_consistencia_modos.py, test_ddm.py) — eles foram tirados dos Plans 01/02 de proposito para
    nao travar o executor com falha falsa antes do rebaseline. ATENCAO (Warning checker): os fatores
    crescimento_*_3a trocaram de ESTIMADOR e de JANELA (3a -> serie completa winsorizada); como as
    bandas REFERENCIA_BSD foram calibradas contra CAGR de 3 anos, um normal pode MUDAR DE BUCKET de
    crescimento. NAO alterar as bandas (D-07 — sao chaves de scoring): apenas anotar qualquer
    movimento de bucket de crescimento_*_3a na tabela de rebaseline. Registrar a lista
    "ticker/teste: antigo -> novo + justificativa" (incluindo movimentos de bucket) no SUMMARY
    deste plano (delta deliberado).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` sai com codigo 0 (suite inteira verde).
    - Todo golden cujo valor mudou tem um comentario no codigo do teste documentando o delta (antigo -> novo) e a justificativa metodologica.
    - Nenhum assert de PROPRIEDADE foi afrouxado/removido para passar (ex.: as desigualdades de test_growth_reconciliacao e os contrastes g_fund<=0 vs >0 seguem intactos).
    - O SUMMARY lista cada rebaseline por ticker/teste com antigo -> novo + porque.
    - Qualquer movimento de bucket dos fatores crescimento_*_3a (mudanca de classificacao de banda REFERENCIA_BSD por causa da troca janela 3a -> serie completa) esta anotado explicitamente na tabela de rebaseline do SUMMARY; as bandas REFERENCIA_BSD NAO foram alteradas (D-07).
  </acceptance_criteria>
  <done>Suite completa verde; toda mudanca de golden e deliberada, documentada por ticker/teste com justificativa; nenhuma propriedade afrouxada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmacao LIVE dos 5 tickers reais</name>
  <read_first>
    - src/analista/cli.py L176-195 (subcomandos analisar/screen/rank e flags)
    - .planning/phases/09-payout-sustent-vel-dy-recorrente-n-cleo-de-metodologia/09-03-SUMMARY.md (numeros-alvo do checkpoint live da Fase 9, se disponivel, para comparar)
  </read_first>
  <what-built>
    O g_historico do Analisar e os fatores de crescimento do BSD agora vem da regressao log-linear
    sobre a serie normalizada (Plans 01/02); a regressao de preco-alvo do Ranking clampa o payout no
    fit (D-06). O executor ja rodou a suite offline inteira verde (Tasks 1-2). Falta a confirmacao
    contra os numeros REAIS dos 5 tickers (exige rede — vive fora dos testes).
  </what-built>
  <how-to-verify>
    1. Rodar `python -m analista.cli analisar VULC3` (ou o entry equivalente). Confirmar que o
       g_historico exibido NAO esta inflado por um ano extraordinario (esta junto da trajetoria
       recorrente, nao num CAGR de ponta).
    2. Rodar `python -m analista.cli analisar` para ITUB4, EGIE3, TAEE11, BBAS3. Confirmar que o g
       exibido de cada um NAO regrediu materialmente vs. o estado anterior (trajetoria coerente).
    3. Rodar `python -m analista.cli rank --tickers VULC3,ITUB4,EGIE3,TAEE11,BBAS3`. Confirmar que o
       ranqueamento dos tickers normais nao colapsa e que o preco-alvo de TAEE11 (payout ~= 2.16) esta
       sensato (nao distorcido) apos o clamp do fit.
    4. Rodar `python -m analista.cli screen --tickers VULC3,ITUB4,EGIE3,TAEE11,BBAS3`. Confirmar que
       VULC3 nao e inflado pelo ano extraordinario no BSD. ALEM do g: como os fatores crescimento_*_3a
       trocaram de janela (3a -> serie completa winsorizada) e as bandas REFERENCIA_BSD foram
       calibradas contra CAGR de 3 anos, inspecionar EXPLICITAMENTE as CLASSIFICACOES de banda do BSD
       (a nota/bucket de cada crescimento_*_3a, nao so o g) para ITUB4/EGIE3/TAEE11/BBAS3 — confirmar
       que nenhum normal muda de bucket de forma que distorca o ranqueamento. As bandas NAO sao
       alteradas (D-07).
  </how-to-verify>
  <acceptance_criteria>
    - VULC3: g exibido e BSD nao inflados pelo ano extraordinario (criterio a).
    - ITUB4/EGIE3/TAEE11/BBAS3: g e ranqueamento nao regridem materialmente (criterio b).
    - TAEE11: preco-alvo/Ranking sensato apos o clamp do fit (criterio c).
    - As CLASSIFICACOES de banda (buckets) dos fatores crescimento_*_3a dos tickers normais foram inspecionadas; qualquer movimento de bucket esta anotado no SUMMARY e nao distorce o ranqueamento (bandas REFERENCIA_BSD inalteradas, D-07).
    - Usuario aprova explicitamente os pontos acima OU descreve a regressao observada.
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" ou descreva qualquer regressao observada (g inflado, ranqueamento colapsado, preco-alvo distorcido).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados reais (rede, so no checkpoint) -> CLI/app | Leitura de dados publicos (CVM/Yahoo/BCB) no entry point, fora da engine pura. Sem auth, sem input de usuario novo, sem escrita. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Denial of Service | checkpoint live (rede) | accept | Degradacao graciosa ja existente nos entry points (ticker sem dados -> campos None); o checkpoint e manual e nao integra ao fluxo automatico. Risco baixo, dado publico. |
| T-10-08 | Information Disclosure | golden offline | accept | N/A: perfis sinteticos, sem dado real/PII no codigo de teste. |
</threat_model>

<verification>
- pytest -q (suite COMPLETA) sai 0.
- tests/test_growth_robusto_multiticker.py verde (3 criterios de aceite a/b/c + consistencia D-04).
- Todo rebaseline documentado por ticker/teste no SUMMARY (delta deliberado).
- Checkpoint live aprovado pelo usuario (5 tickers reais).
</verification>

<success_criteria>
- Validacao multi-ticker (VULC3 + ITUB4/EGIE3/TAEE11/BBAS3) passa por regra geral, offline e live.
- Golden suite verde com rebaseline deliberado e justificado (extensao do invariante TEST-07; antecipa TEST-08).
- VULC3 nao infla g/BSD; normais nao regridem; TAEE11 nao distorce preco-alvo.
</success_criteria>

<output>
After completion, create `.planning/phases/10-crescimento-robusto-de-poison-do-screening/10-03-SUMMARY.md`
(incluir a tabela de rebaseline por ticker/teste: antigo -> novo + justificativa, E anotar qualquer movimento de bucket de crescimento_*_3a contra as bandas REFERENCIA_BSD inalteradas)
</output>
