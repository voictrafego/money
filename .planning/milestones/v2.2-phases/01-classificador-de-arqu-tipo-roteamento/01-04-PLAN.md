---
phase: 01-classificador-de-arqu-tipo-roteamento
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
autonomous: true
gap_closure: true
requirements: [ARQ-01]

must_haves:
  truths:
    - "A UI Streamlit (app.py) exibe 'Arquétipo: {a.arquetipo} → motor {a.motor}' antes do bloco de valuation, no mesmo nível do caption 'Setor/Estágio'"
    - "A exibição aparece inclusive para o caso não-suspenso (pagadora_regulada / TAEE11, motor='ddm'), não só para arquétipos com motor_pendente"
  artifacts:
    - path: "app.py"
      provides: "caption com a.arquetipo e a.motor junto ao caption Setor/Estágio (~linha 881)"
      contains: "a.arquetipo"
  key_links:
    - from: "app.py (render principal)"
      to: "a.arquetipo / a.motor"
      via: "st.caption ao lado de Setor/Estágio"
      pattern: "a\\.arquetipo"
---

<objective>
Fechar o Gap 2 (partial, SC#1 exposição na UI) da 01-VERIFICATION.md: `app.py` — a interface
Streamlit primária e documentada do projeto — nunca referencia `a.arquetipo`/`a.motor` no
fluxo principal de renderização. Para arquétipos com `motor_pendente` o nome vaza
indiretamente pelo texto do veredito, mas para a pagadora regulada (TAEE11, motor='ddm',
não suspenso) a UI não expõe a classificação em lugar nenhum. Só o CLI
(`report.relatorio_markdown`, report.py:452) tem a linha dedicada "Arquétipo: X → motor Y".

Purpose: paridade CLI/UI para a SC#1 ("classifica e exibe o arquétipo antes do bloco de
valuation"), com uma mudança de render aditiva, read-only.
Output: linha de arquétipo/motor visível na UI, sempre, junto ao caption Setor/Estágio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-VERIFICATION.md
@app.py

<interfaces>
<!-- Campos já derivados na engine e o formato de referência do CLI. Sem recálculo na UI. -->

Campos em AnaliseAcao (src/analista/report/report.py:52-56), já populados por analisar_acao:
- a.arquetipo: str      # chave do classificador (ex.: "pagadora_regulada", "financeira", "crescimento")
- a.motor: str          # "ddm" ou "pendente_fase_2"
- a.arquetipo_fronteirico: bool
- a.arquetipo_candidatos: List[str]
- a.motor_pendente: bool

Formato de referência do CLI (report.py:452) — a UI deve espelhar:
  *Arquétipo:* {a.arquetipo or '-'} → motor {a.motor or '-'}

Ponto de inserção na UI (app.py:880-881, dentro do ramo `else:` de dados OK, ANTES do
bloco de veredito na linha ~884):
  st.markdown(f"### {a.ticker} — {a.nome}")
  st.caption(f"Setor: {a.setor or '—'}  ·  Estágio: {a.estagio}")

`esc_md(...)` já é usado no arquivo para escapar markdown em conteúdo dinâmico.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: expor Arquétipo → motor no caption da UI Streamlit</name>
  <files>app.py</files>
  <read_first>
    - app.py (bloco de render principal :879-921; caption Setor/Estágio :881)
    - src/analista/core/arquetipo.py (para entender as chaves possíveis de a.arquetipo)
    - src/analista/report/report.py (:452 — formato de referência do CLI a espelhar; :52-56 — campos de AnaliseAcao)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-VERIFICATION.md (gap 2)
  </read_first>
  <action>No fluxo principal de render de `app.py` (ramo `else:` de dados OK), exibir a classificação de arquétipo antes do bloco de veredito (que começa em ~:884). Espelhar o formato do CLI (report.py:452): mostrar "Arquétipo: {a.arquetipo} → motor {a.motor}". Implementar acrescentando ao caption existente da linha 881 (que hoje mostra só "Setor: … · Estágio: …") um terceiro segmento "· Arquétipo: {a.arquetipo or '—'} → motor {a.motor or '—'}", OU emitindo um `st.caption` adicional imediatamente após a linha 881 — escolha a que ler melhor. Usar `esc_md(...)` em `a.arquetipo`/`a.motor` (conteúdo dinâmico), consistente com o resto do arquivo. Deve renderizar SEMPRE, inclusive para pagadora_regulada/TAEE11 (motor='ddm', não suspenso) — sem condicionar a `a.motor_pendente`. Mudança aditiva e read-only: só LER campos já derivados na engine (`a.arquetipo`/`a.motor`), zero fórmula, zero recálculo, nenhuma alteração em report.py/arquetipo.py.</action>
  <verify>
    <automated>grep -n "a.arquetipo" app.py && python -c "import ast; ast.parse(open('app.py').read())" && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "a.arquetipo" app.py` >= 1 e `grep -c "a.motor" app.py` >= 1 (a UI agora referencia os campos).
    - A referência está no fluxo principal de render (dentro do ramo `else:` de dados OK, próxima ao caption :881), ANTES do bloco de veredito.
    - A exibição NÃO está condicionada a `a.motor_pendente` (aparece também para o caso não-suspenso TAEE11/pagadora_regulada).
    - `python -c "import ast; ast.parse(open('app.py').read())"` sai sem erro (arquivo válido).
    - `python -m pytest -q` continua verde (>= 354 passed; app.py não tem testes que quebrem, mudança é read-only).
    - `git diff --name-only` == `app.py` (nenhum outro arquivo tocado).
  </acceptance_criteria>
  <done>A UI Streamlit exibe explicitamente "Arquétipo: X → motor Y" junto ao Setor/Estágio, para todos os arquétipos (suspensos e não-suspensos), em paridade com o CLI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Mudança de render read-only: apenas lê `a.arquetipo`/`a.motor` (strings derivadas de um enum interno fixo de 5 chaves + 2 nomes de motor), já escapadas via `esc_md`. Sem nova entrada de usuário, API, secret ou I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Injection (XSS/markdown) | render de `a.arquetipo`/`a.motor` na UI | mitigate | Valores vêm de um enum interno fixo (não de input do usuário), e ainda assim passam por `esc_md(...)` como o resto do arquivo — defesa em profundidade. |
| T-01-04-02 | Information Disclosure | caption na UI | accept | Expõe apenas a chave de arquétipo/motor, informação já pública no CLI e no propósito do produto; sem dado sensível. Superfície mínima documentada honestamente. |
</threat_model>

<verification>
- `grep -n "a.arquetipo" app.py` mostra a referência no render principal.
- `python -m pytest -q` → >= 354 passed, 0 failed.
- Inspeção manual opcional: `streamlit run app.py`, analisar TAEE11 → o caption exibe "Arquétipo: pagadora_regulada → motor ddm".
</verification>

<success_criteria>
- Gap 2 fechado: a UI Streamlit exibe o arquétipo/motor antes do valuation, inclusive no caso não-suspenso (pagadora_regulada), em paridade com o CLI.
- Mudança aditiva, read-only; nenhum outro arquivo tocado; suíte verde.
</success_criteria>

<output>
After completion, create `.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-04-SUMMARY.md`
</output>
