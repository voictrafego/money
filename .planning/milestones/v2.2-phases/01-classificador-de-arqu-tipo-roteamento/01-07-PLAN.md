---
phase: 01-classificador-de-arqu-tipo-roteamento
plan: 07
type: tdd
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - src/analista/report/report.py
  - app.py
  - tests/test_guardrails_ddm.py
autonomous: true
requirements: [SAN-01]

must_haves:
  truths:
    - "a faixa intrínseca do DDM NÃO é emitida quando vmax <= 0 (HAPV3: −2,20 a −1,66; PCAR3: −7,67 a −5,95)"
    - "a faixa intrínseca do DDM NÃO é emitida quando degenerada vmin==0 e vmax==0 (PRIO3: 0,00–0,00)"
    - "nesses casos o relatório/UI sinaliza honestamente que o DDM é estruturalmente inaplicável, em vez de mostrar faixa negativa/zero"
    - "casos com faixa válida positiva (TAEE11, EGIE3, SBSP3) seguem exibindo a faixa normalmente — suíte verde"
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "guarda-corpo de emissão: faixa DDM degenerada/inválida suprimida na borda, sem tocar ddm.py"
      contains: "vmax"
    - path: "app.py"
      provides: "exibição honesta: não mostra faixa negativa/zero; nota de DDM inaplicável"
    - path: "tests/test_guardrails_ddm.py"
      provides: "goldens dos casos degenerados (vmax<=0, 0-0) e do caso válido (não suprime)"
  key_links:
    - from: "src/analista/report/report.py::analisar_acao"
      to: "AnaliseAcao.vmin/vmax (borda de emissão da faixa)"
      via: "guarda pós-cálculo da matriz de sensibilidade"
      pattern: "vmax"
    - from: "app.py (render)"
      to: "a.vmin/a.vmax"
      via: "métrica Intrínseco (DDM) — supressão quando inaplicável"
      pattern: "Intr.nseco"
---

<objective>
Fechar o Achado 2 da 01-AUDIT-COERENCIA.md (guarda-corpo do DDM — Fase 3 PUXADA, independe da
Fase 2): mesmo onde o DDM roda, ele produz faixas matematicamente inválidas — NEGATIVAS (HAPV3
−2,20 a −1,66; PCAR3 −7,67 a −5,95) ou DEGENERADAS (PRIO3 0,00–0,00). Hoje esses números aparecem
na seção DDM do relatório/UI mesmo com o veredito suspenso, quebrando a coerência (uma ação
"valendo" R$ negativo).

Guarda-corpo defensivo e read-only sobre o veredito: NÃO emitir/exibir a faixa quando `vmax <= 0`
ou degenerada (0–0), e sinalizar honestamente que o DDM é estruturalmente inaplicável ali. NÃO
tocar as fórmulas de core/ddm.py — só a borda de emissão (report.py) e de exibição (app.py).

Purpose: uma faixa negativa/zero é ruído que o usuário lê como preço-alvo; suprimi-la é o mínimo de
honestidade. (Ambiente-limite: os casos degenerados observados são todos motor_pendente/suspensos,
então o guarda-corpo não altera veredito válido existente.)
Output: faixa DDM degenerada suprimida + nota honesta; casos válidos intactos.
NÃO ESCOPO (Fase 2 / Fase 3): normalizar lucro no pico do ciclo (PETR4 56–91, upside falso — é
lucro-normalizado da Fase 2, faixa POSITIVA logo fora deste guarda-corpo); reconciliação/ensemble.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-AUDIT-COERENCIA.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-AUDIT-COERENCIA-DATA.json

<interfaces>
<!-- Extraído de report.py e app.py. Executor não precisa explorar. -->
De src/analista/report/report.py (analisar_acao):
  # linhas 194-202: vmin/vmax = min/max das células da matriz de sensibilidade (ou fallback 2 cenários)
  celulas_sens = [v for linha in (a.sensibilidade or []) for v in linha if v is not None]
  if celulas_sens: a.vmin, a.vmax = min(...), max(...)
  # linha 203: if a.motor_pendente: veredito suspenso "VERIFICAR ..." (todos os casos degenerados caem aqui hoje)
  # linha 220: elif a.vmin is not None and a.vmax is not None and a.preco_atual: SUB/SOBRE/NO INTERVALO
  # relatorio_markdown linhas 494-523: tabela DDM + "_DDM não calculado ..._" quando a.ddm_constante is None

De app.py (render, linhas 912-916):
  intervalo = f"{fmt_rs(a.vmin)} – {fmt_rs(a.vmax)}" if a.vmin is not None and a.vmax is not None else "—"
  m2.metric("Intrínseco (DDM)", esc_md(intervalo), help=h("valor_intrinseco"))

Fonte dos casos (01-AUDIT-COERENCIA-DATA.json): HAPV3 vmin=-2.20/vmax=-1.66; PCAR3 -7.67/-5.95;
  PRIO3 0.0/0.0; válidos: TAEE11 29.03/47.06, EGIE3 18.42/29.84, SBSP3 6.16/9.75 (positivos — NÃO suprimir).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Guarda-corpo de emissão — suprimir faixa DDM degenerada/inválida em report.py</name>
  <files>src/analista/report/report.py, tests/test_guardrails_ddm.py</files>
  <read_first>
    - src/analista/report/report.py (bloco vmin/vmax linhas 189-243; motor_pendente linha 203; render 494-527)
    - src/analista/report/selo.py (firewall selo↛report — NÃO importar report; a.selo montado em report.py:343-347)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-AUDIT-COERENCIA-DATA.json (valores exatos dos casos)
    - tests/test_arquetipo_roteamento.py (padrão de fixture/roteamento já usado nos goldens do funil)
  </read_first>
  <behavior>
    - Test 1 (RED): AnaliseAcao com faixa vmax<=0 (HAPV3-shape: sensibilidade toda negativa) →
      após analisar_acao, a faixa NÃO é apresentada como intrínseco (vmin/vmax suprimidos ou flag
      `ddm_inaplicavel` setada) e há sinalização honesta.
    - Test 2 (RED): faixa degenerada 0–0 (PRIO3-shape) → mesma supressão + sinalização.
    - Test 3 (guard-rail inverso): faixa válida positiva (TAEE11-shape, 29–47) → NÃO suprime; a
      faixa e o veredito seguem idênticos aos de hoje.
  </behavior>
  <action>
Na borda de emissão de report.analisar_acao (logo após computar a.vmin/a.vmax a partir da matriz de
sensibilidade, ~linhas 194-202), adicionar um guarda-corpo puro: se a faixa for INVÁLIDA — `vmax`
não-None e `vmax <= 0`, OU degenerada (`vmin == 0 and vmax == 0`) — marcar um campo novo
`ddm_inaplicavel: bool = True` em AnaliseAcao e NÃO deixar essa faixa ser apresentada como intrínseco
(zerar a.vmin/a.vmax para None nesse caso, de modo que a métrica "Intrínseco (DDM)" e a tabela caiam
no caminho de "não disponível"). Acrescentar um alerta honesto do porquê (payout baixo/alto capex ou
lucro negativo tornam o DDM por dividendos estruturalmente inaplicável — não é preço-alvo). Como os
casos degenerados observados são todos motor_pendente (veredito já 'VERIFICAR'), o guarda-corpo é
read-only sobre o veredito primário; ainda assim, para robustez, se algum caso NÃO-pendente
degenerar, o veredito não deve estampar SUB/SOBRE a partir de faixa inválida (a supressão de
vmin/vmax já impede isso via o ramo condicional existente). NÃO importar selo em report além do que
já existe (firewall preservado); NÃO tocar core/ddm.py. Novo arquivo de teste
tests/test_guardrails_ddm.py com os 3 casos.
  </action>
  <verify>
    <automated>python -m pytest tests/test_guardrails_ddm.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Faixa vmax<=0 (HAPV3-shape) → a.vmin/a.vmax None (ou a.ddm_inaplicavel True) e alerta honesto presente.
    - Faixa 0–0 (PRIO3-shape) → suprimida + sinalizada.
    - Faixa positiva válida (TAEE11-shape) → NÃO suprimida; veredito inalterado.
    - `git diff --stat src/analista/core/ddm.py src/analista/report/selo.py` vazio (intocados).
  </acceptance_criteria>
  <done>Faixa DDM negativa/degenerada nunca é emitida como intrínseco; caso válido intacto; ddm.py/selo.py intocados.</done>
</task>

<task type="auto">
  <name>Task 2: Exibição honesta na UI/relatório quando o DDM é inaplicável</name>
  <files>app.py, src/analista/report/report.py, tests/test_guardrails_ddm.py</files>
  <read_first>
    - app.py (render do intrínseco linhas 912-916; caption/veredito 880-891)
    - src/analista/report/report.py (relatorio_markdown, seção DDM linhas 494-523)
    - tests/test_guardrails_ddm.py (após Task 1)
  </read_first>
  <action>
Garantir que a supressão da Task 1 se traduza em exibição honesta, sem faixa negativa/zero em lugar
nenhum: em app.py, a métrica "Intrínseco (DDM)" já cai em "—" quando a.vmin/a.vmax são None (Task 1
zera); adicionar uma nota curta (st.caption/st.info) quando `a.ddm_inaplicavel` — algo como "DDM
estruturalmente inaplicável a este perfil (payout baixo / lucro negativo): faixa por dividendos não
exibida". Em report.relatorio_markdown, na seção "Valuation por Desconto de Dividendos", exibir a
mesma nota honesta em vez da tabela quando ddm_inaplicavel (o ramo "_DDM não calculado_" já existe;
diferenciar a mensagem de "inaplicável" vs "faltou insumo"). Read-only sobre o veredito; só texto de
exibição. Adicionar golden de render (markdown) confirmando que faixa negativa não aparece e a nota
honesta aparece.
  </action>
  <verify>
    <automated>python -m pytest tests/test_guardrails_ddm.py -q && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - relatorio_markdown de um caso ddm_inaplicavel NÃO contém "R$ -" nem "0,00–0,00" na seção DDM; contém a nota de inaplicabilidade.
    - app.py exibe "—" no Intrínseco (DDM) e a nota honesta quando a.ddm_inaplicavel (verificável por leitura do fluxo + golden de report).
    - `python -m pytest -q` → suíte verde, 0 failed (casos válidos como TAEE11/test_selo/test_consistencia_modos inalterados).
  </acceptance_criteria>
  <done>Nenhuma faixa negativa/zero exibida; usuário vê nota honesta de DDM inaplicável; casos válidos intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine → UI/relatório | Faixa intrínseca exibida ao usuário (borda de saída; supressão defensiva). |

Superfície mínima: guarda-corpo read-only na borda de emissão + supressão de output na UI. Nenhum
input externo novo, nenhuma fórmula alterada, nenhuma API/segredo/rede.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0107-01 | Tampering | faixa degenerada apresentada como preço-alvo (integridade da informação) | mitigate | Supressão na emissão (vmax<=0 / 0-0) + nota honesta; goldens travam os casos. |
| T-0107-02 | Repudiation | número negativo confunde o usuário sobre a origem/validade | mitigate | Sinalização explícita de "DDM estruturalmente inaplicável" em vez de esconder silenciosamente. |
| T-0107-03 | Denial of Service | guarda-corpo derruba análise de caso válido por engano | mitigate | Guard estritamente `vmax<=0` ou `0-0`; caso positivo válido (TAEE11) travado por golden inverso. |
</threat_model>

<verification>
- `python -m pytest tests/test_guardrails_ddm.py -q` verde (3 casos: 2 degenerados + 1 válido).
- `python -m pytest -q` verde, 0 failed (test_selo/test_consistencia_modos/test_ddm inalterados).
- ddm.py/selo.py intocados; firewall selo↛report preservado.
</verification>

<success_criteria>
Faixas DDM negativas (HAPV3/PCAR3) e degeneradas (PRIO3) nunca são exibidas como intrínseco;
o usuário recebe sinalização honesta de inaplicabilidade; faixas válidas positivas seguem idênticas.
</success_criteria>

<output>
Após completar, criar `.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-07-SUMMARY.md`.
</output>
