---
phase: 01-classificador-de-arqu-tipo-roteamento
plan: 06
type: tdd
gap_closure: true
wave: 2
depends_on: [01-05]
files_modified:
  - src/analista/core/arquetipo.py
  - config.yaml
  - data/ticker_map.json
  - tests/test_arquetipo.py
autonomous: true
requirements: [ARQ-01]

must_haves:
  truths:
    - "MDIA3 (M. Dias Branco, cd_cvm 20338, setor CVM 'Alimentos') NÃO é roteada para 'financeira'"
    - "os hard-routes financeiros genuínos (banco, seguradora, intermediação financeira) continuam roteando 'financeira'"
    - "python -m pytest -q → suíte verde (nenhuma regressão sobre 01-05)"
  artifacts:
    - path: "src/analista/core/arquetipo.py"
      provides: "casamento do hard-route financeiro que não sofre over-match de token em setor não-financeiro"
      contains: "financeiro_tokens"
    - path: "tests/test_arquetipo.py"
      provides: "golden anti-regressão: setor 'Alimentos' (MDIA3-shape) não vira financeira"
      contains: "MDIA3"
  key_links:
    - from: "src/analista/core/arquetipo.py::classificar"
      to: "config.yaml arquetipo.financeiro_tokens"
      via: "hard-route financeira por token de setor"
      pattern: "financeiro_tokens"
---

<objective>
Fechar o Achado 1b (bug de hard-route da 01-AUDIT-COERENCIA.md): MDIA3 (M. Dias Branco, alimentos)
foi classificada como `financeira` — over-match do hard-route financeiro. Diferente do misroute
sistêmico do sinal de ciclicidade (01-05), este é um bug pontual de casamento de token/resolução de
setor que carimba um negócio de consumo como banco.

Purpose: um hard-route "soberano" que captura o arquétipo errado é pior que o refino quantitativo,
porque não passa nem pelo fallback honesto — crava financeira com confiança alta. Corrigir preserva
o core-value (roteamento fiel ao negócio) e o hard-route financeiro genuíno.
Output: hard-route financeiro sem over-match; golden travando o caso MDIA3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-AUDIT-COERENCIA.md

<interfaces>
<!-- Extraído do código. Executor não precisa explorar. -->
De src/analista/core/arquetipo.py (classificar, linhas 108-112):
  setor = (c.setor or "").lower()
  if any(tok.lower() in setor for tok in financeiro_tokens):   # <- casamento por SUBSTRING (over-match)
      return ResultadoArquetipo(FINANCEIRA, confianca="alta")

De config.yaml arquetipo.financeiro_tokens (linhas 178-189):
  banco · "intermediação financeira" · seguradora · "arrendamento mercantil" ·
  "crédito imobiliário" · factoring · securitização · "bolsas de valores"

De src/analista/ingest/build.py (montar_empresa, linhas 46-56):
  cd_cvm, setor = universe.resolver(ticker, dm.nome)   # setor da CVM; fallback dm.setor (yfinance)
  # setor final = (setor or dm.setor)

De src/analista/ingest/universe.py: resolver(ticker, nome) — (cd_cvm, setor). Override por
  data/ticker_map.json tem precedência; forma {cd_cvm, setor} corrige o setor de EXIBIÇÃO.

Fato confirmado no planejamento: MDIA3 NÃO está em data/ticker_map.json (resolver('MDIA3') -> (None,'')).
  cd_cvm real = 20338; SETOR_ATIV CVM = "Alimentos". Na run do audit resolveu via dm.nome (yfinance).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Reproduzir o over-match do MDIA3 e corrigir o hard-route financeiro</name>
  <files>src/analista/core/arquetipo.py, config.yaml, data/ticker_map.json, tests/test_arquetipo.py</files>
  <read_first>
    - src/analista/core/arquetipo.py (hard-route financeira, linhas 108-112; após 01-05)
    - config.yaml (arquetipo.financeiro_tokens, linhas 178-189)
    - src/analista/ingest/build.py (montar_empresa, derivação de setor, linhas 40-68)
    - src/analista/ingest/universe.py (resolver — precedência do ticker_map)
    - data/ticker_map.json (forma {cd_cvm, setor} de override; MDIA3 ausente)
    - tests/test_arquetipo.py (goldens de hard-route financeira, linhas 51-73)
    - .planning/phases/01-classificador-de-arqu-tipo-roteamento/01-AUDIT-COERENCIA.md (Achado 1, linha do MDIA3)
  </read_first>
  <behavior>
    - RED (reproduzir a raiz): montar MDIA3 (build.montar_empresa — usa rede, como o audit) e
      CAPTURAR o valor real de `c.setor` que chega ao classificador, além de identificar QUAL
      financeiro_token casou como substring nesse setor. Registrar o achado no SUMMARY. Um golden
      offline com uma fixture de setor == o setor capturado do MDIA3 (ou 'Alimentos') FALHA hoje
      (retorna 'financeira').
    - GREEN: a mesma fixture retorna chave != 'financeira'.
    - Goldens financeiros genuínos (banco/seguradora/intermediação financeira) permanecem 'financeira'.
  </behavior>
  <action>
Reproduzir primeiro para não corrigir às cegas: rodar build.montar_empresa('MDIA3', ...) e imprimir
`c.setor` + o token que casou. Duas causas possíveis (o reproduce decide qual): (a) MDIA3 resolveu
por nome (yfinance) a um setor cujo texto contém um token financeiro como SUBSTRING — over-match do
`tok in setor`; (b) resolução de setor imprecisa por MDIA3 não estar no ticker_map. Aplicar a
correção adequada à causa confirmada, podendo combinar as duas:
1. Endurecer o casamento do hard-route em arquetipo.py: trocar o `tok.lower() in setor` ingênuo por
   um casamento por SEGMENTO/limite de palavra (ex.: comparar contra tokens de setor delimitados,
   não substring solta), de modo que um token financeiro não case dentro de um rótulo de setor não
   financeiro. Manter config-driven (ler financeiro_tokens do cfg); se algum token do config for
   perigosamente curto/genérico, ajustá-lo no config.yaml com comentário do porquê.
2. Adicionar MDIA3 ao data/ticker_map.json na forma {cd_cvm: 20338, setor: "Alimentos"} — torna a
   resolução determinística e o setor limpo (padrão já existente no arquivo, ex.: VULC3), fechando a
   porta da resolução por nome que trouxe o setor envenenado.
Adicionar golden offline: `_empresa(...)` com o setor de MDIA3 (o capturado no reproduce, ou
'Alimentos') NÃO vira 'financeira'; e re-afirmar que banco/seguradora ainda viram 'financeira'
(não afrouxar o hard-route genuíno). Sem blocos de código no relatório do veredito.
  </action>
  <verify>
    <automated>python -m pytest tests/test_arquetipo.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `classificar()` numa fixture com o setor real do MDIA3 (capturado) / 'Alimentos' retorna chave != FINANCEIRA.
    - `test_banco_vira_financeira`, `test_seguradora_vira_financeira`, `test_financeira_hard_route_soberana_ignora_quantitativo` continuam verdes.
    - `grep -n "MDIA3" tests/test_arquetipo.py` mostra o golden novo; se ticker_map foi usado, `grep -n "20338" data/ticker_map.json` confirma a entrada.
    - `python -m pytest -q` → suíte verde, 0 failed (nenhuma regressão sobre 01-05).
    - O SUMMARY registra o setor real capturado do MDIA3 e o token que causou o over-match.
  </acceptance_criteria>
  <done>Hard-route financeiro não sofre over-match em setor não-financeiro; MDIA3 fora de 'financeira'; financeiras genuínas preservadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CVM/yfinance → resolução de setor | Texto de setor externo que alimenta o hard-route (já ingerido pela engine hoje). |
| data/ticker_map.json → resolver | Override local versionado (curado, sem input externo em runtime). |

Superfície mínima: endurecer casamento de string em função pura + entrada de mapeamento local. Sem
nova API/segredo/rede; o reproduce usa a rede já existente do build (nenhuma superfície nova).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0106-01 | Tampering | over-match de token roteia negócio errado ao motor errado (integridade) | mitigate | Casamento por segmento/limite + golden que trava setor 'Alimentos' fora de financeira. |
| T-0106-02 | Spoofing | rótulo de setor externo (yfinance) "finge" ser financeiro | mitigate | Override determinístico via ticker_map (fonte curada tem precedência sobre nome do Yahoo). |
| T-0106-03 | Elevation of Privilege | n/a — sem autorização/privilégio envolvido | accept | Superfície puramente de classificação local. |
</threat_model>

<verification>
- `python -m pytest tests/test_arquetipo.py -q` verde (golden MDIA3 + financeiras genuínas).
- `python -m pytest -q` verde, 0 failed.
- SUMMARY documenta a causa-raiz reproduzida (setor real + token).
</verification>

<success_criteria>
MDIA3 (alimentos) deixa de ser 'financeira'; o hard-route financeiro genuíno segue soberano;
suíte verde sem regressão.
</success_criteria>

<output>
Após completar, criar `.planning/phases/01-classificador-de-arqu-tipo-roteamento/01-06-SUMMARY.md`.
</output>
