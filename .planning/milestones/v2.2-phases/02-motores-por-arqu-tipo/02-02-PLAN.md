---
phase: 02-motores-por-arqu-tipo
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/analista/core/arquetipo.py
  - src/analista/report/report.py
  - src/analista/cli.py
  - tests/test_arquetipo_roteamento.py
  - tests/test_ranking_freio.py
autonomous: true
requirements: [ENG-02, ENG-03, ENG-04, ENG-05]
must_haves:
  truths:
    - "ITUB4 (financeira) roteia para RIM: a.motor == 'rim', a.intrinseco_motor populado (~R$28 honesto/conservador; com o ke_rim REAL ~0,14 fica um pouco abaixo do golden unitário de Ke 0,125), materialmente acima da referência DDM ao vivo da MESMA fixture (comparação relativa, não piso absoluto)"
    - "ITUB4 continua com veredito 'VERIFICAR' (NÃO regride para 'evitar') — a suspensão migrou de motor_pendente → motor != 'ddm', com o RIM já plugado"
    - "VALE3 (cíclica) roteia para lucro normalizado: a.motor == 'normalizado', intrínseco sobre lucro médio 7-10a"
    - "WEGE3 (crescimento) roteia para DCF: a.motor == 'dcf', intrínseco positivo/finito (sem zero/lixo)"
    - "Um arquétipo holding roteia para NAV: a.motor == 'nav', intrínseco == VPA do ano-base"
    - "TAEE11 (pagadora_regulada) segue com motor 'ddm', veredito NÃO suspenso, números idênticos (ENG-06 preservado)"
    - "O render exibe o intrínseco do motor do arquétipo como referência primária e rotula o DDM como 'lente conservadora' onde motor != 'ddm'"
    - "O Ranking (cli._motor_pendente) mantém paridade: não estampa preço-alvo por regressão para bancos"
  artifacts:
    - path: "src/analista/core/arquetipo.py"
      provides: "ARQUETIPO_MOTOR com os 4 None trocados por 'rim'/'normalizado'/'dcf'/'nav'"
      contains: "FINANCEIRA: \"rim\""
    - path: "src/analista/report/report.py"
      provides: "novos campos intrinseco_motor/motor_rotulo em AnaliseAcao; dispatch dos motores no funil; suspensão migrada (motor != 'ddm'); render DDM rebaixado"
      contains: "intrinseco_motor"
    - path: "src/analista/cli.py"
      provides: "_motor_pendente com predicado migrado (motor != 'ddm')"
      contains: "!= \"ddm\""
    - path: "tests/test_arquetipo_roteamento.py"
      provides: "asserts atualizados (motor plugado + veredito segue VERIFICAR) + e2e anchors por motor"
      contains: "a.motor"
    - path: "tests/test_ranking_freio.py"
      provides: "assert de suspensão do Ranking atualizado (motor != 'ddm')"
      contains: "_motor_pendente"
  key_links:
    - from: "src/analista/core/arquetipo.py::ARQUETIPO_MOTOR"
      to: "src/analista/report/report.py dispatch (a.motor == 'rim'/...)"
      via: "id do motor resolvido do registry dispara o motor certo no funil"
      pattern: "a\\.motor == \"rim\""
    - from: "src/analista/report/report.py:240 (suspensão)"
      to: "src/analista/cli.py:45-54 (_motor_pendente)"
      via: "MESMO predicado migrado motor != 'ddm' (paridade Analisar × Ranking, D-06)"
      pattern: "!= \"ddm\""
    - from: "src/analista/report/report.py dispatch"
      to: "src/analista/core/motores.py (rim/lucro_normalizado/dcf_crescimento/nav_contabil)"
      via: "import + call, consumindo *_valuation() canônicos"
      pattern: "motores\\.(rim|dcf_crescimento|lucro_normalizado|nav_contabil)"
---

<objective>
Plugar os 4 motores do Plan 01 no registry `ARQUETIPO_MOTOR` e wireá-los no funil único de
`report.analisar_acao`, gravando o intrínseco em novos campos de `AnaliseAcao` e **exibindo-o** —
com o DDM rebaixado a "lente conservadora" onde não é o motor do arquétipo (D-06: motor CALCULA e
EXIBE; o selo NÃO consome ainda — isso é VER-01/Fase 3).

**O elemento load-bearing desta fase (ARMADILHA CRÍTICA — Pitfall 1/D-06):** quando os 4 `None` do
registry viram ids de motor, o predicado `motor is None` vira `False` nas TRÊS superfícies que hoje
derivam a suspensão do veredito daí — `report.py:240`, `cli._motor_pendente` e os dois goldens. Se
a suspensão simplesmente cair, o **ITUB4 regride de "VERIFICAR" para "evitar"** (o selo ainda
consome DDM até a Fase 3). Portanto a suspensão MIGRA de `motor_pendente` → `motor != "ddm"` nas três
superfícies, **no mesmo plano/wave que o plug do registry**. `selo.py`, o firewall selo↛report e
`report._veredito_token` NÃO mudam.

Purpose: fazer cada arquétipo calcular e exibir o intrínseco pelo modelo certo, sem regredir o ITUB4
e sem quebrar TAEE11/DDM.
Output: registry preenchido, funil disparando os motores, render com DDM demovido, suspensão migrada
em 3 superfícies, goldens de suspensão atualizados + anchors e2e por motor.

Cobre: ENG-02/ENG-03/ENG-04/ENG-05 (roteamento efetivo dos motores implementados no Plan 01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motores-por-arqu-tipo/02-CONTEXT.md
@.planning/phases/02-motores-por-arqu-tipo/02-RESEARCH.md
@.planning/phases/02-motores-por-arqu-tipo/02-PATTERNS.md
@.planning/phases/02-motores-por-arqu-tipo/02-01-SUMMARY.md

<interfaces>
<!-- Contratos do Plan 01 (motores puros) que este plano CONSOME. -->

De src/analista/core/motores.py (Plan 01):
- `def rim(vpa0, roe0, ke, retencao, n, fade_para=None) -> Optional[ResultadoRIM]`  # .valor_intrinseco
- `def ke_rim(beta, cfg) -> float`  # Ke estrutural (~12,5%) < CAPM ao vivo
- `def lucro_normalizado(lpa_normalizado, ke, g_estavel) -> Number`
- `def dcf_crescimento(lpa_valuation, g_alto, g_estavel, ke, n, decrescente=True) -> Optional[ResultadoDDM]`  # ou float
- `def nav_contabil(patrimonio_liquido, num_acoes) -> Number`
- rótulos humanos por motor (dict MOTOR_ROTULO ou equivalente do Plan 01)

De src/analista/core/arquetipo.py (registry a editar):
- `ARQUETIPO_MOTOR = { FINANCEIRA: None, PAGADORA_REGULADA: "ddm", CICLICA: None, CRESCIMENTO: None, HOLDING: None }`  # :45-51
- chaves: `FINANCEIRA="financeira"`, `CICLICA="ciclica"`, `CRESCIMENTO="crescimento"`, `HOLDING="holding"`

De src/analista/core/fundamentals.py (CompanyData — consumir *_valuation(), FIX-04):
- `roe_valuation()`, `lpa_valuation()`, `payout_valuation()`, `serie_lucro_normalizada()`
- `patrimonio_liquido: Dict[int,float]`, `num_acoes: Dict[int,float]`, `beta`, `ultimo_ano()`, `serie("lucro_liquido")`

De report.py (estado atual, âncoras):
- `AnaliseAcao` dataclass :22-58 (campos aditivos por fase; `motor`, `motor_pendente` em :53/:56)
- resolução do motor :180-186 (`motor = ARQUETIPO_MOTOR.get(arq.chave)`; `a.motor`; `a.motor_pendente`)
- `g_estavel = cfg["ddm"]["g_estavel"]` :126; `a.g_alto` :141; `a.ke` :161
- suspensão :240 (`if a.motor_pendente:` → prefixo "VERIFICAR")
- render `relatorio_markdown` :483; cabeçalho arquétipo→motor :487-489; seção DDM :532
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plug no registry + dispatch dos motores no funil + campos de resultado</name>
  <files>src/analista/core/arquetipo.py, src/analista/report/report.py</files>
  <read_first>
    - src/analista/core/arquetipo.py:36-51 (chaves FINANCEIRA/CICLICA/CRESCIMENTO/HOLDING + dict ARQUETIPO_MOTOR — os 4 None a trocar)
    - src/analista/report/report.py:22-58 (AnaliseAcao — bloco de campos aditivos Fase 1 a espelhar) + :92-141 (topo do funil: lpa, g_estavel, g_alto) + :152-204 (CAPM, roteamento :180-186, bloco DDM :188-204)
    - src/analista/core/motores.py (Plan 01 — assinaturas reais dos 5 motores + rótulos)
    - src/analista/core/fundamentals.py:78-150 (payout_valuation/roe_valuation/lpa_valuation) — insumos canônicos (Pitfall 2: nunca cru)
    - .planning/phases/02-motores-por-arqu-tipo/02-RESEARCH.md:290-307 (esqueleto do disparo no funil) e :261-264 (Pitfall 2)
    - .planning/phases/02-motores-por-arqu-tipo/02-PATTERNS.md:164-224 (registry + dispatch + insumos canônicos)
  </read_first>
  <action>
    (1) Em `core/arquetipo.py`, trocar no dict `ARQUETIPO_MOTOR` (:45-51) os 4 `None` pelos ids:
    `FINANCEIRA: "rim"`, `CICLICA: "normalizado"`, `CRESCIMENTO: "dcf"`, `HOLDING: "nav"`
    (`PAGADORA_REGULADA: "ddm"` intocado). Atualizar o comentário :43-44 para refletir que a Fase 2
    plugou os motores. Nenhuma outra linha do classificador muda. Os ids batem 1:1 com o dispatch
    abaixo e com o predicado de suspensão (`motor != "ddm"`).

    (2) Em `report.py`, adicionar campos aditivos a `AnaliseAcao` (após :56, espelhando o bloco Fase 1
    com comentário de fase): `intrinseco_motor: Optional[float] = None` e `motor_rotulo: str = ""`.

    (3) Em `report.py`, logo APÓS a resolução do motor (:184-186, onde `a.motor` já está resolvido),
    inserir o dispatch por `a.motor`, consumindo SEMPRE os `*_valuation()` (Pitfall 2, nunca o cru):
      - `a.motor == "rim"`: `ult = c.ultimo_ano()`; `a.intrinseco_motor = motores.rim(vpa0=lentes.vpa(
        c.patrimonio_liquido.get(ult), c.num_acoes.get(ult)), roe0=c.roe_valuation(), ke=motores.ke_rim(
        c.beta, cfg), retencao=(1 - (c.payout_valuation() or 0)), n=cfg["motores"]["rim"]["n_fade"])`
        e extrair `.valor_intrinseco` (rim devolve ResultadoRIM).
      - `a.motor == "normalizado"`: `lpa_mid = mult.lpa(norm.base_normalizada(c.serie("lucro_liquido"),
        anos_media=cfg["motores"]["ciclica"]["anos_media"], winsor=cfg["motores"]["ciclica"]["winsor"]),
        c.num_acoes.get(ult))`; `a.intrinseco_motor = motores.lucro_normalizado(lpa_mid, a.ke, g_estavel)`.
      - `a.motor == "dcf"`: `a.intrinseco_motor = motores.dcf_crescimento(c.lpa_valuation(), a.g_alto,
        g_estavel, a.ke, cfg["motores"]["crescimento"]["n_anos_explicito"])` (extrair valor_intrinseco
        se devolver ResultadoDDM).
      - `a.motor == "nav"`: `a.intrinseco_motor = motores.nav_contabil(c.patrimonio_liquido.get(ult),
        c.num_acoes.get(ult))`.
      - `a.motor == "ddm"`: nada a fazer (o bloco DDM :188-204 já é o motor primário).
      Setar `a.motor_rotulo` a partir do dict de rótulos do Plan 01. Importar `motores` (e `norm`/`lentes`
      se ainda não importados) no topo de report.py. O bloco DDM (:188-204) continua rodando SEMPRE
      (agora como lente onde motor != "ddm") — NÃO tocar seu cálculo.

    Nesta task NÃO mexer no bloco de suspensão (:240) — isso é a Task 2 (mas as duas são do MESMO
    plano/wave, então nunca há uma release em que o registry esteja plugado sem a suspensão migrada).
  </action>
  <acceptance_criteria>
    - `arquetipo.py`: `ARQUETIPO_MOTOR` não contém mais nenhum `None` (5/5 preenchido); grep `FINANCEIRA: "rim"` casa
    - `AnaliseAcao` tem `intrinseco_motor` e `motor_rotulo`
    - Dispatch consome `roe_valuation()`/`lpa_valuation()`/`payout_valuation()`/`base_normalizada` — grep confirma NENHUM `c.lucro_liquido.get(ult)` cru dentro do dispatch de motor
    - `python -c "from analista.report import report"` importa sem erro
    - `core/ddm.py`, `core/lentes.py`, `report/selo.py` fora de `git diff --name-only`
  </acceptance_criteria>
  <verify>
    <automated>cd "$PWD" && python -c "from analista.core.arquetipo import ARQUETIPO_MOTOR; assert None not in ARQUETIPO_MOTOR.values() and ARQUETIPO_MOTOR['financeira']=='rim'"</automated>
  </verify>
  <done>Registry 5/5 preenchido; funil dispara o motor certo por arquétipo consumindo insumos canônicos; campos de resultado adicionados; ddm/lentes/selo intocados.</done>
</task>

<task type="auto">
  <name>Task 2: Migração D-06 da suspensão em 3 superfícies (report + cli + goldens)</name>
  <files>src/analista/report/report.py, src/analista/cli.py, tests/test_arquetipo_roteamento.py, tests/test_ranking_freio.py</files>
  <read_first>
    - src/analista/report/report.py:236-278 (bloco de veredito: suspensão :240 `if a.motor_pendente:` → prefixo "VERIFICAR"; ramo DDM :257+)
    - src/analista/cli.py:45-85 (`_motor_pendente` :45-54 + `alvo_regressao_confiavel` :57-85 que o consome — assinatura NÃO muda, só a semântica interna do predicado)
    - src/analista/report/selo.py (firewall — NÃO tocar; o prefixo "VERIFICAR" já é tratado por montar_selo)
    - tests/test_arquetipo_roteamento.py:103-144 (asserts que codificam a semântica "pendente" da Fase 1: :108 `motor_pendente is False` regulada; :120 `motor_pendente is True` financeira; :135 petróleo)
    - tests/test_ranking_freio.py:122-131 (`_motor_pendente(banco) is True` / `_motor_pendente(regulada) is False`)
    - .planning/phases/02-motores-por-arqu-tipo/02-RESEARCH.md:250-259 (Pitfall 1 — os 3 lugares + semântica recomendada) e :347-356 (goldens a atualizar)
    - .planning/phases/02-motores-por-arqu-tipo/02-PATTERNS.md:225-267, :332-352 (migração report/cli + edição dos asserts, IN-SCOPE)
  </read_first>
  <action>
    Migrar o predicado de suspensão de "registry devolveu None" para "o selo ainda não consome o motor
    deste arquétipo" (= `motor != "ddm"`), nas TRÊS superfícies, mantendo o prefixo "VERIFICAR" (que
    `selo.montar_selo` já trata — selo.py INTOCADO):

    (1) `report.py:240` — trocar `if a.motor_pendente:` por `if a.motor != "ddm":`. Atualizar o texto
    do veredito para refletir que o motor JÁ EXISTE mas o selo não o consome nesta fase (ex.: exibir
    o intrínseco do motor como referência primária e dizer que o DDM abaixo é lente conservadora, não
    o motor deste perfil; VER-01/selo é Fase 3). Atualizar o alerta correspondente (:253-256). O ramo
    `elif a.vmin is not None ...` (:257+, veredito DDM) permanece e só é alcançado quando `motor ==
    "ddm"`. NÃO tocar `_veredito_token` nem `selo.py`.

    (2) `cli.py:45-54` `_motor_pendente` — trocar o retorno `... .get(arq.chave) is None` por `... .get(
    arq.chave) != "ddm"`. Ajustar a docstring (motor existe, mas o Ranking segue sem estampar preço-alvo
    por um modelo que não é o do perfil, até a Fase 3). `alvo_regressao_confiavel` (:57-85) NÃO muda de
    assinatura — só recebe o novo valor do predicado.

    (3) Goldens — atualizar os asserts que travam a semântica "pendente" da Fase 1 (edição ESPERADA/
    IN-SCOPE):
      - `tests/test_arquetipo_roteamento.py`: em `test_financeira_...` (:116-128), trocar `assert
        a.motor_pendente is True` por `assert a.motor == "rim"` e PRESERVAR `assert a.veredito.startswith(
        "VERIFICAR")` + os asserts do selo (verificar True, rotulo None) + o alerta. Em
        `test_petroleo_...` (:132-135), trocar `assert a.motor_pendente is True` por `assert a.motor in
        {"dcf","normalizado"}` (petróleo-compounder roteia crescimento). `test_regulada_...` (:104-112,
        `motor == "ddm"`, NÃO suspenso) permanece verde SEM mudança. `test_degradacao_...` (:139-144,
        default regulada motor ddm) permanece.
      - `tests/test_ranking_freio.py`: em `test_motor_pendente_financeira_suspende` (:122-125), o
        `_motor_pendente(banco)` segue `True` mas AGORA por `motor != "ddm"` (motor rim existe) — manter
        o assert `is True` e atualizar o docstring/nome se refletir "pendente" literal.
        `test_motor_pendente_regulada_tem_motor` (:128-131, `is False`) permanece.
  </action>
  <acceptance_criteria>
    - grep em report.py: existe `if a.motor != "ddm":` no bloco de veredito; NÃO há mais `if a.motor_pendente:` disparando a suspensão
    - grep em cli.py `_motor_pendente`: retorna `!= "ddm"`
    - `python -m pytest tests/test_arquetipo_roteamento.py tests/test_ranking_freio.py -x` sai 0
    - `report/selo.py` e `report._veredito_token` fora de `git diff` (firewall preservado)
    - `python -m pytest tests/test_selo.py -x` sai 0 (firewall/cortes intactos)
  </acceptance_criteria>
  <verify>
    <automated>cd "$PWD" && python -m pytest tests/test_arquetipo_roteamento.py tests/test_ranking_freio.py tests/test_selo.py -x</automated>
  </verify>
  <done>Suspensão migrada para motor != "ddm" em report + cli + goldens; ITUB4/financeira segue "VERIFICAR" com motor "rim" plugado; regulada/DDM intacta; selo.py intocado.</done>
</task>

<task type="auto">
  <name>Task 3: Render (DDM rebaixado a lente conservadora + intrínseco do motor) + anchors e2e por motor</name>
  <files>src/analista/report/report.py, tests/test_arquetipo_roteamento.py</files>
  <read_first>
    - src/analista/report/report.py:483-560 (relatorio_markdown: cabeçalho arquétipo→motor :487-489; seção "## Valuation por Desconto de Dividendos" :532; tabela DDM :541-550)
    - tests/test_arquetipo_roteamento.py:1-100 (fixtures sintéticas _regulada_solida/_financeira/_petroleo_compounder/_fronteirico — estilo a copiar para os anchors)
    - src/analista/core/motores.py (rótulos + assinaturas)
    - .planning/phases/02-motores-por-arqu-tipo/02-RESEARCH.md:309-314 (rebaixamento DDM no render) e :338-345 (anchors e2e por motor) e :377-380 (Open Question 2 — render mínimo, UX rica é Fase 3)
    - .planning/phases/02-motores-por-arqu-tipo/02-PATTERNS.md:243-250, :309-328 (render + fixtures e2e)
  </read_first>
  <action>
    (1) Render em `relatorio_markdown` (report.py:532+): onde `a.motor != "ddm"` e `a.intrinseco_motor`
    está populado, adicionar UMA linha exibindo o intrínseco do motor do arquétipo como referência
    primária (ex.: `- **{a.motor_rotulo}: R$ {_num(a.intrinseco_motor)}** (motor do arquétipo)`) ANTES
    da tabela DDM, e um sub-rótulo na seção DDM indicando "(lente conservadora — não é o motor deste
    arquétipo)". Onde `a.motor == "ddm"`, o render segue como hoje (DDM é o primário). Só EXIBIÇÃO
    (D-06) — não tocar cálculo, não adicionar bandeira de divergência (Fase 3). Render mínimo: uma
    linha por motor, sem UX rica (Open Question 2).

    (2) Anchors e2e em `tests/test_arquetipo_roteamento.py` — adicionar testes que validam o roteamento
    ponta-a-ponta via `report.analisar_acao`, usando/estendendo as fixtures sintéticas do arquivo:
      - Financeira (RIM): `_financeira()` → `a.motor == "rim"`, `a.intrinseco_motor` não-None e > 0.
        Assertar que o RIM **destrava vs o DDM comprimido** (o ponto do critério #1), com asserções
        ROBUSTAS ao valor real do ke_rim: (a) `a.intrinseco_motor > lentes.vpa(c.patrimonio_liquido.get(
        ult), c.num_acoes.get(ult))` (RIM excede o VPA porque ROE>Ke — estrutural), E (b)
        `a.intrinseco_motor` MATERIALMENTE acima da referência DDM-ao-vivo da PRÓPRIA fixture — comparar
        contra o DDM da mesma empresa (midpoint de `a.vmin`/`a.vmax`, ou `a.ddm_h.valor_intrinseco`),
        assert `a.intrinseco_motor > 1.3 * <ddm_ref_da_fixture>`. NÃO hardcodar um piso absoluto de
        R$25/28: com o `ke_rim()` REAL (defaults → ~0,14, MAIOR que o 0,125 do golden puro) o intrínseco
        fica ABAIXO do golden unitário — a comparação RELATIVA ao DDM da fixture é o que garante que a
        asserção passa no pipeline wired (checker Warning 1). Opcional: tunar a fixture para VPA~22
        (PL~22000/acoes~1000, ITUB4-like) para o número cair na banda interpretável ~R$26–34. Também:
        `a.veredito.startswith("VERIFICAR")` (D-06 — suspenso mesmo com motor plugado) e
        `selo.montar_selo(70.0, a.veredito, cfg).rotulo is None` (não estampa 'evitar').
      - Cíclica (normalizado): criar/estender fixture cíclica (lucro oscilante, setor "Extração
        Mineral"/commodity) → `a.motor == "normalizado"`, `a.intrinseco_motor` populado.
      - Crescimento (DCF): `_petroleo_compounder()` (ou fixture compounder payout-baixo) → `a.motor ==
        "dcf"`, `a.intrinseco_motor` > 0 e finito (não zero/lixo — aceite #3).
      - Holding (NAV): fixture holding sintética → `a.motor == "nav"`, `a.intrinseco_motor` == VPA do
        ano-base (`lentes.vpa(PL_ult, acoes_ult)`).
      - Regulada (DDM, não suspenso): reafirmar `_regulada_solida()` → `a.motor == "ddm"`, veredito NÃO
        começa com "VERIFICAR" (ENG-06 preservado, aceite #2/#5 de contexto).
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_arquetipo_roteamento.py -x` sai 0 (asserts atualizados da Task 2 + novos anchors)
    - `python -m pytest` (suite completa) sai 0 — `test_ddm`, `test_selo`, `test_consistencia_modos`, `test_vulc3_regressao`, `test_guardrails_fix06` TODOS verdes (nenhuma regressão)
    - Render: um `analisar_acao` de financeira produz markdown com a linha do intrínseco do motor + DDM rotulado "lente conservadora" (verificável por asserção de substring no markdown OU inspeção do relatorio_markdown)
    - Anchors cobrem os 4 motores + regulada, cada um assertando `a.motor` e `a.intrinseco_motor` (exceto regulada)
  </acceptance_criteria>
  <verify>
    <automated>cd "$PWD" && python -m pytest</automated>
  </verify>
  <done>Render exibe o intrínseco do motor do arquétipo + DDM como lente conservadora; anchors e2e verdes para os 4 motores + regulada; suite completa verde (aceites #1-#5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry → funil → veredito | O id do motor resolvido governa qual motor roda E se o veredito é suspenso. A superfície de risco é de CONSISTÊNCIA/regressão (não de segurança): a suspensão precisa migrar junto com o plug ou o ITUB4 regride para "evitar". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0202-01 | Elevation of Privilege (DDM "assume" o veredito de arquétipo que não é dele) | report.py:240 / cli._motor_pendente | mitigate | Migrar o predicado para `motor != "ddm"` nas 3 superfícies NO MESMO plano/wave que o plug do registry; golden e2e (ITUB4 segue "VERIFICAR" com motor "rim") trava a regressão. Firewall selo↛report preservado (selo.py intocado). |
| T-0202-02 | Tampering (Analisar × Ranking divergem para a mesma ação) | funil report.py vs cli Ranking | mitigate | Motores consomem só `*_valuation()` canônicos (fonte única, FIX-04); `test_consistencia_modos` trava a paridade dos 3 modos. |
| T-0202-03 | Denial of Service (dado degenerado no funil levanta) | dispatch report.py | accept | Motores never-raise (Plan 01) devolvem None; o funil já degrada graciosamente. Guarda-corpos anti-aberração completos (SAN-01) são Fase 3 — NÃO puxar. |
</threat_model>

<verification>
- `python -m pytest` (suite completa) verde — aceites #1-#5 do ROADMAP §Phase 2
- ITUB4/financeira: `a.motor == "rim"`, `a.intrinseco_motor` > 0 E materialmente acima da referência DDM da fixture (> 1,3× o DDM da própria empresa), veredito "VERIFICAR", selo sem 'evitar'
- TAEE11/regulada: `a.motor == "ddm"`, veredito NÃO suspenso (ENG-06 preservado)
- `git diff --name-only` NÃO lista core/ddm.py, core/lentes.py, report/selo.py
- Ranking (cli._motor_pendente) mantém paridade com a suspensão do Analisar (motor != "ddm")
</verification>

<success_criteria>
- Registry 5/5 preenchido; cada arquétipo dispara e EXIBE o intrínseco do motor certo
- ITUB4 → RIM (intrínseco honesto ~R$28 exibido, materialmente > DDM ao vivo), DDM rebaixado a "lente conservadora", veredito segue "VERIFICAR" (SEM regredir para "evitar")
- VALE3 → lucro normalizado (7-10a); WEGE3 → DCF positivo/finito; holding → NAV
- TAEE11/DDM idêntica; test_ddm/test_selo/test_consistencia_modos/test_vulc3/test_guardrails verdes
- Suspensão D-06 migrada nas 3 superfícies no mesmo wave do plug (sem janela de regressão)
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-motores-por-arqu-tipo/02-02-SUMMARY.md`.
</output>
