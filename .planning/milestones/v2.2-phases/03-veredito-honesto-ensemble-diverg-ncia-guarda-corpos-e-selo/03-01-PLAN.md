---
phase: 03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/report/report.py
  - config.yaml
  - tests/test_report.py
autonomous: true
requirements: [VER-01, ENS-01]
must_haves:
  truths:
    - "Um arquétipo não-DDM (ITUB4/RIM) recebe veredito de preço REAL derivado do intrínseco do motor + banda do ensemble, não mais o texto VERIFICAR-suspenso do ramo D-06"
    - "O selo consome esse veredito via os prefixos SUB/NO INTERVALO/SOBRE já casados por selo.faixa_do_veredito — o DDM é exibido como lente conservadora, não o motor do perfil"
    - "Quando o motor primário e o contraponto DDM divergem mais que o limiar (maior > 2× menor), a análise grava razão + hipótese curada e a bandeira é exibida, em vez de cravar número único"
    - "TAEE11 (motor == ddm) permanece bit-idêntica: nenhum caminho novo é acionado quando o motor primário JÁ é o DDM"
  artifacts:
    - path: "src/analista/report/report.py"
      provides: "Banda do ensemble (motor × contraponto DDM) alimentando o veredito + campos de divergência + template de hipótese"
      contains: "_HIPOTESE_DIVERGENCIA"
    - path: "config.yaml"
      provides: "Bloco veredito.margem_seguranca (fallback D-01 quando o contraponto degrada)"
      contains: "margem_seguranca"
  key_links:
    - from: "report.analisar_acao (banda vmin/vmax do motor)"
      to: "selo.montar_selo via a.veredito"
      via: "prefixo SUB/NO INTERVALO/SOBRE reconhecido por faixa_do_veredito (sem tocar selo.py)"
      pattern: "faixa_do_veredito"
    - from: "report.analisar_acao"
      to: "comparables.divergencia_entre_lentes(intrinseco_motor, ddm_mid)"
      via: "chamada do helper puro já existente (limiar 2.0)"
      pattern: "divergencia_entre_lentes"
---

<objective>
VER-01 + ENS-01: refatorar a borda do veredito em `analisar_acao()` para que o **selo consuma o motor do arquétipo** (não o DDM fixo) e para que a divergência motor×contraponto vire **bandeira exibida** em vez de número único cravado.

Hoje o ramo de suspensão D-06 (`report.py:296-319`, `if a.motor != "ddm":`) SEMPRE estampa `VERIFICAR` quando o motor não é o DDM — o motor CALCULA e EXIBE o intrínseco (Fase 2) mas o selo ainda não o consome. Este plano substitui esse ramo por veredito real: a banda `vmin/vmax` passa a vir do **ensemble** (min/max entre `a.intrinseco_motor` e o mid do DDM contraponto, D-01), alimenta a MESMA árvore de comparação SUB/NO INTERVALO/SOBRE do ramo DDM (`:320-343`), e a divergência é sinalizada pelo helper puro `comparables.divergencia_entre_lentes()` já existente (D-02) com hipótese curada por `(arquétipo, sinal)` (D-03).

Purpose: destravar o último passo da arquitetura por arquétipo — o selo finalmente reflete o motor certo. É a fundação da fase; SAN-01 (03-02) e VER-02 (03-03) montam em cima.
Output: `report.py` com banda do ensemble + campos de divergência + template de hipótese; `config.yaml` com `veredito.margem_seguranca`.

**Firewall INEGOCIÁVEL:** `selo.py` NÃO é tocado. Reusa os prefixos SUB/NO INTERVALO/SOBRE que `selo.faixa_do_veredito` (`selo.py:88`) já casa. Toda mudança acontece na borda do veredito em `report.py`.
**Invariante-chave:** o caminho `motor == "ddm"` (pagadora regulada, TAEE11) NÃO muda — é isso que garante "regulada idêntica" e mantém `test_ddm`/`test_selo` verdes sem rebaseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo/03-CONTEXT.md
@.planning/phases/03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo/03-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do codebase. Use diretos — sem exploração. -->

De src/analista/report/report.py (AnaliseAcao :23-63, campos já existentes):
- a.intrinseco_motor: Optional[float]   # número do motor do arquétipo (None se degradou); já <= 0 suprimido
- a.motor: str                          # "rim"|"normalizado"|"dcf"|"nav"|"ddm"|"pendente_fase_2"
- a.motor_rotulo: str                   # motores.MOTOR_ROTULO
- a.arquetipo: str
- a.vmin / a.vmax: Optional[float]       # HOJE = min/max da matriz de sensibilidade DDM (após _guarda_faixa_ddm)
- a.veredito: str ; a.alertas: List[str]

De src/analista/core/comparables.py (PURO, never-raise — REUSAR, NÃO editar):
- divergencia_entre_lentes(v_a, v_b, limiar=LIMIAR_DIVERGENCIA) -> (divergiu: bool, razao=maior/menor)
  # dado None/<=0 em qualquer lente -> (False, 1.0)
- LIMIAR_DIVERGENCIA = 2.0   # constante de módulo (comparables.py:84) — NÃO inventar knob novo

De src/analista/report/selo.py (NÃO tocar — firewall):
- faixa_do_veredito(veredito): SUBAVALIADA->Barato · "NO INTERVALO"->Justo · SOBREAVALIADA->Caro · VERIFICAR/outro->None
- montar_selo(bsd, veredito, cfg) já é chamado em report.py:445
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Campos de divergência + banda do ensemble (motor × contraponto DDM)</name>
  <files>src/analista/report/report.py, config.yaml</files>
  <read_first>
    - src/analista/report/report.py:23-63 (dataclass AnaliseAcao — padrão de blocos aditivos comentados por fase)
    - src/analista/report/report.py:192-242 (dispatch do motor + guarda de intrínseco não-positivo; onde a.intrinseco_motor fica gravado)
    - src/analista/report/report.py:278-295 (cálculo de vmin/vmax da matriz de sensibilidade + _guarda_faixa_ddm — DDM band já pronta aqui)
    - src/analista/core/comparables.py:84-108 (LIMIAR_DIVERGENCIA + divergencia_entre_lentes — o helper a reusar)
    - src/analista/cli.py:220,242-247 (analog: ddm_mid = (vmin+vmax)/2 + chamada de divergencia_entre_lentes)
  </read_first>
  <behavior>
    - Contraponto DDM: quando vmin/vmax estão preenchidos (banda DDM após _guarda_faixa_ddm), a.contraponto_valor = (vmin+vmax)/2, capturado ANTES de a banda ser sobrescrita.
    - Para motor != "ddm" com a.intrinseco_motor não-None e contraponto não-None: a.vmin/a.vmax = min/max(intrinseco_motor, contraponto) (banda do ensemble, D-01); a.banda_do_motor = True.
    - Fallback D-01: motor != "ddm", intrinseco_motor não-None, contraponto None (DDM suprimido/degradou) -> banda = intrinseco_motor ± cfg["veredito"]["margem_seguranca"] (default 0.15); a.banda_do_motor = True.
    - divergiu, razao = divergencia_entre_lentes(intrinseco_motor, contraponto) grava a.divergencia_ativa/a.divergencia_razao (com contraponto None -> (False,1.0), sem bandeira espúria).
    - motor == "ddm": NADA muda (contraponto/banda/divergência não são acionados) — TAEE11 idêntica.
    - motor != "ddm" mas intrinseco_motor None (motor degradou): banda fica como estava (DDM), sem crash; divergência inativa.
  </behavior>
  <action>
    Adicionar em `AnaliseAcao` (após `ddm_inaplicavel`, seguindo o comentário `# --- Fase 3 v2.2 ...`) os campos: `contraponto_valor: Optional[float] = None`, `banda_do_motor: bool = False`, `divergencia_ativa: bool = False`, `divergencia_razao: Optional[float] = None`, `divergencia_hipotese: str = ""`. Todos com defaults degradáveis.

    No corpo de `analisar_acao`, LOGO APÓS `_guarda_faixa_ddm(a)` (`report.py:295`) e ANTES do bloco de veredito, inserir o cálculo do ensemble: capturar `contraponto = (a.vmin + a.vmax)/2` quando ambos não-None (o mid do DDM, análogo a `cli.py:220`), gravar em `a.contraponto_valor`; então, SOMENTE quando `a.motor != "ddm"` e `a.intrinseco_motor is not None`, montar a banda do ensemble em `a.vmin/a.vmax` (min/max entre `a.intrinseco_motor` e `contraponto`, ou fallback `± cfg["veredito"]["margem_seguranca"]` quando `contraponto is None`), marcar `a.banda_do_motor = True`, e chamar `comparables.divergencia_entre_lentes(a.intrinseco_motor, contraponto)` gravando `a.divergencia_ativa`/`a.divergencia_razao`. NÃO editar `comparables.py`. Leitura defensiva do knob (`(cfg or {}).get("veredito", {}).get("margem_seguranca", 0.15)`), paridade com o padrão WR-03 do dispatch do motor (`report.py:200`).

    Adicionar em `config.yaml`, novo bloco top-level `veredito:` com `margem_seguranca: 0.15` (comentário: fallback D-01 da banda quando o contraponto DDM degrada).
  </action>
  <acceptance_criteria>
    - `report.py` contém os 5 campos novos em AnaliseAcao com defaults degradáveis.
    - Para motor != "ddm" com intrínseco e contraponto válidos, `a.vmin == min(intrinseco_motor, contraponto)` e `a.vmax == max(...)` e `a.banda_do_motor is True`.
    - `comparables.py` NÃO foi modificado (`git diff --exit-code src/analista/core/comparables.py`).
    - `config.yaml` contém `veredito:` com `margem_seguranca`.
    - Para motor == "ddm", `a.banda_do_motor is False` e `a.divergencia_ativa is False` (nenhum caminho novo acionado).
    - `python -m pytest tests/test_report.py tests/test_ddm.py -q` sai 0 (ou falhas SÓ nas asserções de texto do veredito, resolvidas na Task 2).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -m pytest tests/test_report.py tests/test_ddm.py -q; git diff --exit-code src/analista/core/comparables.py</automated>
  </verify>
  <done>Campos + banda do ensemble + gate de divergência gravados; comparables.py intocado; config veredito.margem_seguranca presente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VER-01 — substituir o ramo de suspensão D-06 por veredito real do motor</name>
  <files>src/analista/report/report.py</files>
  <read_first>
    - src/analista/report/report.py:296-343 (ramo de suspensão a SUBSTITUIR + árvore de comparação DDM a REUSAR — analog exato)
    - src/analista/report/report.py:322-337 (ramo VULC3: flags de risco -> VERIFICAR "possível divergência de modelo" — PRESERVAR no caminho do motor)
    - src/analista/report/selo.py:88-102,119-122 (faixa_do_veredito + overlay VERIFICAR — por que reusar os prefixos casa o selo sem tocá-lo)
    - tests/test_vulc3_regressao.py (invariante: veredito começa com VERIFICAR por armadilha REAL — payout>100%)
    - tests/test_report.py, tests/test_arquetipo_roteamento.py (goldens do roteamento/veredito)
  </read_first>
  <behavior>
    - motor != "ddm" com banda do motor (vmin/vmax) e preço: veredito cai na árvore SUB/NO INTERVALO/SOBRE (mesmos prefixos), agora alimentada pela banda do motor.
    - As flags de risco (flag_payout/flag_dy/flag_div_prejuizo) continuam vetando "SUBAVALIADA" e emitindo "VERIFICAR — ... possível divergência de modelo" TAMBÉM no caminho do motor (VULC3 real preservado).
    - motor != "ddm" SEM banda (intrínseco None E DDM suprimido): veredito degrada para o texto de referência do motor com prefixo VERIFICAR (selo suprime faixa) — nunca crasha, nunca estampa faixa falsa.
    - motor == "ddm": ramo DDM original intacto (TAEE11 idêntica).
    - Alerta honesto: quando o motor alimenta o veredito, um alerta declara "DDM rebaixado a lente conservadora; motor primário = <rótulo>".
  </behavior>
  <action>
    Remover o bloco `if a.motor != "ddm": ... VERIFICAR — arquétipo ...` (`report.py:296-319`) e reestruturar a cadeia de veredito para que o mesmo `elif a.vmin is not None and a.vmax is not None and a.preco_atual:` (`:320-343`) atenda TANTO `motor == "ddm"` quanto `motor != "ddm"` (a banda do motor já foi gravada na Task 1). Preservar integralmente o sub-ramo de flags de risco (`:325-337`) — ele deve disparar no caminho do motor (mantém `test_vulc3_regressao`). Adicionar um ramo terminal de degradação: quando `a.motor != "ddm"` e a banda não existe (`vmin/vmax None`), estampar `a.veredito` com prefixo `VERIFICAR — arquétipo {a.arquetipo}: referência primária pelo intrínseco ≈ R$ {_br(a.intrinseco_motor)} (...)` OU, sem intrínseco, `VERIFICAR — motor '{a.motor}' não pôde estimar preço-alvo` (reusa a supressão de faixa do prefixo VERIFICAR, `selo.py:119`). Onde a banda do motor alimenta o veredito, `a.alertas.append` uma nota declarando o DDM como lente conservadora e o motor primário (`a.motor_rotulo`).

    NÃO alterar `_veredito_token` (`:491`) nem `selo.faixa_do_veredito` — os prefixos SUB/NO INTERVALO/SOBRE/VERIFICAR são reusados como estão. Se algum golden de texto de veredito quebrar por mudança DELIBERADA de prefixo, rebaseline SÓ com intenção declarada no SUMMARY (nenhum prefixo novo é esperado aqui).
  </action>
  <acceptance_criteria>
    - O ramo `if a.motor != "ddm":` de suspensão incondicional (VERIFICAR-arquétipo) foi REMOVIDO; a árvore SUB/NO INTERVALO/SOBRE é única para ddm e não-ddm.
    - ITUB4 (motor rim): `a.veredito` começa com um dos prefixos SUB/NO INTERVALO/SOBRE/VERIFICAR derivado da banda do MOTOR (não o texto "referência primária ... só na Fase 3"); a análise NÃO contém o token "Evitar" via selo do DDM sozinho, e um alerta declara "lente conservadora".
    - VULC3: `a.veredito` AINDA começa com "VERIFICAR" (armadilha real payout>100%) — `python -m pytest tests/test_vulc3_regressao.py -q` sai 0.
    - TAEE11 (motor ddm): veredito e selo idênticos ao baseline — `python -m pytest tests/test_ddm.py tests/test_selo.py tests/test_consistencia_modos.py -q` sai 0.
    - Firewall preservado: `python -m pytest tests/test_selo.py -k firewall -q` sai 0; `grep -n "import report" src/analista/report/selo.py` retorna vazio.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -m pytest tests/test_report.py tests/test_arquetipo_roteamento.py tests/test_vulc3_regressao.py tests/test_ddm.py tests/test_selo.py tests/test_consistencia_modos.py -q</automated>
  </verify>
  <done>Suspensão D-06 substituída por veredito real do motor; VULC3/TAEE11 preservados; firewall intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hipótese curada por (arquétipo, sinal) + render da bandeira no markdown</name>
  <files>src/analista/report/report.py, tests/test_report.py</files>
  <read_first>
    - src/analista/report/report.py:458-504 (_MATRIZ_LEITURA + _veredito_token/_matriz_leitura — analog do dicionário-curado por tupla-chave, D-03)
    - src/analista/report/report.py:594-653 (bloco "Valuation pelo motor do arquétipo" + seção Veredito no relatorio_markdown — onde a bandeira entra)
    - .planning/phases/.../03-CONTEXT.md (D-03: template arquétipo×direção; frases-âncora "compounder subvalorizado pelo DDM", "possível topo de ciclo")
  </read_first>
  <behavior>
    - _HIPOTESE_DIVERGENCIA[(arquetipo, sinal)] devolve frase curada; sinal = "motor_acima" quando intrinseco_motor > contraponto, "motor_abaixo" caso contrário.
    - Fallback genérico quando a tupla não resolve: "modelos divergem ~{razao:.1f}× — ver as duas referências".
    - a.divergencia_hipotese é preenchida SÓ quando a.divergencia_ativa é True.
    - relatorio_markdown exibe, na seção Veredito, a bandeira com AMBOS os números (motor + contraponto) e o "porquê" — divergência é informação exibida, nunca escondida.
    - Sem divergência ativa: nenhum bloco de bandeira é emitido (render limpo).
  </behavior>
  <action>
    Criar `_HIPOTESE_DIVERGENCIA: Dict[tuple, str]` no mesmo estilo de `_MATRIZ_LEITURA` (`report.py:458`), chaveado por `(arquetipo, sinal)` com pelo menos: `("financeira","motor_acima")` -> frase "compounder subvalorizado pelo DDM (Ke alto comprime o DDM de estágio único)"; `("ciclica","motor_abaixo")` -> "possível topo de ciclo (lucro do ano acima do mid-cycle)"; `("crescimento","motor_acima")` -> frase de crescimento subestimado pelo DDM. Adicionar helper puro `_hipotese_divergencia(arquetipo, intrinseco_motor, contraponto, razao)` que resolve o sinal, consulta o dict e cai no fallback genérico. Preencher `a.divergencia_hipotese` na Task-1/2 chamando esse helper quando `a.divergencia_ativa`.

    Em `relatorio_markdown`, dentro da seção `## Veredito` (`report.py:645-653`), quando `a.divergencia_ativa`, `L.append` um bloco "Bandeira de divergência" com `a.intrinseco_motor`, `a.contraponto_valor`, `a.divergencia_razao` e `a.divergencia_hipotese`, formatado ptBR via `_num`. Reaproveitar o rótulo do bloco motor (`:599`) para nomear a referência primária.

    Atualizar/adicionar asserções em `tests/test_report.py`: (a) ITUB4 exibe referência do motor + DDM como lente; (b) quando há divergência >2×, a frase-hipótese casa o template do arquétipo; (c) sem divergência, nenhum bloco de bandeira. Se rebaselinar strings, declarar intenção no SUMMARY.
  </action>
  <acceptance_criteria>
    - `report.py` contém `_HIPOTESE_DIVERGENCIA` com chaves `(arquetipo, sinal)` e um fallback genérico ~"modelos divergem".
    - Um caso de teste com motor>DDM em arquétipo "financeira" produz `a.divergencia_hipotese` contendo "subvalorizado pelo DDM".
    - `relatorio_markdown(...)` de um caso divergente contém os DOIS números e o "porquê"; de um caso não-divergente NÃO contém o bloco de bandeira.
    - `python -m pytest tests/test_report.py -q` sai 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -m pytest tests/test_report.py tests/test_arquetipo.py tests/test_arquetipo_roteamento.py -q</automated>
  </verify>
  <done>Hipótese curada por arquétipo×sinal + bandeira renderizada no markdown; goldens verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream CVM/Yahoo/BCB → engine | Dados públicos gratuitos, possivelmente malformados/atípicos (lucro negativo, payout absurdo) atravessam para o cálculo do intrínseco/banda |
| config.yaml → engine | Knob `veredito.margem_seguranca` lido do arquivo local do operador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0301-01 | Tampering (dado adversarial) | banda do ensemble em analisar_acao | mitigate | contraponto None e intrinseco_motor<=0 já degradam (guarda de :237); banda só se forma com valores válidos; `divergencia_entre_lentes` é never-raise e devolve (False,1.0) sob None/<=0 |
| T-0301-02 | Denial (NaN/ZeroDivision no razão) | divergencia_entre_lentes / mid do contraponto | mitigate | helper puro já trata None/<=0; margem_seguranca lida com `.get(...,0.15)` defensivo (config sem o bloco não quebra o never-raise) |
| T-0301-03 | Information disclosure (veredito enganoso) | selo consumindo banda errada | mitigate | banda só do motor quando motor!="ddm"; motor=="ddm" intocado; goldens test_ddm/test_selo/test_consistencia_modos travam o número |
| T-0301-04 | Elevation (config injection) | cfg["veredito"] | accept | ferramenta local read-only, sem execução de código do config; valor float clamp implícito pela matemática da banda; baixo risco |
</threat_model>

<verification>
- `python -m pytest tests/test_report.py tests/test_arquetipo.py tests/test_arquetipo_roteamento.py tests/test_vulc3_regressao.py tests/test_ddm.py tests/test_selo.py tests/test_consistencia_modos.py tests/test_guardrails_ddm.py tests/test_guardrails_fix06.py -q` sai 0 (rebaseline SÓ declarado).
- Firewall: `grep -n "import report\|from .* report" src/analista/report/selo.py` vazio.
- `git diff --exit-code src/analista/core/comparables.py src/analista/core/ddm.py src/analista/report/selo.py` (esses NÃO mudam).
</verification>

<success_criteria>
- SC#1 (parcial): ITUB4 (motor RIM) recebe veredito do motor + banda do ensemble; DDM rebaixado a lente conservadora; nenhum "Evitar via DDM sozinho" pelo ramo de suspensão (o backstop anti-aberração completo é 03-02).
- SC#2: divergência motor×contraponto >2× exibe razão + hipótese curada (bandeira), não número único.
- SC#5 (parcial): firewall selo↛report preservado; test_selo/test_vulc3_regressao/test_ddm/test_consistencia_modos verdes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-veredito-honesto-ensemble-diverg-ncia-guarda-corpos-e-selo/03-01-SUMMARY.md`
</output>
