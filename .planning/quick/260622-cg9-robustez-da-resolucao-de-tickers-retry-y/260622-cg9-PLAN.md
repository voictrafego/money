---
phase: quick-260622-cg9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/ingest/prices.py
  - src/analista/ingest/universe.py
  - data/ticker_map.json
  - tests/test_ingest_resolucao.py
autonomous: true
requirements: [CG9-FIX1, CG9-FIX2, CG9-FIX3]

must_haves:
  truths:
    - "coletar_mercado re-tenta a busca no Yahoo quando o fetch falha/retorna vazio e desiste so apos N tentativas"
    - "_norm remove sufixo juridico (S.A./S/A/SA/LTDA) sem corromper Saneamento/Sao/SABESP"
    - "resolver casa AGRO3/SBSP3 por sobreposicao de tokens quando exato e contem falham, sem falso-positivo"
    - "Nomes Yahoo que ja resolvem (TOTVS/Cosan/Energisa/TIM) continuam resolvendo para o MESMO CD_CVM"
    - "ticker_map.json contem entradas deterministicas verificadas no cadastro (SBSP3=14443 etc.) e e JSON valido"
  artifacts:
    - path: "src/analista/ingest/prices.py"
      provides: "coletar_mercado com retry interno de fetch"
    - path: "src/analista/ingest/universe.py"
      provides: "_norm corrigido + fallback token-set em resolver"
    - path: "data/ticker_map.json"
      provides: "override deterministico ampliado"
    - path: "tests/test_ingest_resolucao.py"
      provides: "testes offline: retry, _norm, token-set, nao-regressao"
  key_links:
    - from: "resolver"
      to: "_norm"
      via: "normalizacao antes de comparar nomes"
      pattern: "_norm\\("
    - from: "coletar_mercado"
      to: "_fetch_info"
      via: "retry com backoff"
      pattern: "range\\(|for .* in "
---

<objective>
Tornar a resolucao de tickers robusta atacando as 3 causas reais de "Nao encontrei dados suficientes":
(1) Yahoo flaky -> retry; (2) casamento de nome fragil -> token-set + _norm corrigido; (3) map pequeno -> override deterministico ampliado.

Purpose: hoje tickers 100% validos (ELET3, JBSS3, AGRO3, SBSP3...) falham por rate-limit intermitente do Yahoo, por nome legal escrito diferente do Yahoo, e por um bug no _norm que apaga " sa" no meio de palavras reais ("Saneamento" -> "neamento").
Output: prices.py com retry, universe.py com _norm corrigido + fallback token-set aditivo, ticker_map.json ampliado, e suite offline tests/test_ingest_resolucao.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

@src/analista/ingest/prices.py
@src/analista/ingest/universe.py
@data/ticker_map.json
@tests/test_comparables.py

<interfaces>
Contratos que o executor usa diretamente. Nao precisa explorar o codebase.

De src/analista/ingest/prices.py:
- def yahoo_symbol(ticker) -> str
- def coletar_mercado(ticker, meses_beta=60) -> DadosMercado   # NAO mudar assinatura nem retorno
- dataclass DadosMercado(ticker, preco_atual, num_acoes, setor, nome, volume_financeiro_diario, beta, desempenho_relativo_6m, dividendos_por_ano, dpa_trailing_12m, ano_dpa)
- _yf() -> import tardio de yfinance; yf.Ticker(sym).info / .history(...) / .dividends

De src/analista/ingest/universe.py:
- def _norm(s) -> str
- @lru_cache carregar_cadastro() -> Optional[DataFrame]   # colunas: DENOM_SOCIAL, CD_CVM (Int64), SETOR_ATIV, SIT (ja filtrado ATIVO)
- def _carregar_override() -> dict
- def resolver(ticker, nome_yahoo="") -> Tuple[Optional[int], str]
- Ordem atual em resolver: override -> (cad/nome guard) -> exato (_n == alvo) -> contem (x in alvo or alvo in x).
  NAO alterar precedencia; token-set entra SO depois que contem falha.

lookups_verificados (CD_CVM verificados por lookup em data/cvm/cad_cia_aberta.csv, SIT==ATIVO; USE EXATAMENTE estes):
- SBSP3 = 14443  (CIA SANEAMENTO BASICO ESTADO SAO PAULO)
- JBSS3 = 20575  (JBS SA)
- AGRO3 = 20036  (BRASILAGRO CIA BRAS DE PROP AGRICOLAS)
- VIVT3 = 17671  (TELEFONICA BRASIL S.A.)
- TIMS3 = 24929  (TIM S.A.)
- CSMG3 = 19445  (COMPANHIA DE SANEAMENTO DE MINAS GERAIS)
- BBSE3 = 23159  (BB SEGURIDADE PARTICIPACOES S.A.)
- CXSE3 = 23795  (CAIXA SEGURIDADE PARTICIPACOES S.A.)

CD_CVM de regressao (para o teste de nao-regressao, ja resolvem hoje via exato/contem):
- TOTS3 -> 19992 (TOTVS S.A) ; CSAN3 -> 19836 (COSAN S.A.) ; ENGI11 -> 15253 (ENERGISA SA) ; TIMS3 -> 24929 (TIM S.A.)

OMITIDOS (sem CD_CVM ATIVO verificavel no cadastro em cache -- NAO inventar):
- ELET3 / ELET6 -> "Centrais Eletricas Brasileiras / Eletrobras" nao consta como ATIVO no cad_cia_aberta.csv
  cacheado (so existe ELETROPAR cancelada, CD 15784). Documentar a omissao no SUMMARY; o retry (FIX 1) e o
  token-set (FIX 2) ja mitigam parte do problema desses tickers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retry de fetch no coletar_mercado (FIX 1)</name>
  <files>src/analista/ingest/prices.py, tests/test_ingest_resolucao.py</files>
  <behavior>
    - fetch falha 1x (excecao) depois sucesso -> coletar_mercado retorna DadosMercado com nome/preco preenchidos.
    - fetch retorna info vazio/sem nome/sem preco 1x depois sucesso -> idem.
    - todas N tentativas falham -> retorna DadosMercado "vazio" (sem excecao propagada), igual a desistencia atual.
    - sem rede em nenhum teste (fetch e mockado).
  </behavior>
  <action>
    Extrair a obtencao do info do Yahoo para uma funcao interna pequena e mockavel, ex. _fetch_info(tk) -> dict
    que faz "tk.info or {}" mantendo o try/except Exception -> {} atual. Em coletar_mercado, envolver a obtencao do
    info num laco de retry: 3 tentativas (constante nomeada, ex. _MAX_TENTATIVAS = 3), com backoff curto crescente
    (~0.5s, 1.0s; usar time.sleep). Considerar tentativa "falha" quando _fetch_info lanca OU quando o dict resultante
    nao tem nome (longName/shortName) E nao tem preco (currentPrice/regularMarketPrice). Re-tentar enquanto falha e
    ainda ha tentativas; ao esgotar, seguir com info={} (comportamento atual de desistencia, sem levantar excecao).
    Nao tocar no bloco de history/dividends nem na montagem do DadosMercado a jusante. Manter a assinatura
    coletar_mercado(ticker, meses_beta=60) -> DadosMercado e os campos do dataclass inalterados. Comentar apenas o
    porque do retry (rate-limit de IP de datacenter no Yahoo e intermitente).
    Nos testes, monkeypatchar prices._fetch_info com um callable que falha-depois-sucesso (use lista/contador de
    chamadas) e monkeypatchar prices._yf para um stub cujo Ticker(...) devolve um objeto com .history (DataFrame
    vazio) e .dividends (Series/DF vazio), de modo que nenhuma chamada real a rede ocorra. Patchar time.sleep (ou
    monkeypatch) para nao atrasar a suite.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && .venv/bin/python -m pytest tests/test_ingest_resolucao.py -k retry -x -q</automated>
  </verify>
  <done>coletar_mercado re-tenta no fetch flaky e desiste apos 3 tentativas sem excecao; testes de retry passam sem rede; assinatura/retorno inalterados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _norm cirurgico + fallback token-set em resolver (FIX 2)</name>
  <files>src/analista/ingest/universe.py, tests/test_ingest_resolucao.py</files>
  <behavior>
    - _norm("CIA SANEAMENTO BASICO ESTADO SAO PAULO") preserva "saneamento"/"sao" (nao vira "neamento"/"o").
    - _norm("Companhia de Saneamento ... - SABESP") mantem "sabesp" intacto (nao vira "besp").
    - _norm("Itau Unibanco S.A.") remove o sufixo juridico -> "itau unibanco" (sem "s a"/"sa" perdidos no meio).
    - _norm("Energisa S/A") e _norm("X LTDA") removem o token juridico no fim.
    - resolver(nome_yahoo "BrasilAgro - Companhia Brasileira de Propriedades Agricolas") casa CD 20036 via token-set.
    - resolver(nome_yahoo "Companhia de Saneamento Basico do Estado de Sao Paulo - SABESP") casa CD 14443 via
      token-set e NAO casa CSMG (19445) por engano.
    - Nao-regressao: "TOTVS S.A."->19992, "Cosan S.A."->19836, "Energisa S.A."->15253, "TIM S.A."->24929 continuam
      resolvendo para o MESMO CD_CVM (via exato/contem, nao via token-set).
  </behavior>
  <action>
    (i) Corrigir _norm: NAO remover sufixos juridicos via str.replace(" sa", " ") (apaga substrings no meio de
    palavras). Em vez disso, normalizar (NFKD/ascii/lower) e remover os sufixos juridicos como TOKENS em fronteira de
    palavra com re.sub usando \b para os tokens "s.a." / "s/a" / "sa" / "s a" / "ltda"; manter a remocao atual dos
    conectivos genericos ("holding","participacoes","do brasil","brasil","companhia","cia","energia","banco") tambem
    via fronteira de palavra para nao corromper. Garantir que "saneamento","sao","sabesp" sobrevivem. Retornar tokens
    colapsados por espaco como hoje.
    (ii) Antes de comparar, tratar marca separada por " - ": se o nome contem " - ", considerar tanto o nome completo
    quanto os segmentos para o casamento (ex.: "BrasilAgro - Companhia..." e "Companhia... - SABESP").
    (iii) ADITIVO em resolver: manter intactos o caminho override, o guard de cad/nome, o match exato (_n == alvo) e o
    "contem". SO quando exato E contem falham, aplicar fallback token-set: tokenizar alvo e cada _n, descartar tokens
    curtos (len<=2), e escolher a empresa CVM com maior sobreposicao de tokens significativos (Jaccard OU subset dos
    tokens do alvo). Exigir limiar de seguranca (ex.: Jaccard >= ~0.5 OU todos os tokens significativos do nome-CVM
    contidos no alvo com >= 2 tokens em comum) para evitar falso-positivo; se nada passa o limiar, retornar (None, "")
    como hoje. Em empate, escolher o nome CVM mais curto (menos tokens). O caminho exato/contem de quem ja casa NAO
    pode mudar de resultado.
    Nos testes, montar um DataFrame de cadastro SINTETICO pequeno (colunas DENOM_SOCIAL, CD_CVM, SETOR_ATIV, SIT) com
    as linhas reais relevantes (14443, 20036, 19445, 17671, 19992, 19836, 15253, 24929) e monkeypatchar
    universe.carregar_cadastro para devolve-lo e universe._carregar_override para retornar {}, testando o casamento por
    nome sem rede e sem o override mascarar o resultado. Limpar cache se necessario
    (carregar_cadastro.cache_clear()). Comentar so o porque do token-set (nomes legais CVM diferem do longName Yahoo).
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && .venv/bin/python -m pytest tests/test_ingest_resolucao.py -k "norm or tokenset or regress" -x -q</automated>
  </verify>
  <done>_norm preserva palavras reais e remove so sufixo juridico; token-set casa AGRO3/SBSP3 sem falso-positivo; nao-regressao (TOTVS/Cosan/Energisa/TIM -> mesmo CD_CVM) passa; precedencia override/exato/contem inalterada.</done>
</task>

<task type="auto">
  <name>Task 3: Ampliar ticker_map.json com entradas verificadas (FIX 3)</name>
  <files>data/ticker_map.json</files>
  <action>
    Adicionar ao override APENAS as entradas com CD_CVM verificado por lookup no cadastro em cache (ver
    lookups_verificados no contexto): SBSP3=14443, JBSS3=20575, AGRO3=20036, VIVT3=17671, TIMS3=24929, CSMG3=19445,
    BBSE3=23159, CXSE3=23795. NAO adicionar ELET3/ELET6 (sem CD_CVM ATIVO verificavel -- documentar a omissao no
    SUMMARY). Manter exatamente o estilo/identacao atual (2 espacos, agrupamento por bloco com linhas em branco),
    inserindo as novas chaves em blocos coerentes (saneamento/utilities junto, seguradoras junto etc.). Nao remover
    nem reordenar entradas existentes. Garantir JSON valido (sem virgula sobrando).
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && .venv/bin/python -c "import json; m=json.load(open('data/ticker_map.json')); assert m['SBSP3']==14443 and m['TIMS3']==24929 and m['CXSE3']==23795 and 'ELET3' not in m; print('ok', len(m))"</automated>
  </verify>
  <done>ticker_map.json contem as 8 entradas verificadas, NAO contem ELET3/ELET6, e e JSON valido; entradas existentes preservadas.</done>
</task>

</tasks>

<verification>
Suite completa offline (golden tests existentes NAO podem quebrar):

```
cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && .venv/bin/python -m pytest -q
```

Nenhum teste pode bater na rede. Os novos testes em tests/test_ingest_resolucao.py cobrem retry (mock), _norm,
token-set e nao-regressao usando cadastro sintetico via monkeypatch.
</verification>

<success_criteria>
- coletar_mercado re-tenta o fetch flaky do Yahoo (3 tentativas, backoff curto) e desiste sem excecao; assinatura/retorno inalterados.
- _norm preserva "Saneamento"/"Sao"/"SABESP" e remove sufixo juridico (S.A./S/A/SA/LTDA) so em fronteira de palavra.
- resolver casa AGRO3 (20036) e SBSP3 (14443) via token-set sem falso-positivo, e mantem exato/contem inalterados.
- Nao-regressao: TOTVS/Cosan/Energisa/TIM resolvem para o MESMO CD_CVM.
- ticker_map.json ganha 8 entradas verificadas, sem ELET3/ELET6, JSON valido.
- `pytest -q` passa inteiro (golden + novos) sem rede.
</success_criteria>

<output>
After completion, create `.planning/quick/260622-cg9-robustez-da-resolucao-de-tickers-retry-y/260622-cg9-SUMMARY.md`.
Document the ELET3/ELET6 omission (no verifiable ATIVO CD_CVM in the cached cadastro).
</output>
