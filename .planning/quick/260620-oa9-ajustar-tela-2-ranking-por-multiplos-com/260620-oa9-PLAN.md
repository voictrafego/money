---
phase: quick-260620-oa9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/core/comparables.py
  - app.py
autonomous: false
requirements: [RANK-CONF-01, RANK-CONF-02, RANK-CONF-03]
must_haves:
  truths:
    - "Quando a regressão tem poucas empresas (n baixo), a Tela 2 mostra um aviso de baixa confiabilidade"
    - "Quando o coeficiente do ROE sai negativo (contraria a teoria de Gordon), a Tela 2 avisa que o veredito é pouco confiável"
    - "A Tela 2 orienta o usuário a usar empresas do mesmo setor/segmento"
    - "Os testes golden de tests/test_comparables.py continuam passando sem alteração"
  artifacts:
    - path: "src/analista/core/comparables.py"
      provides: "Propriedades de diagnóstico em RegressaoPL (amostra_pequena, roe_sinal_invertido)"
      contains: "def amostra_pequena"
    - path: "app.py"
      provides: "Avisos de confiabilidade na Tela 2 (Ranking)"
      contains: "amostra_pequena"
  key_links:
    - from: "app.py (Tela 2, após exibir a regressão ~linha 309)"
      to: "RegressaoPL.amostra_pequena / RegressaoPL.roe_sinal_invertido"
      via: "leitura das propriedades reg.*"
      pattern: "reg\\.(amostra_pequena|roe_sinal_invertido)"
---

<objective>
Adicionar transparência/avisos na Tela 2 (Ranking por múltiplos + preço-alvo, Cap. 11-12) do
Analista de Dividendos, para que o usuário entenda quando o veredito da regressão
P/L = f(payout, ROE) é estatisticamente frágil — caso TAEE11 (regressão diz "Cara", DDM diz
"Subavaliada"). Tudo via AVISOS aditivos, sem mudar o método de cálculo.

Purpose: cumprir o Core Value do projeto — "a mesma ação não pode parecer barata num menu e
cara em outro sem explicação". A explicação vem por avisos visíveis na UI, não por mudar a engine.

Output:
- Propriedades de diagnóstico em `RegressaoPL` (amostra pequena, coeficiente do ROE invertido).
- Avisos na Tela 2: (1) amostra pequena, (2) ROE com sinal invertido, (3) orientação de mesmo setor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Contratos que o executor consome. Extraídos do código. Sem necessidade de explorar. -->

De src/analista/core/comparables.py (estado atual):
```python
@dataclass
class RegressaoPL:
    coeficientes: np.ndarray   # [intercepto, b_DP, b_ROE]  -> coeficientes[2] = b_ROE
    r2: float
    n: int

    def prever(self, dp: float, roe: float) -> float: ...

def ajustar_regressao_pl(pl, dp, roe) -> Optional[RegressaoPL]:
    # ... retorna RegressaoPL(coeficientes=coef, r2=r2, n=len(linhas))
    # aceita a partir de 4 observações (len(linhas) < 4 -> None)
```

RegressaoPL é construído POSICIONALMENTE em 5 lugares (4 nos testes golden + 1 na engine),
sempre com exatamente (coeficientes, r2, n). REGRA: não adicionar campos posicionais ao
dataclass — usar @property (campos derivados) para não quebrar nenhuma construção existente.

De app.py — Tela 2 (Ranking), trecho relevante (~245-313):
```python
reg = cmp.ajustar_regressao_pl(PL, DP, ROE)
# ... monta tabela ...
if reg:
    st.caption(f"Regressão: P/L = {reg.coeficientes[0]:.2f} + {reg.coeficientes[1]:.2f}·payout "
               f"+ {reg.coeficientes[2]:.2f}·ROE  (R²={reg.r2:.2f}, n={reg.n})")
else:
    st.info("Poucas empresas para a regressão (precisa de ≥4). Os preços-alvo ficam indisponíveis.")
```
O text_area já diz "Tickers (de preferência do mesmo setor)" e o caption inicial existe.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expor flags de diagnóstico em RegressaoPL</name>
  <files>src/analista/core/comparables.py, tests/test_comparables.py</files>
  <behavior>
    - amostra_pequena: True quando n < LIMIAR_AMOSTRA (definir LIMIAR_AMOSTRA = 10 como
      constante de módulo). RegressaoPL(..., n=6) -> amostra_pequena is True;
      RegressaoPL(..., n=10) -> amostra_pequena is False.
    - roe_sinal_invertido: True quando coeficientes[2] (b_ROE) < 0. A teoria de Gordon diz que o
      P/L justo cresce com o ROE, então b_ROE deve ser positivo; negativo = overfitting/
      multicolinearidade. coeficientes=[5, 10, -66.72] -> True; coeficientes=[5, 10, 20] -> False;
      coeficientes=[5, 10, 0.0] -> False (zero não é invertido).
    - Construção posicional existente (coeficientes, r2, n) continua funcionando — propriedades
      não são campos do dataclass.
  </behavior>
  <action>
    Em src/analista/core/comparables.py, adicionar uma constante de módulo LIMIAR_AMOSTRA = 10
    (perto da definição de RegressaoPL, com comentário do "porquê": abaixo disso, 3 parâmetros
    sobre poucas observações deixam a regressão instável). Adicionar duas @property em
    RegressaoPL: `amostra_pequena` (retorna self.n < LIMIAR_AMOSTRA) e `roe_sinal_invertido`
    (retorna float(self.coeficientes[2]) < 0). NÃO adicionar campos ao dataclass nem alterar
    ajustar_regressao_pl, prever, PrecoAlvo, preco_alvo_por_regressao ou o limiar de 4 observações.
    Em tests/test_comparables.py, ADICIONAR (não alterar testes existentes) dois testes:
    um para amostra_pequena (n=6 True, n=10 False) e um para roe_sinal_invertido
    (b_ROE negativo True, positivo False, zero False), usando o construtor posicional já testado.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && python -m pytest tests/test_comparables.py -x -q</automated>
  </verify>
  <done>
    Os 9 testes golden existentes de test_comparables.py passam sem modificação; os 2 testes novos
    de amostra_pequena e roe_sinal_invertido passam; RegressaoPL.amostra_pequena e
    RegressaoPL.roe_sinal_invertido existem como propriedades.
  </done>
</task>

<task type="auto">
  <name>Task 2: Avisos de confiabilidade na Tela 2 (Ranking)</name>
  <files>app.py</files>
  <action>
    Em app.py, na Tela 2 (Ranking), logo após o st.caption da fórmula da regressão
    (o bloco `if reg:` ~linha 309-311), ADICIONAR os avisos abaixo (sem remover nada do existente):

    (1) RANK-CONF-01 amostra pequena — se `reg.amostra_pequena`: st.warning explicando, em PT-BR
    no tom do app, que com poucas empresas (mostrar reg.n) a regressão é instável e o veredito
    de "Subavaliada/Cara" é pouco confiável; recomendar adicionar mais comparáveis do mesmo setor.

    (2) RANK-CONF-02 ROE invertido — se `reg.roe_sinal_invertido`: st.warning explicando que o
    coeficiente do ROE saiu negativo, o que CONTRARIA a teoria (modelo de Gordon: P/L justo cresce
    com o ROE). Isso indica overfitting/multicolinearidade e penaliza empresas mais rentáveis;
    logo o preço-alvo da regressão pode discordar do "Analisar a fundo" (DDM) — citar que, nesse
    caso, o DDM tende a ser mais confiável. Este aviso é o que liga o caso TAEE11 ao Core Value.

    (3) RANK-CONF-03 peer group — st.caption/st.info fixo (sempre que houver tabela) orientando o
    usuário a comparar empresas do MESMO segmento (ex.: geração vs transmissão vs distribuição de
    energia), pois misturar segmentos distorce a regressão. Reforça a dica que já existe no
    text_area. Não detectar segmento automaticamente — apenas texto.

    Usar emojis/ícones no mesmo padrão dos avisos já presentes na Tela 1 (⚠️). Não tocar na
    montagem da tabela, na coleta de dados, nem no ramo `else` (poucas empresas <4).
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && python -c "import ast; ast.parse(open('app.py').read()); print('OK')" && grep -q "amostra_pequena" app.py && grep -q "roe_sinal_invertido" app.py && echo GATES_OK</automated>
  </verify>
  <done>
    app.py compila (ast.parse OK); a Tela 2 lê reg.amostra_pequena e reg.roe_sinal_invertido e
    emite os 3 avisos (amostra pequena, ROE invertido, mesmo setor); nenhum cálculo ou ramo
    existente foi alterado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Avisos de confiabilidade na Tela 2 (Ranking): amostra pequena, coeficiente do ROE invertido
    e orientação de mesmo setor — implementados como flags em RegressaoPL + avisos na UI.
  </what-built>
  <how-to-verify>
    1. Rode o app: `streamlit run app.py` (na raiz do projeto).
    2. Vá em "Ranking por múltiplos + preço-alvo" e clique "Rankear" com a lista padrão
       (TAEE11, EGIE3, CMIG4, ALUP11, CPFE3, EQTL3 — n=6, amostra pequena).
    3. Confirme que aparece: (a) aviso de amostra pequena (n baixo); (b) se o coeficiente do ROE
       na linha "Regressão: ..." estiver negativo, o aviso de ROE invertido explicando a
       divergência com o "Analisar a fundo" (DDM); (c) a orientação de usar o mesmo setor.
    4. Confira que a tabela, os preços-alvo e a fórmula da regressão continuam iguais a antes
       (nada do método mudou).
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva ajustes nos textos/avisos</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -q` passa por inteiro (golden intactos).
- `python -c "import ast; ast.parse(open('app.py').read())"` sem erros.
- Avisos visíveis na Tela 2 para os 3 cenários, sem alterar tabela/preços-alvo.
</verification>

<success_criteria>
- RegressaoPL expõe amostra_pequena e roe_sinal_invertido como propriedades (sem novos campos posicionais).
- Tela 2 avisa: amostra pequena, ROE com sinal invertido (ligando ao Core Value / caso TAEE11), e orientação de mesmo setor.
- Todos os testes golden continuam passando.
- Nenhuma fórmula de valuation (DDM, screening, fundamentals, ingest) nem o cálculo da regressão foi alterado.
</success_criteria>

<output>
After completion, create `.planning/quick/260620-oa9-ajustar-tela-2-ranking-por-multiplos-com/260620-oa9-SUMMARY.md`
</output>
