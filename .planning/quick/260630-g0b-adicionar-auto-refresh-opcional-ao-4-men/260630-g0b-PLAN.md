---
phase: quick-260630-g0b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app.py]
autonomous: true
requirements: [SWING-REFRESH-01]
must_haves:
  truths:
    - "Na aba Swing trade existe um toggle 'Atualização automática' (default OFF) e um selectbox de intervalo (30s / 1min / 5min)."
    - "Com o toggle ligado num timeframe intraday (5m/30m/1h), o bloco do gráfico re-roda sozinho no intervalo escolhido, sem recarregar a página nem re-renderizar as abas Analisar/Garimpar/Ranking."
    - "No timeframe 'Diário' o toggle de auto-refresh fica desabilitado e o app exibe um aviso de que a atualização automática é desnecessária."
    - "O auto-refresh NÃO incrementa o nonce do par (ticker, timeframe) — o único ponto que faz `+= 1` continua sendo o botão Atualizar; o TTL=300s do cache `frame_intraday` segue como porteiro do Yahoo."
    - "O selo de atraso (~15min) e o card de veredito permanecem visíveis e coerentes com os candles re-buscados."
    - "Os 283 testes golden seguem verdes e nenhuma dependência nova é adicionada."
  artifacts:
    - path: "app.py"
      provides: "Auto-refresh opcional via st.fragment(run_every=...) no 4º menu (Swing trade)"
      contains: "st.fragment(run_every="
  key_links:
    - from: "toggle 'Atualização automática' + selectbox de intervalo (fora do fragment)"
      to: "decorador @st.fragment(run_every=run_every)"
      via: "run_every = intervalo_seg if (auto_on and tf_key != 'diario') else None"
      pattern: "st\\.fragment\\(run_every="
    - from: "fragment de render do swing"
      to: "frame_intraday(ticker, tf_key, st.session_state[k])"
      via: "fetch read-only com o nonce ATUAL (sem incrementar)"
      pattern: "frame_intraday\\(ticker, tf_key"
---

<objective>
Adicionar auto-refresh OPCIONAL ao 4º menu (Swing trade) do `app.py` usando `st.fragment(run_every=...)` (nativo no Streamlit 1.58.0, zero dependência nova).

Purpose: Permitir que o usuário acompanhe o candlestick intraday "ao vivo" (best-effort) sem ficar clicando em Atualizar e sem recarregar a página inteira — mantendo o custo-zero e o respeito ao rate-limit do Yahoo via o cache TTL=300s já existente.

Output: Um toggle "Atualização automática" (default OFF) + selectbox de intervalo (30s / 1min / 5min) na aba Swing trade; o bloco de render do gráfico (fetch + figura + selo de atraso + card de veredito) embrulhado num fragment que re-roda sozinho no intervalo escolhido, isolado das outras abas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Pontos exatos do app.py que o executor vai mexer. Já lidos — NÃO re-explorar. -->

Wrapper de cache (NÃO alterar — só consumir), app.py ~linha 54:
  @st.cache_data(show_spinner=False, ttl=300)
  def frame_intraday(ticker: str, timeframe: str, nonce: int) -> ...
  # nonce entra SÓ na chave; TTL=300s = porteiro do Yahoo (D-08). NÃO incrementar no auto-refresh.

Chave de nonce por par (NÃO alterar), app.py ~linha 67:
  def _nonce_key(ticker: str, timeframe: str) -> str

Bloco do 4º menu, app.py linhas 585–879 (`elif modo.startswith("📈"):`):
  - 592–598: col1/col2/col3 — text_input ticker, selectbox Timeframe (_TF_MAP), tf_key.
    _TF_MAP = {"Diário": "diario", "1h": "1h", "30m": "30m", "5m": "5m"}  (linha 596)
  - 600–606: nonce setdefault(k, 0) + botão "Atualizar" que faz `st.session_state[k] += 1`
    (ÚNICO ponto que pode incrementar o nonce — preservar essa exclusividade).
  - 610–612: `if ticker:` → `with st.spinner(...): f = frame_intraday(ticker, tf_key, st.session_state[k])`
  - 614–624: `if f.disponivel is False:` → st.error com _MSG_MOTIVO.
  - 625–879: `else:` → engine read-only (indicators.calcular + setup.montar_setup), estado
    isolado `tec_estado_swing`, `grafico_box = st.container()` (slot ANTES do expander),
    expander "⚙️ Overlays", figura make_subplots em grafico_box, selo de atraso (803–805),
    warning de histórico (808–809) e card de veredito (811–879).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-refresh opcional do bloco Swing trade via st.fragment(run_every=...)</name>
  <files>app.py</files>
  <action>
No 4º menu (`elif modo.startswith("📈"):`, a partir da ~linha 585), adicionar auto-refresh opcional ao bloco do gráfico SEM tocar Analisar/Garimpar/Ranking nem a engine. Duas mudanças, ambas dentro deste bloco:

(1) CONTROLES (FORA do fragment), logo após o bloco do botão "Atualizar" (depois da ~linha 606, ainda antes do `if ticker:`): introduzir um mapa rótulo→segundos `_INTERVALOS = {"30 segundos": 30, "1 minuto": 60, "5 minutos": 300}`. Renderizar numa linha própria (use st.columns ou os widgets diretos):
  - um `st.toggle("Atualização automática", value=False, disabled=(tf_key == "diario"), help=...)` — guarde em `auto_on`. O `help` deve explicar, em pt-BR não-imperativo, que reusa o cache (Yahoo é consultado no máx. 1×/5min);
  - um `st.selectbox("Intervalo", list(_INTERVALOS), disabled=(not auto_on or tf_key == "diario"))` — guarde o rótulo em `auto_intervalo`;
  - quando `tf_key == "diario"`, exibir um `st.caption("ℹ️ A atualização automática só faz sentido em timeframes intraday (1h/30m/5m); no Diário ela é desnecessária.")`.
  Calcular `run_every = _INTERVALOS[auto_intervalo] if (auto_on and tf_key != "diario") else None`.
  Estes controles FICAM FORA do fragment de propósito: mexer no toggle/intervalo dispara um rerun completo que re-decora o fragment com o novo `run_every` (decisão de design: `run_every` é o "tick visual"; o TTL=300s é o "tick de dados").

(2) FRAGMENT (envolve o render): transformar todo o corpo atual de `if ticker:` (do `with st.spinner(...)` da ~linha 611 até o fim do card de veredito na ~linha 879) numa função aninhada decorada `@st.fragment(run_every=run_every)` definida DENTRO de `if ticker:`, e chamá-la em seguida. Decisão do planner: o fragment engloba fetch + figura + selo de atraso + card de veredito (NÃO só o fetch+figura) — assim um snapshot re-buscado mantém candle, selo e veredito coerentes entre si. A função captura `ticker`, `tf_key`, `tf_label`, `k`, `CFG`, `run_every` por closure e lê o estado isolado `tec_estado_swing` / o nonce via `st.session_state[k]` como hoje. NÃO repassar `run_every` à engine. Preservar EXATAMENTE: o spinner, o branch `if f.disponivel is False`, a cadeia read-only `indicators.calcular(...)` + `setup.montar_setup(...)`, o estado isolado `tec_estado_swing` (defaults D-02), o `grafico_box = st.container()` reservado ANTES do expander, todos os overlays/subpainéis da figura, o selo de atraso (D-08) e o card de veredito (decomposição/checklist/tabela de referências/disclaimer SWING-02).

INVARIANTE CRÍTICA: o auto-refresh NÃO pode incrementar o nonce. O único `st.session_state[k] += 1` continua sendo o do botão "Atualizar" (fora do fragment). O fragment apenas LÊ `st.session_state[k]` e chama `frame_intraday(ticker, tf_key, st.session_state[k])` — o re-fetch no timer é barrado pelo TTL=300s (porteiro do Yahoo, anti rate-limit). Copy 100% pt-BR e não-imperativa (gate SWING-02). Zero dependência nova (`st.fragment` é nativo no 1.58.0). Não alterar `frame_intraday`, `_nonce_key`, `_TF_MAP`, nem os menus 1/2/3.
  </action>
  <verify>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && .venv/bin/python -m pytest -q 2>&1 | tail -3</automated>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && .venv/bin/python -c "import ast; ast.parse(open('app.py').read()); print('syntax ok')"</automated>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && grep -n 'st.fragment(run_every=' app.py && grep -n 'Atualização automática' app.py</automated>
    <automated>cd "/Users/giovanelazari/projects/Analista de Investimentos/analista_dividendos" && test "$(grep -c 'st.session_state\[k\] += 1' app.py)" = "1" && echo "nonce-bump-unico OK"</automated>
  </verify>
  <done>
    - `.venv/bin/python -m pytest -q` → 283 passed (sem regressão; nenhum golden tocado).
    - `app.py` parseia (AST ok) e contém `st.fragment(run_every=` + o rótulo "Atualização automática".
    - Existe exatamente UM `st.session_state[k] += 1` (o do botão Atualizar) — o auto-refresh não bumpa o nonce.
    - `run_every` é `None` quando `auto_on` é False OU `tf_key == "diario"`; no Diário o toggle fica `disabled` e há aviso de que é desnecessário.
    - Selo de atraso e card de veredito permanecem dentro do fragment, visíveis. Menus Analisar/Garimpar/Ranking e a engine inalterados.
    - `requirements.txt` inalterado (zero dependência nova).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.py → Yahoo Finance (via frame_intraday) | Fonte externa de dados intraday; já existente, inalterada nesta mudança. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g0b-01 | Denial (self-DoS / rate-limit) | fragment com run_every re-fetch frequente | mitigate | Auto-refresh NÃO incrementa o nonce; `frame_intraday` mantém `@st.cache_data(ttl=300)` → Yahoo consultado no máx. 1×/5min por par, independentemente do intervalo visual escolhido. |
| T-g0b-02 | Tampering (vazamento de re-render p/ outras abas / engine) | st.fragment isolado | mitigate | Fragment envolve só o bloco swing; controles fora do fragment; nenhum import/chamada à engine fundamentalista nem aos menus 1/2/3 alterado; 283 goldens como gate. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → 283 passed.
- Diff restrito a `app.py`, somente dentro do bloco `elif modo.startswith("📈"):`.
- `git diff --stat` mostra apenas `app.py`; `requirements.txt` intocado.
</verification>

<success_criteria>
- Toggle "Atualização automática" (default OFF) + selectbox de intervalo (30s/1min/5min) presentes na aba Swing trade.
- Com o toggle ON em timeframe intraday, o bloco do gráfico re-roda sozinho no intervalo via `st.fragment(run_every=...)`, sem recarregar a página nem afetar outras abas.
- No "Diário" o toggle fica desabilitado com aviso de que é desnecessário (`run_every=None`).
- O nonce só é incrementado pelo botão Atualizar; TTL=300s segue como porteiro do Yahoo.
- 283 testes golden verdes; zero dependência nova.
</success_criteria>

<output>
After completion, create `.planning/quick/260630-g0b-adicionar-auto-refresh-opcional-ao-4-men/260630-g0b-SUMMARY.md`
</output>
