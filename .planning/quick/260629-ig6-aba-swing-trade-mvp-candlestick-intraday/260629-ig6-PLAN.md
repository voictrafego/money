---
phase: quick-260629-ig6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app.py]
autonomous: true
requirements: [SWING-MVP-UI]

must_haves:
  truths:
    - "A sidebar mostra um 4º modo '📈 Swing trade (análise técnica)' no rádio 'O que você quer fazer?'"
    - "Ao selecionar o modo swing, a página exibe campo Ticker da B3 + seletor de timeframe (Diário/1h/30m/5m) + botão Atualizar"
    - "Com ticker válido, um candlestick Plotly (go.Candlestick) renderiza a partir de f.ohlc NOMINAL"
    - "A última barra é anotada quando f.barra_viva e o atraso (f.atraso_min) é exibido"
    - "Quando f.disponivel is False, uma mensagem amigável em pt-BR aparece conforme f.motivo"
    - "Quando f.idx_ultima_fechada is None, exibe aviso de histórico insuficiente"
    - "O botão Atualizar incrementa o nonce via _nonce_key(ticker, timeframe) em st.session_state — invalidação targetada, nunca clear global"
    - "Os 202 testes golden continuam verdes e a aba fundamentalista permanece intacta"
  artifacts:
    - path: "app.py"
      provides: "Novo bloco elif do modo swing trade (rendering confinado)"
      contains: "modo.startswith(\"📈\")"
  key_links:
    - from: "app.py (bloco swing)"
      to: "frame_intraday(ticker, timeframe, nonce)"
      via: "nonce lido de st.session_state via _nonce_key(ticker, timeframe)"
      pattern: "frame_intraday\\("
    - from: "app.py (bloco swing)"
      to: "f.ohlc"
      via: "go.Candlestick a partir do OHLC nominal"
      pattern: "go\\.Candlestick"
---

<objective>
Adicionar uma 4ª aba "📈 Swing trade (análise técnica)" no app.py como MVP visual,
reusando a engine intraday e o cache que a Fase 12 já entregou.

Purpose: Dar ao usuário uma visão de candlestick intraday/diário de tickers da B3,
preparando o terreno visual para os indicadores das Fases 13-15 — sem reimplementar
ingestão nem cache (já existem e estão testados, contrato congelado).

Output: app.py com um novo bloco elif de rendering confinado, zero mudança na engine
(src/analista/), zero impacto na aba fundamentalista e nos 202 testes golden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Contrato JÁ EXISTENTE — usar diretamente, NÃO explorar nem modificar. -->

De src/analista/ingest/intraday.py (NÃO MODIFICAR):
```python
@dataclass
class FrameOHLC:
    timeframe: str
    ohlc: Optional[pd.DataFrame] = None            # OHLCV NOMINAL — usar p/ o gráfico (D-02)
    ohlc_ajustado: Optional[pd.DataFrame] = None   # split-adjusted (Fase 13, NÃO usar aqui)
    ultima_barra_ts: Optional[pd.Timestamp] = None
    barra_viva: bool = False                        # True = última barra em formação (D-04)
    idx_ultima_fechada: Optional[int] = None        # len-2; None se histórico < 2 barras
    atraso_min: Optional[float] = None              # (agora - ultima_barra_ts) em minutos
    disponivel: bool = False
    motivo: str = "sem_dados"

# Categorias de motivo: "" (ok) | "timeframe_invalido" | "fetch_falhou" | "sem_dados" | "historico_insuficiente"
# Colunas de ohlc: padrão yfinance — "Open", "High", "Low", "Close" (índice = timestamps tz America/Sao_Paulo)
# timeframes válidos (chaves de _PERIODO_POR_TF): "diario", "1h", "30m", "5m"
```

De app.py (JÁ EXISTENTE — Fase 12, linhas 53-69, usar como está):
```python
@st.cache_data(show_spinner=False, ttl=300)
def frame_intraday(ticker: str, timeframe: str, nonce: int):  # nonce só entra na chave de cache
    from analista.ingest import intraday
    return intraday.coletar_intraday(ticker, timeframe)

def _nonce_key(ticker: str, timeframe: str) -> str:
    return f"nonce_intraday::{ticker}::{timeframe}"
```

Estrutura de roteamento atual em app.py:
- linhas 100-104: `modo = st.sidebar.radio("O que você quer fazer?", [3 opções], help=h("menu"))`
- linha 122: `if modo.startswith("🔎"):`   (Analisar)
- linha 423: `elif modo.startswith("⛏️"):`  (Garimpar BSD)
- linha 478: `else:`  (Ranking 📊 — captura tudo que sobra)

Padrão de gráfico Plotly já no app.py (aba Analisar, ~linha 343):
- `import plotly.graph_objects as go` já existe (linha 12)
- render: `st.plotly_chart(fig, width="stretch")`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar modo + página de swing trade (candlestick MVP) em app.py</name>
  <files>app.py</files>
  <action>
Modificação CONFINADA a app.py (camada de rendering). NÃO tocar src/analista/. NÃO
adicionar indicadores/pivôs/médias/padrões (Fases 13-15, fora de escopo). NUNCA chamar
st.cache_data.clear().

1. **Rádio (linhas 100-104):** adicionar como 4ª opção da lista `"📈 Swing trade (análise técnica)"`.
   Manter o `help=h("menu")` inalterado.

2. **Roteamento:** o bloco Ranking hoje é `else:` na linha 478, que captura tudo que sobra.
   Converter esse `else:` em `elif modo.startswith("📊"):` (o subheader/lógica do Ranking
   permanecem byte-a-byte idênticos abaixo dele). Em seguida, ao FINAL do arquivo, anexar um
   novo bloco `elif modo.startswith("📈"):` com a página de swing descrita abaixo. Assim a
   cadeia fica: if 🔎 / elif ⛏️ / elif 📊 / elif 📈 — cada modo explícito, sem fall-through.

3. **Página de swing (novo bloco elif):**
   - `st.subheader(...)` curto descrevendo a leitura técnica de swing.
   - Layout de controles em colunas (espelhar o padrão da aba Analisar, ~linha 124): campo
     `st.text_input("Ticker da B3", value="TAEE11", ...).strip().upper()`; um `st.selectbox`
     de timeframe cujos rótulos pt-BR mapeiam para as chaves da engine via dict
     `{"Diário": "diario", "1h": "1h", "30m": "30m", "5m": "5m"}`; e um `st.button("Atualizar")`.
   - **Invalidação targetada por nonce (NUNCA clear global):** `k = _nonce_key(ticker, tf_key)`;
     `st.session_state.setdefault(k, 0)`; `if st.button("Atualizar"): st.session_state[k] += 1`.
     Passar `st.session_state[k]` como o argumento `nonce` de `frame_intraday`.
   - Gatear pelo ticker preenchido (não pelo retorno efêmero do botão): só coletar quando
     `ticker` é não-vazio. `f = frame_intraday(ticker, tf_key, st.session_state[k])` dentro de
     um `with st.spinner(...)`.
   - **Tratamento de indisponibilidade (D-07 — copy mora na UI, não na engine):** se
     `f.disponivel is False`, mapear `f.motivo` → texto pt-BR claro via um dict e exibir com
     `st.error`/`st.warning`. Cobrir as quatro categorias: `timeframe_invalido` ("Timeframe
     inválido."), `sem_dados` ("Sem candles para esse ticker/timeframe — a Yahoo não retornou
     dados. Confira o ticker."), `fetch_falhou` ("Falha ao buscar os dados (instabilidade na
     fonte). Tente o botão Atualizar."), `historico_insuficiente` (mensagem de histórico curto).
     Usar `.get(f.motivo, <fallback genérico>)` para não quebrar com motivo desconhecido.
   - **Candlestick (D-02 — gráfico usa f.ohlc NOMINAL, não ohlc_ajustado):** quando
     `f.disponivel`, construir `fig = go.Figure(data=[go.Candlestick(x=f.ohlc.index,
     open=f.ohlc["Open"], high=f.ohlc["High"], low=f.ohlc["Low"], close=f.ohlc["Close"])])`,
     `fig.update_layout(...)` (altura/margens no estilo da aba Analisar, `xaxis_rangeslider_visible`
     a seu critério) e renderizar com `st.plotly_chart(fig, width="stretch")`.
   - **Barra viva (D-04):** quando `f.barra_viva`, anotar/destacar a última barra (ex.:
     `fig.add_vline`/`add_annotation` na posição `f.ultima_barra_ts`, ou um `st.caption`) e
     exibir o atraso a partir de `f.atraso_min` (ex.: `st.caption(f"... atraso ~{f.atraso_min:.0f} min ...")`).
   - **Histórico insuficiente:** quando `f.idx_ultima_fechada is None` (mesmo com
     `f.disponivel is True`), exibir `st.warning("Histórico insuficiente ...")` além do gráfico.

A aba fundamentalista (Analisar/Garimpar/Ranking) e seus caches (montar/selic_atual/rf_capm)
devem permanecer 100% intactos. Comentários apenas onde o "porquê" não for óbvio (CLAUDE.md).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && .venv/bin/python -m py_compile app.py && echo PYCOMPILE_OK</automated>
    <automated>grep -v '^[[:space:]]*#' app.py | grep -c 'cache_data\.clear' | grep -qx 0 && echo NO_GLOBAL_CLEAR_OK</automated>
    <automated>grep -q 'modo.startswith("📈")' app.py && echo NOVO_MODO_OK</automated>
    <automated>grep -q '📈 Swing trade (análise técnica)' app.py && echo OPCAO_RADIO_OK</automated>
    <automated>grep -q 'frame_intraday(' app.py && grep -q '_nonce_key(' app.py && echo CACHE_WRAPPER_OK</automated>
    <automated>grep -q 'go.Candlestick' app.py && echo CANDLESTICK_OK</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && .venv/bin/python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <done>
app.py compila (py_compile exit 0); a contagem de `cache_data.clear` em linhas não-comentadas
é 0; existe a 4ª opção do rádio e o ramo `elif modo.startswith("📈")`; o bloco swing chama
`frame_intraday` com nonce de `_nonce_key`/session_state e renderiza `go.Candlestick` de
`f.ohlc`; trata `f.disponivel is False` por motivo e `f.idx_ultima_fechada is None`; e a suíte
`pytest -q` reporta 202 passados.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m py_compile app.py` → exit 0
- `grep -v '^[[:space:]]*#' app.py | grep -c 'cache_data\.clear'` → 0
- `.venv/bin/python -m pytest -q` → 202 verdes
- greps confirmam: novo modo no rádio, ramo `elif modo.startswith("📈")`, chamada a
  `frame_intraday`/`_nonce_key`, e `go.Candlestick`
- Sanidade manual (opcional, não-bloqueante): rodar o app e selecionar o modo swing com um
  ticker válido (ex.: PETR4) renderiza o candlestick; ticker inválido mostra mensagem amigável.
</verification>

<success_criteria>
- A sidebar oferece 4 modos; o swing renderiza candlestick nominal do timeframe escolhido.
- Botão Atualizar invalida só o par (ticker, timeframe) via nonce — cache fundamentalista intacto.
- Mensagens amigáveis pt-BR para cada motivo de indisponibilidade e para histórico insuficiente.
- Engine (src/analista/) não modificada; 202 testes golden continuam verdes.
</success_criteria>

<output>
Após completar, criar `.planning/quick/260629-ig6-aba-swing-trade-mvp-candlestick-intraday/260629-ig6-SUMMARY.md`
</output>
