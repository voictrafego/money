---
phase: quick-260712-p6r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analista/core/freio.py
  - src/analista/cli.py
  - app.py
  - tests/test_ranking_freio.py
autonomous: true
requirements: [ARQ-01, ENG-01, ENS-01, SAN-01, VER-01]
must_haves:
  truths:
    - "O Ranking do Streamlit não estampa mais 'Cara'/'Subavaliada' para uma ação motor_pendente (ex.: ITUB4): reetiqueta/suprime com o motivo do freio, igual ao CLI cmd_rank."
    - "Os helpers do freio vivem em UM módulo compartilhado consumido por cli.py E app.py — fonte única de paridade (mesmo objeto/função)."
    - "test_ranking_freio.py segue verde sem edição obrigatória (import `from analista.cli import ...` continua resolvendo via re-export)."
    - "cli.py cmd_rank não rotula mais o mid do ensemble como 'DDM absoluto' — reflete 'ensemble motor×DDM' (ENS-01)."
    - "Suíte completa verde (>=435) e firewall selo↛report intacto (selo.py NÃO importa report)."
  artifacts:
    - path: "src/analista/core/freio.py"
      provides: "motor_pendente(c, cfg) + alvo_regressao_confiavel(reg, pa, motor_pendente) — freio puro compartilhado"
      contains: "def alvo_regressao_confiavel"
    - path: "app.py"
      provides: "Aba Ranking por múltiplos aplicando o freio antes de estampar veredito/preço-alvo"
    - path: "src/analista/cli.py"
      provides: "cmd_rank importando o freio do módulo compartilhado + label ENS-01 corrigido"
  key_links:
    - from: "app.py"
      to: "analista.core.freio"
      via: "import + chamada por empresa no loop do Ranking"
      pattern: "freio\\.(motor_pendente|alvo_regressao_confiavel)"
    - from: "src/analista/cli.py"
      to: "analista.core.freio"
      via: "re-export (from .core.freio import ...)"
      pattern: "from \\.core\\.freio import"
---

<objective>
Fechar o BLOCKER da auditoria v2.2: a aba "Ranking por múltiplos" do Streamlit (app.py:1518-1620)
crava "Cara"/"Subavaliada" a partir da regressão P/L de lente única, sem passar pelo freio de
arquétipo/fragilidade que o CLI `cmd_rank` já aplica. Resultado: a MESMA ação (ITUB4) aparece
"Cara" no Ranking e protegida ("DDM conservador demais — ver motor primário") no Analisar,
violando o Core Value ("a mesma ação não pode parecer cara num menu e protegida em outro").

Além disso, corrigir o label drift ENS-01 em `cli.py cmd_rank`: o mid `(a.vmin+a.vmax)/2` é a
banda do ensemble motor×DDM (desde a Fase 3), não "DDM absoluto".

Purpose: paridade CLI↔Streamlit no freio do Ranking, reusando os helpers JÁ testados — sem nova
lógica de valuation.
Output: módulo compartilhado `core/freio.py`, aba Ranking do app com o freio, label ENS-01 honesto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/v2.2-MILESTONE-AUDIT.md

# Superfícies e helpers relevantes
@src/analista/cli.py
@app.py
@src/analista/core/comparables.py

<interfaces>
<!-- Contratos que o executor consome. Extraídos do código. Não explorar o repositório. -->

Freio canônico (HOJE em src/analista/cli.py — SERÁ MOVIDO para core/freio.py):

```python
# cli.py:45-56  — classifica o negócio e consulta o registry; True = motor do arquétipo != "ddm"
def _motor_pendente(c, cfg: dict) -> bool:
    arq = arquetipo.classificar(c, cfg)
    return arquetipo.ARQUETIPO_MOTOR.get(arq.chave) != "ddm"

# cli.py:59-87  — puro/testável; NÃO toca a NOTA do ranque, só governa a coluna de alvo/upside.
# Devolve (confiavel: bool, motivo: Optional[str]). Suprime em 4 condições:
#   pa is None -> (False, None); reg.r2_baixo/amostra_pequena; upside <= LIMIAR_UPSIDE_ABSURDO; motor_pendente.
def alvo_regressao_confiavel(reg, pa, motor_pendente: bool) -> tuple: ...
```

Dependências (todas em core, sem tocar selo/report):
- `analista.core.arquetipo`: `classificar(c, cfg) -> ResultadoArquetipo(.chave)`, `ARQUETIPO_MOTOR: dict`
- `analista.core.comparables` (cmp): `LIMIAR_UPSIDE_ABSURDO = -0.90`, `RegressaoPL(.r2_baixo, .amostra_pequena)`,
  `PrecoAlvo(.upside, .preco_alvo, .subavaliada, .payout_fora_faixa)`, `preco_alvo_por_regressao(...)`

Teste existente que trava o import atual (NÃO pode quebrar):
- tests/test_ranking_freio.py:21 → `from analista.cli import _motor_pendente, alvo_regressao_confiavel`

app.py já importa: `from analista.core import comparables as cmp` etc. NÃO importa cli hoje.
A aba Ranking (app.py:1560-1587) monta `rows` com veredito = "Subavaliada"/"Cara" a partir de
`pa.subavaliada`, sem freio. O estado "indisponível (ROE/payout ausente)" existe quando `pa is None`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extrair o freio para core/freio.py e re-exportar em cli.py (refator puro, zero mudança de comportamento)</name>
  <files>src/analista/core/freio.py, src/analista/cli.py</files>
  <action>
    Criar `src/analista/core/freio.py` movendo AS DUAS funções do freio de cli.py SEM alterar a
    lógica: `motor_pendente(c, cfg)` (renomear a versão pública — hoje `_motor_pendente`) e
    `alvo_regressao_confiavel(reg, pa, motor_pendente)`. Preservar docstrings integrais (são a
    justificativa do freio: paridade D-04, R²/amostra/upside absurdo/motor_pendente). Imports do
    módulo novo: `from . import arquetipo` e `from . import comparables as cmp` (ambos core; sem
    ciclo — nem arquetipo nem comparables importam freio). NÃO importar report nem selo (mantém o
    firewall e mantém `motor_pendente` offline: só classifica, não roda analisar_acao).

    Em cli.py: remover as definições locais e substituir por
    `from .core.freio import alvo_regressao_confiavel, motor_pendente as _motor_pendente`. O alias
    `_motor_pendente` PRESERVA a superfície de import de tests/test_ranking_freio.py:21 e o uso em
    cmd_rank (linhas ~210). Nenhuma outra mudança em cmd_rank nesta task. Reusar helpers testados,
    não reimplementar (constraint).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./.venv/bin/python -m pytest tests/test_ranking_freio.py -q && ./.venv/bin/python -c "from analista.cli import _motor_pendente, alvo_regressao_confiavel; from analista.core.freio import motor_pendente, alvo_regressao_confiavel as a; assert alvo_regressao_confiavel is a; print('re-export OK')"</automated>
  </verify>
  <done>core/freio.py existe com as 2 funções; cli.py as importa/re-exporta; test_ranking_freio.py verde; `analista.cli.alvo_regressao_confiavel is analista.core.freio.alvo_regressao_confiavel`.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar o freio na aba Ranking do app.py + teste de paridade de superfície</name>
  <files>app.py, tests/test_ranking_freio.py</files>
  <action>
    Em app.py, no bloco `elif modo.startswith("Ranking")` (~1518-1620): importar o freio
    (`from analista.core import freio` no topo do arquivo, junto aos demais imports de analista) e,
    no loop que monta `rows` (~1560-1587), aplicar o freio ANTES de decidir veredito/preço-alvo/upside,
    espelhando cmd_rank (cli.py:224-238). Para cada linha do ranking:
      - `mp = freio.motor_pendente(_c_sel, CFG)` (classifica a empresa já carregada — offline, sem rede);
      - `confiavel, motivo = freio.alvo_regressao_confiavel(reg, pa, mp)`;
      - Se `pa is None` (motivo None): manter o estado atual "indisponível (ROE/payout ausente)" para
        preço-alvo/upside/veredito (dado ausente ≠ freio);
      - Se `not confiavel` com `motivo`: NÃO estampar "Cara"/"Subavaliada". Preço-alvo = "—",
        Upside = "—", Veredito = texto honesto que aponta para o Analisar, incluindo o motivo — ex.:
        f"Ver Analisar a fundo ({motivo})". Isto reetiqueta ITUB4/VALE3 (motor_pendente) e alvos
        frágeis (R² baixo / amostra pequena / upside degenerado) em vez de carimbá-los "Cara".
      - Se `confiavel`: manter o comportamento atual (Subavaliada/Cara + payout ajustado quando
        `pa.payout_fora_faixa`). A NOTA (0-100), o Selo e a régua de config permanecem INTACTOS —
        o freio governa só a coluna de alvo/veredito (paridade com a NOTA intacta do CLI).
    Não tocar selo.py/report.py; não recalcular método (app segue read-only). Os tickers-default
    (utilities reguladas: TAEE11/EGIE3/... → motor_pendente=False) seguem inalterados.

    Em tests/test_ranking_freio.py: adicionar um teste de paridade de superfície provando que app e
    cli compartilham o MESMO freio — importar `from analista.core import freio`, `from analista import cli`
    e asseverar `freio.alvo_regressao_confiavel is cli.alvo_regressao_confiavel` e
    `freio.motor_pendente is cli._motor_pendente` (garante que uma futura divergência entre superfícies
    não passa). Opcional: um caso `motor_pendente=True` + reg sadia + pa válido → `alvo_regressao_confiavel`
    retorna `(False, "motor pendente")`, documentando que a aba Ranking suprime esse caso.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./.venv/bin/python -m pytest tests/test_ranking_freio.py -q && ./.venv/bin/python -c "import ast; ast.parse(open('app.py').read()); print('app.py parse OK')" && grep -n "freio\." app.py</automated>
  </verify>
  <done>Aba Ranking chama freio.motor_pendente + freio.alvo_regressao_confiavel; motor_pendente/reg-frágil deixa de virar "Cara"/"Subavaliada" e vira reetiqueta com motivo apontando ao Analisar; NOTA/Selo intactos; teste de paridade de superfície verde.</done>
</task>

<task type="auto">
  <name>Task 3: Corrigir label drift ENS-01 em cli.py cmd_rank</name>
  <files>src/analista/cli.py</files>
  <action>
    Em cmd_rank (cli.py ~206-247): o mid `(a.vmin+a.vmax)/2` é a banda do ENSEMBLE motor×DDM
    desde a Fase 3 (ENS-01), não "DDM absoluto". Corrigir SEM mudar cálculo:
      - Renomear o dicionário `ddm_mid` → `ensemble_mid` (linhas ~208, 220, 242, 243, 246) — o nome
        atual é o próprio drift;
      - Atualizar o comentário ~216-217 ("2ª lente (DDM absoluto)…") para descrever o mid como a
        banda do ensemble motor×DDM (pós-ENS-01), não DDM puro;
      - Atualizar o texto do aviso ~245-247: trocar "DDM R$ {mid} × regressão" por
        "ensemble motor×DDM R$ {mid} × regressão" e REMOVER a cláusula estagnada
        "reconciliação na Fase 3" (a reconciliação já ocorreu na Fase 3) — manter o "ver Analisar a fundo".
    Nenhum teste assere essas strings de stderr (verificado); a saída numérica não muda.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ! grep -n "ddm_mid\|DDM absoluto\|reconciliação na Fase 3" src/analista/cli.py && ./.venv/bin/python -m pytest -q</automated>
  </verify>
  <done>`ddm_mid`, "DDM absoluto" e "reconciliação na Fase 3" não existem mais em cli.py; o aviso rotula o mid como ensemble motor×DDM; suíte completa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Mudança read-only em UI já autenticada atrás do gate Traefik/Django. Não há novo input do usuário atravessando bordas; o freio consome dados já carregados (montar cacheado) e config local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-p6r-01 | Tampering | firewall selo↛report | accept | core/freio.py NÃO importa selo nem report; verificado por `! grep report src/analista/report/selo.py` permanecer válido (não tocamos selo.py). |
| T-p6r-02 | Information disclosure | aba Ranking (veredito) | mitigate | Reetiqueta honesta com motivo do freio em vez de afirmar "Cara" sobre modelo que não serve ao perfil — fecha exatamente o risco de exibir um veredito enganoso (Core Value). |
</threat_model>

<verification>
- `./.venv/bin/python -m pytest -q` → suíte completa verde (>=435 passed).
- Firewall intacto: `grep -c "import" src/analista/report/selo.py` não introduz report (selo.py não é tocado).
- `analista.cli.alvo_regressao_confiavel is analista.core.freio.alvo_regressao_confiavel` (fonte única).
- app.py: `grep -n "freio\." app.py` mostra as chamadas na aba Ranking; `ast.parse` OK.
- Paridade manual (opcional, com rede): rodar Ranking com ITUB4 no meio de utilities → ITUB4 deixa
  de aparecer "Cara"; aparece reetiqueta apontando ao Analisar.
</verification>

<success_criteria>
- Aba Ranking do Streamlit aplica o MESMO freio do CLI cmd_rank (motor_pendente + fragilidade R²/n +
  upside absurdo), suprimindo/reetiquetando em vez de cravar "Cara"/"Subavaliada".
- Freio vive em core/freio.py, consumido por cli.py (re-export) e app.py — paridade por construção.
- Label drift ENS-01 corrigido em cli.py (mid = ensemble motor×DDM, sem "reconciliação na Fase 3").
- Suíte completa verde; firewall selo↛report preservado; tickers-default (reguladas) inalterados.
</success_criteria>

<output>
After completion, create `.planning/quick/260712-p6r-freio-ranking-streamlit/260712-p6r-SUMMARY.md`
</output>
